# Logic-Variables Registry
*Auto-generated by `npm run logic-vars-docs` — do NOT edit manually.*
*Regenerate after seeding a new logic variable; the `logic-vars-registry.infra.test.ts` drift guard fails CI on staleness.*

The single AI-operator index of every Control-Panel logic variable: default,
bounds, numeric-vs-JSONB, description, and the pipeline scripts that consume it.
Values are operator-tunable at runtime via the Spec 86 Control Panel; the
defaults below are the seed / migration baselines.

- **Numeric vars** (593) live in `scripts/seeds/logic_variables.json` (the parity-tested surface re-exported as `LOGIC_VAR_DEFAULTS` in `src/lib/admin/control-panel.ts`), except the 16 seeded via migrations only (last column notes the migration).
- **JSONB vars** (6) carry non-numeric values in `logic_variables.variable_value_json`; they are migration-seeded (never in the seed JSON — a JSONB value cannot live in the numeric `variable_value` column) and read directly (config-loader passes object JSON through untouched).
- **Consuming scripts** are derived from each script's local `LOGIC_VARS_SCHEMA = z.object({...})` Zod union. A blank cell means no static consumer was found; some consumers read **computed keys** (e.g. `assert-lifecycle-phase-distribution.js` builds `lifecycle_band_${…}` at runtime) invisible to a static scan — those are named in the seed JSON's `CONSUMED by …` annotation, surfaced in the Description.
- **Admin** is the declared `admin` field on each seed key (WF2 "Admin Tunable Coverage" / "ADMIN-1 ratchet to zero"): `group: <label>` means the key renders in `GlobalConfigCard`'s GROUPS under that label; `hidden: <reason>` means it does not render there, with `reason` ∈ `derived | internal | deprecated | migration-only` — the closed enum's transitional `unclassified` marker was RETIRED once programme-backlog item `ADMIN-1` reached 0 (every key now carries a real group or a reviewed hidden reason; declaring `unclassified` is now a structural error, not merely ratcheted). Migration-only / JSONB vars are absent from the seed file, so this column reads "— (governed by GROUPS only)" for them — their admin visibility is unchanged and ungoverned by this declaration.

**Cross-refs:** Spec 40 (`docs/specs/01-pipeline/40_pipeline_system.md`, config-loader / logicVars contract) · Spec 86 (`docs/specs/02-web-admin/86_control_panel.md`, the Control Panel that edits these).

Total: **599** logic variables (593 numeric, 6 JSONB).

---

| Variable | Kind | Default | Bounds | Consumers | Source | Admin | Description |
|----------|------|---------|--------|-----------|--------|-------|-------------|
| `accessory_max_coverage_pct` | numeric | 0.3 | 0.05 – 1 | — | seed | group: Spatial & Massing | Spec 65 §7 (Phase 3) — max share of usable rear-yard area a garage may cover. CONSUMED by enrich-parcels.js (max-build pass). |
| `address_points_download_timeout_ms` | numeric | 60000 | 1000 – 1800000 | — | seed | group: Source Ingestion | Spec 124 Rule 3 (row 3.1, INGESTOR) — AbortController budget for the address-points CSV download. 60000 = the fleet INGESTOR default, adopted from the sibling load_ravines_download_timeout_ms because the pre-conversion loader's downloadFile allowed NO timeout at all (it read none — there is no legacy literal to carry); the value therefore EXTERNALIZES a bound that did not previously exist, and the descriptor literal execution.network.timeout states the same 60000 as the fallback for an un-seeded database (execution.network.timeout_from_config names this variable, so there is ONE source). Raise it when the CKAN edge is slow rather than letting a timeout read as an acquisition FAIL. CONSUMED by load_address_points (scripts/lib/step/acquire.js download, via ctx.config). |
| `address_points_null_address_number_max_pct` | numeric | 0.1 | 0 – 100 | — | seed | group: Source Ingestion | Spec 124 Rule 3 (row 3.1, INGESTOR) — WARN ceiling on the percentage of rows with an EMPTY ADDRESS_NUMBER. More than this means the publisher stripped the column upstream — the same 2026-05-20 strip pattern that emptied parcels.address_number. 0.1 = the pre-externalization literal (the threshold text in scripts/lib/address-points-csv-drift.js:94-96). WARN, never FAIL: a null address_number does not make the address point unloadable (the guarded upsert preserves the existing value), it only makes the point unusable for downstream joins. CONSUMED by load_address_points (scripts/lib/compute/load-address-points.js null_address_number_pct, via checks[].limit_from_config). |
| `address_points_skip_rate_max_pct` | numeric | 5 | 0 – 100 | — | seed | group: Source Ingestion | Spec 124 Rule 3 (row 3.1, INGESTOR) — WARN ceiling on the coordinate/ID skip rate: skipped rows as a percentage of rows_read. A row is skipped when ADDRESS_POINT_ID is not an int, or when neither a parseable `geometry` NOR a numeric LATITUDE/LONGITUDE pair is present. 5 = the pre-externalization literal (loader load-address-points.js:413-418). Never FAIL: a rising skip rate is a CSV-drift signal, not a reason to halt an otherwise-loadable load. CONSUMED by load_address_points (scripts/lib/compute/load-address-points.js skip_rate_pct, via checks[].limit_from_config). |
| `archetype_bundle_confidence` | numeric | 0.55 | — (migration-seeded) | — | migration 182 | — (governed by GROUPS only) | Spec 80 §5.B.5 — confidence for archetype-implied (bundle-prior) trade emissions; below direct tag/rule hits, at/above the 0.50 lead-feed gate so implied trades are real leads |
| `archetype_nofit_residential_warn_pct` | numeric | 25 | 0 – 100 | — | seed | group: Archetype Cost Plausibility | WF2 archetype (Spec 83 §3): WARN when mapper-null residential leads (the mapper was CALLED and returned no line) exceed this share of residential processed. A mapper-quality regression signal — deliberately excludes the non-residential bypass population (mapper never called; counted separately as INFO). |
| `archetype_t1_fsi_max` | numeric | 8 | 1 – 100 | — | seed | group: Archetype Cost Plausibility | WF2 archetype T1 plausibility band ceiling: own_area/lot above this rejects (multi-phase/complex filings, data errors) and falls to T2, counted. Mirrors the comps permit_fsi band. |
| `archetype_t1_fsi_min` | numeric | 0.05 | 0 – 1 | — | seed | group: Archetype Cost Plausibility | WF2 archetype T1 (declared-area) plausibility band floor: own_area/lot below this rejects the declared area (garbage filing) and falls to T2, counted. Mirrors the comps permit_fsi band (Spec 78 P3C). |
| `archetype_t1_total_cap` | numeric | 25000000 | 1000000 – 1000000000 | — | seed | group: Archetype Cost Plausibility | WF2 archetype T1 absolute cap per DWELLING UNIT: T1 price above cap x max(1, dwelling_units_created) falls to T2, counted (Reality-Check: a 17,841 m2 townhouse-complex filing priced $159.9M as a single project; the per-unit divisor keeps legitimate multi-unit complexes while catching single-unit absurdities). |
| `archetype_t2_build_line_cap` | numeric | 20000000 | 100000 – 1000000000 | — | seed | group: Archetype Cost Plausibility | WF2 archetype T2 plausibility cap for BUILD lines (max_build/coa_build) on low-rise residential: above -> fallthrough, counted. |
| `archetype_t2_build_line_min` | numeric | 200000 | 0 – 10000000 | — | seed | group: Archetype Cost Plausibility | WF2 archetype T2 plausibility floor for BUILD lines: a new-build total below this (sliver-parcel envelopes, Reality-Check found 17 rows <$5K) is not a credible build -> fallthrough, counted. |
| `archetype_t2_reno_line_cap` | numeric | 10000000 | 100000 – 1000000000 | — | seed | group: Archetype Cost Plausibility | WF2 archetype T2 plausibility cap for RENO lines (gut/addition/kitchen/bath/basement/underpin/garage/suites) on low-rise residential: a propagated line total above this indicates parcel-side data poison (Reality-Check: live $105M gut scalars from a NULL-lot massing mislink) -> fall to T3-if-priceable else T4, counted per line. Don't-hide: the rejection count is an audit row. |
| `archetype_t3_total_cap` | numeric | 15000000 | 1000000 – 1000000000 | — | seed | group: Archetype Cost Plausibility | WF3 archetype T3 (rate-table) absolute cap per DWELLING UNIT: a T3 price (rate x own area x premium) above cap x max(1, dwelling_units_created) falls to T4, counted (Reality-Check WF3 2026-07-06: T3 shipped uncapped; 19 rows >$20M, max $159.9M, from oversized/mislinked parent-parcel own-area inflation). $15M/unit is a judgment cut on a continuous corrupted-area curve (not a statistical elbow); the per-unit divisor protects ~60 legitimate 8-222-unit developments. CoA has no dwelling_units_created -> effectively flat $15M for CoA (intended). |
| `assert_schema_csv_header_bytes` | numeric | 2048 | 256 – 65536 | — | seed | group: Data Quality Thresholds | Spec 122 §1.2a P4 (Pilot 1 remediation) — the ranged-read window (`Range: bytes=0-N`) assert_schema uses to pull just the HEADER row of the address-points and parcels CSVs instead of the whole multi-hundred-MB file. 2048 = the pre-externalization literal. Raise it if either publisher's header row grows past the window (the check would then FAIL on a truncated first line, not silently pass); it is never a correctness knob, only a how-much-do-we-download one. CONSUMED by assert_schema (scripts/lib/compute/assert-schema.js fetchCsvHeaders, via ctx.config). |
| `assert_schema_geojson_probe_bytes` | numeric | 8192 | 1024 – 1048576 | — | seed | group: Data Quality Thresholds | Spec 122 §1.2a P4 (Pilot 1 remediation) — the ranged-read window (`Range: bytes=0-N`) assert_schema uses to reach the FIRST Feature of the neighbourhoods GeoJSON and read its property keys. 8192 = the pre-externalization literal; it must stay large enough to clear the CRS block plus one full `"properties":{…}` object, or neighbourhood_id_property throws 'Could not find properties' on a healthy file. Larger than the CSV window because a GeoJSON preamble is. CONSUMED by assert_schema (scripts/lib/compute/assert-schema.js fetchGeoJsonPropertyKeys, via ctx.config). |
| `assert_schema_type_sample_rows` | numeric | 20 | 1 – 1000 | — | seed | group: Data Quality Thresholds | Spec 122 §1.2a P4 (Pilot 1 remediation) — how many CKAN DataStore rows the permit_cost_type_sample check pulls (`&limit=N`) to prove EST_CONST_COST still parses as a number. 20 = the pre-externalization literal; raise it when CKAN sentinel density ('DO NOT UPDATE'/'DO NOT DELETE' rows) makes 20 rows too thin to contain a single parseable cost, lower it only to cut CKAN load. Bounds are CKAN's own: below 1 the check samples nothing and reads green-by-blindness, above 1000 the DataStore page is slower than the whole step's budget. CONSUMED by assert_schema (scripts/lib/compute/assert-schema.js validateTypeSample, via ctx.config). |
| `builder_null_rate_warn_pct` | numeric | 95 | 1 – 100 | — | seed | group: Data Quality Thresholds | Maximum acceptable percentage of recent permits (last 24h) with a NULL builder_name before a data-quality warning is emitted CONSUMED by assert_data_bounds. |
| `calibration_default_median_days` | numeric | 30 | 1 – 365 | `scripts/compute-trade-forecasts.js` | seed | group: Forecast & Urgency | Fallback median days used by compute-trade-forecasts when no calibration data exists for a phase transition (level-5 default) |
| `calibration_default_p25_days` | numeric | 15 | 1 – 365 | `scripts/compute-trade-forecasts.js` | seed | group: Forecast & Urgency | Fallback p25 (25th-percentile days) used by compute-trade-forecasts when no calibration data exists for a phase transition (level-5 default) |
| `calibration_default_p75_days` | numeric | 60 | 1 – 365 | `scripts/compute-trade-forecasts.js` | seed | group: Forecast & Urgency | Fallback p75 (75th-percentile days) used by compute-trade-forecasts when no calibration data exists for a phase transition (level-5 default) |
| `calibration_freshness_warn_hours` | numeric | 48 | 1 – 720 | `scripts/compute-phase-calibration.js` | seed | group: Data Quality Thresholds | Hours since last compute_timing_calibration run after which the timing_calibration table is considered stale and a warning is emitted NOTE (2026-09-12, batch1 I2 commit 7; reworded commit 9 to drop the literal false-positive substring step-conformance.infra.test.ts's taggedToStep() scans for): assert_data_bounds's pre-conversion read of this var (ADB-D5) has been dead since migration 106 and its post-conversion descriptor correctly declares no such var — CONSUMED by compute_phase_calibration only; NOT deleted (repo-wide grep found that live second consumer). |
| `calibration_min_sample_size` | numeric | 5 | 1 – 100 | — | seed | group: Coverage & Quality | Minimum number of historical leads required before trade calibration data is trusted |
| `car_footprint_sqm` | numeric | 18.5 | 10 – 40 | — | seed | group: Spatial & Massing | Spec 65 §7 (Phase 3) — one parking-stall footprint incl. clearance (m²); garage_capacity_cars = floor(max_garage_gfa / this). CONSUMED by enrich-parcels.js (max-build pass). |
| `centreline_propagation_coverage_min` | numeric | 0.9 | 0 – 1 | — | seed | group: Coverage & Quality | §8e L24c (Spec 62) — minimum fraction of valid-geom parcels that must carry centreline_dataset_version_when_enriched before enrich-permits.js propagates is_corner_lot/is_through_lot/primary_frontage_street_name onto permits + coa. Below this, §8d is treated as only partially run and propagation HALTs (would otherwise mark most leads false/false/NULL). 0.9 sits below the ~3% zero-intersection floor so a healthy ~97% run never false-HALTs. CONSUMED by scripts/enrich-permits.js (assertCentrelineEnriched). |
| `coa_active_trades_warn_max` | numeric | 18 | 1 – 35 | — | seed | group: CoA Scope & Trades | WF2 P6.5/P6.6 — WARN ceiling for mean ACTIVE trades per CoA (is_active=true rows) in classify-coa-trades. Post-P6.6 (is_active=!fromBundle) the direct tag-matrix + realtor rows are active; bundle-only recall rows are inactive. Expected ~15; WARN above 18 flags a fan-out regression (e.g. bundle rows reactivated). |
| `coa_ancient_hearing_count_warn_max` | numeric | 5 | 1 – 100000 | — | seed | group: CoA Gates & Staleness | assert-data-bounds: coa_applications.hearing_date < 2010-01-01 row-count WARN ceiling. CONSUMED by assert_data_bounds. |
| `coa_bylaw_max_coverage_null_warn_pct` | numeric | 66 | 0 – 100 | — | seed | group: Data Quality Thresholds | WF2 P6.5 — enrich-permits (coa target) bylaw_max_coverage NULL-rate WARN floor. Baseline 59.6+6.4; structural nulls, WARN-above-baseline, never FAIL. |
| `coa_bylaw_max_fsi_null_warn_pct` | numeric | 97 | 0 – 100 | — | seed | group: Data Quality Thresholds | WF2 P6.5 — enrich-permits (coa target) bylaw_max_fsi NULL-rate WARN floor. Baseline 93.9+3; structural nulls, WARN-above-baseline, never FAIL. |
| `coa_cost_coverage_fail_pct` | numeric | 5 | 0 – 100 | — | seed | group: Cost Tuning | WF2 archetype (Spec 83 SS3): coverage FAIL threshold. 5 = catches a half-broken CoA ladder (coverage collapsing from ~69% toward zero) — the old default 0 only caught the exact-zero structural break; post-activation the gate must catch regressions too. |
| `coa_cost_coverage_threshold_pct` | numeric | 65 | 0 – 100 | — | seed | group: Cost Tuning | WF1 Spec 83 SS3.A D3 (CoA OB-2 mirror): coverage WARN threshold. WF2 archetype recalibration: 65 = measured ladder ceiling 68.7% minus margin (cost_coa_total exists on 76.8% of residential CoAs; Severance/Demolition/unlinked consume the rest). Was 70 in the all-safe-skip era. |
| `coa_cost_gt_threshold_cad` | numeric | 10000000 | 1000000 – 500000000 | — | seed | group: CoA Gates & Staleness | assert-data-bounds: coa_applications.estimated_cost magnitude-watch threshold (CAD), P12-C2 (c53f60a8). CONSUMED by assert_data_bounds. |
| `coa_cost_gt_threshold_warn_max` | numeric | 80 | 1 – 100000 | — | seed | group: CoA Gates & Staleness | assert-data-bounds: row-count WARN ceiling for coa_estimated_cost_gt10m (c53f60a8 — baseline ~64 archetype-parcel envelope tail). CONSUMED by assert_data_bounds. |
| `coa_forward_link_sub085_warn_pct` | numeric | 59 | 1 – 100 | — | seed | group: CoA Gates & Staleness | P12-B2: max acceptable percentage of LINKED coa_applications whose linked_confidence is below the 0.85 identity floor (a same-street/geo/flagged tier that must not surface as identity). Baseline sub-0.85 share ~54% (17,842 of 33,114 linked, 2026-07-08); seeded at baseline+5 so a regression in link quality WARNs. assert-data-bounds coa section. CONSUMED by assert_data_bounds. |
| `coa_freshness_fail_days` | numeric | 135 | 14 – 365 | `scripts/quality/assert-coa-freshness.js` | seed | group: CoA Matching | WF2 P6.5 — days since last CoA ingestion before assert-coa-freshness escalates from WARN to FAIL (portal likely dead, not just slow). Default 135 ≈ 3× the 45-day WARN floor. Empty-table branch stays WARN (fresh-staging, not portal rot). |
| `coa_freshness_warn_days` | numeric | 45 | 7 – 180 | `scripts/quality/assert-coa-freshness.js` | seed | group: CoA Matching | Days since last CoA ingestion before assert-coa-freshness emits a portal-rot WARN (data may be frozen in CKAN) |
| `coa_fsi_gt_threshold` | numeric | 5 | 1 – 50 | — | seed | group: CoA Gates & Staleness | assert-data-bounds: coa_applications.coa_fsi magnitude-watch threshold (c53f60a8 — max observed ~3.15). CONSUMED by assert_data_bounds. |
| `coa_future_hearing_window_years` | numeric | 2 | 1 – 20 | — | seed | group: CoA Gates & Staleness | assert-data-bounds: coa_applications.hearing_date future-bound window (years) — FAIL if any hearing exceeds CURRENT_DATE + this many years. CONSUMED by assert_data_bounds. |
| `coa_gate_calibration_window_days` | numeric | 7 | — | `scripts/compute-trade-forecasts.js` | seed | group: CoA Gates & Staleness | Phase F.1 CoA audit-verdict gate freshness window: compute_phase_calibration must have a permits-chain pipeline_runs row within this many days for the gate to consult its verdict. Older runs trigger no_prior_run state. Default 7 aligns with Spec 48 §3.4 baseline window. Operator may raise this if calibration runs are less frequent than weekly. |
| `coa_gate_force_active` | numeric | 0 | 0 – 1 | `scripts/compute-trade-forecasts.js` | seed | group: CoA Gates & Staleness | Spec 79 §7a WF3 #2 — operator safety valve for compute-trade-forecasts.js CoA audit-verdict gate. 0 = normal gate behavior (calibration verdict + cold-start grace decide). 1 = force CoA writes regardless of calibration verdict; use for post-grace deadlocks where coa_cohort_presence remains 0 after 7 days. When 1, script emits WARN audit row + pipeline.log.warn on every run; reset to 0 once root cause is resolved. |
| `coa_gate_policy` | JSONB | "pass_or_warn" | — (migration-seeded) | — | migration 211 | — (governed by GROUPS only) | Spec 85 §3.6 / D3a — CoA audit-verdict gate policy. 'pass_only' = only a PASS calibration verdict activates the CoA forecast branch (strict). 'pass_or_warn' = a WARN verdict within the freshness window ALSO activates it (FAIL/absent/stale stay blocked); each accepted WARN emits the coa_audit_gate_warn_accepted WARN audit row. JSONB string (config-loader reads object JSON only, so compute-trade-forecasts.js reads this directly). CONSUMED by scripts/compute-trade-forecasts.js. |
| `coa_gfa_over_lot_multiple` | numeric | 3 | 1 – 20 | — | seed | group: CoA Gates & Staleness | assert-data-bounds: coa_applications.max_buildable_gfa_sqm-over-lot_size_sqm multiple watch (c53f60a8). CONSUMED by assert_data_bounds. |
| `coa_gfa_over_lot_warn_max` | numeric | 45 | 1 – 100000 | — | seed | group: CoA Gates & Staleness | assert-data-bounds: row-count WARN ceiling for coa_maxbuild_gfa_gt3lot (c53f60a8 — baseline ~29 oversized-envelope). CONSUMED by assert_data_bounds. |
| `coa_imminent_window_days` | numeric | 7 | 1 – 60 | `scripts/update-tracked-projects.js` | seed | group: CoA Matching | Phase F.2 CRM Assistant CoA imminent-alert window: fires COA_HEARING_IMMINENT when 0 < (hearing_date - NOW()) <= this many days AND last_notified_urgency != 'imminent'. Default 7 = one week before hearing. Operator-tunable per trade engagement model. Seeded in DB by mig 136. |
| `coa_inherit_from_permit_min_confidence` | numeric | 0.6 | 0.01 – 1 | `scripts/link-coa.js` | seed | group: CoA Matching | Minimum fuzzy-match confidence for link-coa.js to inherit a linked permit's authoritative lat/long + ward into the matched CoA (R5.6 Part A — lead-identity continuity, Spec 42 §6.X). Default 0.60 covers Tier 1a (0.95), Tier 1b (0.85), and Tier 2a (0.60) but EXCLUDES Tier 2b (0.50, name-only + permit ward NULL — too speculative for dense urban streets) and Tier 3 (≤0.50, description FTS). Raised from initial 0.50 design per 4-reviewer plan-review (Gemini + DeepSeek + observability worktree converged on Tier 2b risk). |
| `coa_lifecycle_transition_stale_days` | numeric | 180 | — | `scripts/compute-trade-forecasts.js` | seed | group: CoA Gates & Staleness | Phase F.1 CoA forecast snowplow staleness gate: if lifecycle_transitions.MAX(transitioned_at) anchor is older than this many days, the CoA forecast becomes snowplow-eligible (treats long-stalled E.2-classified CoAs without subsequent transitions as Rescue Missions). Default 180 = 6 months ≈ p75 of typical CoA decision cohort duration per Spec 84 §7. |
| `coa_match_conf_high` | numeric | 0.9 | 0.01 – 1 | `scripts/link-coa.js` | seed | group: CoA Matching | CoA→permit match confidence at or above which the link is classified as 'high confidence' in the link-coa run summary |
| `coa_match_conf_medium` | numeric | 0.5 | 0.01 – 1 | `scripts/link-coa.js` | seed | group: CoA Matching | CoA→permit match confidence floor for the 'medium confidence' bucket (link-coa summary: >= this AND < high); also used as the low-confidence boundary in the CoA snapshot. CONSUMED by scripts/lib/compute/refresh-snapshot.js (pilot 8, 2026-08-31: RE-HOMED off scripts/refresh-snapshot.js's frozen shape — bound into the CoA confidence-bucket query by buildReads()/buildPermitsScalarQuery, not read via a script-level LOGIC_VARS_SCHEMA any more). |
| `coa_neighbourhood_coverage_pass_pct` | numeric | 95 | 0 – 100 | — | seed | group: Coverage & Quality | assert-global-coverage: CoA neighbourhood_id calibrated coverage -> PASS CONSUMED by assert_global_coverage. |
| `coa_neighbourhood_coverage_warn_pct` | numeric | 90 | 0 – 100 | — | seed | group: Coverage & Quality | assert-global-coverage: CoA neighbourhood_id calibrated coverage -> WARN, else FAIL CONSUMED by assert_global_coverage. |
| `coa_null_address_count_warn_max` | numeric | 10 | 1 – 100000 | — | seed | group: CoA Gates & Staleness | assert-data-bounds: coa_applications.address null/empty row-count WARN ceiling. CONSUMED by assert_data_bounds. |
| `coa_orphan_lead_id_warn_threshold` | numeric | 0 | — (migration-seeded) | — | migration 136 | — (governed by GROUPS only) | Spec 42 §6.6.A.1 CQA gate — lead_id_orphan_audit row count must be 0; >0 = FAIL |
| `coa_parcel_conf_tier1a` | numeric | 0.95 | 0.01 – 1 | `scripts/link-coa-to-parcels.js` | seed | group: CoA Matching | Confidence score for CoA Tier 1a exact-address parcel match in link-coa-to-parcels (addr_num + street_name_normalized both match). DISTINCT from coa_match_conf_high which is consumed by link-coa.js for CoA→permit matching. |
| `coa_parcel_conf_tier1b` | numeric | 0.8 | 0.01 – 1 | `scripts/link-coa-to-parcels.js` | seed | group: CoA Matching | Confidence score for CoA Tier 1b name-only parcel match in link-coa-to-parcels (street_num absent, only street_name_normalized matches). DISTINCT from coa_match_conf_medium which is consumed by link-coa.js. |
| `coa_scope_unmapped_threshold_pct` | numeric | 10 | 0 – 100 | `scripts/classify-coa-scope.js` | seed | group: CoA Scope & Trades | Day-1 threshold (percent) for unmapped scope_tags in classify-coa-scope (Spec 42 §6.5 step 5). Above this, audit_table verdict is WARN (not FAIL). Recalibrate post-burn-in once production-shape data is known. |
| `coa_stall_threshold` | numeric | 30 | 1 – 365 | `scripts/update-tracked-projects.js` | seed | group: Timing & Staleness | Days without CoA update before a linked application is considered stalled |
| `coa_stall_threshold_p2_days` | numeric | 90 | 1 – 365 | `scripts/update-tracked-projects.js` | seed | group: CoA Matching | Phase F.2 CRM Assistant CoA stall threshold for Hearing Scheduled status (Universal Stream B1.B / P2): hearing prep typically runs 1-3 months — NOT a stall. Default 90 = upper edge of typical hearing window. Setting this below coa_stall_threshold defeats the purpose. Seeded in DB by mig 136. |
| `coa_stall_threshold_postponed_days` | numeric | 60 | 1 – 365 | `scripts/update-tracked-projects.js` | seed | group: CoA Matching | Phase F.2 CRM Assistant CoA stall threshold for Postponed/Deferred statuses (mid-tier between generic 30-day and Hearing-Scheduled 90-day). Default 60 = upper edge of typical postponement before considered stalled. Operator-tunable per Spec 86 Control Panel. Seeded in DB by mig 154. |
| `coa_structure_type_coverage_pass_pct` | numeric | 45 | 0 – 100 | — | seed | group: Coverage & Quality | assert-global-coverage: CoA structure_type calibrated coverage (description-classifier ceiling ~52%) -> PASS CONSUMED by assert_global_coverage. |
| `coa_structure_type_coverage_warn_pct` | numeric | 35 | 0 – 100 | — | seed | group: Coverage & Quality | assert-global-coverage: CoA structure_type calibrated coverage -> WARN, else FAIL CONSUMED by assert_global_coverage. |
| `coa_trades_unmapped_threshold_pct` | numeric | 20 | 0 – 100 | — | seed | group: CoA Scope & Trades | Day-1 threshold (percent) for CoAs with non-NULL scope_tags but zero trade matches in classify-coa-trades (Spec 42 §6.8 row 667; R5.4 R8 fold #1). Variance-only CoAs (severance, setback, minor-variance) legitimately produce zero trades, so a non-zero threshold is the correct semantic — the spec literal `== 0 FAIL` would be a permanent FAIL. Set to 20% for first burn-in; recalibrate post-prod once tag distribution is known. |
| `coa_unmatched_threshold_pct` | numeric | 10 | 0 – 100 | `scripts/link-coa-to-parcels.js` | seed | group: CoA Gates & Staleness | Day-1 threshold (percent) for unmatched CoAs in link-coa-to-parcels (Spec 42 §6.5 step 9). Above this, audit_table verdict is WARN (not FAIL). Recalibrate post-burn-in. |
| `commercial_shell_multiplier` | numeric | 0.6 | 0.1 – 2 | — | seed | group: Cost Tuning | Discount multiplier applied to commercial shell-and-core permits for cost estimation (Spec 86) |
| `compute_centroids_compute_rate_warn_pct` | numeric | 98 | 0 – 100 | — | seed | group: Centroid Computation | Spec 124 Rule 3 (T2, Pilot 6, BACKFILL) — WARN floor on centroids_computed / parcels_processed. 98 = the pre-externalization literal (`:198`,`:200` in the pre-conversion script) and traces to commit d32612bb's deliberate business-accuracy tightening ("tighten compute_rate from 90% to 98%"), not an arbitrary number. A `pct >= floor` form (R-N precedent). CONSUMED by compute_centroids (scripts/lib/compute/compute-centroids.js compute_rate, via checks[].limit_from_config). |
| `compute_centroids_failed_geometries_warn` | numeric | 0 | 0 – 10000 | — | seed | group: Centroid Computation | Spec 124 Rule 3 (T1, Pilot 6, BACKFILL) — WARN ceiling on the count of parcels whose geometry JSONB is present but the PostGIS geom cast never populated (a malformed GeoJSON payload), still uncomputed after compute_centroids's own UPDATE. 0 = the pre-externalization literal (`:197` in the pre-conversion script, `== 0`). Never FAIL — a malformed geometry is a data-quality signal to surface, never a reason to halt the run. CONSUMED by compute_centroids (scripts/lib/compute/compute-centroids.js failed_geometries, via checks[].limit_from_config). |
| `compute_centroids_full_recompute_batch_size` | numeric | 10000 | 100 – 50000 | — | seed | group: Centroid Computation | Spec 124 Rule 3 (T3, CC-D3 WF3, 2026-08-30, BACKFILL) — NOT verdict-affecting (on_invalid "clamp": no checks[].limit_from_config names it, R-G/GAP-G-4). Sizes each UPDATE ... WHERE id = ANY($1::int[]) batch of the FULL-mode repair's keyset loop (LG-22, override.force_full only, never scheduled). 10000 chosen as a round, conservative default for a ~292K-row one-off repair (measured live 2026-08-30, see docs/reports/defect-ledger.md CC-D3) — not derived from a prior literal, since no batching existed on this step before this WF3. CONSUMED by compute_centroids (scripts/lib/compute/compute-centroids.js buildFullRecomputeSql, via execution.batch_size_from_config). |
| `compute_parcel_cost_adjustment_factor_default` | numeric | 1 | 0 – 10 | — | seed | group: Cost Tuning | Batch-2 row 2.4 — the NULL cost_adjustment_factor fallback (`?? 1`; column is NOT NULL, so this is defensive). Ported verbatim. CONSUMED by scripts/lib/parcel-cost.js#buildParcelCostMenu (REQUIRED — Rule 3). |
| `compute_parcel_cost_batch_size` | numeric | 1000 | 1 – 3854 | — | seed | group: Source Ingestion | Batch-2 row 2.4 — the write-flush batch size (rows per UPDATE ... FROM (VALUES ...) statement). Ported verbatim from the pre-conversion literal `Math.min(1000, Math.floor((65535-1)/17))`; the 65535/column-count ceiling arithmetic stays derived in the compute (not re-typed here), this is the tunable half. Declared in execution.batch as the literal 1000 (AJV: batch is anyOf[{const:"none"},{integer}], a variable name is invalid there) — this seed is the compute's OWN flush-size read, asserted to agree with execution.batch at construction. CONSUMED by scripts/lib/compute/compute-parcel-cost-estimates.js. |
| `compute_parcel_cost_empty_menu_max_pct` | numeric | 5 | 0 – 100 | — | seed | group: Data Quality Thresholds | Batch-2 row 2.4 — the null_geom_basis_count ceiling, as a percentage of the residential population. Measured live 1.755% (7,674/437,279). CONSUMED by the compute_parcel_cost_empty_menu_max_pct declared check. |
| `compute_parcel_cost_engine_error_max` | numeric | 0 | 0 – 1000 | — | seed | group: Data Quality Thresholds | Batch-2 row 2.4 — the engine_error_count FAIL ceiling (deliberately strict: any engine error fails the step). FOLD-V1: this is the ONLY enforced expression of F4's FAIL half — on_row_error_max_pct is declared-only (the runner never reads it). CONSUMED by the engine_error_count declared check. |
| `compute_parcel_cost_escalation_fallback_multiplier` | numeric | 1 | 0 – 10 | — | seed | group: Cost Tuning | Batch-2 row 2.4 — the missing/invalid-index-or-base escalation multiplier (`return 1`). Ported verbatim. CONSUMED by scripts/lib/parcel-cost.js#escalationMultiplier (REQUIRED — Rule 3). |
| `compute_parcel_cost_escalation_min_multiplier` | numeric | 1 | 0 – 10 | — | seed | group: Cost Tuning | Batch-2 row 2.4 — the never-deflate escalation floor (`Math.max(1, now/base)`). Ported verbatim. CONSUMED by scripts/lib/parcel-cost.js#escalationMultiplier (REQUIRED — Rule 3). |
| `compute_parcel_cost_fsi_max_plausible` | numeric | 99.999 | 0.001 – 1000 | — | seed | group: Cost Tuning | Batch-2 row 2.4 — max storable/plausible FSI (NUMERIC(6,3) column ceiling; a higher derived FSI means a garbage max_buildable_gfa_sqm, the known tree-contaminated-massing artifact class). Ported verbatim from the pre-conversion literal FSI_MAX_PLAUSIBLE = 99.999. Fires 0 times today (measured 2026-09-21) — the lock proving it fires constructs the case. CONSUMED by scripts/lib/parcel-cost.js (opts.config.fsiMaxPlausible, REQUIRED — Rule 3). |
| `compute_parcel_cost_heartbeat_minutes` | numeric | 5 | 1 – 60 | — | seed | group: Source Ingestion | Batch-2 row 2.4 — ENRICHER runner-profile heartbeat interval (I5 ruling precedent: required on this profile). CONSUMED by scripts/lib/step/index.js via execution.heartbeat_minutes_from_config. |
| `compute_parcel_cost_line_total_max_cad` | numeric | 250000000 | 0 – 1000000000000 | — | seed | group: Data Quality Thresholds | Batch-2 row 2.4 — A8 RULED: a labelled FUTURE-REGRESSION TRIPWIRE, not a calibrated bound (the live max, $176,567,416, is itself a suspect upstream cur_floor_gfa_sqm artifact per rf:198). Catches movement ABOVE today's already-suspect tail only. CONSUMED by the compute_parcel_cost_line_total_max_cad declared check. |
| `compute_parcel_cost_lock_timeout_ms` | numeric | 5000 | 0 – 600000 | — | seed | group: Source Ingestion | Batch-2 row 2.4 — advisory-lock (117) wait timeout. CONSUMED by scripts/lib/step/index.js via execution.lock_timeout_ms_from_config. |
| `compute_parcel_cost_menu_coverage_min_pct` | numeric | 99 | 0 – 100 | — | seed | group: Data Quality Thresholds | Batch-2 row 2.4 — Reality-Check plan-altitude collapse floor (§7). Measured live 100.0%. The 2.2 FOLD-RC1 lesson: a ceiling-only set is blind to a collapse. CONSUMED by the compute_parcel_cost_menu_coverage_min_pct declared check. |
| `compute_parcel_cost_min_population` | numeric | 1 | 0 – 1000000 | — | seed | group: Data Quality Thresholds | Batch-2 row 2.4 — the residential_parcels_examined FAIL floor (`threshold: '> 0'` ported). CONSUMED by the residential_parcels_examined declared check. |
| `compute_parcel_cost_min_priceable_area_sqm` | numeric | 0 | 0 – 10000 | — | seed | group: Cost Tuning | Batch-2 row 2.4 — the per-line drop threshold (`area <= 0 => line absent`; strict `>`, seed 0 keeps this byte-identical to the legacy). Ported verbatim. CONSUMED by scripts/lib/parcel-cost.js#buildParcelCostMenu (REQUIRED — Rule 3). |
| `compute_parcel_cost_phase_timeout_minutes` | numeric | 60 | 0 – 290 | — | seed | group: Source Ingestion | Batch-2 row 2.4 — CPCE-A5 RULED: 60 (min 0, max 290). ~40x the measured local full-run runtime (70-95s, §0.4); no cloud pipeline_runs row exists for this slug, so this is a generous Supabase-Small projection, replaced by the measured value after the batch-close cloud run. CONSUMED by execution.phases[0].timeout_minutes_from_config. |
| `compute_parcel_cost_premium_default` | numeric | 1 | 0 – 10 | — | seed | group: Cost Tuning | Batch-2 row 2.4 — the NULL neighbourhood_cost_premium fallback (`?? 1`). Ported verbatim. CONSUMED by scripts/lib/parcel-cost.js#buildParcelCostMenu (REQUIRED — Rule 3). |
| `compute_parcel_cost_stream_batch_size` | numeric | 100 | 1 – 5000 | — | seed | group: Source Ingestion | Batch-2 row 2.4 — the READ cursor's batch size (ctx.stream's own batchSize option), DISTINCT from compute_parcel_cost_batch_size (the WRITE flush size). FOLD-I4: live because ctx.stream's `opts` spreads AFTER the runner's own (wrong-key) batchSize default, so the compute-supplied value wins (§5 test 16, both directions). CONSUMED by scripts/lib/compute/compute-parcel-cost-estimates.js. |
| `cost_coverage_pass_pct` | numeric | 55 | 0 – 100 | — | seed | group: Cost Audit Thresholds | WF3 F4: assert-global-coverage Step-14 cost_estimates rows PASS floor. Whole-corpus cost coverage is ~62% BY DESIGN under the archetype+safe-skip model (non-residential T4 matrix-miss safe-skip, fit-blocked permissioning, MEC-only nofit are legitimately NULL) — the global 90% bar was inherited indiscriminately and was already red pre-WF2 (40%). Scoped recalibration of these ~6 non-halting observability rows only; global 90/70 untouched (don't blind other row-quality checks). 55 leaves ~7pt headroom over measured 61.8% while still FAILing on a genuine collapse. CONSUMED by assert_global_coverage. |
| `cost_coverage_warn_pct` | numeric | 50 | 0 – 100 | — | seed | group: Cost Audit Thresholds | WF3 F4: assert-global-coverage Step-14 cost_estimates rows WARN floor (below -> FAIL). Pairs with cost_coverage_pass_pct. CONSUMED by assert_global_coverage. |
| `cost_escalation_index` | numeric | 100 | 0.001 – 100000 | — | seed | group: Cost Tuning | Batch-2 row 2.4 — the current StatCan BCPI (Toronto CMA) escalation index, manually updated quarterly. §0.6 REFUTATION: this row EXISTS live in the DB (100.0, migration 205 + admin panel) but was ABSENT from this seed file — a fresh cloud seed apply would have left it unset. Under the converted standard (LM-D15, on_invalid:'fail', min>0) a missing/invalid row now THROWS rather than the legacy's 'missing -> 1.0 multiplier + WARN' tolerance — a DECLARED, LOUDER-NEVER-QUIETER deviation (EH-D2 precedent). Shared key: also read (unaffected in behaviour — none of the other readers declares a descriptor) by compute-cost-estimates.js, compute-coa-cost-estimates.js, and the wf3-* analysis scripts. CONSUMED by scripts/lib/compute/compute-parcel-cost-estimates.js. |
| `cost_est_legacy_cost_ceiling_cad` | numeric | 50000000 | 1000000 – 2000000000 | — | seed | group: Cost Tuning | P13-1/P13-2: magnitude ceiling (CAD) on cost_estimates.estimated_cost for the LEGACY geometric/permit-passthrough sources (cost_source IN ('model','permit')). Above this a legacy row is treated as implausible (the mislinked-massing tail: whole-campus/whole-block GFA attributed to a single permit — e.g. Sunnybrook 792K m² on an elevator-cab permit). compute-cost-estimates nulls estimated_cost (keeps cost_source) via the durable clamp; assert-data-bounds WARNs on any residual not in the accepted-by-id list. Archetype (lot-validated) sources are exempt — the archetype caps already bound them. CONSUMED by assert_data_bounds. |
| `cost_est_legacy_gfa_ceiling_sqm` | numeric | 50000 | 10000 – 5000000 | — | seed | group: Cost Tuning | P13-1/P13-2: magnitude ceiling (m²) on cost_estimates.modeled_gfa_sqm for LEGACY sources. A single permit modeling >50,000 m² GFA is almost always mislinked whole-block/campus massing (max observed 1.89M m²). Rows breaching this are nulled by the compute clamp; assert-data-bounds WARNs on residual priced rows (estimated_cost IS NOT NULL) above it. CONSUMED by assert_data_bounds. |
| `cost_est_min_tiers` | numeric | 2 | 1 – 20 | — | seed | group: Data Quality Thresholds | Minimum number of distinct cost_tier values expected in cost_estimates; fewer triggers a data-quality warning CONSUMED by assert_data_bounds. |
| `cost_est_null_rate_warn_pct` | numeric | 80 | 1 – 100 | — | seed | group: Data Quality Thresholds | Maximum acceptable percentage of NULL estimated_cost rows in cost_estimates before a data-quality warning is emitted CONSUMED by assert_data_bounds. |
| `cost_index_stale_months` | numeric | 4 | 1 – 120 | — | seed | group: Cost Tuning | Batch-2 row 2.4 — the cost_escalation_index WARN staleness threshold (age in months, updated_at clock). §0.6 REFUTATION: EXISTS live (4) but was ABSENT from this seed file. Ported verbatim from the pre-conversion literal ConfigSchema default. CONSUMED by scripts/lib/compute/compute-parcel-cost-estimates.js. |
| `cost_matrix_miss_fail_pct` | numeric | 68 | 0 – 100 | — | seed | group: Cost Audit Thresholds | WF1 Spec 83 §3.A OB-3b: matrix_miss_pct FAIL threshold. Default 68 = PI-1 predicted miss rate (48%) + 20pp FAIL margin. Catches large-step coverage regressions (e.g., new permit_type vocabulary appearing in CKAN that the matrix doesn't cover). WF2 archetype: DEMOTED to INFO (full-population miss rate is meaningless once T1-T3 bypass the matrix); the gated metric is cost_t4_matrix_miss_*. |
| `cost_matrix_miss_warn_pct` | numeric | 58 | 0 – 100 | — | seed | group: Cost Audit Thresholds | WF1 Spec 83 §3.A OB-3b: matrix_miss_pct WARN threshold. Default 58 = PI-1 predicted miss rate (48%) + 10pp WARN margin. Operator-tunable per Spec 86 Control Panel as the scope_intensity_matrix coverage scales. |
| `cost_model_coverage_fail_pct` | numeric | 25 | 0 – 100 | — | seed | group: Cost Audit Thresholds | WF1 Spec 83 §3.A OB-2: model_coverage_pct FAIL threshold. Default 25 = half of PI-3 predicted post-fix coverage (~52%) — catches not just the 14-day exact-zero regression but also half-broken states (e.g., coverage drops from 52% to 5% via partial vocabulary mismatch). Recalibrate via Spec 86 Control Panel after the post-deploy spike settles. |
| `cost_model_coverage_warn_pct` | numeric | 80 | 1 – 100 | — | seed | group: Data Quality Thresholds | Minimum percentage of processed permits that must receive a non-null cost estimate; below this threshold the cost estimate run emits a WARN verdict |
| `cost_outlier_ceiling_cad` | numeric | 2000000000 | 1000000 – 10000000000 | — | seed | group: Data Quality Thresholds | Construction cost ceiling (CAD) above which a permit est_const_cost is flagged as a data-quality outlier (assert-data-bounds E7). $500M was too tight for legitimate Toronto megaprojects (hospitals, transit stations, mixed-use towers); raised to $2B. CONSUMED by assert_data_bounds. |
| `cost_outlier_count_warn_max` | numeric | 20 | 1 – 100000 | — | seed | group: Data Quality Thresholds | assert-data-bounds: permits cost-outlier row-count WARN ceiling (IL-2, f238b814 — 16-permit baseline, C4 panel A1 2026-08-13 re-review). CONSUMED by assert_data_bounds. |
| `cost_ptc_skipped_warn_pct` | numeric | 14.5 | 0 – 100 | — | seed | group: Cost Audit Thresholds | WF1 Spec 83 §3.A OB-3a: permit_type_class_skipped_pct WARN threshold. Default 14.5 = steady-state non-construction rate (~4.5%) + 10pp margin. Catches permit_type_classifications drift if the JOIN starts producing unclassified for previously-classified types. |
| `cost_rates_stale_months` | numeric | 3 | 1 – 120 | — | seed | group: Cost Tuning | Batch-2 row 2.4 — the archetype_cost_rates WARN staleness threshold (age in months, business as_of_date clock). §0.6 REFUTATION: EXISTS live (3) but was ABSENT from this seed file. Ported verbatim from the pre-conversion literal ConfigSchema default. Name kept as-is (not re-prefixed) — consumed by name in raw SQL. CONSUMED by scripts/lib/compute/compute-parcel-cost-estimates.js. |
| `cost_t4_matrix_miss_fail_pct` | numeric | 80 | 0 – 100 | — | seed | group: Cost Audit Thresholds | WF2 archetype (Spec 83 §3): matrix-miss FAIL threshold over the T4 population — catches a T4-only regression (e.g. matrix vocabulary drift) that the demoted full-population metric can no longer see. |
| `cost_t4_matrix_miss_warn_pct` | numeric | 60 | 0 – 100 | — | seed | group: Cost Audit Thresholds | WF2 archetype (Spec 83 §3): matrix-miss WARN threshold over the T4 population ONLY (leads on the legacy path: non-residential + mapper-null residential). The T4 denominator keeps the gate meaningful after T1-T3 bypass the matrix — a full-population gate would always pass (Observability plan-review item 1). |
| `desc_null_rate_warn_pct` | numeric | 5 | 1 – 100 | — | seed | group: Data Quality Thresholds | Maximum acceptable percentage of recent permits (last 24h) with a NULL description before a data-quality warning is emitted CONSUMED by assert_data_bounds. |
| `engine_health_dead_tuple_min_rows` | numeric | 1000 | 0 – 100000000 | — | seed | group: Data Quality Thresholds | assert-engine-health: minimum live-row count a table must have before its dead-tuple ratio is evaluated at all (small tables are legitimately dead-tuple-heavy; autovacuum handles them). Ported verbatim from the pre-conversion bare literal `live >= 1000` (report §9.6, R1 peel commit 8) — the sibling seq_scan_min_rows floor was externalized at commit 7 but this one was missed until the review panel caught it. CONSUMED by assert_engine_health. |
| `engine_health_dead_tuple_ratio_warn_max` | numeric | 0.1 | 0 – 1 | — | seed | group: Data Quality Thresholds | assert-engine-health: per-table dead-tuple ratio (n_dead_tup / n_live_tup, tables with >= 1000 live rows only) WARN ceiling — ratio above this value warns and is also the auto-VACUUM ANALYZE trigger threshold. Ported verbatim from the pre-conversion DEAD_TUPLE_RATIO constant (0.10). CONSUMED by assert_engine_health. |
| `engine_health_insp_dead_tuple_fail_pct` | numeric | 10 | 0 – 100 | — | seed | group: Data Quality Thresholds | assert-engine-health: permit_inspections (deep_scrapes chain) AND coa_applications (coa chain) dead-tuple percentage WARN ceiling — shared by both per-chain checks (AEH-IL-6: the two pre-conversion audit tables used the identical 10% bound, disagreeing only on displayed severity, never on value). Ported verbatim from the pre-conversion inspAuditTable/coaAuditTable dead_tuple_pct literal (10). CONSUMED by assert_engine_health. |
| `engine_health_insp_update_insert_fail_ratio` | numeric | 5 | 0 – 10000 | — | seed | group: Data Quality Thresholds | assert-engine-health: permit_inspections (deep_scrapes chain) cumulative update/insert ratio WARN ceiling. Ported verbatim from the pre-conversion inspAuditTable update_insert_ratio literal (5) — genuinely distinct from the top-level engine_health_ping_pong_ratio_warn_max (10) for the conceptually same metric (AEH-D3, this conversion's own finding, kept as a separate variable rather than silently unified). CONSUMED by assert_engine_health. |
| `engine_health_ping_pong_ratio_warn_max` | numeric | 10 | 0 – 10000 | — | seed | group: Data Quality Thresholds | assert-engine-health: per-table cumulative update/insert ratio (n_tup_upd / n_tup_ins) WARN ceiling. Ported verbatim from the pre-conversion PING_PONG_RATIO constant, raised from 2 to 10 at 8c9e64d7 (2026-03-21) — dimensional tables naturally run 5-6x and the spec's own >2x bound (docs/specs/01-pipeline/41_chain_permits.md) produced chronic false-positive WARNs; that spec-vs-code drift is named, not silently resolved, at this conversion (spec-diff owed at cutover). CONSUMED by assert_engine_health. |
| `engine_health_seq_scan_min_rows` | numeric | 10000 | 0 – 100000000 | — | seed | group: Data Quality Thresholds | assert-engine-health: minimum live-row count a table must have before its sequential-scan ratio is evaluated at all (small tables are legitimately seq-scan-dominant). Ported verbatim from the pre-conversion SEQ_SCAN_MIN_ROWS constant (10000). CONSUMED by assert_engine_health. |
| `engine_health_seq_scan_ratio_warn_max` | numeric | 0.8 | 0 – 1 | — | seed | group: Data Quality Thresholds | assert-engine-health: per-table sequential-scan ratio (seq_scan / (seq_scan + idx_scan), tables with >= engine_health_seq_scan_min_rows live rows only) WARN ceiling. Ported verbatim from the pre-conversion SEQ_SCAN_RATIO constant (0.80). CONSUMED by assert_engine_health. |
| `enrich_heritage_address_levenshtein_threshold` | numeric | 2 | 0 – 10 | — | seed | group: Data Quality Thresholds | Batch-2 row 2.2 — the Part IV tiebreak: when a parcel contains more than one Part IV heritage point, the LATERAL orders candidates by whether levenshtein(parcel's normalized address, point's normalized address) <= this threshold before falling back to point id. Ported verbatim from the pre-conversion literal ConfigSchema default (heritageAddressLevenshteinThreshold, 2). FOLD-I5 (declared limitation): changing this value does NOT re-stale any parcel — the lineage stamp is a source-content hash, so a threshold change only takes effect on the next genuine heritage version bump or an ENRICH_HERITAGE_FORCE_FULL run (measured impact bound: 94 multi-Part-IV-point parcels, 272 points, max 15 in one parcel, out of 1,217 parcels with any Part IV point; the only field a threshold change can move is heritage_designation_date). CONSUMED by scripts/lib/compute/enrich-heritage.js. |
| `enrich_heritage_designated_count_collapse_floor` | numeric | 1000 | 0 – 500000 | — | seed | group: Data Quality Thresholds | Batch-2 row 2.2 — Reality-Check O4-class plan-altitude demand (§7): a floor on parcels_heritage_designated_count far below the measured count but far above zero, catching a partial-collapse regression that a ceiling-only ruleset is blind to (the FAIL-at-0 gate only fires at exactly zero, and the share ceiling alone would pass a join silently matching 3% of what it should). Applied at plan altitude, following the enrich_ravines output-panel O4 precedent. Measured live 2026-09-20: 9,958. CONSUMED by scripts/lib/compute/enrich-heritage.js. |
| `enrich_heritage_designated_min_count` | numeric | 1 | 0 – 500000 | — | seed | group: Data Quality Thresholds | Batch-2 row 2.2 — the broken-join FAIL gate: parcels_heritage_designated_count must be >= this floor or the run reads FAIL (a hard zero means the spatial join matched nothing — wrong SRID / unbound GIST / a broken join — distinct from a legitimately small heritage subset). Ported exactly from the legacy's `designated === 0 ? 'FAIL' : 'INFO'` literal. Measured live 2026-09-20: 9,958. CONSUMED by scripts/lib/compute/enrich-heritage.js. |
| `enrich_heritage_designated_share_plausible_max_pct` | numeric | 10 | 0 – 100 | — | seed | group: Data Quality Thresholds | Batch-2 row 2.2 — Reality-Check plan-altitude demand (§7): a city-wide ceiling on how much of the parcel fabric can plausibly be heritage-designated. A join that started over-matching (SRID drift, unbound GIST returning a Cartesian product) would spike this share; the ceiling sits well above the measured value so it catches the regression class without ever nearing the legitimate value. Measured live 2026-09-20: 2.047%. CONSUMED by scripts/lib/compute/enrich-heritage.js. |
| `enrich_heritage_heartbeat_minutes` | numeric | 5 | 1 – 60 | — | seed | group: Source Ingestion | Batch-2 row 2.2 — how often (wall-clock minutes) the ENRICHER runner writes a progress heartbeat while enrich_heritage's one phase holds the shared transaction. REQUIRED on the ENRICHER profile. NEW behaviour (Class A diff): the legacy script had no heartbeat at all. Default 5 mirrors enrich_ravines_heartbeat_minutes / enrich_parcels_heartbeat_minutes / geocode_permits_heartbeat_minutes. CONSUMED by scripts/enrich-heritage.js. |
| `enrich_heritage_lock_timeout_ms` | numeric | 1800000 | 0 – 3600000 | — | seed | group: Source Ingestion | Batch-2 row 2.2 — bounded, LOUD `SET LOCAL lock_timeout` (ms) for enrich_heritage's shared transaction. REQUIRED on the ENRICHER profile. NEW behaviour (Class A diff): the legacy script had no lock_timeout at all. 0 = disabled. Default 1800000 (30 min) mirrors enrich_ravines_lock_timeout_ms. CONSUMED by scripts/enrich-heritage.js. |
| `enrich_heritage_part_iv_min_count` | numeric | 1 | 0 – 500000 | — | seed | group: Data Quality Thresholds | Batch-2 row 2.2 — EH-D2 (declared diff): parcels_part_iv_count must be >= this floor or the run reads WARN. The legacy's WARN was conditional on the source register carrying at least one Part IV point (partIvSource > 0); the ported bound is unconditional, so the two forms differ only when the register loads zero Part IV points, where the ported form is LOUDER (WARN vs INFO), never quieter. Measured live 2026-09-20: 1,217. CONSUMED by scripts/lib/compute/enrich-heritage.js. |
| `enrich_heritage_phase_timeout_minutes` | numeric | 240 | 0 – 290 | — | seed | group: Source Ingestion | Batch-2 row 2.2 — wall-clock phase deadline for the `heritage_join` phase. Operator ruling H-A2 = (a) 240 (2026-09-20): parity with enrich_ravines_phase_timeout_minutes, below the platform's 300-minute wall so an abort dies LOUD at this named phase boundary rather than as a GitHub SIGKILL; to be replaced by the measured cloud value after this step's batch-close acceptance run. Measured local full recompute: 59-75s, so 240 is generous headroom, not a tight fit. NEW behaviour (Class A diff): the legacy script had no statement/phase timeout at all. CONSUMED by scripts/enrich-heritage.js. |
| `enrich_heritage_unlinked_point_fail_pct` | numeric | 30 | 0 – 100 | — | seed | group: Data Quality Thresholds | Batch-2 row 2.2 — R-AD 3-tier WARN ceiling for heritage_points_no_parcel_match, same unit-change note as enrich_heritage_unlinked_point_warn_pct (30, not 0.30). CONSUMED by scripts/lib/compute/enrich-heritage.js. |
| `enrich_heritage_unlinked_point_warn_pct` | numeric | 15 | 0 – 100 | — | seed | group: Data Quality Thresholds | Batch-2 row 2.2 — R-AD 3-tier PASS ceiling for heritage_points_no_parcel_match (% of Part IV source points with no containing valid-geom parcel). Unit change from the legacy: the pre-conversion ConfigSchema compared a FRACTION (0.15) against a fraction while REPORTING a percentage; this check compares the reported percentage directly, so the seed is 15, not 0.15 — comparison semantics unchanged. Spec §12.3a's own defaults (0.05) assumed a radius match (~0% unmatched); under containment ~10% of Part IV points legitimately fall outside any parcel, so this default is calibrated above that baseline. Measured live 2026-09-20: 10.4%. CONSUMED by scripts/lib/compute/enrich-heritage.js. |
| `enrich_parcels_bbox_degree_divisor` | numeric | 78000 | 1000 – 1000000 | — | seed | group: Source Ingestion | Pilot 9 commit 7b (2026-09-04, Ask 5 externalization). Pass-1 road-overlay bbox pre-filter's metres-to-degrees-of-longitude divisor at Toronto's latitude (a degree of longitude is ~80.5km there; dividing by 78000 < 80500 guarantees the box is >= roadDist) — was the bare literal `78000` at scripts/enrich-parcels.js:237-246. Value preserved verbatim. CONSUMED by scripts/enrich-parcels.js. |
| `enrich_parcels_comp_fsi_max_plausible` | numeric | 8 | 1 – 50 | — | seed | group: Source Ingestion | Pilot 9 commit 7b (2026-09-04, Ask 5 externalization, Spec 78 §P3C.2). Upper plausible bound on a new-build comp's permit_fsi for inclusion in comp_fsi_p50 — was the bare literal `COMP_FSI_MAX_PLAUSIBLE = 8` at scripts/enrich-parcels.js:1098. Value preserved verbatim. CONSUMED by scripts/enrich-parcels.js (:1166). |
| `enrich_parcels_comp_fsi_min_plausible` | numeric | 0.05 | 0 – 1 | — | seed | group: Source Ingestion | Pilot 9 commit 7b (2026-09-04, Ask 5 externalization, Spec 78 §P3C.2). Lower plausible bound on a new-build comp's permit_fsi for inclusion in comp_fsi_p50 — was the bare literal `COMP_FSI_MIN_PLAUSIBLE = 0.05` at scripts/enrich-parcels.js:1097. Value preserved verbatim. CONSUMED by scripts/enrich-parcels.js (:1166). |
| `enrich_parcels_comp_knn_overfetch` | numeric | 50 | 1 – 500 | — | seed | group: Source Ingestion | Pilot 9 commit 7b (2026-09-04, Ask 5 externalization, Spec 78 §P3C.2). GiST kNN over-fetch count before the similarity post-filter — was the bare literal `COMP_KNN_OVERFETCH = 50` at scripts/enrich-parcels.js:1089. Value preserved verbatim. CONSUMED by scripts/enrich-parcels.js (:1184). |
| `enrich_parcels_comp_lot_tol` | numeric | 0.2 | 0 – 1 | — | seed | group: Source Ingestion | Pilot 9 commit 7b (2026-09-04, Ask 5 externalization, Spec 78 §P3C.2). Pass-4 comps kNN candidate similarity window (±20% lot-size/frontage) — was the bare literal `COMP_LOT_TOL = 0.2` at scripts/enrich-parcels.js:1088. Value preserved verbatim; only the declaration changed. CONSUMED by scripts/enrich-parcels.js (comp candidate CTE, :1191-1193). |
| `enrich_parcels_comp_over_capture_clamp` | numeric | 1.1 | 1 – 5 | — | seed | group: Source Ingestion | Pilot 9 commit 7b (2026-09-04, Ask 5 externalization, Spec 78 §P3C.2). build_ratio above this = massing over-capture, excluded from comp_build_ratio_p50 — was the bare literal `COMP_OVER_CAPTURE_CLAMP = 1.1` at scripts/enrich-parcels.js:1091. Value preserved verbatim. CONSUMED by scripts/enrich-parcels.js (:1160). |
| `enrich_parcels_comp_top_n` | numeric | 10 | 1 – 100 | — | seed | group: Source Ingestion | Pilot 9 commit 7b (2026-09-04, Ask 5 externalization, Spec 78 §P3C.2). Comps kept per subject parcel — was the bare literal `COMP_TOP_N = 10` at scripts/enrich-parcels.js:1090. Value preserved verbatim. CONSUMED by scripts/enrich-parcels.js (:1195). |
| `enrich_parcels_comps_window_years` | numeric | 5 | 1 – 20 | — | seed | group: Source Ingestion | Pilot 9 commit 7b (2026-09-04, Ask 5 externalization, Spec 78 §P3C.1). The rolling comps-eligibility window (permits.issued_date >= as-of-date minus this many years) — was the bare literal `interval '5 years'` at scripts/enrich-parcels.js:1114, the estate's first genuinely clock-relative gate. Fold G3 (MANDATORY, Spec 122 §5.5): the CLOCK ANCHOR itself moves to ctx.clock.asOfDate() at commit 7c (compute seam rewrite); this variable is the WINDOW LENGTH only, a Rule-3 threshold externalization, value preserved verbatim (5). CONSUMED by scripts/enrich-parcels.js. |
| `enrich_parcels_defer_threshold_rows` | numeric | 50000 | 1000 – 500000 | — | seed | group: Source Ingestion | Phase B B2 (R3-B8, Spec 40 §3.1.2 / Spec 47 §8.7) — row-count threshold that trips enrich-parcels.js's clean scope-defer (chain terminalizes deferred_to_full instead of attempting the pass) when a gated pass's PRE-TRANSACTION scope count exceeds it. Default 50,000 = 10-15x measured-normal decision scope (~3,461/14d) and ~9x under measured-escalated full 5-pass citywide runtime (46.5-53 min); sized against the 08-03 CKAN re-export citywide event (485,567 rows) as the designed defer trigger, not the common case. Every run emits scope_count/threshold/ratio audit rows and WARNs at >=80% of this value (code-side, not a var) so the operator sees the cliff coming before a defer fires. Two CONSECUTIVE defers on the same step escalate to a red verdict (Spec 40 §3.1.2 defer-streak rule) requiring a supervised force-full (ENRICH_PARCELS_FORCE_FULL=1). Threshold is a labeled projection until Phase B B7 measures real per-pass durations against it. CONSUMED by scripts/enrich-parcels.js. |
| `enrich_parcels_heartbeat_minutes` | numeric | 5 | 1 – 60 | — | seed | group: Source Ingestion | WF3 cloud-parity FIX 3.2b (2026-09-03) — how often (wall-clock minutes) enrich-parcels.js's optimal-config pass logs a progress heartbeat while streaming parcels (the one genuine per-row loop in the file; the four SQL passes ahead of it are single set-based statements with no JS-side iteration). Filed after a live cloud run (GH run 33324653235, 2026-08-30) stalled for 3h31m with zero log output between config load and the 300-minute hard-ceiling kill. Minimal, no-behaviour-change instrumentation — does not fix the stall itself (own WF3, wf3_enrich_parcels_cloud_stall), only makes the next one diagnosable. Default 5 = frequent enough to bound a silent gap without spamming logs on the common ~2h full run. CONSUMED by scripts/enrich-parcels.js. |
| `enrich_parcels_lock_timeout_ms` | numeric | 1800000 | 0 – 3600000 | — | seed | group: Source Ingestion | WF3 enrich_parcels stall (2026-09-03, commit 1) — bounded, LOUD `SET LOCAL lock_timeout` (ms) for enrich-parcels.js's shared passes-1-4 transaction, paired with enrich_parcels_pass_statement_timeout_minutes. Deliberately far shorter than the statement timeout (default 30 min vs 75 min) so a genuine heavyweight-lock wait (H3 in the premise verification, UNDETERMINED — needs a live pg_stat_activity/pg_locks capture) is named distinctly (57014 vs 55P03, surfaced via runPass()) rather than being masked by the coarser statement-timeout abort. 0 = disabled. CONSUMED by scripts/enrich-parcels.js. |
| `enrich_parcels_optcfg_batch_size` | numeric | 500 | 50 – 5000 | — | seed | group: Source Ingestion | Pilot 9 commit 7b (2026-09-04, Ask 5 externalization). Pass-5 optimal-config flush batch size — was the bare literal `OPTCFG_BATCH = 500` at scripts/enrich-parcels.js:1251. Value preserved verbatim. CONSUMED by scripts/enrich-parcels.js (:1726). |
| `enrich_parcels_pass_statement_timeout_minutes` | numeric | 75 | 0 – 180 | — | seed | group: Source Ingestion | WF3 enrich_parcels stall (2026-09-03, commit 1) — bounded, LOUD `SET LOCAL statement_timeout` for enrich-parcels.js's shared passes-1-4 transaction, so a wedged statement dies at a NAMED boundary (the pass name is folded into the thrown error by runPass()) instead of the 300-minute chain-sources platform wall (Spec 115 §2.2 fail-safe-loud). STEP 0 premise verification (2026-09-03, wf3_enrich_parcels_cloud_stall) measured REAL cloud pipeline_runs.records_meta per-pass durations: no single pass exceeds 90 min in either successful completion, pass2 (max-build envelope) dominates at 46.7/48.3 min. Default 75 = measured max (48.3 min) x1.5 margin (the Spec 122 §10.1 remedy formula), rounded up. 0 = disabled — falls through to createPool()'s session-level statement_timeout (today 0/unbounded, UNCHANGED by this var). Does NOT touch scripts/lib/pipeline.js's withPipelineStatementTimeout, which still governs every other caller. CONSUMED by scripts/enrich-parcels.js. |
| `enrich_parcels_pass5_stream_batch_size` | numeric | 200 | 10 – 2000 | — | seed | group: Source Ingestion | Pilot 9 commit 7b (2026-09-04, Ask 5 externalization). Pass-5 pipeline.streamQuery batchSize (the eligible-parcels read cursor, distinct from enrich_parcels_optcfg_batch_size's own write-flush size) — was the bare literal `{ batchSize: 200 }` at scripts/enrich-parcels.js:1692. Value preserved verbatim. CONSUMED by scripts/enrich-parcels.js. |
| `enrich_parcels_pass5_timeout_minutes` | numeric | 60 | 0 – 180 | — | seed | group: Source Ingestion | Pilot 9 commit 7b (2026-09-04, Ask 7). NEW — bounded, LOUD pass-5 (optimal-config) statement timeout, applied via a live SET on the pass-5 session by runEnrichPhase (commit 7d) since pass 5 runs on a SEPARATE post-commit connection outside the SET LOCAL pair enrich_parcels_pass_statement_timeout_minutes/enrich_parcels_lock_timeout_ms bound for passes 1-4 (EP-D7, OPEN — legacy pass 5 is unbounded today, inheriting the session-level statement_timeout=0). 0 = disabled. Default 60 is a conservative first bound pending real per-pass-5 duration measurement (pass 5 is JS-streaming, not a single SQL statement, so the passes-1-4 measured-max x1.5 formula does not directly transfer). CONSUMED by scripts/lib/step/index.js (runEnrichPhase). |
| `enrich_parcels_pending_scope_warn_max` | numeric | 50000 | 0 – 5000000 | — | seed | group: Data Quality Thresholds | WF3 EP-D14 (2026-09-09, .cursor/wf3_ep_d14_pass5_recovery_scan_active_task.md C1). WARN (not FAIL — a large backlog is the legitimate state right after a killed run, and a blocking FAIL would abort the very run whose job is to drain it) bound on enrich_parcels_pass3_scope rows unconsumed from a PRIOR run, observed at pass-5 start. Cloud-measured 2026-09-09: 439,130 unconsumed (well above this default) — the descriptor's pending_scope_parcels check is what would have surfaced that backlog before the stall. CONSUMED by scripts/lib/compute/enrich-parcels.js; descriptor check pending_scope_parcels. |
| `enrich_parcels_scope_recovery_batch_size` | numeric | 1000 | 100 – 10000 | — | seed | group: Source Ingestion | WF3 EP-D14 (2026-09-09, .cursor/wf3_ep_d14_pass5_recovery_scan_active_task.md C1). Batch size for consumePendingScope's INCREMENTAL D4' recovery path (the number of ids per ANY($1::int[]) round trip for both the recompute SELECT and the consumed_at stamp UPDATE) — replaces the per-parcel loop that was a full-scan UPDATE with no index leading on parcel_id (cloud-measured 2026-09-09: 439,130 unconsumed rows, one backend active 23 minutes on a single such UPDATE). Under --full this variable is unused: the recovery is one set-based stamp with no recompute (Ask 1 ruling). CONSUMED by scripts/lib/compute/enrich-parcels.js (consumePendingScope). |
| `enrich_parcels_scope_retire_after_hours` | numeric | 24 | 1 – 720 | — | seed | group: Source Ingestion | WF3 EP-PASS3-BACKLOG (2026-09-15, .cursor/wf3_enrich_parcels_pass3_backlog_active_task.md C1). Retention window for UNCONSUMED enrich_parcels_pass3_scope rows belonging to a PRIOR run. At step start (before the shared transaction opens) enrich_parcels DELETEs foreign unconsumed rows whose created_at is older than this many hours, and only while no OTHER enrich_parcels invocation shows status='running' (the one-off prune script's F3 guard, encoded in SQL). 24 h chosen because a --full chain-sources run is ~3.5-5 h: 24 h clears yesterday's corpse before today's dispatch and cannot touch a same-day run. Note created_at DEFAULT now() returns the TRANSACTION start timestamp and the hand-off INSERT runs inside the shared txn, so this window is measured from the shared txn's BEGIN. Cloud-measured 2026-09-15: 886,046 rows across 2 cohorts, 100% unconsumed, because both the stamp and the EP-D10 prune are the LAST things pass 5 does and neither killed run reached them. CONSUMED by scripts/lib/compute/enrich-parcels.js retireStaleScope, via scripts/lib/step/index.js runEnrichPhase. |
| `enrich_parcels_zone_class_pct_pass_floor` | numeric | 95 | 0 – 100 | — | seed | group: Data Quality Thresholds | Pilot 9 commit 7b (2026-09-04, Ask 5 externalization, Spec 65 §3a DEC-4). The zoning_class-coverage PASS floor (>= this = PASS, else falls to the WARN/FAIL split) — was the bare literal `zonePct >= 95` at scripts/enrich-parcels.js:2109. Value preserved verbatim. Live measured 96.8%. CONSUMED by scripts/enrich-parcels.js; descriptor check parcels_with_zone_class_pct_warn. |
| `enrich_parcels_zone_class_pct_warn_floor` | numeric | 90 | 0 – 100 | — | seed | group: Data Quality Thresholds | Pilot 9 commit 7b (2026-09-04, Ask 5 externalization, Spec 65 §3a DEC-4). The zoning_class-coverage WARN floor (>= this = WARN, below = FAIL) — was the bare literal `zonePct >= 90` at scripts/enrich-parcels.js:2109. Value preserved verbatim. CONSUMED by scripts/enrich-parcels.js; descriptor check parcels_with_zone_class_pct_fail. |
| `enrich_ravines_distance_collapse_floor_m` | numeric | 100 | 0 – 1000 | — | seed | group: Spatial & Massing | Output-panel O4 (Reality-Check, 2026-09-18) — a floor on the observed MAXIMUM parcels.ravine_distance_m, catching a degrees-not-metres regression that both existing distance checks (value_max ceilings) would silently pass, since every distance shrinking toward a degrees-scale value stays comfortably under either ceiling. Seeded at 100 m, far below the measured live maximum (2739.69 m) and far above a plausible degrees-scale collapse band. CONSUMED by scripts/enrich-ravines.js. |
| `enrich_ravines_distance_coverage_pass_pct` | numeric | 95 | 0 – 100 | — | seed | group: Data Quality Thresholds | R-AD 3-tier PASS floor for parcels_with_ravine_distance_pct (COUNT(ravine_distance_m IS NOT NULL) / COUNT(*) over geom-bearing parcels). Ported verbatim from the pre-conversion literal DISTANCE_COVERAGE_PASS_PCT (95). CONSUMED by scripts/enrich-ravines.js. |
| `enrich_ravines_distance_coverage_warn_pct` | numeric | 90 | 0 – 100 | — | seed | group: Data Quality Thresholds | R-AD 3-tier WARN floor for parcels_with_ravine_distance_pct. Ported verbatim from the pre-conversion literal DISTANCE_COVERAGE_WARN_PCT (90). CONSUMED by scripts/enrich-ravines.js. |
| `enrich_ravines_distance_plausible_max_m` | numeric | 20000 | 0 – 50000 | — | seed | group: Spatial & Massing | F-RC1a, ceiling half — physical bound on parcels.ravine_distance_m. Measured live maximum 2739.69 m (2026-09-18); seeded with generous headroom above it. CONSUMED by scripts/enrich-ravines.js. |
| `enrich_ravines_distance_plausible_min_magnitude_m` | numeric | 2000 | 0 – 5000 | — | seed | group: Spatial & Massing | F-RC1a (Reality-Check plan-altitude demand, batch-2 row 2.1) — physical ceiling on the MAGNITUDE of the most-negative parcels.ravine_distance_m (i.e. how far a parcel can sit "inside" the deepest ravine; city-invariant, one ravine network). Measured live magnitude 1470.30 m (2026-09-18, most-negative distance -1470.30 m); seeded with generous headroom above it rather than at the measured value, so the bound catches a genuine defect class (a mis-signed distance, or a unit/CRS error) rather than today's own legitimate tail. Declared as a MAGNITUDE (always non-negative) rather than a signed floor: verdict.js's LIMIT_NUMBER_RE is a digits-only pattern with no sign, so a negative-signed `limit_from_config` value mangles the rendered bound string into a double-negative ("value_min --2000", reproduced live 2026-09-18) — a pre-existing library gap out of scope for this conversion; the compute negates the SQL instead so both the measured value and this config value stay positive. Never intended to retighten toward the measured value — see the plausibility row's own retighten_when. CONSUMED by scripts/enrich-ravines.js. |
| `enrich_ravines_heartbeat_minutes` | numeric | 5 | 1 – 60 | — | seed | group: Source Ingestion | How often (wall-clock minutes) the ENRICHER runner writes a progress heartbeat while enrich_ravines' one phase holds the shared transaction. REQUIRED on the ENRICHER profile (execution.heartbeat_minutes_from_config names this key; a non-finite resolution THROWS rather than silently disabling the ticker — ER-D1). NEW behaviour (Class A diff): the legacy script had no heartbeat at all. Default 5 mirrors enrich_parcels_heartbeat_minutes / geocode_permits_heartbeat_minutes. CONSUMED by scripts/enrich-ravines.js. |
| `enrich_ravines_lock_timeout_ms` | numeric | 1800000 | 0 – 3600000 | — | seed | group: Source Ingestion | Bounded, LOUD `SET LOCAL lock_timeout` (ms) for enrich_ravines' shared transaction. REQUIRED on the ENRICHER profile; execution.lock_timeout_ms_from_config names this key and a non-finite resolution THROWS. NEW behaviour (Class A diff): the legacy script had no lock_timeout at all. 0 = disabled (inherit the connection's own setting). Default 1800000 (30 min) mirrors enrich_parcels_lock_timeout_ms / geocode_permits_lock_timeout_ms. CONSUMED by scripts/enrich-ravines.js. |
| `enrich_ravines_phase_timeout_minutes` | numeric | 240 | 0 – 290 | — | seed | group: Source Ingestion | Wall-clock phase deadline (EP-PHASE-DEADLINE) for the `ravine_join` phase — armed via startPhaseDeadline/pg_cancel_backend on a dedicated connection, distinct from the SAME resolved value's `SET LOCAL statement_timeout` use on the phase's own statement. Operator ruling A3 (2026-09-18): seeded at 240, below the platform's 300-minute wall so an abort dies LOUD at this named phase boundary (Spec 115 §2.2) rather than as a GitHub SIGKILL; replaced by the measured cloud value after this step's acceptance run rather than raised to absorb it (R-AQ: a ceiling measures the step, an instance measures the hardware). NEW behaviour (Class A diff): the legacy script had no statement/phase timeout at all. CONSUMED by scripts/enrich-ravines.js. |
| `expired_threshold_days` | numeric | -90 | -730 – -1 | `scripts/compute-trade-forecasts.js` | seed | group: Timing & Staleness | Negative offset (days) used to classify a permit as expired; permits older than this are excluded from leads |
| `external_coverage_pass_pct` | numeric | 10 | 0 – 100 | — | seed | group: Coverage & Quality | assert-global-coverage: scraper-sourced (external) field coverage -> PASS (AGC-D3) CONSUMED by assert_global_coverage. |
| `external_coverage_warn_pct` | numeric | 5 | 0 – 100 | — | seed | group: Coverage & Quality | assert-global-coverage: scraper-sourced (external) field coverage -> WARN, else FAIL (AGC-D3) CONSUMED by assert_global_coverage. |
| `fallback_range_pct` | numeric | 0.40 | — (migration-seeded) | — | migration 156 | — (governed by GROUPS only) | CoA geometric cost-model fallback range when the primary model declines (insufficient confidence). Default 0.40 = ±40% — wider than primary range to reflect lower confidence. Operator-tunable via Spec 86 Control Panel. |
| `forecast_default_calibration_fail_pct` | numeric | 50 | 0 – 100 | `scripts/compute-trade-forecasts.js` | seed | group: Forecast & Urgency | WF2 D2a (Spec 85 §3.6): compute-trade-forecasts.js default_calibration_pct FAIL threshold. At or above this share on calibration_method='default', the verdict FAILs (blocks nothing — verdict-only, run-chain.js halts on crashes not FAIL verdicts). STRICT baseline, restored 2026-08-05 by migration 238 alongside its warn twin, after the 2026-07-07 relaxation to 85 (c6310d65) was retired. The strict pair is 20/50, mirrored as STRICT_CALIB_WARN_PCT/STRICT_CALIB_FAIL_PCT in compute-trade-forecasts.js; the calibration_thresholds_relaxed guard makes any loosening loud + permanent-by-choice-only. |
| `forecast_default_calibration_warn_pct` | numeric | 20 | 0 – 100 | `scripts/compute-trade-forecasts.js` | seed | group: Forecast & Urgency | WF2 D2a (Spec 85 §3.6): compute-trade-forecasts.js default_calibration_pct WARN threshold. At or above this share of forecast rows on the level-5 calibration_method='default', the trade-forecast audit verdict WARNs. STRICT baseline, restored 2026-08-05 by migration 238 after the 2026-07-07 relaxation to 70 (c6310d65) met its own stated revert condition: calibration_cohort_fill_pct recovered past 80% (measured 88.7%, default_calibration_pct 11.3%) once compute_timing_calibration_v2 cohorts grew 32 to 76 on the Phase A inspection drain. Any future relaxation re-arms the calibration_thresholds_relaxed guard, which emits on EVERY run while this exceeds the strict 20 and escalates to FAIL once the corpus recovers. |
| `forecast_excluded_trade_slugs` | JSONB | ["site-maintenance"] | — (migration-seeded) | `scripts/compute-trade-forecasts.js` | migration 210 | — (governed by GROUPS only) | Spec 80 P4 / Spec 85 §3 — trade slugs excluded from trade_forecasts as non-forecastable (no phase-anchored install window). Rows carrying these slugs are skipped WITHOUT incrementing unmapped_trades; counted as excluded_rows + listed in excluded_trade_slugs audit rows. JSONB array per mig 097 convention (variable_value sentinel 0, array in variable_value_json). CONSUMED by scripts/compute-trade-forecasts.js. |
| `garage_max_gfa_sqm` | numeric | 60 | 10 – 200 | — | seed | group: Spatial & Massing | Spec 65 §7 (Phase 3) — by-law cap on a detached garage footprint (m²). CONSUMED by enrich-parcels.js (max-build pass). |
| `garage_min_footprint_sqm` | numeric | 18 | 5 – 200 | — | seed | group: Spatial & Massing | Spec 65 §7 (Phase 3) — minimum usable rear-yard footprint (m²) for a garage to fit (one car). CONSUMED by enrich-parcels.js (max-build pass). |
| `garage_min_lot_sqm` | numeric | 230 | 100 – 2000 | — | seed | group: Spatial & Massing | Spec 65 §7 (Phase 3) — minimum lot area (m²) to consider an accessory garage. CONSUMED by enrich-parcels.js (max-build pass). |
| `garden_suite_max_gfa_sqm` | numeric | 60 | 10 – 200 | — | seed | group: Spatial & Massing | Spec 65 §7 (Phase 3) — externalized garden-suite GFA cap (m²); default = previously-hardcoded. CONSUMED by enrich-parcels.js (max-build pass). |
| `garden_suite_min_lot_sqm` | numeric | 270 | 100 – 2000 | — | seed | group: Spatial & Massing | Spec 65 §7 (Phase 3) — externalized garden-suite min lot area (m²); default = the previously-hardcoded value (byte-stable). CONSUMED by enrich-parcels.js (max-build pass). |
| `garden_suite_min_rear_yard_m` | numeric | 5 | 2 – 30 | — | seed | group: Spatial & Massing | Spec 65 §7 (Phase 3) — externalized garden-suite min usable rear-yard depth (m); default = previously-hardcoded. CONSUMED by enrich-parcels.js (max-build pass). |
| `garden_suite_storeys` | numeric | 1 | 1 – 4 | — | seed | group: Spatial & Massing | Spec 65 §7 (Phase 3) — garden suite storeys; suite footprint = GFA / storeys for the greenspace test. CONSUMED by enrich-parcels.js (max-build pass). |
| `geocode_permits_coverage_warn_pct` | numeric | 95 | 0 – 100 | — | seed | group: Source Ingestion | Minimum permit geocode coverage (%) below which the geocode_coverage check WARNs. Declared form `pct >= 95`, the floor form. CONSUMED by geocode_permits via checks[].limit_from_config. Externalizes the ONLY literal threshold the pre-conversion scripts/geocode-permits.js contained — the 95 that appeared TWICE, once as the audit row's `>= 95%` render string plus its status comparison at :142 and once as a second, independent verdict comparison at :166 (the Rule 10 parallel boolean the conversion retires by construction). SEEDED AT 95 ON PURPOSE so the conversion commits are a genuine zero-diff. SHIPPED RED, and the reason is GP-D1: measured 2026-09-16, coverage is 91.2816% (231,930 of 254,082) and 15 of the last 40 ledger rows carrying an audit verdict read WARN with ZERO PASS. The gate has been evaluated at 95 for its entire life and the one measurement ever taken against it — 4de16d00's 90.9% reading, which lowered the threshold to 85 — changed only ONE of the two literals, leaving the verdict comparison at 95, so it never took effect and was correctly reconciled away five days later by d24c964c. The structural ceiling is 96.99% (the 7,660-row no_geo_id tail can never be geocoded by a geo_id equijoin). The VALUE ruling is its own peel and its value is 90, the measured coverage floored to the nearest 5 below — never 85 restored by transcription. Ledgered GP-D1. |
| `geocode_permits_heartbeat_minutes` | numeric | 5 | 1 – 60 | — | seed | group: Source Ingestion | How often (wall-clock minutes) the ENRICHER runner writes a progress heartbeat while geocode_permits' two phases hold the shared transaction. REQUIRED on the ENRICHER profile since d7668b8a (batch-2 Phase 0.10): execution.heartbeat_minutes_from_config names this key, and the runner's resolveInterval THROWS on a non-finite resolution rather than silently disabling the ticker — which is ER-D1, the defect 0.10 closed (a hardcoded enrich_parcels key resolved NaN for any other step, `if (!intervalMs) return () => {}` swallowed it, and last_heartbeat_at stayed NULL for the whole run with no warning, no audit row and no throw). DECLARED AS A REAL VARIABLE RATHER THAN THE LITERAL "none" (I5 ruling, 2026-09-16): "none" is a legal declared disable, but choosing it one commit after 0.10 paid to remove the same silence would re-create it by declaration instead of by accident. Default 5 mirrors enrich_parcels_heartbeat_minutes. CONSUMED by scripts/geocode-permits.js. |
| `geocode_permits_lock_timeout_ms` | numeric | 1800000 | 0 – 3600000 | — | seed | group: Source Ingestion | Bounded, LOUD `SET LOCAL lock_timeout` (ms) for geocode_permits' shared transaction. REQUIRED on the ENRICHER profile since d7668b8a; execution.lock_timeout_ms_from_config names this key and a non-finite resolution THROWS. Genuinely load-bearing for this step rather than ceremonial: it runs in BOTH the permits and sources chains under one advisory lock (id 5), and its phase-1 statement is a full re-join of all 246,416 numeric-geo_id permits against the 525,346-row address_points table inside a transaction that also holds a destructive retraction — so a heavyweight-lock wait here blocks a table five other steps read. 0 = disabled (inherit the connection's own setting). Default 1800000 (30 min) mirrors enrich_parcels_lock_timeout_ms. CONSUMED by scripts/geocode-permits.js. |
| `income_premium_tiers` | JSONB | {"100000": 1.2, "150000": 1.5} | — (migration-seeded) | — | migration 097 | — (governed by GROUPS only) | JSON map of neighbourhood median income (CAD) bracket → cost multiplier. Keys are income thresholds (lowest → highest), values are multipliers. E.g. {"100000": 1.2, "150000": 1.5} |
| `inference_weight` | numeric | 0.5 | 0 – 1 | `scripts/compute-trade-forecasts.js` | seed | group: Scope Inference | Spec 80 §5.C / P16-16E [Gemini MEDIUM pinned] — provenance weight for INFERENCE-basis trade attachments in compute-trade-forecasts.js input aggregation: an inference row's calibration sample is scaled by this factor before classifyConfidence banding (needs 1/weight x the evidence sample for the same confidence band). Ranking/serving authority stays attachment_basis (D5) — this weights, never gates. Panel-adjustable; 0.5 = the pinned default so re-activated inference never re-creates P13-3 full-weight inflation. CONSUMED by scripts/compute-trade-forecasts.js. |
| `inspection_ancient_dates_count_warn_max` | numeric | 5 | 1 – 100000 | — | seed | group: Data Quality Thresholds | assert-data-bounds: permit_inspections.inspection_date < 2020-01-01 row-count WARN ceiling — count > this value warns. WIRED at commit 8b (ADB-D7, defect-ledger.md, CLOSED): the pre-conversion checkInsp() helper only ever compared value > 0 for ancient_dates; its displayed "<= 5" threshold was never a live comparison until this fix. Verdict-safety measured before wiring: ancient_dates read 0 in every golden capture. CONSUMED by assert_data_bounds. |
| `inspection_stall_days` | numeric | 300 | 30 – 730 | `scripts/classify-inspection-status.js` | seed | group: Inspection & Closure | Days without inspection activity before an Active Inspection permit is reclassified as Stalled |
| `invariants_every_run_budget_ms` | numeric | 5000 | 100 – 60000 | — | seed | group: Step Validator | WF2 'Step Validator, Data-First' (Spec 124 §2 Rule 13 addendum, VAL-WF2, commit 1) — the cost-adjudication default for a `checks[].invariants[]`/`plausibility[]` candidate's `frequency` field: a candidate whose measured single-query cost (grounder median, ≥5 timings across ≥2 sessions, `last_measured.cost_ms`) is below this budget defaults to `every_run` (hooked into scripts/lib/step/index.js on every pipeline execution); at or above it, `validate_only` (executed only from `step:validate --write` / chain-end synthesis). 5000 = the plan's own stated default (Fold A-1), pending re-derivation per step at commit 3/4's live re-timing. Not yet CONSUMED by any executor as of commit 1 (schema-shape only; the executor lands at commit 3). |
| `laneway_suite_max_gfa_sqm` | numeric | 120 | 20 – 400 | — | seed | group: Spatial & Massing | Spec 65 §7 (Phase 3) — by-law cap on a laneway suite GFA (m², ~2-storey). CONSUMED by enrich-parcels.js (max-build pass). |
| `laneway_suite_min_lot_sqm` | numeric | 230 | 100 – 2000 | — | seed | group: Spatial & Massing | Spec 65 §7 (Phase 3) — minimum lot area (m²) for a laneway suite. CONSUMED by enrich-parcels.js (max-build pass). |
| `laneway_suite_min_rear_yard_m` | numeric | 5 | 2 – 30 | — | seed | group: Spatial & Massing | Spec 65 §7 (Phase 3) — minimum usable rear-yard depth (m) for a laneway suite. CONSUMED by enrich-parcels.js (max-build pass). |
| `laneway_suite_storeys` | numeric | 2 | 1 – 4 | — | seed | group: Spatial & Massing | Spec 65 §7 (Phase 3) — laneway suite storeys; suite footprint = GFA / storeys for the greenspace test. CONSUMED by enrich-parcels.js (max-build pass). |
| `lead_expiry_days` | numeric | 90 | — (migration-seeded) | — | migration 093 | — (governed by GROUPS only) | TTL in days for claimed_unverified tracked projects before auto-archive |
| `lead_view_retention_days` | numeric | 90 | 7 – 365 | — | seed | group: Data Retention & Privacy | PIPEDA/GDPR retention window in days for lead_views rows; rows older than this are purged daily by the pg_cron job lead_views_retention_purge (Spec 115 §5, migration 233) — retired scripts/purge-lead-views.js (P3-F6, 2026-07-20) previously read this same variable |
| `liar_gate_threshold` | numeric | 0.25 | 0.01 – 1 | — | seed | group: Cost Tuning | Minimum fraction of active permits with cost estimate before the Liar's Gate override triggers (Spec 83 §4) |
| `lifecycle_band_coa_p1_max` | numeric | 80 | 0 – 10000 | — | seed | group: Lifecycle Phase Distribution Bands | Max count for CoA lifecycle_phase=P1 (application received). |
| `lifecycle_band_coa_p1_min` | numeric | 30 | 0 – 10000 | — | seed | group: Lifecycle Phase Distribution Bands | Min count for CoA lifecycle_phase=P1 (application received). Small counts; ±30% tolerance. |
| `lifecycle_band_coa_p2_max` | numeric | 200 | 0 – 10000 | — | seed | group: Lifecycle Phase Distribution Bands | Max count for CoA lifecycle_phase=P2 (committee decision rendered). |
| `lifecycle_band_coa_p2_min` | numeric | 120 | 0 – 10000 | — | seed | group: Lifecycle Phase Distribution Bands | Min count for CoA lifecycle_phase=P2 (committee decision rendered). |
| `lifecycle_band_o1_max` | numeric | 3449 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Max count for lifecycle_phase=O1 (active orphan trade permit). |
| `lifecycle_band_o1_min` | numeric | 2549 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Min count for lifecycle_phase=O1 (active orphan trade permit). Per Spec 84 §7 O-phases scope ONLY to standalone trade permits (HVA/PLB/DRN); BLDs are never orphan post-WF3. |
| `lifecycle_band_o2_max` | numeric | 3329 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Max count for lifecycle_phase=O2 (orphan trade permit, mid-lifecycle). |
| `lifecycle_band_o2_min` | numeric | 2461 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Min count for lifecycle_phase=O2 (orphan trade permit, mid-lifecycle). |
| `lifecycle_band_o3_max` | numeric | 49941 | 0 – 200000 | — | seed | group: Lifecycle Phase Distribution Bands | Max count for lifecycle_phase=O3 (stalled orphan trade permit). |
| `lifecycle_band_o3_min` | numeric | 36913 | 0 – 200000 | — | seed | group: Lifecycle Phase Distribution Bands | Min count for lifecycle_phase=O3 (stalled orphan trade permit, beyond lifecycle_orphan_stall_days threshold). |
| `lifecycle_band_p18_max` | numeric | 123270 | 0 – 500000 | — | seed | group: Lifecycle Phase Distribution Bands | Max count for lifecycle_phase=P18. |
| `lifecycle_band_p18_min` | numeric | 91112 | 0 – 500000 | — | seed | group: Lifecycle Phase Distribution Bands | Min count for lifecycle_phase=P18 (Inspection-status fallback when no detailed inspection records). |
| `lifecycle_band_p19_max` | numeric | 9130 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Max count for lifecycle_phase=P19 (winddown / pre-occupancy). |
| `lifecycle_band_p19_min` | numeric | 6748 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Min count for lifecycle_phase=P19 (winddown / pre-occupancy). |
| `lifecycle_band_p20_max` | numeric | 9951 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Max count for lifecycle_phase=P20 (terminal / occupancy). |
| `lifecycle_band_p20_min` | numeric | 7355 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Min count for lifecycle_phase=P20 (terminal / occupancy). |
| `lifecycle_band_p3_max` | numeric | 970 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Max count for lifecycle_phase=P3 (Intake) before band-violation FAIL. |
| `lifecycle_band_p3_min` | numeric | 716 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Min count for lifecycle_phase=P3 (Intake) before assert-lifecycle-phase-distribution emits a band-violation FAIL. Calibrated against 2026-05-07 snapshot (843) ±15%. |
| `lifecycle_band_p4_max` | numeric | 4695 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Max count for lifecycle_phase=P4 (Examination/review). |
| `lifecycle_band_p4_min` | numeric | 3471 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Min count for lifecycle_phase=P4 (Examination/review) before band-violation FAIL. |
| `lifecycle_band_p5_max` | numeric | 1687 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Max count for lifecycle_phase=P5 (On Hold/deficiency notice). |
| `lifecycle_band_p5_min` | numeric | 1247 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Min count for lifecycle_phase=P5 (On Hold/deficiency notice). |
| `lifecycle_band_p6_max` | numeric | 3370 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Max count for lifecycle_phase=P6 (Ready for issuance / approved). |
| `lifecycle_band_p6_min` | numeric | 2491 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Min count for lifecycle_phase=P6 (Ready for issuance / approved). |
| `lifecycle_band_p7a_max` | numeric | 2367 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Max count for lifecycle_phase=P7a (issued, fresh). |
| `lifecycle_band_p7a_min` | numeric | 1749 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Min count for lifecycle_phase=P7a (issued, fresh — ≤30 days bucket). |
| `lifecycle_band_p7b_max` | numeric | 2914 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Max count for lifecycle_phase=P7b (issued, active). |
| `lifecycle_band_p7b_min` | numeric | 2154 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Min count for lifecycle_phase=P7b (issued, active — 31–90 days bucket). |
| `lifecycle_band_p7c_max` | numeric | 38303 | 0 – 200000 | — | seed | group: Lifecycle Phase Distribution Bands | Max count for lifecycle_phase=P7c (issued, bulk). |
| `lifecycle_band_p7c_min` | numeric | 28311 | 0 – 200000 | — | seed | group: Lifecycle Phase Distribution Bands | Min count for lifecycle_phase=P7c (issued, bulk — >90 days, no inspections recorded). |
| `lifecycle_band_p7d_max` | numeric | 2264 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Max count for lifecycle_phase=P7d (issued, not started). |
| `lifecycle_band_p7d_min` | numeric | 1674 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Min count for lifecycle_phase=P7d (issued, not started). |
| `lifecycle_band_p8_max` | numeric | 21805 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Max count for lifecycle_phase=P8 (revision in progress). |
| `lifecycle_band_p8_min` | numeric | 16117 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Min count for lifecycle_phase=P8 (revision in progress). |
| `lifecycle_band_p9_p17_agg_max` | numeric | 80000 | 0 – 200000 | — | seed | group: Lifecycle Phase Distribution Bands | Max sum of permits in P9–P17. Wide upper bound until inspection ingestion scales up. |
| `lifecycle_band_p9_p17_agg_min` | numeric | 0 | 0 – 100000 | — | seed | group: Lifecycle Phase Distribution Bands | Min sum of permits in P9–P17 (detailed inspection-stage phases). Wide tolerance — current scraper coverage ~5.5%; raise as inspection ingestion scales up. |
| `lifecycle_cross_active_inspection_threshold` | numeric | 500 | 0 – 100000 | `scripts/quality/assert-lifecycle-phase-distribution.js` | seed | group: Lifecycle Phase Distribution Bands | Max count of permits with enriched_status=Active Inspection NOT in P9-P18/O1-O3 before the assertion FAILs. |
| `lifecycle_cross_issued_threshold` | numeric | 500 | 0 – 100000 | `scripts/quality/assert-lifecycle-phase-distribution.js` | seed | group: Lifecycle Phase Distribution Bands | Max count of permits with enriched_status=Permit Issued NOT in P7a/b/c/d/P8/P18/O1-O3 before the assertion FAILs. |
| `lifecycle_cross_stalled_threshold` | numeric | 1000 | 0 – 100000 | `scripts/quality/assert-lifecycle-phase-distribution.js` | seed | group: Lifecycle Phase Distribution Bands | Max count of permits with enriched_status=Stalled but lifecycle_stalled=false before the assertion FAILs. Detects classifier ↔ scraper signal divergence. |
| `lifecycle_inspection_stall_days` | numeric | 180 | 1 – 3650 | — | seed | group: Lifecycle Ledger | Days since last inspection activity before lifecycle_stalled flag is set |
| `lifecycle_issued_stall_days` | numeric | 730 | 1 – 3650 | — | seed | group: Lifecycle Ledger | Days since Permit Issued (no passed inspection) before lifecycle_stalled flag is set |
| `lifecycle_live_status_null_warn_count` | numeric | 50 | 0 – 100000 | `scripts/quality/assert-lifecycle-phase-distribution.js` | seed | group: Lifecycle Phase Distribution Bands | WARN threshold for the additive breakout counters in assert-lifecycle-phase-distribution: live_status_null_count (NULL phase + non-dead status + matched_rule IS NULL) and never_classified_count (lifecycle_classified_at IS NULL). Both are dirty-queue drain-lag signals that clear on the next in-chain classify run (per docs/reports/pipeline-validation/2026-07-07-lifecycle-null-trace.md). Default 50 sits above steady-state noise (~1) and below a single day's new-permit load (~500). |
| `lifecycle_orphan_stall_days` | numeric | 180 | 1 – 3650 | — | seed | group: Lifecycle Ledger | Days since issued for an orphan permit (no parent BLD/CMB) with no passed inspection before it degrades from O2 (active) to O3 (stalled orphan) |
| `lifecycle_p7a_max_days` | numeric | 30 | 1 – 365 | — | seed | group: Lifecycle Ledger | Max days since Permit Issued for P7a (fresh-issued) bucket; above this is P7b |
| `lifecycle_p7b_max_days` | numeric | 90 | 1 – 365 | — | seed | group: Lifecycle Ledger | Max days since Permit Issued for P7b (active-issued) bucket; above this is P7c |
| `lifecycle_seq_band_1_max` | numeric | 33 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=1 (Received). Catalog snapshot rows_count=10; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_1_min` | numeric | 7 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=1 (Received). Catalog snapshot rows_count=10; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_10_max` | numeric | 444 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=10 (Conditional Consent). Catalog snapshot rows_count=326; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_10_min` | numeric | 228 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=10 (Conditional Consent). Catalog snapshot rows_count=326; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_100_max` | numeric | 22 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=100 (Refused). Catalog snapshot rows_count=1; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_100_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=100 (Refused). Catalog snapshot rows_count=1; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_101_max` | numeric | 179 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=101 (Abandoned). Catalog snapshot rows_count=122; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_101_min` | numeric | 85 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=101 (Abandoned). Catalog snapshot rows_count=122; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_102_max` | numeric | 84 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=102 (Application Withdrawn). Catalog snapshot rows_count=49; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_102_min` | numeric | 34 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=102 (Application Withdrawn). Catalog snapshot rows_count=49; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_103_max` | numeric | 32 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=103 (Not Accepted). Catalog snapshot rows_count=9; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_103_min` | numeric | 6 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=103 (Not Accepted). Catalog snapshot rows_count=9; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_104_max` | numeric | 23 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=104 (Permit Revoked). Catalog snapshot rows_count=2; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_104_min` | numeric | 1 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=104 (Permit Revoked). Catalog snapshot rows_count=2; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_105_max` | numeric | 23 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=105 (Revoked). Catalog snapshot rows_count=2; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_105_min` | numeric | 1 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=105 (Revoked). Catalog snapshot rows_count=2; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_106_max` | numeric | 44 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=106 (Work Suspended). Catalog snapshot rows_count=18; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_106_min` | numeric | 12 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=106 (Work Suspended). Catalog snapshot rows_count=18; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_107_max` | numeric | 41 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=107 (VIOLATION). Catalog snapshot rows_count=16; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_107_min` | numeric | 11 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=107 (VIOLATION). Catalog snapshot rows_count=16; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_108_max` | numeric | 30 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=108 (Order Issued). Catalog snapshot rows_count=7; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_108_min` | numeric | 4 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=108 (Order Issued). Catalog snapshot rows_count=7; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_109_max` | numeric | 22 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=109 (Cancelled). Catalog snapshot rows_count=1; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_109_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=109 (Cancelled). Catalog snapshot rows_count=1; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_11_max` | numeric | 340 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=11 (Approved). Catalog snapshot rows_count=246; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_11_min` | numeric | 172 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=11 (Approved). Catalog snapshot rows_count=246; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_110_max` | numeric | 22 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=110 (Follow-up Required). Catalog snapshot rows_count=1; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_110_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=110 (Follow-up Required). Catalog snapshot rows_count=1; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_12_max` | numeric | 741 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=12 (Approved + Conditions). Catalog snapshot rows_count=554; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_12_min` | numeric | 387 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=12 (Approved + Conditions). Catalog snapshot rows_count=554; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_13_max` | numeric | 97 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=13 (Refused). Catalog snapshot rows_count=59; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_13_min` | numeric | 41 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=13 (Refused). Catalog snapshot rows_count=59; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_14_max` | numeric | 22 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=14 (Final & Binding). Catalog snapshot rows_count=1; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_14_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=14 (Final & Binding). Catalog snapshot rows_count=1; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_15_max` | numeric | 52 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=15 (Awaiting Expiry). Catalog snapshot rows_count=24; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_15_min` | numeric | 16 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=15 (Awaiting Expiry). Catalog snapshot rows_count=24; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_16_max` | numeric | 22 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=16 (Appealed). Catalog snapshot rows_count=1; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_16_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=16 (Appealed). Catalog snapshot rows_count=1; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_17_max` | numeric | 472 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=17 (TLAB Appeal). Catalog snapshot rows_count=347; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_17_min` | numeric | 242 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=17 (TLAB Appeal). Catalog snapshot rows_count=347; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_18_max` | numeric | 304 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=18 (OMB Appeal). Catalog snapshot rows_count=218; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_18_min` | numeric | 152 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=18 (OMB Appeal). Catalog snapshot rows_count=218; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_19_max` | numeric | 1196 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=19 (Withdrawn). Catalog snapshot rows_count=904; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_19_min` | numeric | 632 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=19 (Withdrawn). Catalog snapshot rows_count=904; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_2_max` | numeric | 383 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=2 (Accepted). Catalog snapshot rows_count=279; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_2_min` | numeric | 195 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=2 (Accepted). Catalog snapshot rows_count=279; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_20_max` | numeric | 22 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=20 (Cancelled). Catalog snapshot rows_count=1; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_20_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=20 (Cancelled). Catalog snapshot rows_count=1; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_21_max` | numeric | 31 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=21 (Complete). Catalog snapshot rows_count=8; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_21_min` | numeric | 5 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=21 (Complete). Catalog snapshot rows_count=8; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_22_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=22 (Closed). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_22_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=22 (Closed). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_23_max` | numeric | 22 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=23 (Request Received). Catalog snapshot rows_count=1; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_23_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=23 (Request Received). Catalog snapshot rows_count=1; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_24_max` | numeric | 304 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=24 (Application Received). Catalog snapshot rows_count=218; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_24_min` | numeric | 152 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=24 (Application Received). Catalog snapshot rows_count=218; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_25_max` | numeric | 625 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=25 (Application Acceptable). Catalog snapshot rows_count=465; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_25_min` | numeric | 325 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=25 (Application Acceptable). Catalog snapshot rows_count=465; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_26_max` | numeric | 695 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=26 (Open). Catalog snapshot rows_count=519; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_26_min` | numeric | 363 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=26 (Open). Catalog snapshot rows_count=519; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_27_max` | numeric | 52 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=27 (Active). Catalog snapshot rows_count=24; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_27_min` | numeric | 16 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=27 (Active). Catalog snapshot rows_count=24; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_28_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=28 (Not Started). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_28_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=28 (Not Started). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_29_max` | numeric | 140 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=29 (Not Started - Express). Catalog snapshot rows_count=92; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_29_min` | numeric | 64 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=29 (Not Started - Express). Catalog snapshot rows_count=92; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_3_max` | numeric | 91 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=3 (Notice Prep). Catalog snapshot rows_count=54; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_3_min` | numeric | 37 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=3 (Notice Prep). Catalog snapshot rows_count=54; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_30_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=30 (Under Review). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_30_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=30 (Under Review). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_31_max` | numeric | 59 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=31 (Examination). Catalog snapshot rows_count=30; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_31_min` | numeric | 21 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=31 (Examination). Catalog snapshot rows_count=30; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_32_max` | numeric | 95 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=32 (Plan Review Complete). Catalog snapshot rows_count=57; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_32_min` | numeric | 39 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=32 (Plan Review Complete). Catalog snapshot rows_count=57; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_33_max` | numeric | 23 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=33 (Consultation Completed). Catalog snapshot rows_count=2; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_33_min` | numeric | 1 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=33 (Consultation Completed). Catalog snapshot rows_count=2; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_34_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=34 (Examiner's Notice Sent). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_34_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=34 (Examiner's Notice Sent). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_35_max` | numeric | 22 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=35 (Notice Sent). Catalog snapshot rows_count=1; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_35_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=35 (Notice Sent). Catalog snapshot rows_count=1; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_36_max` | numeric | 173 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=36 (Deficiency Notice). Catalog snapshot rows_count=117; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_36_min` | numeric | 81 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=36 (Deficiency Notice). Catalog snapshot rows_count=117; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_37_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=37 (On Hold). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_37_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=37 (On Hold). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_38_max` | numeric | 26 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=38 (On Hold (variant)). Catalog snapshot rows_count=4; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_38_min` | numeric | 2 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=38 (On Hold (variant)). Catalog snapshot rows_count=4; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_39_max` | numeric | 587 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=39 (Response Received). Catalog snapshot rows_count=436; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_39_min` | numeric | 305 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=39 (Response Received). Catalog snapshot rows_count=436; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_4_max` | numeric | 117 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=4 (Notice Prepared). Catalog snapshot rows_count=74; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_4_min` | numeric | 51 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=4 (Notice Prepared). Catalog snapshot rows_count=74; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_40_max` | numeric | 63 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=40 (Parent Folder Pending). Catalog snapshot rows_count=33; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_40_min` | numeric | 23 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=40 (Parent Folder Pending). Catalog snapshot rows_count=33; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_41_max` | numeric | 50 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=41 (Approved). Catalog snapshot rows_count=23; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_41_min` | numeric | 16 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=41 (Approved). Catalog snapshot rows_count=23; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_42_max` | numeric | 323 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=42 (Ready for Issuance). Catalog snapshot rows_count=233; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_42_min` | numeric | 163 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=42 (Ready for Issuance). Catalog snapshot rows_count=233; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_43_max` | numeric | 24 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=43 (Forwarded for Issuance). Catalog snapshot rows_count=3; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_43_min` | numeric | 2 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=43 (Forwarded for Issuance). Catalog snapshot rows_count=3; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_44_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=44 (Issuance Pending). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_44_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=44 (Issuance Pending). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_45_max` | numeric | 33 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=45 (Agreement in Progress). Catalog snapshot rows_count=10; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_45_min` | numeric | 7 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=45 (Agreement in Progress). Catalog snapshot rows_count=10; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_46_max` | numeric | 26 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=46 (Licence Issued). Catalog snapshot rows_count=4; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_46_min` | numeric | 2 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=46 (Licence Issued). Catalog snapshot rows_count=4; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_47_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=47 (Permit Issued). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_47_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=47 (Permit Issued). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_48_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=48 (Work Not Started). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_48_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=48 (Work Not Started). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_49_max` | numeric | 24 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=49 (Extension Granted). Catalog snapshot rows_count=3; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_49_min` | numeric | 2 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=49 (Extension Granted). Catalog snapshot rows_count=3; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_5_max` | numeric | 174 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=5 (Tentatively Scheduled). Catalog snapshot rows_count=118; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_5_min` | numeric | 82 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=5 (Tentatively Scheduled). Catalog snapshot rows_count=118; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_50_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=50 (Inspection). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_50_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=50 (Inspection). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_51_max` | numeric | 22 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=51 (Forward to Inspector). Catalog snapshot rows_count=1; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_51_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=51 (Forward to Inspector). Catalog snapshot rows_count=1; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_52_max` | numeric | 22 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=52 (Rescheduled). Catalog snapshot rows_count=1; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_52_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=52 (Rescheduled). Catalog snapshot rows_count=1; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_53_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=53 (Site Grading). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_53_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=53 (Site Grading). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_54_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=54 (Excavation/Shoring). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_54_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=54 (Excavation/Shoring). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_55_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=55 (Demolition). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_55_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=55 (Demolition). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_56_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=56 (Footings/Foundations). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_56_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=56 (Footings/Foundations). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_57_max` | numeric | 23 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=57 (Foundation). Catalog snapshot rows_count=2; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_57_min` | numeric | 1 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=57 (Foundation). Catalog snapshot rows_count=2; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_58_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=58 (Structural Framing). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_58_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=58 (Structural Framing). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_59_max` | numeric | 1168 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=59 (HVAC Rough-in). Catalog snapshot rows_count=883; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_59_min` | numeric | 618 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=59 (HVAC Rough-in). Catalog snapshot rows_count=883; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_6_max` | numeric | 433 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=6 (Hearing Scheduled). Catalog snapshot rows_count=317; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_6_min` | numeric | 221 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=6 (Hearing Scheduled). Catalog snapshot rows_count=317; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_60_max` | numeric | 1175 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=60 (Water Service). Catalog snapshot rows_count=888; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_60_min` | numeric | 621 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=60 (Water Service). Catalog snapshot rows_count=888; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_61_max` | numeric | 1205 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=61 (Water Distribution). Catalog snapshot rows_count=911; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_61_min` | numeric | 637 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=61 (Water Distribution). Catalog snapshot rows_count=911; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_62_max` | numeric | 1203 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=62 (Drain/Waste/Vents). Catalog snapshot rows_count=910; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_62_min` | numeric | 637 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=62 (Drain/Waste/Vents). Catalog snapshot rows_count=910; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_63_max` | numeric | 1177 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=63 (Sewers/Drains/Sewage). Catalog snapshot rows_count=890; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_63_min` | numeric | 623 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=63 (Sewers/Drains/Sewage). Catalog snapshot rows_count=890; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_64_max` | numeric | 1176 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=64 (Fire Service). Catalog snapshot rows_count=889; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_64_min` | numeric | 622 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=64 (Fire Service). Catalog snapshot rows_count=889; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_65_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=65 (Fire Access Routes). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_65_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=65 (Fire Access Routes). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_66_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=66 (Fire Protection Systems). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_66_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=66 (Fire Protection Systems). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_67_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=67 (Insulation/Vapour Barrier). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_67_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=67 (Insulation/Vapour Barrier). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_68_max` | numeric | 22 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=68 (Insulation). Catalog snapshot rows_count=1; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_68_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=68 (Insulation). Catalog snapshot rows_count=1; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_69_max` | numeric | 22 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=69 (Insulation & AirBarrier). Catalog snapshot rows_count=1; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_69_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=69 (Insulation & AirBarrier). Catalog snapshot rows_count=1; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_7_max` | numeric | 22 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=7 (Hearing Rescheduled). Catalog snapshot rows_count=1; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_7_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=7 (Hearing Rescheduled). Catalog snapshot rows_count=1; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_70_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=70 (Fire Separations). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_70_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=70 (Fire Separations). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_71_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=71 (Interior Final). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_71_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=71 (Interior Final). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_72_max` | numeric | 1209 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=72 (Plumbing Final). Catalog snapshot rows_count=914; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_72_min` | numeric | 639 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=72 (Plumbing Final). Catalog snapshot rows_count=914; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_73_max` | numeric | 1168 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=73 (HVAC Final). Catalog snapshot rows_count=883; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_73_min` | numeric | 618 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=73 (HVAC Final). Catalog snapshot rows_count=883; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_74_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=74 (Exterior Final). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_74_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=74 (Exterior Final). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_75_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=75 (Occupancy). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_75_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=75 (Occupancy). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_76_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=76 (Final Inspection). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_76_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=76 (Final Inspection). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_77_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=77 (Pool Suction/Gravity). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_77_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=77 (Pool Suction/Gravity). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_78_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=78 (Pool Circulation). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_78_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=78 (Pool Circulation). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_79_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=79 (Repair/Retrofit). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_79_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=79 (Repair/Retrofit). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_8_max` | numeric | 400 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=8 (Postponed). Catalog snapshot rows_count=292; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_8_min` | numeric | 204 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=8 (Postponed). Catalog snapshot rows_count=292; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_80_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=80 (Change of Use). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_80_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=80 (Change of Use). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_81_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=81 (System). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_81_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=81 (System). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_82_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=82 (Security Device). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_82_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=82 (Security Device). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_83_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=83 (Tent/Portable Classroom). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_83_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=83 (Tent/Portable Classroom). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_84_max` | numeric | 22 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=84 (Final Interior). Catalog snapshot rows_count=1; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_84_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=84 (Final Interior). Catalog snapshot rows_count=1; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_85_max` | numeric | 22 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=85 (HVAC Permit?). Catalog snapshot rows_count=1; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_85_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=85 (HVAC Permit?). Catalog snapshot rows_count=1; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_86_max` | numeric | 22 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=86 (Survey). Catalog snapshot rows_count=1; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_86_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=86 (Survey). Catalog snapshot rows_count=1; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_87_max` | numeric | 22 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=87 (Survey Submitted?). Catalog snapshot rows_count=1; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_87_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=87 (Survey Submitted?). Catalog snapshot rows_count=1; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_88_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=88 (Revision Issued). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_88_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=88 (Revision Issued). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_89_max` | numeric | 56 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=89 (Revised). Catalog snapshot rows_count=27; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_89_min` | numeric | 18 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=89 (Revised). Catalog snapshot rows_count=27; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_9_max` | numeric | 371 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=9 (Deferred). Catalog snapshot rows_count=270; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_9_min` | numeric | 189 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=9 (Deferred). Catalog snapshot rows_count=270; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_90_max` | numeric | 49 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=90 (Order Complied). Catalog snapshot rows_count=22; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_90_min` | numeric | 15 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=90 (Order Complied). Catalog snapshot rows_count=22; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_91_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=91 (Pending Closed). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_91_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=91 (Pending Closed). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_92_max` | numeric | 655 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=92 (Pending Cancellation). Catalog snapshot rows_count=488; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_92_min` | numeric | 341 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=92 (Pending Cancellation). Catalog snapshot rows_count=488; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_93_max` | numeric | 22 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=93 (Inspection Request to Cancel). Catalog snapshot rows_count=1; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_93_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=93 (Inspection Request to Cancel). Catalog snapshot rows_count=1; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_94_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=94 (Revocation Pending). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_94_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=94 (Revocation Pending). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_95_max` | numeric | 22 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=95 (Revocation Notice Sent). Catalog snapshot rows_count=1; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_95_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=95 (Revocation Notice Sent). Catalog snapshot rows_count=1; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_96_max` | numeric |  | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=96 (Closed). Catalog snapshot rows_count=NULL; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_96_min` | numeric | 0 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=96 (Closed). Catalog snapshot rows_count=NULL; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_97_max` | numeric | 28 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=97 (File Closed). Catalog snapshot rows_count=6; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_97_min` | numeric | 4 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=97 (File Closed). Catalog snapshot rows_count=6; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_98_max` | numeric | 23 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=98 (Permit Issued/Close File). Catalog snapshot rows_count=2; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_98_min` | numeric | 1 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=98 (Permit Issued/Close File). Catalog snapshot rows_count=2; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_99_max` | numeric | 1266 | 0 – 999999 | — | seed | group: Lifecycle Seq Bands | Max row count for lifecycle_seq=99 (Refusal Notice). Catalog snapshot rows_count=958; 2-branch continuous formula (NULL=no upper bound, INFO-only). Recalibrated in E.5. |
| `lifecycle_seq_band_99_min` | numeric | 670 | 0 – 99999 | — | seed | group: Lifecycle Seq Bands | Min row count for lifecycle_seq=99 (Refusal Notice). Catalog snapshot rows_count=958; 2-branch continuous formula. Recalibrated in E.5. |
| `lifecycle_seq_band_promote_to_fail_band_violation` | numeric | 0 | 0 – 1 | `scripts/quality/assert-lifecycle-phase-distribution.js` | seed | group: Lifecycle Phase Distribution Bands | Phase E.5 gate for `band_violation` kind. 0=WARN (E.4 default; seqBandsWarn++); 1=FAIL (E.5 promotion; seqBandsFailing++ → verdict FAIL → pipeline halt). Canonical regression-detection gate. See Spec 84 §3.4 pre-promotion checklist. |
| `lifecycle_seq_band_promote_to_fail_expected_data_missing` | numeric | 0 | 0 – 1 | `scripts/quality/assert-lifecycle-phase-distribution.js` | seed | group: Lifecycle Phase Distribution Bands | Phase E.5 gate for `expected_data_missing` kind (band has min > 0 but zero observed rows — possible data deletion or classifier-skip). 0=WARN (default); 1=FAIL (after verifying no structurally-absent seqs remain; see Spec 84 §3.4 structural-absence resolution path). |
| `lifecycle_seq_band_promote_to_fail_no_band_configured` | numeric | 0 | 0 – 1 | `scripts/quality/assert-lifecycle-phase-distribution.js` | seed | group: Lifecycle Phase Distribution Bands | Phase E.5 gate for `no_band_configured` kind (seq present in data but no band loaded — operator config gap). 0=WARN (default); 1=FAIL (rare). See Spec 84 §3.4. |
| `lifecycle_seq_unclassified_max` | numeric | 5000 | 0 – 100000 | `scripts/quality/assert-lifecycle-phase-distribution.js` | seed | group: Lifecycle Phase Distribution Bands | Max row count where lifecycle_seq IS NULL on permits or coa_applications. WARN threshold (E.4); Phase D + E.2 first-run state expected to violate. Tighten via E.5 after ramp-up. |
| `lifecycle_status_history_retention_days` | numeric | 1825 | 730 – 3650 | — | seed | group: Lifecycle Ledger | Days to retain rows in lifecycle_status_history (default 1825 = 5 years per Spec 86 §1). Long retention supports forecast cohort segmentation by traversal pattern across the median 1,078-day CoA→permit lag. Hard floor: must be ≥ 730 to support typical CoA cohort learning. Seeded in DB by mig 136. |
| `lifecycle_unclassified_max` | numeric | 100 | 0 – 10000 | `scripts/quality/assert-lifecycle-phase-distribution.js` | seed | group: Scraper & Network Health | Maximum number of non-terminal permits/CoAs with a NULL lifecycle_phase before assert-lifecycle-phase-distribution emits a FAIL (strongest correctness gate) |
| `link_massing_centroid_confidence` | numeric | 0.95 | 0 – 1 | — | seed | group: Spatial & Massing | Confidence written to parcel_buildings.confidence for a link made by the building-centroid-in-parcel predicate. CONSUMED by link_massing (scripts/lib/compute/link-massing.js classifyMatches) and read downstream by enrich-parcels as link_confidence. Pre-externalization the literal lived at two sites across two code paths that had to agree. |
| `link_massing_link_rate_fail_pct` | numeric | 50 | 0 – 100 | — | seed | group: Spatial & Massing | Minimum cumulative link rate (%) — linked parcels over parcels with a centroid — below which the massing link verdict FAILs. CONSUMED by link_massing via checks[].limit_from_config; the check reports the UNLINKED complement so the declared `pct <=` limit form expresses a floor. Pre-externalization this was the bare literal 50 in `massingLinkRate >= 50 ? …` PLUS a duplicated '>= 50%' render string, with zero registered variables. Live headroom is large (99.71% linked), which is itself a finding — the floor cannot fire before the damage. |
| `link_massing_nearest_confidence` | numeric | 0.6 | 0 – 1 | — | seed | group: Spatial & Massing | Confidence written to parcel_buildings.confidence for a link made by the bounded nearest-footprint fallback. CONSUMED by link_massing (scripts/lib/compute/link-massing.js classifyFallback). 21.3% of linked parcels carry only this value, so it is the marker of the lower-trust link population the cost model must be able to distinguish. |
| `link_neighbourhoods_link_rate_fail_pct` | numeric | 50 | 0 – 100 | — | seed | group: Neighbourhood Linking | Minimum CUMULATIVE neighbourhood link rate (%) below which the run FAILs — the catastrophic-collapse arm, declared as its OWN check row (link_rate_floor) rather than a computed severity inside link_rate, because Spec 124 Rule 10 derives the verdict from rows and two bounds on one metric are two rows. Externalizes the bare literal 50 in the pre-conversion `nhoodLinkRate < 50 ? 'FAIL' : 'WARN'`. Live headroom is large (the rate is 94.83%), which is the same standing finding every link-rate floor in this registry carries: the floor cannot fire before the damage. Declared form `pct >= 50`. CONSUMED by link_neighbourhoods via checks[].limit_from_config. |
| `link_neighbourhoods_link_rate_warn_pct` | numeric | 95 | 0 – 100 | — | seed | group: Neighbourhood Linking | Minimum CUMULATIVE neighbourhood link rate (%) below which the link_rate check WARNs. The declared form is `pct >= 95` — the FLOOR form the MATCHER pilot added to verdict.js's PCT_RE. ⚠️ Do NOT "fix" this to a complement (e.g. 5): two older LINK steps store their own link-rate bound as an UNLINKED CEILING because their rows use `pct <=`, which was the only direction available when they were written. This row uses `pct >=`, so the value here is the link-rate floor itself, exactly as the pre-conversion literal spelled it. CONSUMED by link_neighbourhoods via checks[].limit_from_config. Externalizes the bare literal 95 in the pre-conversion `nhoodLinkRate >= 95 ? 'PASS' : ...` at scripts/link-neighbourhoods.js:346, PLUS the duplicated '>= 95%' render string beside it. MEASURED AT CONVERSION 2026-09-16 and shipped RED ON PURPOSE: the rate is 94.83% (240,947 of 254,082 permits) because the denominator is ALL permits while 22,152 of them carry no coordinates and this step's own eligibility predicate can therefore never touch one; over the 231,930 coordinate-bearing permits the same numerator reads 100.00%. 11 of the 12 recorded runs carrying this audit row (2026-03-21 to 2026-07-17) verdicted WARN on it. That denominator defect is PINNED, not fixed here (Spec 123 §3.1 — changing it inside the conversion would be an undeclared differential diff); it is ledgered LN-D7 and carries its own retighten_when on the check. |
| `link_neighbourhoods_no_match_warn_count` | numeric | 0 | 0 – 1000000 | — | seed | group: Neighbourhood Linking | Maximum number of geocoded permits a run may walk whose point falls inside NO neighbourhood polygon before link_neighbourhoods's no_neighbourhood_match check WARNs. CONSUMED by link_neighbourhoods via checks[].limit_from_config, substituted into `viol <= 0`. Pre-conversion this population had NO bound at all — it was an INFO row (scripts/link-neighbourhoods.js:347) beside a `-1` sentinel write that made it write-once, so it vanished from every later run. With the sentinel retired (LN-D1) the same permits are re-walked every run, so the counter stops being descriptive and becomes the standing cost signal, which Rule 10's R-H addendum makes WARN-with-a-retighten-condition, never INFO-by-taste. The default is 0 because 0 is what it MEASURES today (2026-09-16: every one of the 231,930 coordinate-bearing permits is linked, so the tail is genuinely empty) — not because zero is assumed. on_invalid is `clamp`, not `fail`: this bounds a WARN-only observational counter that moves no write and no verdict beyond its own row, so an out-of-range admin edit should degrade the signal rather than halt two chains. |
| `link_parcel_addresses_batch_size` | numeric | 1000 | 100 – 10000 | — | seed | group: Parcel-Address Bridge | The keyset-paginated batch size for the parcel <-> address_points spatial bridge's INSERT...SELECT...JOIN ST_Within batch loop. CONSUMED by link_parcel_addresses (scripts/lib/step/index.js runMaterializePhase). Pre-externalization this was the bare literal BATCH_SIZE = 1000. Not verdict-affecting (on_invalid: clamp) — a bad value changes pacing, never correctness. |
| `link_parcel_addresses_fanout_warn_condo` | numeric | 400 | 1 – 1000 | — | seed | group: Parcel-Address Bridge | Per-parcel address-point fan-out ceiling for CONDO parcels (a large condo complex's common-elements parcel legitimately contains many unit entrances); link_parcel_addresses's parcel_fanout_condo_outliers check WARNs on the count of CONDO parcels exceeding it. CONSUMED via checks[].limit_from_config. NEW this pilot. Measured live max fan-out 346 (p99 80), comfortably under the default. |
| `link_parcel_addresses_fanout_warn_noncondo` | numeric | 20 | 1 – 1000 | — | seed | group: Parcel-Address Bridge | Per-parcel address-point fan-out ceiling for non-CONDO parcels; link_parcel_addresses's parcel_fanout_outliers check WARNs on the count of non-CONDO parcels exceeding it. CONSUMED via checks[].limit_from_config. NEW this pilot (Fold B, Cross-read Adversary AMEND, 2026-08-29) — replaces a dropped 5-id allow-list with an honest aggregate. Measured live 130 non-CONDO parcels exceed the default TODAY, so the WARN fires by construction (R-H: a standing non-zero population is WARN, never FAIL, never silently PASSed). |
| `link_parcel_addresses_fanout_warn_rd_rs` | numeric | 15 | 1 – 1000 | — | seed | group: Parcel-Address Bridge | Per-parcel address-point fan-out ceiling for parcels zoned RD or RS (low-density residential — detached/semi-detached, where a large fan-out is least architecturally expected); link_parcel_addresses's parcel_fanout_rd_rs_outliers check WARNs on the count of RD/RS parcels exceeding it (same viol<=N double-duty shape as the noncondo/condo ceilings, LPA-D3). CONSUMED via checks[].limit_from_config. LPA-D5 (WF3-B, 2026-08-29) — the declared retighten target for parcel_fanout_outliers' (T4) flat, zone-blind 20 ceiling. Measured live 13 RD/RS parcels exceed fan-out 15 (of 258,596 RD/RS-zoned parcels; RD max 35, RS max 64). |
| `link_parcel_addresses_no_address_warn_pct` | numeric | 50 | 0 – 100 | — | seed | group: Parcel-Address Bridge | Maximum fraction (%) of geom-bearing parcels with zero linked address points before link_parcel_addresses's parcels_with_no_address_pct check WARNs. CONSUMED via checks[].limit_from_config (a pct <= ceiling form). Pre-externalization this was the bare literal 0.50 in `noAddressFraction >= 0.50 ? WARN : PASS` (Independent IMPL I1 fold recalibrated it from an original 10% ceiling that would WARN on every clean run — PI-2's Poisson-like distribution implies ~37% of parcels legitimately have zero address points). Measured live 3.85%, well under the ceiling. |
| `link_parcel_addresses_no_parcel_warn_pct` | numeric | 5 | 0 – 100 | — | seed | group: Parcel-Address Bridge | Maximum fraction (%) of geom-bearing address points with zero linked parcel before link_parcel_addresses's address_points_with_no_parcel_pct check WARNs. CONSUMED via checks[].limit_from_config. Pre-externalization this was the bare literal 0.05. Measured live 2.69%, under the ceiling. |
| `link_parcel_addresses_structure_link_rate_warn_pct` | numeric | 85 | 0 – 100 | — | seed | group: Parcel-Address Bridge | Minimum fraction (%) of geom-bearing Structure-class (address_points.address_class_desc = 'Structure') address points that must be linked before link_parcel_addresses's structure_class_link_rate_warn check WARNs. CONSUMED via checks[].limit_from_config (a pct >= floor form, R-N precedent). LPA-D5 (WF3-B, 2026-08-29): the class-blind parcel_link_rate_pct average hides that the 4-class distribution ranges 71.5-98.0%; Structure is the class this step's downstream consumers (link-parcels.js Strategy 1a, link-coa-to-parcels.js Tier 1a) most depend on. Measured live 87.96%. |
| `link_parcels_confidence_address_points_exact` | numeric | 0.97 | 0 – 1 | — | seed | group: Parcel Linking | T1 (LP-D4, pilot 7, 2026-08-30) — confidence for an address_points-bridge exact match (Strategy 1a, link-parcels). Pre-externalization this was the bare literal 0.97 in the batch CTE's address_points_exact tier. |
| `link_parcels_confidence_exact_address` | numeric | 0.95 | 0 – 1 | — | seed | group: Parcel Linking | T2 (LP-D4, pilot 7, 2026-08-30) — confidence for a legacy parcels-table exact address match (Strategy 1b, link-parcels). Pre-externalization this was the bare literal 0.95 in the batch CTE's exact tier. |
| `link_parcels_confidence_name_only` | numeric | 0.8 | 0 – 1 | — | seed | group: Parcel Linking | T4 (LP-D4, pilot 7, 2026-08-30) — confidence for a name-only address match (Strategy 2, link-parcels). Pre-externalization this was the bare literal 0.80 in the batch CTE's name_only tier. |
| `link_parcels_confidence_spatial_polygon` | numeric | 0.9 | 0 – 1 | — | seed | group: Parcel Linking | T3 (LP-D4, pilot 7, 2026-08-30) — confidence for a spatial polygon-containment match (Strategy 3 Step 1, ST_Contains, link-parcels) — UNCHANGED, already geometry-correct, untouched by THE FIX. Pre-externalization this was the bare literal 0.90. |
| `link_parcels_link_rate_warn_pct` | numeric | 25 | 0 – 100 | — | seed | group: Parcel Linking | T5 (LP-D4, pilot 7, 2026-08-30) — maximum UNLINKED cumulative percentage (100 - link rate) above which link_parcels's own link_rate check WARNs. Stored as the UNLINKED ceiling (25), NOT the link-rate floor (75) the old code's own literal used — scripts/lib/step/verdict.js's `limit_from_config` substitutes the RAW config value into the check's declared `pct <=` form with NO transform, and `pct >=` is explicitly documented there as unimplemented/unevaluable, so a `pct <=` bound requires the config value itself to already be in complement terms (mirrors link-massing.js's own link_rate convention exactly — its own config is likewise the unlinked ceiling, just numerically symmetric at 50 in that step's case, which hides the same requirement). Pre-externalization this step's own literal was `parcelLinkRate >= 75 ? 'PASS' : 'WARN'` — the SAME underlying threshold (75% link rate), expressed here as its complement (100-75=25) to fit the library's mechanism. Flagged explicitly in the pilot 7 report as a deviation from the plan's own 'default 75' text, for a hard architectural reason, not an oversight. |
| `link_wsib_entity_fanin_warn` | numeric | 20 | 2 – 1000 | — | seed | group: WSIB Matching | The maximum wsib_registry rows any single entity may link to before link_wsib's entity_fanin_warn check WARNs. CONSUMED by link_wsib via checks[].limit_from_config. Fires immediately on the known-bad population (171 magnet entities, worst MDK CONSTRUCTION at 2,118) — intended, not a defect (Fold B ACCEPTED ruling, C1 pilot 4). |
| `link_wsib_link_rate_warn_pct` | numeric | 5 | 0 – 100 | — | seed | group: WSIB Matching | Minimum entity-level link rate (%) — entities with >=1 linked wsib_registry row (entities.is_wsib_registered=true) over total entities — below which link_wsib's link_rate_warn check WARNs. CONSUMED by link_wsib via checks[].limit_from_config (a pct >= floor form). Pre-externalization this was the bare literal 5 in `linkRate >= 5 ? PASS : WARN`, with zero registered variables (LW-D1, the P4 violation C1 pilot 4 closes). LW-D18 (2026-08-29) re-ruled the denominator from wsib_registry ROWS to ENTITIES (a magnet entity's hundreds of contaminated rows no longer inflate the ratio); default kept at 5 — measured entity-level truth post-LW-D18 sits well above the floor, giving genuine WARN headroom (see docs/reports/defect-ledger.md LW-D18 for the exact measured figure). |
| `link_wsib_tier1_confidence` | numeric | 0.95 | 0 – 1 | — | seed | group: WSIB Matching | Confidence written to wsib_registry.match_confidence for a link made by Tier 1's exact trade-name match. CONSUMED by link_wsib (scripts/lib/compute/link-wsib.js buildTierSql). Pre-externalization the literal lived at two sites (the UPDATE + the entities/copyContacts scope predicate). |
| `link_wsib_tier2_confidence` | numeric | 0.9 | 0 – 1 | — | seed | group: WSIB Matching | Confidence written to wsib_registry.match_confidence for a link made by Tier 2's exact legal-name match. CONSUMED by link_wsib (scripts/lib/compute/link-wsib.js buildTierSql). |
| `link_wsib_tier3_confidence` | numeric | 0.6 | 0 – 1 | — | seed | group: WSIB Matching | Confidence written to wsib_registry.match_confidence for a link made by Tier 3's pg_trgm fuzzy match. CONSUMED by link_wsib (scripts/lib/compute/link-wsib.js buildTierSql). 61.9% of tier-3 links fail today's article-stripping predicate (d704a447) — the population A-7's tier-3 repair (LG-16) exists to correct. |
| `link_wsib_tier3_full_max_iterations` | numeric | 20 | 1 – 100 | — | seed | group: WSIB Matching | Bounds the mode-full tier-3 repair's convergence loop (A-7/LG-16): TIER3_SELECT is capped LIMIT 1000/invocation, so a single pass cannot repair the full ~5,515-row clean population. CONSUMED by link_wsib (the runCascadePhase mode-full branch, scripts/lib/step/index.js). Exhaustion is WARN (tier3_full_not_converged), never FAIL, per R-H. |
| `link_wsib_tier3_token_overlap_fail_pct` | numeric | 50 | 0 – 100 | — | seed | group: WSIB Matching | LW-D14 (2026-08-28). The floor for tier3_token_overlap: the pct of Tier-3 links whose WSIB name and matched entity name share at least one non-generic (stopword-stripped) token. Measured live pre-fix: 10.49% (840 of 8,009 tier-3 links share a genuine non-generic token; the other 89.51% share ONLY a generic word like CONTRACTING/CONSTRUCTION with their matched entity, or nothing at all — the magnet-entity fan-in contamination's root cause). CONSUMED by link_wsib via checks[].limit_from_config (the FAIL threshold only — the stopword list buildFuzzyMatchSql's predicate itself reads is a separate declared field, checks[].expect.stopwords). Post-fix, buildFuzzyMatchSql requires the SAME token-overlap rule to hold before a Tier-3 link is written at all, so a healthy corpus should read at or near 100%. |
| `load_ravines_count_drift_fail_pct` | numeric | 0.5 | 0 – 1 | — | seed | group: Source Ingestion | Spec 122 §1.2a P4 (Pilot 2, INGESTOR) — L7 feature-count drift bound: \|loaded − prior\| / prior above this FAILs the run before any DB write. 0.5 = the pre-externalization literal (ravineDriftFeatureCountPct). Bound to check ravine_count_drift_pct through checks[].limit_from_config, so the resolved value renders as the audit row threshold. RAVINE_ACCEPT_FEATURE_COUNT_DRIFT=1 lets the run proceed but never suppresses the FAIL row. CONSUMED by load_ravines (scripts/lib/compute/load-ravines.js ravine_count_drift_pct, via ctx.config). |
| `load_ravines_dataset_age_warn_years` | numeric | 20 | 1 – 200 | — | seed | group: Source Ingestion | Spec 122 §1.2a P4 (Pilot 2, INGESTOR) — how many years the Ravine + Natural Feature Protection archive may sit unchanged upstream before the dataset-age audit row escalates from INFO to WARN. 20 = the pre-externalization ConfigSchema literal (ravineSkipCheckThresholdYears). The CKAN resource has been frozen at Mon, 14 Mar 2022 since publication, so at 20 the row stays INFO until 2042; lower it to make the staleness of a genuinely abandoned source visible sooner. It moves a WARN, never a write. CONSUMED by load_ravines (scripts/lib/compute/load-ravines.js ravine_dataset_age_years, via ctx.config). |
| `load_ravines_download_timeout_ms` | numeric | 60000 | 1000 – 600000 | — | seed | group: Source Ingestion | Spec 122 §1.2a P4 (Pilot 2, INGESTOR) — AbortController budget for BOTH CKAN calls the acquisition seam makes (the pre-acquisition HEAD and the streamed ZIP GET). 60000 = the pre-externalization literal (ravineDownloadTimeoutMs) and is ALSO the descriptor literal execution.network.timeout, which is only the fallback for an un-seeded database: execution.network.timeout_from_config names this variable and scripts/lib/step/acquire.js resolveTimeoutMs prefers the resolved value, so there is ONE source and a conformance lock asserts the two AGREE. Raise it when the CKAN edge is slow rather than letting a timeout read as an acquisition FAIL. CONSUMED by load_ravines (scripts/lib/step/acquire.js headValidators + downloadArchive, via ctx.config). |
| `load_ravines_geometry_update_warn_pct` | numeric | 0.5 | 0 – 1 | — | seed | group: Source Ingestion | Spec 122 §1.2a P4 (Pilot 2, INGESTOR) — L7b geometry-update drift bound: updated / prior_feature_count above this WARNs (never halts). 0.5 = the pre-externalization literal (ravineDriftGeometryUpdatePct). Bound to check ravine_geometry_update_pct through checks[].limit_from_config. A high update ratio on a source frozen since 2022 means the upstream geometry was silently re-cut — worth a human look, not a refusal. CONSUMED by load_ravines (scripts/lib/compute/load-ravines.js ravine_geometry_update_pct, via ctx.config). |
| `load_ravines_invalid_geometry_fail_pct` | numeric | 0.05 | 0 – 1 | — | seed | group: Source Ingestion | Spec 122 §1.2a P4 (Pilot 2, INGESTOR) — L8 invalid-geometry bound: skipped / feature_count above this FAILs the run BEFORE the transaction opens, so no partial polygon set is ever committed. 0.05 = the pre-externalization literal (ravineInvalidGeometryFailPct) and the same 5% the downstream consumer enrich-ravines.js re-checks at :57 — move both together or producer and consumer disagree. CONSUMED by load_ravines (scripts/lib/compute/load-ravines.js ravine_geometry_skipped_pct, via ctx.config). |
| `load_ravines_mass_delete_fail_pct` | numeric | 0.5 | 0 – 1 | — | seed | group: Source Ingestion | Spec 122 §1.2a P4 (Pilot 2, INGESTOR) — L7c mass-deletion bound: deleted / prior_feature_count above this FAILs the run (fence 1ceebd17, Severity HIGH — the abort was non-functional pre-review). 0.5 = the pre-externalization literal (ravineMassDeletePct). Bound to check ravine_mass_delete_pct through checks[].limit_from_config; RAVINE_ACCEPT_MASS_DELETE=1 lets an acknowledged full reload COMPLETE but never suppresses the FAIL row. CONSUMED by load_ravines (scripts/lib/compute/load-ravines.js ravine_mass_delete_pct, via ctx.config). |
| `los_base_cap` | numeric | 30 | 1 – 100 | `scripts/compute-opportunity-scores.js` | seed | group: Lead Scoring | Maximum LoS base score before multipliers are applied |
| `los_base_divisor` | numeric | 10000 | 100 – 100000 | `scripts/compute-opportunity-scores.js` | seed | group: Lead Scoring | Divisor applied to construction cost when computing the LoS base score |
| `los_decay_divisor` | numeric | 25 | 1 – 1000 | `scripts/compute-opportunity-scores.js` | seed | group: Lead Scoring | Scales the asymptotic decay curve for competition penalties (rawPenalty / this = decayFactor; higher = gentler decay). At 25, a single high-intensity tracker halves the score. |
| `los_multiplier_bid` | numeric | 2.5 | 0.5 – 10 | `scripts/compute-opportunity-scores.js` | seed | group: Lead Scoring | LoS score multiplier applied when a lead is in active bidding phase |
| `los_multiplier_work` | numeric | 1.5 | 0.5 – 10 | `scripts/compute-opportunity-scores.js` | seed | group: Lead Scoring | LoS score multiplier applied when a lead is in active work phase |
| `los_penalty_saving` | numeric | 10 | 0 – 100 | `scripts/compute-opportunity-scores.js` | seed | group: Lead Scoring | LoS penalty (points) deducted when a lead is saved but not acted on |
| `los_penalty_tracking` | numeric | 50 | 0 – 100 | `scripts/compute-opportunity-scores.js` | seed | group: Lead Scoring | LoS penalty (points) deducted when a lead has been viewed/tracked without conversion |
| `massing_garage_max_sqm` | numeric | 60 | 1 – 500 | — | seed | group: Spatial & Massing | Maximum building footprint area (m²) for a secondary structure to be classified as a garage in link-massing. CONSUMED by link_massing (scripts/lib/compute/link-massing.js structure_thresholds, classifyStructure). |
| `massing_nearest_max_distance_m` | numeric | 50 | 1 – 500 | — | seed | group: Spatial & Massing | Maximum haversine distance (metres) for the nearest-building fallback in link-massing when no polygon match is found. CONSUMED by link_massing (scripts/lib/compute/link-massing.js match_nearest_fallback, buildMatchSql). |
| `massing_shed_threshold_sqm` | numeric | 20 | 1 – 200 | — | seed | group: Spatial & Massing | Building footprint area (m²) below which a secondary structure is classified as a shed in link-massing. CONSUMED by link_massing (scripts/lib/compute/link-massing.js structure_thresholds, classifyStructure). |
| `max_build_lot_max_sqm` | numeric | 2000 | 500 – 50000 | — | seed | group: Spatial & Massing | Spec 65 §4 MB-2 (WF3 S0.1, Rule 3 / R-G) — upper bound of the sane residential lot band (m²), inclusive: lot_size_sqm <= this. A lot above this ceiling gets lot_size_confidence='low' and envelope_constraint_reason='lot_too_large' — buildable, just not modelled by the residential max-build MODEL (the UI must not say 'can't build'). Promoted from the bare literal scripts/lib/max-build.js LOT_MAX_SQM. CONSUMED by enrich-parcels.js (buildMaxBuildSql). NOTE: WF3 S0.2 (Spec 88 §2.1) will widen the reno cost-menu product-scope bound (18,529 parcels, $77,593,312,011.51 of cost_gut_total pre-S0.2) to honour this same variable in a SEPARATE step's descriptor, landed and declared there, not here. Operator-tunable. |
| `max_build_lot_min_sqm` | numeric | 50 | 0 – 500 | — | seed | group: Spatial & Massing | Spec 65 §4 MB-2 (WF3 S0.1, Rule 3 / R-G) — lower bound of the sane residential lot band (m²), inclusive: lot_size_sqm >= this. A lot below this floor gets lot_size_confidence='low' and envelope_constraint_reason='lot_too_small' (emit=false — no max-build envelope). Promoted from the bare literal scripts/lib/max-build.js LOT_MIN_SQM (mislinks 7,695 parcels; 24 of them priced pre-S0.2). CONSUMED by enrich-parcels.js (buildMaxBuildSql, the lot_size_confidence/envelope_constraint_reason CASE). NOTE: WF3 S0.2 (Spec 88 §2.1) will widen the reno cost-menu product-scope bound to honour this same variable in a SEPARATE step's descriptor, landed and declared there, not here. Operator-tunable. |
| `max_build_min_dimension_m` | numeric | 3 | 0 – 10 | — | seed | group: Spatial & Massing | Spec 65 §4 MB-3 (WF3 Phase 1 D-C): minimum viable build dimension (m). A max-build width/length below this floor is NULLed — the setback box is excluded as degenerate; non-ravine envelopes fall back to the coverage cap only (max_buildable_gfa_basis='coverage_only'); ravine sub-floor parcels are 'ravine_constrained' with the whole envelope withheld (no coverage fallback). CONSUMED by enrich-parcels.js. Operator-tunable. |
| `min_comp_count` | numeric | 3 | — (migration-seeded) | — | migration 205 | — (governed by GROUPS only) | Spec 88 §2 / Spec 78 R4: min family-filtered comps before kNN falls back to zoning-only. |
| `min_soft_landscaping_pct` | numeric | 0.3 | 0.05 – 0.9 | — | seed | group: Spatial & Massing | Spec 65 §7 (Phase 3) — share of the lot that must remain soft landscaping; an accessory pushing greenspace below this is buildable only via a CoA minor variance (drives garage_permission / rear_suite_permission). CONSUMED by enrich-parcels.js (max-build pass). |
| `mislink_footprint_lot_tol` | numeric | 0.05 | 0 – 1 | — | seed | group: Data Quality Thresholds | Spec 65 §5 (WF3-A) — mislink guard tolerance: existing_footprint > lot_size_sqm × (1 + this) means the WRONG building was linked (block/neighbour attribution); the whole existing structure is NULLed + existing_data_quality_flag='footprint_exceeds_lot'. CONSUMED by enrich-parcels.js. Operator-tunable. |
| `model_range_pct` | numeric | 0.20 | — (migration-seeded) | — | migration 156 | — (governed by GROUPS only) | CoA geometric cost-model range as a fraction (Spec 83 §3.A). The cost estimate ±range produces the displayed low/high envelope. Default 0.20 = ±20%. Operator-tunable via Spec 86 Control Panel. |
| `notifications_disabled_types` | JSONB | [] | — (migration-seeded) | — | migration 218 | — (governed by GROUPS only) | JSONB array of notification type strings the dispatcher must skip (operator kill-list, in addition to the code-fenced types). |
| `notifications_dispatch_enabled` | numeric | 0 | — (migration-seeded) | — | migration 218 | — (governed by GROUPS only) | Master kill-switch for the dispatch_notifications step. 0=OFF (no pushes sent), 1=ON. Seeded OFF; flipped ON last after the engine is validated. |
| `notifications_max_per_user_per_day` | numeric | 10 | — (migration-seeded) | — | migration 218 | — (governed by GROUPS only) | Throttle: max notifications delivered per user per Toronto calendar day. Excess rows are deferred, not sent. |
| `notifications_max_stale_hours` | numeric | 168 | — (migration-seeded) | — | migration 222 | — (governed by GROUPS only) | Freshness bound in hours for START_DATE_URGENT queue rows. A URGENT row older than this has an elapsed predicted-start (its starts-in-N-days body is now false); the dispatcher retires it as stale_dropped instead of sending. Scoped to START_DATE_URGENT only. 168h is the 6-7 day URGENT enqueue horizon. The dispatcher also carries a Zod default so a missing row never throws. |
| `p16_inference_layer_enabled` | numeric | 0 | 0 – 1 | — | seed | group: Scope Inference | Spec 80 §5.C / P16-D3 [BUG-6] — HARD gate for the lean scope-mapped inference layer in classify-permits.js + classify-coa-trades.js. 0 = OFF (evidence-only emission; 16C/16D wire the code but emit NO inference rows). 1 = ON (the LINE_TRADE_COMPLEMENT lean inference is UNIONed onto evidence at is_active=true / attachment_basis='inference'). Seeded OFF on the 16B GO gate (2026-07-10, hold-out recall 61.4% / prec(insp) 70.5% / mean 10.2); flips to 1 in 16F only AFTER 16E's consumer contract ships. A NO-GO complement cannot ship by accident. The deep_scrapes-resume re-measure can flip it back to 0. |
| `parcel_cost_menu_coverage_pass_pct` | numeric | 85 | 0 – 100 | — | seed | group: Cost Audit Thresholds | assert-global-coverage: sources-chain parcel_cost_menu calibrated coverage (residential w/ building) -> PASS CONSUMED by assert_global_coverage. |
| `parcel_cost_menu_coverage_warn_pct` | numeric | 80 | 0 – 100 | — | seed | group: Cost Audit Thresholds | assert-global-coverage: sources-chain parcel_cost_menu calibrated coverage (residential w/ building) -> WARN, else FAIL CONSUMED by assert_global_coverage. |
| `parcel_sanity_bylaw_fsi_max` | numeric | 8 | 0 – 50 | — | seed | group: Data Quality Thresholds | assert_parcel_sanity: residential bylaw_max_fsi upper bound (corrupt-source watch). Ported verbatim from the pre-conversion literal (8). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_coa_fsi_max` | numeric | 5 | 0 – 50 | — | seed | group: Data Quality Thresholds | assert_parcel_sanity: coa_fsi upper bound (garbage-GFA watch). Ported verbatim from the pre-conversion literal (5). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_comp_fsi_p50_max` | numeric | 4 | 0 – 50 | — | seed | group: Data Quality Thresholds | assert_parcel_sanity: comp_fsi_p50 upper bound (INFO-first, 212 standing). Ported verbatim from the pre-conversion literal (4.0). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_comp_fsi_p50_min` | numeric | 0.05 | 0 – 5 | — | seed | group: Data Quality Thresholds | assert_parcel_sanity: comp_fsi_p50 implausibly-low lower bound. Ported verbatim from the pre-conversion literal (0.05). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_cost_addition_max_cad` | numeric | 50000000 | 0 – 10000000000 | — | seed | group: Data Quality Thresholds | assert_parcel_sanity: cost_addition_total magnitude watch (CAD), COST_ADDITION_GT50M_LEGIT accept-list of 42 ids filters the current investigated population. Ported verbatim from the pre-conversion literal (50000000). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_cost_coherence_tolerance_cad` | numeric | 1 | 0 – 10000 | — | seed | group: Data Quality Thresholds | assert_parcel_sanity: GATE — cost_fb_total vs cost_coa_total coherence tolerance (CAD). Ported verbatim from the pre-conversion literal (1). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_dim_lot_tolerance_m` | numeric | 0.01 | 0 – 0.5 | — | seed | group: Spatial & Massing | assert_parcel_sanity: GATE — max_build width/length vs frontage/depth mislink tolerance (m). Ported verbatim from the pre-conversion literal (0.01). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_distribution_median_floor` | numeric | 0.0001 | 0 – 1 | — | seed | group: Spatial & Massing | assert_parcel_sanity: per-zone distribution-scan zero-median floor (GREATEST(median, floor)). Ported verbatim from the pre-conversion literal (0.0001). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_distribution_median_multiplier` | numeric | 3 | 1 – 100 | — | seed | group: Spatial & Massing | assert_parcel_sanity: per-zone distribution-scan outlier median multiplier (value > multiplier x zone median). Ported verbatim from the pre-conversion literal (3). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_distribution_percentile` | numeric | 0.99 | 0.5 – 0.9999 | — | seed | group: Spatial & Massing | assert_parcel_sanity: per-zone distribution-scan outlier percentile (percentile_cont). Ported verbatim from the pre-conversion literal (0.99). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_footprint_coverage_max_ratio` | numeric | 0.65 | 0 – 2 | — | seed | group: Data Quality Thresholds | assert_parcel_sanity: max_buildable_footprint_sqm / lot_size_sqm upper ratio bound. Ported verbatim from the pre-conversion literal (0.65). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_gfa_coherence_tolerance_sqm` | numeric | 0.5 | 0 – 100 | — | seed | group: Data Quality Thresholds | assert_parcel_sanity: GATE — opt_aor_gfa_sqm vs opt_coa_gfa_sqm / max_buildable_gfa_sqm coherence tolerance (sqm). Ported verbatim from the pre-conversion literal (0.5). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_greenspace_tolerance_sqm` | numeric | 0.5 | 0 – 100 | — | seed | group: Data Quality Thresholds | assert_parcel_sanity: existing_greenspace_sqm vs lot_size_sqm coherence tolerance (sqm). Ported verbatim from the pre-conversion literal (0.5). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_height_per_storey_max_m` | numeric | 5.5 | 0 – 50 | — | seed | group: Spatial & Massing | assert_parcel_sanity: bylaw_max_height_m / bylaw_max_stories generous-zoning INFO ceiling (never a bug). Ported verbatim from the pre-conversion literal (5.5). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_height_per_storey_min_m` | numeric | 2.5 | 0 – 20 | — | seed | group: Spatial & Massing | assert_parcel_sanity: GATE — bylaw_max_height_m / bylaw_max_stories physical-impossibility floor (the WELD signature). Ported verbatim from the pre-conversion literal (2.5). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_lot_size_max_sqm` | numeric | 100000 | 1000 – 10000000 | — | seed | group: Data Quality Thresholds | assert_parcel_sanity: residential lot_size_sqm upper bound (sqm). Ported verbatim from the pre-conversion literal (100000). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_lot_size_min_sqm` | numeric | 40 | 0 – 10000 | — | seed | group: Data Quality Thresholds | assert_parcel_sanity: residential lot_size_sqm lower bound (sqm). Ported verbatim from the pre-conversion literal (40). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_lowrise_bylaw_fsi_max` | numeric | 1.5 | 0 – 20 | — | seed | group: Data Quality Thresholds | assert_parcel_sanity: lowrise bylaw_max_fsi upper bound (FSI-borrow bug watch). Ported verbatim from the pre-conversion literal (1.5). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_lowrise_bylaw_height_max_m` | numeric | 15 | 0 – 200 | — | seed | group: Spatial & Massing | assert_parcel_sanity: lowrise bylaw_max_height_m upper bound (tree-massing watch). Ported verbatim from the pre-conversion literal (15). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_lowrise_cost_fb_max_cad` | numeric | 15000000 | 0 – 10000000000 | — | seed | group: Data Quality Thresholds | assert_parcel_sanity: lowrise cost_fb_total magnitude watch (CAD), COST_FB_GT15M_LEGIT accept-list of 24 ids filters the current investigated population. Ported verbatim from the pre-conversion literal (15000000). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_lowrise_coverage_max_pct` | numeric | 50 | 0 – 100 | — | seed | group: Data Quality Thresholds | assert_parcel_sanity: lowrise bylaw_max_coverage_pct upper bound. Ported verbatim from the pre-conversion literal (50). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_lowrise_maxbuild_height_max_m` | numeric | 15 | 0 – 200 | — | seed | group: Spatial & Massing | assert_parcel_sanity: lowrise max_build_height_m upper bound (tree-massing / envelope-height watch). Ported verbatim from the pre-conversion literal (15). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_lowrise_maxbuild_stories_max` | numeric | 4 | 0 – 100 | — | seed | group: Spatial & Massing | assert_parcel_sanity: lowrise max_build_stories upper bound (over-tall envelope watch). Ported verbatim from the pre-conversion literal (4). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_lowrise_opt_aor_gfa_max_sqm` | numeric | 2500 | 0 – 100000 | — | seed | group: Spatial & Massing | assert_parcel_sanity: lowrise (RD/RS/RT) opt_aor_gfa_sqm upper bound. Ported verbatim from the pre-conversion literal (2500). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_max_build_fsi_max` | numeric | 5 | 0 – 50 | — | seed | group: Data Quality Thresholds | assert_parcel_sanity: max_build_fsi upper bound (garbage-GFA watch). Ported verbatim from the pre-conversion literal (5). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_max_build_length_max_m` | numeric | 100 | 0 – 1000 | — | seed | group: Spatial & Massing | assert_parcel_sanity: max_build_length_m upper bound (m), RC-measured p995 58.9 / max obs 316.23. Ported verbatim from the pre-conversion literal (100). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_max_build_width_max_m` | numeric | 30 | 0 – 500 | — | seed | group: Spatial & Massing | assert_parcel_sanity: max_build_width_m upper bound (m), RC-measured p995 27.2 / max obs 42.66. Ported verbatim from the pre-conversion literal (30). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_newbuild_cost_per_sqm_max` | numeric | 12000 | 0 – 1000000 | — | seed | group: Data Quality Thresholds | assert_parcel_sanity: cost_fb_total / opt_aor_gfa_sqm rate upper bound (CAD/sqm). Ported verbatim from the pre-conversion literal (12000). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_newbuild_cost_per_sqm_min` | numeric | 2000 | 0 – 100000 | — | seed | group: Data Quality Thresholds | assert_parcel_sanity: cost_fb_total / opt_aor_gfa_sqm rate lower bound (CAD/sqm). Ported verbatim from the pre-conversion literal (2000). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_nonlowrise_opt_aor_gfa_max_sqm` | numeric | 3500 | 0 – 100000 | — | seed | group: Spatial & Massing | assert_parcel_sanity: non-lowrise opt_aor_gfa_sqm upper bound. Ported verbatim from the pre-conversion literal (3500). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_opt_storeys_max` | numeric | 12 | 0 – 200 | — | seed | group: Spatial & Massing | assert_parcel_sanity: opt_aor_storeys / opt_coa_storeys physical upper bound. Ported verbatim from the pre-conversion literal (12). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_priced_newbuild_min_gfa_sqm` | numeric | 30 | 0 – 1000 | — | seed | group: Spatial & Massing | assert_parcel_sanity: priced new-build opt_aor_gfa_sqm lower bound (micro-envelope INFO watch). Ported verbatim from the pre-conversion literal (30). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_rd_maxbuild_stories_max` | numeric | 3 | 0 – 100 | — | seed | group: Spatial & Massing | assert_parcel_sanity: RD-only max_build_stories upper bound (tighter than the lowrise bound). Ported verbatim from the pre-conversion literal (3). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_realized_fsi_p90_max` | numeric | 6 | 0 – 50 | — | seed | group: Data Quality Thresholds | assert_parcel_sanity: realized_fsi_p90 upper bound. Ported verbatim from the pre-conversion literal (6). CONSUMED by assert_parcel_sanity. |
| `parcel_sanity_realized_fsi_p90_min` | numeric | 0.1 | 0 – 10 | — | seed | group: Data Quality Thresholds | assert_parcel_sanity: realized_fsi_p90 lower bound. Ported verbatim from the pre-conversion literal (0.1). CONSUMED by assert_parcel_sanity. |
| `parcels_dead_tuple_ratio_warn_max` | numeric | 0.3 | 0 – 5 | — | seed | group: Data Quality Thresholds | WF3 EP-D17 (2026-09-10, .cursor/wf3_ep_d17_pass5_fullscan_checks_active_task.md C4). WARN bound on parcels' pg_stat_user_tables dead_ratio, read by TWO independent consumers of the SAME measurement by naming convention ("${table}_dead_tuple_ratio_warn_max"): the descriptor's own declared parcels_dead_tuple_ratio plausibility check (observability, rung b) and scripts/lib/step/plausibility.js's runMaintenance executor (the execution.maintenance trigger, rung d) — the same threshold name means the two can never silently disagree about it. Cloud-measured 2026-09-10 (run 34506962436): parcels read 0.697, well above this default, right after enrich_parcels' own pass-4 comps rewrite bloated the heap inside one shared 93-minute transaction whose xmin pinned the vacuum horizon. CONSUMED by scripts/lib/step/plausibility.js (runMaintenance) and scripts/enrich-parcels.descriptor.json's parcels_dead_tuple_ratio check (limit_from_config). |
| `parcels_maintenance_timeout_minutes` | numeric | 15 | 1 – 60 | — | seed | group: Source Ingestion | WF3 EP-D17 output-panel fix F5 (2026-09-10, .cursor/wf3_ep_d17_pass5_fullscan_checks_active_task.md C4). Declared ceiling for the execution.maintenance VACUUM/ANALYZE/REINDEX statement scripts/lib/step/plausibility.js's runMaintenance issues on parcels — the original design had NO ceiling on this statement at all, the exact unbounded-statement shape EP-D17's own C1 rung exists to close everywhere else. Applied via a SET statement_timeout on a dedicated autocommit client (VACUUM forbids an enclosing transaction). Default 15 min carries real margin over the 3-10 min a manual, unthrottled VACUUM was estimated to take for the ~1.35M dead tuples that motivated this WF3 (scripts/one-time/wf3-vacuum-analyze-parcels.js's own header). CONSUMED by scripts/lib/step/plausibility.js (runMaintenance, keyed by "${table}_maintenance_timeout_minutes" — same naming convention as parcels_dead_tuple_ratio_warn_max); today's sole declaring consumer is enrich_parcels via parcels_maintenance_timeout_minutes. |
| `pending_closed_grace_days` | numeric | 30 | 1 – 365 | `scripts/close-stale-permits.js` | seed | group: Inspection & Closure | Days a permit must remain in Pending Closed status before being promoted to Closed |
| `permit_declared_cost_ceiling` | numeric | 500000000 | 10000000 – 2000000000 | — | seed | group: Cost Tuning | P13-2: upper-sentinel ceiling (CAD) on a permit's declared est_const_cost in the Liar's Gate. A declared cost above this is treated as a placeholder (the mirror of PLACEHOLDER_COST_THRESHOLD's lower guard — e.g. the exact-$1e9 round-number filings on 38-39 storey towers) and the geometric model takes over instead of passing the sentinel through as cost_source='permit'. Seeded at $500M: above the largest plausible single-permit declared build, below the $1e9 placeholder band. |
| `permits_bylaw_max_coverage_null_warn_pct` | numeric | 72 | 0 – 100 | — | seed | group: Data Quality Thresholds | WF2 P6.5 — enrich-permits bylaw_max_coverage NULL-rate WARN floor (density zones regulate by FSI-not-coverage). Baseline 66.3+5.75; WARN-above-baseline, never FAIL. |
| `permits_bylaw_max_fsi_null_warn_pct` | numeric | 88 | 0 – 100 | — | seed | group: Data Quality Thresholds | WF2 P6.5 — enrich-permits bylaw_max_fsi NULL-rate WARN floor. Nulls are ~100% STRUCTURAL (residential zones regulate by coverage-not-FSI) so this WARNs only above baseline+margin (83.0+5); never FAIL. |
| `phase_b_revision_num_max_length` | numeric | 2 | — (migration-seeded) | — | migration 136 | — (governed by GROUPS only) | Phase B preflight — MAX(LENGTH(revision_num)) on permits; surface for review if exceeded |
| `placeholder_cost_threshold` | numeric | 1000 | 100 – 10000000 | — | seed | group: Cost Tuning | Construction cost values below this threshold (CAD) are treated as placeholder/zero and excluded from analysis |
| `pre_permit_expiry_months` | numeric | 18 | 6 – 36 | — | seed | group: Pre-Permits | Months after CoA approval before an unlinked Pre-Permit lead is reclassified as Expired/Abandoned |
| `pre_permit_stale_months` | numeric | 12 | 3 – 24 | — | seed | group: Pre-Permits | Months after CoA approval before an unlinked Pre-Permit is flagged as stale in the aging audit (early-warning bucket) |
| `product_scope_max_existing_gfa_sqm` | numeric | 750 | 0 – 100000 | — | seed | group: Spatial & Massing | WF3 S0.2 (2026-09-21/22, Spec 88 §2.1, FOLD-RC2) — the residual product-scope cut: a parcel whose EXISTING structure (cur_floor_gfa_sqm) exceeds this is never priced, even if it is inside the max-build model. EXCLUSIVE (excluded iff cur_floor_gfa_sqm > this; a parcel at exactly 750.00 m2 is IN scope). Evidence: inside the in-model population (max_buildable_gfa_sqm IS NOT NULL, n=411,076) existing-GFA p99=337, p999=716, max=1,739; >750 = 322 parcels (0.078%) — ~2.2x p99, just above p999. This distribution holds ONLY under the R% zoning filter (FOLD-RC2: without it, p99=449/max=1,947) — CONSUMED by compute-parcel-cost-estimates.js's buildSourceSql, which carries the SAME `upper(zoning_class) LIKE 'R%'` clause as the population this bound was measured against. Retighten (M2): S1.4 re-derives this against the post-apportionment distribution (expected near 500) and records the result whether or not the value changes. |
| `profiling_coverage_pass_pct` | numeric | 90 | 0 – 100 | — | seed | group: Coverage & Quality | assert-global-coverage: coverage >= this integer pct -> PASS (spec 49) CONSUMED by assert_global_coverage. |
| `profiling_coverage_warn_pct` | numeric | 70 | 0 – 100 | — | seed | group: Coverage & Quality | assert-global-coverage: coverage >= this integer pct -> WARN; below -> FAIL (spec 49) CONSUMED by assert_global_coverage. |
| `refresh_snapshot_phase_deadline_minutes` | numeric | 12 | 0 – 15 | — | seed | group: Source Ingestion | WF3 2026-09-18 (deep_scrapes cause B, Spec 118 §1/§7.2 layer 3 — the missing per-step ceiling). Wall-clock deadline for runRecorderPhase's WHOLE read phase — ONE shared budget across BOTH the 8 main reads (one pinned REPEATABLE READ client) AND the 6 optional reads (a separate autocommit client, output-panel fold R1/V1, 2026-09-18): the optional block is armed with the REMAINING milliseconds after the main reads, never a fresh full budget, so the two blocks together can never exceed this ONE declared deadline (worst case ~2x was the V1 defect this fold closed). armed via startPhaseDeadline/pg_cancel_backend on a dedicated cancel client per block — the same re-arm mechanism EP-PHASE-DEADLINE established for the enrich-shaped phases, generalized to the recorder shape for the first time. A per-statement bound alone (refresh_snapshot_read_statement_timeout_minutes) cannot catch 14 reads that are each individually fast but sum past the budget; this is the PHASE bound Spec 118 §3 names as the missing layer-3 hierarchy rung. Default 12min sits under the unchanged 15-minute step ceiling (execution.step_timeout, the backstop — Spec 118 §3's own sizing rule: the platform axe is the backstop, never the mechanism) with margin over the measured healthy total (110-121s). 0 = disabled. CONSUMED by scripts/lib/step/index.js (runRecorderPhase); today's sole declaring consumer is refresh_snapshot via execution.phase_deadline_minutes_from_config. Cloud seed required before cutover — docs/runbook/README.md section 1a exact form (Git Bash, not PowerShell — an empty-string PG_HOST= assignment deletes the var in PowerShell, defeating the guard): SUPABASE_CA_CERT_PATH=scripts/certs/supabase-ca.pem PG_HOST= DATABASE_URL=$SUPABASE_DATABASE_URL node -r dotenv/config scripts/seeds/apply-logic-variables.js |
| `refresh_snapshot_read_statement_timeout_minutes` | numeric | 5 | 0 – 15 | — | seed | group: Source Ingestion | WF3 2026-09-18 (deep_scrapes cause B, Spec 118 §1/§7.1). Per-statement statement_timeout applied to BOTH of runRecorderPhase's read blocks (`scripts/lib/step/index.js`): `SET LOCAL` on the pinned REPEATABLE READ READ ONLY client for the 8 main reads, and a session-level `SET`/`RESET` pair on a separate autocommit client for the 6 optional reads (output-panel fold R1, 2026-09-18) — the SAME resolved value, one logic variable, both blocks. Before this the descriptor declared `execution.statement_timeout: "none"` and a single read could block forever with zero trace — the 2026-09-15 incident measured the tradeByType read riding a decorrelated permits heap from a healthy 110-121s run to the 900s (15m) platform axe with no statement-level bound at all. Default 5min carries real margin over the measured healthy runtime (110-121s total across all 8 reads) while staying comfortably under the unchanged 15-minute step ceiling (Spec 118 §3 layer 4, the backstop). 0 = disabled (falls through to the session's own statement_timeout, today unbounded). CONSUMED by scripts/lib/step/index.js (runRecorderPhase); today's sole declaring consumer is refresh_snapshot via execution.statement_timeout_minutes_from_config. Cloud seed required before cutover — docs/runbook/README.md section 1a exact form (Git Bash, not PowerShell — an empty-string PG_HOST= assignment deletes the var in PowerShell, defeating the guard): SUPABASE_CA_CERT_PATH=scripts/certs/supabase-ca.pem PG_HOST= DATABASE_URL=$SUPABASE_DATABASE_URL node -r dotenv/config scripts/seeds/apply-logic-variables.js |
| `reno_bath_gfa_pct` | numeric | 0.07 | 0.01 – 1 | — | seed | group: Cost Tuning | Spec 65 §6 (Phase 2) — bathroom-reno floor area as a fraction of the current-home GFA (cur_est_bath_gfa_sqm). CONSUMED by enrich-parcels.js. Operator-tunable. |
| `reno_coa_uplift_pct` | numeric | 0.05 | 0 – 1 | — | seed | group: Cost Tuning | Spec 65 §6 (Phase 2) — CoA variance uplift on the by-law max-build GFA: max_newbuild_coa_gfa_sqm = max_buildable_gfa_sqm × (1 + this). CONSUMED by enrich-parcels.js (scenario pass). Operator-tunable calibration heuristic. |
| `reno_kitchen_gfa_pct` | numeric | 0.15 | 0.01 – 1 | — | seed | group: Cost Tuning | Spec 65 §6 (Phase 2) — kitchen-reno floor area as a fraction of the current-home GFA (cur_est_kitchen_gfa_sqm). CONSUMED by enrich-parcels.js. Operator-tunable. |
| `road_overlay_distance_m` | numeric | 5 | 0 – 100 | — | seed | group: Source Ingestion | Distance (metres) for the LineString zoning-overlay spatial join (Policy Road, Priority Retail). SEEDED by Spec 58 load-zoning; CONSUMED by enrich-parcels.js (pilot 9 commit 9 cutover, 2026-09-08) via ST_DWithin(parcels.geom::geography, road.geom::geography, road_overlay_distance_m) — the ::geography cast is mandatory (F-C2), else SRID 4326 treats the value as degrees (~111km/deg). |
| `score_tier_elite` | numeric | 80 | 51 – 100 | `scripts/compute-opportunity-scores.js` | seed | group: Scoring Tiers | Minimum opportunity_score (0-100) for a lead to be classified as 'elite' tier in the scoring distribution telemetry |
| `score_tier_moderate` | numeric | 20 | 1 – 79 | `scripts/compute-opportunity-scores.js` | seed | group: Scoring Tiers | Minimum opportunity_score (0-100) for a lead to be classified as 'moderate' tier (below strong) in the scoring distribution telemetry; scores below this are 'low' |
| `score_tier_strong` | numeric | 50 | 21 – 99 | `scripts/compute-opportunity-scores.js` | seed | group: Scoring Tiers | Minimum opportunity_score (0-100) for a lead to be classified as 'strong' tier (below elite) in the scoring distribution telemetry |
| `scrape_early_phase_threshold_pct` | numeric | 5 | 1 – 50 | `scripts/quality/assert-staleness.js` | seed | group: Scraper & Network Health | Scrape coverage percentage below which the staleness monitor treats stale permits as WARN (early phase) rather than FAIL (production) |
| `scrape_stale_days` | numeric | 30 | 1 – 180 | `scripts/quality/assert-staleness.js` | seed | group: Scraper & Network Health | Days since last scrape_at before a permit inspection is considered stale in the staleness monitor |
| `scraper_empty_streak_warn` | numeric | 20 | 1 – 1000 | `scripts/quality/assert-network-health.js` | seed | group: Scraper & Network Health | Number of consecutive empty scraper responses at or above which assert-network-health emits a WARN (WAF shadow-ban detection) |
| `scraper_error_rate_warn_pct` | numeric | 5 | 1 – 100 | `scripts/quality/assert-network-health.js` | seed | group: Scraper & Network Health | Proxy error rate percentage at or above which assert-network-health emits a FAIL (proxy_errors / permits_attempted * 100) |
| `scraper_latency_p50_warn_ms` | numeric | 2000 | 100 – 60000 | `scripts/quality/assert-network-health.js` | seed | group: Scraper & Network Health | Scraper latency p50 (ms) at or above which assert-network-health emits a WARN |
| `snapshot_coa_conf_high` | numeric | 0.8 | 0.01 – 1 | — | seed | group: CoA Matching | Minimum linked_confidence for a CoA application to be counted as 'high_confidence' in the refresh-snapshot dashboard metrics. CONSUMED by scripts/lib/compute/refresh-snapshot.js (pilot 8, 2026-08-31: RE-HOMED off scripts/refresh-snapshot.js's frozen shape, same treatment as coa_match_conf_medium above). |
| `snowplow_buffer_days` | numeric | 7 | 1 – 60 | `scripts/compute-trade-forecasts.js` | seed | group: Forecast & Urgency | Days added to today when the Historic Snowplow snaps a fallback-anchor forecast out of the deep past (spec 85 §3). Higher = more lead time before the window opens. |
| `sources_address_points_floor` | numeric | 500000 | 1 – 5000000 | — | seed | group: Sources Catastrophic-Load Floors | assert-data-bounds: address_points row-count FAIL floor (IL-8, 1f8ca38a — ~95% of live 525K). ALSO CONSUMED by load_address_points (row 3.1, batch-2 P3.1) through its checks[].limit_from_config on rows_read_floor — the SAME registered variable, not a second key (Rule 3: one home per concern). CONSUMED by assert_data_bounds and load_address_points. |
| `sources_building_footprints_floor` | numeric | 400000 | 1 – 5000000 | — | seed | group: Sources Catastrophic-Load Floors | assert-data-bounds: building_footprints row-count FAIL floor (IL-8, 1f8ca38a — ~95% of live 427K). CONSUMED by assert_data_bounds. |
| `sources_centreline_floor` | numeric | 40000 | 1 – 500000 | — | seed | group: Sources Catastrophic-Load Floors | assert-data-bounds: toronto_centreline row-count FAIL floor (IL-10, f6047e89, Spec 62 §8c; migration 173 deploy-order guarded). CONSUMED by assert_data_bounds. |
| `sources_heritage_districts_floor` | numeric | 20 | 1 – 1000 | — | seed | group: Sources Catastrophic-Load Floors | assert-data-bounds: heritage_districts row-count FAIL floor (IL-10, 169f22af, Spec 61 §8c; migration 170 deploy-order guarded). CONSUMED by assert_data_bounds. |
| `sources_heritage_properties_floor` | numeric | 8000 | 1 – 100000 | — | seed | group: Sources Catastrophic-Load Floors | assert-data-bounds: heritage_properties row-count FAIL floor (IL-10, 169f22af, Spec 61 §8c; migration 170 deploy-order guarded). CONSUMED by assert_data_bounds. |
| `sources_maxbuild_coverage_pass_pct` | numeric | 88 | 0 – 100 | — | seed | group: Coverage & Quality | assert-global-coverage: sources-chain max-build/opt envelope calibrated coverage (residential w/ building) -> PASS CONSUMED by assert_global_coverage. |
| `sources_maxbuild_coverage_warn_pct` | numeric | 75 | 0 – 100 | — | seed | group: Coverage & Quality | assert-global-coverage: sources-chain max-build/opt envelope calibrated coverage (residential w/ building) -> WARN, else FAIL CONSUMED by assert_global_coverage. |
| `sources_neighbourhoods_floor` | numeric | 158 | 1 – 1000 | — | seed | group: Sources Catastrophic-Load Floors | assert-data-bounds: neighbourhoods row-count FAIL floor (IL-9, e3dad53d/b4e3d56e). SHARED ACROSS TWO STEPS DELIBERATELY (I4 Ask 5 ruling, 2026-09-16): 158 is ONE municipal boundary count, and a second registry row for the same fact would be a second source of truth for it (Rule 1, one home per concern). The un-prefixed shared-variable precedent is spatial_match_max_distance_m / spatial_match_confidence, which a converted LINK step consumes without owning the name. NOTE for a future editor: the two consumers must keep IDENTICAL min/max bounds, because one of them declares on_invalid "fail" with hoisted_above_gate true, so an admin edit legal for one step would halt the other above its advisory lock in two chains. CONSUMED by assert_data_bounds and by link_neighbourhoods (via checks[].limit_from_config on both neighbourhoods_loaded_before_write and neighbourhoods_loaded). |
| `sources_parcels_floor` | numeric | 460000 | 1 – 5000000 | — | seed | group: Sources Catastrophic-Load Floors | assert-data-bounds: parcels row-count FAIL floor (IL-8, 1f8ca38a — ~95% of live 486K). CONSUMED by assert_data_bounds. |
| `sources_ravines_floor` | numeric | 500 | 1 – 100000 | — | seed | group: Sources Catastrophic-Load Floors | assert-data-bounds: ravines row-count FAIL floor (IL-10, 1ceebd17, Spec 59 §8c — ~854 expected; does-not-exist deploy-order guarded, migration 167). CONSUMED by assert_data_bounds. |
| `sources_zoning_class_coverage_pass_pct` | numeric | 90 | 0 – 100 | — | seed | group: Coverage & Quality | assert-global-coverage: sources-chain parcels.zoning_class calibrated coverage -> PASS CONSUMED by assert_global_coverage. |
| `sources_zoning_class_coverage_warn_pct` | numeric | 85 | 0 – 100 | — | seed | group: Coverage & Quality | assert-global-coverage: sources-chain parcels.zoning_class calibrated coverage -> WARN, else FAIL CONSUMED by assert_global_coverage. |
| `spatial_match_confidence` | numeric | 0.65 | 0.01 – 1 | — | seed | group: Parcel Linking | Confidence score assigned to a permit→parcel spatial match (Strategy 3 Step 2, KNN-boundary-distance fallback); upgraded to link_parcels_confidence_spatial_polygon (T3) if permit geocode falls inside the parcel polygon (Strategy 3 Step 1). CONSUMED by scripts/link-parcels.js (bound directly into the spatial-fallback SQL's params by scripts/lib/step/index.js's runLinkKeyedPhase, not read inside the compute file itself). |
| `spatial_match_max_distance_m` | numeric | 100 | 1 – 1000 | — | seed | group: Parcel Linking | Maximum metres between a permit geocode and a parcel boundary for a spatial match (Strategy 3 Step 2, KNN-boundary-distance fallback, in link-parcels). Description corrected pilot 7 (2026-08-30, Finding 6) — the join predicate no longer ranks by parcel CENTROID (THE FIX, LP-D1); only this description text changes, the value and name do not. CONSUMED by scripts/link-parcels.js (bound directly into the spatial-fallback SQL's params by scripts/lib/step/index.js's runLinkKeyedPhase, not read inside the compute file itself). |
| `stale_closure_abort_pct` | numeric | 10 | 1 – 50 | `scripts/close-stale-permits.js` | seed | group: Inspection & Closure | Maximum percentage of permits that may be closed in a single run; exceeding this aborts the pipeline as a safety guard against partial CKAN downloads |
| `staleness_max_days_stale` | numeric | 60 | 0 – 365 | `scripts/quality/assert-staleness.js` | seed | group: Pipeline Staleness Thresholds | Single-permit max_days_stale ceiling (days). Above this assert-staleness emits an informational WARN row indicating permits are aging beyond expectation. Not a halt. |
| `staleness_max_stale_over_30d` | numeric | 10000 | 0 – 100000 | `scripts/quality/assert-staleness.js` | seed | group: Pipeline Staleness Thresholds | Max permits with last inspection scrape older than scrape_stale_days before assert-staleness emits FAIL (Spec 44 step 7). Sized against the 2026-05-08 snapshot (6,514 stale → WARN). Tighten to <2000 once scrape coverage ≥50% per Spec 38. |
| `staleness_min_coverage_pct` | numeric | 10 | 0 – 100 | `scripts/quality/assert-staleness.js` | seed | group: Pipeline Staleness Thresholds | Scraped/total ratio (percent) below which assert-staleness emits an informational WARN row. Per Spec 44 §3.5 informational, not a halt. |
| `stall_penalty_active` | numeric | 14 | 1 – 365 | `scripts/compute-trade-forecasts.js` | seed | group: Timing & Staleness | Days without phase progression before an active-construction lead is considered stalled |
| `stall_penalty_precon` | numeric | 45 | 1 – 365 | `scripts/compute-trade-forecasts.js` | seed | group: Timing & Staleness | Days without phase progression before a pre-construction lead is considered stalled |
| `step_post_check_concurrency` | numeric | 4 | 1 – 10 | — | seed | group: Source Ingestion | WF3 EP-D17 output-panel fix F7 (2026-09-10, .cursor/wf3_ep_d17_pass5_fullscan_checks_active_task.md C1/C2b). Caps the number of invariants[]/plausibility[] entries scripts/lib/step/plausibility.js's runValidatorEntries executes SIMULTANEOUSLY (batch width) at the run-end every_run hook. The original C2b design ran every selected entry concurrently, unbounded — measured against link_wsib's own 9 declared entries, that would open 9 simultaneous pool clients against a pg default max of 10, on top of the step's own outer advisory-lock client, over budget. Default 4 keeps a wide margin for every converted step regardless of how many entries it declares. CONSUMED by scripts/lib/step/index.js (the every_run invariants/plausibility executor's batch-width resolution); today's sole declaring consumer is enrich_parcels, but the library-level default (4) applies to every step even when undeclared. |
| `step_post_check_statement_timeout_minutes` | numeric | 10 | 1 – 75 | — | seed | group: Source Ingestion | WF3 EP-D17 (2026-09-10, .cursor/wf3_ep_d17_pass5_fullscan_checks_active_task.md C1). The declared ceiling an every_run invariants[]/plausibility[] entry inherits when it declares no statement_timeout of its own — before this, that entry took the bare pool.query branch inside scripts/lib/step/plausibility.js's executeEntry and ran with NO ceiling at all (PIPELINE_STATEMENT_TIMEOUT_MS defaults to 0). Cloud EXPLAIN measured enrich_parcels' 5 post checks (full Seq Scans on parcels) at 16-30s on a vacuumed heap vs 40+ min against a bloated one (run 34506962436, 2026-09-10) — 10 min is comfortably inside the clean-heap cost with headroom, and a genuine regression past it now FAILs loudly instead of wedging the chain. CONSUMED by scripts/lib/step/index.js (the every_run invariants/plausibility executor's default-ceiling resolution); today's sole declaring consumer is enrich_parcels. |
| `storey_height_m` | numeric | 3 | 2 – 6 | — | seed | group: Spatial & Massing | Spec 65 §6 (Phase 2) — residential storey height (metres) for the height→storey translation (max_build_stories = round(bylaw_max_height_m / storey_height) when the by-law gives no storey count). Non-residential zones use a taller inline value. CONSUMED by enrich-parcels.js (max-build pass). |
| `stripe_price_id_default` | JSONB | "" | — (migration-seeded) | — | migration 219 | — (governed by GROUPS only) | Spec 20 §2 / P26-26B — the Stripe Price ID (price_...) for the single-price v1 subscription checkout. JSONB string; empty = unconfigured (checkout-session route returns the named STRIPE_PRICE_NOT_CONFIGURED 500). Set from the Stripe dashboard (Products -> Price -> API ID). Role-based pricing (stripe_price_id_trade/realtor/manufacturer) is v-next per the Spec 20 rewrite. CONSUMED by src/app/api/subscribe/exchange/route.ts. |
| `stripe_price_product_map` | JSONB | {} | — (migration-seeded) | — | migration 228 | — (governed by GROUPS only) | Spec 116 N2 / Phase 1 — maps a Stripe Price ID (price_...) to the Buildo product it entitles (one of entitlements.product's CHECK values). Empty object = unconfigured; webhook/reconcile/reactivate fall back to lead_gen (OD5 default) and log a WARN when a price is unmapped. Operator sets real price->product pairs from the Stripe dashboard. CONSUMED by src/lib/stripe/client.ts resolvePriceProduct(). |
| `suburban_coverage_ratio` | numeric | 0.4 | 0.01 – 1 | — | seed | group: Coverage & Quality | GFA coverage ratio applied to suburban parcels when estimating construction area (Spec 83 §3) |
| `trust_threshold_pct` | numeric | 0.25 | 0.01 – 1 | — | seed | group: Coverage & Quality | Minimum fraction of permits with parcel-linked cost data before a coverage ratio is trusted (Spec 83 §4) |
| `urban_coverage_ratio` | numeric | 0.7 | 0.01 – 1 | — | seed | group: Coverage & Quality | GFA coverage ratio applied to urban parcels when estimating construction area (Spec 83 §3) |
| `urgency_overdue_days` | numeric | 30 | 1 – 365 | `scripts/compute-trade-forecasts.js` | seed | group: Forecast & Urgency | Days past the predicted start date (as a positive integer) before a trade forecast is classified as 'overdue' rather than 'delayed' (code: daysUntil <= -urgency_overdue_days) |
| `urgency_upcoming_days` | numeric | 30 | 1 – 365 | `scripts/compute-trade-forecasts.js` | seed | group: Forecast & Urgency | Days until the predicted start date at or below which a trade forecast is classified as 'upcoming' rather than 'on_time' |
| `vocab_coverage_pass_pct` | numeric | 90 | 0 – 100 | — | seed | group: Coverage & Quality | assert-global-coverage vocabulary-coverage (spec 49 §3): distinct values present / defined vocabulary >= this integer pct -> PASS CONSUMED by assert_global_coverage. |
| `vocab_coverage_warn_pct` | numeric | 70 | 0 – 100 | — | seed | group: Coverage & Quality | assert-global-coverage vocabulary-coverage (spec 49 §3): >= this integer pct -> WARN; below -> FAIL CONSUMED by assert_global_coverage. |
| `wsib_fuzzy_match_threshold` | numeric | 0.6 | 0.1 – 1 | — | seed | group: WSIB Matching | pg_trgm similarity threshold for Tier 3 fuzzy builder→WSIB name matching; controls both the GIN index filter and the WHERE similarity() comparison. CONSUMED by link_wsib (scripts/lib/compute/link-wsib.js buildTierSql). Already registered and GROUPed before C1 pilot 4's conversion (714dc48e) — kept verbatim; this tag is new at commit 9 cutover, the row itself is not. |
| `zoning_class_coverage_pass_pct` | numeric | 80 | 0 – 100 | — | seed | group: Coverage & Quality | assert-global-coverage: zoning_class calibrated coverage (DEC-1, #406) -> PASS (CoA + permits) CONSUMED by assert_global_coverage. |
| `zoning_class_coverage_warn_pct` | numeric | 75 | 0 – 100 | — | seed | group: Coverage & Quality | assert-global-coverage: zoning_class calibrated coverage (DEC-1, #406) -> WARN, else FAIL CONSUMED by assert_global_coverage. |

---

*Generated from 583 seed vars + 16 migration-only vars + 60 consumer-mapped keys across 2 script dirs.*
