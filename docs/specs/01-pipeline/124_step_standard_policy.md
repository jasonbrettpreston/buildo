# SPEC 124 — Step Standard Policy (the McDonald's standard)

> ## ✅ RATIFIED 2026-08-28 — R-G..R-J
> Extracted from Spec 122/123 (P1–P5, the architecture decisions, the programme and vocabulary rulings) by a read-only policy audit, then finalized and ratified by operator ruling R-G..R-J. ~~Registered in `docs/specs/00_system_map.md`~~ — **CORRECTED 2026-09-10 (measured): no such path exists; the file is `docs/specs/00-architecture/00_system_map.md`** (§7 Step 0 of this SAME file already cites the correct path — this header line is the one site that never caught up). Spec 122's own §1.2a now points back here (R-J) — this is the standalone home of the policy, not a summary of it.

**Status:** RATIFIED · **Scope:** every step in the pipeline estate, present and future — not scoped to the
Step-Opt conversion programme.
**Relationship to Spec 122:** 122 is the *architecture* (descriptor + library + generated validator, under
islands); 124 is the *policy* (what any step, in any architecture, must hold). 122's P1–P5 and its permanent
rulings are the source this spec was extracted from; where 122 is retired or superseded, 124 survives it.
**On conflict during the transition, Spec 122 governs** (it carries the live schema).
**Relationship to Spec 123:** 123 is the *procedure* for getting an existing step to comply; 124 does not
restate it.

---

## §1. Purpose

Every step in this estate is the same step, except for its compute. That is the creed: nothing about how a
step is gated, guarded, validated, recovered, or observed is a per-step invention — it is a declaration
chosen from a closed menu, and the library, the schema, and a small set of shape rules make every other
answer either impossible to omit or impossible to hide. A step author's discretion begins and ends at the
compute. This spec is the durable half of that standard — the part that must still be true after Spec 122's
conversion programme finishes and the packaging it describes is forgotten.

## §2. The rules

Thirteen rules (Rule 13 added by R-R, 2026-08-29). Each is enforced today, or marked ⚠ GAP with the artifact that would close it.

---

**1. Nothing about a step's behaviour may live only in code — it is descriptor data, a declared check, a shape rule, or (last resort) a new schema field.**
- **Enforced by:** `checks` cannot be omitted (`step.schema.json:1095-1099`, `type:"array", minItems:1`); `scripts/ast-grep-rules/compute-shape.yml`'s five rules block the commonest hiding places (console, bare fetch, wall clock, `process.env`, forbidden `require`s) inside compute.
- **Archetype variance:** none.
- **Evidence:** pilot 1's four fences (`assert_schema`) were each moved to descriptor data or a named check rather than left as unexplained code.
- **Violation reads as:** a build failure at `pipeline.step()` construction (AJV) or a red `compute-shape` ast-grep scan in `.husky/pre-commit`.
- ✅ **GAP G-1 CLOSED (2026-08-29).** The *preference order itself* still cannot be verified after the fact (there is no artifact proving a cheaper rung was tried first) — what closes instead is that reaching for the last-resort rung must now be DECLARED, in writing, at the moment it happens. `scripts/steps/_schema/schema-baseline.json` snapshots every `category.field` pair the schema declares; `scripts/steps/_schema/generate-schema-baseline.mjs --check` (shelled by `step-conformance.infra.test.ts`'s "G-1" lock) requires any field NOT in that snapshot to carry `x-ruling: {rungs_tried, why}` on its own schema node, or fails the build — the same "adjudication is a reviewed diff, not something a descriptor grants itself" posture `grandfathered.json` already uses for V7. `--write` regenerates the baseline, intended to run only after a human reviews the new field's `x-ruling` in the same diff. Proven both directions (RED on a fixture field with no `x-ruling`, GREEN on the same field once one is added).

**2. Compute contains domain logic only — no gating, ledger, lock, emission, logging side-channel, environment read, or policy comment.**
- **Enforced by:** `compute-shape.yml` (6 rules — the original 5 plus `compute-no-postgis-branch`, R-W) + `check-step-shape.mjs` (blocking over `scripts/lib/compute/**`) + ~~`step-conformance.infra.test.ts:532-620`~~ **CORRECTED 2026-09-10 (measured, now `:611`/`:670`/`:699` — ~80 lines of drift; cite by greppable anchor, not line number, per Spec 124 §5 R-R's own lesson): `step-conformance.infra.test.ts`'s `COMPUTE_RULE_IDS` battery / "dispatch keys are exactly the descriptor's check ids" test** (dispatch keys ≡ declared check ids, proven to fire on a known-bad fixture).
- **Archetype variance:** none, except ASSERT's compute never opens `ctx.pool` at all (its `outputs`, `recovery`, `counters` are forced `"none"`).
- **Evidence:** the compute-shape gate's own blocking loop was found missing on 2026-08-25 (it scanned and reported but never failed the build) — fixed the same day; the incident is the strongest argument for keeping this a shape rule rather than a review checklist.
- **Violation reads as:** a red ast-grep scan naming the file, line, and rule id.

> **R-W addendum (2026-08-30).** Compute MUST NOT branch on PostGIS availability — `hasPostGIS`,
> `pg_extension`, `postgis_version` (bare identifier, property access, or a string/SQL fragment naming one
> of them), or a `try`/`catch` wrapped around a PostGIS-only call, are all the same anti-pattern: a second,
> silently-selected algorithm hidden inside compute. `guards.requires: {kind: "extension", name: "postgis",
> on_missing: "fail"}` is the only legal form — the step refuses before compute ever runs, and a database
> genuinely missing the extension HALTS rather than silently degrading to a different answer. ~~Three
> precedents on record~~ — **CORRECTED 2026-09-10 (measured): four** (R-W's own §5 register row, below, already
> lists a fourth — `link_parcels` LP-D9 — this addendum text just never caught up): `link_massing` A-8 (in-memory grid/haversine fallback, retired), `compute_centroids`
> A-1(a) (unweighted arithmetic-mean centroid fallback, retired), `link_parcels` A-1 (JS nearest-parcel
> fallback, retired commit 7, 2026-08-30), `link_parcels` LP-D9 (Strategy 1a's `address_points_exact` predicate
> gap, fixed commit 8a — see the §5 R-W row for the full fourth entry). **Enforced by:** a new ast-grep rule,
> `compute-no-postgis-branch` (`compute-shape.yml`), wired through the same always-blocking
> `check-step-shape.mjs` compute corpus as the other 8 rules — no separate wiring needed, since the driver
> runs every rule in the file over `scripts/lib/compute/**`. Proven RED-first on
> `scripts/steps/_schema/fixtures/compute/bad-compute-shape.js`'s `ctx.hasPostGIS` branch (added to the
> fixture's known-violation set), GREEN on all six converted computes (`assert-schema.js`,
> `compute-centroids.js`, `link-massing.js`, `link-parcel-addresses.js`, `link-wsib.js`, `load-ravines.js` —
> none references `hasPostGIS`/`pg_extension`/`postgis_version` today), asserted by
> `src/tests/step-conformance.infra.test.ts`'s `COMPUTE_RULE_IDS` prove-red battery.

**3. Every threshold, sample size, byte window, timeout, retry count, limit, or rate a step consumes is a registered logic variable resolved through `ctx.config`, never a literal — PRESENCE of its `logic_variables` row is always FAIL (R-G), and VALIDITY is per-variable via the closed `on_invalid` enum: `fail` is MANDATORY for a verdict- or write-affecting variable, `default`/`clamp` are allowed only for the rest, each carrying a stated `why`.**
- **Enforced by:** `compute-no-literal-url-tunable`/`-byte-window`/`-threshold` (`compute-shape.yml`) + `step-conformance.infra.test.ts:772-943` (declared ⊆ registry, declared ⊆ admin GROUPS, consumed ≡ declared, each direction proven RED) + `scripts/lib/step/config.js` (PRESENCE half: throws when a declared name has no `logic_variables` row, naming the remedy — LM-D15).
- **Archetype variance:** none.
- **Evidence:** pilot 1's own first violation (`assert_schema` declared `config:"none"` while compute hard-coded `limit=20`, `Range: bytes=0-2048` and `bytes=0-8192`), remediated before pilot 2. **R-G's grounding:** the old P4 sentence ("no row ⇒ FAILED run") never distinguished presence from validity, and practice already contradicted its literal reading — `link-massing.descriptor.json` uses `on_invalid:"fail"` for only 3 of 6 variables, `load-ravines`/`assert-schema` use `default`/`clamp` for every one, each justified in `deviations[]`. This rule states the split that was already true.
- **Violation reads as:** a red conformance test naming the literal, or a runtime throw naming the missing registry row (presence). No machine reads a validity failure yet.
- ✅ **GAP G-4 CLOSED for the VERDICT half (2026-08-29), WRITE-affecting stays PROSE-ONLY.** VERDICT-affecting is mechanically decidable — a `config.logic_variables[]` entry is verdict-affecting iff some `checks[].limit_from_config` in the same descriptor names it, the schema's own established mechanism for "this value moves a check's PASS/WARN/FAIL boundary." `scripts/analysis/step-validate.mjs`'s `checkOnInvalidFail` requires `on_invalid:"fail"` for every such variable, UNLESS `deviations[]` carries a dated, adjudicated exception naming `on_invalid`/`fail` — real descriptors already have one: `load_ravines`'s 6 variables are all `default` despite 3 being verdict-affecting, ratified against the mig-099 cloud-seed-timing hole (`fail` would halt the `sources` chain on every un-seeded database). Measured live 2026-08-29: **0 violations across all 5 converted steps** — every verdict-affecting variable is already correctly `fail`, or covered by a reviewed deviation. WRITE-affecting has no equivalent named schema mechanism anywhere (no `*_write_affecting` field convention exists) and stays an honest GAP rather than an invented one.
- ✅ **GAP (R-D) CLOSED (corrected 2026-09-03, WF3 cloud-parity FIX 1.6 — this line was stale).** The chain-start presence check (`declared_logic_variables_present` on `assert_schema`, `severity:"FAIL"`, `blocking:true`, `when:"pre"`) IS in the descriptor: 42 keys in `checks[].expect`, locked equal to `config.probe_presence` and the live `scripts/lib/declared-logic-variables.js` derivation by `src/tests/steps/assert_schema/`. `config.js`'s runtime throw (LM-D15) remains the backstop for steps that run no `assert_schema` at all (entities/wsib/deep_scrapes — a separate, narrower gap, unchanged). See Spec 122 R-D.

> **R-AU addendum (2026-09-21, operator-adjudicated).** This rule's reach is SCALAR TUNABLES.
> Pricing DATA — a DB rate table, or a keyed catalogue of priced lines with its permitted-value
> vocabulary — is not a tunable and is NOT externalized by porting it: it is carried byte-for-byte
> and DECLARED in the descriptor as a NON-ADMIN SURFACE (`limitations[]`), so a reader can see that
> the values exist, where they live, and that no admin surface edits them. Declaring them under
> `config.logic_variables[]` would be a second source of truth for the same values and a false
> claim of admin tunability. Every scalar the step consumes still obeys the rule above, unchanged.
> **This is an INTERIM state carrying a named closing row, not a permanent exemption** — see §5 R-AU.

**4. A rule kept in compute rather than declared data must still be written down — as a check's `why`, not a comment.**
- **Enforced by:** nothing machine-checked. Applied narratively in the Intent Ledger, whose discoverer≠ adjudicator role split is Spec 123 §7.1 (§4.2 below states the *rule*).
- ✅ **GAP G-2 CLOSED (2026-08-29), narrowed honestly.** A disposition (a markdown table row in an assessment report) and a specific check's `why` (a JSON string in a descriptor) share no id to join on — there is no mechanical way to prove row N's rule IS check id X's `why` without semantic understanding this tool does not have. What closes instead, matching the report culture's own existing practice (pilot 3's Intent Ledger already writes *"preserved-in-compute (`buildMatchSql`, A-2) ... DECLARED in `notes.json`"*): `scripts/analysis/step-validate.mjs`'s `checkPreservedInComputeHasWhy` requires every `preserved-in-compute` Intent Ledger row to itself NAME where the rule is written down (`why`/`notes.json`/`checks[]` appearing in the same row) — feeds the Rule-4 policy-matrix status. Proven both directions in the tool's own self-test (Spec 121 §12b.6), exercised on every `step:validate` run. **Measured live 2026-09-03 (CC-D4), superseding the 2026-08-29 reading:** 1 of 8 converted steps (`link_wsib` 4/8 rows ungrounded) currently fails this check — `assert_schema` is now green (its 5/6-ungrounded reading was stale; peel 8b's row-derived cascade closed it) and `compute_centroids` was closed this commit (3/3 rows grounded — 2 cited in-row, 1 re-disposed `knowingly-retired`, `docs/reports/2026-08-29-pilot6-compute-centroids-assessment.md` §11). `link_wsib`'s red is left as-is, one finding per WF3, per the same "do not retroactively fix pilots 1-4" discipline `review_followups.md`'s R-R backfill entry already states. **Re-measured live 2026-09-03 (LW-D21, same day, later commit):** `link_wsib` narrowed 4/8 → 2/8 ungrounded — 2 rows cited in place (`30ff8805`, `b71db6e0`; no descriptor/notes.json change), 2 rows (`a81c6a7c`, `76dcca28`) had a REFUTED premise (the construct they describe was never ported into the converted compute — the same supersession class CC-D4 found) and were reported for operator ruling rather than silently re-disposed, per that WF3's explicit instruction. **Ruled the same day, one commit later:** the operator adjudicated both — `a81c6a7c` → `encoded-as-descriptor-field`, `76dcca28` → `knowingly-retired` (mirrors `1933c1e0`'s own Row C precedent). **GAP G-2 is now fully CLOSED across all 8 converted steps** — 0 of 8 steps fail this check as of `link_wsib`'s re-write; see `docs/reports/2026-08-28-pilot4-link-wsib-assessment.md` §12 and `docs/reports/defect-ledger.md` `LW-D21` (CLOSED). **Caveat: this check is textual, not semantic** — `checkPreservedInComputeHasWhy` passes on the literal presence of `why`/`notes.json`/`checks[]` in the row, so a row can read green while its own prose says in words that the grounding is absent (a gap already filed as a followup); the gate is a citation check, never weakened to a semantic one.
- **Archetype variance:** none.
- **Evidence:** pilot 1's coordinate-source OR-contract — kept in compute, its rule stated in the descriptor `checks[].why` and `limitations` by ruling, not by a check.
- **Violation reads as:** nothing today (this is exactly the ⚠ GAP).

**5. `checks` may never be `"none"` — every step declares at least one.**
- **Enforced by:** `step.schema.json:1095-1099` — `checks` is typed `array, minItems:1`; a step cannot construct without it.
- **Archetype variance:** none — the one category every archetype leaves untouched, even ASSERT (which forces `outputs`/`recovery`/`counters` to `"none"` but not `checks`).
- **Evidence:** all 3 converted steps carry 8–19 checks each; ASSERT (`assert_schema`) has the fewest live categories of any archetype but still declares 9.
- **Violation reads as:** a schema-validation throw at `pipeline.step()` construction, before compute runs.

**6. Every category is present, and every field inside it is answered — `"none"` is legal but must be written, never omitted.**
- **Enforced by:** `step.schema.json`'s top-level `required` (~~18 categories~~ **CORRECTED 2026-09-10 (measured): 20**) plus per-field `required` arrays nested throughout.
- **Archetype variance:** none.
- **Evidence:** the schema itself is the enforcement; a descriptor missing any of the 18 fails AJV outright.
- **Violation reads as:** a schema-validation throw naming the missing field.

**7. A step's archetype determines which categories are live — a step may not omit a category its archetype requires, and may not answer a category its archetype forbids.**
- **Enforced by:** `step.schema.json`'s `allOf` blocks keyed on `identity.archetype` (`step.schema.json:1373-1443`) — six profiles, one per non-degenerate archetype grouping.
- **Archetype variance:** this rule *is* the variance mechanism — see §3 below.
- **Evidence:** the schema comment on the ENRICHER profile names claim #54 (a `staleness.scope` on a lineage column implies a declared invalidator) as "the centroid defect made unexpressible."
- **Violation reads as:** a schema-validation throw at construction naming the archetype and the forbidden or missing field.

**8. A write target's discipline class, guard, and scope are declared per write target — never assumed for the whole step.**
- **Enforced by:** `step.schema.json`'s `outputs.writes[].write_discipline` shape, with `if`/`then` blocks requiring `guard_why` when `guard:"none"` (`:279-280`) and `why` for `staging_full_replace` (`:291-292`).
- **Archetype variance:** applies to INGESTOR/LINK/MATCHER/ENRICHER (any archetype whose `outputs` is an object); N/A for ASSERT (`outputs` forced `"none"`).
- **Evidence:** `link_massing`'s E1/E2 targets declare two different disciplines (`set_based_scoped` guarded none, `guarded_upsert`) on two different write targets in the same step.
- **Violation reads as:** a schema-validation throw naming the write target and the missing `why`.

**9. A banned write shape (an unguarded set-based write, an insert-only step with no retraction) may ship only with a dated, committed, SHA-anchored ledger entry naming who approved it — a `why` string alone is not enough (promoted from pilot practice, R-I.2).**
- **Enforced by:** `scripts/steps/_schema/grandfathered.json`, read by `assertGrandfathered` at `pipeline.step()` construction (before any pool opens) — refuses a banned value with no matching entry even when the schema's own `guard_why` is present.
- **Archetype variance:** applies wherever `outputs` is an object; N/A for ASSERT.
- **Evidence:** `link_massing`'s E1 target (`write_discipline.guard:"none"`) is grandfathered against commit `5bb31faf`, operator name, and date — the schema's own fixture for this shape is grandfathered the same way, so the rule cannot be satisfied by weakening the fixture.
- **Violation reads as:** a construction-time throw refusing the descriptor, naming the ungrandfathered path.

**10. The verdict is always derived from the declared checks' rows — never a parallel boolean, never a hardcoded literal.**
- **Enforced by:** `scripts/lib/step/verdict.js` (`deriveVerdict(rows)`) is the only place a verdict is computed; `step-library.logic.test.ts:145-283` — "the verdict is ROW-DERIVED, and all three values are reachable."
- **Archetype variance:** none — runner-owned, no archetype may opt out.
- **Evidence:** AS-D1 (sources verdict read the raw error array, not the row set) and LM-D2 (a hand-rolled per-script cascade plus a boolean-driven severity) both closed by routing through this single function; a repo-wide grep for `'FAIL'`/`'WARN'` literals across the library returns exactly the rows inside `verdict.js` itself.
- **Violation reads as:** a red `step-library.logic.test.ts` assertion, or (pre-conversion) a fleet-wide finding that a script structurally cannot emit WARN or FAIL.
- **R-Q / R-R note (2026-08-29):** `step-library.logic.test.ts` is outside `step:validate`'s own run scope (spec-named as step-conformance + golden-fingerprint + `src/tests/steps/<slug>/` only), so `step:validate`'s policy matrix used to print this rule PROSE-ONLY per step rather than claiming a false enforced-green — a scope caveat, not a claim the rule is unenforced elsewhere. Separately, this commit's G6 ledger-normalization work fixed one concrete instance of a severity-conflated counter reaching the ledger (`LPA-D2`), the same defect class R-Q (`checks_failed`/`checks_warned` split, `LPA-D6`) closed in the library itself.
- **Enforced by (WF2 "Rules 10/11/12 mechanical checkers", C1, 2026-09-03):** `scripts/analysis/step-validate.mjs`'s `checkVerdictSingleSource` closes the PROSE-ONLY gap above with a genuine mechanical checker over a closed corpus, `VERDICT_LIBRARY_CORPUS` (every `scripts/lib/step/*.js` file except `verdict.js` itself + `scripts/lib/pipeline.js` + `scripts/lib/source-version.js` — the files that see a full `rows` array and could hand-roll a second cascade). Two halves: **(a)** no unsanctioned second PASS/WARN/FAIL derivation anywhere in that corpus — `pipeline.js`'s `emitSummary` escalate-only recompute is the ONE declared, cited exception (`SANCTIONED_VERDICT_SITES`; not behaviour-identical to `deriveVerdict` — never downgrades, preserves SKIP/UNKNOWN), and `scripts/lib/source-version.js`'s prior hand-rolled duplicate was routed through `deriveVerdict` in this same commit (behaviour-identical, proven in `step-conformance.infra.test.ts`); `scripts/lib/step/plausibility.js`'s `verdictCascade` (a second duplicate, consumed by the live `assert_parcel_sanity` step) was retired outright, its one consumer now importing `deriveVerdict` directly. **(b)** a `SELF_SKIPPED` terminal's audit rows must fold to a verdict other than PASS, AND that verdict must be genuinely row-derived (≥1 declared non-INFO row, `verdict === deriveVerdict(rows)`) — **CLOSED 2026-09-09 (WF3 VRD-SKIP, Rule 10 ladder rung b, see §7 below and the `VRD-SKIP` row of `programme-items.json`, now `BUILT`):** `skipRecordsMeta`'s `status` row declares severity `WARN` with threshold `'ran'` (was `INFO`/`null`), so the checker now ships `enforced-green`. Was pinned `KNOWN-DEFECT` per Spec 123 §3.1 against `review_followups.md` ("A lock-skipped converted step verdicts as PASS", now RESOLVED) from 2026-09-03 until closure. Both halves proven both-directions: `--self-test-only` runs in-memory RED/GREEN proofs on every invocation (`step-validate.mjs`'s own `selfTest()`), and `compute-no-verdict-derivation` (`scripts/ast-grep-rules/compute-shape.yml`) bans a compute from ever assigning anything named `verdict`, proven on `scripts/steps/_schema/fixtures/compute/bad-verdict-parallel.js` + the extended `bad-compute-shape.js` battery.

> **R-H addendum (2026-08-28).** Rule 10 governs HOW the verdict is derived, not WHICH severity a check should choose. That gap let pilot 3's ruling A-6 dispose `nearest_footprint_gt_lot_count` and `shared_primary_buildings` as INFO for a standing non-zero population — conflicting with Spec 48 §4.9 / `tasks/lessons.md:117`: **a metric expected to be permanently non-zero is WARN with a declared, machine-observable retighten condition — never FAIL, never INFO-by-taste.** INFO is legal ONLY for a purely descriptive counter no threshold could bound, and that exception must itself be declared in `checks[].why`. Consequence: **LM-D6/LM-D11 re-dispositioned OPEN → WARN + a declared retighten condition, carried to pilot 4** (defect ledger + review followups amended in the same commit). ⚠ **GAP G-5** — no check yet distinguishes "purely descriptive, no threshold possible" from "INFO chosen to avoid a red"; author judgment only, same class as G-2. **First closed instance (LG-29, WF3 VRD-SKIP, 2026-09-09):** a `SELF_SKIPPED` terminal is the maximal case of "a check the library could not evaluate" — zero declared checks ran — so `status:'INFO'` on the skip's own `status` row was exactly the "INFO chosen to avoid a red" failure mode G-5 names, not a purely descriptive counter (a threshold — `'ran'` — was possible all along). Closed by flipping that one row's declared severity to `WARN`; `skipRecordsMeta` (`scripts/lib/step/index.js`) is this addendum's named precedent for the self-skip case specifically.

**11. A ruling that reorders a step's phases must re-derive every "before X" guarantee stated in that step's own governing spec, in the same commit (promoted from pilot practice, R-I.1).**
- **Enforced by (STALE until this amendment — `checkOrderGuaranteesCited` existed since `b2a85079`, 2026-09-03, but this row was never updated to say so):** `scripts/analysis/step-validate.mjs`'s `checkOrderGuaranteesCited(descriptor)` — the DECLARED half only (GAP G-3's completeness half, below, stays open). Verifies every `checks[].when:"pre_write"` entry carries `order_guarantee{guarantee, spec_ref, anchor}` (schema-enforced, re-verified defensively), that `spec_ref` resolves to a real `docs/specs/**` file, that `anchor` is found LITERALLY in that file (a rotted citation REDs), and that `spec_ref` agrees with the descriptor's own `identity.spec` — a pre_write check's ordering guarantee must trace to THIS STEP's own governing spec, never an unrelated one. **Ask 6 amendment (pilot 9 commit 1, 2026-09-04):** `identity.spec` is single-valued, but a step's outputs can genuinely span more than one governing spec (`enrich_parcels`: `identity.spec` `"65"`, pass 5's own post-commit-read order_guarantee governed by Spec 78 §P3A.1 — grep-confirmed `comp_*`/`opt_*`/`optimal_config` appear only in Specs 78/88/89/100, never 55 or 65). `checkOrderGuaranteesCited` now accepts a spec-qualified anchor, `"<specnum>:<anchor text>"` — when present, the spec_ref-agreement check resolves against the QUALIFIER's own spec instead of `identity.spec`'s; an unqualified anchor is byte-for-byte the pre-existing behavior. Proven both directions against the real `docs/specs/` tree in `step-validate.mjs`'s own `selfTest()`: an unqualified anchor whose `spec_ref` disagrees with `identity.spec` still REDs (the qualifier, not the mere citation, is what licenses the disagreement); a qualified anchor naming the SAME spec `spec_ref` already points at GREENs; a qualifier naming a spec number that resolves to no file REDs.
- ⚠ **GAP G-3** — the DECLARED half above is real and non-vacuous, but its own completeness is not proven: nothing re-checks a step's governing spec for "before X" language when the library's phase order changes generically (e.g. a future ordering change to LINK or a second ENRICHER), and nothing forces a `pre_write` check + `order_guarantee` to be AUTHORED in the first place merely because a step's spec states a "before X" guarantee — `enrich_parcels`'s own pass 5 (Spec 78 §P3A.1) needed this pilot to explicitly author `pass5_post_commit_read_order` before Rule 11 had anything to check.
- **Archetype variance:** matters most for INGESTOR (acquire→validate→write→score) and any archetype using `checks[].when:"pre_write"`; N/A for ASSERT (no writes to order against). ENRICHER (pilot 9) is the first archetype whose post-commit phase order guarantee spans a SECOND spec beyond `identity.spec`.
- **Evidence:** LR-D9 — converting `load_ravines` to the INGESTOR archetype silently retired Spec 59's write-order guarantee until the Regression Guardian caught it in a fold review, one commit after it shipped. `enrich_parcels` (pilot 9) — the multi-spec case above, closed by this amendment rather than left as a second per-incident fix.
- **Violation reads as:** a `checkOrderGuaranteesCited` violation in `step:validate`'s Rule 11 matrix row (`enforced-red` when a real violation exists); GAP G-3's completeness half is still nothing today, absent a reviewer catching it by hand.

**12. A write target with a destructive full retraction (`retract_when:full_only` or `retract:"all"`) declares a truthful crash-recovery posture — `"none"` is legal only with a stated why.**
- **Enforced by:** `step.schema.json`'s `recovery.interrupted` field (required when a destructive retraction target exists) + ~~`step-conformance.infra.test.ts:1039-1073`~~ **CORRECTED 2026-09-10 (measured, now `:1647-1656` — ~600 lines of drift; cite by greppable anchor, per Spec 124 §5 R-R's own lesson): `step-conformance.infra.test.ts`'s "recovery.interrupted reddens against its destructive retraction target" battery** (RED on `"none"`, RED on missing).
- **Archetype variance:** applies to LINK and any INGESTOR with a full-retraction write; N/A for ASSERT/RECORDER.
- **Evidence:** a killed `link_massing` forced-FULL run left `parcel_buildings` at 29,330/520,492 rows; the next run's gate read "unchanged" and the hole persisted silently — this is the incident the rule exists to make undeclarable.
- **Violation reads as:** a schema-validation throw at construction. *(Note, updated 2026-08-29: BOTH halves are now CLOSED. The DECLARATION half — `recovery.interrupted` and the paired `recovery.before_image` (R-M), both required, truthful, schema-enforced fields, R-M's before-image mechanism BUILT and LIVE-FIRED (`d07529af`, LG-17 — `link_massing`'s genuine forced-FULL wrote a 520,492-row before-image file before its retraction ran). The RUNTIME half — `staleness.detectInterruptedRetraction` auto-detects a `crashed`/stuck-`running` prior run for a step's own producer slugs and `selectMode` forces FULL unconditionally, wired into `runCascadePhase`/`runLinkPhase` (`LW-D20`/`LG-19`, Spec 122 R-B) — closed by a live kill-and-rerun proof against `link_wsib` that found and fixed two real bugs (a step self-detecting its own just-opened row; the check being unreachable behind the ledger gated-skip), both now regression-locked fast (fake-pool) and against real Postgres. "Declared" and "automatically enforced" are now the same posture.)*
- **Enforced by (WF2 "Rules 10/11/12 mechanical checkers", C3, 2026-09-03) — `step:validate`'s own matrix, closing this rule's prior PROSE-ONLY gap from THAT tool's own vantage:** `scripts/analysis/step-validate.mjs`'s `checkInterruptedPostureTruthful(descriptor)`, two parts. **(a) Declaration** — the same "destructive retraction requires `force_full_on_next_run`" rule as the existing `step-conformance.infra.test.ts` R-B lock, re-verified here. **(b) Reachability** — a NEW, static, runner-derived claim beyond the declaration: for the descriptor's `execution.shape`, is `staleness.detectInterruptedRetraction` actually REACHABLE on every path through that shape's runner in `scripts/lib/step/index.js`? Derived from a source scan (`extractFunctionBody` + `runnerReachability`), never hand-listed — a runner behind a `staleness.ledgerGatedSkip` early-return is reachable only if its `bypassed` term folds in `interruptedRetraction.interrupted` BEFORE that call (the exact shape `runMaterializePhase` lacked until `febd0968`, closed AHEAD of this checker landing per Fold A item 2 of `.cursor/wf2_rules_10_12_checkers_active_task.md`); a runner with no such early-return instead reaches it via an unconditional `staleness.selectMode` call. Measured live over all 8 converted descriptors: `link`/`link_keyed`/`cascade` (the three declaring `force_full_on_next_run`) all REACHABLE; the other five declare `"none"` (no reachability claim to verify). Proven both directions via `selfTest()` (RED on the exact pre-fix `runMaterializePhase` shape reconstructed in-memory; GREEN on the post-fix shape, a LINK-shaped unconditional-`selectMode` runner, and a declaration-only violation; RED on a runner reaching neither `ledgerGatedSkip` nor `selectMode`). **Behavioural proof, WRITTEN but BLOCKED** (the DB-tier half beyond static reachability): `src/tests/db/step-crash-posture.db.test.ts` spawns a REAL `link_wsib` process, SIGTERMs it once its `pipeline_runs` row reads `running`, and asserts the row stays `running` (no SIGTERM handler exists anywhere in this codebase — grepped — so Node's default disposition abandons the in-flight `finalizeLedgerRow` call) AND the next `staleness.selectMode` call over the SAME descriptor/pool resolves `mode:"full", reason:"recover_interrupted_retraction"` — declared posture matching MEASURED behaviour, not just source reachability. Committed `describe.skip`-gated: measured live 2026-09-03 that a real converted step spawned against the ephemeral `BUILDO_TEST_DB` testcontainer always refuses at `assertDbTarget` (`scripts/lib/resolve-db.js`) — the container is named `buildo_test`, every descriptor's `database.assert_current_database` is the single literal `"postgres"` with no test-context override, and step scripts have no descriptor-path override hook either. Filed HIGH in `review_followups.md` ("WF2 Rule 12 behavioural half — no `db.test.ts` can spawn a REAL converted step against the ephemeral test container") as its own scoped WF2/WF3 — a deliberate production safety fence (Spec 122 §P0), not weakened under this WF2's time pressure. Until that lands, the STATIC reachability half above + the existing fake-pool unit lock (`src/tests/step-library.logic.test.ts`) are the enforced mechanisms. Scoped, once unblocked, to ONE archetype first (CASCADE via `link_wsib`, the same step LW-D20/LG-19 were originally found against) — the other two live shapes (`link`/`link_massing`, `link_keyed`/`link_parcels`) are `programme-items.json`'s `CRASH-BEHAV` (`nice_to_have`, `NOT_STARTED`).

**13. A step validates itself — the Spec 123 §6 scorecard and this spec's own policy-coverage matrix are generated by one command, run for every converted step, and enforced (never a separate, hand-maintained track).**
- **Enforced by:** `scripts/analysis/step-validate.mjs` (`npm run step:validate -- --step=<slug>` / `--all`): runs `validateDescriptor` (Rules 1, 5–9), the ast-grep shape gate scoped to the step's own file + compute (Rules 1–2), the step's own `src/tests/steps/<slug>/` + the shared conformance/golden-fingerprint suites, the golden-capture invocation/fingerprint/diff check (G8), and derives the full G0–G9 scorecard plus a Rules-1–13 coverage matrix — all from committed artifacts. `step-conformance.infra.test.ts` asserts every converted step's assessment report carries a `## Validation scorecard (generated)` block equal to a fresh run (drift is red). `.husky/pre-commit` runs `step:validate --all --fast` first (the vitest spawn skipped, everything else run, plus 7 always-on fast invariants); `.husky/pre-push` re-runs it plus `typecheck` (defeats a bare `--no-verify`).
- **Archetype variance:** none — every archetype's descriptor is validated, shape-gated, and scored the same way.
- **Evidence:** pilots 1–5 backfilled by `step:validate --all --write` (R-R, 2026-08-29) — `link_parcel_addresses` scored 8/17 on the FIRST honest run (real hard-stop: `LPA-D2`'s ledger status wasn't literally `CLOSED`/`PIN`, and a later WF3 fix's `records_meta` shape change wasn't yet re-explained in the report), fixed by two real documentation commits rather than by loosening the gate — 14/17 after.
- **Violation reads as:** a red `step-conformance.infra.test.ts` assertion naming the stale/missing scorecard block, or a non-zero `step:validate` exit (a hard stop on G6/G7/G8/G9 or a fast invariant) in `.husky/pre-commit`/`pre-push`.
- **Rule 13's own boundary (R-T addendum, filed 2026-08-29, BUILT 2026-08-30 — this same programme's follow-on WF2).** `step:validate` conflates two different questions under one name: whether a step's *process* was followed (G0–G9, the policy coverage matrix) and whether a step's *data* is plausible (Spec 79/120/30 §5.4.1's bound doctrine — **not bare "§5"**, Fold B-6, 2026-08-30: Spec 30 §5 is the CI/CD section; §5.4.1 is the runtime verdict/halting-semantics section the bound doctrine actually lives in). **The rule: "the validator validates DATA (Spec 79/120/30 §5.4.1); the scorecard validates process, from structured artifacts."** Both halves are now BUILT: the scorecard half (G0–G9, as specified above) and the DATA half — `invariants[]`/`plausibility[]` bounds (threshold-derived/reachable/last-measured-value-beside-the-bound, §9 below), the seam-validation pass (`scripts/lib/step/seam.js` — over `inputs.reads.steps[]` declared step-edges, corrected from the original "`outputs.invalidates` edges" phrasing: that category is `{table,column,when}`-shaped, a step's own re-eligibility scope, not a producer→consumer edge — closes VAL-3/LDG-2/LDG-7's concern), chain-end synthesis automation (`scripts/analysis/chain-end-synthesis.mjs`, the `sources` chain's roll-up + the declared cloud trigger for `validate_only` invariants, Ask 6(b)), and R-U's chain-run correlation (`records_meta.chain_run_id`, below) that both the seam pass and chain-end synthesis join on. The layer table above states WHERE each half lives (descriptor declares, library executes, compute only measures). **The DATA/PROCESS split has a SECOND enforcement point, not just declaration (Fold B-3):** every synthetic invariant/plausibility row carries a REQUIRED, closed `source: check|invariant|plausibility` field distinct from `kind` (`scripts/lib/step/verdict.js`'s `checkRow`) — so a consumer reading only the EMITTED audit table (never the descriptor) can still tell a DATA row from a PROCESS row without re-deriving it from naming convention; chain-end synthesis and `step-validate.mjs`'s scorer both key off this field. **Scoped as BUILT, not fully DE-REGEXED**: the scorecard's own six prose-regex gates (`G0`/`G1`/`G3`/`G4`/`G5`/`G9`, `section()`/`splitTableRow()`-scraped from free-form report text) are UNCHANGED — only `G2`'s vacuous-green (Spec 123 §6's own documented defect) and the defect-ledger/`notes.json` strict-row-schema parse (`G6`/`G7`/`G4d`) were fixed, per Ask 3's own scoped default; the larger de-regex lift is filed as `nice_to_have` in `programme-items.json` (§9 below), not silently dropped. Programme item `VAL-WF2` (`batching_prereq`) CLOSES with this commit; `REGEXSC` is `SUPERSEDED` by it (unchanged from the original filing); `G2` is `PARTIAL` — the vacuous-green half closes here, the churn×complexity-plot BUILD half stays its own `S6b` item (operator ruling 2026-08-25 decision 2, `review_followups.md:2994`, unchanged).

## §3. Archetype variance table

`✓` = must answer per the schema's `allOf` profile · `N/A` = category is forced `"none"`/inapplicable by
profile · text = modified how.

| Rule | ASSERT | INGESTOR | LINK | MATCHER | ENRICHER | BACKFILL | MATERIALIZER | RECORDER |
|---|---|---|---|---|---|---|---|---|
| 1 Nothing hidden | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| 2 Compute is just compute | ✓ (no `ctx.pool` reachable) | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| 3 Tunables externalized (presence/validity) | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| 4 Compute rule declared | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| 5 checks ≥ 1 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| 6 Omission fails | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| 7 Archetype gates categories | forces `outputs`/`recovery`/`counters`→`none` | `outputs` must be an object | `invalidates` non-empty, `counters` object | `invalidates` non-empty, `counters` object | `invalidates` required when `staleness.scope` keys a lineage column | `recovery.reset` ≠ `none` | `recovery.reset` ≠ `none` | `outputs.publish` ∈ {direct,pointer} |
| 8 Per-target write discipline | N/A (`outputs:"none"`) | ✓ | ✓ | ✓ | ✓ | ✓ (if it writes) | ✓ (if it writes) | ✓ (if it writes) |
| 9 Banned write needs ledger | N/A | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| 10 Verdict row-derived (+severity, R-H) | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| 11 Phase-order re-derive | N/A (no writes) | ✓ (acquire→validate→write→score) | modified — link/retract ordering | modified | modified | modified | modified | N/A (verdict_only) |
| 12 Truthful crash posture | N/A | ✓ if full-retraction write | ✓ | ✓ if applicable | ✓ if applicable | ✓ if applicable | ✓ if applicable | N/A |
| 13 A step validates itself | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |

## §4. When the policy is unclear — the ruling protocol

1. An agent pass (or a reviewer) **discovers** the ambiguity and proposes a disposition, using the closed Intent Ledger vocabulary (`preserved-in-runner \| preserved-in-validator \| preserved-in-compute \| encoded-as-descriptor-field \| encoded-as-deviation \| knowingly-retired`).
2. **A different party adjudicates — never the pass that discovered it.** This is a **policy rule**, not merely a procedure step (Spec 123 §7.1, restated here per R-I.3): the same pass may never both discover a fence/ambiguity and rule on its disposition. The adjudicator is named and dated.
3. The ruling is recorded in **§5's Register of rulings** with an id, the statement, and what it amends.
4. **A rule without a lock is not yet a rule** — the same commit that records the ruling adds a both-directions test (red on the old behaviour, green on the new) proving it.
5. If the ruling changes what §2 says generically (not just for one step), this spec is amended in the same commit as the lock — a ruling that only lives in a pilot report or a defect-ledger row has not yet reached policy.
6. **Promotion criterion — pilot-local → estate-wide.** A ruling stays pilot-local (a Defect Ledger row, `<PREFIX>-D<n>`) until it is **expected to recur in another archetype** — that expectation is what promotes it into this register as a lettered ruling. The feeder is the `§R Reflection` "RECURRING/STANDARD-SHAPING" table (Spec 122 R-F; Spec 123 G9): an issue entered there with a named archetype match becomes a register candidate at the next ruling pass, not before. A ruling with no recurrence claim stays a ledger row.
7. **A document move or consolidation this protocol produces is verified by a line-set diff before commit (R-I.4)** — every non-empty moved line found verbatim in the destination, nothing dropped; a mismatch is filed as a defect, never silently merged. A transcription claim is executable, and is therefore executed.

## §5. Register of rulings

**This section is the authoritative register going forward (R-J, 2026-08-28).** Spec 122's own top-of-file
tables — the three architecture decisions (A1–A3), the six programme rulings (R1–R6), the seven vocabulary
rulings (V1–V7), and its copy of R-A..R-F — remain as **history**: read them there for full pre-2026-08-28
text and evidence. This table is live for anything estate-wide from here on.

| # | Ruling (one line) | Amends | Full text |
|---|---|---|---|
| R-A | Retirement of a tunable is a declaration (`config.retired[]`), never deletion of the live registry row; a retired name still holding a live row is a WARN | Rule 3 · `config` category | Spec 122 R-A |
| R-B | `recovery.interrupted` is a required, truthful declaration on any destructive-retraction write target. **CLOSED for the declaration + before-image (R-M) as of pilot 4** (`d07529af`, LG-17 — before-image live-fired on a genuine forced-FULL) **and CLOSED for the runtime reader as of 2026-08-29** (`LW-D20`/`LG-19` — `staleness.detectInterruptedRetraction` + `selectMode`'s unconditional FULL branch, wired into `runCascadePhase`/`runLinkPhase`; two bugs found and fixed by a live kill-and-rerun proof against `link_wsib` — see Spec 122 R-B and the pilot 4 assessment §R addendum). "Declared" and "automatically enforced" are now the same posture for any step declaring `force_full_on_next_run`. **Recurrence found and closed 2026-09-03**: `runMaterializePhase` (added at pilot 5, `5ee14f5b`) never received this fold — its `bypassed` omitted the `interruptedRetraction.interrupted` term entirely, the same unreachable-behind-the-ledger-gated-skip shape LW-D20 fixed for CASCADE — now wired identically (fake-pool RED→GREEN lock in `step-library.logic.test.ts`; no live MATERIALIZER declares `force_full_on_next_run` today so this closes a latent archetype-level gap, not an observed defect) | Rule 12 | Spec 122 R-B |
| R-C | The golden differential is a mechanical fingerprint lockfile gate, never a commit-message claim. **CLOSED** — `golden-fingerprint.infra.test.ts` was built at `3f7d6645` (pre-dates the R-T addendum programme); this row's own "not yet built" note was stale documentation debt, corrected 2026-08-30 while adding the row below. **Extended, R-T addendum commit 3 (Fold B-10):** the SAME "mechanical, not claimed" principle now also proves an invariant/plausibility MIGRATION preserves VALUES, not just declarations — before a migrated step's golden `invariants.json` is deleted, a round-trip regression lock proves the descriptor-driven executor's computed value is IDENTICAL to the retired file's value for the same `sql`, against real DB state (`link_massing`, commit 3, first migrated; the remaining 4 steps' commit-4 migrations inherit the same executor code path this lock validated, not a per-step re-proof) | §7 Step 4 | Spec 122 R-C; `golden-fingerprint.infra.test.ts` |
| R-D | `assert_schema` gains `declared_logic_variables_present` (blocking, `when:"pre"`) wherever it runs today; ✅ in the descriptor (42 keys, corrected 2026-09-03 — was stale ⚠ GAP text) — LM-D15's throw remains the backstop only where `assert_schema` itself does not run | Rule 3 | Spec 122 R-D |
| R-E | Gate G7's mutation-≥80% clause is replaced by a both-directions red-first lock per class-A behaviour; mutation testing is a bounded followup, not a gate | Spec 123 §6 G7 | Spec 122 R-E |
| R-F | A standing `§R Reflection` section (low-confidence + recurring/standard-shaping tables) is required after every pilot's cutover starting pilot 4; feeds §4.6's promotion criterion | §4.6 | Spec 122 R-F |
| R-G | PRESENCE of a declared variable's `logic_variables` row is always FAIL (LM-D15, R-D); VALIDITY is per-variable via the closed `on_invalid` enum — `fail` MANDATORY for a verdict-/write-affecting variable, `default`/`clamp` allowed otherwise with a `why` | Rule 3 (rewrites the old P4 sentence) | this spec, §2 Rule 3 |
| R-H | A metric expected to be permanently non-zero is WARN with a declared, machine-observable retighten condition — never FAIL, never INFO-by-taste; INFO only for a purely descriptive counter no threshold could bound, declared in `checks[].why`. LM-D6/LM-D11 re-dispositioned OPEN → WARN+retighten, carried to pilot 4 | Rule 10 addendum | this spec, §2 Rule 10 addendum; Spec 48 §4.9; `tasks/lessons.md:117` |
| R-I | Four pilot-practice promotions: phase-reorder re-derivation (Rule 11), grandfathered-ledger requirement (Rule 9), discoverer≠adjudicator as policy not just procedure (§4.2), document-move line-set-diff verification (§4.7) | §4 | this spec, §4 |
| R-J | Spec 124 is the standalone home of this policy; Spec 122 §1.2a is now a pointer here; this register is authoritative going forward | Spec 122 §1.2a | this spec (whole) |
| R-K | A stage-gated (not-yet-converted) step's tests are declared data, never a code skip: `scripts/steps/_schema/converted.json.pending` names the step file (`registers_at`/`reason`/`declared`) and every genuinely-red claim for it is wrapped `it.fails(...)` with a "flips at commit N" comment; conformance ties every `it.fails` under `src/tests/steps/<slug>/` to its `pending` slug and enforces `pending ∩ converted = ∅`. Cutover deletes the `pending` entry AND flips the tied `it.fails` calls to plain `it()` in the SAME commit — commit 7 (`69de8a13`) declared the `link_wsib` `pending` entry, commit 9 (`903fe5a7`) deleted it. **Extended, R-K.1 below (pilot 9, 2026-09-04): the SAME declared `pending` entry now also gates `step-validate.mjs`'s hard-stop set** — see R-K.1's own amendment for the mechanics | §4.6 (promotion/staging mechanics) | Spec 122 R-K |
| R-L | A destructive-repair step's autonomous mode-`full` trigger is gated behind a per-chain `manifest.json` `chain_args` argv declaration, never a bare corpus-signal check: `chain_args: {"sources": ["--full"]}` makes `explicitFull` structurally true only on the chains authorized to retract, so `selectMode`'s `forced \|\| (explicitFull && changed)` resolves `full` off the real corpus-change signal on `sources` while `permits` (no `--full`) can never resolve `full`. **Enforced by (STA-3, WF1 "state tables reset", 2026-09-03):** `scripts/lib/step/reset.js assertForceFullAuthorized` — a `generateReset` execution refuses unless `override.force_full === true` AND the slug's own `manifest.json scripts[slug].chain_args.sources` declares `"--full"`, both directions locked (fake-pool) in `src/tests/step-library.logic.test.ts` | Rule 3 worked-example pattern; `staleness`/mode-select | Spec 122 R-L |
| R-M | A write target with a destructive retraction declares `recovery.before_image` (`generated`\|`none`+`before_image_why`), mirroring R-B's exact shape — a runtime AUDIT TRAIL, distinct from R-B's crash-recovery POSTURE. The library mirrors the write plan's own scope/keys/columns as a read-only SELECT and writes it to `docs/reports/golden/<slug>/before-image/*.jsonl` on the SAME transaction client, strictly before the retraction, unwrapped by any try/catch — a write failure aborts the run before the retraction runs, never a silent skip. **Enforced by (STA-3, WF1 "state tables reset", 2026-09-03):** `scripts/lib/step/reset.js assertBeforeImageDeclared` refuses a `generateReset` execution carrying >=1 destructive statement unless `recovery.before_image === "generated"`; `applyGeneratedReset` then calls `write.writeBeforeImage` verbatim, on the same client, strictly before each statement, unwrapped by try/catch — both directions locked (fake-pool) in `src/tests/step-library.logic.test.ts` | Rule 12 (`recovery` category) | Spec 122 R-M; `d07529af`, LG-17 |
| R-N | A cumulative link-rate floor's denominator is the primary entity population, never the linked-source row count: T2 (`link_rate_warn`) moved from `wsib_registry` ROWS to `entities.is_wsib_registered=true` / total entities — a single magnet's hundreds of contaminated rows no longer inflate a row-based ratio. The floor VALUE is unchanged; what changed is that it is now validated against measured entity-level truth instead of a row-inflated number | Rule 3 worked-example pattern | Spec 122 R-N; `e01cad11` |
| R-O | A matcher's accuracy is a sampled precision/recall number against a before-image, recorded in the assessment report at each repair — never inferred from a predicate agreeing with itself, and never from a link rate. `tier3_token_overlap_pass_pct` reading 100.00% (the write predicate's own self-consistency) coexisted with a fixed-rule 60-row sample measuring only up to 46.7% genuine precision (§8d table) — the fix hardened the declared stopword/token mechanism itself rather than adding a new bypass/threshold tunable ("same declared mechanism as LW-D14, no new tunable"), because the evidence showed the predicate, not its threshold, was wrong | Rule 10 (verdict derivation); MATCHER accuracy-measurement doctrine (new) | Spec 122 R-O; pilot 4 assessment §8d |
| R-P | A step that can gated-skip declares >=1 `checks[].when:"pre"` — the gated-SKIP narrowing (`onlyChecks`, `scripts/lib/step/index.js`) reduces `stepCtx.checks` to the `when:"pre"` set, and zero such checks means a SKIP's `audit_table` carries only the always-present `sys_*` rows with the skip reason unobservable outside a log line. "Can gated-skip" is a descriptor-level fact: a `terminals[]` entry of kind `"skip_gated"`. Row pending fold — full text lands in the docs commit | Rule 1 (nothing hidden) rung (b); §7 Step 2 | LPA-D4, `docs/reports/defect-ledger.md` |
| R-Q | A verdict-cascade counter never conflates severities: `records_meta.checks_failed`/`errors[]` count FAIL rows ONLY, `checks_warned`/`warnings[]` count WARN rows ONLY (`scripts/lib/step/verdict.js buildAuditTable`) — before this, a single conflated `errors[]` fed `checks_failed`, so a pure-WARN run reported `checks_failed > 0` with zero FAIL rows, measured live on `link_parcel_addresses`. Mirrors the un-converted `scripts/quality/assert-data-bounds.js`'s own pre-existing `errors`/`warnings`/`checks_failed`/`checks_warned` split, brought to parity rather than invented fresh. Row pending fold — full text lands in the docs commit | Rule 10 (verdict derivation) — a counter is a per-severity projection of the row-derived rows, never a mixed one | LPA-D6, `docs/reports/defect-ledger.md` |
| R-R | Validation is integrated and enforced, never a separate track (new **Rule 13**, §2 above). `scripts/analysis/step-validate.mjs` is the ONE command generating the Spec 123 §6 G0–G9 scorecard and the Rules-1–13 policy coverage matrix from artifacts, wired into `.husky/pre-commit --fast` and `.husky/pre-push`, asserted by `step-conformance.infra.test.ts`. Backfilled pilots 1–5's scorecards; Specs 122 and 123 ratified (their `⛔ UNRATIFIED DRAFT` banners retired) in the same commit, operator: *"it should be standard and enforced."* Closes GAPs G-1 (Rule 1 preference order — schema `x-ruling` field + baseline), G-2 (Rule 4 — `preserved-in-compute` names a check), G-4 (Rule 3 — `on_invalid:fail` binding), V7 `no_retraction` (Rule 9), and P3 (`execution.io_budget`) across the five follow-on WF3s this same task authorizes | §2 Rule 13 (new); Spec 123 §6/§7 | this spec §2 Rule 13; Spec 122 R-R; Spec 123 §6 |
| R-T | **Cross-cutting programme promises are declared data, generated, and gated — never re-discovered by a manually-commissioned inventory.** `scripts/steps/_schema/programme-items.json` declares every item owned by no single pilot (status/evidence/owner/gate, gate ∈ `batching_prereq`\|`cutover_prereq`\|`nice_to_have`), generated to `docs/reports/generated/122-programme-backlog.md` (`npm run programme-backlog`), drift-guarded and enforced by `src/tests/programme-backlog.infra.test.ts` (an unmet `cutover_prereq` blocking an already-converted slug is RED, proven both directions with a fixture); `step:validate`'s hook fast path prints the live "blocks batching: N" count on every run. Seeded 2026-08-29 with every row of a full six-pilot grounding pass, including the 11 previously-no-owner items this same ruling assigns an owner and a gate to (§6 R6-lettered items excepted, which stay R6's own territory). **RS-D-STA (operator ruling, pilot 8 commit 9, 2026-09-03):** a gate's applicability may itself be declared data — `gate.applies_when: {descriptor_path, equals}` scopes a `cutover_prereq` to slugs whose OWN descriptor value at that dot-path matches, so a gate authored against a generic archetype-level promise before any descriptor of that archetype existed (STA-2/STA-3 vs. Spec 120 §6b, predating every RECORDER) is not a hand-edited exemption but a measured, re-derivable fact (`checkCutoverPrereqs`, `scripts/analysis/step-validate.mjs`). | §2 Rule 13 (extends it); §8/§9 (new) | Spec 122 §10.3; this spec §8, §9 |
| R-K.1 | `pending` entries gain a `stage` field (`"red_suite"` \| `"shape_clean"`), letting a new pilot declare `pending` contemporaneously with its commit-6 red suite instead of deferring past it — R-R's fast invariant #5 (`it.fails(` needs a declared pending slug) had never been exercised against a genuine pre-descriptor commit-6 file (only backfilled against already-converted pilots), making it and the pre-existing "pending must be shape-clean" test jointly unsatisfiable for any future pilot. A descriptor appearing while stage stays `"red_suite"` is itself RED ("stage not advanced"). `LW-D11`'s harness-fidelity lock amended in the same commit: a per-step test file with no ctx-builder is legal (assert only when a builder exists) — found the same incident, a BACKFILL archetype genuinely has none. **Amended, pilot 9 commit "step-validate honours declared pending.stage for hard-stop" (2026-09-04): stage gates the hard-stop set.** Every pilot through 8 landed descriptor+compute+runner in ONE commit, so a step's OWN `pending` entry never needed to speak to `step-validate.mjs`'s G7 (RED-evidence)/G8 (golden POST captures)/G9 (closing Reflection) gates or Rules 4/11/12 (compute-rule grounding, order-guarantee and crash-posture reachability) — those artifacts either all existed or none did. Pilot 9 (ENRICHER) is the first pilot to SPLIT commit 7 (descriptor before compute before runner), which makes `.husky/pre-commit`'s `step-validate.mjs --staged` hard-stop UNDECIDABLE-not-merely-unmet for a descriptor-only commit: G7/G8/G9 and Rules 4/11/12 read `enforced-red` for the correct reason (their artifact genuinely does not exist yet) but that is indistinguishable, at the hard-stop boolean, from a genuine regression. `stage` is WIDENED to a five-value closed vocabulary — `"red_suite"` (unchanged) \| `"descriptor_only"` \| `"compute_ported"` \| `"runner_wired"` \| `"shape_clean"` (unchanged) — and `scripts/analysis/step-validate.mjs`'s `computeScorecard`/`aggregateHardStop` reads the step's OWN declared `pending[].stage` to EXCLUDE the gates/rules that stage cannot yet decide from `hardStop` (`descriptor_only` excludes G7/G8/G9 + Rules 4/11/12; `compute_ported` excludes G8/G9; `runner_wired` excludes G9) — the excluded row's own score/status is UNCHANGED (still 0/`enforced-red`, never coerced green) and its `detail`/`note` gains a `"— stage-gated (<stage>)"` suffix, so the printed scorecard never reads as a silent pass. A step absent from `pending`, or a `pending` entry with no declared `stage` (or `stage` `"red_suite"`/`"shape_clean"`), gets TODAY'S BEHAVIOUR BYTE FOR BYTE — `stageExclusions()` returns empty sets for all four, so every downstream exclusion check is always-false. An unrecognised `stage` string THROWS (RED the schema) rather than silently defaulting either direction. Both-directions locked in-memory (Spec 121 §12b.6) in `step-validate.mjs`'s own `selfTest()`: a fixture pending entry per partial stage excludes the right rows and does not hard-stop; the SAME reds with no declared stage DOES hard-stop, naming every excluded gate/rule; an unknown stage value throws. **R-K.2 amendment (pilot 9 commit 8 P1, 2026-09-08): a SIXTH stage value, `"shape_clean_pending_recapture"`.** Pilot 9's own commit-8 sequence (Spec 123 §3.1 pin-then-fix, one policy concern per peel) is the first time a step's compute changes across MULTIPLE separate commits (P1-P4) after the file already reached genuine `"shape_clean"` — but the golden POST captures are deliberately recaptured ONCE, in a later commit (P6), rather than once per peel (a ~40-real-minute `--full` run apiece, to explain a diff only the FINAL post-P4 state needs to account for). Unlike the five original values (each excluding gates that are UNDECIDABLE because their artifact does not exist yet), this one excludes G8 ALONE from a gate that IS fully decidable and genuinely red — a declared, bounded deferral (the pending entry's own `reason` text must name the commit that restores plain `"shape_clean"`, `src/tests/step-conformance.infra.test.ts` locks this), not an artifact gap. `conformanceFindings()` stays `[]` throughout — the SAME promise `"shape_clean"` makes — so `src/tests/step-conformance.infra.test.ts`'s own shape-clean check now accepts either value. Both-directions locked the same way as the original five (in-memory `selfTest()` fixtures: G8-alone-red does not hard-stop; G8+G7-both-red still hard-stops on G7, proving the exclusion is G8-only, not "everything late-stage") | R-K (pending mechanics); Rule 13 (`step-validate.mjs` fast invariants); LW-D11 | pilot 6 (`compute_centroids`) commit 6, `tasks/lessons.md` "a same-day gate must be exercised on the path it gates"; pilot 9 (`enrich_parcels`) commit 7b, `.cursor/pilot9_enrich_parcels_active_task.md` Fold H; pilot 9 commit 8 P1 (R-K.2) |
| R-U | **Chain-run correlation is an explicit, propagated id, never re-derived from a timestamp window.** `scripts/run-chain.js` exports `CHAIN_RUN_ID=<its own pipeline_runs id>` (the id it already inserts for its own chain-level row — nothing new minted) as an env var to each spawned step child. `scripts/lib/step/index.js` stamps `records_meta.chain_run_id` on EVERY row it emits — both the happy path and the lock-contention-skip path — `null` when a step runs standalone (no env var, e.g. a manual `run-step.mjs` invocation). The seam pass and chain-end synthesis (R-V, below) JOIN on `chain_run_id` PRIMARILY; the LIKE-prefix + `started_at`/`completed_at` time-window correlation `observe-chain.js` already used (Spec 48) is demoted to the declared FALLBACK for legacy rows only (a chain run that predates this ruling). Ruled within Fold B-5's own scope (an orchestrator/regression ruling, not an operator-facing ambiguity) — proceeded without a separate sign-off | §7 Step 4 (write phase); Spec 48 §3 (`observe-chain.js`'s pre-existing correlation query) | R-T addendum commit 5, `scripts/lib/step/index.js`, `scripts/run-chain.js:638` |
| R-V | **The seam-validation pass + chain-end synthesis are DERIVED from declared step-edges, never hand-maintained; `validate_only` invariants execute exactly once, at chain-end.** `scripts/lib/step/seam.js` derives every "live" seam — an edge where BOTH endpoints are converted steps with a real descriptor — from `inputs.reads.steps[].step` (the declared producer→consumer edge; NOT `outputs.invalidates`, which is `{table,column,when}`-shaped, a step's own re-eligibility scope, e.g. migration 245's trigger, not an edge between two steps — a premise correction made live during R-T addendum commit 5). ~~Measured against the 6 converted descriptors: exactly ONE pair qualifies (`compute_centroids` → `link_massing`).~~ **CORRECTED 2026-09-10 (measured by calling the real `scripts/lib/step/seam.js` `deriveSeamPairs()` over the 8 converted descriptors, not re-derived by hand): 5 pairs qualify** — `compute_centroids`→`link_massing` (the original pair) **+** `link_parcel_addresses`→`link_parcels`, `link_massing`→`refresh_snapshot`, `link_parcels`→`refresh_snapshot`, `link_wsib`→`refresh_snapshot`. The CODE is correct and unchanged; only this register row's count rotted as pilots 7–8 added `inputs.reads.steps[]` edges. The cleanest exhibit of §R-8's own "hand-maintained trackers rot." `scripts/analysis/chain-end-synthesis.mjs` is the `sources` chain's roll-up, spawned detached from `run-chain.js` the SAME way `observe-chain.js` already is (Spec 48), and is the declared cloud trigger point for `validate_only`-tier invariants/plausibility (Ask 6, option (b)) — the priced cost cap (summed `last_measured.cost_ms` medians vs. the chain's own measured duration) is REPORTED in the artifact, never a runtime gate: silently skipping these queries on cost would defeat Ask 6(b)'s whole point, which is that they finally run somewhere unattended. Ruled within Fold A-6/B-9's own scope | §2 Rule 13 (DATA half, above); this spec §8 (layer table) | R-T addendum commit 5, `scripts/lib/step/seam.js`, `scripts/analysis/chain-end-synthesis.mjs` |
| R-W | **Compute MUST NOT branch on PostGIS availability** — `hasPostGIS`, `pg_extension`, `postgis_version` (identifier, property access, or a string/SQL fragment naming one), or a `try`/`catch` wrapped around a PostGIS-only call, are the same anti-pattern: a second, silently-selected algorithm hidden inside compute. `guards.requires: {kind: "extension", name: "postgis", on_missing: "fail"}` is the only legal form — the step refuses before compute runs. Four precedents: `link_massing` A-8 (grid/haversine fallback, retired), `compute_centroids` A-1(a) (arithmetic-mean centroid fallback, retired), `link_parcels` A-1 (JS nearest-parcel fallback, retired commit 7 — `scripts/link-parcels.js`'s entire non-PostGIS `pointInPolygon`/`haversineDistance` fallback removed, `guards.requires` on `postgis`/`parcels.geom`/`idx_parcels_geom_gist` declared instead), `link_parcels` LP-D9 (Strategy 1a's `address_points_exact` predicate gap, fixed commit 8a — not a PostGIS-branch instance itself, but the FOURTH consecutive pilot to find a live, previously-invisible defect only by executing a comprehensive FULL pass, reinforcing R-W's own "no silently-selected second algorithm" spirit one step further up the cascade). **Landed and enforced, not a standing proposal** (Fold C item 5, 2026-08-30: this row was itself already ratified before the "not self-authorizing" sentence below was written — the sentence describes the ORIGINAL 2026-08-30 filing's own posture at the time, superseded by ratification, not the row's current status) | Rule 2 (compute is just compute) addendum | pilot 7 assessment (`docs/reports/2026-08-30-pilot7-link-parcels-assessment.md`) §0.8 item 7, §7, §8a; `scripts/ast-grep-rules/compute-shape.yml` `compute-no-postgis-branch`; `src/tests/step-conformance.infra.test.ts` `COMPUTE_RULE_IDS` |
| R-X | **A frozen enum value that has no live executor is a declaration, not a deletion — it must carry a disposition row, never silently drift.** `outputs.write_discipline.class` (15 `x-frozen` values) and `sharing.on_contention` (3 `x-frozen` values) may be declared by any descriptor with zero AJV pushback even when a value has no real codegen/runner behind it (V7's own `staging_full_replace`/`set_based_unscoped` bans were prose-only until this ruling). `scripts/steps/_schema/write-class-disposition.json` is the reviewed-diff registry — one row per live enum value, disposition ∈ `implemented` \| `executor_by_runner` (executed by a named runner dispatch resolved at runtime, e.g. `runEnrichPhase`'s `execution.phases[].name` lookup, never by class-keyed codegen) \| `banned_for_new` \| `retire` (measured-refuted or structurally undeclarable — **retiring a class is a declaration that stays in the frozen enum, never a removal**; pruning the enum itself is a separate, deliberately-priced re-freeze, Spec 122 §8, not something this ruling authorizes) — mirroring `grandfathered.json`'s "adjudication is a reviewed diff, not something a descriptor grants itself" posture, applied to the enum rather than a per-step exemption. Enforced both directions by `src/tests/write-class-disposition.infra.test.ts`: every live enum value has a row; every `implemented`/`executor_by_runner` row's cited executor function/runner resolves to a real exported `scripts/lib/step/{write,index}.js` symbol (grep-verified, never trusted from the registry's own prose); no live descriptor declares a `banned_for_new`/`retire` class; the registry carries no orphan row. WD-1's own founding gap (class E, `write_once_backfill`, genuinely unimplemented before LG-20 built it) is the worked example this ruling generalizes: the estate had no mechanism to catch the NEXT such gap systematically, only after a pilot happened to need the class. **Same class, outside this registry's current scope — POST-B1-1 (2026-09-15):** `execution.on_check_error`'s `fail_step` was a frozen enum value with NO live executor (declared by 12/12 converted descriptors, "operationally meaningless" — `review_followups.md` HIGH, I3 commit 8) until `scripts/lib/step/verdict.js` `checkRow` gained its arm; its disposition is now **executed** on all three arms (`fail_step` ⇒ FAIL row severity-independently, `warn_row` ⇒ WARN row, `omit_row` ⇒ no row), any other value throwing per Rule 12. `write-class-disposition.json` still covers only `outputs.write_discipline.class` and `sharing.on_contention` — extending R-X's registry to the `execution.*` posture enums (`on_row_error`, `on_batch_error`, `on_check_error`, `on_degrade`, `on_prior_read_error`) is **not yet built**, and is the generalization this row's own founding argument implies. **Extended, R-AX (2026-09-21):** the `execution.*` posture enums this row filed as "not yet built" now have their registry (`execution-posture-disposition.json`); `on_row_error`'s `quarantine`, `on_batch_error`'s `retry` and `on_degrade`'s `prior_values`/`zeroes` are the first four **`retire`** rows in the estate. *(Also corrects this row's own field list: **`on_prior_read_error` does not exist in `step.schema.json`** — measured 2026-09-21, 0 hits. Recorded as never-existing, not silently dropped.)* | Rule 1 (nothing hidden) rung, `x-banned-for-new`/`x-frozen` enforcement; §2 Rule 9 (grandfathering precedent) | Spec 122 §1.4, §8.2; `scripts/steps/_schema/write-class-disposition.json`; `src/tests/write-class-disposition.infra.test.ts`; `.cursor/wf5_wd1_write_class_audit_active_task.md` |
| R-Y | **K1 (a 21st-category schema-overflow kill criterion) is RETIRED — operator ruling A2, 2026-09-09.** Measured across every descriptor on disk today (9: the 8 registered in `converted.json` + `enrich_parcels` pending registration at pilot 9 commit 9), each carries exactly the 20 top-level non-`$` keys `step.schema.json.required` names, `beyond20 = 0` for all nine. This is not restraint that could lapse — `step.schema.json`'s `additionalProperties: false` + `required.length === 20`, pinned both directions by `template-freeze.infra.test.ts`'s `"categories is exactly step.schema.json.required"` test (`categories === step.schema.json.required`), makes a 21st top-level key structurally impossible to declare at all; AJV would reject it before any descriptor could land. A kill criterion that can never fire is a green-because-it-never-looked criterion (Spec 123 KFM 1), not a standing guard — retiring it removes a false promise, not a real one. **No successor criterion is invented by this ruling** (A2 forbids it); a genuine replacement candidate is filed as a new Ask, not written into this row | Spec 122 §8.3 (K1, kill criteria) | Spec 122 §8.3; operator ruling A2, 2026-09-09; WF2 "FREEZE-1, the freeze precondition" commit 2 |
| R-Z | **A programme-gate PRECONDITION is locked both directions, never a printed count.** Spec 122 §8.2's FREEZE-1 precondition ("the template may honestly freeze after the eighth pilot only when the `batching_prereq` set is genuinely empty") had, until this ruling, only a console line (`blocks batching: N`) as its guard — no test asserted the snapshot may be empty only when FREEZE-1 reads BUILT, nor the reverse. `checkFreezeDeclarationHonesty` (`generate-template-freeze.mjs`) is now wired as a THIRD `--check` failure mode (beside plain DRIFT and the R-E schema-consistency lock), both-directions fixture-tested (`template-freeze.infra.test.ts`, no fs/git in the predicate itself, Spec 121 §12b.6). The two RED arms read from DELIBERATELY DIFFERENT sources — arm A (declared BUILT/SUPERSEDED but another `batching_prereq` item is still OPEN, per a fresh scan of `programme-items.json` excluding FREEZE-1's own row) catches the LEDGER disagreeing; arm B (declared BUILT/SUPERSEDED but the COMMITTED, on-disk `template-freeze.json`'s own `batching_prereq_snapshot` still lists an open item) catches the ARTIFACT disagreeing even when the ledger, read fresh, is honest — a hand-edited or stale-regenerated artifact that arm A alone cannot see. Arm C (FREEZE-1 not yet declared) is vacuous GREEN — today's actual state, unchanged by this commit; arm D (genuinely clear, every `archetype_profiles[]` row proven) is the target GREEN. **Arm E is armed per Ask A1 (ruled NO, operator 2026-09-09):** FREEZE-1 may not read BUILT while any `archetype_profiles[]` row is not `proven === true` (a missing/non-boolean value fails closed, same as an explicit `false`) — "8 archetypes dispatched" counted rather than honest otherwise — so commit 9's ENRICHER `proven` flip pays its own RE-FREEZE (#5) before STD-7 may close | Spec 122 §8.2 (FREEZE-1 precondition) | Spec 122 §8.2; `generate-template-freeze.mjs`; `template-freeze.infra.test.ts`; WF2 "FREEZE-1, the freeze precondition" commit 1 |
| R-AA | **A move of spec content is manifest-declared, tool-performed, and gated both directions — never a hand-edit.** A moved block's source/destination, content hash, and stub are declared in a manifest (`122_split_manifest.json`); `--refresh` is the only sanctioned mechanism (cut + stub + hash, atomically); `--check` re-verifies six arms (content survived, stub present, citations resolve, budget, reader guards, totality) on every commit/push. Amends: Spec 124 §4 R-I(4) (restates it as a gate, not a sentence); Spec 122 §10.3 (cross-cutting promises generated + gated, extended to spec prose itself) | §4 R-I(4); Spec 122 §10.3 | `scripts/analysis/spec-split-check.mjs`; `122_split_manifest.json`; `src/tests/spec-split.infra.test.ts`; WF2 "Specs 122/123/124 grounding + reorg + split checker" |
| R-AB | **One chain-sources cloud acceptance run per archetype batch proves every slug converted in that batch; per-slug rows (present, none skipped) remain mandatory; valid only while the database is not in production use; any scheduled chain disabled for the window is re-enabled in the batch's closing commit.** CORRECTED 2026-09-10 (output-review H3) — restates the ruling AS MADE; a prior draft of this row inverted it into a "disable/re-enable is REFUTED" claim not supported by the cited plan. | §2 Rule 13 (extends it); §8/§9 | Spec 122 §10.3; `.cursor/wf1_conversion_roadmap_active_task.md` Asks A5/A6; operator authorisation 2026-09-10 |
| R-AC | **Assessment (descriptor drafting, golden captures) for step N+1 may proceed in a worktree while step N's commits go through the hook; exactly one committer; no parallel full suites on the host; PRE captures are committed before any re-run (the harness overwrites in place).** CORRECTED 2026-09-10 (output-review H3) — restates the ruling AS MADE; a prior draft of this row truncated the parallelism condition to a cloud-dispatch checklist that is not what the cited plan rules. | §4 (ruling protocol); §8/§9 | Spec 122 §10.3; `.cursor/wf1_conversion_roadmap_active_task.md` Asks A5/A6; operator authorisation 2026-09-10 — **LOCKED 2026-09-11 (C4 step H):** the "overwrites in place" clause is retired by the `--out` guard (e49bd594) and the "committed before any re-run" clause by fast invariant #22 GOLD-PRE-FRESH (2f4bd3f7); both share the harness's `captureGitState` definition of recoverable |
| R-AD | **`checks[]` gains an optional `warn_limit` (same frozen string bound grammar as `limit` — `definitions.bound`'s string arm) + `warn_limit_from_config` (same substitution semantics as `limit_from_config`) — a second, looser, config-driven bound giving one check 3-tier PASS/WARN/FAIL coverage in a single row: `limit` ok → PASS, else `warn_limit` present and ok → WARN, else the check's declared severity; one row per metric, names unchanged, both tiers config-substituted. Ruled to unblock `assert_global_coverage` (C4 batch 1 I1) commit 7, which measured 183 census rows needing exactly this 3-tier shape with no existing mechanism (the `{warn,fail}` object form is not config-substitutable; a second checks[] entry per metric would double the rows and break `FreshnessTimeline.tsx`'s metric-name lookups).** Paid as RE-FREEZE #7 (additive fields carrying `x-ruling`, `does_not_freeze`'s own clause) — `schema_sha256`/`frozen_at` re-derived by `generate-template-freeze.mjs --refresh`, Spec 122 §8 amended in the same commit (R-E lock). Both directions locked in `src/tests/step-library.logic.test.ts`. | Rule 3 (worked-example pattern, a second config-driven bound); §2 Rule 10 (still row-derived, no parallel boolean — see this row's own lock) | Spec 122 §8 RE-FREEZE #7; Spec 122a Appendix §A9; `scripts/lib/step/verdict.js` `resolveWarnLimit`/`checkRow`; `.cursor/batch1_i1_assert_global_coverage_active_task.md` Fold B |
| R-PACE-1 | **COMPRESSED COMMIT FORM.** Once **two** steps of an archetype have landed in the full Spec 123 §7 nine-commit form, further steps of that archetype may land in **3** commits: ① assessment + red tests (PH-0…PH-7) · ② conversion + golden differential (descriptor+compute+runner+captures) · ③ cutover + spec diff. **Unchanged:** the ≥14/17 floor, hard-stop=false, all G0–G9, Rules 1–13, both-directions locks. What *moves* is the per-commit **evidence**, into the assessment report's own sections (PH-0 boundary, PH-3 intent ledger, PH-5 seam map, PH-6 classification, line accounting) — declared in one document instead of six commit messages. **Landed 2026-09-13** — the register row + `step-validate.mjs` lock A4 itself promised (see Full text); a prior citation of this ruling as "Spec 124 R-AA" (Spec 122 §8.3, and the batch1 I2 commit 9 message) is WRONG — R-AA is the spec-move rule, unrelated — corrected to R-PACE-1 in the same commit that lands this row. | Spec 123 §7 (amends the nine-commit table with the archetype-maturity precondition); §7.1's role split held unchanged | `.cursor/wf1_conversion_roadmap_active_task.md` Ask A4 (operator authorisation, 2026-09-10); `scripts/analysis/step-validate.mjs` fast invariant #23 `COMPRESSED-FORM-ELIGIBLE` (legal only when `template-freeze.json.archetype_profiles[A].proven === true` AND ≥2 `converted.json` entries share archetype A; RED, scoped to the slug, otherwise; `selfTest()` both directions); Spec 123 §7 (new paragraph) |
| R-AE | **A step whose only write is a ledger/snapshot table it exists to record is RECORDER even when it also evaluates thresholds; the write must be declared, never hidden under ASSERT's `outputs:"none"`.** `assert_engine_health`'s PH-0 measured a genuine, 6-column `IS DISTINCT FROM`-guarded upsert to `engine_health_snapshots` with no legal home under ASSERT's forced `outputs:"none"` (`step.schema.json:1718`) and confirmed no threshold breach in the file ever reaches the halt (four independent verdict paths, all WARN/PASS-only or exception-only) — the write is domain-owned, not the SDK ledger row, and the step's own halt posture already matches RECORDER's "verdict is always PASS" steady state (Spec 122 §1.10:1112) rather than ASSERT's "checks gate the traffic light" contract. Resolves the AST+REC hybrid Spec 122 §1.10:647 named and deferred. Threshold `checks[]` do not make a step ASSERT by themselves — only whether a breach halts the run does; a step that both writes a domain snapshot AND evaluates non-blocking thresholds is RECORDER with `checks[]`, not ASSERT with a smuggled write. **Ruled 2026-09-14 (operator, Ask 1).** | Spec 122 §1.10:647 (hybrid footnote — this ruling is its resolution pointer); §3 archetype variance table (RECORDER row, `outputs.publish` required) | `docs/reports/2026-09-14-batch1-i3-assert-engine-health-assessment.md` §2; `scripts/steps/_schema/step-archetype-census.json` (`assert_engine_health` row); `.cursor/batch1_i3_assert_engine_health_active_task.md` |
| R-AF | **Every spec that names a chained step script anywhere in its body classifies it in exactly one Operating Boundaries list — `### Target Files` (the spec defines the step's behaviour, thresholds or written columns), `### Cross-Spec Dependencies` (the spec consumes the step's output or references it), or `### Out-of-Scope Files`.** Measured 2026-09-14 from the manifest + spec bodies (never transcribed): 65 chained steps, 55 with a domain spec that names the script without declaring it, 216 spec→step pairs across 42 specs — the domain specs (54–62, 65/66, 80–88, 76/82–86 …) had never declared the steps they govern, so the system map's owner row (Spec 123 G0) was true only for the chain specs 41–44. Cross-cutting architecture specs (00-architecture/*, 30, 40, 47, 48, 79, 118–124) are exempt readers: they name every script by design. Enforced by `src/tests/system-map.infra.test.ts` ("spec Operating Boundaries" describe) in the existing pre-commit hook — no new process. Operator ruling 2026-09-14. Companion (own WF after batch 1, filed): source specs get a generated enforcement note listing the assert checks that guard their tables, derived from the assert descriptors the way the system map is derived from the specs. |
| R-AG | **Gate placement: the full Vitest suite moves to pre-push (once per push, gating a whole landed group), and stays in CI (test.yml) as the non-skippable backstop; pre-commit runs only the cheap/staged-scoped gates — stages 1-8 unchanged (churn-complexity, template-freeze, spec-split-check, `step-validate --staged --fast`, lint-staged, migration validators, ast-grep-leads) plus `typecheck` + `lint` + `vitest related <staged files>`.** Live-DB infra tests (every file under `src/tests/db/`, plus the handful of `*.infra.test.ts` living outside it that genuinely connect via `src/tests/db/setup-testcontainer.ts`'s `dbAvailable()`/`getTestPool()` — measured 2026-09-14: `restore-db.infra`, `enrich-heritage.infra`, `enrich-ravines.infra`, `load-ravines.infra`, and the `link_massing`/`link_parcel_addresses` `metamorphic`/`rung1-inline-wkt` suites) run ONLY under `npm run test:db` — structurally excluded from `npm run test`'s own selection via `package.json`'s `--exclude` flags, never merely conditional on whether a developer happens to have `BUILDO_TEST_DB`/`DATABASE_URL` exported. Measured 2026-09-14 (`bash .husky/pre-commit` against one real staged file, `git commit --dry-run` does not run hooks — `git add` one file, run the hook script directly, then `git restore --staged`): a docs-only staged change (`docs/reports/*.md`) = **65.4s, clean (exit 0)** — dominated by the unchanged whole-project `typecheck`/`lint` cost, `vitest related` itself adding ~0s ("No test files found" for a docs-only diff). A `scripts/lib/compute/*.js` staged change on a CONVERTED step (`compute-centroids.js`, one trailing comment line) = **9.6s, HARD STOP** — step-validate's own pre-existing G8 golden-fingerprint-staleness gate (unrelated to this ruling) fires immediately on the content-hash change, before `typecheck`/`lint`/`vitest related` are ever reached; a genuine compute edit legitimately requires a recapture (`capture-step-golden.js --out`) before it can commit, same as before R-AG. Net effect measured against the ~250s/10,437-test baseline: any commit whose staged files don't trip a structural gate now clears pre-commit in well under a third of the prior full-suite time. Both directions locked by `src/tests/hooks-composition.infra.test.ts` — pure predicates over the hook text (comment-blind), exercised against tampered string fixtures; the hook files are static shell scripts, not a generated artifact, so this mirrors template-freeze.infra.test.ts's fixture pattern (Spec 121 §12b.6) rather than that file's AJV/git machinery. | Rule 13 (`step:validate` wiring; hook composition) | `.husky/pre-commit`; `.husky/pre-push`; `src/tests/hooks-composition.infra.test.ts`; `package.json` `test`/`test:db`; `.cursor/wf2_conversion_velocity_active_task.md` §1 R-AG |
| R-AH | **Compressed form (R-PACE-1) is the DEFAULT once an archetype has two full-form members, not an option — a full-form plan for an already-eligible archetype is legal only when the assessment report states a reason.** Enforced by `step-validate.mjs` fast invariant #24 COMPRESSED-FORM-DEFAULT, the inverse of #23 COMPRESSED-FORM-ELIGIBLE: a pending slug whose archetype reads `template-freeze.json.archetype_profiles[A].proven === true` AND has >=2 `converted.json` members, but whose assessment report declares neither the compressed marker (`**Commit form: compressed (R-PACE-1)**`) nor a literal `**Full form reason:**` line, is FAIL; vacuous (not-applicable) while no pending slug's archetype has matured past the two-member threshold. Eligible today: ASSERT (3 members) and LINK (2); INGESTOR and ENRICHER each need one more full-form member before batch 2's remaining 7 + 4 go compressed. Both directions locked in `step-validate.mjs`'s own `selfTest()` (Spec 121 §12b.6, exercised on every `--self-test-only` run, itself proven green by `step-conformance.infra.test.ts`). | Spec 123 §7 (compressed-form paragraph, R-PACE-1's own home — see the pointer added there); §2 Rule 13 | `scripts/analysis/step-validate.mjs` fast invariant #24; `.cursor/wf2_conversion_velocity_active_task.md` §1 R-AH; Spec 124 R-PACE-1 |
| R-AI | **Capture pairs follow the descriptor's own declared chain-variance, not a blind sweep of every manifest chain: when `sharing.varies_by_chain`'s four fields (`checks`/`phase`/`audit_table`/`scope`) are ALL the literal `"none"`, the expected capture set collapses to ONE representative chain (the first, in `manifest.chains` declaration order) + standalone; any other combination keeps every chain, unchanged (today's behaviour).** `derivedInvocations`'s new `expectedCaptureChains` predicate (`scripts/analysis/step-validate.mjs`) feeds the same G8/GOLD-PRE completeness check every converted step already runs. No live descriptor qualifies for the collapse today — `phase` is always a genuine per-chain map in every one of the 12 converted descriptors measured 2026-09-14, even where `checks`/`audit_table`/`scope` already read `"none"` (the LINK/RECORDER/MATERIALIZER family) — so this is forward plumbing proven both directions on synthetic fixtures in `selfTest()`, not yet observed to fire live. Registered `nice_to_have` (VEL-3, `programme-items.json`); a real capture-count reduction awaits a descriptor that genuinely declares no chain-varying field at all. | §2 Rule 13 (extends the G8/GOLD-PRE capture-completeness check); §8/§9 | `scripts/analysis/step-validate.mjs` `expectedCaptureChains`/`derivedInvocations`; `.cursor/wf2_conversion_velocity_active_task.md` §1 R-AI; `scripts/steps/_schema/programme-items.json` VEL-3 |

| R-AJ | **R-X, generalized to the `execution.*` DURATION declarations: each of `execution.budget`, `execution.txn_budget`, `execution.statement_timeout` and `execution.step_timeout` is `executed` (a named, grep-verified live executor) or `descriptive` (declared, NOT enforced, and says so) — never a frozen declaration that reads as enforcement and enforces nothing.** R-X's own row records extending its registry to the other `execution.*` postures as "not yet built"; this is that generalization, in its own registry (`scripts/steps/_schema/execution-budget-disposition.json`, closed 2-value menu) rather than in `write-class-disposition.json`, because these are free-form `duration` values, not frozen enum members. **Dispositions as measured 2026-09-15:** `execution.statement_timeout` = **executed** (`scripts/lib/step/index.js`, the post-check ceiling fallback); `execution.step_timeout` = **executed**, but through `scripts/manifest.json`'s `scripts[slug].step_timeout_minutes` (Spec 118 §3 layer 3, `run-chain.js`), NOT through the descriptor — so the registry drift-locks the two numbers for every `wired[]` slug and requires every other step declaring `step_timeout` to appear in `pending[]`, making the 10 still-inert declarations a DECLARED, dated gap instead of an invisible one; `execution.budget` = **descriptive** (zero executors in `scripts/` — it documents the intended wall-clock envelope an operator reads when choosing the manifest ceiling); `execution.txn_budget` = **descriptive** (zero executors — it documents how much of `budget` the step's own transaction is expected to hold open, the vacuum-horizon arithmetic EP-D17 did by hand). **The founding measurement:** cloud chain-sources run 34971921328 (`main@824ef357`, 2026-09-15) died at the GitHub Actions 300-min wall clock inside `enrich_parcels`, which declares `budget:150m`, `txn_budget:130m`, `statement_timeout:75m` and `step_timeout:180m` — three of the four had no executor at all, the fourth (`phases[].timeout_minutes_from_config`) was executed as a per-STATEMENT `SET LOCAL statement_timeout` giving a 5-statement phase a 375-min effective ceiling, and the CI wall clock was the entire live enforcement stack. Closed by EP-PHASE-DEADLINE: a real wall-clock phase deadline (`pg_cancel_backend` on the captured pid, from a second connection, surfacing through the existing 57014 wrapper — cancel, not terminate, per operator ruling), `step_timeout_minutes: 180` wired for `enrich_parcels`, and the resulting ladder (CI 300 > step 180 > phase 75) locked rung-by-rung from each rung's OWN source file. | §5 R-X (this row is its named generalization); Spec 118 §3 (stop-mechanism hierarchy); Spec 48 §3.6 (silent-timeout class); Rule 3 (every bound an admin logic variable) | `scripts/steps/_schema/execution-budget-disposition.json`; `src/tests/execution-budget-disposition.infra.test.ts`; `scripts/lib/step/index.js` `startPhaseDeadline`; `scripts/manifest.json`; `.cursor/wf3_enrich_parcels_pass3_backlog_active_task.md` C2 |

| R-AK | **`runEnrichPhase` becomes a GENERIC ENRICHER runner: the Spec 58 §9/§11 contract read and the Spec 122 §3.0b defer-scope decision move behind an OPTIONAL, STEP-LEVEL `execution.enrich_hooks` declaration (absent ⇒ the runner does not call them at all; declared-but-not-exported ⇒ a NAMED throw citing the field, never a raw `TypeError`), and the heartbeat + lock-timeout intervals come from two new REQUIRED `execution.*_from_config` fields instead of two hardcoded `config.enrich_parcels_*` keys. A non-finite resolution THROWS above the lock (`Number.isFinite`, never `!x`) — a deliberate `0` still disables the ticker. Closes **ER-D1** (Spec 48 §3.6 silence class): a second enrich-shaped step would have run with `last_heartbeat_at` permanently NULL and no `SET LOCAL lock_timeout` at all, with nothing anywhere saying so. The enrich write path also gains two runner-owned `passCtx` seams (`ctx.retract`, `ctx.joinUpdate`) over write.js's EXISTING executors — before-image strictly first and unwrapped (R-M), class-checked, counter-owned by the runner — so `recovery.before_image: "generated"` stops being a no-op on an enrich shape. Ruled at batch-2 Phase 0.10 to unblock I5 `geocode_permits` and the four Phase-2 C5 ENRICHERs; `enrich_parcels` declares its hooks explicitly and its five committed goldens must hash-equal.** Paid as RE-FREEZE #8 (three additive `execution.*` fields carrying `x-ruling`, ENRICHER-profile `required` widened, `phase_runners[runEnrichPhase].phase_order` re-derived) — `generate-template-freeze.mjs --refresh` + Spec 122 §8 amended in the same commit (R-E lock). Locks L1–L7, all RED-first. | Rule 1 (nothing hidden — an accident is not a contract); Rule 3 (every tunable resolved through `ctx.config`, never a hardcoded key); Rule 12 + R-M (before-image on any retraction; a guard that cannot arm must be loud) | Spec 122 §8 RE-FREEZE #8; Spec 122a Appendix §A9; Spec 48 §3.6; `scripts/lib/step/index.js` `runEnrichPhase`; `.cursor/wf2_enrich_runner_generic_active_task.md` |

| R-AL | **A cloud acceptance dispatch is PINNED to a named commit; the commit it grades is never inferred.** `workflow_dispatch` gains an `expected_sha` input, and the FIRST job step asserts (a) the checked-out tree equals `github.sha` and (b) when `expected_sha` is declared, that it MATCHES — failing **before any step opens a ledger row**, so a verdict can never be read off a stale `main`. The unpinned path is not silent: the graded sha is echoed and the run is warned **NOT valid as an R-AB acceptance run**. **Founding measurement (2026-09-15):** `chain-sources.yml` used a bare `actions/checkout@v4` — no `ref:`, no input, `/headSha|expected_sha/` matched **zero times** — while runs 34964116903/34916671708 graded `headSha 17058af7` against local HEAD `824ef357`; EP-D13 was prose only. **NOT ruled here:** the *reapability* half (the concurrency guard runs BEFORE `reconcile`, so a stranded `running` row blocks every dispatch for 12 h, the reaper downstream of the gate it would open) — own WF3, filed HIGH. | R-AB; Spec 115 §3/§4; EP-D13 | `.github/workflows/chain-sources.yml`; `src/tests/chain-sources-workflow.infra.test.ts` (4 arms, RED pre-fix) |

| R-AM | **A register-ruling id cited anywhere in the 119–124 family must resolve to a real §5 row** — `Spec 124 R-xx` is checked exactly like a `§n` anchor, and a knowingly-dangling one is DECLARED in `known_dangling[]`. **Premise correction (measured 2026-09-15, recorded not silently re-scoped):** batch 2 proposed *extending* the census to `R-xx` ids; that mechanism ALREADY SHIPPED (arm (iii), 2,472 citations, 0 dangling). Missing was the POLICY row — the rule lived only as tool behaviour — and a both-directions lock on the ruling arm (the committed test exercised `kind:'section'` only). The class is live — R-PACE-1 was cited as **“Spec 124 R-AA”** (the spec-move rule) in Spec 122 §8.3 and in I2’s commit-9 message. `R-S` stays never-written and is asserted ABSENT — a silent fill is the rot this forbids. | R-AA (arm (iii)’s ruling half, as policy); §4.3 | `spec-split-check.mjs` arm (iii); `src/tests/spec-split.infra.test.ts` |

| R-AN | **Fleet counts are DERIVED from the registries that own them** — `converted.json`, `manifest.scripts`, the census’s `exemptions[]` — **never retyped into a test.** One deliberate literal is allowed only where it IS the content proven (a named edge list) with a both-directions RED; a bare count never is. **Provenance:** Guardian, I3 c9 — `conversion-roadmap.infra`, `step-schema.logic` and `step-seam.logic` pinned `toBe(12)`/`**52**`/`**54**`/`toHaveLength(6)`, hand-edited every cutover (I2 did 10→11 the same way); batch 2 would pay it 13×. **Measured here:** R-AP moved ONE slug and three literals went red arithmetically while the roadmap was correct. Replacements are stronger, not weaker — `step-schema`’s count canary became a TOTALITY assertion (registered descriptors == descriptors on disk minus declared `pending`), red at any fleet size. | Spec 122 §10.3; R-T | the three test files above; `review_followups.md` (Guardian LOW, I3 c9) |

| R-AO | **A step’s archetype is PROVISIONAL until PH-0 re-derives it from the CODE, and the plan’s commit-form declaration is provisional with it.** A census row or plan table may STATE an archetype; neither is the fact. Once converted the provisional value must be reconciled — `step-validate` fast invariant **#25 ARCHETYPE-PARITY** fails a converted slug whose census row reads `UNDECLARED`, disagrees with its own `descriptor.identity.archetype`, has no archetype at all, has no `template-freeze.json` profile, or is simultaneously a declared exemption (R-AP). **Founding measurement:** `assert_engine_health` was censused **ASSERT** and measured **RECORDER** at PH-0 (R-AE) — RECORDER then had one member, R-PACE-1 became ineligible, and I3 **reverted from compressed to the full nine-commit form mid-flight** (`331f97ad`). Batch 2 declares a commit form for 13 steps off that field. | §2 Rule 7; R-AE; R-AH | `step-validate.mjs` `checkArchetypeParity` (#25; 6 self-test arms, 5 RED) |

| R-AP | **A THIRD exemption class — `runner_owned`: a manifest slug whose JS file EXISTS but whose concern is owned by THE RUNNER is a declared exemption, not a conversion and not a silent drop.** The two existing classes key on the ABSENCE of a JS file; this one does not, and is the only reason permitted to name a real JS file (enforced both directions). **Grounding — Spec 122 owns the archetype question, so this row records only the class:** §4 gives `reconcile` to **THE RUNNER**; §7.3 row 4 reads *“Reconcile the previous run | RUNNER | nothing declared”*; §7.4 A3 places it at the head of `manifest.chains.sources`. RUNNER is a HOME, not a member of `identity.archetype`’s 8-value enum — “nothing is declared per step, and a step cannot opt out” — so no descriptor, no `x-profile`, no compressed form. Distinct from `identity.gate_exempt` (a CONVERTED step’s field). **Applied:** exemptions 2→3; totality `68 = 12 + 0 + 3 + 53`; **C5 = 13 conversions, not 14**; Spec 125 §0 item 3’s `archetypeRegistry` keys on a declared exemption, not `null`. | Spec 122 §4 / §7.3 row 4 / §7.4 A3; Ask A2 | `step-archetype-census.{json,schema.json}`; `generate-conversion-roadmap.mjs`; `conversion-roadmap.infra.test.ts` (4 arms, 3 RED) |
| R-AQ | **A batch's cloud acceptance is a PARTIAL chain run over that batch's converted slugs; the FULL chain is proven ONCE at end of conversion; and where the standing instance cannot complete a proof inside its ceilings, the sanctioned remedy is a TEMPORARY COMPUTE RESIZE, never a raised ceiling.** `run-chain.js --only`/`--from` + the `chain-sources.yml` inputs, pinned per R-AL. Per-slug rows from one `chain_run_id` per acceptance run stay mandatory (R-AB's ACC-1 arm unchanged); what is retired is R-AB's implicit whole-chain scope. **Measured cause (2026-09-18):** Supabase Small (2 GB RAM, 512 MB `shared_buffers`, 22 MB/s baseline) reads ~4.7 MB/s sequential against a 4.4 GB `parcels` heap; `enrich_parcels` exceeds its 180-min step ceiling and full chains exceed the 300-min job cap. **A ceiling measures the STEP; an instance measures the HARDWARE — raising the former to absorb the latter destroys the only signal that says the step regressed.** So `enrich_parcels_pass5_timeout_minutes` and `manifest…step_timeout_minutes` stay UNCHANGED. **The resize is an OPERATOR ACTION the orchestrator REQUESTS via a hard-stop prompt, never performs** — dispatch waits for the operator's live-resize confirmation, and the downgrade waits for a second operator confirmation once the proof and its per-slug batch are adjudicated. Procedure: `docs/runbook/README.md` §3c. | Amends: R-AB (scope), R-AL (pinning unchanged) | `scripts/run-chain.js`; `.github/workflows/chain-sources.yml`; `docs/runbook/README.md` §3c; `.cursor/batch2_c5_active_task.md` §2.2a/§5 |
| R-AR | **The ENRICHER runner has NO Layer-1 ledger/COUNT skip branch, by design. A legacy pre-transaction skip gate is RETIRED AS A MECHANISM and its eligibility predicate ported INTO the write's own `write_discipline.scope` (Layer-2), which SUBSUMES it** — a zero-stale run is then already a 0-row write, and the skip's OBSERVABLE (its audit row) is RE-DERIVED from the write count, never from the retired branch. The retirement is a `deviations[]` entry naming its adjudicator and date; any input class the Layer-1 form healed only BY ACCIDENT is declared in `limitations[]` with a measured bound and a named remedy (`override.force_full`, or a stated absence). **Measured 2026-09-21:** `runEnrichPhase` contains ZERO `staleness.ledgerGatedSkip` call sites — the only two are `runCascadePhase` and `runMaterializePhase`. **Founding cases:** `enrich_ravines` (`83b0cb98`) and `enrich_heritage` (`c3c36315`, operator ruling H-A1 (a)); heritage additionally declares FOLD-I5 (its stamp is a source-content hash, so a levenshtein-threshold change or an address-only parcel edit no longer re-stales — bound 94 of 1,217 Part IV parcels, only `heritage_designation_date` movable) with `ENRICH_HERITAGE_FORCE_FULL` as the declared remedy; `enrich_ravines` declares none. Distinct from R-P, which governs a step that CAN still gated-skip. | Rule 1 rung (b); §7 rung (b); bounds R-P | `scripts/lib/step/index.js`; Specs 59/61 |
| R-AR.1 | **The OTHER branch of R-AR: where a legacy run-ledger gate has NO home in the runner AND the step has NO lineage-stamp column to carry a Layer-2 predicate, the gate is KNOWINGLY RETIRED — a register row plus a `deviations[]` entry — and is NEVER replaced by a migration invented for the conversion.** A conversion may not grow the schema to preserve a gate; that is a behaviour change wearing a port's clothes (Spec 123 §1.1/§3.1). The retirement declares its RISK — the cost of a no-op run — in `limitations[]`, carried to the batch's cloud proof rather than assumed away. **Founding measurement (`compute_parcel_cost_estimates`, batch-2 row 2.4; re-verified live 2026-09-21):** the step's gate decision lives in `pipeline_runs.records_meta`, not in any lineage column, and it has NEVER skipped — the only two ledger rows (`pipeline_runs` 1486, 1408) are both `completed` with `records_meta->'gate'` NULL and `records_meta->>'skipped'` NULL. | Extends R-AR; Rule 1 rung (b) | `scripts/lib/step/index.js`; `scripts/lib/source-version.js` |
| R-AS | **Where a write target's `IS DISTINCT FROM` guard includes the lineage/version stamp the step itself writes, an unchanged-source re-run writes 0 rows — so a forced FULL proves NOTHING about value equivalence, and the differential instrument is a COMMITTED PERTURBATION COHORT.** The cohort is captured against the LEGACY step before the shell is replaced; the artifact is committed and re-runnable, never a session-only action; it records a baseline projected hash, perturbs a declared row set, runs the REAL step, and asserts (i) `records_updated` equals the cohort size, (ii) the whole-table hash returns to baseline EXACTLY, (iii) a declared NEGATIVE CONTROL is untouched — under an UNCONDITIONAL restore bracket, so a failed run never leaves the dev DB corrupted. Never a hand-adjudicated diff. **Founding measurement:** `enrich_heritage`'s forced-FULL instrument was REFUTED at row 2.2; `scripts/analysis/enrich-heritage-cohort-differential.js` + `docs/reports/golden/enrich_heritage/differential/cohort.json` (494 rows; the 16 invalid-geom parcels held out as the negative control) is the built form, made auditable at the output panel (O2). | Extends R-C (the differential's INSTRUMENT); Spec 123 §7 row 9 | `scripts/analysis/enrich-heritage-cohort-differential.js` |
| R-AT | **A write target's `updated`/`rows_changed` counters are owned by whichever mechanism ACTUALLY wrote the rows THIS RUN.** `runEnrichPhase` keeps a per-run (function-local, never module-scope) `seamOwned` set, filled inside `ctx.joinUpdate`/`ctx.retract`, and its post-phase reconciliation loop skips those keys — a pass that both used the seam AND returned `updated` is counted ONCE; `scanned` stays unconditional, since no seam writes it. **Ownership is by USE, NEVER by declared `write_discipline.class`:** the class-gated form was tried and REFUTED — `enrich_parcels`' `comparable_builds` target declares `set_based_join_update` while its pass never calls the seam and reports via its own return value, so a class-gated skip reads its counters as 0. **Founding measurement:** RV-D4, measured live on `enrich_ravines` (`written.e1.updated` read double the real row count), reachable-but-inert on `geocode_permits`. Fix + both-directions locks landed `172adcc9` (`step-library.logic.test.ts` M1e–M1h, RED at `39b246c0`); THIS row is the policy half that commit's own Spec 124 diff was empty of. | Rule 11 / §11 counter scoping; R-AK | `scripts/lib/step/index.js`; `src/tests/step-library.logic.test.ts` |
| R-AU | **Rule 3 governs SCALAR TUNABLES, not pricing DATA. A rate table or a priced line catalogue — a DB table of rates, or a keyed catalogue of cost lines with its permitted-value vocabulary — is PORTED BYTE-FOR-BYTE and DECLARED in the descriptor as a NON-ADMIN SURFACE (`limitations[]`); it does NOT count as externalized.** Every scalar threshold, window, timeout, limit or rate a step consumes still goes to an admin logic variable, unchanged. **INTERIM, with a named closing row — not permanent policy:** the admin editor is batch-2 work (`.cursor/batch2_c5_active_task.md` row **2.5**, a Cross-Domain WF), and on its completion the descriptor's non-admin-surface limitation is REMOVED and the values count as externalized. **Measured 2026-09-21:** `archetype_cost_rates` (12×3) has ZERO references under `src/app`, `src/features/admin-controls`, `src/lib/admin` or `src/components` — DB data no admin surface edits; `PARCEL_COST_LINES` (~78 literals) + `PERMITTED_VALUES` live in `scripts/lib/parcel-cost.js`. | Rule 3 (carve-out; addendum under §2 Rule 3) | `scripts/lib/parcel-cost.js`; batch-2 row 2.5 |
| R-AV | **An ENRICHER declares `override.dry_run: "none"`; a legacy dry-run / `--limit` is RETIRED as a declared `deviations[]` entry, never declared-and-inert.** `runEnrichPhase` reads `overrides.dry_run` at ZERO sites (the only readers are `runLinkPhase`, `runLinkKeyedPhase` and `runCascadePhase`), while the shared audit assembly emits `dryRunRow()` whenever `overrides.dry_run` is truthy — so a declared-but-unhonoured `dry_run` would print `dry_run_no_writes: true` on a run that WROTE, the exact Rule 1 violation the declaration exists to prevent. `--limit=N` has no declarable home at all: `override` is `additionalProperties:false` over `force_full`/`force_run`/`dry_run` (+ optional `accept_anomaly`), and `dry_run`'s argv arm is a bare `^--[a-z][a-z0-9-]*$` flag. **Measured 2026-09-21:** all four converted ENRICHERs (`enrich_parcels`, `geocode_permits`, `enrich_ravines`, `enrich_heritage`) declare `dry_run: "none"`. **INTERIM, with a named closing row:** the runner-level dry-run seam is batch-2 work (row **2.6**); on its completion every converted ENRICHER's `dry_run` declaration is revisited and `compute_parcel_cost_estimates` regains its legacy `--dry-run`/`--limit` capability. | Rule 12 / `override` category; R-AK; §9 ENRICHER | `scripts/lib/step/index.js`; `scripts/steps/_schema/step.schema.json`; batch-2 row 2.6 |
| R-AW | **A model constant already ratified elsewhere in the estate is promoted to a registered logic variable using the EXISTING value as its default — never re-derived, never a second number — and the code constant survives only as the JS-fallback/bootstrap default; every SQL-generating consumer threads the config value as a caller-supplied parameter, never destructuring the module constant for interpolation.** **Founding case (WF3 S0.1, 2026-09-21):** `max_build_lot_min_sqm`/`max_build_lot_max_sqm` promote `scripts/lib/max-build.js`'s bare `LOT_MIN_SQM=50`/`LOT_MAX_SQM=2000` (Spec 65 §4 MB-2) — `buildMaxBuildSql` gains `lotMinSqm`/`lotMaxSqm` params, threaded from `config.max_build_lot_min_sqm`/`max_build_lot_max_sqm` in `runPass2`. | Rule 3 / R-G (instance) | `scripts/lib/max-build.js`; `scripts/lib/compute/enrich-parcels.js`; `scripts/seeds/logic_variables.json` |

| R-AX | **A frozen `execution.*` POSTURE enum is `executed` only where a named executor or a NAMED DECLARED CHECK consumes it; a value with neither is `retire`d in place, and a step declaring a tolerant posture (`skip`/`quarantine`) must CITE the check that bounds it.** R-X's registry is extended to the posture enums it named as "not yet built", in its own file (`execution-posture-disposition.json`, closed menu `executed`\|`descriptive`\|`retire`) rather than in `write-class-disposition.json`, mirroring R-AJ's split for the duration declarations. **Measured 2026-09-21:** `on_row_error` and `on_row_error_max_pct` had **zero** readers in `scripts/lib/**` and `scripts/analysis/step-validate.mjs` while 18/18 converted steps declared them; `quarantine` had **zero** declarers and no sink (Spec 120 §6's `step_quarantine` unbuilt; migrations 245-247 consumed by unrelated work) — `retire`d in place, never pruned (R-X). **The runner is NOT the enforcer, and that is a ruling, not an omission:** across the ten runner entry points only `runIngestPhase`'s validate phase does per-row work (`index.js:670`), and it already counts and gates; all other per-row work is compute's, so **R-AT**'s "ownership is by USE, never by declared class" makes a `ctx.rowError()` seam a counter the runner does not produce — the same class-gated form R-AT records as tried and REFUTED. The bound is enforced where it already lived: a FAIL-severity declared check (§7 rung (b)), now CITED by the new `execution.on_row_error_why.liveness {kind:"check", ref}` and verified by `step-validate` fast invariant **#27 ROW-ERROR-GATE** (renumbered live from the plan's working #26, which pre-existing WF3 COUNTER-ROOT already occupied — measured 2026-09-21). Rung (c) (`limit_from_config`) holds for both live citations landed by this WF2 (`load_ravines`/`ravine_geometry_skipped_pct`, `compute_parcel_cost_estimates`/`engine_error_count`) — invariant #27 requires only a real FAIL-severity, bound-carrying check, not a config-resolved one, so a future citation whose check has a literal (not config-driven) bound still passes; `enrich_parcels`' own truthing (`fail_fast`→`skip`, citing `opt_config_engine_errors`, which carries a FAIL-severity `limit` but no `limit_from_config` yet — F-4) is PREPARED (descriptor edit, why-text, both register rows written) but its golden recapture is a genuine `--full` 5-pass recompute that did not complete inside this session's time budget (still executing after 25+ minutes, within the plan's own ~40-minute worst-case estimate, §4) — landed separately, own follow-up commit, not abandoned or re-drafted. The two live literal `on_row_error_max_pct` values (`load_ravines` 0.05, `compute_parcel_cost_estimates` 0) each **duplicated** a registered logic variable and are deleted as **Rule 3** violations; the field stays in the schema as `x-banned-for-new`. Separately fixed, **F-1, HIGH:** the `if/then` at `step.schema.json` (`on_row_error`/`on_row_error_max_pct`) used `properties` where its three siblings use `required`, so `skip`/`quarantine` with **no** bound at all validated (AJV-proven live against `compute_centroids` pre-fix, and against the fixed schema post-fix, both directions). Paid as **RE-FREEZE #12** (one additive field carrying `x-ruling`, one conditional rewritten, one `x-banned-for-new` marker) — `--refresh` + Spec 122 §8 amended in the same commit (R-E lock). Locks L1.1-L5.2 (execution-posture-disposition.infra.test.ts) + the invariant's own self-test arms, all RED-first. | §5 R-X (this row is its named generalization for the posture enums); §2 Rule 1 (nothing hidden), Rule 3 (no literal threshold), Rule 4 (the rule is written down as a check's `why`); R-AT (counter ownership) | `scripts/steps/_schema/execution-posture-disposition.json`; `src/tests/execution-posture-disposition.infra.test.ts`; `scripts/analysis/step-validate.mjs` #27; Spec 122 §8 RE-FREEZE #12; Spec 120 §3.2/§4.1a③/§6 |

| R-AY | **A step that produces a PRICED or MODELLED output declares the PRODUCT POPULATION it is for, as a bound on its own producing step, and every pass deriving from the same source honours the same bound.** A population is a declared predicate over registered logic variables (never a literal, never an id accept-list), it carries a reset arm for rows that leave it, and a parcel outside it stores the declared "not modelled" sentinel with a typed reason — never a stale value and never a priced one. An id accept-list that exists to excuse out-of-population outputs is retired by the bound, not curated. **Founding measurement (2026-09-21):** `enrich_parcels` had already ruled 26,203 parcels out of the max-build model (`emit=false`, 6,397 of them `lot_too_large`) while `compute_parcel_cost_estimates` priced reno menus on 18,529 of them — $77,593,312,011.51 of $598.7 B in `cost_gut_total`, including 103 of 103 parcels above $50 M and all 42 ids of `COST_ADDITION_GT50M_LEGIT`. **Landed:** the population bound (Spec 88 §2.1) gained `max_buildable_gfa_sqm IS NOT NULL` (in the max-build model) AND `cur_floor_gfa_sqm <= product_scope_max_existing_gfa_sqm` (new logic variable, default 750, exclusive) alongside the existing `upper(zoning_class) LIKE 'R%'` filter — result: 26,525 menus retracted, $80.43 B, 0 lines > $10 M; `COST_ADDITION_GT50M_LEGIT` (42 accepted ids) retired whole, dissolved by the bound rather than curated down. *Amends:* §2 Rules 1 and 3; §7 Step 2 rung (a). *Locks:* both-directions in `src/tests/steps/compute_parcel_cost_estimates/violations.test.ts` + `logic-var-parity.logic.test.ts`. **§4.6 recurrence:** the same bound must be honoured by slice 1's apportionment and by enrich-permits §4D propagation — neither is built yet; named here so the NEXT pass that derives from this source cannot silently skip the bound the way `compute_parcel_cost_estimates` originally did. | §2 Rule 1, Rule 3; §7 Step 2 rung (a) | Spec 88 §2.1/§2.4/§2.5/§2.8-§2.11; `ffeb703b` (S0.2, bound + retract arm), `2301f4a7` (S0.4, accept-list retired); `src/tests/steps/compute_parcel_cost_estimates/violations.test.ts`; `src/tests/logic-var-parity.logic.test.ts` |

~~**R-Z exhausts single-letter ids (R-A…R-Z, plus R-K.1/R-K.2).**~~ **CORRECTED 2026-09-10 (measured): this is not quite a complete A–Z run.** The rows above run A–R, T, U–Z (26 lettered rows) plus K.1/K.2 — **`R-S` has NO row** (cited twice as a promised ruling — `.cursor/wf1_integrated_validation_active_task.md` "§5 register R-R..R-S" and `.cursor/validator_v2_active_task.md` — that never landed; **recorded here as never-written, not silently filled**) and **`R-K.2` has no row of its own** (it lives inside R-K.1's row, above). The next ruling in this register is **`R-AA`**, then `R-AB`, … (double-letter, same alphabetical order a spreadsheet column uses past `Z`) — never a restart at `R-A` under a new sub-scheme, and never a silent renumbering to close the R-S gap. **Allocated through `R-AY` (2026-09-22); next free id `R-AZ`.** Batch-2 Phase 0.8 drafted its rows as `R-AJ`…`R-AM`, found `R-AJ`/`R-AK` taken by concurrent enrich-phase WFs, and **shifted the whole block to `R-AL`…`R-AP`** rather than renumbering in place — recorded so two id sets never circulate. `R-AW` was taken by a concurrent WF (commit `42dac2c8`, max-build lot band externalization) while this WF2 was drafting its own row as a placeholder `R-AW` — corrected to `R-AX` throughout before landing (never two rows sharing one id). **R-AA move performed 2026-09-18 (move `M06`, `122_split_manifest.json`):** §8 moved to Spec 122a Appendix §A12 (the `## §8.` heading stays, as a stub, in place); `budgets["124"].measured_at` ratcheted to 356 lines / 115,130 bytes (commit `4c14d82d`) — the prior "⚠ >99% of byte budget" state (129,491/129,501 bytes, 10 B headroom) is CLOSED, not merely postponed. **§R-8 move performed 2026-09-22 (move `M07`, `122_split_manifest.json`):** the pilot retrospective moved to Spec 122a Appendix §A13 (the `## §R-8` heading stays, as a stub, in place), freeing room for `R-AX`/`R-AY` — the next register row after these two needs a fresh measurement, not an assumed headroom figure.

## §6. What this spec does NOT do

- **No programme mechanics.** Assessment phases (PH-0..PH-8), conversion gates (G0–G9), the nine-commit procedure, and pilot ordering are Spec 123's territory. Spec 122's disk-I/O cost-adjudication discipline (P3 — every proposed box states its measured cost before the operator rules) is also programme mechanics and stays in Spec 122 §1.2a; it does not migrate here.
- **No library API.** `pipeline.step()`'s signature, `ctx`'s shape, and the runner's lifecycle are Spec 122 §4's territory — this spec states *that* a step must behave this way, never *how*.
- **No claim register.** The 290-claim tier assignment (Spec 121/123 §4.3) is verification-cost accounting, not policy.
- **No schema authoring detail.** The ~~18~~ **20 (corrected 2026-09-10, measured)** categories' field-by-field menus live in `step.schema.json` (R2 — the schema is canonical); this spec cites it, never restates it.

## §7. How to resolve a problem in a step — the standard approach

**Every problem a step surfaces — a bug, a fence, a new requirement, an ambiguity — is resolved by this
ladder, in order, never ad hoc.** Rungs (a)/(b)/(e) are Rule 1's order of preference (descriptor →
declared check → last-resort compute); rung (c) is Rule 3's directive; rung (d) generalizes Spec 122 §4.3
"what the library owns" + Known Failure Mode 5 to a library bug rather than a missing declaration. §4 names
*who* rules; this names *where the answer goes*. Grounded in Spec 119 §1 (ground before reasoning; a claim
carries the query that proved it).

**Step 0 — Ground it.** Execute the query, grep, or read that establishes the problem is real — never infer
from a name or a comment. Cite the governing spec section — **cite the owner spec from the system map**
(`docs/specs/00-architecture/00_system_map.md`'s owner row for the step file), not only the architecture
specs (122/123/124): pilot 4's own plan draft first cited only 122/123/124 plus the step's upstream-source
spec, omitting the step's own governing Spec 46/60 until a correction pass found Spec 60's Step Registry
entry (Spec 123 G0). *(Spec 119 §1 stage 1–2; e.g. LM-D14 was found only because a forced-FULL capture was
inspected, not assumed — "found by EXECUTING, not by reading.")*

**Step 1 — Classify.** CONTRACT (a downstream consumer depends on it, even if ugly) / INCIDENTAL (nothing
observes it — do not assert on it) / DEFECT (a spec or invariant asserts the opposite). An undefended fence
is CONTRACT until proven otherwise. *(Spec 123 §3, the four PIN-vs-FIX questions.)*

**Step 2 — Resolve, stopping at the first rung that fits, in this order:**

| Rung | Resolution | Ground |
|---|---|---|
| **(a)** | Declare it in the descriptor — a field from a closed menu | §2 Rule 1 |
| **(b)** | A declared check / audit row — observable, verdict-bound | §2 Rules 1, 10 |
| **(c)** | An admin logic variable — never a literal | §2 Rule 3 |
| **(d)** | A library change — one fix, every step benefits | Spec 122 §4.3 "what the library owns"; KFM 5 |
| **(e)** | A compute change — last resort; compute is JUST compute | §2 Rule 2 (the rule itself still gets written down per Rule 4) |

**NEVER:** a second code path, a silent fallback, a hidden literal, a skip, or a commit-message claim in
place of one of the five rungs above. *(Each has a named incident: AS-D1/LM-D2 — a hand-rolled second verdict
path; LM-D14 — a check that silently read `undefined` and reported CLEAN, "green because it never looked";
pilot 1's original `config:"none"` — a hidden literal; R-C's own text — "a commit-message claim of
'differential green' is no longer evidence.")*

**Step 3 — If no rung fits, or two rungs conflict, the policy is unclear.** Do not pick one by taste — go to
**§4's ruling protocol.**

**Step 4 — Lock it.** A both-directions test, red on the old behaviour and green on the new, at the designed
assertion (Spec 119 §2 "Behaviorally red-first"); re-run the golden capture so the differential stays honest
(§5 register R-C — ⚠ today this re-run is a manual discipline, not yet a standing gate; see Rule 12's note
and R-C's GAP). **A rule without a lock is not yet a rule** (§4 step 4, restated here because it is the step
most often skipped under time pressure).

**Step 5 — Record.** A Defect Ledger id (`<STEP-PREFIX>-D<n>`) for a per-step disposition; a `§R Reflection`
entry (low-confidence or recurring/standard-shaping table) if the same shape is expected to recur in another
archetype — that recurrence claim is what §4.6 promotes into the register.

**The ladder itself is not exempt from §4.** It may be reordered, split, or have a rung added as the
optimization programme proceeds — but only through §4's ruling protocol, with a lock proving the change,
never by a single pilot quietly resolving a problem differently.

#### Worked examples from pilots 1–3

| Problem | Rung used | Resolution | Ground |
|---|---|---|---|
| The 345-line JS fallback link path (0 of 11 runs since 2026-04-02 took it) was dead weight with a silent swallow, orphaning its `link_massing_grid_degrees` tunable | (a) descriptor, dead compute deleted | `knowingly-retired` (Intent Ledger), replaced by declared `guards.requires` preconditions; the orphaned tunable gets `config.retired: [{name, since, why, ledger: LM-D7}]`, never a silent seed-row deletion | LM-D7, ruling A-8, R-A |
| `link_massing_link_rate_fail_pct` was the bare literal `50`, INTENT-UNKNOWN | (c) admin logic variable | `link_massing_link_rate_fail_pct` (default 50, 0–100, `on_invalid:fail`) + `checks[].limit_from_config` | LM-D5 |
| No tiebreak on equal-distance nearest-building matches — links could flip between runs | (e) compute change, last resort | `ORDER BY` rule fixed in `buildMatchSql`; the rule itself stated in the check's `why`, in `notes.json`, and in the compute header — not left as a bare code change | LM-D13 |
| A killed forced-FULL retraction left `parcel_buildings` at 29,330/520,492 rows; the next run's gate read "unchanged" | (a) descriptor | `recovery.interrupted` (`force_full_on_next_run`\|`none`+why) required on any destructive-retraction write target | R-B |
| A declared logic variable with no `logic_variables` row silently resolved to the seed default, stamped as if operator-set | (d) library change | `resolveConfig` (all steps, not just `link_massing`) issues one extra presence query and THROWs, naming the remedy | LM-D15 |
| A standing non-zero link-quality population (`nearest_footprint_gt_lot_count`) was dispositioned INFO with no gate | (b) declared check, severity corrected | INFO conflicted with Spec 48 §4.9; re-dispositioned WARN + declared retighten condition | R-H, LM-D6/LM-D11 |

## §8. The standard step as built (R-T, 2026-08-29 — ~~6/8 archetypes proven, HEAD `d9057a54`~~ **CORRECTED 2026-09-10 (measured): 7/8 proven** — `template-freeze.json`'s `archetype_profiles`: all proven `true` except ENRICHER (`false`, unexercised until pilot 9's cutover); the pinned HEAD is 8 pilots stale)

**MOVED to `122a_step_optimization_appendix.md` ## Appendix §A12 — The standard step as built (moved from Spec 124 §8) — HISTORICAL, 2026-09-18 — 2026-09-18 (Spec 124 §4 R-I(4)).** Historical; R-AA prerequisite move (WF2 'Cloud acceptance model + cron cadence', operator Ask 1, 2026-09-18): Spec 124 grew to 129,491 bytes against its stale 117,729-byte measured_at (10 bytes of headroom left, ceiling 129,501), so a new §5 register row (R-AQ) could not be added without a move. No §5 register row (R-AA..R-AP) is itself historical/superseded — every one carries a live 'Enforced by' clause. §8 is Spec 124's largest non-Rules, non-register section: a regenerated-per-pilot-cutover illustrative snapshot ('what a converted step actually looks like TODAY'), not one of Rules 1-13's binding policy statements, and its own text says the layer table is 'Mirrored, condensed, at Spec 122 §5.6' — a condensed equivalent survives in-place even after this move. The '## §8.' heading itself is PRESERVED by the stub (per this tool's own heading-anchor convention), so every existing 'Spec 124 §8' citation (docs/specs, .cursor plans) still resolves to a real heading; none needed a known_dangling entry.

## §9. Known concerns per archetype (R-T, 2026-08-29 — lifted from the ~~six~~ **nine (corrected 2026-09-10, measured)** pilots' §R Reflections)

*One row per proven archetype, citing the pilot report it was first measured in. Not a defect list (see `docs/reports/defect-ledger.md` for that) — this is the standing, archetype-level caveat a future pilot of the SAME archetype should read before assuming its predecessor's shape transfers cleanly.*

| Archetype | Pilot | Known concern |
|---|---|---|
| ASSERT | `assert_schema` | Compute never opens `ctx.pool` at all (Rule 2's stated exception) — the shape's thinnest test of the contract; `outputs`/`recovery`/`counters`/`config`/`override` all forced `"none"`, so ASSERT alone cannot exercise `write_discipline`, `recovery.reset`, or tunable-presence checks at all. Four fences moved to descriptor data or a named check at conversion (Rule 1 worked example). |
| INGESTOR | `load_ravines` | The richest archetype (9 members) but only ONE representative converted — class B/4-`finally`/drift+mass-delete-env-override coverage is real, but classes A and C (the OTHER two write mechanics INGESTOR spans, §8.2's coverage caveat) are validated only by the enum port, never by a converted INGESTOR exercising them directly. LR-D9 (a phase-reordering ruling silently retiring a "before X" write-order guarantee, Rule 11's origin incident) was FIRST found here, not in a more write-heavy archetype. |
| LINK | `link_massing` | E1's `guard:"none"` is a genuinely correct grandfathered case (an `IS DISTINCT FROM` guard here would skip the exact rows the clear exists to move) — the archetype's own worked example for why `guard:"none"` is sometimes RIGHT, not merely tolerated (Rule 9). Also the archetype that forced R-B's crash-recovery declaration (an interrupted forced-FULL retraction leaving a table at 29,330/520,492 rows with a FALSE `recovery.resume` claim). |
| MATCHER | `link_wsib` | Tier-3 precision required hardening post-cutover (LW-D18, a WF3) after a self-agreeing predicate (`tier3_token_overlap_pass_pct` at 100.00%) coexisted with a 60-row sample measuring as low as 46.7% genuine precision — R-O's "sampled precision/recall, never a link rate" accuracy doctrine originates here, and is the ONE Rule-10-adjacent doctrine every future MATCHER must apply, not just cite. |
| MATERIALIZER | `link_parcel_addresses` | LPA-D1 (`insert_only_no_retraction`, class D, banned-for-new under V7's `no_retraction` rule) is GRANDFATHERED, not fixed — a real, filed, zero-measured-live-exposure known defect the archetype's sole representative carries forward rather than resolving. A future MATERIALIZER pilot (if the estate ever gets a second one) inherits this as an open question, not a closed pattern. |
| BACKFILL | `compute_centroids` | `write.js`'s class-E (`write_once_backfill`) executor (`executeBackfillUpdate`) was newly built THIS pilot (LG-20) — the "zero library growth" hypothesis every pilot since 3 has tested was REFUTED here too, for the third time. Fork-over-share was chosen a THIRD time over a shared phase-runner scaffold (`LG-21`, RATIFIED fork-over-share at the freeze — `e029c37d`, `template-freeze.json`'s `lg21_decision` — see §8 above). Downstream: this archetype's output (`parcels.centroid_lat/lng`) exposes a real, measured, pre-existing consumer defect in `link-parcels.js`'s Tier-3 join (CC-D2/CC-D3, `RT-CC3` — 6,808/17,500 spatial-tier links, 38.9%, would resolve to a different parcel under a containment-based join) that is NOT this archetype's own defect to fix, but is the first pilot where a downstream consumer's join STRATEGY, not just its data freshness, was found to be in question. |
| RECORDER | *(pilot 8, run — corrected 2026-09-03, WF3 cloud-parity FIX 1.6)* | `refresh_snapshot` converted (`converted.json`, 8 entries) via `runRecorderPhase` (LG-26/LG-27 library growth). Was mislabeled "pilot 7, not yet run" — pilot 7 was `link_parcels` (LINK archetype). RECORDER's daily-keyed upsert fit `guarded_upsert` with `guard:"none"` (declared, not invented); see the pilot 8 commit history and `docs/reports/2026-08-31-pilot8-refresh-snapshot-assessment.md` for the worked answer. |
| ENRICHER | `enrich_parcels` (pilot 9, run; cut over commit 9, 2026-09-11) | **Four measured concerns, all invisible locally — every one only surfaced against real cloud scale/heap-state:** **EP-D14** (2026-09-09) — pass-5 D4' recovery walked unconsumed `enrich_parcels_pass3_scope` rows ONE AT A TIME, a per-row full-scan UPDATE with no index leading on `parcel_id`; cloud-measured 439,130 unconsumed rows, one backend active 23 minutes on a single such UPDATE, invisible locally because EP-D10's own prune leaves the local table at 0 rows. **EP-D16** (2026-09-09) — the declared pass-5 statement-timeout bound was applied ONLY inside `flushBatch`'s per-batch transaction, never session-wide on `postClient`; every OTHER statement on that connection (`consumePendingScope`'s reads/writes, the citywide backstop check) ran with NO ceiling at all, invisible locally because a small local table never runs long enough to expose an unbound statement. **EP-D15** (2026-09-09) — the post_commit phase's "heartbeat" advanced only at 4 phase-boundary call sites, never periodically DURING a phase, so a genuinely healthy 30+-minute pass looked identical to a wedged one; invisible locally because a local pass finishes before the gap would ever matter. **EP-D17** (2026-09-10) — the run-end `invariants[]`/`plausibility[]` post checks (and `run-chain.js`'s T1/T4 telemetry) ran as unbounded, unfolded, serial full scans of `parcels`; cost is not a constant but a 150x cliff keyed on heap state (16-30s clean vs 40+ minutes against the 1,346,759 dead tuples the step's OWN pass 4 had just created), invisible locally because a small local table has no bloat to hit. All four were closed as code fixes in the SAME step's own WF3 cycle, not deferred to a future pilot's discovery. |
| ENRICHER *(superseded 2026-09-11 by the row above — retained, never deleted)* | *(pilot 9, not yet run — corrected 2026-09-03; was mislabeled "pilot 8, not yet run")* | **Untested, named in advance:** `enrich_parcels` is ~~2,153~~ **2,068 (corrected 2026-09-10, measured — the same stale figure was carried in Spec 122 §8.2's archetype table, fixed in the same pass)** lines, 5 passes, the largest single conversion in the programme; its clock-relative comps-window gate (Spec 122a's own open question Q3 — "no count- or watermark-based gate can ever skip it") has no analogue in any archetype converted so far, and is the last of the eight-archetype coverage table's rows to be proven against real code rather than the aspirational dispatch-map comment in `scripts/lib/step/index.js`. |

## §R-8 Pilot retrospective (after the eighth, 2026-09-03)

**MOVED to `122a_step_optimization_appendix.md` ## Appendix §A13 — Pilot retrospective (moved from Spec 124 §R-8) — HISTORICAL, 2026-09-22 — 2026-09-22 (Spec 124 §4 R-I(4)).** Historical; WF2 'runner row-error policy' prerequisite move (2026-09-22): Spec 124 had ~1.2 KB of its 10%-headroom budget remaining and this WF2's own R-AX register row (~2.6 KB) could not land without a move. §R-8 is explicitly retrospective ('after the eighth [pilot]'), carries no 'Enforced by' clause, and matches M06's own criterion ('a regenerated-per-pilot-cutover illustrative snapshot, not one of Rules 1-13's binding policy statements') exactly — it is a dated lessons table, not live policy. The '## §R-8' heading itself is PRESERVED by the stub (this tool's own heading-anchor convention), so every existing 'Spec 124 §R-8' citation still resolves to a real heading.

## Operating Boundaries

**Target files:** none — this is a policy document with no owned implementation. It is *read by* every
future step's descriptor author and reviewer.

**Out-of-scope files:** `scripts/lib/step/**`, `scripts/steps/_schema/step.schema.json`,
`scripts/ast-grep-rules/*.yml` — these implement the policy; Spec 122 owns their shape and this spec must not
duplicate their content, only cite it.

**Cross-spec dependencies:**
- **Relies on:** Spec 122 (architecture; §1.2a now points back here, R-J), Spec 123 (§4 generalizes its §7.1 discoverer≠adjudicator rule, R-I.3), Spec 119 (governs on any conflict), Spec 48 §4.9 (the severity rule Rule 10's addendum restates, R-H).
- **Consumed by:** every future step's descriptor and its reviewer; Spec 123's gates cite this spec's rule numbers instead of restating them.
