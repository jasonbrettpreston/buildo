# Review Queue Triage — 2026-08-24

_Auto-generated by scheduled triage agent. Source: `docs/reports/review_followups.md` (2907 lines, ~1058 table rows). No prior triage file exists — this is the baseline._

---

## Stale Items (Likely Resolved)

These items name specific symbols that no longer exist or have been superseded. Grep evidence below.

| Item | Filed | Stale Evidence |
|---|---|---|
| `docs/reports/review_followups.md:37` — "parcel-sanity-audit blind spots for cost/heritage bug class" (~~OPEN~~ RESOLVED) | 2026-07-01 | Explicitly marked DONE in the file; `heritage_basis_footprint_gt_lot`, `cost_fb_on_footprint_gt_lot`, `stale_cost_fsi_without_gfa` audit checks confirmed added. |
| Mobile Findings: `[flight-job].tsx` cold-boot (#1), `[lead].tsx` schema gaps (#2), amber flash (#4) | 2026-05-05 | All three marked ✅ in the file; resolved by WF1-A/B/C commits `657faf8`/`4e2df49`/`6416262`. |
| `neighbourhoods FK-join` 4-site wrong-column bug | 2026-05-08 | Marked ✅ RESOLVED; commits `<WF3>` + regression-locked by `neighbourhoods-fk-join.db.test.ts`. |
| `cost_estimates/trade_forecasts Wrong JOIN` coverage | 2026-05-08 | ✅ in Resolved Historical Index; operator re-run of `compute-cost-estimates.js` noted as post-merge step. |
| `Mobile §9.21 lint check`— `searchTree` boolean always-true | 2026-05-06 | Listed as "resolved" under Architectural Reinforcement — verify the `countMatches` refactor landed; OPEN if it didn't. |

_Genuine dead code not yet cleaned up (confirmed open, low-priority):_
- `scripts/load-massing.js:26` — `SQM_TO_SQFT = 10.7639` unused constant (filed WF2 #C 2026-05-09)
- `scripts/load-massing.js:43` — `shoelaceArea()` function dead after WF2 #C (filed same)
- `scripts/load-massing.js:351` — `isProjected` variable assigned but no longer used in logic (filed same)

---

## Top 5 Items This Week

### #1 — HIGH (data): Parcel 1944170/1944175 NULL-lot mislink ($105M gut line)
**Files:** `scripts/enrich-parcels.js`, `scripts/lib/parcel-cost.js`, `scripts/analysis/parcel-sanity-audit.js`
**Source:** WF2 archetype-cost plan review — Reality-Check 2026-07-06 (top of the file)

`cur_floor_gfa_sqm = 14,171 m²` on NULL-lot RT parcels produces a `parcel_cost_menu` gut line of **$105.24M**, propagated to ≥15 permits. Root cause: all mislink invariants gate on `lot_size_sqm IS NOT NULL`, so NULL-lot parcels slip every guard. Also: no cost-magnitude check (zone-aware bounds) exists in `parcel-sanity-audit.js`.

**Staleness check:** `git log --all -S '1944170'` returns only `e27dec9` (deep-scrape persist, unrelated — false hit). `git log -- scripts/enrich-parcels.js` shows only geometry fixes (D-A corner-width, D-C buffer sliver) since the filing. **Confirmed open.**

**Why #1:** $105M data bug in a live-priced field, confirmed blast radius ≥15 permits, and the sanitiy audit has a documented blind spot. Unblocks the Reality-Check class of audit coverage.

---

### #2 — HIGH: 3× falsy-0 bundle in `cost-model-shared.js`
**File:** `src/features/leads/lib/cost-model-shared.js` (lines 208, 227, 286)
**Source:** Gemini WF2 #3 review 2026-05-08

Three `||` guards that silently override a legitimate `0` value:
- L208: `(row.storeys || 1)` — foundation-only permit (0 storeys) inflates GFA
- L227: `pct > 0` gate — `gfa_allocation_percentage = 0` falls through to 1.0 full-GFA
- L286: `complexity_factor || 1.0` — operator-set `0` overridden to 1.0

**Staleness check:** `git log --since='90 days ago' -- src/features/leads/lib/cost-model-shared.js` returns nothing. `grep -n 'storeys || 1' cost-model-shared.js` confirms `|| 1` at L208. **Confirmed open.**

**Why #2:** All three share the same `||` → `??` root cause, can be fixed in one commit. Affects every cost estimate where either field legitimately holds 0.

---

### #3 — HIGH: Zone-aware FSI ceiling missing from `plausibleFsi`
**File:** `scripts/lib/parcel-cost.js` (line 46–52)
**Source:** Reality-Check WF3 heritage-mislink 2026-07-01

`FSI_MAX_PLAUSIBLE = 99.999` is a numeric-overflow guard only. An RD/RS parcel with FSI 20 (physically impossible for low-rise) prices as real because `20 < 99.999`. The upstream heritage-mislink WF3 fixed the known source; this catches future garbage.

**Staleness check:** `scripts/lib/parcel-cost.js:46` confirmed `FSI_MAX_PLAUSIBLE = 99.999` with no zone-aware branch. No fix commit found. **Confirmed open.**

**Why #3:** Zone-aware bounds (lowrise ≤ 2, RA ≤ 8) is a ~20-line add inside `plausibleFsi`. High residual risk because any future upstream mismatch reprices garbage through a global-only overflow guard. Pairs naturally with #1's NULL-lot audit work.

---

### #4 — HIGH: `get-lead-feed.ts` NaN guards + `lead_id` prefix gap
**File:** `src/features/leads/lib/get-lead-feed.ts` (lines 1003–1004, 121)
**Source:** DeepSeek WF3 2026-05-08 (NaN) + Spec 76 WF2 P5 2026-05-06 (separator)

Two distinct bugs in the same file:
1. **NaN feed break:** `Math.min(input.radius_km, MAX_RADIUS_KM)` / `Math.max(1, input.limit)` produce `NaN` when inputs are `undefined` → `ST_DWithin` returns false (empty feed) or `LIMIT NaN` errors.
2. **lead_id prefix gap:** L121 builds `permit_num || ':' || LPAD(revision_num, 2, '0')` as `lead_id` (missing `permit:` prefix). Mobile feed→detail flow passes this as `?id=NUM:REV`; if `parseLeadId` expects `permit:NUM:REV`, the detail route fails silently.

**Staleness check:** No commits in 90 days on this file. Lines 1003-1004 lack `?? DEFAULT` guards. L121 confirmed missing `permit:` prefix. **Confirmed open.**

**Why #4:** NaN path breaks the full feed for a request without explicit `limit`/`radius_km`. The two fixes are ≤10 lines each and live in the same file.

---

### #5 — HIGH: `§4 B6` thundering-herd 401 missing promise-mutex (mobile API)
**File:** `mobile/src/lib/apiClient.ts` (~lines 65-84)
**Source:** Gemini WF2 M1+M2+M3 batch 2026-05-06; Architectural Reinforcement section

Spec 99 §B6 admits "N parallel `getIdToken(true)` calls under burst-401" — a known-limitation footnote that violates the spec's own "bridges safe by construction" principle. Under a deploy-induced 401 storm, N concurrent requests all start independent `getIdToken(true)` refresh calls rather than one shared promise. Filed at ~15 lines of code; currently a "known limitation" comment in apiClient.ts.

**Staleness check:** No commits referencing `thundering`/`refreshPromise`/`inflightRefresh` found. **Confirmed open.**

**Why #5:** Cheapest high-leverage structural reinforcement: removes the spec's own caveat footnote; prevents auth storms from saturating Firebase token-refresh quotas. Prioritized over other mobile items because it's a safety guarantee, not a UI polish.

---

## Proposed Sweep WFs

### Sweep A — `cost-model-shared falsy-0` (WF3)
**Scope:** `src/features/leads/lib/cost-model-shared.js` only
**Items from queue:** #2 (L208, L227, L286) — all three `||` → `??` changes
**Estimated effort:** 1 commit, ~10 lines, straightforward

Replace:
- L208: `(row.storeys || 1)` → `(row.storeys ?? 1)` 
- L227: `pct > 0` gate → `pct !== undefined`
- L286: `complexity_factor || 1.0` → `complexity_factor ?? 1.0`

Add 3 test fixtures: `storeys=0`, `gfa_allocation_percentage=0`, `complexity_factor=0`.

---

### Sweep B — `enrich-parcels NULL-lot guard + sanity-audit cost checks` (WF3)
**Scope:** `scripts/enrich-parcels.js`, `scripts/lib/parcel-cost.js`, `scripts/analysis/parcel-sanity-audit.js`
**Items from queue:** #1 (mislink + NULL-lot invariants + cost-magnitude audit) + #3 (zone-aware FSI ceiling)
**Estimated effort:** 2–3 commits

Steps:
1. Fix parcel 1944170/1944175 mislink in `enrich-parcels.js`
2. Extend all mislink invariants to handle `lot_size_sqm IS NULL` (e.g., `cur_floor > 1000 AND lot IS NULL`)
3. Add zone-aware FSI ceiling to `plausibleFsi` in `scripts/lib/parcel-cost.js`
4. Add cost-magnitude checks (zone-aware per-line bounds) to `parcel-sanity-audit.js`

---

### Sweep C — `get-lead-feed hardening` (WF3)
**Scope:** `src/features/leads/lib/get-lead-feed.ts` only
**Items from queue:** #4 (NaN guards + lead_id prefix)
**Estimated effort:** 1 commit, ~15 lines

Fix NaN guards with `?? DEFAULT_FEED_LIMIT` / `?? DEFAULT_RADIUS_KM` before clamp operations. Verify route-handler Zod validation — if upstream already prevents `undefined`, add defensive assert; if not, guards are load-bearing. Investigate `lead_id` prefix gap: if `buildLeadKey()` (record-lead-view.ts) expects `NUM:REV` not `permit:NUM:REV`, the L121 SQL may be intentional — confirm before changing.

---

## Queue Health

| Metric | Value |
|---|---|
| File size | 2907 lines |
| Table rows (proxy for item count) | ~1058 |
| Estimated open actionable items | ~220 |
| Open CRITICAL | ~6 (all accepted pre-existing or design deferrals) |
| Open HIGH | ~40 (10 actionable, 30 accepted/pre-existing architectural) |
| Open MED | ~80 |
| Open LOW/NIT | ~90 |
| Prior triage file | None (baseline) |
| Items resolved in last 14 days | ~8 (deep-scrapes F1/F4/F5/F6, assert-data-bounds C4, enriched-status C3/C7, trg_permits_lead_id C6) |
| Oldest active item | 2026-05-06 (WF1-C MMKV adapter silent swallowing) |
| Newest item | 2026-07-06 (Spec 89 parcel cost tool) |
| Domain breakdown | Backend/Pipeline ~60%, Mobile ~30%, Admin UI ~10% |

**Delta vs prior:** No prior triage — this is the baseline. Recommend monthly cadence; weekly is excessive given the pipeline-focused sprint velocity.

**Next recommended sweep after the 3 above:** mobile PaywallScreen hardening WF3 (4× HIGH items: `handlePrimary` unhandled rejection, premature success haptic, `accessibilityLabel` mismatch, `handleRefresh` missing catch — all in `PaywallScreen.tsx`, one commit).
