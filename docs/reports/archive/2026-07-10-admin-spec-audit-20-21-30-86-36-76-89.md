# Admin Spec Audit — 2026-07-10
**Specs:** 20, 21, 30, 86, 36, 76, 89  
**Method:** file-tree + DB schema verification, test runs, DeepSeek adversarial reviews  
**Auditor:** read-only investigation; no code edits made  

---

## 7-Spec Verdict Table

| Spec | Title | (a) Spec Accurate? | (b) Built? | (c) Tests pass? | (d) Integrated? |
|------|-------|-------------------|-----------|----------------|----------------|
| **20** | Stripe Web Checkout | STALE (missing sections; web flow absent from spec scope) | PARTIAL (nonce + webhook only; no `/subscribe` page) | PASS 36/36 (webhook + session only) | ORPHANED (no nav card; web checkout unreachable) |
| **21** | Admin User Management | STALE (no Operating Boundaries; no implementation) | NOT BUILT | NO TESTS | NOT INTEGRATED |
| **30** | App Health Dashboard | CURRENT (minor implementation gaps) | BUILT | PASS 36/36 | INTEGRATED |
| **86** | Control Panel | CURRENT (minor key-name drift) | BUILT | PASS 52/52 | INTEGRATED |
| **36** | Flight Center Tool | CURRENT | BUILT | PASS 71/71 | INTEGRATED |
| **76** | Lead-Feed Health Dashboard | STALE (health dashboard §3.1/§3.3 never built; spec file list references missing files) | PARTIAL (test-feed + inspectors only) | PASS 50/50 (covers only partial scope) | PARTIALLY INTEGRATED |
| **89** | Parcel Cost Model Tool | CURRENT | BUILT | PASS 38/38 | INTEGRATED |

---

## Spec 20 — Stripe Web Checkout

### (a) Spec Accuracy: STALE

- No `## Operating Boundaries` or `## Known Failure Modes` sections (spec template requires both).
- The spec's SPEC LINK in both route files (`/api/webhooks/stripe/route.ts`, `/api/subscribe/session/route.ts`) points to **Spec 96** (Mobile Subscription), not Spec 20. The canonical authorship is inverted.
- §3.2 ("Web App `/subscribe` page") and §5 ("Customer Portal") specify pages/endpoints that are NOT built and NOT even acknowledged as deferred — the spec presents them as normative current state.
- Spec §4.2 requires four event handlers including `checkout.session.completed` and `invoice.payment_succeeded`. The actual webhook handles `customer.subscription.*`, `invoice.payment_failed`, `customer.subscription.deleted` — a different (Spec-96-derived) mapping. Both `checkout.session.completed` and `invoice.payment_succeeded` are missing.

### (b) Is It Built? PARTIAL

| Artifact | Status |
|---|---|
| DB: `subscribe_nonces` | EXISTS |
| DB: `stripe_webhook_events` | EXISTS |
| DB: `user_profiles` (subscription fields) | EXISTS |
| `/api/subscribe/session/route.ts` (nonce generation — §3.1 mobile side) | EXISTS |
| `/api/webhooks/stripe/route.ts` (event handler — §4) | EXISTS (but with event gaps) |
| `/subscribe/page.tsx` (web checkout landing — §3.2) | **NOT FOUND** |
| `/api/stripe/checkout-session` (Stripe session creation — §3.2 step 3) | **NOT FOUND** |
| `/api/stripe/portal-session` (billing portal — §5) | **NOT FOUND** |

The entire web-side checkout flow (§3.2 + §5) is absent. Only the mobile nonce-generation side (§3.1) and the backend webhook (§4) exist.

### (c) Tests: PASS 36/36

Files: `stripe-webhook.infra.test.ts` (13), `stripe-webhook.security.test.ts` (6), `subscribe-session.infra.test.ts` (11), `subscribe-session.security.test.ts` (6). All pass. Coverage is limited to the two existing routes only — no tests cover the non-existent web checkout page or portal-session route.

### (d) Integration: ORPHANED

No nav card in `/admin/page.tsx`. The web checkout surface has no entry point in the admin hub or the web app. SPEC LINKs in source files credit Spec 96, not Spec 20.

### DeepSeek Findings (triaged)

| Severity | Finding | Real? |
|---|---|---|
| CRITICAL | WHERE clause `stripe_customer_id IS NULL OR stripe_customer_id = $2` blocks re-subscriptions when a returning subscriber creates a new Stripe customer ID | **REAL** — the activation UPDATE silently skips, leaving the user in non-active status |
| HIGH | `checkout.session.completed` event not handled | **REAL** — spec §4.2 requires it |
| HIGH | `invoice.payment_succeeded` not handled; Stripe does not always fire `subscription.updated` on payment recovery | **REAL** |
| MEDIUM | Strict `<` timestamp guard allows same-second duplicate events | **REAL** (low probability but spec-deviant) |
| LOW | Stripe API version unpinned (SDK default changes on update) | **REAL** but low severity |
| NIT | `customerIdFromUnknown` shape check could be stronger | Speculative — low priority |

### Fix List (ranked)

1. **[CRITICAL]** Fix the `stripe_customer_id` WHERE guard in `customer.subscription.*` handler — remove the `stripe_customer_id` guard or use `COALESCE` correctly.
2. **[HIGH]** Add `checkout.session.completed` event handler per spec §4.2.
3. **[HIGH]** Add `invoice.payment_succeeded` event handler per spec §4.2.
4. **[HIGH]** Build `/subscribe/page.tsx` + `/api/stripe/checkout-session` route (web checkout §3.2).
5. **[HIGH]** Build `/api/stripe/portal-session` route (§5).
6. **[MEDIUM]** Add `Operating Boundaries` and `Known Failure Modes` sections to Spec 20.
7. **[LOW]** Correct SPEC LINKs in route files to reference Spec 20 (or both 20 + 96 as appropriate).

---

## Spec 21 — Admin User Management & Config Hub

### (a) Spec Accuracy: STALE

- No `Operating Boundaries` section, no `Known Failure Modes` section.
- The entire spec describes features not yet built; the spec presents them as ACTIVE intent but there is no deferred annotation or phase separation.
- §5 Configuration Hub overlaps substantially with Spec 86 (Control Panel), which is built. The two specs are not cross-referenced in a way that resolves the overlap (Spec 86 is not mentioned in Spec 21 at all; Spec 21 is mentioned in Spec 86's cross-references but only in the context of Stripe Price IDs).
- The nav page (`/admin/page.tsx`) links to `/admin/control-panel` (Spec 86 page), but Spec 21 §5 specifies `/admin/config` — these are diverged routes serving similar concerns.

### (b) Is It Built? NOT BUILT

| Artifact | Status |
|---|---|
| DB: `user_profiles` (used by the feature) | EXISTS |
| DB: `admin_audit_log` | **NOT FOUND** (0 rows in information_schema) |
| `/admin/users/page.tsx` (User Directory) | **NOT FOUND** |
| `/admin/users/[id]/page.tsx` (User Detail) | **NOT FOUND** |
| `/admin/config/page.tsx` (Config Hub) | **NOT FOUND** |
| `/api/admin/users` route | **NOT FOUND** |
| `/api/admin/config` route | **NOT FOUND** |
| Nav cards in `/admin/page.tsx` | **NOT FOUND** (no links to `/admin/users` or `/admin/config`) |

### (c) Tests: NO SPEC-21-SPECIFIC TESTS

General user-profile tests exist (`user-profiles.infra.test.ts`, etc.) but none carry a SPEC LINK to Spec 21, and none cover User Directory or Config Hub functionality.

### (d) Integration: NOT INTEGRATED

Completely absent from the admin hub navigation and from the codebase.

### DeepSeek Findings (triaged)

| Severity | Finding | Real? |
|---|---|---|
| CRITICAL | Admin `page.tsx` is `'use client'` with no auth check — anyone who knows `/admin` URL can access it | **REAL** — the middleware must enforce `isAdmin`; this is a production security issue (worth verifying that `src/middleware.ts` covers `/admin/**`) |
| HIGH | Delete Account: Firebase deletion succeeds then DB update fails → orphaned PII | **REAL** spec-level design gap (feature not built yet) |
| HIGH | `admin_audit_log` allows Sentry as an audit substitute — dangerous | **REAL** design concern in the spec |
| LOW | `/admin/users` and `/admin/config` missing from nav | **REAL** (not built) |
| NIT | Nav uses `/admin/control-panel` but spec says `/admin/config` | **REAL** alignment gap |

### Fix List (ranked)

1. **[CRITICAL]** Verify `src/middleware.ts` enforces `isAdmin` for all `/admin/**` routes (independent of Spec 21 build status).
2. **[HIGH]** Build Spec 21 (User Directory + Config Hub) — this is a full WF1 epic.
3. **[HIGH]** Create `admin_audit_log` table (migration needed).
4. **[MEDIUM]** Clarify the §5/Spec-86 overlap in both specs before building Config Hub.
5. **[LOW]** Add `Operating Boundaries` and `Known Failure Modes` to Spec 21.

---

## Spec 30 — App Health Dashboard

### (a) Spec Accuracy: CURRENT

Spec is accurate. All §2.4 implementation files exist on disk. `Known Failure Modes` section present (legitimately empty). `Operating Boundaries` present. §3.1 auth matrix matches implementation. The spec's `Cross-Spec Dependencies` section accurately documents the Sentry/PostHog funnel names consumed by `posthog-client.ts`.

Minor: §6 "Consumed by: none initially. Future Spec 30 Phase B (in-repo alert promotion)..." — this self-reference anomaly (a spec referencing itself as "future Spec 30") is a copy-paste artefact but harmless.

### (b) Is It Built? BUILT

| Artifact | Status |
|---|---|
| `src/app/admin/app-health/page.tsx` | EXISTS |
| `src/components/admin/HealthTile.tsx` | EXISTS |
| `src/app/api/admin/app-health/route.ts` | EXISTS |
| `src/lib/admin/sentry-client.ts` | EXISTS |
| `src/lib/admin/posthog-client.ts` | EXISTS |
| `src/lib/admin/healthSchema.ts` | EXISTS |
| `src/lib/admin/cache.ts` (extracted for Next.js route compatibility) | EXISTS |

### (c) Tests: PASS 36/36

| File | Tests | Result |
|---|---|---|
| `admin-app-health.logic.test.ts` | 19 | PASS |
| `admin-app-health.infra.test.ts` | 11 | PASS |
| `admin-health-tile.ui.test.tsx` | 6 | PASS |

### (d) Integration: INTEGRATED

Nav card "App Health" present in `/admin/page.tsx` linking to `/admin/app-health`. Route is fully wired.

### DeepSeek Findings (triaged)

| Severity | Finding | Real? |
|---|---|---|
| HIGH | Per-tile Sentry breadcrumbs missing — spec §2.6 mandates `{ tile, status, duration_ms }` per tile; code logs only one aggregate breadcrumb | **REAL** |
| HIGH | Rate-limit 429 → cache TTL not extended to 5 min (spec §4 edge case); code uses a single minute-boundary cache with no per-tile TTL extension | **REAL** |
| MEDIUM | Cache promise race on rejection: concurrent second request may re-trigger a fresh fan-out before the first handles the rejection | **REAL** (low probability but spec-deviant) |
| MEDIUM | Zod schema error not emitting a Sentry breadcrumb | **REAL** (minor observability gap) |
| LOW | Cache key strategy not verified (spec says "minute boundary") | Likely already correct; NIT |
| NIT | Test for rate-limit backoff would pass even though production code lacks the TTL extension | **REAL** (test gap) |

### Fix List (ranked)

1. **[HIGH]** Add per-tile Sentry breadcrumbs with `duration_ms` in the aggregator route's `settle` wrapper.
2. **[HIGH]** Implement 5-min cache TTL extension when any tile is `rate_limited` per spec §4.
3. **[MEDIUM]** Fix cache rejection race: use `Promise.finally` to clear the stale-promise slot atomically.
4. **[LOW]** Add test assertion verifying rate-limited tile triggers extended TTL.

---

## Spec 86 — Control Panel ("Gravity" Schema)

### (a) Spec Accuracy: CURRENT (with drift note)

Status annotation "IMPLEMENTED — Migrations 092+093+097" is accurate; all three migrations exist on disk. `Operating Boundaries` section present. The spec's §1 logic-variables table is comprehensive but has grown extensively through WF amendments (seq-level lifecycle bands, CoA CRM keys) — the `GlobalConfigCard.tsx` GROUPS array has not kept pace.

### (b) Is It Built? BUILT

| Artifact | Status |
|---|---|
| `migrations/092_control_panel.sql`, `093_control_panel_gaps.sql`, `097_control_panel_final.sql` | ALL EXIST |
| DB: `logic_variables` table | EXISTS |
| DB: `trade_configurations` table | EXISTS |
| `src/app/admin/control-panel/page.tsx`, `error.tsx` | EXISTS |
| `src/app/api/admin/control-panel/configs/route.ts` | EXISTS |
| `src/app/api/admin/control-panel/resync/route.ts` | EXISTS |
| `src/features/admin-controls/` (api/, components/, lib/, store/) | EXISTS |
| `src/lib/admin/control-panel.ts` | EXISTS |

### (c) Tests: PASS 52/52

| File | Tests | Result |
|---|---|---|
| `control-panel.logic.test.ts` | 27 | PASS |
| `control-panel.store.test.ts` | 12 | PASS |
| `control-panel.api.infra.test.ts` | 6 | PASS |
| `control-panel.ui.test.tsx` | 7 | PASS |

### (d) Integration: INTEGRATED

Nav card "Control Panel" present in `/admin/page.tsx` linking to `/admin/control-panel`.

### DeepSeek Findings (triaged)

DeepSeek's context was `GlobalConfigCard.tsx`; findings are about the UI variable list vs the spec §1 table.

| Severity | Finding | Real? |
|---|---|---|
| CRITICAL | `GlobalConfigCard.tsx` `GROUPS` array still has legacy `lifecycle_band_p{N}_min/_max` keys (P-code) instead of the authoritative seq-level `lifecycle_band_seq_<seq>_min/_max` (×110 pairs per spec §1 WF1-2026-05-13) | **REAL** — UI shows dead controls; actual pipeline reads seq-level keys |
| CRITICAL | Missing `coa_stall_threshold_p2_days` and `coa_imminent_window_days` from GlobalConfigCard | **REAL** — CoA CRM tuning invisible to operators |
| HIGH | Key name `liar_gate_threshold` in GROUPS vs `liar_gate_threshold_pct` in spec — potential miss | **REAL** concern; needs reconciliation against `scripts/seeds/logic_variables.json` |
| HIGH | Undocumented keys in GROUPS not in spec (e.g. `score_tier_elite`, `los_base_cap`, `urgency_overdue_days`) — possible schema drift | **REAL** — need to verify against seed JSON |
| MEDIUM | `calibration_freshness_warn_hours` appears in two GROUPS (duplicate) | **REAL** — would render two inputs |
| MEDIUM | "Hardcoded GROUPS is wrong architecture, should be dynamic" | **MOOT** — architectural choice already committed; not a bug |
| LOW | Missing `lifecycle_status_history_retention_days` from GROUPS | **REAL** |

### Fix List (ranked)

1. **[HIGH]** Reconcile `GlobalConfigCard.tsx` GROUPS against `scripts/seeds/logic_variables.json` — remove dead P-code bands, add seq-level bands (or at minimum add a "seq-level bands" group).
2. **[HIGH]** Add `coa_stall_threshold_p2_days` and `coa_imminent_window_days` to GlobalConfigCard.
3. **[HIGH]** Verify `liar_gate_threshold` vs `liar_gate_threshold_pct` key — fix to match seed JSON.
4. **[MEDIUM]** Remove duplicate `calibration_freshness_warn_hours` entry.
5. **[MEDIUM]** Audit and remove/document undocumented keys (`score_tier_elite`, `los_base_cap`, etc.).

---

## Spec 36 — Flight Center Tool

### (a) Spec Accuracy: CURRENT

Spec was authored 2026-07-09 (per numbering note) and shipped 2026-07-10. All file paths cited in §2 and §6 Operating Boundaries match disk. `Known Failure Modes` section present and non-empty. `Operating Boundaries` and `Cross-Spec Dependencies` present. Spec 76 §3.4 carries the correct SUPERSEDED annotation. The `admin_watchlist` table schema (verified via `information_schema`) matches the §2 column definition including the XOR shape. `lifecycle_stalled` EXISTS on `coa_applications` (DB verified — DeepSeek's concern was moot).

### (b) Is It Built? BUILT

| Artifact | Status |
|---|---|
| `migrations/215_admin_watchlist.sql` | EXISTS |
| DB: `admin_watchlist` (9 columns, XOR check) | EXISTS |
| `src/app/admin/flight-center/page.tsx`, `error.tsx` | EXISTS |
| `src/app/api/admin/leads/watchlist/route.ts` | EXISTS |
| `src/app/api/admin/leads/watchlist/search/route.ts` | EXISTS |
| `src/components/admin/FlightCenterTool.tsx` | EXISTS |
| `src/components/admin/SearchPermitsModal.tsx` | EXISTS |
| `src/lib/admin/watchlist-schemas.ts` | EXISTS |
| `src/features/admin-flight-center/` (store + hooks) | EXISTS |

### (c) Tests: PASS 71/71

| File | Tests | Result |
|---|---|---|
| `flight-center-store.logic.test.ts` | 11 | PASS |
| `flight-center-temporal.logic.test.ts` | 14 | PASS |
| `admin-watchlist.infra.test.ts` | 20 | PASS |
| `admin-watchlist-hooks.logic.test.ts` | 14 | PASS |
| `admin-flight-center.ui.test.tsx` | 12 | PASS |

DB-gated test `db/admin-watchlist.db.test.ts` exists but not run (requires `BUILDO_TEST_DB=1`).

### (d) Integration: INTEGRATED

Nav card "Flight Center" present in `/admin/page.tsx` linking to `/admin/flight-center`. Spec 76 `/admin/lead-feed` page now links to `/admin/flight-center` (the Spec 36 tool). Spec 76 §3.4 SUPERSEDED annotation is in place.

### DeepSeek Findings (triaged)

| Severity | Finding | Real? |
|---|---|---|
| CRITICAL | CSRF Origin check not visible in route file — assumed to be inside `verifyAdminAuth` | **NEEDS VERIFICATION** — the spec §3 says "CSRF Origin check on mutations is handled inside the guard"; verify `src/lib/auth/verify-admin.ts` actually enforces Origin for POST/DELETE |
| HIGH | `opportunity_score` joined from a different LATERAL than `predicted_start` — shows max score across all trades paired with min predicted_start (mismatched rows) | **REAL** — misleading data combination for operators |
| MEDIUM | `OFFSET` pagination grows slow as watchlist grows; no composite index on `(admin_uid, saved_at, id)` | **REAL** but acceptable at current scale |
| MEDIUM | `ids` array not deduplicated before `DELETE ... WHERE id = ANY($2)` | **REAL** minor (mostly harmless, document behavior) |
| LOW | `lifecycle_stalled` on `coa_applications` confirmed absent | **MOOT** — column EXISTS (DB verified) |
| LOW | `track()` fired before INSERT; cold-start serverless risk | **REAL** low-severity |

### Fix List (ranked)

1. **[HIGH]** Verify `verifyAdminAuth` enforces same-Origin check on mutations; if not, add explicit check in watchlist route.
2. **[HIGH]** Merge `opportunity_score` and `predicted_start` from the SAME forecast row (use `ORDER BY predicted_start ASC LIMIT 1` in a single LATERAL rather than separate `MAX`/`MIN` subqueries).
3. **[LOW]** Deduplicate `ids` server-side before DELETE.

---

## Spec 76 — Admin Lead-Feed Tooling

### (a) Spec Accuracy: STALE

The spec's §3.4 carries the correct SUPERSEDED-by-Spec-36 annotation. However:

- **§3.1 + §3.3 (Lead Feed Health Dashboard):** The spec documents `GET /api/admin/leads/health` and the traffic-light dashboard (`feed_ready_pct`, engagement aggregation, cost coverage) as normative. These are **not built**. The spec's §5 Testing Mandate names three test files (`lead-feed-health.logic.test.ts`, `lead-feed-health.infra.test.ts`, `LeadFeedHealthDashboard.ui.test.tsx`) that **do not exist**. The §5 Operating Boundaries lists `src/components/LeadFeedHealthDashboard.tsx` which **does not exist**.
- **§3.3 CoA cost coverage addition (2026-07-06):** References `coa_cost_coverage_pct` in `pipeline_runs` and the Spec 83 acceptance re-derivation. The underlying data may exist but the health dashboard endpoint to expose it doesn't.
- **Stale §3.5 language:** The paragraph "admin must save the permit via §3.4 first" is a copy-paste leftover from the old thin-pass-through. The Cycle 7 admin inspect endpoint has no save-state gate.
- The spec's file list in §5 Operating Boundaries includes `/api/admin/leads/health/route.ts` which doesn't exist.

### (b) Is It Built? PARTIAL

| Artifact | Status |
|---|---|
| `src/app/admin/lead-feed/page.tsx` | EXISTS (renders TestFeedTool only) |
| `src/app/api/admin/leads/test-feed/route.ts` (§3.2) | EXISTS |
| `src/lib/admin/test-feed-utils.ts` (§2.2) | EXISTS |
| `src/components/admin/TestFeedTool.tsx` (§2.3) | EXISTS |
| `src/app/admin/lead-feed/inspector/page.tsx` (§3.5/§3.6) | EXISTS |
| `src/components/admin/LeadDetailInspector.tsx` (§3.5) | EXISTS |
| `src/components/admin/FlightJobDetailInspector.tsx` (§3.6) | EXISTS |
| `src/app/api/admin/leads/inspect/[id]/route.ts` (§3.5 Cycle 7 expand) | EXISTS |
| **`src/app/api/admin/leads/health/route.ts`** (§2.1 + §3.1) | **NOT FOUND** |
| **`src/components/LeadFeedHealthDashboard.tsx`** (§3.3) | **NOT FOUND** |
| **`src/lib/admin/lead-feed-health.ts`** (§2 shared utils) | **NOT FOUND** |

The test feed (§3.2/§3.3), detail inspectors (§3.5/§3.6), and the redirected Flight Center (§3.4) are all built. The **health dashboard itself** (§1 goal, §2.1 endpoint, §3.1 traffic-light) is NOT built.

### (c) Tests: PASS 50/50 (partial scope coverage)

Tests cover the built portions only:

| File | Tests | Result |
|---|---|---|
| `test-feed-utils.logic.test.ts` | 13 | PASS |
| `test-feed.infra.test.ts` | 11 | PASS |
| `TestFeedTool.ui.test.tsx` | 14 | PASS |
| `admin-leads-inspect.infra.test.ts` | 12 | PASS |

Spec-mandated test files `lead-feed-health.logic.test.ts`, `lead-feed-health.infra.test.ts`, `LeadFeedHealthDashboard.ui.test.tsx` are **missing**.

### (d) Integration: PARTIALLY INTEGRATED

Nav card "Lead Feed Testing" in `/admin/page.tsx` links to `/admin/lead-feed` (the test-feed page). The page links out to Flight Center and the Detail Inspector sub-page. The health dashboard surface (the original §1 goal) is not accessible anywhere.

### DeepSeek Findings (triaged, context = Spec 36 for supersession coherence)

| Severity | Finding | Real? |
|---|---|---|
| HIGH | Stale "admin must save first" paragraph in §3.5 | **REAL** — spec text misleads; must be removed |
| HIGH | ILIKE min-length guard on address search (§3.4 / Spec 36 §2) — short queries hit seq-scan | **REAL** concern for Spec 36's search; Spec 76 itself delegates to Spec 36 which should add `MIN_SEARCH_LENGTH = 3` |
| MEDIUM | Stale cross-references in §3.5 edge-cases to old `admin-test` sentinel and `lead_views`-backed save | **REAL** spec-text cleanup needed |
| LOW | 10-second health dashboard polling is aggressive | **MOOT** (dashboard not built) |
| NIT | CoA key display format (`COA-APP-NUM`) vs internal format (`coa:APP-NUM`) may confuse copy-paste | **PARTIALLY REAL** — the Inspector translates `COA-` input → `coa:` internal correctly, but the user-facing placeholder only partially explains this |

### Fix List (ranked)

1. **[HIGH]** Either build `GET /api/admin/leads/health` + `LeadFeedHealthDashboard` (the spec's §1 goal), OR annotate §3.1/§3.3 as DEFERRED and update the spec's file list to remove the unbuilt artifacts.
2. **[HIGH]** Remove stale "admin must save the permit via §3.4 first" paragraph from §3.5 edge-cases.
3. **[MEDIUM]** Remove stale references to `admin-test` sentinel, `lead_views` save, and the old Cycle-3 thin pass-through in §3.5.
4. **[MEDIUM]** Update spec's §5 Testing Mandate + Operating Boundaries to reflect actual test files (rename or align existing tests; remove references to missing health dashboard tests).

---

## Spec 89 — Parcel Cost Model Tool

### (a) Spec Accuracy: CURRENT

Spec is current as of WF1 2026-07-06. All file paths in Operating Boundaries exist. §4 column-to-group mapping has a live schema-drift test (`parcels-columns.snapshot.json` + `.db` test). `Known Failure Modes` and `Operating Boundaries` sections both present and accurate. The `parcel_lookup` Spec-35 §3.1 registration is documented in cross-refs.

### (b) Is It Built? BUILT

| Artifact | Status |
|---|---|
| `src/app/admin/parcel-cost/page.tsx`, `error.tsx` | EXISTS |
| `src/app/api/admin/parcels/lookup/route.ts` | EXISTS |
| `src/app/api/admin/parcels/lookup/types.ts` | EXISTS |
| `src/components/admin/ParcelCostTool.tsx` | EXISTS |
| `src/components/admin/GenericFieldRenderer.tsx` | EXISTS |
| `src/lib/admin/parcel-lookup.ts` | EXISTS |
| `src/tests/fixtures/parcels-columns.snapshot.json` | EXISTS |

### (c) Tests: PASS 38/38

| File | Tests | Result |
|---|---|---|
| `parcel-lookup.infra.test.ts` | 12 | PASS |
| `parcel-cost-tool.logic.test.ts` | 12 | PASS |
| `ParcelCostTool.ui.test.tsx` | 14 | PASS |

DB-gated `db/parcel-lookup.db.test.ts` exists but requires `BUILDO_TEST_DB=1`.

### (d) Integration: INTEGRATED

Nav card "Parcel Cost Model" in `/admin/page.tsx` linking to `/admin/parcel-cost`. Fully reachable from the admin hub.

### DeepSeek Findings (triaged)

| Severity | Finding | Real? |
|---|---|---|
| CRITICAL | `ParcelCostTool.tsx` accesses `data.parcel.costMenu.menu`, `data.parcel.areas`, `data.parcel.neighbourhood.coaProjects` without null guards — spec §2.4 says tiers can degrade to `null`; this causes `TypeError` on tier degradation | **REAL** — confirmed against spec §2.4 contract |
| CRITICAL | Loading state renders both the skeleton and previous parcel data simultaneously (when `lookup.isLoading` is true but `data` still holds prior result) | **REAL** UX bug |
| HIGH | `data.parcel.neighbourhood.coaProjects.map(...)` crashes when neighbourhood tier is `null` | **REAL** (same root as above) |
| MEDIUM | `menuRows` function casts `v` to `CostLine` without runtime shape validation; `fits === undefined` silently suppresses badge | **REAL** (minor, degrades presentation without error) |
| MEDIUM | `Object.entries(data.parcel.areas)` crashes if `areas` tier is `null` | **REAL** |
| LOW | `n.toLocaleString(...)` called on value that may be string from API | Minor — `Number(n)` coercion would be safer |

### Fix List (ranked)

1. **[CRITICAL]** Add null guards for all tier-nullable accesses in `ParcelCostTool.tsx`: `costMenu?.menu`, `areas && Object.entries(areas)`, `neighbourhood?.coaProjects?.map(...)`.
2. **[CRITICAL]** Fix loading state: clear or hide previous `data` when a new search is in-flight (use an `isStale` ref or clear `data` on submit).
3. **[HIGH]** Guard `neighbourhood?.coaProjects` before `.map()`.
4. **[MEDIUM]** Add minimal shape validation in `menuRows` (guard `line.fits` undefined case).

---

## Cross-Cutting Observations

### Admin Middleware Auth (Spec 21 DeepSeek finding, applicable to all)
The admin hub `page.tsx` is a `'use client'` component with no auth check. If `src/middleware.ts` does not enforce `isAdmin` for the `/admin/**` path prefix, the entire admin surface is publicly accessible. This is worth verifying as a first-priority security check independent of any individual spec.

### Spec 22 Resolution
There is no Spec 22 anywhere in `docs/specs/` or its subdirectories, and no file in the tree cross-references "Spec 22" by that number. The fourth Group A spec is Spec 20 (confirmed by user; investigated and reported above). Spec 22 does not exist.

---

## Recommended Priority Order for Fix Work

| Priority | Spec | Finding |
|---|---|---|
| P0-security | Admin middleware | Verify `src/middleware.ts` covers `/admin/**` for `isAdmin` |
| P0 | 20 | Fix `stripe_customer_id` WHERE guard bug blocking re-subscriptions |
| P1 | 20 | Add `checkout.session.completed` + `invoice.payment_succeeded` handlers |
| P1 | 89 | Add null guards for tier-degraded objects in `ParcelCostTool.tsx` |
| P1 | 89 | Fix loading state / stale-data render bug |
| P2 | 86 | Reconcile `GlobalConfigCard.tsx` GROUPS against seed JSON (seq-level bands, CoA keys, key names) |
| P2 | 30 | Add per-tile Sentry breadcrumbs + rate-limit TTL extension |
| P2 | 36 | Merge opportunity_score and predicted_start from same forecast row |
| P3 | 76 | Decide: build the health dashboard or annotate §3.1/§3.3 as DEFERRED |
| P3 | 76 | Remove stale "save first" language from §3.5 |
| P4 | 20 | Build web checkout flow (`/subscribe` page, portal-session route) |
| P4 | 21 | Full build (WF1 epic) — User Directory + Config Hub + admin_audit_log |
| P5 | All | Add `Operating Boundaries`/`Known Failure Modes` to Spec 20, Spec 21 |
