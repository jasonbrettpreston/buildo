# Review Queue Triage — 2026-09-21

_First triage run. Generated by automated weekly triage routine._  
_Source: `docs/reports/review_followups.md` (3,844 lines, ~447 active items)._  
_No prior triage report — no delta available._

---

## 1. Stale Items

Items that name specific files/functions verified against the codebase or git log.

### S1 — Spec 126 group A security rows (followups.md lines 34–41) — RESOLVED, no marker

**Original claim:** 11 admin routes missing `verifyAdminAuth`; 7 unaudited mutating exports; 2 public `SELECT *` routes.  
**Evidence:** WF3 SEC-1 closeout (§3688 of followups.md) explicitly states it "Closes the Spec 126 surface research group A rows (guard 11/11, audit 9 files / 13 exports, projections 7 routes, reaper)." `grep -rn "verifyAdminAuth" src/app/api/admin/` confirms per-route guard present in: `stats`, `rules`, `sync`, `builders`, `schedules`, `pipelines/[slug]`, `pipelines/schedules`, `users`, `users/[uid]`, `users/[uid]/subscription/reconcile`, `users/[uid]/subscription/retry-cancel`.  
**Status:** CLOSED 2026-09-15 (WF3 SEC-1). Original Spec 126 group A table rows carry no "CLOSED" disposition cell (the table format omits the column), so a reader scanning the file head sees them as active. No code action needed; residuals from that WF3 are separately filed at §3694–3711 and _are_ still open (see #2 below).

### S2 — `enrich_parcels` declared counters resolve null (I5/0.10b residual) — LIKELY RESOLVED

**Original claim (embedded in followups.md §3754):** After 0.10b landed, `enrich_parcels`' three declared counters still resolve null because descriptor sources root at `compute.*` while the runner roots at `matched.compute.*`.  
**Evidence:** `git log -- src/` shows `e46ab39 fix(65_enrich_parcels): declared counters resolve - compute.* -> matched.compute.*` in recent history. Commit message matches the filed defect exactly.  
**Status:** LIKELY CLOSED by `e46ab39`. The inline residual within §3754's disposition prose should be annotated CLOSED if `e46ab39` is confirmed correct. No separate row exists to mark; this entry documents the evidence for the next person reading §3754.

### S3 — `runEnrichPhase` seamOwned double-count RV-D4 (followups.md line 3819) — already marked CLOSED

Confirmed by `172adcc fix(124_step_standard_policy): RV-D4 runner seam counters single-counted`. Row is already marked "CLOSED · WF3 RV-D4, 2026-09-20" in the file. Recorded here to confirm no stale open entry exists under a different read.

---

## 2. Top 5 This Week

### #1 — HIGH: `step-validate.mjs` red_suite exclusion gap (followups.md line 3760)

**What:** `STAGE_HARDSTOP_EXCLUSIONS` in `scripts/analysis/step-validate.mjs:2082–2092` deliberately omits `red_suite` and `shape_clean`. Confirmed live: the map has entries for `descriptor_only`, `compute_ported`, `runner_wired`, `shape_clean_pending_recapture` only. Any in-flight conversion registering a `pending` slug hard-blocks `step-validate --all --fast` from the moment of registration until the descriptor lands on disk (10/17, `hard-stop=true` on G7/G8/G9 + Rules 1/5/6/7/8/9/13 whose only cause is "no descriptor").  
**Rationale:** Unblocks all 11 remaining batch-2 conversion slugs (`enrich_heritage`, `enrich_centreline`, `compute_parcel_cost_estimates`, `address_points`, `load_centreline`, `load_zoning`, `load_heritage`, `load_wsib`, `massing`, `parcels`, `neighbourhoods`). Fix is ~5 lines: add `red_suite: { gates: ['G7','G8','G9'], rules: [4,5,6,7,8,9,11,12,13] }` to `STAGE_HARDSTOP_EXCLUSIONS` + both-directions self-test arms in `selfTest()`. Annotate Spec 124 R-K.  
**Planned home:** Small Backend/Pipeline WF3. Files: `scripts/analysis/step-validate.mjs:2082`, `docs/specs/01-pipeline/124_step_standard_policy.md`.

### #2 — HIGH: 4 admin mutations gating on `authMethod === 'admin_key'` instead of `!== 'session'` (followups.md line 3696)

**What:** SEC-1 close-out (2026-09-15) left four already-guarded admin mutations using the NARROWER shape: `src/app/api/admin/users/route.ts:131`, `users/[uid]/route.ts:148`, `users/[uid]/subscription/reconcile/route.ts:180`, `users/[uid]/subscription/retry-cancel/route.ts:37`. `dev_bypass` (`'dev-user'` sentinel) reaches the mutation body, hits `admin_audit_log.admin_uid UUID NOT NULL`, raises 22P02 inside the txn — a rolled-back 500 where a 403 is correct. Named in the `SESSION_GATE_GAPS_FILED` allow-list in `src/tests/api.infra.test.ts`; closing one REDs that lock until the list shrinks.  
**Rationale:** Live security regression on production admin routes. Fix is one line per handler (`!== 'session'`) + infra test update. The pre-built `SESSION_GATE_GAPS_FILED` lock turns each fix into a verified, tracked commit.  
**Planned home:** Cross-Domain WF3, Security seat. Files: the 4 listed route handlers + `src/tests/api.infra.test.ts`. MFA enrolment audit row (`security/mfa/route.ts` POST, §3698) is a natural co-rider.

### #3 — HIGH: `DataQualityDashboard.tsx` shows PASS/green for partial chain runs (followups.md line 3799)

**What:** `getChainVerdict` (`:121-135`) derives the chain chip from `info.status` alone; `scheduleStatus` (`:152-172`) derives the freshness dot from `last_run_at` alone. Neither reads `records_meta.partial_selection`. With `--from`/`--only` partial dispatch now the NORMAL operating mode (sources chain exceeds 300-min CI budget), both lie on every routine deployment cycle. Measured: a `--only=assert_schema` run (1 of 28 steps, 4.3 s) makes the card read PASS / "On schedule" for a chain whose other 27 steps haven't run.  
**Rationale:** Operational correctness issue on the primary human pipeline-health signal. Fix path precisely stated in the disposition: one `if (info.records_meta?.partial_selection) return { label: 'PARTIAL', … }` branch above the PASS fallthrough + parallel fix in `scheduleStatus`. Both use `records_meta` already projected by `src/app/api/admin/stats/route.ts:225-267`. No API change needed.  
**Planned home:** Admin/Cross-Domain WF3. Files: `src/components/DataQualityDashboard.tsx:121-135`, `:152-172`.

### #4 — HIGH: `step_timeout_minutes` unwired for 10 converted steps (followups.md line 13)

**What:** Only `enrich_parcels` (180 min) and `refresh_snapshot` (15 min) have `step_timeout_minutes` wired in `scripts/manifest.json` (confirmed: `grep -c step_timeout_minutes manifest.json` = 2). Ten converted steps declare `execution.step_timeout` in their descriptor but `run-chain.js` reads `|| 0`, which its own docblock calls INERT. Steps: `compute_centroids`, `link_massing`, `link_parcel_addresses`, `link_parcels`, `link_wsib`, `load_ravines`, `assert_data_bounds`, `assert_engine_health`, `assert_global_coverage`, `assert_schema`.  
**Rationale:** A ceiling set below a step's honest runtime converts a healthy run into a failure — the defect the filed row cites explicitly. Measurement can start this week (`SELECT pipeline, percentile_disc(0.95) WITHIN GROUP (ORDER BY duration_seconds) FROM pipeline_runs WHERE status='completed' GROUP BY 1`). Once each step's p95 is confirmed, wiring is a 1-line manifest edit per step.  
**Planned home:** WF2 after measurement. Files: `scripts/manifest.json` (10 step entries). Each step needs own measured ceiling before wire — do NOT batch-set without data.

### #5 — MED+MED: Two perpetually-WARNing `chain_sources` checks (followups.md lines 132–133)

**What:** (a) `parcels_null_address_pct`: threshold `< 10%`, live value 100% permanently — `ADDRESS_NUMBER` was deliberately removed from the source CSV and `load-parcels.js:508-525` confirms `record.ADDRESS_NUMBER` is always empty. The check can never pass again. (b) `massing_zero_link_ghost`: comment `"day-one value 0"` (`scripts/enrich-parcels.descriptor.json:543`) contradicted by the only two cloud rows ever recorded (both read 1,395). Together these two checks make every `chain_sources` run false-WARN regardless of real data quality.  
**Rationale:** Low effort, high signal. For (a): retire or rebase onto `parcel_address_points` bridge coverage (the column's documented replacement). For (b): measure whether 1,395 = real orphaned massing links (fix the data) or wrong comment (correct it). Neither fix requires pipeline changes; both contain within descriptor + compute.  
**Planned home:** WF3 Backend/Pipeline (2-item sweep). Files: `scripts/enrich-parcels.descriptor.json:543`, `scripts/load-parcels.js`/descriptor (for `parcels_null_address_pct`).

---

## 3. Proposed Sweep WFs

### Sweep A — "WARN calibration" WF3 (2 items → 1 WF3)

**Items:** `parcels_null_address_pct` (retire/rebase) + `massing_zero_link_ghost` (measure 1,395 then fix comment or data).  
**Scope:** `scripts/enrich-parcels.descriptor.json`, `scripts/load-parcels.js` + its descriptor.  
**Items closed:** 2 MED.  
**Domain:** Backend/Pipeline WF3.

### Sweep B — "admin session-gate tighten + MFA audit" WF3 (5 items → 1 WF3)

**Items:** 4 × `authMethod === 'admin_key'` → `!== 'session'` (HIGH, SEC-1 residual) + MFA enrolment audit row (MED, SEC-1 residual). The `SESSION_GATE_GAPS_FILED` lock in `api.infra.test.ts` provides a pre-built CI gate that REDs on each open item and greens as each closes.  
**Scope:** `src/app/api/admin/users/route.ts`, `users/[uid]/route.ts`, `users/[uid]/subscription/reconcile/route.ts`, `users/[uid]/subscription/retry-cancel/route.ts`, `security/mfa/route.ts`, `src/tests/api.infra.test.ts`.  
**Items closed:** 4 HIGH + 1 MED = 5 items.  
**Domain:** Cross-Domain WF3, Security seat.

### Sweep C — "step-validate red_suite gate" WF3 (1 item → 1 small WF3)

**Items:** Add `red_suite` exclusion entry + Spec 124 R-K annotation + `selfTest()` arms.  
**Scope:** `scripts/analysis/step-validate.mjs:2082`, `docs/specs/01-pipeline/124_step_standard_policy.md`.  
**Items closed:** 1 HIGH. **Unblocks:** 11 downstream batch-2 conversion slugs.  
**Domain:** Backend/Pipeline WF3.

---

## 4. Queue Health

| Metric | Count |
|--------|-------|
| Total active items (est.) | ~447 |
| HIGH | 127 |
| MED | 156 |
| LOW | 164 |
| Prior triage report | None (baseline run) |
| Delta vs prior | N/A |

**Age distribution (by section timestamp):**

| Age bucket | Est. items | Notes |
|-----------|-----------|-------|
| < 1 week (≥ 2026-09-14) | ~55 | `enrich_heritage`/`enrich_ravines` output-panels, partial chain runs, EP-PASS3-BACKLOG |
| 1–4 weeks (2026-08-24 – 09-13) | ~95 | Spec 126/128 research, SEC-1 residuals, batch-2 I4/I5, Pilot 9 |
| 1–3 months (2026-06-16 – 08-23) | ~130 | Pilots 1–8, P0 residuals, WF1 cross-step ledger, Spec 88/89 DEFERs, Supabase migration waves |
| > 3 months (pre 2026-06-16) | ~167 | Phase E/F/I series, WF1/WF2 multi-agent batches, Spec 47/79/91/95/96, mobile Active Open Items |

**Notable observations:**

1. **Batch-2 conversion deferrals are healthy, not rot.** The ~55 items added in the last week are well-documented Spec 123 §1.1 behaviour-neutral deferrals with clear owners and fix shapes. They track genuine follow-up work, not skipped obligations.

2. **The > 3-month tail (~37% of all items) is the real concern.** Items like the Playwright E2E bootstrap (Spec 89, 2026-07-06), `useLeadView` dead-meter retirement (Spec 128 R-08), 28 envelope-deviation routes, and `envelope_constraint_reason` split (Spec 65, 2026-07-02) have accumulated across many WFs without a named owner or scheduled WF.

3. **HIGH items requiring measurement before action (not in Top 5):**
   - `max_build_dim_below_floor` 27,984 FAIL-gated — needs `enrich-parcels --full` re-run first (line 168)
   - LN-D4: 5,138 permits outside their neighbourhood polygon — root-cause the 2026-02 event, then operator remediation (line 3720)
   - `*_dataset_version_when_enriched` stamps: no trigger invalidator — measure staleness count first (line 159)
   - `pipeline.run()` legacy SDK: no ledger row on standalone invocation — pre-existing architectural gap requiring `pipeline.run()` or runbook change (line 3813)

4. **Recommend: next triage** check whether the > 3-month items in "Active Open Items" (`## Active Open Items`, line 828) can be consolidated into 1–2 scheduled WFs for the mobile PaywallScreen hardening cycle and the `useLeadView` retirement.
