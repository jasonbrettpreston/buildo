<!-- GENERATED by scripts/violations/plan-claims.mjs --checklist — do not hand-edit. -->

# Per-conversion claim checklist — 55 items, split 44 / 5 / 6

> Copy into each step's conversion task. **These claims cannot be discharged in advance** — they need the step's own tables, predicates and fences. Every line becomes a test in `src/tests/steps/<slug>/`.

> ⚠️ **The 55 are NOT one block.** At conversion #1 the list is unsatisfiable as written: 5 items can only be *partly* discharged and 6 cannot be gated at all, not even vacuously. Gating all 55 at pilot 1 makes "55-A proven red" undefined — so the list splits by its K assignment (see `123-claim-plan.md`): **A = hard gate · B = partial named · C = deferred, arming k named.** Only the A block is a per-conversion pass/fail.

## 55-A — the hard per-conversion gate (44)

> Every one of these is fully dischargeable against a SINGLE converted step (k=1). All 44 present and proven red before the conversion's Gate 4d passes. No exceptions, no partials.

| ☐ | # | Test ID | Claim | The violation to write |
|---|---|---|---|---|
| ☐ | 30 | `R-030` | Cap of 12 prose entries | add a 13th → build fails |
| ☐ | 31 | `R-031` | Exactly two legal resolutions — promote or delete; **no overflow file** | add `notes-overflow.json` → the step-dir shape lint rejects the unknown file |
| ☐ | 33 | `R-033` | `blind_spots[].detected_by` names a check that exists | name a nonexistent check → CI fails |
| ☐ | 34 | `R-034` | `detected_by:"none"` is permitted **but counted** | add one → the conformance report's open-blind-spot count increments |
| ☐ | 35 | `R-035` | Every prose entry carries `measured{value,date,query}` | omit → rejects |
| ☐ | 37 | `R-037` | Unpromoted `suspicious_if` entries are counted | add one → count increments |
| ☐ | 38 | `R-038` | `review_notes` ship to the reviewer prompt automatically | remove the injection → assert the prompt-assembly test reds |
| ☐ | 148 | `R-148` | `deviations[]` and `fences[]` are required; **empty must be an explicit `[]`** | omit either → rejects (intent with nowhere to live is intent that will be lost) |
| ☐ | 149 | `R-149` | Gate 0 — script #3 adds **zero** new bespoke runner paths | assert the runner diff across conversion #3 is empty |
| ☐ | 150 | `R-150` | Gate 1 — the **old** script is reproducible against itself | run it twice on the frozen snapshot → identical dump modulo declared normalisations |
| ☐ | 151 | `R-151` | The non-determinism inventory is declared **before** the first diff | a git-order assertion: an inventory entry committed after a red diff → red |
| ☐ | 6b | `R-06b` | ⚠️ **Every plan item declares a done-test** (§12.16) | add an S/R/A/C item with no done-test → **CI fails**. ⚠️ **Measured: 30 of 49 items had none — a stage gate ca |
| ☐ | 6a | `R-06a` | ⚠️ **Every claim covering a TABLE declares that table's row count** (Appendix H) | drop a row from `T3.2` (57) · `T3.1` (13) · `T5.0` (12) · `T8.2` (10) · `T6b` (6) · `T4.1a` (5) → **count mism |
| ☐ | 151a | `R-151a` | ⚠️ **The non-determinism disposition vocabulary is CLOSED** — `must-match-exactl | declare a fourth disposition → **rejected**. Every other vocabulary in these specs is closed; this one was lef |
| ☐ | 152 | `R-152` | Gate 2 — Intent Ledger 100% dispositioned, **no row `unknown`** | leave one → gate blocks |
| ☐ | 153 | `R-153` | Every `knowingly-retired` row names a human approver | omit → rejects |
| ☐ | 154 | `R-154` | Gate 3 — a peel commit contains **only** that peel | a commit mixing a peel with a restructure → red |
| ☐ | 155 | `R-155` | Gate 4c — **line accounting = 100%**; an unassigned line blocks | leave one line unassigned → gate blocks |
| ☐ | 156 | `R-156` | Gate 4d — every fence has a lock test **proven in both directions** | this **is** the reversion mechanism: revert the fence value → its lock test goes red |
| ☐ | 157 | `R-157` | Gate 4f — dead code proved dead by **instrumentation**, never by reading | a deletion with no zero-hit run record → blocked |
| ☐ | 158 | `R-158` | Gate 5 — the old script is deleted or dated-ticketed | a converted step whose old script still exists → red |
| ☐ | 159 | `R-159` | Idempotence-successor run is a **supplement, never the sole gate** | a conversion gated only on run-2-zero-diff → rejected (it proves fixpoint agreement, not path agreement) |
| ☐ | 162 | `R-162` | The same pass never both discovers and retires a fence | a disposition whose author == the discoverer → rejects |
| ☐ | 163 | `R-163` | **Tie-breaker 1** — a step test that survives swapping its compute is a runner t | **run each step's suite against a different step's compute; every test that still passes is a finding.** Mecha |
| ☐ | 164 | `R-164` | Logic tests must **not** run in production | assert the production entry point cannot reach the logic-test path |
| ☐ | 165 | `R-165` | Every declared check has a **must-fail fixture** | a check whose negative fixture passes → red |
| ☐ | 167 | `R-167` | Banned anti-pattern — no step test asserts ledger, lock or transaction behaviour | write one → lint fires (64 copies of the runner's suite is **negative coverage**) |
| ☐ | 169 | `R-169` | Rung 1 inline-WKT is non-negotiable for every azimuth / KNN / area step | a spatial step with no rung-1 test → red |
| ☐ | 170 | `R-170` | Rung 2 requires rung 1 to exist first | approve a rung-2 file with no rung 1 → rejects |
| ☐ | 171 | `R-171` | An approving commit states **why** each value is right | approve with an empty message → rejects |
| ☐ | 172 | `R-172` | Metamorphic invariants hold | translate +1000/+1000 → areas identical, azimuths unchanged; rotate 30° → every azimuth shifts exactly 30°; sc |
| ☐ | 173 | `R-173` | Every golden snapshot query has an explicit `ORDER BY` | omit → lint fires |
| ☐ | 174 | `R-174` | pgTAP carries schema assertions only | a value assertion in pgTAP → lint fires |
| ☐ | 176 | `R-176` | Generator correctness is tested **per branch** | four reversion patches — insert path, update path, `IS DISTINCT FROM` no-op, partial-index conflict target |
| ☐ | 180 | `R-180` | Shapefile fixtures include one corrupt, one non-UTF8 `.dbf`, one missing `.prj` | the malformed cases are the point — three tests |
| ☐ | 182 | `R-182` | Fixtures are minimal — one row per branch, per check, plus null/empty/boundary | a 500-row fixture → lint fires |
| ☐ | 184 | `R-184` | Fixtures live next to their step and are deleted with it | a shared global fixture directory → lint fires |
| ☐ | 199 | `R-199` | No step defines its own `verdictCascade` | define one → conformance red |
| ☐ | 200 | `R-200` | The §11 Counter Semantic Contract — which variable feeds `records_total` | feed it from a secondary entity → red |
| ☐ | 201 | `R-201` | `load-massing`'s `ON CONFLICT` area-column exclusion | include the area columns → the worktree BUG-2 regression lock reds |
| ☐ | 202 | `R-202` | The `tier_1_exact_address` name freeze | rename → red |
| ☐ | 203 | `R-203` | Frozen `records_meta` producer/consumer blocks (`ravine_load` 18 fields, `herita | drop a field → the consumer test reds. **These are runtime contracts, not documentation** |
| ☐ | 204 | `R-204` | `RUN_AT` captured once — the midnight-cross fence | capture it twice → red |
| ☐ | 205 | `R-205` | Lock-ID uniqueness across manifest ∪ `one-time/` ∪ `backfill/` | collide → red |

## 55-B — partial now, closure at the named k (5)

> The mechanism is provable at this conversion; the **population is not there yet**. The partial is the gate — and it is monotone, so it can never go red on the fleet's own progress. Asserting the full claim here would be vacuously green.

| ☐ | # | Test ID | Claim | Arms | Why only a partial at #1 | The partial that IS gated now |
|---|---|---|---|---|---|---|
| ☐ | 36 | `R-036` | Entries older than N months are flagged `stale_interpretation` | k=27 → C5 | TIME-armed, not k-armed: at conversion #1 no note is old enough to be stale, so a hard gate is vacuously green | the flag fires now on a backdated `measured.date` fixture; the real population only grows, and no entry ever un-ages |
| ☐ | 175 | `R-175` | All 64 generated statements `PREPARE`/`EXPLAIN` cleanly | k=27 → C5 | the 64 statements exist only once every generator does — "all 64" at conversion #1 is a count of 2 | every generated statement of the converted subset (⊆ the 64) PREPAREs/EXPLAINs cleanly, and the clean set only grows |
| ☐ | 181 | `R-181` | `pg_trgm` precision/recall never regress below a committed number | k=2 → C1 | conversion #1 COMMITS the baseline; "never regress" has nothing to compare against until the second run | the committed precision/recall number is a ratchet — it may only rise, never regress |
| ☐ | 183 | `R-183` | No fixture exceeds 180 days without review | k=27 → C5 | TIME-armed: no fixture is 180 days old at conversion #1 | the age check is red now on a backdated fixture, and the reviewed-fixture set only grows |
| ☐ | 206 | `R-206` | `records_meta` merge collisions are detected | k=2 → C1 | a merge collision needs two producers — conversion #1 has one | the collision detector is red now on a two-producer fixture; the set of merged producer keys only grows |

## 55-C — DEFERRED, arming k named (6)

> **Not a conversion-#1 gate.** No conversion can satisfy these, so a checkbox here would be a lie in either direction. Each names the k at which it arms and the stage that k lands on; each still carries a monotone partial, so progress toward it is visible rather than merely promised.

| # | Test ID | Claim | Arms at | Gate stage | Why it cannot be gated at #1 | The monotone partial |
|---|---|---|---|---|---|---|
| 160 | `R-160` | Conversions are rate-limited to review capacity | k=2 | **C1** | a rate LIMIT over conversions is undefined at the first conversion | the rate-limit counter is asserted over the conversions already merged (⊆ the fleet) and only grows |
| 161 | `R-161` | One converted step is re-opened and re-audited at ~#20 | k=20 | **C5** | the claim names its own arming k: the re-audit happens at ~#20 | the re-audit queue names ≥1 converted candidate from the merged subset and only grows |
| 168 | `R-168` | Exactly **six** chain-level e2e tests, not sixty-four | k=27 | **C6** | SIX CHAIN-level tests, not step-level: it arms only when the last chain converts (C6), and "exactly six" is no | chain-level e2e tests are ⊆ six at every k — the count is a ceiling that may not be exceeded |
| 177 | `R-177` | `nock` runs in `lockdown` mode | k=27 | **C5** | BLOCKED, not merely early — `nock` is not installed; S6b must resolve the library before any conversion can be | the HTTP-fixture harness is red on its own known-bad fixture, and the set of HTTP-source steps under lockdown only grows |
| 178 | `R-178` | `scope.done()` — an **unused** fixture also fails | k=27 | **C5** | same `nock` blocker as #177 (S6b) | the unused-fixture assertion is red on its own fixture, and the set of steps under it only grows |
| 179 | `R-179` | Paging fixtures include the **empty terminal page** | k=27 | **C5** | same `nock` blocker as #177 (S6b) | the empty-terminal-page fixture is red on its own known-bad case, and the set of paging steps covered only grows |

**Gate:** all 44 55-A items present and proven red, plus all 5 55-B partials present and proven red, before the conversion's Gate 4d passes. The 6 55-C items are tracked at their arming stage (C1, C5, C6) and are **not** counted against the conversion.

> Expect the 55 to GROW per step (v2 plan, R5: growth is PH-3 working). The split is regenerated with the register — it is not a hand-maintained list.
