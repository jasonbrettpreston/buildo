◇ injected env (18) from .env // tip: ⌁ auth for agents [www.vestauth.com]
🔍 Adversarial review of scripts/compute-cost-estimates.js

This review has identified several significant issues, ranging from critical design flaws and spec compliance failures to severe performance bottlenecks. While the script shows good foundational practices like idempotency and transactional integrity, it is not in a shippable state.

- **[CRITICAL]** (Spec): The implementation completely omits a key piece of business logic from the specification. The spec document `ctx_83.md` under "Edge Cases" requires: "Commercial Shells: If the permit is for a 'Shell,' a `0.60x` multiplier is applied to interior trade slices." There is no code anywhere in this script that checks for "Shell" permits or applies this multiplier. This will lead to significantly overestimated costs for all interior trades on commercial shell projects, providing incorrect data to users and the marketplace. Fix this by adding logic to detect "shell" permits (likely via `permit_type` or `work` description) and applying the `0.60` multiplier within `sliceTradeValues` or `estimateCostInline` for the relevant trades.

- **[CRITICAL]** (lines 3-8, 17-80, 107-336): The core cost model logic and all its constants are duplicated between this JS script and the `cost-model.ts` file. The comment on lines 3-8 acknowledges this is a "DUAL CODE PATH" that "MUST stay in sync... byte-for-byte." This is an extremely fragile design. It is not a question of *if* these two files will diverge, but *when*. A developer will inevitably update one and forget the other, leading to subtle, maddening bugs where the API and the pipeline report different cost estimates for the same permit. This is a ticking time bomb. The suggested fix in the comment ("extract to a shared JSON file") must be implemented immediately, not as a "future hardening WF." The logic itself should be extracted to a shared library that can be imported by both the Node.js script and the TypeScript service.

- **[HIGH]** (line 387, 392): The `flushBatch` function is a severe performance bottleneck. It iterates through the batch and executes one `INSERT ... ON CONFLICT` query per row inside a transaction. For the default `BATCH_SIZE` of 5000, this means 5000 individual database round-trips per batch. This pattern does not scale and will cause extreme database load and slow processing times as the `permits` table grows. The entire batch should be inserted using a single statement with a multi-row `VALUES` clause. Given the potential for exceeding Postgres's 65,535 parameter limit, a more robust solution would be to use a library like `pg-copy-streams` to bulk-load the data efficiently.

- **[MEDIUM]** (line 434): The `model_version` is hardcoded to the integer `1`. This provides no useful information about the model that generated the estimate. When constants like `BASE_RATES` or logic in `determineBaseRate` are updated, there is no way to distinguish estimates made with the old model from the new one. This prevents accurate historical analysis and makes debugging model changes impossible. The model version should be a more descriptive constant, such as a date string (`'2026-04-10'`) or a semantic version (`'1.1.0'`), and it must be updated every time the model's logic or constants change.

- **[MEDIUM]** (lines 208-213, 237-241): The logic in `sumScopeAdditions` and `computeComplexityScore` incorrectly handles multiple tags. Both functions loop through all `scope_tags` and add a value if a specific tag is found. If the input array is `['pool', 'elevator', 'pool']`, the `SCOPE_ADDITIONS.pool` value will be added twice, and the `COMPLEXITY_SIGNALS.complexScope` score will be added twice. This is almost certainly not the intended behavior. The cost or score should be added only once per unique feature. To fix this, either process a `new Set(tags)` to ensure uniqueness before iterating, or use a flag to track if the value has already been added for that group of features.

- **[LOW]** (line 353): The `SOURCE_SQL` query arbitrarily selects the "primary" parcel for a permit using `ORDER BY parcel_id ASC LIMIT 1`. This is deterministic, which is good, but it may not be correct. A permit could span multiple parcels, and the one with the lowest `parcel_id` may not be the one containing the primary structure or the largest land area. This could lead to using the wrong `lot_size_sqm` or `building_footprints` data, skewing the cost model, especially for the fallback scenario. The business logic for selecting the primary parcel needs to be confirmed; it may be better to order by lot size descending or use an existing `is_primary` flag if available on the `permit_parcels` join table.

- **[LOW]** (line 164): The `determineBaseRate` function defaults to `BASE_RATES.interior_reno` if no other condition is met. This is a risky assumption. A permit that doesn't fit any of the other categories (e.g., a demolition-only permit, a sign permit) will be incorrectly assigned the cost model for an interior renovation. This could be addressed by adding an explicit "other" or "unknown" category with a more conservative base rate, or by having the function return `null` to signal that the model cannot apply, which would then result in a `null` estimate.

- **[NIT]** (line 256): The `sliceTradeValues` function uses `Math.round()` on each calculated trade value. Due to compounding rounding errors, the sum of all trade values in `trade_contract_values` will not necessarily equal the `estimated_cost`. This can cause confusion and minor data discrepancies. While likely not a major issue, a more robust approach would be to calculate all values, and then assign the remainder of the rounding difference to the largest trade slice (e.g., framing) to ensure the total is preserved.

### Overall Verdict

This script is built on a solid foundation, correctly using transactions, streaming for large datasets, and advisory locks for concurrent run safety. However, it is critically flawed in its current state. The complete omission of the "Commercial Shells" logic is a failure to meet the product specification. More alarmingly, the dual-code-path architecture for the core cost model is an unacceptable maintenance risk that guarantees future bugs. Combined with a severe N+1 performance issue in the database persistence layer, this script is not ready for production. The identified critical and high-severity issues must be addressed before this code can be considered reliable or scalable.

---
⏱  55582ms
📊 Tokens: 15588 (input: 9881, output: 1483)
