◇ injected env (18) from .env // tip: ⌘ suppress logs { quiet: true }
🔍 Adversarial review of docs/reports/script_review_80_86/_ctx/holistic_bundle.md

This review is for `scripts/run-chain.js`.

- **[CRITICAL]** (line 84-92): **`pipeline_schedules` disable is chain-agnostic, causing cross-chain contamination.** The query `SELECT pipeline FROM pipeline_schedules WHERE enabled = FALSE` has no `chain_id` filter. A step like `classify-lifecycle-phase` exists in both the `permits` and `coa` chains. Disabling it via the schedule table for `coa` maintenance will silently disable it for the `permits` chain as well, breaking downstream dependencies. This is a critical design flaw in a multi-chain system. Fix by adding a `chain_id TEXT` column to `pipeline_schedules` and filtering `WHERE enabled = FALSE AND (chain_id IS NULL OR chain_id = $1)`.

- **[CRITICAL]** (line 279-282, manifest): **`compute_opportunity_scores` produces no useful telemetry.** The manifest should configure telemetry for this script. However, the script only performs `UPDATE`s on existing rows in `trade_forecasts`. A pre/post row-count diff (`telemetry_tables`) will always be zero, rendering the step's health invisible. The script's primary output, `opportunity_score`, can fail to populate, and the orchestrator would not detect it. The telemetry contract is broken. Fix by adding `telemetry_null_cols: { "trade_forecasts": ["opportunity_score"] }` to this script's manifest entry to track the column's fill rate.

- **[CRITICAL]** (line 372-376, 475-484): **Chain verdict aggregation silently ignores steps without an `audit_table`.** The code aggregates verdicts by checking `recordsMeta?.audit_table?.verdict`. Sibling triage reports confirm that scripts 81, 82, 85, and 86 *do not emit a structured audit_table*. The Pipeline SDK auto-injects a stub with a default `PASS` verdict for these, but this code path doesn't even see that. The `stepVerdicts` map remains mostly empty, so the chain-level status will report `completed` even if multiple steps had data quality failures that should have been reported as `WARN` or `FAIL`. The entire cross-step health reporting system is a facade. Fix by either enforcing that all scripts emit a proper `audit_table` or changing this code to warn loudly when a step's `records_meta` is missing an expected `audit_table`.

- **[CRITICAL]** (line 128-131, manifest): **`permit_phase_transitions` table growth is untracked and unbounded.** Triage for script 84 confirms it writes to `permit_phase_transitions`. The pre-flight bloat gate at L128 only checks tables listed in `telemetry_tables` in the manifest. `permit_phase_transitions` is not listed in *any* script's manifest entry. It has no retention policy and no monitoring, making it a time bomb that will exhaust disk space and degrade performance invisibly. Fix by adding `"permit_phase_transitions"` to `classify_lifecycle_phase`'s `telemetry_tables` in the manifest.

- **[HIGH]** (line 299, 418-419, 462-464): **Child process `stderr` is discarded on failure.** The `spawn` options are `stdio: ['inherit','pipe','inherit']`. `stderr` is piped to the parent's `stderr` (visible on the console) but is not captured in a buffer. When a child script fails, the `error_message` stored in `pipeline_runs` is just the generic `Command failed...` string from L337. The actual stack trace or error message printed by the child to `stderr` is lost, making post-mortem debugging impossible without access to raw container logs. Fix by changing `stdio` to `['inherit', 'pipe', 'pipe']`, buffering `stderr`, and prepending the last 2-4KB of stderr to the `error_message` string on failure.

- **[HIGH]** (line 72): **No orchestrator-level advisory lock.** Two instances of `run-chain.js permits` can be started concurrently (e.g., by two admins during an incident). The script does not acquire a chain-level lock. This will cause two sets of child processes to run, and since triage reports show that 4 of the 6 tail scripts lack their own locks, they will race to read and write data, leading to corrupt state and non-deterministic outcomes. Fix by adding `pg_try_advisory_lock(hashtext('chain_' || chainId))` at the start of the run and exiting if the lock is held.

- **[HIGH]** (line 173-186, 296): **Step cancellation is not propagated to running children.** The cancellation check only runs *between* steps. If `classify-lifecycle-phase` is running (a 130-second operation), a cancellation request will be ignored for the entire duration. The operator's intent is to stop the work now, not two minutes from now. Fix by polling the cancellation status in parallel with the child process execution and calling `child.kill('SIGTERM')` when a cancellation is detected.

- **[HIGH]** (line 392-396): **`records_updated` metric is stored despite being known to be wrong.** Triage reports for scripts 81, 82, and 85 explicitly state that their `records_updated` count is implemented incorrectly and overcounts reality. This orchestrator blindly accepts and stores this incorrect value in `pipeline_runs`, polluting dashboards and misleading operators about script activity. Fix the producer scripts first (per their triage reports). Optionally add a sanity check here that warns if `records_updated > records_total`.

- **[HIGH]** (line 500-504): **`pipeline_runs.records_meta` has no retention policy and will grow indefinitely.** Each step of each chain run appends 5-15KB of JSONB to this table. With multiple chains running daily, this table will bloat, degrading query performance and consuming significant storage. There is no archival, partitioning, or cleanup strategy. Fix by adding a daily cleanup job that truncates `pipeline_runs` and associated logs older than a reasonable threshold (e.g., 90 days), or implement table partitioning by `started_at`.

- **[MEDIUM]** (line 194, 223, 488): **Gate-skip success pollutes `error_message` column, causing false alerts.** When a chain is correctly skipped due to zero new records, the message `'0 new records — downstream steps skipped'` is stored in the `error_message` column. Any downstream monitoring that alerts on `WHERE error_message IS NOT NULL` will fire false positives on days with no data ingest. A clean, expected skip is not an error. Fix by using a separate `skipped_reason` column or by setting `error_message = NULL` for gate-skipped chains and encoding the reason in the `status` or `records_meta`.

- **[MEDIUM]** (line 194, 223): **`skipped` status is ambiguous.** The status `'skipped'` is used for two distinct reasons: a step was disabled by an operator via `pipeline_schedules` (an intentional override), or a step was skipped by the gate-skip logic (an automatic, data-driven event). This ambiguity hinders observability; an operator cannot distinguish a deliberate manual action from a routine data condition by looking at the status alone. Fix by introducing distinct statuses (`skipped_disabled`, `skipped_gate`) or adding a `skipped_reason` column.

- **[MEDIUM]** (line 121-122): **Pre-flight bloat thresholds are hardcoded.** `BLOAT_WARN_THRESHOLD = 0.30` and `BLOAT_ABORT_THRESHOLD = 0.50` are magic numbers. The pattern established in other scripts is that operational thresholds should be tunable via `logic_variables` in the database. This allows operators to adjust sensitivity without a code deployment. Fix by loading these thresholds from `logic_variables` via `loadMarketplaceConfigs`.

- **[LOW]** (line 293, 60): **`--force` option is not propagated to child scripts.** An operator using `--force` might reasonably expect it to bypass safeties in downstream scripts, such as re-running a step even if an advisory lock is held. The current implementation only uses `--force` to bypass the orchestrator's gate-skip logic. This creates a mismatch between user expectation and behavior. Fix by documenting the limited scope of `--force` or by propagating a `PIPELINE_FORCE_MODE=true` environment variable to children that support it.

## Verdict

The orchestrator is structurally sound but suffers from critical observability and safety gaps that make it **unsafe for production**. It creates a false sense of security. Four CRITICAL findings mean failures can go completely undetected: a global disable flag causes cross-chain contamination (RC-W1), key telemetry for a core script is effectively null (RC-W2), chain-level health verdicts silently ignore the majority of steps (RC-W3), and a major data table's growth is totally unmonitored (RC-W4). Furthermore, the lack of `stderr` capture, no top-level advisory lock, and no propagation of cancellation signals represent significant operational risks. These issues must be fixed before this can be considered a reliable production system.

---
⏱  52070ms
📊 Tokens: 69419 (input: 64433, output: 2120)
