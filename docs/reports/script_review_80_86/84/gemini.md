◇ injected env (18) from .env // tip: ⌘ enable debugging { debug: true }
🔍 Adversarial review of scripts/classify-lifecycle-phase.js

Here is my adversarial code review.

---

- **[CRITICAL]** (lines 204, 222, 402): Unbounded SELECTs load all dirty rows and prerequisite data into memory. This script violates engineering standard §3.2 by loading the entire set of dirty permits, dirty CoAs, and all BLD/CMB permits into Node.js memory before processing. While incremental runs of 5-15K rows may fit, a first-run backfill (~270K rows) or a large-scale re-classification event (e.g., after a logic change) will cause extreme memory pressure and likely crash the process with an out-of-memory error. This is a severe scaling bottleneck. To fix this, refactor the main processing loops to use streaming. Use `pipeline.streamQuery` or a server-side cursor to process one dirty row at a time, or in smaller, memory-managed chunks, instead of loading the entire result set with `pool.query`.

- **[HIGH]** (lines 485-492, 526-530): Correlated subqueries in backfill `UPDATE` statements will perform poorly at scale. The one-time backfill for `phase_started_at` and `permit_phase_transitions` use correlated subqueries inside `CASE` and `NOT EXISTS` clauses, respectively. For each row being updated, the database must execute an inner query. On a large table, this results in an `O(N*M)` or worse execution plan that will be extremely slow and may time out. These should be rewritten as joins. For example, the `phase_started_at` backfill could join against a pre-aggregated `permit_inspections` CTE, and the initial transitions backfill can use a `LEFT JOIN` to the transitions table and check for `NULL` on the right side.

- **[MEDIUM]** (lines 380-388, 448-455): Redundant `UPDATE`s for `lifecycle_classified_at` in every batch. The main `UPDATE` query for changed rows (defined in `buildPermitUpdateSQL` and `buildCoaUpdateSQL`) already sets `lifecycle_classified_at = NOW()`. A second `UPDATE` statement is then run against the entire batch to do the exact same thing for all rows, including those just updated. The comment on line 382 acknowledges this is redundant. This burns unnecessary database CPU, generates extra WAL traffic, and adds latency to every single batch. The fix is to make the second `UPDATE` only target rows that were *not* changed by the first. The first `UPDATE` should use a `RETURNING` clause to get the primary keys of the rows it modified. The second `UPDATE` can then update the remaining rows in the batch by excluding those returned IDs.

- **[MEDIUM]** (line 605): Duplicated and fragile SQL-based normalization logic for CoA decisions. The unclassified-count check for CoAs re-implements normalization logic (`lower`, `trim`, `regexp_replace`) in SQL. This logic is likely different from the canonical normalization happening inside the `classifyCoaPhase` Javascript function, creating two sources of truth and risking drift (a violation of §7 Dual Code Path Safety). If the JS logic changes, this SQL check will silently become inaccurate, potentially masking unclassified rows or reporting false positives. The source of truth for normalization should be the JS library. Instead of re-implementing it in SQL, either add a `normalized_decision` column to the `coa_applications` table populated at ingest time, or accept that this check may have minor inaccuracies and add a comment acknowledging the risk.

- **[LOW]** (line 222): Inefficiently builds orphan-detection map from the entire `permits` table. To detect orphans, the script loads every single `BLD` and `CMB` permit from the database into a map. This is wasteful, as it only needs sibling information for the prefixes of the *currently dirty* permits. A much more efficient approach would be to first collect the unique prefixes from `dirtyPermits`, and then run a second query like `SELECT permit_num FROM permits WHERE split_part(permit_num, ' ', 2) = ANY($1::text[])`, passing the array of dirty prefixes. This would drastically reduce the memory footprint and query time for the orphan detection step on incremental runs.

- **[NIT]** (lines 282-296): Overly verbose orphan detection loop. The current logic uses a `for...of` loop with a `break` to check if a permit has any siblings in its prefix set. This can be expressed more clearly and concisely. Replace the entire block from `let is_orphan = true;` to the end of the loop with a single line: `const siblings = bldCmbByPrefix.get(prefix); const is_orphan = !siblings || (siblings.size === 1 && siblings.has(row.permit_num));`. This is easier to read and directly expresses the logic: a permit is an orphan if its prefix group doesn't exist, or if it exists and contains only that one permit.

### Overall Verdict

This script is a complex and thoughtful piece of engineering that correctly handles critical concerns like concurrency control with advisory locks and transactional integrity with per-batch commits. The author has clearly contended with subtle bugs in the past, as evidenced by the detailed comments regarding the lock client and parameter indexing. However, the core data-loading strategy is fundamentally flawed from a scaling perspective. The reliance on loading the entire set of dirty rows into memory is a ticking time bomb that will cause catastrophic out-of-memory failures during a full backfill or a large re-classification event. While the business logic appears sound, this critical architectural issue must be addressed by refactoring to a streaming-based approach before this script can be considered production-ready for the long term.

---
⏱  59382ms
📊 Tokens: 18636 (input: 12534, output: 1278)
