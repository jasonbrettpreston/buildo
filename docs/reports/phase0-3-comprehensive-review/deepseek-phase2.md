◇ injected env (16) from .env // tip: ⌘ enable debugging { debug: true }
🔍 Adversarial review of docs/reports/phase0-3-comprehensive-review/bundle-phase2.txt

**[HIGH]** (line 40, get-user-context.ts): Database query may succeed but return null trade_slug. The interface declares `trade_slug: string` (non-nullable), but the query selects directly from the database column without null validation. If `trade_slug` is NULL in the database, this will pass a malformed UserContext downstream. This violates TypeScript contracts and will cause runtime errors when trade_slug is used. Fix by adding explicit null check: `if (!row?.trade_slug) return null`.

**[CRITICAL]** (line 28, route.ts): Missing content-type validation in POST route. The route calls `request.json()` without checking `Content-Type: application/json`. An attacker could send a malformed body with `text/plain` or `multipart/form-data`, causing `request.json()` to throw a different error or parse incorrectly. This could bypass validation or cause unpredictable behavior. Fix by adding header check before parsing: `if (!request.headers.get('content-type')?.includes('application/json')) return err('INVALID_CONTENT_TYPE', ...)`.

**[MEDIUM]** (line 30-32, error-mapping.ts): `rateLimited` function exposes internal rate limit remaining count in error details. This leaks system state and enables attackers to precisely gauge rate limit windows and optimize attack timing. The spec requires `Retry-After` header, but `remaining` should not be exposed. Fix by removing `details: { remaining }` from the response.

**[MEDIUM]** (line 20, envelope.ts): `err` function does not validate or sanitize `details` parameter. If a caller passes a non-serializable object (like a function, circular reference, or Error instance with getters), `NextResponse.json()` will throw, crashing the route. This violates the "never-throws" contract of the error helpers. Fix by adding safe serialization: `JSON.parse(JSON.stringify(details))` or a try-catch wrapper.

**[HIGH]** (line 33, route.ts): Trade slug comparison is case-sensitive but may not match database collation. If `user_profiles.trade_slug` uses case-insensitive collation (common in PostgreSQL), and a client sends `Trade-Slug` vs `trade-slug`, the 403 check will incorrectly reject legitimate requests. This creates a mismatch between authorization logic and actual data constraints. Fix by normalizing both sides: `if (params.trade_slug.toLowerCase() !== ctx.trade_slug.toLowerCase())`.

**[MEDIUM]** (line 36, route.ts): The route uses `Object.fromEntries(request.nextUrl.searchParams)` which flattens duplicate query parameters. If a client sends `?lat=1&lat=2`, only the last value (`2`) is used silently. This could cause unexpected behavior where the server validates `lat=2` but the client intended `lat=1`. The Zod schema will validate the last value, but this is a potential source of confusion and attack. Fix by detecting duplicates and returning 400: `if (request.nextUrl.searchParams.getAll('lat').length > 1) return badRequest(...)`.

**[LOW]** (line 15-16, request-logging.ts): `logRequestComplete` uses `Date.now() - startMs` but doesn't handle clock skew or negative durations. In serverless environments with cold starts, `start` could be recorded incorrectly, causing negative durations in logs. While not security-critical, this pollutes metrics. Fix by `Math.max(0, Date.now() - startMs)`.

**[MEDIUM]** (line 29, schemas.ts): `cursor_score` uses `.int()` constraint but scoring algorithm could produce fractional values in future. The comment acknowledges this, but the current implementation would truncate fractional scores if they're introduced, causing pagination gaps and duplicate/missing records. This is a time bomb. Fix by either: 1) Document as a breaking change requiring migration, or 2) Use `.number().finite()` now and adjust SQL casting.

**[HIGH]** (line 18-19, route.ts): Race condition in auth + rate limit flow. The route authenticates first (`getCurrentUserContext`), then applies rate limiting using the UID. An attacker could spam invalid authentication tokens, bypassing rate limits because the UID isn't established yet. This enables denial-of-service against the authentication system itself. Fix by applying IP-based rate limiting BEFORE authentication, or implement separate rate limiting for authentication failures.

**[LOW]** (line 47, route.ts): The cursor conditional spread uses loose boolean evaluation. If `cursor_score` is `0` (valid nonnegative integer), the condition fails because `0 !== undefined` is true but `0` is falsy. This means a cursor with `score: 0` would be incorrectly treated as undefined, breaking pagination for low-scoring leads. Fix by explicit `params.cursor_score !== undefined` checks (already done) - but note the current code uses AND with three separate checks, which is correct. However, the comment says "Conditional spread satisfies exactOptionalPropertyTypes" which is misleading because the condition already uses `!== undefined`. This is actually fine but the comment is wrong.

**[MEDIUM]** (line 40, route.ts): No request body size limit in POST route. An attacker could send megabytes of malformed JSON, causing memory exhaustion or CPU drain in `request.json()` parsing. This is a classic DoS vector. Fix by checking `Content-Length` header upfront and rejecting >1MB, or configuring Next.js body size limit at the framework level.

Overall verdict: The codebase demonstrates good architectural patterns with clear separation of concerns, but contains several critical security oversights (content-type validation, rate limit leaking, case-sensitivity mismatch) and data integrity issues (nullable trade_slug, cursor score integer truncation). The most urgent fixes are adding proper content-type validation, removing rate limit details from responses, and ensuring trade slug comparison matches database collation. The race condition in authentication vs rate limiting is also significant for production deployment under attack.

---
⏱  183736ms (183.7s)
📊 Tokens — total: 13670, input: 5815, output: 7855, reasoning: 6587
