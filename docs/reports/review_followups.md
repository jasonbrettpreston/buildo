# Active Review Follow-ups (Consolidated)
_Generated following the Pipeline Clean-up Mandate._

## WF3 — Spec 99 §7.2 Cache Invalidation Telemetry 2026-05-05 (RESOLVED)

**Source:** §9.21 mandates-lint test (commit `e655417`) surfaced that §7.2 (cache invalidation telemetry) had ZERO implementation evidence at HEAD — `Sentry.addBreadcrumb({category:'query',...})` paired with `invalidateQueries` did not exist anywhere in `mobile/src/`. Audit Phase 4 verified §7.1 + §7.3 but did NOT check §7.2; the lint test surfaced the gap and parked it as `it.skip` with `pendingReason` until follow-up WF3.

**Resolution:** New `logQueryInvalidate(key)` helper at `mobile/src/lib/queryTelemetry.ts` consolidates the §7.2 two-call pattern (`Sentry.addBreadcrumb` always + `track('query_invalidate')` DEV-only). Wired the helper at 10 non-trivial `invalidateQueries` sites across `mobile/src/` and `mobile/app/`. Strict spec-letter reading applied: `onSettled` is the only exempt mutation lifecycle handler; `onSuccess` is non-trivial.

**Sites wired (10 non-trivial):**
- `mobile/src/store/authStore.ts:266` (auth listener UID-change branch)
- `mobile/src/components/paywall/PaywallScreen.tsx:114` (refresh button)
- `mobile/src/components/feed/SearchPermitsSheet.tsx:53` (mutation `onSuccess`)
- `mobile/src/hooks/useRemoveFromBoard.ts:31` (mutation `onSuccess` — initially misclassified as `onSettled` and missed; code-reviewer caught and fix landed inline)
- `mobile/app/_layout.tsx:239` (reactivation handler)
- `mobile/app/(app)/_layout.tsx:139` (AppState 'active' handler)
- `mobile/app/(app)/_layout.tsx:190` (post-payment expired→active transition)
- `mobile/app/(app)/settings.tsx:110` (notification-prefs mutation `onSuccess`)
- `mobile/app/(onboarding)/complete.tsx:84` (onboarding completion bare invalidate)
- `mobile/app/(onboarding)/terms.tsx:100` (terms acceptance bare invalidate)

**Sites left exempt (2 — `onSettled` per spec letter):**
- `mobile/src/hooks/usePatchProfile.ts:81` (`useMutation.onSettled`)
- `mobile/src/hooks/useSaveLead.ts:65` (`useMutation.onSettled`)

**Whitelist extension:** `mobile/src/lib/analytics.ts` `ALLOWED_KEYS` + `AllowedKey` gained `'key'` (operational metadata only — values are TanStack queryKey roots like `'user-profile'`, `'leads'`, `'flight-board'`, `'notification-prefs'`).

**§9.21 lint test flipped:** `mobile/__tests__/spec99.mandates.lint.test.ts:§7.2` row no longer has `pendingReason`; the case is now an active `it()` enforcing 3 assertions: (a) helper file exists + exports `logQueryInvalidate`, (b) helper has canonical Sentry breadcrumb shape, (c) at least one caller exists in `mobile/src/` or `mobile/app/`. Total cases: 12 passed (was 11+1 skip).

**New helper unit tests:** `mobile/__tests__/queryTelemetry.test.ts` (NEW, 3 tests) — runtime regression coverage beyond the static lint:
1. `Sentry.addBreadcrumb` fires with the canonical `{category:'query', message:'invalidate', data:{key}}` shape.
2. `track('query_invalidate', {key})` fires when `__DEV__=true`.
3. `track('query_invalidate', ...)` does NOT fire when `__DEV__=false` (production builds dead-code-eliminate via Hermes per WF3 H3 verified pattern).

**Code-reviewer findings (single agent per WF3 default — user did not request adversarial):**
- 1 BUG (fixed inline before commit): site classification miss on `mobile/src/hooks/useRemoveFromBoard.ts:31`. The handler is `onSuccess` (NOT `onSettled` as I initially classified). Under the strict-spec-letter reading the user accepted at PLAN LOCK, `onSuccess` is non-trivial and requires the helper call. Other `onSuccess` sites (`SearchPermitsSheet.tsx`, `(app)/settings.tsx`) were correctly wired; this one was missed because the surrounding comment said "Only invalidate" which I misread as the `onSettled` "always-runs" idiom. Total wired sites went from 9 → 10.
- 1 DEFER (recorded below for future cleanup): §9.21 lint check comment overstates enforcement. The comment claims "at least 2 hits in src/ (helper + caller)" but the actual condition is `if (!appCallerFound && !srcCallerFound)` using boolean searchTree results. Since the helper file itself matches `logQueryInvalidate\(`, `srcCallerFound` is permanently `true`, making the guard inert via the `src/` path. Acceptable today (any wired site in `app/` makes the test pass for the right reasons), but the comment should be corrected OR a `countMatches` helper should replace the boolean `searchTree` to actually enforce the "2 hits" intent.

**Test results:** 344/348 mobile pass (was 340/345 before; +4 = 1 §7.2 flip + 3 new helper unit tests) + 4 skipped (was 5; the §7.2 it.skip flipped to passing it). Mobile typecheck: 0 errors. Repo-root lint: warnings only. Repo-root typecheck: 0 errors.

**Red Light evidence (mutation test, reverted):** Nullified the helper body to `void key;` (no Sentry, no track). Both the §7.2 lint case AND all 3 helper unit tests failed correctly. Reverted; all green.

**New finding deferred for future cleanup:**

| Severity | Source | Item | Planned Home |
|---|---|---|---|
| LOW | §7.2 code-reviewer | **§9.21 lint check comment overstates enforcement.** `mobile/__tests__/spec99.mandates.lint.test.ts:149-155` comment claims "at least 2 hits in src/" but actual condition uses boolean `searchTree`. Helper file itself matches the regex, so `srcCallerFound` is permanently `true`. Guard is inert via `src/` path. | Future doc-only WF — correct the comment to match actual semantics OR implement a `countMatches` variant returning a number for proper enforcement |


## WF1 — Spec 99 §9.21 Mandates-Lint Test (Pattern A class fix) 2026-05-05 (RESOLVED)

**Source:** WF5 audit `docs/reports/audit_spec99_2026-05-04.md` Phase 5 (lines 161-168) — "Pattern A: Spec mandate exists but no enforcement test (HIGH H2 + HIGH H3 both)... Routing: Add §9.21 (NEW) — `mobile/__tests__/spec99.mandates.lint.test.ts` that statically asserts every `## §X mandate` in Spec 99 has a corresponding implementation grep-target."

**Resolution:** New test file `mobile/__tests__/spec99.mandates.lint.test.ts` (~360 lines) with hardcoded `MANDATES` array of 10 rows (§7.1-§7.4 Observability + §8.1-§8.6 Test Mandates). 12 cases total: 1 sanity (Spec 99 reachable) + 10 per-mandate evidence checks + 1 meta-count guard. Pattern matches `routerHygiene.lint.test.ts` (51-test §5.4+§6.1 enforcement) and `storeReset.coverage.test.ts` (§8.5).

**§9 row appended** to Spec 99 §9 Migration Plan table marking §9.21 ✅ DONE (referenced in commit `<this commit>`).

**NEW finding surfaced by writing the lint test:**

| Severity | Source | Item | Planned Home |
|---|---|---|---|
| HIGH | §9.21 lint test | **§7.2 cache invalidation telemetry has zero implementation evidence at HEAD.** Spec 99 §7.2 mandates `Sentry.addBreadcrumb({category:'query', message:'invalidate', data:{key}})` paired with every non-trivial `queryClient.invalidateQueries` call. Grep across `mobile/src/` finds zero matches for the breadcrumb pattern. Audit Phase 4 verified §7.1 + §7.3 but did NOT check §7.2 — the §9.21 lint test surfaced this gap. The §7.2 case is `it.skip` with `pendingReason` until implementation lands. | WF3 — wire `Sentry.addBreadcrumb({category:'query',...})` at every non-trivial `invalidateQueries` site (notably `(app)/_layout.tsx` AppState and post-payment effects, `useSaveLead`, `useRemoveFromBoard`, `usePatchProfile`); also add the DEV-only `track('query_invalidate', {key})` PostHog event per §7.2 line 490. When wired, remove the `pendingReason` field from the §7.2 row in `MANDATES` to flip `it.skip` → `it`. |

**Code-reviewer findings (single agent per user "skip adversarial" request):**
- 0 BUG / 0 CRITICAL.
- 2 IMPORTANT — fixed inline before commit:
  - **F1: `walkSource` excludes `.test.ts` but not `.test.tsx`.** Forward-compat: a future co-located component test in `mobile/src/` could contain a mock `Sentry.addBreadcrumb({category:'query'})` and satisfy §7.2 as a false positive. Fixed by adding `!entry.name.endsWith('.test.tsx')` to the exclusion list.
  - **F2: §8.1 and §8.3 read test-file source without `stripComments`.** Inconsistent with §7.1, §7.3, §7.4, §8.4 which all `stripComments` before matching, and contradicts the file-header rationale ("commented-out evidence does NOT count as implementation"). Fixed by wrapping both `readOrEmpty` calls with `stripComments`.
- 3 DEFER (recorded for future cleanup, not blocking):
  - **D1: §7.2 `searchTree` scope when implementation lands.** Currently scans all of `mobile/src/`; could scope to `mobile/src/hooks/` + `mobile/src/store/` for tighter semantics + faster runtime. Pure performance/semantic improvement; defer until §7.2 is wired and the `it.skip` is flipped to `it`.
  - **D2: §7.4 suppression-marker vocabulary is static.** Check tests `strictModeSuppress|suppressDoubleFire`. A future contributor suppressing via different name (`dedupRender`, `strictModeNoop`) would evade. Add a TODO comment in §7.4's check body naming the accepted gap; if `stateDebug` ever gains a config arg, expand the regex.
  - **D3: §8.3 regex matches regex-literal syntax, not prose.** The `Permitted (narrow )?carve-outs` check matches against a JS regex literal in `subscriptionGate.test.ts`. If the gate-stability test is refactored to use `.toContain('Permitted carve-outs')`, the lint silently loses coverage. Consider re-anchoring to the more stable `it(` test title string instead.

**Red Light evidence (mutation tests, both reverted):**
- Mutation A: renamed `track('route_decision'` → `track('route_decision_DISABLED'` in `mobile/app/_layout.tsx` only. Initial weak §7.3 check (concatenated bodies) PASSED — caught a real test weakness during mutation testing. Hardened the §7.3 check to require BOTH layout files independently. Re-mutated → §7.3 case correctly failed with `no track("route_decision") in app/_layout.tsx (AuthGate authority site)`.
- Mutation B: bumped `expect(MANDATES).toHaveLength(10)` to `99` → meta-count case correctly failed.

**Test results:** 11/12 pass + 1 skip (§7.2 pending). Full mobile suite: 340/345 pass + 5 skipped (4 baseline + 1 new §7.2). Mobile typecheck: 0 errors (note: `.expo/types/router.d.ts` is an auto-regenerated typed-routes artifact that intermittently surfaces a pre-existing `router.replace(decision.to)` Href narrowing issue at `app/_layout.tsx:180`; this is a stale-cache pattern, NOT introduced by §9.21, and not in §9.21 scope to fix).


## WF2 — Spec 99 M1+M2+M3 Doc-Only Spec Sync 2026-05-05 (RESOLVED)

**Source:** WF5 audit `docs/reports/audit_spec99_2026-05-04.md` rows 203-205 (M1, M2, M3) — all tagged "WF2 doc-only" sync of `docs/specs/03-mobile/99_mobile_state_architecture.md` to current code reality.

**Resolution:** ONE bundled commit covering 3 audit M-findings + 3 review fixes (1 BUG + 1 BUG + 1 DEFER from code-reviewer, all addressed inline). Net spec change: +40 / -16 lines, single file. Zero source code change.

**Edits landed:**
- **§B5 line 290 (rule body) + §B5 code block (lines 268-285) + §9.10 row** — M1 + code-reviewer F1 (DeepSeek C2 confirms): rule rewritten from `removeQueries({queryKey:['user-profile']})` to `queryClient.clear()` (broader purge for defense-in-depth) AND code block updated to reflect post-§9.19 production shape (`try/finally` wrapping `auth().signOut()` + `clearLocalSessionState()` helper extraction). The original code block was pre-§9.19 inline pattern, contradicting both the surrounding rules and the actual implementation.
- **§5.2 stale-profile guard + §9.11 row** — M2: code example updated to `if (profile && (!profile.user_id || profile.user_id !== user?.uid)) return;`; new "Hardening beyond spec letter" paragraph documents the falsy-uid attack class (corrupted/poisoned cache with `user_id = null/''/undefined`) and cites Gemini WF2 §9.6 F1 + DeepSeek #2 consensus. §9.11 closed to ✅ DONE.
- **§9 Migration Plan table** — M3: appended 4 new rows §9.17-§9.20:
  - **§9.17** cursor backward-compat (`3fa96a1`) — server-side fix at `src/features/leads/lib/get-lead-feed.ts`. Code-reviewer F2 caught a SQL parameter error in my initial draft (`CASE WHEN $8::text` → corrected to `CASE WHEN $7::text = 'builder' THEN LPAD($8::text, 20, '0') ELSE $8::text END` per actual production SQL).
  - **§9.18** PII MMKV strip (`671aa87+202a9aa`) — `mobile/src/lib/persistFilter.ts` (NEW), `mobile/app/_layout.tsx`, `mobile/src/store/authStore.ts`, `mobile/__tests__/offline.test.ts`.
  - **§9.19** forced-signout cleanup unification (`381a0c9+f2f7147`) — `clearLocalSessionState()` extraction; `mobile/src/store/authStore.ts`, `mobile/__tests__/storeReset.coverage.test.ts`, `mobile/__tests__/useAuth.test.ts`.
  - **§9.20** dead-code sweep (`7bcb681`) — server-side `CLIENT_SAFE_COLUMNS` removal at `src/lib/userProfile.schema.ts`.

**Multi-Agent Review (per WF2 protocol — all 3 agents):**

- **feature-dev:code-reviewer (worktree-isolated):** 2 BUG + 2 DEFER. BUGs (F1 §B5 code block stale, F2 §9.17 SQL parameter error) fixed inline. DEFER F3 (§B5 attribution should mention §9.19 consolidation of `removeClient()`) also fixed inline as a small-cost improvement. DEFER F4 (`§B5 Hardening Beyond Spec Letter` subsection per audit Pattern B) deferred — §B5's rule body already explains the broader-purge rationale inline; a separate labeled subsection would be redundant. Filed as continuation of the audit's Pattern B class-level fix recommendation.
- **Gemini Pro adversarial:** 1 CRITICAL + 4 HIGH + 2 MEDIUM + 1 LOW. ALL OUT OF SCOPE for M1+M2+M3 (doc-only sync). Filed as new findings below for future WF triage.
- **DeepSeek-R1 adversarial:** 2 CRITICAL + 2 HIGH + 1 MEDIUM + 1 LOW. ONE CRITICAL overlaps with code-reviewer F1 (signOut error handling — same site, same fix already applied). The remaining items are OUT OF SCOPE; filed below for future WF triage.

**New findings from Gemini + DeepSeek (out-of-scope for this WF2; for future triage):**

| Severity | Source | Item | Planned Home |
|---|---|---|---|
| CRITICAL | Gemini | **`userProfileStore` persists PII to unencrypted MMKV via Zustand `persist` middleware (§3.1 mirrored fields).** §9.18 fixed the TanStack persister but `userProfileStore` is a separate persistence layer carrying the same 5 PII fields (`full_name`, `phone_number`, `company_name`, `email`, `backup_email`). Direct violation of §2.1 Layer 4a/4b boundary. | WF3 — add `partialize` to `userProfileStore` to strip PII fields from `persist()`; or migrate user-profile mirror out of Zustand and rely on TanStack canonical fetch |
| CRITICAL | DeepSeek | **B4 re-sign-in same-user cache invalidation gap.** After sign-out, `lastKnownUid` retains the previous user's UID (by design for cold-boot detection). When that user signs in again, `isUidChange` is false, so no `invalidateQueries({queryKey: ['user-profile']})` fires; the stale cached profile (including `subscription_status`) is served until `staleTime` (5 min) or next refetch trigger. | WF3 — always invalidate on `onAuthStateChanged(user)`, regardless of `isUidChange`; move the `isUidChange` guard to the telemetry breadcrumb only |
| HIGH | Gemini | **§6.5 isFetching carve-out is fragile.** Couples generic query loading state to specific business logic transition (post-payment). Future query refactor could silently break post-payment UI. Recommends a dedicated `useMutation`-backed `isAwaitingSubscriptionUpdate` flag instead. | WF1 — already considered at H1 plan time; documented in H1 RESOLVED entry as the rejected alternative. Re-open if a regression surfaces. |
| HIGH | Gemini | **B4 stale-bearer-token race.** `onAuthStateChanged` fires making `user` truthy; if a query fires before the async `getIdToken().then(setAuth)` completes, `apiClient` reads the OLD `idToken` while `enabled: !!user` already passes. | WF3 — tighten `enabled` to `!!user && !!idToken` for user-scoped queries; clear `idToken` synchronously when `user` changes |
| HIGH | Gemini | **signOut() not crash-resilient.** App crash between `auth().signOut()` and the cleanup leaves stale data persisted on disk. (PARTIALLY addressed by §9.19's try/finally but Gemini's specific concern is app-startup recovery.) | WF3 — add `clearLocalSessionState()` invocation to app startup when `auth().currentUser` is null |
| HIGH | DeepSeek | **B4 mid-session token expiry handling not specified.** Firebase ID tokens expire after ~1 hour; spec doesn't document the refresh strategy. | WF2 — document the existing `apiClient` 401 interceptor as the canonical refresh path (Bridge B6?), or codify a refresh strategy |
| HIGH | DeepSeek | **B3 optimistic rollback can overwrite concurrent state changes.** `onMutate` captures `previous`, `onError` writes it back — if another mutation/hydrate touches the same field between, rollback reverts legitimate changes. | WF2 — document version-counter or last-write-wins strategy in B3 spec; or amend B3 hook to re-read current value before rollback |
| MEDIUM | Gemini | **§8.3 / §7.3 mandates have/had no enforcement (§8.3 closed by H2, §7.3 closed by H3).** Gemini correctly observed the audit's H2/H3 findings. Both now resolved. | ✅ ALREADY DONE (H2 + H3) |
| MEDIUM | Gemini | **MMKV ban lacks automated enforcement** — §2.1 hard rule banning direct `createMMKV().getString()` outside `mobile/src/lib/persistence/` is verified manually only. | WF2 — add ESLint rule banning `react-native-mmkv` imports outside the allowed module list |
| MEDIUM | DeepSeek | **`getDiagnosticsSnapshot()` returns empty in production builds — CI tests in production mode pass vacuously.** §8.4's `expect(maxRendersPerSecond).toBeLessThan(20)` assertion would mask render-storm regressions if CI runs with `__DEV__=false`. | WF2 — gate the assertion to dev-mode tests OR provide a production-safe diagnostic fallback |
| LOW | Gemini | **`queryClient.clear()` too aggressive on sign-out** (recommends scoped predicate-based `removeQueries`). DIRECTLY CONTRADICTS the audit's M1 ruling (chose broader purge for defense-in-depth). | NOT actioned — audit ruling stands. The trade-off (cache loss on next sign-in) is intentional |
| LOW | DeepSeek | **Stale-profile guard returns early without loading state — could leave previous user's UI visible for several frames.** | WF2 — add a loading-state route or component for the stale-profile branch |

These findings expand the Spec 99 bug surface but are NOT blocked by the M1+M2+M3 doc sync; the spec now matches code reality, which is the precondition for evaluating each new finding against accurate normative text.


## WF3 — Spec 99 H5 MEMORY.md Auto-Memory Cleanup 2026-05-05 (RESOLVED)

**Source:** WF5 audit `docs/reports/audit_spec99_2026-05-04.md` finding **HIGH H5** — `~/.claude/projects/.../memory/MEMORY.md` was a 94-line snapshot blob violating the auto-memory protocol's "MEMORY.md is an index, not a memory" rule (CLAUDE.md auto-memory section). Audit cited specific stale numeric claims (`41 mig vs 114 actual`, `36 specs vs 67 actual`, `1823 tests vs ~4990 actual`) as the symptom; the deeper structural concern was that the file's shape was drift-prone by construction.

**Resolution:** Memory-side cleanup only — these files live at `~/.claude/projects/C--Users-User-Buildo/memory/` and are user-scope (NOT in the repo). The only repo edit is this RESOLVED entry.

**MEMORY.md trim — 94 → 22 lines:** Deleted 11 inline-content sections that violated the auto-memory "What NOT to save" rules (code patterns / conventions / architecture / file paths / project structure / anything already documented in CLAUDE.md or `tasks/lessons.md`):

- Project Overview, Key Architecture Decisions, Admin Dashboard, Auth & Middleware, CoA Integration, Testing, 32 Trade Slugs, Classification Architecture — all derivable from CLAUDE.md + specs.
- DB Column Gotchas, TypeScript Quirks Found, ESLint Config — already in `tasks/lessons.md` (read at session start per CLAUDE.md Prime Directive #8). Verified at lessons.md lines 17-19 (all three CoA aliases + notification column names present).

**Audit's specific stale numbers were ALREADY cleaned** in a prior session — confirmed via grep before the trim. The numeric-claim portion of H5 was already partially closed; this WF3 closed the structural portion.

**Kept (already-indexed) sections:** Active Investigations (4 entries), Pipeline Hardening Roadmap, User Environment, Feedback (4 entries) — all retained verbatim except for two stale-hook updates:
- Pipeline Hardening Roadmap hook: "Phase 1 Step 2 next, B20-23 deferred" → "~65 pipeline bugs; Phases 1-3 fully resolved; Phase 4 WF1 epics remain". Verified against `project_pipeline_hardening_roadmap.md` which marks Step 2 + B20-B23 BOTH done with commit hashes.
- Multi-worker architecture hook: "WF1 for next session" → "IMPLEMENTED + HARDENED (commits `1e8eea1`, `f9cb5aa`)". Verified against `project_multiworker_architecture.md` which shows the implementation + hardening fixes already committed.

**Orphan disposition:** `wf5_pipeline_audit.md` (55 days old, not previously indexed) was initially proposed for deletion as a stale orphan. User clarified it's REFERENCE input used when running WF5 on the pipeline — captures the "Option C — Raw DB Transparency" design decision + a bug-pattern checklist that subsequent audits validate against. Disposition reversed: file KEPT, NEW "Pipeline Audits" section added in MEMORY.md with index entry. **This was a near-miss — would have deleted load-bearing reference material; saved by the user's mid-execution intervention.**

**Code-reviewer findings (single feature-dev:code-reviewer agent, isolation:worktree):**
- **IMPORTANT — Pipeline Hardening Roadmap hook exceeded ~150 chars** (was ~196 chars after the stale-claim update). Fixed inline by trimming to 142 chars: "~65 pipeline bugs; Phases 1-3 fully resolved; Phase 4 WF1 epics remain". The longer version's per-phase status itemisation is recoverable from the linked file.
- **DEFER — No memory captures the per-finding WF3 cadence pattern.** Across H1-H5, the user consistently ran one WF3 per audit finding with full plan-lock ceremony (never bundled findings into a single WF3). Existing `feedback_always_use_workflow.md` covers "ceremony mandatory" but not "per-finding granularity". Recommend creating `feedback_wf3_granularity.md` ("audit findings are fixed one-at-a-time, never bundled; sequential phase commits preferred within a single WF3") in a future cleanup. Not addressed in this WF3 because it stretches scope (the H5 finding is about MEMORY.md hygiene, not adding new memories).

**No commit beyond this RESOLVED entry.** All MEMORY.md and `wf5_pipeline_audit.md` changes are in user-scope `~/.claude/projects/.../memory/` — not version-controlled.

**Update 2026-05-05:** Deferred item **D1** (per-finding WF3 cadence memory) closed by follow-up WF3. Created `~/.claude/projects/C--Users-User-Buildo/memory/feedback_wf3_granularity.md` capturing the rule + body (rule, **Why** with 9-commit citation chain, **How to apply** with 6 sub-rules covering sequential default, per-finding granularity, multi-phase per-phase commits, new-findings routing, inline-DEFER exception, per-WF override scope). Added MEMORY.md index entry under `## Feedback` between `feedback_always_use_workflow.md` (ceremony) and `feedback_review_protocol.md` (review). Index hook initially read as absolute; code-reviewer flagged as IMPORTANT; fixed inline to add "default + bundle only on override" so the hook accurately conveys the opt-out clause. Code-reviewer 0 BUG, 1 IMPORTANT (fixed inline), 2 DEFER (D1: commit SHA chain in Why fragile after rebase — low priority, not load-bearing; D2: recursive deferred-item case not explicitly stated — covered by composition with `feedback_always_use_workflow.md`'s ceremony rule, no action). Memory file is user-scope, NOT committed; this RESOLVED note is the only repo edit.


## WF3 — Spec 99 H4 Mobile Typecheck Cleanup 2026-05-05 (RESOLVED — 4 commits)

**Source:** WF5 audit `docs/reports/audit_spec99_2026-05-04.md` finding **HIGH H4** — `npx tsc --noEmit` from `mobile/` had 15 pre-existing errors across 4 root causes (bridges.test.ts QueryObserver invariant generics × 2; offline.test.ts missing dev-dep × 1; FlashList v2 generic erasure × 10; expo-notifications + expo-router × 2). Blocked future WF5 GO verdict.

**Resolution:** 4 commits, 5 files modified, +1 dev-dep added. Type-only changes — no runtime behaviour change EXCEPT the C1 BUG fix below, which restores the silent-foreground notification intent that had silently regressed when `expo-notifications` v0.27+ split `shouldShowAlert` into `shouldShowBanner` + `shouldShowList` (the regression was hidden by `skipLibCheck: true` in `expo/tsconfig.base.json`).

| Commit | Phase | Closes | Files |
|---|---|---|---|
| `47a1b24` | 1 | bridges.test.ts ×2 (TanStack v5 QueryKey readonly + helper variance) | `mobile/__tests__/bridges.test.ts` |
| `19de789` | 2 | offline.test.ts ×1 (missing `@tanstack/query-sync-storage-persister` dev-dep) | `mobile/package.json`, `mobile/package-lock.json` |
| `21520d9` | 3 | FlashList ×10 (`Animated.createAnimatedComponent(FlashList) as unknown as typeof FlashList`) | `mobile/app/(app)/index.tsx`, `mobile/app/(app)/flight-board.tsx` |
| `<phase-4-commit>` | 4 | NotificationBehavior fields (BUG fix, not pre-existing tsc error — see below) | `mobile/app/_layout.tsx` |

**Phase 4 — code-reviewer-discovered BUG (was NOT what the H4 audit thought it was):**

The audit listed `_layout.tsx` lines 56 + 171 as 2 of the 15 typecheck errors, attributing them to `NotificationBehavior` shape change + expo-router `Href` narrowing. After Phase 2's `npm install --legacy-peer-deps` refreshed transitive deps, the typecheck count dropped to 0 WITHOUT any code change to `_layout.tsx` — Phase 4 was initially marked "absorbed". Code-reviewer flagged this as suspicious and verified by reading `node_modules/expo-notifications/build/Notifications.types.d.ts`: the installed `expo-notifications@~0.32.16` `NotificationBehavior` interface DOES still require `shouldShowBanner: boolean` and `shouldShowList: boolean` as non-optional fields. The reason typecheck passes anyway is `skipLibCheck: true` in `mobile/node_modules/expo/tsconfig.base.json` — TypeScript skips checking the .d.ts return-type contract on `handleNotification`, so the missing fields are invisible to the compiler.

The runtime impact is real: on iOS 14+/Android 13+, the native layer reads `shouldShowBanner` to decide whether to show a foreground notification banner. Without the field, the OS uses native defaults (likely `true` — banner shown), defeating the silent-foreground intent (we render in-app toasts via `NotificationToast` instead).

Fixed in Phase 4 by adding both fields explicitly with `false` and an inline comment documenting the `skipLibCheck` invisibility for future contributors. The `shouldShowAlert: false` line stays as deprecated-API compatibility for any older expo-notifications consumers in the dep tree.

**Phase 4 was therefore NOT absorbed — the audit's H4 baseline misattributed `_layout.tsx`'s 2 errors. Whatever those original errors were, Phase 2 cleared them; the actual `NotificationBehavior` runtime defect was a separate finding only surfaced by the code-reviewer's `skipLibCheck` analysis.**

**Code-reviewer findings (3 commits + Phase 4 BUG amendment):**

- **BUG (C1) — `_layout.tsx:56-60` NotificationBehavior runtime regression** — fixed inline in Phase 4 commit.
- **DEFER (C2) — `as unknown as typeof FlashList` double-cast loses Reanimated v3 Animated-specific props in TypeScript's view** (`entering`/`exiting`/`layout`). The library ships its own `AnimatedFlashList` from `@shopify/flash-list` which is also typed at `<any>` for items. Acceptable for the immediate type-error fix, but if either screen ever needs Reanimated layout animation props on the FlashList, TypeScript will silently accept misuse via the cast. **Mitigation:** in a future cleanup, consider importing `AnimatedFlashList` directly from `@shopify/flash-list` (equivalent typing but no double-wrap) and tracking whether layout animation props are needed on either list.

**`--legacy-peer-deps` analysis:** Required because of pre-existing `react-dom@19.2.5` vs `react@19.1.0` peer conflict (NOT introduced by this WF3). Code-reviewer verified that the install isolated the new sync-persister's transitive `@tanstack/query-core@5.100.9` + `@tanstack/query-persist-client-core@5.100.9` under nested `node_modules/`, leaving the prod TanStack stack at `5.100.6`. No prod dep version drift.

**Test count delta:** 329/333 still passes (same as pre-H4). No new tests added — the `tsc --noEmit | grep -cE "error TS" → 0` is the regression contract for this finding.


## WF3 — Spec 99 H3 Router Decision Telemetry 2026-05-05 (RESOLVED)

**Source:** WF5 audit `docs/reports/audit_spec99_2026-05-04.md` finding **HIGH H3** — zero `track('route_decision', ...)` calls anywhere in `mobile/`; 3 of 4 mandated production-only events missing per Spec 99 §7.3 (only `signOut → /(auth)/sign-in` partially covered via `track('signout_initiated')` at `authStore.ts:131`).

**Resolution:** Wired DEV-only `route_decision` event at every `router.replace` from AuthGate and AppLayout (2 sites total), plus the 3 missing production-only events. The existing `signout_initiated` event covers production event #1 unchanged — renaming would have broken existing PostHog dashboards. Touched 4 source files + 1 new test file:

- `mobile/src/lib/analytics.ts` — extended `ALLOWED_KEYS` whitelist + `AllowedKey` type union with 8 new keys (`authority`, `branch`, `from`, `to`, `reason`, `days_remaining`, `prev`, `next`). The whitelist extension is a Spec 90 §11 contract change subjected to PII review (see code-reviewer findings below) — all 8 keys verified to carry only operational metadata (route paths, enum values, day counters), not PII.
- `mobile/app/_layout.tsx` (AuthGate) — `case 'navigate':` gains a DEV-guarded `route_decision` event before `router.replace(decision.to)`; `case 'reactivation-modal':` gains a production `reactivation_modal_shown` event after `setReactivationState(...)` (compliance-critical — proves the user saw the 30-day deletion-window prompt).
- `mobile/app/(app)/_layout.tsx` (AppLayout) — deletion useEffect gains production `cancelled_pending_deletion_signout` + DEV-guarded `route_decision`; post-payment transition useEffect gains production `subscription_expired_to_active` inside the `if (prev === 'expired' && next === 'active')` block.
- `mobile/__tests__/routerTelemetry.test.ts` — NEW source-grep test file (sibling pattern of `subscriptionGate.test.ts`). 8 source-invariant `it()` cases: 2 whitelist+type-union assertions, 5 site assertions with DEV/production distinction, 1 regression guard for `signout_initiated`.

**Why source-grep, not behavioural render:** Same rationale as H2 — `_layout.tsx` orchestrates 6 third-party libraries impractical to render under jest-node. Source invariants give regression coverage equivalent to the spec mandate's intent at lower scope cost.

**Red Light evidence:** 7 of 8 new tests failed at HEAD before the fix; the 1 passing test was the `signout_initiated` regression guard (by design — that event already exists in `authStore.ts:131`). After the fix all 8 pass. Note: Test 7 (`subscription_expired_to_active` site) initially failed even after the fix because the original regex `[\s\S]*?\}` matched the inner `{ queryKey: ['leads'] }` closing brace before reaching the if-block's own `}`. Fixed by anchoring on `prevStatusRef.current = next;` (the next statement at outer indent level) — same regex-brittleness lesson as H2's `[^{]+` fix, applied via a different anchoring strategy here.

**Code-reviewer findings — both fixed inline (zero deferred):**

- **DEFER class — `__DEV__` guard regex false-pass risk in Tests 3 and 5.** Original pattern `if\s*\(__DEV__\)\s*\{[\s\S]*?track\(['"]route_decision['"][\s\S]*?\}` would false-pass if a contributor moved the track call OUTSIDE the guard with the props-object's `}` landing between them; the lazy `[\s\S]*?\}` matched the props-close, not the if-block close. Fixed by replacing with explicit brace counting via a new `assertDevGuardedTrack(fragment, eventName)` helper: scans from `if (__DEV__) {` opening to the track call, counts `{` vs `}` characters, asserts the delta is ≥ 1 (proving the call is inside an unclosed brace block).
- **DEFER class — `extractSwitchCase` terminator fired on inner `}\s*$` lines.** Original terminator `case '...'|default:|\}\s*$` with the `m` flag matched any line ending in `}`, including the navigate case's inner `if (__DEV__) {}` block close, silently truncating the extracted case body. Fixed by dropping the `\}\s*$` alternative; over-capture past the switch close is harmless because the regexes consuming the captured fragment are anchored on case-specific tokens.

**PII whitelist verification (Spec 90 §11):** Code-reviewer PASS — all 8 new keys carry only operational metadata. `from` uses `segments.join('/')` which Expo Router's `useSegments()` returns as route GROUP names (`(app)`, `[lead]` — file tokens, not resolved query parameters). `to` is hard-coded path literals. `days_remaining`, `prev`, `next` are integers/enum values. The `stripPii` whitelist is the correct defense-in-depth layer.

**Idempotency under refetch storms:** Code-reviewer PASS — `cancelled_pending_deletion_signout` is gated by `deletedHandledRef.current = true` (synchronous before async signOut). `subscription_expired_to_active` is gated by `prev === 'expired' && next === 'active'`; after the event fires `prevStatusRef.current = next` makes `prev = 'active'`, so a re-fire on stable status fails the `prev === 'expired'` check. Cold-boot false-positive avoided because `prevStatusRef` initialises undefined (first useEffect fire skips the branch).

**Hermes constant-folding:** Code-reviewer PASS — Metro bundler substitutes `__DEV__ → false` at the JS transform layer for production builds before Hermes receives the source; Hermes dead-code-eliminates the `if (false) {...}` block during bytecode compilation. Same mechanism as the existing `stateDebug` hub (Spec 99 §9.5). `route_decision` events carry zero overhead in production.

**Pattern A cross-reference (NOT in this WF3's scope):** Same cross-link as H2 — the audit's Phase 5 routes the H2+H3 class to a future §9.21 generic mandates-lint test (`mobile/__tests__/spec99.mandates.lint.test.ts`). Future WF1.


## WF3 — Spec 99 H2 Gate-Stability Tests 2026-05-05 (RESOLVED)

**Source:** WF5 audit `docs/reports/audit_spec99_2026-05-04.md` finding **HIGH H2** — zero tests across `mobile/__tests__/*.test.ts` reference `isFetching`. Spec 99 §8.3 mandates each render gate condition (per §6.5) MUST have a test asserting `isFetching` toggle does NOT flip the gate. Coupled with the now-resolved H1: the narrow carve-out at `(app)/_layout.tsx:220` had no regression guard AND the broad-stripping had no enforcement.

**Resolution:** Test-only addition to `mobile/__tests__/subscriptionGate.test.ts`. New `describe('subscription gate — §6.5 gate stability (WF5 H2 / §8.3)')` block with 4 source-invariant `it()` cases — one per render gate in `(app)/_layout.tsx`: broad loading gate (line 206), §6.5 amendment carve-out (line 220), `cancelled_pending_deletion` gate (line 226), paywall gate (line 233). Each test asserts `isFetching` is absent from the gate's condition (or, for the carve-out, that it is the SOLE permitted occurrence AND pairs with `=== 'expired'`). Zero production source files modified. Mutation-test evidence: prepending `isFetching ||` to the broad gate caused Test 1 to fail with the expected diagnostic; prepending `Boolean(true) && isFetching &&` to the deletion gate caused Test 2 to fail showing both gates in the `gateConditionMatches` array. Both reverted. SPEC LINK header extended to cite §8.3.

**Why source-grep, not behavioural render:** The file's preamble (lines 5-13) explicitly rules out behavioural rendering as impractical because `_layout.tsx` orchestrates 6 third-party libraries (expo-router, TanStack Query, Reanimated, BottomTabBar, AppState, IncompleteBanner). Source invariants give regression coverage equivalent to the spec's literal "render twice" intent at lower scope cost; the audit's H2 routing column ("WF3 — add `subscriptionGate.test.ts` cases for the carve-out + the broad gate") matches this approach.

**Code-reviewer findings — both fixed inline (zero deferred):**

- **BUG — Test 2 false-negative escape via parenthesized `isFetching`.** Original regex `/if \([^)]*isFetching[^)]*\)/g` would miss `if (foo() && isFetching)` because the inner `)` after `foo(` blocks `[^)]*` from spanning. A second `isFetching` gate hidden behind any inner `()` would evade the `.toHaveLength(1)` count. Fixed by switching the body delimiter from `[^)]*` to `[^{]*` (the `{` opening the if-block is the unambiguous terminator) and applying the same change uniformly across Tests 1/3/4 to prevent the equivalent silent false-pass on capture-group brittleness. Mutation test confirmed the hardened regex catches the parenthesized case.
- **DEFER class — Tests 1/3/4 same `[^)]+` brittleness.** Resolved inline by the same uniform `[^{]+` capture-window fix; not deferred.

**Pattern A cross-reference (NOT in this WF3's scope):** The audit's Phase 5 routes the H2+H3 class ("Spec mandate exists but no enforcement test") to a future §9.21 — `mobile/__tests__/spec99.mandates.lint.test.ts` that statically grep-asserts every spec mandate has implementation evidence. That's a class-level fix covering H2 + H3 + future mandate drift; file as future WF1 when prioritised.


## WF3 — Spec 99 H1 Amendment 2026-05-05 (RESOLVED)

**Source:** WF5 audit `docs/reports/audit_spec99_2026-05-04.md` finding **HIGH H1** — `mobile/app/(app)/_layout.tsx:216` retains an `isFetching && subscription_status === 'expired'` carve-out that Spec 99 §6.5's categorical text did not permit.

**Resolution:** Spec 99 §6.5 amended (2026-05-05) to enumerate the AppLayout case as a permitted narrow carve-out under three conditions: stable status field; same loading element as parent gate; explicit enumeration. The categorical rule is preserved for the broad / unscoped case. No executable code change — only the comment block in `_layout.tsx:200-218` updated to cite the new amendment subsection. Regression guard: `mobile/__tests__/subscriptionGate.test.ts` asserts the amendment header + AppLayout enumeration both exist (catches accidental amendment removal).

**Why amend not refactor:** The alternative `useMutation`-backed `postPaymentRefetchInFlight` flag would narrow the anti-flicker protection to AppState→active-initiated refetches only; the existing carve-out covers ALL refetch sources (including `refetchOnReconnect`) for the same UX cost. The audit explicitly noted the carve-out is "provably correct... well-commented" (audit line 114).

**Deferred test-hardening items** (from independent code-reviewer agent, both DEFER severity, zero BUG):

- **D1 — Tighten the amendment-block extraction window in `subscriptionGate.test.ts`.** Current regex `Permitted (narrow )?carve-outs[\s\S]{0,2000}` captures a 2000-char slice. The actual amendment block is ~1200 chars, so the extra 800 chars overlap the "Current violations being remediated" paragraph and the §6.6 heading. No live false-pass today (the three search terms `_layout.tsx` / `subscription_status` / `'expired'` appear only inside the amendment), but a future move of the AppLayout enumeration into the violations paragraph (within the window) would slip through. Mitigation: tighten window to ~1400 OR replace regex slice with a section-bounded extraction (substring from `Permitted carve-outs` up to next `###` heading).

- **D2 — Enforce enumeration exhaustiveness with a paired lint test.** Amendment condition 3 ("enumerated in the SAME commit") has no automated check — relies on reviewer vigilance. A second future `isFetching && profile.subscription_status === 'past_due'` carve-out added without amending the spec would not be caught by the current test (which only asserts the `expired` case is enumerated, not that the enumeration is exhaustive). Mitigation: add a companion test that greps `_layout.tsx` for all `isFetching && profile.subscription_status` occurrences and asserts the count matches the number of enumerated items in §6.5's "Permitted carve-outs" block.


## WF3 Top-6 Deferred Bug Sweep — 2026-05-04 (RESOLVED)

7 commits closed 9 CRITICAL + 8 HIGH from the queue below. Phases 1–6 attacked one bundle each (smallest-blast-radius first); Phase 7 applied amendments from the adversarial trio (Gemini + DeepSeek + code-reviewer) which surfaced 3 additional CRITICAL + 4 additional HIGH that were also fixed inline. Phase 4 was a no-op — the original deferred item (compute-trade-forecasts atomicity) was already pre-fixed by commit "WF3 B2-C1 (2026-04-23)" and locked by the existing test at `compute-trade-forecasts.infra.test.ts:429`.

| Commit | Bundle | Items closed |
|---|---|---|
| `d609b9b` | Phase 1 — Auth hardening (`src/lib/auth/get-user.ts`) | CRIT × 2 (timing-attack on cookie compare; verifyIdToken revocation check) + HIGH × 2 (silent-401-storm in prod; dev-mode dual-gate) + MED × 1 (8 KB token length DoS) |
| `08ff823` | Phase 2 — `/api/user-profile` route hardening | CRIT × 2 (`SELECT *` leakage; trade_slug first-write race) + HIGH × 1 (trade_slug validation bypass) + MED × 1 (Cache-Control on GET-with-side-effects, free) |
| `6b518ae` | Phase 3 — push-dispatch hardening (`scripts/classify-lifecycle-phase.js`) | CRIT × 2 (N+1 query; `DISTINCT ON` without `ORDER BY`) + HIGH × 1 (`callExpoPushApi` ignores statusCode) |
| (Phase 4 — no-op, pre-fixed) | compute-trade-forecasts atomicity (review_followups.md:378) | CRIT × 1 verified resolved by 2026-04-23 commit |
| `857bf51` | Phase 5 — Mobile MMKV PII (`mobile/src/lib/userProfile.schema.ts`) | CRIT × 1 (`stripe_customer_id` → MMKV violated Spec 99 §2.1) + HIGH × 3 (`.datetime()` on timestamps, coord bounds, `lead_views_count` nullable default) |
| `fefc2a3` | Phase 6 — Builder pagination (`src/features/leads/lib/get-lead-feed.ts`) | CRIT × 1 (lexicographic cursor sort skipped builders 10..89 after page 1 ended at builder 9) |
| `0fa1314` | Phase 7 — Adversarial trio amendments | CRIT × 3 (mobile vs server contract break, partial-PATCH data loss, lead_views_count `.default(0)` doesn't fire on null) + HIGH × 4 (string-match re-throw → custom error class, tos_accepted_at mutability, non-trade UPDATE 0-row check, unnest NULL-revision filter) |

**Items deliberately NOT selected** (deferred to standalone WFs — already documented in the active task plan):
- Sentry @~7.2.0 broken on RN 0.81 New Architecture (CRIT) — version bump risk; standalone
- `lifecycle-phase.js` `classifyBldLed` returns P18 when `has_passed_inspection=true` (CRIT) — needs spec author input
- `lifecycle-phase.js` TRADE_TARGET_PHASE / stall thresholds hardcoded (2× CRIT) — DB migration scope
- `pipeline.js:465` `checkQueueAge` SQL injection vector (CRIT) — library-level fix
- Backup-email persistence bridge missing in `sign-up.tsx` (CRIT) — cross-feature
- ✅ Auth-state reset on forced sign-out PROMOTED CRIT — **RESOLVED** by `381a0c9` + `f2f7147` (see Items deferred from the Phase 7 adversarial review below for full detail)
- Lifecycle "Unstall cliff" CRIT — Phase 2 classifier upgrade

**Items deferred from the Phase 7 adversarial review** (status):
- ✅ **RESOLVED** — Phase 6 cursor wire-format break for in-flight clients (Gemini CRIT) — fixed by `3fa96a1` (server-side `CASE WHEN $7='builder' THEN LPAD($8, 20, '0') ELSE $8 END` accepts both pre-deploy bare-int and post-deploy padded cursors)
- ✅ **RESOLVED** — Phase 5 broader PII scope: `full_name` / `phone_number` / `company_name` / `email` / `backup_email` persisted to unencrypted MMKV (Gemini CRIT) — fixed by `671aa87` (`shouldDehydrateQuery` filter excludes `['user-profile']`) + `202a9aa` (`buster` bump clears stale pre-WF3 blobs on first cold boot, `mmkvPersister.removeClient()` in signOut purges disk state). Full encryption-at-rest (`encryptionKey` on `createMMKV`) remains a future hardening for any new PII-adjacent queries — not blocking.
- ✅ **RESOLVED** — PROMOTED CRITICAL "Auth-state reset placement leaks data on forced sign-out" (the §9.14 Phase D PROMOTED item; same item as the Weekly Triage 2026-05-03 entry below) — fixed by `381a0c9` (extracted `clearLocalSessionState()` helper in `authStore.ts`, invoked from BOTH `signOut()`'s finally block AND the `onAuthStateChanged(null)` listener branch when `lastKnownUid !== null`; cold-boot first-fire keeps the original `clearAuth()` to avoid thrashing the persisted TanStack cache on every app launch by an unauthenticated user) + `f2f7147` (code-reviewer Phase 3 amendments — `__resetLastKnownUidForTests()` test-only export prevents Jest module-state contamination across tests; new cold-boot test pins the `clearAuth()` branch behavior with negative mock-call assertions). Telemetry: new `track('forced_signout')` event distinguishes server-initiated from user-initiated signouts in PostHog.
- Phase 1 timing-safe length leak on `timingSafeStringEqual` (Gemini + DeepSeek HIGH) — DEV cookie length is a hardcoded constant; low-risk in practice
- Phase 1 catch-all narrowing to Firebase codes only (Gemini HIGH) — scope creep
- Phase 2 location_mode coord-clearing inconsistency 500-vs-400 (Gemini HIGH) — pre-existing
- Phase 2 409 reconciliation SELECT outside transaction (DeepSeek + code-reviewer HIGH) — cosmetic; field is diagnostic
- Phase 2b lifecycle backfill N+1 (Gemini HIGH) — pre-existing one-time migration concern
- Expo Push API positive-shape assertion (DeepSeek HIGH) — defensive; nice-to-have

**WF3 cursor + PII follow-up commits (3 new):**
| Commit | Scope |
|---|---|
| `3fa96a1` | Cursor backward-compat — `CASE WHEN $7='builder' THEN LPAD($8, 20, '0') ELSE $8 END` |
| `671aa87` | PII MMKV strip — `shouldDehydrateQuery` filter on user-profile + Spec 99 §2.1 amendment |
| `202a9aa` | Code-reviewer amendments — `buster: 'wf3-pii-strip-1'` clears stale blobs at deploy moment, `mmkvPersister.removeClient()` in signOut, predicate extracted to `persistFilter.ts` for behavioral test import |

**WF3 forced-signout + dead-code sweep commits (3 new):**
| Commit | Scope |
|---|---|
| `381a0c9` | Forced-signout cleanup unification — extract `clearLocalSessionState()` helper, invoke from BOTH `signOut()` finally AND listener null-branch (guarded by `lastKnownUid !== null`); add `track('forced_signout')` + Sentry breadcrumb |
| `7bcb681` | Dead-code sweep (conservative-keep posture) — only 1 of 9 knip-flagged items removed: `export` dropped from `CLIENT_SAFE_COLUMNS` in `src/lib/userProfile.schema.ts` (only the joined string `CLIENT_SAFE_SELECT_LIST` is consumed externally; verified zero importers via grep). KEPT: `UserProfileSchema` + `UserProfileType` + `UserProfileUpdateType` (canonical contract types — even if no current consumer, removal would silently drop the contract authority); persist `migrate` functions in `userProfileStore` v0→v1 + `onboardingStore` v0→v2 (existing-user upgrade path); `cleanupLegacyUserProfileCache` (one-time MMKV migration for orphaned legacy blob); `INSPECTION_PIPELINE_P18_SET` + `TRADE_TARGET_PHASE_FALLBACK` (consumed by `scripts/lib/lifecycle-phase.js` — knip false positive); §3.5 deprecated mirrors entries (audit-trail, not dead code). |
| `f2f7147` | Code-reviewer Phase 3 amendments — closed 2 HIGH test-reliability gaps (`__resetLastKnownUidForTests()` test-only export prevents Jest module-state contamination across tests; new "cold-boot null-fire" test pins the `clearAuth()` branch with negative mock-call assertions on `mmkvPersister.removeClient` / `queryClient.clear` / `resetIdentity` / `track('forced_signout')`). Code-reviewer returned 0 CRITICAL on production logic. |

---

## Weekly Triage 2026-05-03

_Sample triage — pre-routine validation pass (does not cover every item; full weekly run will sweep the whole queue)._

**Items marked STALE (4):** all resolved by recent Spec 99 mobile-state work.
- _§ "WF1 — Spec 93 Mobile Authentication"_ MED — `paywallStore.clear()` / `userProfileStore.reset()` missing from `signOut()`. **Reason:** `authStore.ts:114-117` now resets all 5 peer stores; covered by `mobile/__tests__/storeReset.coverage.test.ts` (Spec 99 §B5).
- _§ "WF1 Spec 95 Mobile User Profiles"_ LOW — signOut does not clear `user-profile-cache` MMKV key. **Reason:** Spec 99 §9.1 deleted the entire MMKV blob; only the one-time cleanup migration survives.
- _§ "WF3 — AuthGate Server-Driven Routing"_ LOW — `useUserProfile.ts markComplete()` bridge BLOCKED. **Reason:** Spec 99 §9.2a/b/c removed both the bridge and the `isComplete` field; `IncompleteBanner.tsx:42` reads server `profile.onboarding_complete` directly.
- _§ "WF5 Spec 96 — 6-Reviewer Adversarial Sweep"_ MED — `IncompleteBanner` rendered unconditionally. **Reason:** component is still always-mounted but self-hides via `profile?.onboarding_complete === true` early-return; symptom resolved.

**Items PROMOTED (1):** Reactive trigger has fired — newly-actionable.
- ✅ **RESOLVED** — _§ "WF2 Spec 93 RNFirebase migration — Round 2"_ CRITICAL — Forced sign-out leaks peer-store data. **Trigger:** Spec 99 §9.12 storeReset coverage test landed but covers only the explicit `signOut()` path; the `onAuthStateChanged(null)` listener branch at `authStore.ts:242` calls `clearAuth()` only (line 79: clears `user/idToken/isLoading`), skipping the peer-store resets at lines 114-117. **Fix:** `381a0c9` extracted `clearLocalSessionState()` helper invoked from BOTH the explicit `signOut()` finally AND the listener's null branch (guarded by `lastKnownUid !== null` to skip cold-boot first-fire so an unauthenticated user's app launch doesn't thrash the persisted TanStack cache). Listener path also fires `track('forced_signout')` + Sentry breadcrumb for product-analytics distinction. `f2f7147` closed code-reviewer Phase 3 HIGHs on test reliability (`__resetLastKnownUidForTests()` + cold-boot path test).

**Recommended next 3 LOW items to tackle:**
1. _§ "WF1 Spec 96 Subscription"_ LOW — `subscribe/session` race window (SELECT then INSERT). One-line fix: wrap in `withTransaction` or `SELECT ... FOR UPDATE`. Matches the existing webhook handler pattern.
2. _§ "WF3 Set 2 — Mobile Hydration & Network Hardening"_ LOW — `apiClient.ts` concurrent 401 mutex. ~10-line addition of a module-level `refreshPromise` mutex; eliminates a known race that's currently masked by Firebase token-fetch dedup.
3. _§ "WF1 Two-Client 2026-04-22"_ LOW — Dead code sweep (`haptics.ts`, `haversine.ts`, `useLeadView.ts`, `OnboardingWizard.tsx`). `npm run dead-code` will surface them; deletion is mechanical and frees ~5 modules.

**Counts (sampled subset; full sweep on routine activation):** 4 STALE applied · 1 PROMOTED · 3 actionable LOW recommendations.

---

## WF2 Task 3 — Retroactive annotation sweep + validate-migration.js marker fixes (commit: TBD, 2026-04-24)

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| LOW | Both / Defensible | **Escaped single-quote `''` not handled in string/comment parsers** — `SELECT 'it''s a trap'; -- DROP TABLE` causes quote counting desync in `stripLineComments` and `blankStringLiterals`. Known limitation; schema migrations virtually never contain escaped quotes in string values. | Known limitation — document in script header if/when reported |
| LOW | Gemini / Defensible | **Nested block comments not handled** — `/* /* nested */ */` pattern. PostgreSQL supports these; rare in migrations. Current `stripBlockComments` regex would terminate early at first `*/`. | Future hardening (regex replacement with stack-based parser) |
| LOW | Gemini / Defensible | **Dollar-quoted strings not handled** — `$$..$$` with commas or `--` inside. Same family as escaped-quote issue; rare in schema migrations. | Known limitation |
| LOW | Gemini MEDIUM / Defensible | **Multiple ALTER TABLE without semicolons — greedy regex body** — If two `ALTER TABLE` statements exist without a terminating `;`, the `alterRe` body match could consume both. Pre-existing; real migrations always terminate ALTER TABLE with a semicolon. | Reactive — only if false positives reported |
| LOW | Independent / Design | **`concurrentlyExempt` reads full content, not UP-block-only** — A `-- CONCURRENTLY-EXEMPT` placed exclusively in the DOWN block would suppress Rule 2 for the entire file. Accepted tradeoff: Rule 2 is not a security gate; file-scope annotation is appropriate for a file-level exemption. | Documented design decision; no planned fix |
| LOW | Gemini / Out-of-scope | **Quoted identifiers with special chars in table name regex** — `"my-table"` not matched by `[\w]+`. Same as Task 2 deferral. | Future hardening |
| LOW | DeepSeek / Out-of-scope | **Double-quoted identifiers in `splitTopLevelCommas`** — commas inside `"col,name"` treated as clause separators. Extremely rare in migration column names. | Future hardening |
| LOW | Independent G1 / Cosmetic | **Stale `-- ALLOW-DESTRUCTIVE` in 069_lead_views.sql DOWN block** — The marker on the DOWN block's commented-out DROP has no functional effect (allowDestructive reads from upBlockContent). Confusing but harmless. | Low-priority cleanup |

## WF2 B1+B2+A4 — validate-migration.js hardening (commit: TBD, 2026-04-24)

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| LOW | Gemini CRITICAL / Defensible | **ALLOW-DESTRUCTIVE marker checkable via string literal** — placing `SELECT '-- ALLOW-DESTRUCTIVE'` in a migration sets the bypass flag. Defensible: requires adversarial dev; visible in review; validator is a hook aid, not a security gate against malicious insiders. Fixed the realistic case (DOWN-block scope); the string-literal bypass remains a theoretical gap. | If RELATIONSHIPS source changes |
| MED | Gemini+Independent / Out-of-scope | **B2 does not handle table-level PRIMARY KEY constraint** — `(source_id INTEGER NOT NULL, ..., PRIMARY KEY(source_id))` pattern (PK as separate clause) still triggers the Rule 5 integer-ID warning. Mitigation: `-- FK-EXEMPT`. A proper fix requires scanning the full table body for PK constraint references to each candidate column. | Future hardening WF on validate-migration |
| LOW | Gemini / Out-of-scope | **Table name regex does not support quoted identifiers with special chars** — `"my-table"` with hyphens would not be parsed by `[\w]+`. Affects Rule 2 table-name matching and Rule 5 extractCreateTableBlocks. Uncommon in this codebase. | Future hardening |
| LOW | DeepSeek+Gemini / Defensible | **Escaped single-quote (`''`) not handled in stripLineComments / blankStringLiterals** — SQL escaped-quote sequences could cause quote-counting to desync. Rare in schema migrations; existing Known Limitation comment documents multi-line string limits. | Known limitation |
| LOW | DeepSeek / Defensible | **Dollar-quoted strings not handled** — Same family as escaped-quote issue. Rare in schema migrations. | Known limitation |
| LOW | DeepSeek / Out-of-scope | **stripBlockComments called before blankStringLiterals** — a `/*` inside a string literal could corrupt the block-comment stripper. Would require reordering the pipeline. Pre-existing; no real migration in the corpus uses this pattern. | Future hardening |
| LOW | Both / Out-of-scope | **lineOf() O(N) per violation** — pre-compute newline index for O(log N) lookup. Pre-existing; acceptable for pre-commit hook scale. | Reactive — only if hook latency becomes an issue |
| LOW | Both / Out-of-scope | **LARGE_TABLES hardcoded** — maintenance burden. Proposal: externalize to config or package.json property. | Future hardening |
| LOW | Independent / Out-of-scope | **`parcels` potentially absent from LARGE_TABLES** — Toronto has ~440K registered parcels; if table grows to full city scope it exceeds 100K threshold. Verify actual row count before adding. | Verify row count, then file WF2 if confirmed |
| LOW | Independent / Informational | **Rule 2 and Rule 3 not scoped to UP-only** — asymmetry with B1's Rule 1 fix. A DOWN block `CREATE INDEX` on a large table would still trigger Rule 2. Unlikely in real rollback SQL; informational only. | Future consideration |

## WF3 B3 — audit-fk-orphans stale entry fix (commit: TBD, 2026-04-24)

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| LOW | Gemini CRITICAL / Defensible | **`buildOrphanQuery` uses string interpolation for table/column names** — Gemini flagged this as SQL injection. Defensible: identifiers come exclusively from the static `RELATIONSHIPS` array (code-controlled, not user input); PostgreSQL cannot parameterize table/column identifiers via `$N`; the script is admin-only tooling. Not a real risk in current form. Would become real only if `RELATIONSHIPS` were loaded from an external/user-controlled source. | If RELATIONSHIPS source changes |
| LOW | Gemini / Defensible | **No PostgreSQL schema qualification on table names** — All queries use unqualified names (e.g., `FROM permits c`) relying on `search_path`. Defensible: single-schema application (`public`); `search_path` is stable and not configurable by end users. | Future hardening |
| LOW | Gemini / Defensible | **`res.rows[0]` assumption on COUNT(*) result** — Gemini flagged potential crash if rows is empty. Defensible: `COUNT(*)` always returns exactly one row in PostgreSQL regardless of data state. | Not a real risk |
| MED | Gemini / Out-of-scope | **Tier 1 entries not verified against `pg_constraint`** — Script assumes entries marked Tier 1 actually have enforced FK constraints in the DB. Adding `information_schema` pre-check would catch misconfigured entries. Not introduced by this fix; significant new feature. | Future hardening WF on audit-fk-orphans |
| LOW | Gemini / Out-of-scope | **`NOT EXISTS` subquery vs `LEFT JOIN ANTI-JOIN` pattern** — Gemini flagged potential performance issue on large tables. Pre-existing pattern; not introduced by this fix; query plan on indexed FK columns is typically efficient. | Reactive — only if audit run time becomes an issue |
| LOW | DeepSeek / Defensible | **`NOT VALID` constraints produce false Tier 1 confidence** — DeepSeek raised concern that a `NOT VALID` constraint allows existing orphans. Defensible: audit-fk-orphans measures orphan data counts, not constraint existence or validation state. The split-lock `NOT VALID + VALIDATE` pattern is the intended migration strategy per spec 47 §18. | Not applicable to this script's purpose |
| LOW | DeepSeek / Defensible | **nullable:false may overcount orphans if column is actually nullable in DB** — Pre-existing design decision; `RELATIONSHIPS` config correctness is the operator's responsibility. | Documented design constraint |
| LOW | Gemini+DeepSeek / Out-of-scope | **`parseInt(a.Tier)` sort / `r.Tier.startsWith('2')` fragility** — Both reviewers flagged that sort and summary logic couples to display string format. Pre-existing pattern not introduced by this fix. | Future hardening — store raw tier number in results object |
| LOW | Independent / Out-of-scope | **Tier 2 dead code and "All Tier 2 clean" always-printing message** — After migration 109, no RELATIONSHIPS entries have `tier: 2`. The tier 2 summary branch at line 375+ always prints "clean" with nothing to report. Pre-existing; cosmetic. | Low-priority cleanup WF |
| LOW | Independent / Out-of-scope | **`emitSummary` missing `audit_table`** — Admin UI shows "UNKNOWN" verdict for this script. Pre-existing pipeline observability gap. | Future observability WF tied to spec 48 |

## 1. Systemic Pipeline Patterns
These systemic issues appeared multiple times across various scripts and have been consolidated into overarching tech-debt epics.

| Severity | Pattern Name | Summary Description |
|----------|--------------|---------------------|
| HIGH | Midnight Cross (NOW() vs RUN_AT) | Multiple scripts use inline NOW() or CURRENT_DATE instead of the db-sourced RUN_AT variable. Violates Midnight Cross protection. Affects: link-coa.js, assert-global-coverage.js, compute-trade-forecasts.js, close-stale-permits.js. |
| MED | Pipeline SDK Emits (audit_table/emitMeta) | Missing or empty emitMeta / audit_table payloads on early exits (e.g., link-coa.js tier skips) and incorrect counter tracking (violates §11.1). |
| MED | Advisory Lock Boilerplate Drift | Missing lockClientReleased handlers on SIGTERM, incorrect lock ID mapping, or missing lock error pass-through (affects run-chain.js, flight tracker, etc). |
| MED | File-shape Tests vs Behavioral Cover | Across almost all rules and pipeline engines, we rely heavily on file-regex matching rather than DB-level behavioral testing. Blocking full safety. |
| HIGH | Hardcoded Constants vs logic_variables | Several scripts (e.g. Cost Model version, TRACE_WINDOW in CQA, Stall Thresholds) are hardcoding variables that should be driven by the Control Panel. |


## 2. Distinct Active Work Items
Filtered to exclusively active, un-rejected items. Organized by severity.

| Severity | Source | Item | Planned Home |
|----------|--------|------|--------------|
| MED | Gemini+DeepSeek | `cost-model.ts` string-based permit categorization via `.toLowerCase().includes(...)` is brittle against misspellings or unusual permit types. Matches the existing codebase pattern in `classifier.ts`. Extract to a shared categorization function in a future hardening WF. | Future hardening WF |
| MED | Gemini+DeepSeek | Cliff effect at `tenure_renter_pct = 50%` boundary (0.4 → 0.7 coverage). A 0.2% change in rent-pct flips a 75% cost difference. Spec-compliant but brittle. Smooth to interpolation in a future refinement. | V2 cost model refinement |
| MED | DeepSeek | `compute-cost-estimates.js` uses single-row INSERTs inside the transaction (5000 sequential queries per batch). Bulk `INSERT ... VALUES` or `UNNEST` would be 50-100x faster. Fine for correctness; optimize later if nightly run time becomes an issue. | Phase 2+ perf optimization |
| LOW | Gemini+DeepSeek | `lat`/`lng`/score fields in `types.ts` are unconstrained `number`. Validation happens at Phase 2's `/api/leads/feed` Zod layer per spec 70. | Phase 2 API route |
| LOW | Gemini | `model_version` hardcoded to 1. Will be incremented manually when formula changes; current = initial version. | On next formula change |
| LOW | DeepSeek | Duplicate scope tags (e.g., `['pool', 'pool']`) would stack the addition twice. Extremely unlikely in real data; defer to source data hygiene. | Reactive |
| LOW | Independent (retry pending) | Independent review agent hit 529 API overload twice on commit `a460904`. Dual code path was verified manually inline via constant-by-constant audit + 3 sample-input walkthroughs (documented in commit message and triage response). Full self-generated checklist deferred to next agent availability. | Re-run on next sub-WF or standalone |
| MED | Independent | `db:generate` deferred during Phase 1a because local `npm run migrate` fails at pre-existing migration 030 (`column "updated_at" of relation "trade_mapping_rules" does not exist`). Drizzle types may drift from the new lead_views/cost_estimates/inspection_stage_map/timing_calibration tables. | Phase 1b (run after CI clean DB OR repair local migration 030) |
| LOW | Independent | No DB-roundtrip integration tests for the new CHECK constraints, FK CASCADEs, or XOR logic on lead_views. File-shape regex tests verify SQL structure but cannot catch a runtime constraint failure. Only safe to rely on once CI runs against a clean DB. | Phase 1b CI step |
| LOW | Independent | No idx on `(trade_slug, precedence)` in inspection_stage_map. Fine for 21 seed rows; would matter if seed grows to thousands. | Reactive — only if seed grows |
| LOW | Gemini | `src/lib/permits/types.ts` `PermitChange.old_value`/`new_value` typed as `string \ | null` — loses type fidelity for numeric/date/boolean fields. Pre-existing, not touched by Phase 1a. |
| LOW | Gemini | `src/lib/permits/types.ts` `PermitFilter.sort_by` typed as `string` — should be `keyof Permit` for compile-time safety + SQL injection defense. Pre-existing. | Future types-hardening WF |
| LOW | Gemini | `src/lib/permits/types.ts` `Inspection.inspection_date` and `scraped_at` typed as `string` — inconsistent with `Permit` interface using `Date`. Pre-existing. | Future types-hardening WF |
| LOW | Gemini | `src/lib/permits/types.ts` `Permit.dwelling_units_created`, `dwelling_units_lost`, `housing_units`, `storeys` typed as `number` (non-nullable) but ingested from raw strings — no protection against `""` / `"N/A"` producing `NaN`. Pre-existing. | Future types-hardening WF |
| LOW | Gemini | `src/lib/permits/types.ts` `TradeMappingRule.match_field` typed as `string` — should be `keyof RawPermitRecord` to prevent typos. Pre-existing. | Future types-hardening WF |
| LOW | Gemini | `src/lib/permits/types.ts` `Permit.location` typed as `unknown \ | null` — no GeoJSON shape definition. Pre-existing. |
| LOW | Gemini | `src/lib/permits/types.ts` `SyncRun.status` typed as generic `string` — should be a literal union. Pre-existing. | Future types-hardening WF |
| LOW | Gemini | `src/lib/permits/types.ts` `Entity.is_wsib_registered: boolean` cannot represent the "unknown / not yet checked" state — should be `boolean \ | null`. Pre-existing. |
| MED | Stryker baseline 2026-04-08 | `builder-query.ts` mutation score 50.00% — 11 surviving mutants. The SQL CASE expressions in the file are mostly tested via regex shape, not via behavior, so Stryker can flip thresholds (e.g., `>= 5 → > 5`) with no test failure. Add behavioral tests that exercise the SQL with mocked rows at each band boundary. | Targeted test additions |
| LOW | Stryker baseline 2026-04-08 | `record-lead-view.ts` mutation score 91.84% — 4 surviving mutants. Inspect after the cost-model + builder-query gaps are closed. | Reactive |
| LOW | Stryker WF3 2026-04-08 | `builder-query.ts` remains at 63.64% after the boundary test additions. The 8 remaining survivors are StringLiteral mutants on the multi-line SQL template (Stryker treats the whole SQL as one literal, so mutating interior text like `'5'` inside the template isn't trivially killed by JS-side tests). Future fix: extract the band threshold constants out of the SQL template into a JS object and interpolate, then test the constants directly. | Future hardening |
| LOW | WF3 2026-04-08 Pre-Review Self-Checklist | `scripts/compute-cost-estimates.js` (the JS sibling of `cost-model.ts` per ADR 001 dual code path) has NO matching JS-side boundary tests. The TS tests added in this WF lock the spec 72 constants via `contracts.infra.test.ts` and the JS file re-declares the same constants. Drift would still be caught by the shared constants contract, but a JS-side boundary test file (`compute-cost-estimates.infra.test.ts` additions) would close the gap completely. | Future hardening WF |
| MED | WF3 2026-04-10 Self-checklist | `/api/admin/stats` fires 37 parallel `pool.query(...)` calls. Latent same-class pool-exhaustion risk as the `/api/admin/leads/health` bug fixed in this WF3. Queries are individually fast and have not triggered the timeout in production, but a future slowdown in any one of them could starve the pool. Either consolidate via `COUNT FILTER` or adopt the `/api/admin/leads/health` pattern. | Future hardening WF |
| MED | WF3 2026-04-10 Self-checklist | 7+ sibling admin API routes return canned opaque 500 error strings (`market-metrics`, `rules`, `stats`, `pipelines/history`, `sync`, `builders`, etc.). Same class as the bug that hid the pool exhaustion for 3+ commits. Apply the `sanitizePgErrorMessage` + NODE_ENV-gated pattern to every admin route as a sweep. | WF6 sweep |
| MED | WF3 2026-04-10 Adversarial (Bug #3) | `builders_feed_eligible` in `getLeadFeedReadiness` counts all globally-WSIB-eligible builders (via the wsib_per_entity CTE filters), but the actual feed additionally applies `ST_DWithin` against the querying user's coordinates. A dashboard showing "618 builders feed eligible" may overstate how many a user actually sees. Pre-existing from commit `8528f16`, not introduced by this fix. Field name is accurate per JSDoc ("intersection of wsib_per_entity CTE filters") but potentially misleading at the UI layer. Either rename to `builders_wsib_eligible` or add a prominent caveat. | Future UX/naming WF |
| LOW | WF3 2026-04-10 Adversarial | `src/lib/db/client.ts:26` — `parseInt(process.env.PG_PORT |  |
| LOW | WF3 Phase 1 Adversarial + Independent | `getCostCoverage` in `src/lib/admin/lead-feed-health.ts` initializes `coverage_pct_vs_active_permits: 0` as a placeholder that the route handler overrides. JSDoc explains the contract but nothing at the TypeScript level prevents a future caller from using `getCostCoverage(pool)` directly and silently consuming the 0. Type-safe fix: have `getCostCoverage` return `Omit<CostCoverage, 'coverage_pct_vs_active_permits'>` and only the route handler adds the field. | Future type-safety hardening |
| LOW | WF3 Phase 1 Independent | `permits_with_cost` in `getLeadFeedReadiness` counts cost_estimates with `estimated_cost IS NOT NULL` regardless of permit status, while `active_permits` filters to ADMIN_ACTIVE statuses. The derived `coverage_pct_vs_active_permits` can exceed 100% if cost_estimates lags permit cancellations. Phase 1 decision: display honestly (no cap) because >100% is an actionable signal that the cache has drifted. Future fix option: add a permit-status JOIN filter to the numerator query, scoped to ADMIN_ACTIVE. | Future query hardening |
| LOW | WF3 Phase 2 Adversarial | `getCachedLeadFeedHealth(pool, ttlMs?)` accepts a `pool` parameter but the cache is a module-level singleton keyed by nothing. If a future caller passes a different `Pool` instance within the TTL window, both see data fetched by whichever pool populated the cache first. Production has one shared pool so it's invisible today; a multi-pool setup would need a cache key derived from pool identity. JSDoc now documents the limitation. | Future multi-pool WF |
| LOW | WF3 Phase 2 Adversarial | `getCachedLeadFeedHealth` returns the cached `LeadFeedHealthResponse` by reference — no defensive clone. If any caller mutates the returned object, subsequent cache hits within the TTL window see the mutation. Current route handler passes it directly to `NextResponse.json()` without mutation; this is a latent trap, not a production bug. JSDoc now warns. Fix option: `structuredClone()` on cache read (cheap for this payload shape). | Future hardening |
| LOW | WF3 Phase 2 Independent | No cache-bypass mechanism in production. `getCachedLeadFeedHealth` has no `force: true` option — a future CLI health check or debug script wanting fresh data would need to call `getLeadFeedReadiness`/`getCostCoverage`/`getEngagement` directly. `__resetLeadFeedHealthCacheForTests` exists but is labelled test-only. | Future debug tooling WF |
| LOW | WF3 Phase 3 Adversarial | No DB-level CHECK constraint enforces the `(saved = false ⟺ saved_at IS NULL)` invariant on lead_views. Current enforcement is application-layer only (record-lead-view.ts CASE expression + recorder JSDoc). A future pipeline script writing `UPDATE lead_views SET saved = true WHERE ...` without populating saved_at would produce rows invisible to `saves_7d`/`saves_today` metrics. Fix option: `ALTER TABLE lead_views ADD CONSTRAINT lead_views_saved_at_invariant CHECK ((saved = false AND saved_at IS NULL) OR (saved = true AND saved_at IS NOT NULL))`. Deferred because there is currently exactly one writer (record-lead-view.ts) which enforces the invariant correctly, and a CHECK constraint would reject ad-hoc debug UPDATEs. | Future pipeline-hardening WF |
| LOW | WF3 Phase 3 Adversarial | `getEngagement` daily aggregation removes the outer `WHERE viewed_at >= CURRENT_DATE - 7d` clause, forcing a full-table sequential scan on `lead_views`. At current table size this is imperceptible; at 1M+ rows (plausible at scale) the COUNT(*) FILTER aggregate could degrade to 50-200ms. BRIN index on viewed_at and the new partial index on saved_at cannot accelerate a predicate-free aggregate. Mitigation option: add `WHERE viewed_at >= CURRENT_DATE - 7d OR saved_at >= CURRENT_DATE - 7d` which allows the BRIN to skip pre-window pages while still catching saves on old-viewed leads. | Future perf hardening |
| LOW | WF3 Phase 3 Adversarial | No DB-roundtrip integration test exercises the Phase 3 recorder/query SQL against a real pg instance. All new tests use `vi.fn()` mocks — a syntax error in the CASE expression would pass all tests and only fail at runtime. The project has a `db.test.ts` infra pattern; Phase 3 has no counterpart. | Phase 1b CI step (same deferral as prior DB integration tests) |
| NIT | WF3 Phase 3 Independent | Pre-existing Drizzle schema.ts bug: `idx_lead_views_lead_trade_viewed` covering index uses `op("timestamptz_ops")` on `tradeSlug` (varchar) — should be `text_ops`. Not introduced by Phase 3, carried forward by the Phase 3 regen. | Future Drizzle hygiene WF |
| LOW | WF3 2026-04-11 Independent | `/api/leads/feed` (user-facing) also calls `getLeadFeed` but has no `isPostgisAvailable` pre-flight. A dev hitting the user feed without PostGIS gets a 500 with sanitized error message (already in place) but not the actionable 503 + install instructions. The test-feed route gets the pre-flight; the user feed doesn't. Not a production issue (Cloud SQL has PostGIS) but a dev experience gap for whoever works on the user feed next. | Future dev-experience WF |
| LOW | WF3 2026-04-11 Independent | `sanitizePgErrorMessage` strips credentials from `error.message` but doesn't touch pg error object fields `detail`, `hint`, `where`, `file`, `routine`. The route handler only serializes `error.message` to the client (so no leak today), but nothing prevents a future refactor from spreading `error` or accessing `error.detail` and leaking schema/column names. Consider a type-safe wrapper or an explicit test asserting only `message` is in the response body. | Future hardening |
| LOW | WF3 2026-04-11 Adversarial | 500 error envelope shape mismatch between sister routes: `/api/admin/leads/health` returns `{ error: message }` (bare string) while `/api/admin/leads/test-feed` returns `{ data: null, error: { code, message }, meta: null }` (structured). Pre-existing; not introduced by this WF3. The dashboard's `extractErrorMessage` helper (added earlier this session) already handles both shapes, so the UI is not broken. Harmonize in a future cleanup — probably move the health route to the structured envelope. | Future admin envelope harmonization |
| LOW | WF3 2026-04-11 Adversarial | No behavioral integration test asserts that `getLeadFeed` is NEVER called when `isPostgisAvailable` returns false. The file-shape test now includes a positional assertion (`isPostgisAvailable(pool)` index < `getLeadFeed(` index) which catches the most obvious regression, but a true behavioral test would mock the helper and assert the route handler short-circuits. Left as file-shape because route-handler-level tests require a different harness. | Future integration test pattern |
| LOW | WF1 Phase 7 2026-04-11 Adversarial near-miss | Error-path observability gap in `/api/leads/feed`: perf marks are captured during the try block but NOT passed through to `internalError()` on the catch path, and are NOT emitted on 401/403/503/429 early returns. Operationally, a slow-path failure that takes 3 seconds in `getLeadFeed` shows a `duration_ms` in logs but no `perf_marks` breakdown — exactly the data you'd want to debug the failure. Fix is a one-liner on the catch path (`return internalError(cause, { route: ..., perf_marks: perf.toLog() })`) plus threading `perf.toLog()` through the early-return helpers. Deferred because the early-return helpers have tight contracts and the change touches `error-mapping.ts` for every leads route. | Future observability WF |
| NIT | WF1 Phase 7 2026-04-11 Independent | `LeadFeedHeader.tsx` filter trigger button has no `aria-controls="lead-filter-sheet"` linkage to the DrawerContent's `id`. The code comment at LeadFilterSheet.tsx:105 acknowledges the expected linkage but it isn't wired. Pre-existing, not introduced by Phase 7. Minor a11y completeness gap; screen readers use implicit focus flow instead. | Future a11y polish |
| NIT | WF1 Phase 7 2026-04-11 Independent | No integration test verifying that an OS-level `prefers-reduced-motion` toggle mid-session takes effect on the next `hapticTap` call. The unit test (`haptics.logic.test.ts:92-107`) locks the behavior via mock `matchMedia`, but no real-browser test covers the toggle. Acceptable for Phase 7 (unit coverage is sufficient), worth noting for QA. | QA manual test |
| LOW | WF3 2026-04-11 migration 083 Adversarial | Migration 084's `pg_constraint` lookup (`WHERE conname = 'entities_photo_url_https'`) scopes by constraint name only, not by `conrelid`. A same-named constraint on a DIFFERENT table would cause a false skip. Pre-existing pattern inherited from migration 039's FK adds. Low practical risk because constraint names in this repo are distinctive, but the defensive form `AND conrelid = 'entities'::regclass` would be stricter. | Future migration hygiene |
| LOW | WF3 2026-04-11 migration 083 Adversarial | Migration 080 has PARTIAL drift — `cost_estimates_null_cost` and `timing_calibration_avg_sample` columns on `data_quality_snapshots` are marked added in the migration file but missing from the live schema. Not blocking the feed, so not repaired in the 083/084 cycle. Could affect quality/health dashboard edge cases. | Future drift sweep |
| NIT | WF3 2026-04-11 migration 083 Adversarial | Migration 083 uses `ST_GeomFromGeoJSON(geometry::text)` on `parcels.geometry` / `neighbourhoods.geometry` jsonb without a pre-validation guard. Production rows are already migrated so the backfill WHERE clause catches zero rows in prod. On dev, malformed jsonb would abort the entire migration. Low risk given data source, but a defensive `WHERE jsonb_typeof(geometry) = 'object'` guard would be safer. | Future migration hygiene |
| LOW | WF3 2026-04-11 migration 083 Independent + Adversarial | Broader drift audit: unknown how many other migrations in 040-082 have the same drift class. Only 067/078 had defensive `RETURN` guards that could silently no-op; others would have errored loudly at apply time. 039/074/080 drifts are historical (DB restore, manual tampering). A comprehensive audit would cross-reference every migration's column adds against `information_schema.columns`. | Future drift audit WF |
| HIGH | WF2 2026-04-11 Lifecycle Adversarial (C5) | `aic-scraper-nodriver.py` (the Python inspection scraper) writes new rows to `permit_inspections` but does NOT bump `permits.last_seen_at`. The lifecycle classifier's incremental predicate is `last_seen_at > lifecycle_classified_at`, so a permit that just passed a new inspection stage would NOT be picked up by the next classifier run until the downstream `classify-inspection-status.js` step bumps `last_seen_at`. Latency is bounded by deep_scrapes chain cadence, not a correctness bug but a staleness gap for the P9-P17 construction sub-stage signal. Fix: bump `permits.last_seen_at` on every inspection insert in the Python scraper, OR change the classifier's dirty predicate to also check `EXISTS (SELECT 1 FROM permit_inspections WHERE permit_num = p.permit_num AND created_at > p.lifecycle_classified_at)`. | `scripts/aic-scraper-nodriver.py` |
| MED | WF2 2026-04-11 Lifecycle Adversarial (H1) | `daysBetween()` JS uses `Math.floor((later.getTime() - earlier.getTime()) / MS_PER_DAY)` while SQL reproducer uses `(NOW()::date - issued_date)`. If Postgres session timezone and Node process timezone differ (e.g., pg session = UTC but Node = America/Toronto), the two can disagree at day boundaries — a permit issued "exactly 30 days ago" could be P7a in JS and P7b in SQL. Production runs both on the same host with the same TZ so it's latent, but a future cloud deployment or DST change could surface it. Fix: normalize both sides to UTC date or force `SET TIME ZONE 'UTC'` at the top of the reproducer. | Future timezone hardening |
| MED | WF2 2026-04-11 Lifecycle Adversarial (H6) | Migration 085's DOWN block is commented out (forward-only per project convention), but nothing prevents a future operator from running a manual `ALTER TABLE permits DROP COLUMN lifecycle_phase` — which would immediately break `/api/leads/feed` because `get-lead-feed.ts:123-124` hardcodes `p.lifecycle_phase` / `p.lifecycle_stalled` in the SQL. There is no `COALESCE(lifecycle_phase, 'P18')` defensive fallback, no feature flag env var. Defender options: (a) wrap the projection in `COALESCE(p.lifecycle_phase, NULL)` and have mapRow fall back to `TIMING_DISPLAY_BY_CONFIDENCE`, (b) add `LIFECYCLE_PHASE_ENABLED=1` env gate, (c) document as "do not drop" in the schema doc. Deferred because the project convention is forward-only migrations and the DOWN block being commented is the enforcement mechanism. | Future rollback hardening |
| MED | WF2 2026-04-11 Lifecycle Adversarial (M6) | `EXPECTED_BANDS` in `assert-lifecycle-phase-distribution.js` are hardcoded against the 2026-04-11 snapshot. Three months from now after CKAN normalization drift, these bands will be stale and the assertion will false-positive. The P9-P17 combined band is also `min: 0, max: 80000` which accepts any value, making the assertion useless for those phases. Fix: rebuild the bands with a 10-day moving average + last-reviewed date, OR scrap the bands in favour of a day-over-day delta check. | Future CQA refinement |
| MED | WF2 2026-04-11 Lifecycle Adversarial (M1-M4) | Test coverage gaps in `src/tests/lifecycle-phase.logic.test.ts`: (M1) no combinatorial test for orphan + every dead/P20/P19 status — only `Cancelled` is explicitly tested in combination. (M2) no test for `Permit Issued` + `has_passed_inspection=true` + `latest_passed_stage=null` (the defensive P18 branch). (M3) no test pinning the semantics of a 3-year-old permit with a recent passing inspection (P18 vs stalled?). (M4) the `computeStalled` Signal 3 test uses `has_passed_inspection: true` but the signal only requires `status='Inspection' + latest_inspection_date>180d`. Add targeted tests for these gaps. | Future test hardening |
| LOW | WF2 2026-04-11 Lifecycle Independent | `O4` (orphan closed) is declared in `VALID_PHASES` and `LIFECYCLE_PHASE_DISPLAY` but is architecturally unreachable — the decision tree short-circuits closed statuses to `P20` before the orphan branch runs. The test at line 207 of lifecycle-phase.logic.test.ts explicitly pins `P20 not O4` for this case. The `LIFECYCLE_PHASE_DISPLAY.O4` entry at `src/features/leads/lib/lifecycle-phase-display.ts:57` is dead code but not a bug. Either remove the entry or add an explicit "unreachable by design" comment next to it. | Future cleanup |
| LOW | WF2 2026-04-11 Lifecycle Independent | CoA `NORMALIZED_DEAD_DECISIONS` includes `'closed'` and `'delegated consent refused'` which are NOT documented in spec §1.1. The SQL reproducer was updated to match, so the round-trip gate won't catch the divergence. These were presumably found empirically in the live DB but there's no test comment or spec note explaining the provenance. A CoA row with `decision = 'Closed'` (the lone 'closed') gets killed silently. Document in the spec or add an inline comment citing the source. | Future spec update |
| LOW | WF2 2026-04-11 Lifecycle Adversarial (M8) | `lifecycle_phase` is `VARCHAR(10)` per migration 085, which is a tight constraint. The current phase domain uses 2-3 character codes (P7a, P20, O3) so there's headroom, but any future hyphenated or stateful phase codes (e.g., `'O3-stalled'`) would fail with `value too long`. Document the limit or widen to VARCHAR(20) preemptively. | Future spec refinement |
| LOW | WF2 2026-04-11 Lifecycle Adversarial (M9) | `scripts/link-coa.js:339` uses `INTERVAL '1 second'` buffer on the `permits.last_seen_at` bump idempotency guard. This only prevents rapid-fire retries (<1s apart); two cron firings 2+ seconds apart would both pass the guard and double-bump. Production has one cron instance so the guard is sufficient today, but a distributed scheduler would need an advisory lock. | Future distributed-cron hardening |
| LOW | WF2 2026-04-11 Lifecycle Adversarial (M10) | `TIMING_DISPLAY_BY_CONFIDENCE` is still exported from `src/features/leads/lib/get-lead-feed.ts` and imported by `get-lead-feed.logic.test.ts:7` even though `mapRow` no longer reads it. The old legacy describe block still runs. Remove the export + test block in a followup sweep. | Future cleanup |
| LOW | WF2 2026-04-11 Lifecycle Independent | No infra test simulates the `lifecycle_classified_at` stamp failure path — the per-batch `withTransaction` rewrite (this commit) ensures phase + stamp commit atomically, but a behavioral test that injects a transient pg error mid-transaction and asserts no partial state would be stronger than regex shape tests. | Future test pattern |
| HIGH | WF1 Phase 2 2026-04-12 Adversarial (HIGH-2) | Stale `old_phase` from JS memory can log incorrect `from_phase` in transitions if a concurrent writer (load-permits.js, link-coa.js) changes the permit's phase between the dirty SELECT and the batch UPDATE. Advisory lock prevents concurrent classifiers but not other pipeline scripts. Risk is low (narrow window, self-correcting on next run) but bad calibration data is hard to detect. Fix: use RETURNING from the batch UPDATE to capture the pre-update phase, or accept and document. | Future hardening |
| MED | WF1 Phase 2 2026-04-12 Adversarial (MEDIUM-1) | The 242K-row `phase_started_at` backfill UPDATE runs as a single un-batched statement, locking all rows simultaneously. One-time cost (self-healing via `WHERE phase_started_at IS NULL` guard), but blocks concurrent writers for ~30-60s on the first run. Consider chunking into 10K batches in a future hardening pass. | Future hardening |
| MED | WF1 Phase 2 2026-04-12 Independent (Item 10) | Phase 2 state-machine transition detection has no behavioral logic tests — only regex shape tests. A `lifecycle-phase.logic.test.ts` entry should exercise: (a) first classification produces from_phase=null transition, (b) stalled-only change produces no transition, (c) P7a→P7b is suppressed, (d) P7a→P20 is logged. | Future test hardening |
| MED | WF1 Phase 2 2026-04-12 Independent (Item 9) | Terminal-state transitions (any-phase → null/dead) are intentionally suppressed by the `r.phase !== null` filter. The calibration engine cannot measure "time from active phase to closure" from transitions alone without joining back to permits. Document this design choice in the calibration engine spec (Phase 3). | Phase 3 spec |
| HIGH | WF1 Phase 3 2026-04-12 Adversarial (Probe 4) | `TRADE_TARGET_PHASE` maps each trade to ONE lifecycle phase, but MEP trades (plumbing, hvac, fire-protection) have TWO active phases: P12 (rough-in) and P15 (final). A plumber subscribing to leads would miss "plumbing final coming in N days." The flight tracker (Phase 4) needs either `Map<string, string[]>` or a `TRADE_SECONDARY_PHASE` mapping to generate predictions for both phases. | Phase 4 design |
| MED | WF1 Phase 3 2026-04-12 Adversarial (Probe 5) | The flight tracker's fallback for uncalibrated phase pairs is a hardcoded 30-day default. If the actual median for a rare pair is 150 days, the prediction is 120 days too optimistic. Better: two-tier fallback (specific → all-types aggregate → configurable default), or store a `default_gap_days` in the calibration table. | Phase 4 design |
| MED | WF1 Phase 3 2026-04-12 Adversarial (Probe 1) | `PREV_STAGE_TO_PHASE_SQL` built via `.replace(/stage_name/g, 'prev_stage')` — works today but fragile if a future edit adds `stage_name` in a comment or string literal. Refactor to a template function `stageToPhaseCase(colName)`. | Future hardening |
| LOW | WF1 Phase 3 2026-04-12 Adversarial (Probe 2) | Per-row UPSERT (207 individual `pool.query()` calls) is 200x more round-trips than necessary. A single multi-row `INSERT...VALUES ON CONFLICT` would be cleaner. Not blocking at 1.1s for a daily batch. | Future perf cleanup |
| LOW | WF1 Phase 3 2026-04-12 Independent (Gap 1) | No concrete stage-name parity test: infra test checks structural patterns but never executes SQL CASE against known inputs and compares to `mapInspectionStageToPhase()` output. | Future test hardening |
| MED | WF1 Phase 4 2026-04-12 Adversarial (Probe 5) | Flight tracker has no advisory lock. Concurrent runs produce identical outputs (ON CONFLICT safe), but pre/post row count telemetry could be wrong. Add `pg_try_advisory_lock(86)` matching the classifier pattern. | Future hardening |
| MED | WF1 Phase 4 2026-04-12 Both | 92% of forecasts are `overdue` — architecturally correct for old permits (issued_date months/years ago + median_days < today) but has low signal value for the feed. Product decision: either recalibrate urgency tiers for aged permits, or filter overdue from the default feed view and surface only the 8% actionable forecasts. | Product design |
| MED | WF1 Phase 4 2026-04-12 Both | Infra tests are regex shape-only — no behavioral coverage for `classifyUrgency`, `classifyConfidence`, `lookupCalibration`, or the `isPastTarget` ordinal logic. These are pure functions easily unit-testable. | Future test hardening |
| MED | WF3 Phase 4 2026-04-12 Independent (Issue 3) | Ghost purge DELETE runs before batch UPSERT — if the script crashes mid-batch, purged rows are not replenished until the next run. Consistent with existing pipeline pattern (non-transactional). Transient consistency gap only, not data corruption. | Existing pattern |
| MED | WF3 Phase 4 2026-04-12 Adversarial (Probe 2) | Ghost purge race with concurrent reclassification — if permit_trades has a brief gap during reclassification (old trades deleted, new not yet inserted), the NOT EXISTS fires and deletes the forecast. Re-created on next run. Eventually consistent. | Existing pattern |
| HIGH | WF3 Phase 4 2026-04-12 Adversarial (Defect 2) | UTC midnight normalization shifts `today` forward for evening Toronto runs (8pm-midnight). Both `today` and `anchorDate` shift in the same direction so `daysUntil` is mostly stable, but off-by-one at tier boundaries is possible. Correct fix: normalize to Toronto business date via `toLocaleDateString('en-CA', {timeZone: 'America/Toronto'})`. Deferred because the cancellation makes practical impact minimal and the prior local-TZ bug was worse. | Future hardening |
| MED | WF3 Phase 4 2026-04-12 Adversarial (Defect 3) | ISSUED calibration has no rows for P3-P6 targets (pre-issuance phases aren't inspection stages). P3 bid-phase forecasts use the 30-day hardcoded default with low confidence. This is expected — the bid window prediction is "application submitted + 30 days, low confidence." The flight tracker correctly labels these as low confidence so downstream consumers can distinguish. | Accepted design limitation |
| LOW | WF3 Phase 4 2026-04-12 Independent | P8 (Revised) uses ISSUED calibration but its `phase_started_at` reflects the revision date, not the original issuance date. Calibration data was built from initial-issuance events. For revised permits the median may undercount. Pre-dates this diff. | Future calibration refinement |
| CRITICAL | WF3 2026-04-12 Adversarial (D1) | **Unstall cliff**: when `lifecycle_stalled` flips false, the snowplow disappears and `predictedStart` reverts to `anchor + median`. For old permits the daysUntil is -700+, hitting expired instantly. A permit that just resumed construction shows as dead data. Fix: Phase 2 classifier should reset `phase_started_at` to NOW() when `lifecycle_stalled` transitions from true→false (same CASE logic as phase transitions). Without this, the system punishes unstalling. | Phase 2 classifier upgrade |
| HIGH | WF3 2026-04-12 Adversarial (D2) | Rolling snowplow stores a daily-moving `predicted_start` in trade_forecasts. For stalled permits, the stored date is `today + penalty`, not a real prediction. Changes by +1 day per daily run. By design per user spec ("every day it stays stalled, the date must push forward") but consumers should be aware the date is synthetic. Document or add `stall_adjusted BOOLEAN` column. | Documentation / schema |
| HIGH | WF3 2026-04-12 Adversarial (D3) | `isPastTarget` overrides stall penalty for permits physically past the work_phase. The 14-day penalty math is wasted. Accepted: if the permit passed P12, the plumbing window is closed regardless of stall. | Accepted design |
| MED | WF3 2026-04-12 Adversarial (D5) | 45-day bureaucracy penalty is undersized for P3-P5 stalls (Toronto zoning appeals take 6-18 months). Consider proportional penalty: `max(45, Math.round(cal.median * 0.5))` or stall-duration calibration table. | Future calibration |
| MED | WF2 089 2026-04-12 Adversarial | `tracked_projects.updated_at` has no Postgres trigger for auto-update on row changes. Application must explicitly `SET updated_at = NOW()` on every UPDATE. If forgotten, the column shows stale timestamps. Consider a `BEFORE UPDATE` trigger function, or accept as application-layer responsibility and document. | Claiming API WF1 |
| MED | WF2 089 2026-04-12 Adversarial | No TTL/expiry mechanism for stale `claimed_unverified` rows in `tracked_projects`. A user can claim and never verify, blocking others indefinitely. The claiming API (future WF1) should include an expiry job or `expires_at` column. | Claiming API WF1 |
| HIGH | WF1 090 2026-04-12 Adversarial | No advisory lock on update-tracked-projects.js. Concurrent runs read same last_notified state, generate duplicate alerts. DB updates don't conflict but notification dispatch gets doubled. Fix: pg_advisory_lock or SELECT FOR UPDATE SKIP LOCKED. | Future hardening |
| MED | WF1 090 2026-04-12 Independent | isWindowClosed uses work_phase only, doesn't mirror the bimodal bid_phase routing from compute-trade-forecasts.js. Low blast radius with current ordinals but could cause premature archiving for edge-case trade targets. | Future alignment |
| MED | WF1 090 2026-04-12 Adversarial | last_notified_urgency only tracks 'imminent'. The 'upcoming' tier is silently ignored — users get zero signal until the final tier. Intentional per user spec (imminent-only alerting). Document or rename column. | Documentation |
| MED | WF1 090 2026-04-12 Adversarial | last_notified_stalled DEFAULT false vs last_notified_urgency NULL asymmetry. Both NULL and false mean "never notified" and the check `!== true` handles both. But a future `=== false` check would miss NULL rows. Pick one convention. | Future hardening |
| HIGH | WF3 090 2026-04-12 Adversarial | Single withTransaction wraps ALL merged updates. At 5K+ updates, row-level locks are held for the full loop duration, blocking concurrent API writes (user claiming a project mid-pipeline). Fix: batch into chunks of ~500 with separate transactions. Not critical at current scale (tracked_projects is small). | Future scaling |
| MED | WF1 091 2026-04-12 Adversarial (CRITICAL-1) | `lead_analytics.lead_key` is a VARCHAR(100) synthetic key ('permit:num:rev') instead of a composite FK to permits. Intentional: behavioral signal table is decoupled from permits to retain history. No FK means orphan rows accumulate silently — plan a periodic cleanup job. | Future cleanup job |
| MED | WF1 091 2026-04-12 Adversarial (MEDIUM-4) | `trade_forecasts.target_window` allows NULL. All 13K existing rows have NULL. Downstream consumers must handle 3 states (bid, work, NULL). NULL = "not yet classified" — acceptable until compute-trade-forecasts.js populates it. Document the semantics. | Future forecaster update |
| HIGH | WF1 Phase 2 2026-04-12 Adversarial | Opportunity score elite tier (>=80) is mathematically unreachable: max base=30, max multiplier=2.5, max raw=75. Either raise the multiplier/cap, lower the elite threshold to 70+, or accept that elite is aspirational. | Product formula calibration |
| MED | WF1 Phase 2 2026-04-12 Independent | Competition penalty of 50 per tracker is too aggressive: 2 trackers = penalty 100, which exceeds the max base*urgency (75). Every lead with 2+ trackers scores 0 regardless of dollar value. Consider reducing to 5-10 per tracker. | Product formula calibration |
| HIGH | WF2 Phase 3 2026-04-12 Adversarial | Zero-out subquery uses per-row string concatenation in a correlated NOT EXISTS. At 100K tracked_projects × 50K lead_analytics this is O(n²) string ops. Fix: materialize the distinct key set in a CTE, then anti-join. At current scale (0 rows) this is moot. | Future scaling |
| MED | WF2 Phase 3 2026-04-12 Adversarial | lead_analytics rows are never deleted, only zeroed. Unbounded accumulation of dead (0,0) rows. Add periodic `DELETE WHERE counts=0 AND updated_at < now() - 90 days`. | Future cleanup |
| MED | WF2 Phase 3 2026-04-12 Independent | No behavioral test for multi-status GROUP BY (same permit saved + claimed). Shape tests only. | Future test hardening |
| LOW | WF3 092 2026-04-13 Independent | `let` module-scope reassignment for DB-loaded config (TRADE_ALLOCATION_PCT, LIAR_GATE_THRESHOLD, TRADE_TARGET_PHASE) is structurally fragile. A future developer calling these outside pipeline.run gets fallback values. Safer: pass as params. Not a current bug. | Future refactor |
| HIGH | WF3-04 2026-04-14 Independent | 82-W6 (`update-tracked-projects.js` L381): `records_updated: updates.length` is the pre-merge accumulator count, inflates telemetry when a single row has both stall+imminent deltas. Fix is one-liner: `records_updated: mergedUpdates.length`. Out of scope for WF3-04 (H-W14); file as standalone WF3. | Standalone WF3 |
| LOW | WF2 Phase 5 2026-04-17 Independent | `ConfirmSyncModal.tsx` uses custom responsive CSS (`items-end sm:items-center`, `rounded-t-2xl sm:rounded-2xl`) instead of Shadcn `<Drawer>` (mobile) + `<Dialog>` (desktop) as required by spec 86 §5 Mobile & Responsive Behavior. Pre-existing; not introduced by Phase 5. | Future WF2 86_control_panel mobile polish |
| NIT | WF2 Phase 5 2026-04-17 Independent | Spec 86 §1 table labels the variable `liar_gate_threshold_pct` but the codebase (seed JSON, DB migration, config-loader, API, tests, UI) consistently uses `liar_gate_threshold`. Docs-only drift, code is internally consistent. | Docs WF — update spec 86 §1 table label |
| NIT | WF2 Phase 5 2026-04-17 Independent | `StickyActionBar` uses `flex gap-3` without `flex-col sm:flex-row` — spec 86 §5 requires buttons to stack vertically on mobile (< 640px). Pre-existing. | Future WF2 86_control_panel mobile polish |
| LOW | WF2 CQA 2026-04-18 Gemini | `THRESHOLDS.opportunity_score = 0.80` in `assert-entity-tracing.js` may false-alarm when `compute_opportunity_scores` correctly assigns score=0 to permits in the wrong phase or with no competing bids. The script cannot distinguish "pipeline skipped" from "legitimately low scores." Add an inline comment or expose threshold via `logic_variables` so ops can re-calibrate without a code deploy. | Future CQA threshold refinement |
| NIT | WF2 CQA 2026-04-18 Gemini | Opportunity_score failure message in `assert-entity-tracing.js` says "% of forecast rows scored > 0" — "scored > 0" is ambiguous because the column defaults to 0. Clearer: "% of forecast rows where compute_opportunity_scores set a positive value (> 0 = non-default)." | Future message clarity |
| LOW | WF2 CQA 2026-04-18 Independent | `assert-entity-tracing.js` THRESHOLDS are hardcoded (unlike `assert-lifecycle-phase-distribution.js` which loads `lifecycle_unclassified_max` from `logic_variables` via `loadMarketplaceConfigs`). Coverage expectations may need tuning as data volume grows. Future: promote THRESHOLDS to `logic_variables` following the assert-lifecycle-phase-distribution.js config-loader pattern. | Future CQA config |
| HIGH | WF1 49 2026-04-19 DeepSeek | `FUNNEL_SOURCE_BY_SLUG` in `src/lib/admin/funnel.ts` is built via `Object.fromEntries(FUNNEL_SOURCES.map(s => [s.statusSlug, s]))`. `trades_residential` and `trades_commercial` both have `statusSlug: 'classify_permits'` (lines 32-33), so `trades_commercial` silently overwrites `trades_residential` in the map. `FreshnessTimeline.tsx:786` looks up `FUNNEL_SOURCE_BY_SLUG[step.slug]` so the `classify_permits` step renders the commercial config only. Both rows have identical `auditMetric` and display so the visual impact is currently minimal, but any future differentiation will silently apply only to commercial. Pre-existing; not introduced by this WF (we only added one STEP_DESCRIPTIONS line). Fix: give each a distinct key (`classify_permits_residential` / `classify_permits_commercial`) or add an assertion in quality.logic.test.ts. | Future WF3 funnel.ts |
| MED | WF1 49 2026-04-19 Gemini | `FreshnessTimeline.tsx` has multiple pre-existing CRITICAL/HIGH architectural issues not introduced by this WF: (1) optimistic toggle timer — `setTimeout(8s)` for state sync creates race conditions; (2) global `runError` state replaced per pipeline run; (3) widespread unsafe `as Record<string,unknown>` casts on `records_meta`; (4) monolithic 1449-line component re-renders on every state change; (5) duplicate audit-table rendering in two code paths; (6) memory leak in `toggleExpand` fetch without AbortController. None were introduced by this WF (new code was registry entry + columnar render path only). | Future FreshnessTimeline refactor WF2 |
| MED | WF1 49 2026-04-19 DeepSeek | Additional `FreshnessTimeline.tsx` pre-existing bugs confirmed by DeepSeek: (1) `runAllDisabled` passes `undefined?.has(...)` → `every` returns false, incorrectly enables Run All button (line 284); (2) `isEffectivelyDisabled` double-negates optimistic toggles — stores enabled-state but reads as disabled (line 414); (3) `tileFlash` condition `Aging`/`Overdue` unreachable when `Failed`/`Warnings` cases come first (line 396); (4) chain error summary `failedSteps.map(...)` missing React `key` prop (line 1016). All pre-existing. | Future FreshnessTimeline refactor WF2 |
| LOW | WF1 49 2026-04-19 Gemini | `assert-global-coverage.js` aggregate query destructuring pattern `const { rows: [ca] } = await pool.query(...)` would throw `TypeError` if a query unexpectedly returned 0 rows. Aggregate queries without `GROUP BY` in PostgreSQL always return exactly one row, so the risk is effectively zero. Defensive form: `const ca = (await pool.query(...)).rows[0] ?? defaultObj`. | Future hardening |
| LOW | WF1 49 2026-04-19 Gemini+DeepSeek | `assert-global-coverage.infra.test.ts` urgency operator test asserts `<>` specifically (line 112). Both `<>` and `!=` are valid in PostgreSQL. Since the script currently uses `<>` the test is correct, but a regex `/<> | !=` would be more resilient to whitespace/style changes. Low priority since implementor controls the operator choice. |
| LOW | WF1 49 2026-04-19 DeepSeek | `assert-global-coverage.infra.test.ts` does not validate that the `SKIP_PHASES_SQL` constant contains exactly the 7 expected phases matching `compute-trade-forecasts.js`. A typo or missing phase would pass the current regex test. Add a test that asserts the exact phase list `('P19','P20','O1','O2','O3','P1','P2')` appears verbatim. | Future test hardening |
| MED | WF1 85/41 2026-04-19 Adversarial+Independent | `lookupCalibration` emits `'fallback_issued_type'` and `'fallback_issued_all'` as `cal.method` values. These are not in the spec-85 column enum (`exact`, `fallback_all_types`, `fallback_issued`, `default`). They can be written verbatim to `trade_forecasts.calibration_method` when `phase_started_at` IS present but calibration lookup falls back to ISSUED-based rows (the `anchorIsFallback` guard does not help here). Fix: rename levels 3 and 4 inside `lookupCalibration` to map to `'fallback_issued'` per the spec. | Future WF3 compute-trade-forecasts.js |
| MED | WF1 85 2026-04-19 Adversarial (Item 11) | Stale-purge DELETE commits in its own `withTransaction` before any UPSERT batch. A crash between the purge commit and first UPSERT batch leaves purged permits with zero trade forecasts until next run. Violates spec 47 §7.3 "DELETE + UPSERT in same withTransaction." Pre-existing architecture, not introduced by this WF1. | Future WF2 compute-trade-forecasts.js |
| MED | WF1 85 2026-04-19 Adversarial (Item 12) | `records_total: upserted` in emitSummary is the UPSERT row count, not rows evaluated (`totalRows`). Understates work done and inflates `sys_velocity_rows_sec`. Pre-existing. | Future WF3 telemetry |
| MED | WF1 85 2026-04-19 Adversarial (Item 15) | `scoring.ts` `freshnessBoost`/`stalenessPenalty` call `differenceInDays(new Date(), issuedDate)` using live wall clock; `classify-permits.js` correctly uses the captured `runAt` parameter. Dual-path divergence: TS API lead scores drift from JS pipeline scores for stale permits. Pre-existing; not introduced by this WF. | Future dual-path parity WF |
| LOW | WF1 85 2026-04-19 Adversarial (Item 4) | `finalCalMethod = anchorIsFallback ? 'fallback_issued' : cal.method` masks the double-failure case: a permit with no anchor AND no calibration data gets `'fallback_issued'`, not `'default'`. The `default_calibration_pct` audit metric understates the worst-quality forecasts. Spec-compliant, but observability gap. | Future observability WF |
| LOW | WF3 2026-04-21 Self | `assert-global-coverage.js` forecastEligible Denom G still has `NOT LIKE 'PRE-%'` filter that is absent from SOURCE_SQL. After P1/P2 inclusion, CoA pre-permits (PRE- prefix) that genuinely get forecasts would be excluded from the denominator — producing overcounting. Low risk until PERT pipeline actually generates P1/P2 forecasts (currently 0 rows). | Future CQA hygiene |
| LOW | WF3 2026-04-21 Adversarial (COALESCE cast) | `assert-entity-tracing.js` eligiblePermits + numerator use 3-arg COALESCE without `::timestamptz` cast, while SOURCE_SQL uses 2-arg `COALESCE(phase_started_at, issued_date::timestamptz)`. The 3-arg form includes `application_date` (date not timestamptz), which may over-count denominator for permits with null phase_started_at AND null issued_date but valid application_date. Conservative direction (false FAIL risk not false PASS) and currently 0 such rows. | Future COALESCE parity |
| LOW | WF3 2026-04-21 Adversarial (stale purge gap) | Phase-Past-Target Guard skips forecast generation but does NOT purge existing `overdue` forecasts for past-target permits. Such permits remain in SOURCE_SQL (within 3 years) so NOT EXISTS stale-purge won't clean them. They'll eventually be grace-purged when urgency = `expired` + 180-day grace. Acceptable degradation — false `overdue` forecasts exist until natural expiry. | Future purge alignment WF |
| LOW | WF3 2026-04-21 DeepSeek (pre-existing) | `compute-trade-forecasts.js` map key collision: NULL `permit_type` rows use `undefined` as key, which is silently overwritten on each NULL-type permit. All NULL-type permits use the same calibration entry. Low practical impact but should be handled explicitly (`'UNKNOWN'` key or a null guard). | Future hardening |
| LOW | WF3 2026-04-21 DeepSeek (pre-existing) | `compute-trade-forecasts.js` bimodal routing: `targetPhase` is set from `targets.bid_phase` or `targets.work_phase` but no guard validates that the resolved phase is a valid key in `PHASE_ORDINAL`. An unknown phase silently produces `targetOrdinal = undefined`, which makes the Phase-Past-Target Guard a no-op for that row. | Future validation |
| MED | WF3+WF2 2026-04-21 DeepSeek+Gemini | `timing.ts` ensureCalibrationLoaded uses `MIN(median_days)` / `MAX(p75_days)` aggregation across multiple `to_phase` rows in phase_calibration. Semantically different from old timing_calibration which had one row per permit_type. MIN takes shortest transition (e.g., ISSUED→P9 at ~20d) not first-inspection median (~105d). Impact on Tier 2 estimates needs separate investigation of actual phase_calibration data shape. | Future timing.ts calibration query refinement |
| LOW | WF3+WF2 2026-04-21 DeepSeek | `calibration_freshness_warn_hours` Zod schema entry and local variable in assert-data-bounds.js are dead code after v1 timing staleness check removal. Variable is loaded but never referenced. | Future cleanup |
| LOW | WF3+WF2 2026-04-21 Independent | `docs/specs/product/future/71_lead_timing_engine.md` referenced in SPEC LINK headers of timing.ts and timing.logic.test.ts does not exist in repo. May have been renamed/moved. | Future spec hygiene |
| LOW | WF3+WF2 2026-04-21 DeepSeek | refresh-snapshot.js now writes NULL for timing_calibration columns in data_quality_snapshots. Any dashboard query computing trends over these columns will see a cliff. Current consumers handle NULL correctly. | Monitor only |
| NIT | WF3+WF2 2026-04-21 Gemini | `compute-trade-forecasts.js` SOURCE_SQL second OR branch has redundant `NOT IN ('P1','P2')` guard — P1/P2 are not in SKIP_PHASES_SQL so the guard is a no-op. Correct but fragile against future SKIP_PHASES_SQL changes. | Future cleanup |
| LOW | WF3 2026-04-22 Gemini | `compute-trade-forecasts.js` stale-purge NOT EXISTS uses `NOW()` for the 18-month and 3-year recency gates instead of the captured `runAt` parameter. A permit exactly at the 3-year boundary could be included in SOURCE_SQL (evaluated at T+0) but excluded by the NOT EXISTS subquery (evaluated at T+seconds). Pre-existing pattern unrelated to this WF3. Fix: pass `runAt` as a bind parameter and replace `NOW()` with `$N` in both recency gates. | Future hardening |
| LOW | WF3 2026-04-22 Gemini | `compute-trade-forecasts.js` emitMeta reads list includes `lifecycle_stalled` but the column is now only used as a filter predicate (WHERE clause), not as a computational input to any row. Accurate metadata but potentially misleading — an operator reading records_meta would expect stall logic to run. Cosmetic only; the column IS read from the DB (it appears in the SELECT list). | Future observability cleanup |
| HIGH | Gemini+DeepSeek | compute-timing-calibration.js stale permit_types not deleted (table accumulates obsolete rows over time) | n/a |
| HIGH | Gemini+DeepSeek | timing.ts test "missing Tier 2 coverage" | n/a |
| MED | DS-P2 | Trade slug case collation | Deferred — both sides from DB |
| MED | WF3 Batch 2 2026-04-23 Independent (FAIL 2) | `anchorSourceCounts` and `anchorFallbackCount` are incremented after the Phase-Past-Target Guard (line 427) but before the in-memory grace-cutoff check (line 483). Rows dropped by `graceCutoffMs` are counted in `anchor_sources` telemetry but never reach `allForecasts`. Over-counts relative to actual written forecasts on data-sparse envs. Fix: move the increment after `allForecasts.push()` or add a decrement in the grace-skip branch. | Future WF3 compute-trade-forecasts.js telemetry |
| LOW | WF3 Batch 2 2026-04-23 Independent (FAIL 3) | `compute-trade-forecasts.infra.test.ts` line 141: `not.toMatch(/await pool\.query\([\s\S]{0,40}DELETE FROM trade_forecasts/)` only verifies the DELETE is not a bare `pool.query` call — it does not verify the DELETE is inside the `withTransaction` callback. The structural guarantee relies entirely on the multiline regex at lines 444-447. Test description overstates its scope. | Future test-description cleanup |
| MED | WF3 Batch 3 2026-04-23 Independent (CL-21) | `config-loader.infra.test.ts` allocSum=0 guard (B3-C1) is verified by source-text regex only — no behavioral test constructs a mock pool returning all-zero `allocation_pct` rows and asserts the fallback is returned. If the revert target changed from `FALLBACK_TRADE_CONFIGS` to a partial map, the regex test would still pass. | Future behavioral test WF3 |
| LOW | WF3 Batch 3 2026-04-23 Independent (CL-22) | `config-loader.infra.test.ts` structuredClone isolation (B3-H2) is verified by source-text regex only — no mutation isolation test verifies that a second `loadMarketplaceConfigs` call after a consumer mutates a nested property on the first call's result still returns the original fallback. | Future behavioral test WF3 |
| LOW | Independent | `inspection_stage_map` seed (migration 072) covers 21 of 32 trade slugs. The 12 unmapped trades (excavation, shoring, insulation, glazing, elevator, demolition, pool-installation, solar, security, temporary-fencing, caulking, eavestrough-siding) always fall to Tier 2 calibration in `timing.ts` even when inspection data is available. Not a correctness bug (Tier 2 is the documented fallback) but a data-quality gap. | Future seed expansion |
| HIGH | Gemini | `estimateCost` impure (calls `new Date()` internally) | n/a |
| MED | DeepSeek | No query timeout on `getCurrentUserContext` — slow DB hangs the route | n/a |
| MED | DeepSeek | No request-ID correlation across log layers (auth → validation → DB → logging) | n/a |
| LOW | Gemini | `trade_slug` schema duplicated 3x across schemas.ts | n/a |
| LOW | Independent | `updated_at` on `user_profiles` has no auto-update trigger | n/a |
| LOW | DeepSeek | No index on `user_profiles.trade_slug` for future analytics | n/a |
| LOW | DeepSeek | No ON DELETE cleanup strategy for orphaned profiles when Firebase user is deleted | n/a |
| LOW | Independent | No test asserts retryAfterSec format | n/a |
| MED | Independent | No `withErrorBoundary` wrapper helper — every Phase 2 route will repeat top-level try/catch boilerplate | Phase 2-ii / 2-iii |
| LOW | Independent | `unauthorized()` cannot distinguish "no session" from "no profile row" — onboarding flow will need to redirect differently | Future onboarding WF |
| MEDIUM | DeepSeek | "no query timeout — could trigger full table scan" | n/a |
| HIGH | DeepSeek | Error logging includes PII (user_id, lat, lng) — violates privacy regulations | n/a |
| HIGH | Gemini | `useGeolocation` race condition: external `request()` call BEFORE the async `checkPermissionState()` resolves can be overwritten by the resolved state → user sees "denied" UI even though the prompt is showing | n/a |
| CRITICAL | Gemini | Builder lead_id `e.id::text` sorts lexicographically — `'9' < '10'` breaks cursor pagination on relevance ties | n/a |
| HIGH | User review (earlier WF3) | Zod deadlock Layer 3: "Reset filters" UI failsafe — deferred "to Phase 3-v where the filter sheet naturally hosts the affordance" | (this WF1) |
| IMPORTANT | Indep Item 15 | Header "open sheet" tap emitted `lead_feed.filter_changed` with `action:'open'` — semantically wrong (opening a sheet is UI navigation, not a filter value mutation). Would inflate PostHog `filter_changed` counts by 1 for every sheet open and corrupt funnel analysis. | (this WF1) |
| HIGH | Gemini | `aria-label="Open filter sheet to change location or radius"` on the header button OVERRIDES the visible dynamic text (`"Near you · 20km"`) for screen readers. Assistive tech users hear the generic static string and miss the contextually useful location/radius readout. | (this WF1) |
| DEFERRED | Gemini | i18n hardcoded strings (km units, "lead/leads" pluralization) | DEFERRED — out of scope for Phase 3. Toronto-only product, English-only. File for a dedicated i18n WF if/when we expand. |
| DEFERRED | DeepSeek | `{lat: 0, lng: 0}` Null Island check in locationLabel | DEFERRED — we never set `(0, 0)` as a default; locations come from real geolocation or persisted state. Null Island IS valid coords (middle of the Gulf of Guinea). Edge case not worth the complexity. |
| DEFERRED | DeepSeek | Intl.PluralRules for lead count | DEFERRED — same as i18n above. |
| DEFERRED | DeepSeek | DrawerPortal conditional rendering for perf | DEFERRED — Shadcn default. Premature optimization for this use case. |
| DEFERRED | Gemini | `DrawerHandle` extractable component for customization | DEFERRED — YAGNI. Only one Drawer consumer exists today. Extract when a second one has a different need. |
| MED | Indep P3 I6 | `lead_feed.lead_clicked` emits `lead_id` (cardinality + PII-adjacent) | DEFERRED — `lead_id` is the core click-attribution analytic; dropping it kills funnel analysis. Product RFC required. |
| LOW | DeepSeek P03 | `getStageSequence` hardcoded stage strings (timing.ts) | DEFERRED — bundle with next timing-engine WF. |
| LOW | DeepSeek P03 | `user_profiles.updated_at` missing trigger | DEFERRED — verify project convention before adding. Low impact. |
| LOW | DeepSeek P03 | `tier2IssuedHeuristic` permit_type null guard | DEFERRED — bundle with next timing engine cleanup. |
| LOW | Gemini P03 | cost-model dual-path TS↔JS | DEFERRED — acknowledged ADR; re-litigate via spec spike. |
| LOW | DeepSeek P3 | `__opportunityLookup` test export | DEFERRED — minor; refactor when test churn warrants. |
| MEDIUM | Adversarial BUG-5 | `compute_` prefix in isInfraStep also exempts `compute_centroids` in sources chain — latent if sources gate ever added. Consider slug-specific allow-list or rename to `recompute_centroids`. | WF2 if sources gate added |
| LOW | Adversarial BUG-7 | Cost/timing check results in assert-data-bounds not appended to `permitsAuditTable.rows` — invisible in dashboard drill-down. | WF2 polish |
| MEDIUM | DeepSeek F9 | compute-cost-estimates 237K single-row UPSERTs take 4-8 min. Future optimization: multi-row VALUES like classify-permits.js (MAX_ROWS_PER_INSERT pattern). | WF2 performance |
| MEDIUM | DeepSeek F6 | compute-timing-calibration has no advisory lock — asymmetry with cost script. Safe (small table + UPSERT) but inconsistent. | WF2 hardening |
| LOW | Independent | Migration 080 DOWN block commented out. Matches existing convention (071, 073) but rollback path is inoperable. | Design decision |
| LOW | Adversarial | No `ORDER BY` on compute-cost-estimates SOURCE_SQL — non-deterministic if fan-out occurs. Mitigated by LATERAL LIMIT 1 but not fully defended. | Future hardening |
| LOW | Independent | PostGIS path uses confidence 0.90 vs JS fallback 0.95 for same centroid_in_parcel operation. Undocumented inconsistency. | Future harmonization |
| HIGH | Adversarial | `saves_today` counts `viewed_at >= CURRENT_DATE AND saved=true`, not actual save timestamp. `lead_views` has no `saved_at` column — needs schema change for accurate engagement metrics. | WF2 migration + schema |
| HIGH | Adversarial | Admin auth is cookie-shape-only (no `getUserIdFromSession` call). Pre-existing across ALL admin routes, not specific to this feature. | Auth hardening WF |
| MEDIUM | Independent | `cost_coverage_in_results` and `timing_tier_distribution` missing from TestFeedDebug — LeadFeedItem doesn't carry the data needed. | Phase B (add fields to LeadFeedItem) |
| MEDIUM | Adversarial | Health endpoint runs 10 unthrottled DB queries per poll cycle, no cache. | Phase B (add 30s server-side cache) |
| LOW | Independent | Median/percentile uses floor-index (biased on even arrays). Test only covers odd-length. | Future hardening |
| LOW | Adversarial | `computeTestFeedDebug` parameter typed as inline object instead of `LeadFeedItem[]`. | Type hygiene |
| HIGH | Adversarial | `useEffect` for data fetching in admin dashboard — technically violates §12.2 (TanStack Query). DEFENSIBLE: §12.2 scope is `src/features/leads/` per AST-grep, not `src/components/`. Same pattern as existing `DataQualityDashboard.tsx`. | Future admin TanStack Query migration |
| HIGH | Adversarial | Test feed form uses `useState` for form fields — violates §12.3 (React Hook Form). DEFENSIBLE: same scope reasoning as above, admin-only form. | Future admin RHF migration |
| MEDIUM | Adversarial | Race condition: overlapping interval fetches can overwrite fresh data with stale. DEFENSIBLE: 10s interval + 10s AbortController timeout means at most one in-flight. Same pattern as DataQualityDashboard. | Future TanStack Query migration (fixes both) |
| MEDIUM | Adversarial | `performance` section of `LeadFeedHealthResponse` fetched but not rendered — spec goal "Is the feed responding?" unmet. Spec explicitly marks performance fields as "Phase A: null (not yet instrumented)". | Phase C — when performance metrics instrumented |
| MEDIUM | Adversarial | No client-side input validation on test feed form — NaN/out-of-range coords sent to server. Server Zod catches with 400 but no inline UX feedback. | UX polish WF |
| LOW | WF3 2026-04-22 Gemini+DeepSeek | `entity_id` in `get-lead-feed.ts` `RawLeadFeedRow` is typed `number \| null` but node-pg returns PostgreSQL BIGINT (`entities.id` is `int8`) as a string to avoid JS number precision loss above 2^53. Current builder IDs are small (< 10K) so the implicit coercion works silently, but a future high-volume import that generates IDs > 9007199254740991 would silently truncate. Fix: cast `e.id::int` in the SQL (safe if entity IDs stay under 2.1B) or update `RawLeadFeedRow.entity_id` to `number \| string \| null` and add a `toNumberOrNull`-style coercion in `mapRow`. Pre-existing; not introduced by this WF3. | Future types-hardening WF |
| LOW | WF3 2026-04-22 Gemini | `latitude` and `longitude` projected in `LEAD_FEED_SQL` are computed via `ST_Y(p.location::geometry)` / `ST_X(p.location::geometry)`. PostGIS returns these as `float8`; node-pg maps `float8` to JS `number` correctly. However, the `RawLeadFeedRow` interface types them as `number \| null` with no coercion, so a future schema change from `geometry` to `geography` (which changes how ST_X/ST_Y behave) or a NULL geometry row would produce `NaN` silently rather than `null`. Pre-existing; not introduced by this WF3. | Future hardening |
| MEDIUM | Adversarial | Polling test only verifies `setInterval` registration, never advances timers. Fake timers + async React rendering caused 5s timeout in initial implementation. | Future test hardening |
| LOW | Independent | `saves_today` fetched in engagement response but not rendered as stat box. Spec focuses on 7-day window; today's saves is complementary data. | UX enhancement |
| LOW | Independent | Retry button in error state has no loading feedback — button text stays "Retry" during fetch. | UX polish |
| LOW | Adversarial | GREEN traffic light fires when `timing_freshness_hours` is null (never calibrated). Spec requires `< 48` for GREEN, but null ≠ "< 48". DEFENSIBLE: null timing = "never calibrated" is a separate state; treating as non-stale is a product decision, locked by test. | Spec clarification if product disagrees |
| HIGH | Self (sibling bug) | `scripts/enrich-web-search.js` and `scripts/enrich-wsib.js` have the same observability gap — they emit `emitSummary` without a custom `audit_table`, so the admin UI shows only sys_* rows. Both ARE in active chains (`enrich_wsib_builders`, `enrich_named_builders`, `enrich_wsib_registry`). Scope discipline: user reported steps 14/15 only; fix siblings in a separate WF3. | Dedicated sibling WF3 |
| MEDIUM | Both | New audit_table tests are pure file-text regex matching — cannot catch row-construction bugs where string literals exist but the audit_table is in a dead code branch, a comment, or has transposed values. DEFENSIBLE: pre-existing pattern across all `*.infra.test.ts` file-shape tests. A logic-level test (stub `emitSummary`, assert on captured payload) would catch this class. | Future test hardening WF |
| LOW | Adversarial | `parseInt(r.sample_size, 10)` in `compute-timing-calibration.js` is unnecessary — `COUNT(*)::int` always returns JS number via pg driver. Harmless no-op defensive code. | Type hygiene |
| INFO | Self | Issue 3 (health endpoint 500) had NO code fix — resolution was applying migrations 070 + 076 + 079 manually to local DB. Production state may also need reconciliation if the same partial-apply occurred there. No test added (existing `lead-views-schema.infra.test.ts` already locks the expected schema, and it's covered by the migration files themselves). | DB reconciliation for prod if needed |
| MEDIUM | Adversarial | No integration test exercises the real 13-query SQL in `getLeadFeedReadiness`. A typo in a column reference would pass all existing file-shape tests. | Future test hardening WF (pre-existing pattern — matches project convention of file-shape tests for infra scripts) |
| MEDIUM | Both | Pool connection pressure: 14 parallel queries against default-10 `pg` pool every 10s poll, no cache. No immediate failure but can starve other routes under load. Fix options: (a) add 60s TTL cache on `/api/admin/leads/health`, (b) set explicit `max: 20` on poolConfig, (c) batch queries into a single SQL CTE | Future perf WF — add caching layer to admin endpoints |
| MEDIUM | Independent | `permits_feed_eligible` uses `latitude IS NOT NULL` as proxy for `p.location IS NOT NULL`. On PostGIS-absent environments (local dev without PostGIS), the trigger in migration 067 no-ops, so lat/lng can be populated while `location` stays NULL. The count will be INFLATED relative to what the feed actually returns on such envs | Add UI caveat OR switch to direct `location` check (blocks on PostGIS-absent envs) |
| LOW | Independent | No tests for traffic light at exactly `feed_ready_pct = 80` or `= 50` boundaries. Spec says GREEN > 80, YELLOW = 50-80, RED < 50 — boundary behaviour is unverified | Future test hardening |
| LOW | Self | Spec 76 §2.3 references the card as "Timing Calibration" but we relabeled to "Detail-Page Timing Engine". Spec should be updated or label reverted | Doc WF |
| LOW | Self | Feed-Path Coverage section has no explicit 375px mobile viewport test. Uses existing responsive patterns (flex-col md:flex-row, grid-cols-1 md:grid-cols-2) but untested | Future test hardening |
| INFO | Self | Test feed will still fail locally until PostGIS is installed (`permits.location` doesn't exist without the extension). The client error message now shows the real error (e.g., `column "p.location" does not exist`) instead of `[object Object]`, so the operator can diagnose. Install PostGIS: Scoop → `scoop install postgresql`, WSL2 → `sudo apt install postgis` + `CREATE EXTENSION postgis` | Dev env setup doc |
| MEDIUM | Adversarial (Probe 1) | If migration 093 hasn't run but scripts are deployed, the LEFT JOIN to `tc.multiplier_bid`/`tc.multiplier_work` in compute-opportunity-scores will hard-fail (column doesn't exist). Config loader fallback doesn't help — it's the main data query that fails. | Deploy runbook: enforce `npm run migrate` before script deployment |
| LOW | Adversarial (Probe 2) | If someone manually NULLs `multiplier_bid` in trade_configurations (bypassing NOT NULL DEFAULT), parseFloat(null) → NaN would silently produce NaN scores stored as NULL. Cannot happen post-migration since column is NOT NULL. | Defensive — no action needed unless NOT NULL constraint is dropped |
| LOW | Adversarial (Probe 5) | If operator sets `stall_penalty_precon=0` in logic_variables, stall recalibration is silently disabled. Seed is 45, fallback is 45 — only reachable via manual SQL UPDATE. | Doc: note in Admin UI that 0 disables stall penalty |
| INFO | Self-checklist | Spec 81/86 use `los_base_unit` but code/seed use `los_base_divisor` — naming drift between spec and implementation. Functionality is correct. | Spec doc update |
| LOW | Independent (F4) | SPEC LINK in compute-opportunity-scores.js and update-tracked-projects.js point to `lifecycle_phase_implementation.md` report instead of their canonical specs (81/82). | Doc cleanup |
| LOW | Independent (FAIL-1) | `compute_opportunity_scores.telemetry_tables` lists `trade_forecasts` but the script only UPDATEs one column — pre/post row-count diff will always show 0. Dashboard telemetry cosmetic issue. | Future WF2 — custom telemetry for UPDATE-only scripts |
| LOW | Adversarial (Probe 3) | Pre-existing: `opportunity_score` column on `expired` urgency rows is never recomputed (WHERE clause excludes them). Score from last non-expired computation persists. Existed before this WF2 but more visible now. | Future WF3 — scrub opportunity_score when urgency flips to expired |
| LOW | Adversarial (Probe 4 caveat) | CoA chain reclassifies lifecycle_phase but does NOT run the marketplace tail. CoA-triggered reclassification takes until next permits chain run (~24h) to propagate into trade_forecasts. | Document or wire tail to CoA chain |
| LOW | Adversarial (Probe 4) | `coa_stall_threshold = 0` means "feature off", not "flag immediately" — surprising UX. Not a bug, but undocumented in admin UI spec. | Admin UI copy when Control Panel card ships |
| LOW | Adversarial (Probe 5) / Convention | Project uses commented-out DOWN blocks. Adversarial flagged migration 094 as non-compliant with CLAUDE.md rule. Investigating reveals ALL project migrations (090-094) use the same commented pattern. | Spec clarification in CLAUDE.md §Backend Mode Rule 5 |
| INFO | User decision | v1 `compute_timing_calibration` removed from chain (Path A). `timing_calibration` table will go stale. `src/features/leads/lib/timing.ts` (spec 71 detail-page timing), `src/lib/admin/lead-feed-health.ts`, `scripts/refresh-snapshot.js` still read the stale table. | Future frontend WF — migrate timing.ts to phase_calibration |
| LOW | Gemini + DeepSeek | PostgreSQL `ROUND(numeric)` rounds half-away-from-zero → slight upward bias across large datasets vs round-half-to-even ("banker's rounding"). Current fix swaps truncation's downward bias for rounding's tiny upward bias. Statistically neutral approach would require `CASE` expression or custom function. | Defer — improvement over truncation regardless; consider in future recalibration WF if bias becomes observable in drift telemetry. |
| LOW | Gemini | Repo lacks `.gitattributes` — diff noise from CRLF/LF line-ending drift between contributors. | Defer — repo-wide concern, out of scope for H-W7. File as infra hardening WF1. |
| NIT | Gemini | `NULL::varchar AS permit_type` (L166, L244) cast is redundant when type can be inferred. | Defer — pre-existing pattern, cosmetic only. |
| INFO | Plan deviation | WF3-01 plan specified a `logic.test.ts` with data fixture asserting `ROUND(10.9)::int = 11` against a live DB. Repo convention (documented in `classify-lifecycle-phase.infra.test.ts` header) is regex-only infra tests; end-to-end correctness covered by separate SQL reproducer scripts in `scripts/quality/`. Shipped with ratio-based regex test; data-driven reproducer deferred. | Consider adding `scripts/quality/calibration-rounding-reproducer.sql` for manual DB-side verification in a future WF. |
| MEDIUM | Independent CK-3 | `src/lib/db/generated/schema.ts` Drizzle types not regenerated — still shows old PK and lacks `chain_id` column. | Defer — table is accessed only via raw SQL in this codebase; run `npm run db:generate` post-migrate in the deploy. File follow-up if Drizzle consumers are ever added. |
| HIGH | Gemini | PATCH endpoint hardcodes `cadence='Daily'` on INSERT path — could overwrite a pre-existing cadence. | Defer — pre-existing behaviour from migration 047, not introduced by WF3-02. File as independent hardening ticket. |
| HIGH | DeepSeek / Gemini | Admin API can only write global rows (`chain_id=NULL`); per-chain disable is inaccessible through UI. | Defer — explicit out-of-scope per WF3-02 plan; front-end work deferred to user's separate UI pass. Future WF1 to expose the knob. |
| HIGH | DeepSeek | Mixed-version rolling deploy could break if app binary ships before migration 095 applies. | Defer — operational concern common to all schema migrations; CLAUDE.md deploy model runs migrations before code. Document in runbook. |
| MEDIUM | Gemini + DeepSeek | Tests are shape-only (regex over source); no behavioural integration test that proves disable for coa chain does NOT affect permits chain. | Defer — codebase convention is regex-only infra tests per `classify-lifecycle-phase.infra.test.ts` header; SQL reproducer script is the established pattern for DB-level verification. Consider adding `scripts/quality/pipeline-schedules-chain-scope-reproducer.sql` in a future WF. |
| LOW | DeepSeek | `chain.logic.test.ts` regex-anchors `[chainId]` but not `$1` placeholder. | Defer — typo-level concern; existing test sufficient for the primary drift modes. |
| LOW | Gemini | No FK from `pipeline_schedules.chain_id` to a real `chains` table. | Defer — no `chains` table exists; CHECK constraint is sufficient for the 4 known chains. Consider if `chains` table is ever added. |
| MEDIUM | Independent C8 | Plan called for 3 behavioural fixtures in `compute-trade-forecasts.logic.test.ts` (e.g. `classifyUrgency(10,false,-90,7) → 'upcoming'`). Only shape tests landed because `classifyUrgency` is not exported from the script (would require pipeline.run restructuring outside this WF's scope). | Defer — codebase convention is regex-only infra tests + SQL/eval reproducer scripts; consider exporting `classifyUrgency` in a future refactor WF and adding the 3 fixtures then. |
| HIGH | DeepSeek | No validation on `imminent_window_days` from DB — could be `-1`, `999`, or non-numeric string. | Defer — generic concern across all numeric configs in `config-loader.js`; address as a single config-loader hardening WF3 (`Number.isFinite` + range checks for all numeric keys). The current `??` handles null/undefined; non-numeric strings would fail at the `<=` comparison loudly. |
| MEDIUM | DeepSeek | `imminentWindow > 30` makes the `upcoming` tier unreachable. | Defer — current fallback config maxes at 21 days; not reachable in practice. Add a spec note when an operator surface enables setting >30. |
| NIT | DeepSeek | Comment claims "callers should always pass the per-trade value" but the default parameter undermines this. | Defer — trivial wording polish. |
| HIGH | Gemini | "Connection leak if error occurs after chain lock acquired but before step loop" — `chainLockClient.release()` not called from the fatal handler. | Defer — `_pool.end()` in the fatal handler closes the underlying TCP session, which causes PostgreSQL to release ALL session-level advisory locks on that connection. No phantom DB-side lock. The pool client is also reaped on process exit. Code-quality concern (explicit `try/finally` would be cleaner) but not a correctness/leak bug. |
| WARN | Independent WARN-1 | Same finding as Gemini HIGH from a different angle: fatal handler omits explicit `chainLockClient.release()`. | Same disposition — defer; rely on `pool.end()` TCP-close to release. |
| MEDIUM | DELETE NOT EXISTS performance bottleneck risk | Defer — pre-existing query unchanged structurally by this PR (only wrapped in `withTransaction`). Index optimization is separate concern. | OPEN |
| MEDIUM | "Chain lock ID collision risk between different chain IDs (hashtext collision)" | Defer — collision space 2^32 vs 4 chain IDs ≈ negligible. The 2-arg fix already addresses the script↔chain collision; cross-chain hashtext collision behaviour is "two chains can't run concurrently" (annoying, not corrupting). | OPEN |
| WARN | Independent W-1 | Tests don't anchor `lockClient.release()` ordering BEFORE `return` on the lock-not-acquired path. A future refactor could leak a connection on every skipped run (the exact bug the canonical 84 pattern's L183-187 comment warns about). | Defer — production code is correct; gap is test-expressiveness. Add `expect(content).toMatch(/lockClient\.release\(\)[\s\S]{0,30}return/)` in a future test-hardening WF. |
| WARN | Independent W-B | Pre-existing: `updated += batch.length` in script 81 overcounts when `IS DISTINCT FROM` no-ops rows. PR-B did not introduce or change this. | Already tracked as 81-W5 in the holistic triage. Defer. |
| WARN | Independent W-1 | Cosmetic: in 83, `let processed=0` etc. declarations sit between lock-acquire-catch and main `try` opener. Cannot throw in practice (primitive inits) but deviates from canonical pattern. | Defer — pure cosmetic; structural follow-up if future linter enforces this pattern. |
| HIGH | Gemini | flushBatch N+1 row-by-row INSERTs (5000 round trips per batch) — performance bottleneck. | Defer — pre-existing issue, not introduced by PR-C. Tracked from holistic triage. Future bulk-UPSERT WF would address it (multi-row VALUES or UNNEST or COPY FROM STDIN). |
| LOW + NIT | DeepSeek | "Hard-coded model_version=1; magic number." | Defer — pre-existing concern, not introduced by PR-C. Already tracked from holistic triage. |
| CRITICAL | Gemini+DeepSeek | `pipeline.js:465` | `checkQueueAge` interpolates raw `options.where` SQL — injection vector if caller passes user data. Comment warns but library must be safe by default. |
| CRITICAL | DeepSeek | `config-loader.js:105` | `allocSum=0` division by zero during normalization. If all `allocation_pct` rows are 0, loop divides by zero, producing `Infinity`/`NaN` allocations throughout pipeline. |
| HIGH | Gemini+DeepSeek | `pipeline.js:40` | `PG_PASSWORD` defaults to `'postgres'` — security risk if env var omitted in staging/prod. |
| HIGH | Gemini | `pipeline.js:538` | `streamQuery` outer finally calls `client.release()` — if `pool.connect()` throws, `client` is undefined → secondary TypeError masks the original. Add `if (client)` guard. |
| HIGH | Gemini | `pipeline.js:353-399` | `parseInt` on PostgreSQL `bigint` columns in `captureTelemetry`/`diffTelemetry` silently truncates values > `Number.MAX_SAFE_INTEGER` for very active tables. |
| HIGH | Gemini | `config-loader.js:89` | `FALLBACK_TRADE_CONFIGS` assigned by reference (not deep-copied) — consumer mutation of nested objects corrupts the global fallback for subsequent calls. |
| HIGH | DeepSeek | `config-loader.js:78-86` | `parseFloat(c.allocation_pct)` etc with no `isFinite` guard — `NaN` propagates silently if DB returns NULL or non-numeric. `tradeConfigs` validation should mirror the `ZERO_IS_INVALID` logic already applied to `logicVars`. |
| HIGH | DeepSeek | `config-loader.js:120-135` | `ZERO_IS_INVALID` guards against 0 but not negative values. `expired_threshold_days < 0` is intended (days in past) but `los_base_divisor < 0` would flip multiplier sign; range constraints missing. |
| HIGH | DeepSeek | `pipeline.js:140-145` | Global mutable `_trackedNew/_trackedUpdated` — if pipeline module is `require`d in multiple scripts in same process (e.g., test runner), counters bleed across runs. |
| MED | Gemini | `pipeline.js:89` | `log.error` passes ctx directly to `JSON.stringify` — circular references in ctx throw, preventing the error from being logged. |
| MED | Gemini+DeepSeek | `pipeline.js:212` | `dq_null_rate_*` metric `value` stored as formatted string `'X.X%'` — can't be compared numerically by dashboards. |
| MED | DeepSeek | `pipeline.js:176` | `emitSummary` assigns `payload.records_meta = stats.records_meta` by reference then mutates `audit_table` — caller's original `stats` object is silently modified. |
| MED | DeepSeek | `config-loader.js:63` | No query timeout on `pool.query()` — DB hang blocks pipeline indefinitely. |
| MED | DeepSeek | `config-loader.js:78` | Duplicate `trade_slug` rows silently overwrite each other (last wins). |
| LOW | Gemini | `pipeline.js:422` | `quoteIdent` rejects valid hyphened identifiers (e.g. `my-table`). Low impact — all Buildo table names are `[a-z_]` only. |
| LOW | DeepSeek | `config-loader.js:78` | `imminent_window_days` not parsed to integer (`parseInt`) — left as raw DB string, downstream `<` comparisons may coerce unexpectedly. |
| LOW | Gemini+DeepSeek | `config-loader.js:138` | `ZERO_IS_INVALID` hardcoded set — new logic vars with same constraint must be manually added. `validateLogicVars` Zod schemas can encode this per-variable now. Migration of existing ZERO_IS_INVALID set to per-script schemas is future work. |
| NIT | DeepSeek | `config-loader.js:106` | In-place normalization mutation — modifies row-derived objects directly. Low risk now; add `Object.assign({}, tc, { allocation_pct: ... })` if caching added. |
| HIGH | Phase 3 ops | Wire `liar_gate_overrides` metric to alerting. Threshold: `liar_gate_overrides / permits_processed > 0.15` (>15% of permits hit the geometric override path) should page on-call — indicates either systematic permit cost under-reporting or a valuation model regression. `liar_gate_overrides` is already emitted in `audit_table` (metric row, status INFO). The counter is also written to `data_quality_snapshots.cost_estimates_liar_gate_overrides` each nightly run. | WF1 ticket: wire to Grafana/PagerDuty alert. Dashboard panel at `cost_estimates` step in pipeline chain. |
| MED | Phase 3 ops | `is_active` is a post-filter on `permit_trades` index scan (not index-pushed). Currently adds ~1M extra row reads per run (negligible at 243K permits). If `permit_trades` grows past ~5M rows, add a partial index: `CREATE INDEX CONCURRENTLY idx_permit_trades_active ON permit_trades (permit_num, revision_num) WHERE is_active = true`. | Follow-up migration 097 — only if query plan degrades. File WF3 when `Rows Removed by Filter` > 20 per probe on average. |
| LOW | Phase 3 ops | `model_version` is hardcoded to `MODEL_VERSION = 2` in the Muscle. When the surgical formula is updated, this constant must be bumped and a migration or backfill strategy agreed (V1 rows would coexist until re-processed). Document bump protocol in spec 83 §9 before next formula change. | Spec update WF2 — low urgency, next formula change triggers it. |
| MED | Adversarial | `compute-cost-estimates.js:234` | `trust_threshold_pct` is Zod-validated and in `ZERO_IS_INVALID` but never passed to Brain config. The logic_variables admin knob has zero effect on valuation. `liar_gate_threshold` correctly reaches the Brain; `trust_threshold_pct` does not. Fix: either pass `trustThresholdPct: logicVars.trust_threshold_pct` to Brain config, or remove the knob from Zod/ZERO_IS_INVALID if it's intentionally a duplicate of `liar_gate_threshold`. |
| MED | Adversarial | `cost-model-shared.js:282` | `rateRow.structure_complexity_factor \ |
| MED | Adversarial | `cost-model-shared.js` typedef | `trustThresholdPct` is in the `CostModelConfig` typedef but the Brain never reads it. Either remove it from the typedef or route the Liar's Gate to use it. Paired with the item above on `compute-cost-estimates.js`. |
| MED | Adversarial | `compute-trade-forecasts.js:455` | `computed_at = NOW()` in UPSERT conflict resolution. Within a single `pipeline.withTransaction`, PostgreSQL's `NOW()` is transaction-scoped so values are consistent within a run. Non-critical, but passing an explicit `RUN_AT` parameter (captured once after lock acquisition) would align with spec 47 §8 best practice. |
| MED | Adversarial | `compute-trade-forecasts.js:199-210` | `pool.query(...)` loads the full permit-trade join (~183K active rows) into memory as `permitTradeRows`. For the current data volume this is fine, but it violates spec 47 §9.2 streaming mandate for large datasets. If `permit_trades` grows, switch to `pipeline.streamQuery` + incremental accumulation. |
| LOW | Adversarial | `compute-trade-forecasts.js` | No SIGTERM handler. If the process is killed during the batch UPSERT, the advisory lock (now correctly 85) is orphaned until the session ends. Pattern: add `process.on('SIGTERM', ...)` with `lockClientReleased` flag, mirroring `classify-lifecycle-phase.js`. |
| LOW | Adversarial | `classify-permit-phase.js:45` | `last_seen_at = NOW()` in single UPDATE statement. Not a Midnight Cross risk (single statement, no batch loop) but deviates from spec 47 §8 `RUN_AT` convention. Low urgency — no cross-batch drift possible. |
| LOW | Adversarial | `classify-permit-phase.js:96` | `verdict: 'PASS'` hardcoded in audit_table. The script doesn't have failure detection — it could silently succeed with 0 rows updated and still report PASS. Add `verdict: examCount > 0 ? 'PASS' : 'WARN'` or a threshold check against expected minimum examination rate. |
| LOW | Adversarial | `classify-permit-phase.js` | No advisory lock. The single `UPDATE … IS DISTINCT FROM` is idempotent so concurrent runs produce no corruption, but two concurrent runs waste CPU and emit duplicate telemetry. Add `pg_try_advisory_lock` on a pinned client for consistency with spec 47. |
| MED | Adversarial | `run-chain.js` | No SIGTERM handler on the orchestrator itself. Killing the orchestrator mid-chain leaves the current child process running with no parent to collect telemetry. A partial `pipeline_runs` record with `status = 'running'` may stick permanently. Consider: register SIGTERM to kill the active child and mark the run as `failed` before exit. |
| MED | Adversarial | `compute-timing-calibration-v2.js` | Holistic audit flagged: (1) no `ZERO_IS_INVALID` guard on `min_sample_size` (0 would silently disable calibration quality filter); (2) `computed_at = NOW()` in batch loop (true Midnight Cross risk here unlike trade-forecasts — multiple `pool.query` calls across midnight); (3) no lockClientReleased double-release guard. |
| LOW | Adversarial | `update-tracked-projects.js` | Holistic audit flagged: (1) no stream try/catch around `for await` loop — a single bad row could abort the full run silently; (2) accumulates all project updates in memory before flush (similar to trade-forecasts memory concern). |
| MED | Adversarial | `cost-model-shared.js` | `premiumTiers` processed in insertion order. If tiers are loaded from DB with a non-ascending sort, the first matching band wins by insertion order, not by min value. Add explicit sort by `tier.min` ascending in `computePremiumFactor`. Affects TS path (`cost-model.ts` builds the tiers array) and pipeline path (pre-fetched from DB). |
| LOW | Adversarial | `cost-model-shared.js` | Negative `estimated_stories` from bad DB data produces a negative GFA. `computeGfa` checks `footprint_area_sqm > 0` but not `stories > 0`. Add `&& stories > 0` guard to the primary massing path. |
| LOW | Adversarial | `compute-cost-estimates.js` WAL guard | IS DISTINCT FROM covers 5 of the 15 upsert columns (estimated_cost, cost_source, is_geometric_override, effective_area_sqm, trade_contract_values::text). The remaining 10 columns (cost_tier, cost_range_low, cost_range_high, premium_factor, complexity_score, model_version, modeled_gfa_sqm, computed_at, cost_range_low, cost_range_high) always write on conflict even when unchanged. Low risk — these are derived fields that change whenever the main 5 change. Adding all 15 to the guard would over-complicate the SQL. |
| MED | WF3-12 | `compute-trade-forecasts.js:455` | `computed_at = NOW()` inside batch UPSERT (item 1128 — still open). Per-batch `withTransaction` makes this txn-scoped, but passing an explicit `RUN_AT` param would align with spec 47 §8. |
| LOW | WF3-12 | `compute-trade-forecasts.js` | No SIGTERM handler (item 1130 — still open). Advisory lock 85 orphaned if process is killed during flush. Add `process.on('SIGTERM', ...)` with `lockClientReleased` flag per canonical `classify-lifecycle-phase.js` pattern. |
| LOW | WF3-12 | `compute-trade-forecasts.infra.test.ts:1` | SPEC LINK header points to `docs/reports/lifecycle_phase_implementation.md (Phase 4)` — should be `docs/specs/product/future/85_trade_forecast_engine.md §6`. |
| LOW | WF3-12 | `src/tests/` | No `compute-trade-forecasts.logic.test.ts` — spec 85 §4 Testing Mandate requires a logic test covering bimodal routing, urgency classification, stall recalibration, and 4-level calibration fallback. |
| MED | Plan (deferred) | **Phase 7 Playwright E2E** — spec 86 §5 Phase 7 mandates a browser E2E test that logs in as Admin, alters a trade multiplier, clicks "Apply & Re-Sync", and asserts the DB + pipeline timestamps changed. Current WF1 shipped without it (Phase 7 is listed as out-of-scope). | Future WF1 (Playwright E2E for Control Panel) |
| MED | Plan (deferred) | **`trade_sqft_rates` → `trade_configurations` data migration** — spec 86 §2 prose implies `base_rate_sqft` and `structure_complexity_factor` live on `trade_configurations` ("cleanly merged"). They actually remain on `trade_sqft_rates` (migration 096). The Control Panel UI and GET/PUT routes JOIN the tables and present them as one unified view (correct for now), but the physical merge is unfinished. | Future WF2 on `trade_sqft_rates` merge |
| LOW | Plan (deferred) | **Biome scope expansion** — `biome.json` is currently scoped to `src/features/leads/`. The new `src/features/admin-controls/` module should be added to Biome's `include` paths for consistent lint coverage (hook order, floating promises, telemetry enforcement). | Next Biome config update |
| LOW | Plan (deferred) | **Migrate existing admin pages** — `src/app/admin/data-quality/`, `src/app/admin/market-metrics/`, `src/app/admin/lead-feed/` still use vanilla `useState` + `fetch` (no TanStack Query, no Zustand). The Control Panel is now the first fully-conformant admin page. Harmonize in a future WF2 sweep. | Future WF2 admin pages harmonization |
| LOW | Plan (deferred) | **`npm run migrate` + `npm run db:generate` + `npm run system-map`** — migration 097 is written and committed but has not yet been applied to the local or production DB. Drizzle types and system map will drift from the new `variable_value_json` column and 3 new `logic_variables` rows until these commands are run against a live DB connection. | Run locally before next DB-touching WF |
| LOW | Compliance + Adversarial | **Shadcn `<Tabs>`, `<Slider>`, `<Dialog>`, `<Drawer>` not used** — Phase 1 mandates `npx shadcn@latest add tabs/slider/dialog`; Phase 3 mandates Sliders for numeric vars; Phase 6 mandates Shadcn Dialog+Drawer for ConfirmSyncModal. Hand-rolled equivalents are in place (custom nav, number inputs, fixed-position div). Functionally correct; prescriptive primitive not used. | WF2 polish pass — Shadcn primitive alignment |
| LOW | Compliance | **`TradeGrid` missing UI test for inline-edit → store update** — Phase 4 spec requires tests asserting inline edits trigger store mutations. DeltaGuardInput is tested in isolation; TradeGrid end-to-end edit is not. | WF2 test hardening on TradeGrid |
| LOW | Independent | **`QueryClient` at module scope in `'use client'` admin page** — SSR cache-share risk if Next.js ever server-renders this boundary. Currently safe: no `dehydrate/hydrate` pattern used, TanStack docs accept this for client-only pages. Fix: move to `useState(() => new QueryClient(...))`. | WF2 hardening if SSR patterns are introduced |
| HIGH | Independent (pipeline audit Apr-2026) | **`PING_PONG_RATIO` spec vs operational conflict** — `assert-engine-health.js:30`. Spec §CQA-Tier3 says `> 2x`; operational baseline for healthy tables is 5–6x lifecycle updates per insert. Setting to 2 causes permanent chronic false-positive WARNs for all active tables. Current value: 10x (above natural baseline). Options: (a) move to `logic_variables`, (b) update spec to document 10x as correct, (c) per-table exclusion list. Spec owner decision needed before any change. | Spec 47 / logic_variables — spec owner decision |
| MED | Independent (pipeline audit Apr-2026) | **`assert-engine-health.js:150,158` — `CURRENT_DATE`/`NOW()` in snapshot loop violates §14.2** — Snapshot loop uses inline `CURRENT_DATE` for `snapshot_date` and `NOW()` for `captured_at` per-iteration. If loop crosses midnight, rows split across two days, breaking `ON CONFLICT (table_name, snapshot_date)` semantics. Fix: capture `RUN_AT` from DB before loop; pass `RUN_AT::date` and `RUN_AT` as bound params. | `scripts/quality/assert-engine-health.js` — WF3 |
| MED | DeepSeek + Independent (pipeline audit Apr-2026) | **`classify-lifecycle-phase.js` per-batch transitions INSERT lacks `ON CONFLICT`** — No UNIQUE constraint exists on `permit_phase_transitions(permit_num, revision_num, from_phase, to_phase)`, so `ON CONFLICT` cannot be added without a migration. Transaction atomicity mitigates duplicate risk, but a migration + `ON CONFLICT DO NOTHING` would be belt-and-suspenders. | Future migration + WF3 |
| MED | DeepSeek + Independent (pipeline audit Apr-2026) | **`assert-engine-health.js` early-exit `emitSummary` lacks `audit_table`** — When no lock is acquired or nothing is processed, `emitSummary` doesn't include `records_meta.audit_table`. SDK injects `{ verdict: 'UNKNOWN' }` which shows as green in the admin dashboard. Fix: add a `PASS` audit_table stub to all early-exit emitSummary calls, or add a convenience helper to the pipeline SDK. | `scripts/quality/assert-engine-health.js` + SDK — WF3 |
| LOW | Independent (pipeline audit Apr-2026) | **`load-coa.js` N+1 INSERT pattern** — `upsertBatch` runs one `client.query` per record in a for-loop (~33K sequential queries per full run). Refactor to multi-row VALUES INSERT (15 cols/row → max 4369 rows/batch = Math.floor(65535/15)). Medium effort; requires reworking `xmax`-based insert/update count logic. | `scripts/load-coa.js` — WF2 refactor |
| LOW | DeepSeek + Independent (pipeline audit Apr-2026) | **`link-massing.js` confidence mismatch PostGIS vs JS path** — PostGIS path: `confidence = 0.90` for `centroid_in_parcel`. JS fallback path: `confidence = 0.95` for equivalent match. Creates test vs production divergence. Fix: align both paths to spec-declared canonical value. | `scripts/link-massing.js` — WF3 after spec clarification |
| LOW | All agents (pipeline audit Apr-2026) | **SPEC LINK rot across all pipeline scripts** — All scripts point to non-existent spec files (`02_permit_sync.md`, `12_coa_integration.md`, `05_geocoding.md`, `11_builder_enrichment.md`, `28_data_quality_dashboard.md`). Update headers to actual spec paths after running `npm run system-map`. | All scripts in `scripts/` — housekeeping WF2 |
| LOW | All agents (pipeline audit Apr-2026) | **ADVISORY_LOCK_IDs don't match owning spec numbers for infrastructure scripts** — Per §5.2 lock ID should equal spec number. Infrastructure scripts (link-parcels=90, link-massing=91, load-coa=95, assert-engine-health=104, assert-data-bounds=103) use sequential IDs not matching spec 28/12/40. Strategy is correct (avoids lock collisions when multiple scripts share a spec) but violates §5.2. Options: (a) update §5.2 to allow sequential assignment, (b) give each script its own spec file. | Spec 47 §5.2 update — doc WF2 |
| LOW | DeepSeek (pipeline audit Apr-2026) | **`update-tracked-projects.js` notifications INSERT has no `ON CONFLICT DO NOTHING`** — Re-run after crash delivers duplicate notifications to users. Unbounded `updates[]`/`alerts[]` arrays also accumulate during stream (OOM risk on city-wide stall event). Fix: add `ON CONFLICT (user_id, permit_num, notification_type, created_at::date) DO NOTHING` + stream-flush the arrays. | `scripts/update-tracked-projects.js` — WF3 |
| LOW | DeepSeek (pipeline audit Apr-2026) | **`assert-schema.js` network failure produces false "schema drift"** — When CKAN API is unreachable, the script reports schema drift and halts the chain incorrectly (can't distinguish "can't reach CKAN" from "schema changed"). Fix: catch network errors separately and emit `SKIP` instead of `FAIL`. | `scripts/quality/assert-schema.js` — WF3 |
| NIT | All agents (pipeline audit Apr-2026) | **`'use strict'` missing from most pipeline scripts** — `classify-lifecycle-phase.js` has it; most others don't. Add `'use strict';` after shebang+docblock in all CommonJS scripts. Non-blocking. | All `scripts/*.js` — housekeeping |
| NIT | Independent (pipeline audit Apr-2026) | **RUN_AT captured inside `withAdvisoryLock` callback** — Per §14.1, should be captured before lock acquisition. Current drift is <1ms (lock acquires near-instantly). Not a correctness issue at current scale. | All scripts — systematic WF2 |
| LOW | Independent | **`triggerPipeline` fires even when `rows_updated: 0`** — if the diff is empty (race: edits discarded between setShowConfirm and confirm click), PUT succeeds with 0 rows, yet the pipeline fires. Confirm button `totalChanges === 0` guard prevents the most common case; the race window is narrow. Fix: skip `triggerPipeline` when `data.data.rows_updated === 0`. | WF2 hardening |
| CRITICAL | Gemini+DeepSeek | `lifecycle-phase.js` | **TRADE_TARGET_PHASE hardcoded business logic** — entire mapping belongs in `trade_configurations` DB table per §4.1. Changes require code deploys. |
| CRITICAL | Gemini+DeepSeek | `lifecycle-phase.js` | **Hardcoded stall thresholds 730/180/30/90 days** — `computeStalled`, `classifyBldLed`, `classifyOrphan` use hardcoded day constants. Must load from `logic_variables` per §4.1. `classifyCoaPhase` already does this correctly (proof the pattern is known). |
| CRITICAL | Gemini | `compute-trade-forecasts.js` | **DELETE + UPSERT in separate transactions** — stale-purge `DELETE` commits before forecast `UPSERT` batches. A crash between them leaves `trade_forecasts` empty. Violates spec §7.1/§7.3 atomicity requirement. |
| CRITICAL | DeepSeek | `lifecycle-phase.js` | **`classifyBldLed` returns `P18` when `has_passed_inspection = true`** — P18 = "Inspection Pipeline" but the permit has already passed; should be P17 (Final Inspection) or terminal. Potential misclassification of many permits. Needs cross-check with TS counterpart. |
| CRITICAL | Gemini | `link-coa.js` | **Advisory lock ID `93` ≠ spec link `12`** — spec line 25 has `SPEC LINK: docs/specs/pipeline/12_coa_integration.md` but `ADVISORY_LOCK_ID = 93`. Per §5.2 lock ID must equal owning spec number. Risk: silent lock contention with any future spec-93 script. |
| HIGH | Gemini | `assert-entity-tracing.js` | **`THRESHOLDS` and `TRACE_WINDOW` hardcoded** — business-tunable values (opportunity_score ≥ 80%, cost_estimate coverage ≥ 70%, trace window = 26h) hardcoded as `const`. Must load from `logic_variables` per §4.1. |
| HIGH | Gemini | `assert-entity-tracing.js` | **`cost_estimates` check uses `COUNT(*)` not `COUNT(DISTINCT)`** — table has one row per permit+trade; counting rows inflates the numerator when a permit has multiple trades. Fix: `COUNT(DISTINCT (ce.permit_num, ce.revision_num))`. |
| HIGH | Gemini | `assert-entity-tracing.js` | **`NOW()` called independently in 7 queries** — violates spec §14 (single DB-sourced `RUN_AT`). For a 26h window, clock drift between queries is low-risk today but will cause flaky assertions as runtime grows. |
| HIGH | Gemini | `link-coa.js` | **Tier 3 dry-run `catch` block is empty** — silently swallows DB errors during dry runs. `descErrors` increments but no `pipeline.log.warn` call. Violates `no-empty` principle. |
| HIGH | Gemini | `link-coa.js` | **`actualCandidates` dry-run metrics mislead** — in dry run, `crossWardCleaned` records are counted but not actually unlinked, so the subsequent tier queries (filtered `linked_permit_num IS NULL`) won't find them as candidates. Including them in `actualCandidates` deflates `matchRate`. Fix: `actualCandidates = totalUnlinked + (dryRun ? 0 : crossWardCleaned)`. |
| HIGH | Gemini | `compute-trade-forecasts.js` | **`anchorIsFallback` logic too broad** — `!phase_started_at` includes permits anchored to `last_passed_inspection_date`; Historic Snowplow then incorrectly fires for them, pushing valid forecasts far into the future. Fix: track `anchorSource` explicitly and only snowplow when `anchorSource === 'issued_date' \ |
| HIGH | Gemini | `compute-trade-forecasts.js` | **`overdueWindow`/`upcomingWindow` hardcoded at 30** — `classifyUrgency` defaults these to 30 but the script loads `urgency_overdue_days` / `urgency_upcoming_days` from `logicVars` and never passes them. Operator changes have no effect. |
| HIGH | DeepSeek | `lifecycle-phase.js` | **`PHASE_ORDINAL` P18 = P12 = 4** — Inspection Pipeline (P18) shares ordinal with Rough-In (P12). May cause incorrect progression comparisons in the flight tracker. Assign a distinct ordinal (e.g. 3.5 or renumber). |
| HIGH | DeepSeek | `link-coa.js` | **Pre-pass Tier 3 0.10 confidence inconsistency** — pre-pass unlinks cross-ward mismatches except when `linked_confidence = 0.10`, but Tier 3 can also produce 0.10 confidence with ward conflicts. Tier 1c ward-conflict matches are preserved as intended; Tier 3 ward-conflict 0.10 matches would be unlinked on re-run. Semantic inconsistency between tiers. |
| HIGH | Gemini | `assert-global-coverage.js` | **No `RUN_AT` startup timestamp** — uses `NOW()` and `CURRENT_DATE` in multiple queries. Violates §14 Midnight Cross protection. Script is read-only so no data corruption risk, but metrics lose determinism across day boundaries. |
| HIGH | Gemini | `assert-global-coverage.js` | **`misc` query has 17 independent sub-selects** — each is a separate scan/index scan. Severe performance anti-pattern at scale. Rewrite as `CROSS JOIN` on single-row aggregates. |
| HIGH | Gemini | `assert-global-coverage.js` | **`denominator \ |
| MED | Gemini | `assert-entity-tracing.js` | **`SKIP_PHASES_SQL` empty-guard missing** — current code imports the literal from `lifecycle-phase.js` (never empty). If the export ever changes to an array-derived value, `NOT IN ()` would be vacuously true. Add startup guard matching spec §4.3. |
| MED | Gemini | `link-coa.js` | **`LOGIC_VARS_SCHEMA` uses `.passthrough()`** — allows misspelled keys to pass silently as `undefined`, producing NaN later. Use `.strict()` to fail fast at startup. |
| MED | Gemini | `link-coa.js` | **`UPDATE` in `runTier` lacks `IS DISTINCT FROM` guard** — re-running with same data produces phantom writes, unnecessary WAL traffic, and incorrect `last_seen_at` bumps. |
| MED | Gemini | `compute-trade-forecasts.js` | **Forecast UPSERT lacks `IS DISTINCT FROM` guard** — every run rewrites all forecast rows even if unchanged. Inflates `records_updated` and WAL. |
| MED | Gemini | `compute-trade-forecasts.js` | **`finalCalMethod` stamped wrong for inspection-anchored permits** — consequence of `anchorIsFallback` bug above. Fix together. |
| MED | Gemini | `assert-global-coverage.js` | **`emitMeta` call is empty** — violates §8.3; breaks DataFlowTile data-lineage view. Populate with all tables read. |
| LOW | Gemini | `link-coa.js` | **`FTS_MAX_KEYWORDS` magic number `8`** — unexplained. Define as named constant with comment explaining the limit. |
| LOW | Gemini | `link-coa.js` | **CLI arg parsing `--dry-run` brittle** — `process.argv.includes('--dry-run')` fails for `--dry-run=true`. Use `minimist` or `yargs-parser`. |
| NIT | Gemini | `link-coa.js` | **`effective_match_rate_pct` rounding vs verdict discrepancy** — raw float used for status but rounded value displayed (e.g., shows 50.0% but status=FAIL at 49.98%). Use rounded value for status check. |
| HIGH | Independent + Gemini + DeepSeek | **`link-coa.js` + `load-coa.js` SPEC LINK points to `docs/specs/12_coa_integration.md`** — file does not exist. Pre-existing in both scripts. Requires creating the spec file or redirecting to `docs/specs/pipeline/51_source_coa.md`. | WF2 doc task |
| HIGH | Gemini + DeepSeek | **`preRowCount` captured AFTER grace+stale DELETEs** in `compute-trade-forecasts.js` — violates §8.1 "capture preRowCount BEFORE any DELETE". `records_new` in emitSummary underestimates new inserts. Pre-existing structural issue; now harder to fix since purge is inside `flushForecastBatch`. | WF3 architectural |
| HIGH | Gemini | **`records_total = upserted`** in `compute-trade-forecasts.js` emitSummary violates §11.1 (should be `totalRows` — rows streamed from source). Pre-existing. | WF3 §11 fix |
| HIGH | DeepSeek | **`classify-lifecycle-phase.js` captures `RUN_AT` and `loadMarketplaceConfigs` BEFORE `withAdvisoryLock`** — violates §R3.5/§R6 requiring RUN_AT to be the first query inside the lock. Pre-existing. Benign in practice (no side effects, config is read-only), but inconsistent with `compute-trade-forecasts.js` which correctly captures inside lock. | WF3 spec compliance |
| HIGH | DeepSeek | **Zero-stream path purges `trade_forecasts` but no CQA gate checks `total_forecast_rows > 0`** — if SOURCE_SQL returns 0 rows (WHERE regression or fresh DB), the table is emptied and audit_table emits PASS. Pre-existing data-safety gap. | WF1 CQA gate |
| MED | DeepSeek | **`link-coa.js` early-return (nothing to link) emits summary without `audit_table`** — SDK injects stub PASS, admin shows green even when CoA feed was empty due to ingestion failure. Pre-existing. | WF3 observability |
| MED | Gemini + DeepSeek | **`LIFECYCLE_CONFIG_SCHEMA` missing `.passthrough()`** in `classify-lifecycle-phase.js` — Zod strips unknown keys if `validateLogicVars` ever returns `result.data`. Latent inconsistency vs `LOGIC_VARS_SCHEMA` in `compute-trade-forecasts.js` which uses `.passthrough()`. | WF3 NIT |
| MED | Gemini | **`coa_stall_threshold` in `LIFECYCLE_CONFIG_SCHEMA` missing `.int()` guard** — allows fractional values while other four lifecycle vars use `.int().positive()`. Pre-existing. | WF3 NIT |
| MED | Gemini | **`undefined-phase` guardrail in `compute-trade-forecasts.js` uses `WARN` instead of `THROW`** — plan specified a warning; reviewers suggest a throw for schema regression detection. Design decision open. | WF2 design review |
| MED | Gemini + DeepSeek | **Advisory lock infra test `extractLockId` regex captures only first match** — fragile against scripts with commented-out stale lock IDs. Pre-existing. | WF3 test hardening |
| MED | Gemini | **Advisory lock test registry not machine-derived from spec §A.5** — manual sync required whenever a script changes its lock ID; drift risk. | WF2 tooling |
| LOW | DeepSeek | **`\ | \ |
| LOW | DeepSeek | **`SKIP_PHASES_SQL` string interpolation** in `compute-trade-forecasts.js` stale-purge violates §9 parameterization ban. Pre-existing structural constant; no injection vector. | WF3 §9 compliance |
| NIT | DeepSeek | **`link-coa.js` lock-not-acquired path** returns without calling `pipeline.emitMeta({}, {})` — `compute-trade-forecasts.js` emits empty META on skip; inconsistency. Pre-existing. | WF3 NIT |
| MED | Gemini | **`link-wsib.js:378` main-path `audit_table` phase=5 should be 7** per manifest position in permits chain. Pre-existing — early return was corrected to `sources?12:7` in WF2 sweep. | WF3 phase fix |
| MED | Gemini | **`link-parcels.js:564` main-path `audit_table` phase=7 should be 9** per manifest position in permits chain. Pre-existing — early return was corrected to `sources?6:9` in WF2 sweep. | WF3 phase fix |
| MED | Independent | **`compute-opportunity-scores.js:201,208` `{ raw: row.multiplier_bid }` log context** — verbatim match to §8.6 prohibited PII pattern. Pre-existing, not introduced by this sweep. | WF3 §8.6 |
| LOW | Independent | **`close-stale-permits.js:130` uses `CURRENT_DATE`** in SQL write (`COALESCE(completed_date, CURRENT_DATE)`) — §14.2 Midnight Cross pattern. Registry marks "Writes Timestamps? = NO" so low severity. | WF3 §14.2 |
| LOW | DeepSeek | **`link-coa.js`, `link-wsib.js`, `link-parcels.js` lock-not-acquired handlers** missing `emitMeta({},{})` — pre-existing inconsistency vs AP2 scripts that emit empty META. | WF3 NIT |
| LOW | DeepSeek | **`compute-trade-forecasts.js:138` no `{ skipEmit: false }`** — emits bare SKIP summary without `audit_table`, unlike sibling scripts. Pre-existing. | WF3 NIT |
| NIT | Gemini | **`link-wsib.js` name mismatch** — early return `'Link WSIB'` vs main path `'WSIB Registry Matching'`. Cosmetic inconsistency. | WF3 NIT |
| LOW | WF1 Two-Client 2026-04-22 Gemini+Independent | **Dead code survivors after Two-Client Architecture purge** — `npm run dead-code` will surface: `src/features/leads/lib/haptics.ts` (haptic utility, all consumers deleted), `src/features/leads/lib/haversine.ts` (distance util, PostGIS replaced all JS geo math), `src/features/leads/api/useLeadView.ts` (record-view hook, lead card deleted), `src/components/ui/OnboardingWizard.tsx` (tradesperson onboarding, page deleted), plus several `src/components/ui/` Shadcn primitives (Badge, Carousel, Tooltip, etc.) that only had leads-feature consumers. Run `npm run dead-code` and delete confirmed-unreachable modules in a dedicated WF2. | WF2 dead-code sweep |
| LOW | WF1 Two-Client 2026-04-22 DeepSeek | **Additional unused package.json dependencies** — DeepSeek identified ~9 packages with zero remaining Next.js consumer after Phase 1 deletions: `react-infinite-scroll-component` (already removed), plus likely `@tanstack/react-query-persist-client`, `@tanstack/query-async-storage-persister`, `idb-keyval`, `posthog-js`, `@turf/boolean-point-in-polygon`, `@turf/centroid`, `@turf/helpers`, `vaul` (Shadcn Drawer dep — keep if Drawer used in admin). Verify each via `npm run dead-code` before removing — some may still be used in admin or API routes. Remove confirmed-unused in dedicated WF2. | WF2 dependency sweep |
| HIGH | Mobile Ph3/4 2026-04-22 Ind+Gemini+DeepSeek | **`_layout.tsx` tab bar hide-on-scroll is dead code** — `tabBarOffset`/`tabBarStyle` computed via Reanimated `useDerivedValue`/`useAnimatedStyle` but `screenOptions.tabBarStyle` is a plain static object. The animated `translateY` never drives the actual tab bar. Requires replacing `screenOptions.tabBarStyle` with a custom `tabBar={(props) => <Animated.View style={tabBarStyle}><BottomTabBar {...props} /></Animated.View>}` prop. `mobile/app/(app)/_layout.tsx` lines 21-28. | Mobile hardening WF |
| HIGH | Mobile Ph3/4 2026-04-22 Ind+DeepSeek | **`OpportunityRing` colour keyed on `opportunityType`, not score thresholds** — spec §4.1 requires amber ≥80, green 50-79, zinc <50 on the 0-100 `relevance_score`. Component receives `opportunity_score` (0-20 pillar) and maps to colour via `opportunityType`. Two separate bugs: (1) wrong input field (should be `relevance_score`), (2) wrong mapping logic (type→colour vs threshold→colour). `mobile/src/components/feed/OpportunityRing.tsx` lines 24-29 + `LeadCard.tsx` line 51. | Mobile hardening WF |
| MED | Mobile Ph3/4 2026-04-22 Ind+Gemini | **`map.tsx` pan-to-refetch unimplemented** — spec §4.2 requires "400ms `onRegionChangeComplete` buffer before updating bounding box state and refetching leads." `map.tsx` does not pass `onRegionChangeComplete` to `LeadMapPane` and has no region state. Map always shows initial-location feed data regardless of pan. `mobile/app/(app)/map.tsx` line 64. | Map V2 WF |
| MED | Mobile Ph3/4 2026-04-22 Gemini | **`PHASE_INDEX` missing O1/O2/O3 orphan phases** — `computeTargetWindow` returns `'bid'` for any phase not in the index (`?? 0` fallback). Orphan permits (O2/O3 = active-construction equivalent of P8-P11) should map near mid-construction, not bid. No test covers orphan phases. `src/features/leads/lib/get-lead-feed.ts` lines 557-578. Fix: add `O1: 8, O2: 11, O3: 11` to `PHASE_INDEX`. | WF3 |
| MED | Mobile Ph3/4 2026-04-22 Gemini+DeepSeek | **`handleScroll` runs on JS thread, not UI thread** — `tabBarScrollY.value` / `tabBarVisible.value` are written from a JS-thread `onScroll` callback. Correct pattern is `useAnimatedScrollHandler` with `'worklet'` directive. On older Android devices the JS thread may be busy processing feed data, causing animation stutter. `mobile/app/(app)/index.tsx` lines 52-63. | Mobile perf WF |
| MED | Mobile Ph3/4 2026-04-22 DeepSeek | **iOS dark map style broken without `PROVIDER_GOOGLE`** — `customMapStyle` is silently ignored on iOS when the default Apple Maps provider is used. The zinc-950 dark style only renders on Android. Fix: `<MapView provider={PROVIDER_GOOGLE} ...>` with Google Maps API key configured for iOS in `app.json`. `mobile/src/components/feed/LeadMapPane.tsx` lines 67-70. | Map V2 WF |
| MED | Mobile Ph3/4 2026-04-22 DeepSeek | **Cold-boot auth race — `apiClient.ts` has no 401 token-refresh retry** — on expired token, TanStack Query retries twice with the same stale token (3x 401). No `getIdToken(true)` force-refresh before retry. Users who reopen the app after 1h see the feed error state until the `onIdTokenChanged` listener re-fires. `mobile/src/lib/apiClient.ts` line 66. | Mobile hardening WF |
| MED | Mobile Ph3/4 2026-04-22 DeepSeek | **`mmkvPersister.restoreClient` unsafe cast** — `JSON.parse(raw) as PersistedClient` performs no shape validation. Stale MMKV data from a previous app version could corrupt the hydration pipeline. Should validate minimum shape (`clientState` key presence) before accepting. `mobile/src/lib/mmkvPersister.ts` line 18. | Mobile hardening WF |
| MED | Mobile Ph3/4 2026-04-22 DeepSeek | **`contracts.ts` `coord_precision: 1000` vs `useLocation.ts` `SNAP_FACTOR = 500`** — two divergent definitions of the same constant. `useLocation.ts` never reads from `CONTRACTS`. Also: the snap is applied symmetrically in degree-space so longitude snap at Toronto latitude (~43.65°) is actually ~623m, not 500m. `mobile/src/constants/contracts.ts:30` vs `mobile/src/hooks/useLocation.ts:15`. | Mobile hardening WF |
| LOW | Mobile Ph3/4 2026-04-22 Ind | **`useSaveLead` error toast missing** — `onError` restores snapshot but emits no user-visible feedback. Spec §4.4 requires a NativeWind error toast on save mutation failure. `mobile/src/hooks/useSaveLead.ts` lines 54-60. | Mobile hardening WF |
| LOW | Mobile Ph3/4 2026-04-22 Ind | **`save-heart-filled` testID uses `leadId` not `index`** — Maestro asserts `id: "save-heart-filled-0"` but `SaveButton.tsx` renders `testID={\`save-heart-filled-\${leadId}\`}` (e.g. `save-heart-filled-permit-23-145678-BLD--01`). E2E save-confirmation test will always fail. `mobile/src/components/shared/SaveButton.tsx` line 45. | Mobile hardening WF |
| LOW | Mobile Ph3/4 2026-04-22 Ind | **`LeadCard` address text-sm vs spec text-base** — spec §4.1 prescribes `text-base leading-tight` for the address line. Component uses `text-sm`. `mobile/src/components/feed/LeadCard.tsx` line 42. | Mobile hardening WF |
| LOW | Mobile Ph3/4 2026-04-22 Ind | **`LeadCard` outer Pressable missing `min-h-[44px]`** — spec §6.3 requires all interactive elements ≥44px touch target ("non-negotiable for field use with work gloves"). `mobile/src/components/feed/LeadCard.tsx` line 36. | Mobile hardening WF |
| LOW | Mobile Ph3/4 2026-04-22 Ind | **`LeadCard` missing Reanimated spring scale on press** — spec §4.1 prescribes `withSpring(0.97, { stiffness: 400, damping: 20 })` on `onPressIn`. Component uses a plain `Pressable` with only `active:bg-zinc-800/70`. `mobile/src/components/feed/LeadCard.tsx`. | Mobile hardening WF |
| LOW | Mobile Ph3/4 2026-04-22 Ind | **`LeadCard` target_window badge copy mismatch** — `target_window === 'bid'` renders `"Bid Window"` but spec §4.1 locks the label to `"💎 Early Bid"`. `mobile/src/components/feed/LeadCard.tsx` line 74. | Mobile hardening WF |
| LOW | Mobile Ph3/4 2026-04-22 Ind | **`LeadCard` competition_count renders inline text not "👁 {n} Tracking" pill** — spec §4.1 requires a styled `bg-zinc-800 border border-zinc-700` pill with the eye emoji. Current code renders `· {n} watching` as plain inline text. `mobile/src/components/feed/LeadCard.tsx` line 99. | Mobile hardening WF |
| LOW | Mobile Ph3/4 2026-04-22 Ind | **`LeadCardSkeleton` missing 56×56 ring placeholder** — spec §4.4 requires a `bg-zinc-800 rounded-full` bone at the OpportunityRing position. `mobile/src/components/feed/LeadCardSkeleton.tsx`. | Mobile hardening WF |
| LOW | Mobile Ph3/4 2026-04-22 Ind | **"offline" empty state unreachable** — `EmptyFeedState reason="offline"` exists but `index.tsx` never checks network state. Feed only shows `no_results` or `unreachable`. Requires `@react-native-community/netinfo` integration. `mobile/app/(app)/index.tsx`. | Mobile hardening WF |
| LOW | Mobile Ph3/4 2026-04-22 Gemini | **SQL `$9` undocumented in parameter comment block** — `LEAD_FEED_SQL` comment header documents `$1–$8` only; `$9 = user_id` added in Phase 3 was never added to the header. A developer counting from the header would produce a broken 8-param array. `src/features/leads/lib/get-lead-feed.ts` lines 53-65. | WF2 doc fix |
| LOW | Mobile Ph3/4 2026-04-22 DeepSeek | **Coordinate type-guard after filter** — `LeadMapPane.tsx` uses `as number` cast on lat/lng after a `.filter()` that excluded nulls. TypeScript doesn't narrow through `filter` predicates on object properties. Replace with a type predicate: `.filter((p): p is PermitLeadFeedItem & { latitude: number; longitude: number } => ...)` to eliminate the unsafe cast. `mobile/src/components/feed/LeadMapPane.tsx` lines 62-64. | Mobile hardening WF |
| LOW | Mobile Ph3/4 2026-04-22 DeepSeek | **`useLeadFeed` `params!` non-null assertion** — `queryFn: ({ pageParam }) => fetchPage(params!, ...)` lies to the compiler. If TQ ever calls `queryFn` when `enabled:false` (theoretical), it crashes. Replace with `if (!params) throw new Error(...)` guard. `mobile/src/hooks/useLeadFeed.ts` line 41. | Mobile hardening WF |
| NIT | Mobile Ph5/6 2026-04-22 Adversarial | **`notification_type` enum spec drift** — spec §6.1 example shows `PHASE_CHANGED` but all Phase 6 code (dispatch, toast, settings badge) uses `LIFECYCLE_PHASE_CHANGED`. Code is internally consistent; spec example needs correcting. No code change required. | Spec doc WF2 — `docs/specs/03-mobile/92_mobile_engagement_hardware.md §6.1` |
| MED | Mobile Ph5/6 2026-04-22 Independent | **`FlightCard` GestureDetector + Pressable event-bus conflict on Android** — RNGH v2 dispatches swipe gestures via a JS-thread recognizer that can collide with RN `Pressable`'s built-in touch handler. On low-end Android devices, a swipe that begins on the card's press area may fire both the `onPress` (navigation) and the swipe gesture simultaneously. Fix: wrap `FlightCard`'s `Pressable` in `<GestureDetector gesture={nativeViewGesture}>` or use `simultaneousHandlers`. | Mobile hardening WF |
| MED | Mobile Ph5/6 2026-04-22 All | **`device_tokens` table unbounded growth** — push tokens rotate silently when a user reinstalls the app or signs in on a new device. Old tokens accumulate in `device_tokens` and generate `DeviceNotRegistered` errors from Expo on every dispatch. Fix: poll Expo Push API receipts endpoint (`/push/getReceipts`) and DELETE stale tokens. Alternatively add an `expires_at` column with a nightly cleanup job. `migrations/107_device_tokens.sql`. | Future WF1 — push token hygiene job |
| HIGH | Mobile Ph5/6 2026-04-22 Gemini+DeepSeek | **`callExpoPushApi` swallows Expo delivery errors** — Expo returns `DeviceNotRegistered` and `MessageTooBig` errors in the HTTP response body (not via HTTP status codes). `callExpoPushApi` resolves the raw response string without parsing it; failed deliveries are silently dropped. Fix: parse `JSON.parse(data).data[]` for `status: 'error'` + `details.error === 'DeviceNotRegistered'`, then DELETE the stale token from `device_tokens`. `scripts/classify-lifecycle-phase.js` `callExpoPushApi`. | Future WF — push error handling |
| MED | Mobile Ph5/6 2026-04-22 Independent | **Expo two-step delivery receipt not polled** — Expo's push pipeline is async: `/push/send` returns tickets, final delivery status requires polling `/push/getReceipts` after ~5 minutes. Phase 6 only sends and never checks receipts. `DeviceNotRegistered` errors surfaced via receipts would be missed entirely. Fix: add a nightly `poll-push-receipts.js` pipeline step. | Future WF — push observability |
| MED | Mobile Ph5/6 2026-04-22 Independent | **`dispatchPhaseChangePushes` N+1 query pattern** — for each permit in a 9362-row batch, a separate `pool.query(...)` fires to look up device tokens. At current user count (< 1000 users) negligible. At scale (100K+ users × 9K permits), could add 9M+ sequential queries per classification run. Fix: batch look-ups into a single `WHERE lv.permit_num = ANY($1) AND lv.revision_num = ANY($2)` IN-clause query outside the loop. `scripts/classify-lifecycle-phase.js` `dispatchPhaseChangePushes`. | Future perf WF |
| MED | Mobile Ph5/6 2026-04-22 DeepSeek | **`hasAskedPermission` gate can't re-prompt after OS denial** — `SaveButton.tsx` calls `markAskedPermission()` before showing the modal; if the user denies the OS dialog, the MMKV gate is already set and the in-app pre-prompt will never show again (even after the user re-grants in Settings). Fix: on app foreground, check `requestPermissionsAsync()` status; if `granted` without the MMKV flag set (user granted in Settings independently), call `registerPushToken()` directly. | Mobile hardening WF |
| LOW | Mobile Ph5/6 2026-04-22 Independent | **`NotificationToast` timer fires while app is backgrounded** — 4-second auto-dismiss `setTimeout` runs regardless of app state. If the app backgrounds mid-count, `onDismiss` fires after the app resumes and clears the toast. No user impact today, but adding `AppState.addEventListener('change', ...)` to pause/resume the timer would be more correct. `mobile/src/components/shared/NotificationToast.tsx`. | Mobile hardening WF |
| LOW | Mobile Ph5/6 2026-04-22 Gemini | **`incrementUnread()` fires even when user is on the flight board** — `NotificationHandlers.tsx` calls `incrementUnread()` for `LIFECYCLE_PHASE_CHANGED` / `LIFECYCLE_STALLED` notifications regardless of which screen is active. A user actively viewing the flight board gets an unread badge on the tab that is already open. Fix: read current route segment and skip `incrementUnread()` when `segments[1] === 'flight-board'`. | Product/UX decision — accept or fix in Phase 7 |
| LOW | Mobile Ph5/6 2026-04-22 Independent | **`registerPushToken` called on every auth gate re-render** — `useEffect` in `AuthGate` calls `registerPushToken()` whenever `user` / `segments` / `_hasHydrated` change (i.e. any route navigation after login). The MMKV stored-token deduplication prevents re-hitting the API, but the function is invoked repeatedly. Add a module-level `registered` boolean flag or a React `useRef` session guard. `mobile/app/_layout.tsx` line 44. | NIT — low priority |
| LOW | Mobile Ph5/6 2026-04-22 Gemini | **`notification_prefs` JSONB has no schema version** — if a new preference key is added in a future phase (e.g. `market_report: true`), the GET default-object and the PATCH merge both work correctly for new rows. But existing rows that never hit the PATCH endpoint won't have the new key; GET returns the stored JSONB without the new field. Requires either a migration backfill or a defensive `??` in each consumer. | Future migration hardening |
| LOW | Mobile Ph5/6 2026-04-22 Adversarial | **No Maestro E2E for background-tap routing** — `mobile/maestro/notifications.yaml` tests the foreground permission flow only. Background tap (app minimized → tap push notification → expect navigation to `flight-board` + job detail) is not tested. Add a second Maestro file `mobile/maestro/notifications_background_tap.yaml` in a future test WF. | Future test WF — Maestro |
| LOW | Mobile Ph5/6 2026-04-22 Gemini | **Settings screen `trade_slug` + radius not persisted to server** — the settings screen reads/writes `notification_prefs` (schedule, alert toggles, cost tier) but trade slug and search radius are local-only (MMKV state via `useFilterStore`). Spec §2.1 describes profile-level preferences on the server but the `user_profiles.notification_prefs` JSONB schema (migration 108) does not include these fields. Either clarify scope in spec §2.1 or extend the schema. | Spec clarification WF — `docs/specs/03-mobile/92_mobile_engagement_hardware.md §2.1` |
| DEFERRED | Mobile Ph5/6 2026-04-22 | **Offline "Basement Mode"** — all three reviewers flagged the absence of offline optimistic saves, offline queue, and background sync. Explicitly Phase 7 scope per the active task plan. No code change required now. | Phase 7 WF1 |

## 3. Mobile Ph4-7 Follow-ups (2026-04-23 three-reviewer adversarial pass)
_Applied fixes committed separately; these items are lower-priority deferrals from the same review pass._

| Severity | Source | Item | Planned Home |
|----------|--------|------|--------------|
| HIGH | Mobile Ph4-7 2026-04-23 DeepSeek+Independent | **`[lead].tsx` Detailed Investigation View unimplemented** — the lead detail route is a 13-line Phase 3 stub (header + permit ID placeholder). Spec 91 §4.3 mandates a full screen with OpportunityRing overlay, Street View via expo-image, cost tier, sq_footage, predicted_start (font-mono amber), neighborhood profile, and bottom-anchored SaveButton. `mobile/app/(app)/[lead].tsx`. | Phase 8 or dedicated WF1 |
| HIGH | Mobile Ph4-7 2026-04-23 DeepSeek+Gemini | **`[flight-job].tsx` cold-boot from notification shows "Job not found"** — the detail screen reads `item` from `useFlightBoard()` cache. If the user deep-links via a push tap before the board query loads (cold boot), `data` is undefined and the empty state fires. Spec 77 §3.3 specifies a dedicated `useFlightJobDetail` query with its own endpoint. | WF1 — new hook + API route |
| HIGH | Mobile Ph4-7 2026-04-23 DeepSeek+Independent | **Amber "newly updated" flash is dead code** — `FlightCard.hasUpdate` prop + 2s fade animation exist but `FlightBoardItem` schema has no `updated_at` field, `flight-board.tsx` `renderItem` never passes `hasUpdate`, and no 60s-since-foreground compute exists. Spec 92 §4.4 card-level unread indicator is fully unwired. Requires backend: add `permits.updated_at` to the flight-board SELECT; client: compute `hasUpdate` based on app-foreground time. | WF1 — schema + client |
| MED | Mobile Ph4-7 2026-04-23 Independent | **`FilterTriggerRow` active-count label not amber** — spec 91 §4.1 requires the button text to change from `"Filters"` to `"Filters · {n}"` in `text-amber-400` when `activeFilterCount > 0`. Current implementation keeps static `"Filters"` text and adds a separate amber count badge dot. `mobile/src/components/feed/FilterTriggerRow.tsx`. | Mobile UI polish WF |
| MED | Mobile Ph4-7 2026-04-23 Gemini+DeepSeek | **`onlineManager` cold-boot state is `false` until first NetInfo event** — NetInfo's `addEventListener` doesn't fire an initial synchronous state (100-500ms on Android). During this window `onlineManager.isOnline()` returns `false`, pausing queries even on a connected device. Fix: call `onlineManager.setOnline(true)` after `setEventListener` as a provisional state, and/or use `NetInfo.fetch()` to seed the initial state. `mobile/src/lib/queryClient.ts`. | Mobile hardening WF |
| MED | Mobile Ph4-7 2026-04-23 DeepSeek+Gemini | **`useNetInfo` Jest mock is static — `setConnected()` does not trigger React re-renders** — the mock returns a plain object computed at call time; it does not subscribe to `_listeners` or use React state. Tests that render `OfflineBanner` and call `setConnected(false)` expect the banner to appear but get a stale initial snapshot. Fix: convert the hook to use `useState` + `useEffect` subscription to `_listeners`. `mobile/__mocks__/@react-native-community/netinfo.js`. | Mobile test hygiene WF |
| MED | Mobile Ph4-7 2026-04-23 Independent | **No `dark:` NativeWind variants anywhere** — Spec 90 §2 mandates System Dark Mode support via `dark:` class variants on all UI components. The entire app currently uses static dark colors (`bg-zinc-950`, `text-zinc-100`) without conditional variants. Not a functional bug (app is dark-only by design), but a spec compliance gap. | Phase 8 |
| MED | Mobile Ph4-7 2026-04-23 Independent | **Sentry + PostHog not wired** — Spec 90 §13 mandates Sentry for crash reporting and PostHog for product telemetry (`captureEvent` on every user interaction). No `Sentry.captureException` or `captureEvent` calls exist in the mobile source tree. `captureEvent('job_removed_from_board')` in particular is commented out in `useRemoveFromBoard.ts:41`. | Phase 8 observability WF |
| LOW | Mobile Ph4-7 2026-04-23 DeepSeek | **`FlightCard` urgency badge shows negative day count for overdue predicted_start** — `⚡ {Math.ceil(...)} DAYS` with no `Math.max(0, ...)` floor; a permit predicted 2 days ago renders `"⚡ -2 DAYS"`. Spec 77 §3.2 shows only positive cases. Fix: clamp at 0 or change badge to `⚠ OVERDUE` for negative values. `mobile/src/components/feed/FlightCard.tsx` line 126. | Mobile UI polish WF |
| LOW | Mobile Ph4-7 2026-04-23 Gemini | **`[flight-job].tsx` percentage string cast may break on Android** — `'${rangeLeft.value * 100}%' as unknown as number` in `useAnimatedStyle` worklet. RN StyleSheet validates `left`/`width` as numbers; string percentages work on iOS but have inconsistent Android behavior in Reanimated v3. Fix: compute absolute pixel offsets with the parent container width, or use `transform: [{ translateX: ... }]` with a fixed-width container. | Cross-platform hardening WF |
| LOW | Mobile Ph4-7 2026-04-23 DeepSeek | **Push token not re-registered on cold boot for already-authenticated users** — `AuthGate` in `_layout.tsx:44` only calls `registerPushToken()` on the auth-group → app-group transition. Returning authenticated users (the common case) skip this branch. If the Expo Push Token rotates (OS upgrade, reinstall), the server never learns. MMKV dedup makes a cold-boot call safe. Fix: call `registerPushToken()` unconditionally when `user && _hasHydrated`. | Mobile hardening WF |
| LOW | Mobile Ph4-7 2026-04-23 Independent | **`LeadCard` missing Reanimated spring `onPressIn`/`onPressOut`** — spec 91 §4.1 requires `withSpring(0.97, { stiffness: 400, damping: 20, mass: 1 })` scale animation on press. Current implementation uses NativeWind `active:bg-zinc-800/70` class — a background tint only, no scale animation. Animation polish. | Mobile UI polish WF |
| LOW | Mobile Ph4-7 2026-04-23 Independent | **`LeadCardSkeleton` pulse uses `withRepeat(withTiming, -1, true)` instead of spec's `withRepeat(withSequence(withTiming(0.4...), withTiming(1.0...)), -1, false)`** — visual output is similar (both reverse between 0.4 and 1.0 opacity) but spec is prescriptive about the explicit sequence form. `mobile/src/components/feed/LeadCardSkeleton.tsx`. | Mobile UI polish WF |
| LOW | Mobile Ph4-7 2026-04-23 Independent | **`FilterTriggerRow` container styling deviates from spec** — spec 91 §4.1 requires `bg-zinc-900 border-b border-zinc-800 px-4 py-2`. Actual uses transparent bg, `border-zinc-800/50`, `py-3`. Minor visual deviation. | Mobile UI polish WF |
| LOW | Mobile Ph4-7 2026-04-23 Gemini | **`dispatchPhaseChangePushes` SQL does not filter NULL/empty `push_token`** — a user whose push token registration partially failed has NULL or empty `push_token` in `device_tokens`. These rows get joined and pushed as `{ to: null }` to the Expo Push API (which returns per-message error tickets that `callExpoPushApi` already discards — see prior review_followup). Defensive filter: `AND dt.push_token IS NOT NULL AND LENGTH(dt.push_token) > 0`. `scripts/classify-lifecycle-phase.js` both dispatch functions. | Future WF — defensive push hardening |
| LOW | Mobile Ph4-7 2026-04-23 Independent | **`LeadMapPane` super-cluster not implemented** — spec 91 §4.2 names `react-native-maps-super-cluster` for clustering; current implementation uses a MARKER_CAP=50 hard limit. At scale (500+ pins in a dense region), users will see a partial view with no clustering. Not a blocker today but blocks growth. | Phase 2 map WF |
| LOW | Mobile Ph4-7 2026-04-23 WF6-revert | **Spec 90 §12 `estimatedItemSize` MUST references FlashList v1 API** — FlashList v2 (installed) removed this prop in favor of automatic sizing. The prop is a typecheck error in v2. Update Spec 90 §12 to say "FlashList v2 auto-sizes; do not set `estimatedItemSize`." Fix attempt was reverted during WF6 after `tsc` flagged TS2322 on both `<FlashList>` call sites. | Spec update WF |

## 4. Design-Audit Remediation Follow-ups (2026-04-23 three-reviewer adversarial pass on the 13 executed items)
_13 items executed as bundled WF2 + WF3 were adversarial-reviewed by DeepSeek, Gemini, Independent. Critical/important gaps were WF3'd inline. These items are lower-priority deferrals from the same review pass._

| Severity | Source | Item | Planned Home |
|----------|--------|------|--------------|
| HIGH | Design-audit Ph4-7 2026-04-23 Independent | **`[lead].tsx` Core Project Intelligence fields absent** — spec 91 §4.3 names four required sections: Street View Image (via `expo-image`), Square Footage Projection (`sq_footage`), Target Start Date (`predicted_start` in `font-mono text-amber-500`), Neighborhood Profile (income tier, area trends, surrounding active permits count). Schema `PermitLeadFeedItemSchema` has NONE of these fields (no `sq_footage`, no `predicted_start`, no `income_tier`, no active-permits-nearby count). The executed `[lead].tsx` rebuild rendered what the schema exposed; the missing data model is a WF1 cross-domain task: extend `PermitLeadFeedItemSchema` + backend `/api/leads/feed` projection. | WF1 cross-domain (schema + API + client) |
| HIGH | Design-audit Ph4-7 2026-04-23 Independent | **`[flight-job].tsx` contextual data thin** — spec 77 §3.3 requires cost, sq_footage, neighborhood profile on the investigation detail. `FlightBoardItemSchema` only carries `permit_num`, `revision_num`, `address`, `lifecycle_phase`, `lifecycle_stalled`, `predicted_start`, `p25_days`, `p75_days`, `temporal_group`. Full detail would require a discrete `useLeadDetail(id)` hook + new `/api/leads/detail/:id` endpoint that joins permit + cost + neighborhood data. | WF1 cross-domain (new endpoint + hook) |
| HIGH | Design-audit Ph4-7 2026-04-23 Gemini+DeepSeek | **`@react-native-community/slider` used but absent from `package.json`** — `settings.tsx` imports `Slider from '@react-native-community/slider'` but the package is not in `dependencies`. Currently resolving transitively (fragile). Predates this WF but surfaced during typecheck sweep. Fix: `npx expo install @react-native-community/slider`. | Mobile package hygiene WF3 |
| MED | Design-audit Ph4-7 2026-04-23 Independent | **Tab bar unread indicator is a native badge (count), not the spec's custom 10px styled dot with halo** — spec 92 §4.4 shows `w-2.5 h-2.5 bg-red-500 border border-zinc-900`. Current implementation uses React Navigation's `tabBarBadge` API (native count chip). Fix requires replacing `tabBarBadge` with a custom `tabBarIcon` render function that wraps the default icon and overlays a styled dot in the top-right corner. WF2-sized. | Mobile UI polish WF2 |
| MED | Design-audit Ph4-7 2026-04-23 Independent | **`captureEvent('job_removed_from_board')` never fires** — spec 77 §5 requires PostHog telemetry on confirmed DELETE (not on optimistic remove in case of undo). `posthog-react-native` is in `package.json` but no `captureEvent` call site exists anywhere in the mobile app. Phase 8 observability WF will wire the full telemetry layer. | Phase 8 observability WF |
| MED | Design-audit Ph4-7 2026-04-23 Gemini | **`getQueriesData({ queryKey: ['lead-feed'] })` prefix match hits concurrent filter caches** — during a radius change, the old feed query persists in cache (until `gcTime` evicts) while the new query fires. `[lead].tsx` walks both and returns first-hit. Same `lead_id` may appear in both with different `distance_m` values. Fix: read current `{ lat, lng, tradeSlug, radiusKm }` from `useFilterStore` and use an exact key match. | WF3 hardening (post-observation) |
| LOW | Design-audit Ph4-7 2026-04-23 Independent | **`SearchPermitsSheet` snap points `['60%', '90%']` vs spec-generic sheet template `['50%', '85%']`** — spec 77 §3.1 does not name snap points for the Search sheet specifically. Defensible deviation but non-canonical. | Mobile UI polish |

## 5. Phase 8.0 Pre-Test Gauntlet Follow-ups (2026-04-23 three-persona × seven-review audit)
_From the 7-agent audit (4 micro + 3 macro + 2 automated guards). Critical/important fixed inline; below are lower-priority deferrals._

**2026-04-23 follow-on WF3:** 7 items from this section were executed in a subsequent batch (commit to follow). Items marked ✅ RESOLVED below.

| Severity | Source | Item | Planned Home |
|----------|--------|------|--------------|
| HIGH | Phase 8.0 DirtyData DeepSeek | **401 → token refresh retry not wired** — `fetchWithAuth` throws `ApiError(401)` without calling Firebase `getIdToken(true)` + retrying. User's session silently breaks for up to 60 minutes after expiry. Requires Firebase Auth SDK integration (currently stub). | WF1 when Firebase Auth wiring completes (Phase 1b) |
| ✅ RESOLVED | Phase 8.0 BasementMode Designer | **`NotificationPermissionModal` is a centered RN `<Modal>`** → migrated to `@gorhom/bottom-sheet` (snap points `['40%']`), `successNotification()` on Allow, `lightImpact()` on Maybe Later, equal-weight bordered CTAs. | Done |
| ✅ RESOLVED | Phase 8.0 BasementMode Designer | **`OfflineBanner` lacks "Last updated [time]" timestamp** → `mmkvPersister` now writes `tq-last-updated`; banner reads + formats relative time (`"Offline mode. Last updated 5 min ago."`); `"No cached data"` fallback on first install. | Done |
| ✅ RESOLVED | Phase 8.0 BasementMode Designer | **`EmptyFeedState` lacks illustration** → added ambient blueprint SVG (8 dashed lines + 2 amber survey-mark brackets, ≤20% opacity), non-animated (zero battery cost), `accessibilityElementsHidden`. Thematic parity with `EmptyBoardState` radar. | Done |
| MED | Phase 8.0 BasementMode Designer | **Settings notifications section uses `<ActivityIndicator>` not skeleton bones** — causes CLS when prefs resolve. Requires building a `SettingsNotificationsSkeleton` matching the three-toggle + segmented control + slider layout. | Mobile UX WF2 |
| ✅ RESOLVED | Phase 8.0 DirtyData DeepSeek | **ZodError from async queryFn does not reach `ErrorBoundary`** → three hooks (`useLeadFeed`, `useFlightBoard`, `useSearchPermits`) now set `throwOnError: (err) => err instanceof ZodError`. Network errors stay in `isError`. `schemas.ts` comment corrected. | Done |
| ✅ RESOLVED | Phase 8.0 PII Adversarial | **API `logInfo` correlates `user_id` + precise `lat`/`lng`** → truncated to 3 decimals (~100m grid) in `src/app/api/leads/feed/route.ts:147-159` before `logRequestComplete`. | Done |
| ✅ RESOLVED | Phase 8.0 PII Adversarial | **`entityId` passed to Sentry `logError` context** → dropped from `src/app/api/entities/[id]/route.ts:63`; context reduced to `{ handler: 'GET' }`. | Done |
| ✅ RESOLVED | Phase 8.0 Designer BadgeRadius | **Badge radius inconsistency** → `LeadCard.tsx` + `[lead].tsx` pill containers updated from `rounded-full` → `rounded-md` (spec 91 §4.1, matches `FlightCard`). `LeadCardSkeleton` badge bones also updated for shape parity. Other `rounded-full` uses (status dot, progress track, count-badge chip) are legitimately circular and retained. | Done |
| ✅ RESOLVED | Phase 8.0 Bundle | **`lucide-react-native@1.8.0` 35MB in node_modules — bundle audit** → `"sideEffects": false` + per-icon ESM files in `dist/esm/icons/`; Metro tree-shakes the 3 used icons (Search, ChevronLeft, X) to ~3-5KB in the shipped bundle. 35MB is dev-only (source + `.d.ts` artifacts). Swapping to `@expo/vector-icons` would add Ionicons font files (~200KB) to production — a net loss. Keep. | Done (documented, no code change) |
| LOW | Phase 8.0 BasementMode Designer | **`NotificationToast` doesn't use `useSafeAreaInsets()`** — resting position `translateY: -8` from `top: 0` partially clips on iPhones with Dynamic Island. Platform-specific polish. | Mobile UX polish |
| LOW | Phase 8.0 BasementMode Designer | **`EmptyBoardState` sweep arm lacks gradient fade** — spec 77 §4.2 describes "fade to transparent toward centre." Current implementation uses flat `opacity={0.5}`. Requires SVG `<LinearGradient>` + `<Defs>`. | Mobile UX polish |
| LOW | Phase 8.0 BasementMode Designer | **`NotificationPermissionModal` "Maybe Later" CTA visually suppressed vs "Allow"** — asymmetric hierarchy. Spec + double-permission pattern want both equally discoverable. Add `border border-zinc-700 rounded-xl` to Maybe Later. | Mobile UX polish |
| LOW | Phase 8.0 Polish Designer | **Haptic polish — Toast X-dismiss + UNDO snackbar + NotificationPermissionModal Allow have no haptics** — not spec-mandated but premium apps pair these with `lightImpact` / `successNotification`. Below the must-ship bar. | Mobile UX polish |
| LOW | Phase 8.0 Polish Designer | **Typography nit — `lifecycle_phase` uses `font-mono` in `[flight-job].tsx:180` but system font in `FlightCard.tsx:166`** — same data field rendered differently across screens. Remove `font-mono`. | Mobile UX polish |
| LOW | Phase 8.0 Polish Designer | **`ScoreRow` labels in `font-mono`** — "Proximity" / "Timing" / "Value" / "Relevance" are human-readable category names, not data. Spec 91 §6.2 reserves `font-mono` for data values. | Mobile UX polish |
| LOW | Phase 8.0 Polish Designer | **`FilterTriggerRow` chip touch target marginal** — ~26px visible height + 8px hitSlop = ~42px, barely fails 44px bar. Increase hitSlop top/bottom to 12. | Mobile UX polish |
| LOW | Phase 8.0 Polish Designer | **`FlightCard` Remove action panel no explicit `minHeight: 44`** — relies on parent Swipeable for height parity. Fine in practice (parent card is always ≥64px) but spec-prescribed minimum is missing. | Mobile UX polish |
| LOW | Phase 8.0 Polish Designer | **Empty state CTAs (`EmptyFeedState`, `EmptyBoardState`) lack hitSlop** — bare Pressable with ~20px tap zone on text-only CTAs. Add `hitSlop` or `paddingVertical: 12`. | Mobile UX polish |
| LOW | Phase 8.0 DirtyData | **`.nullable()` without `.optional()` in `PermitLeadFeedItemSchema`** — Zod rejects `undefined` even when server might omit rather than `null`. Contract alignment question with backend serializer. | Schema audit WF |
| DEFERRED | Mobile Ph5/6 2026-04-22 Gemini | **`scrollToTop` vs `scrollToOffset({offset:0})`** — Gemini flagged `listRef.current?.scrollToTop()` as non-existent in FlashList. Confirmed in FlashList v2.3.1 changelog: `scrollToTop()` was added in v2.0.0. Finding is incorrect; no code change required. | Disputed — FlashList v2.3.1 API confirmed |
| MED | WF2 DOWN Blocks 2026-04-24 Reviewer B | **`validate-migration.js` blind spot: `DROP MATERIALIZED VIEW` not caught by Rule 1** — `migrations/034_mv_monthly_permit_stats.sql` line 6 has `DROP MATERIALIZED VIEW IF EXISTS` in its UP block. The destructive-statement regex only matches `DROP TABLE`, `DROP COLUMN`, `TRUNCATE TABLE`. No `-- ALLOW-DESTRUCTIVE` required or present. The DROP is intentional (idempotent CREATE pattern) but the validator does not enforce consistency. Fix: extend `dropRe` to include `DROP\s+MATERIALIZED\s+VIEW` and add `-- ALLOW-DESTRUCTIVE` to 034. | scripts/validate-migration.js |
| MED | WF2 DOWN Blocks 2026-04-24 Reviewer A | **Migrations 051, 059, 060 have executable DROP SQL in DOWN sections** — Unlike all other migrations where DOWN is commented SQL, these three have live `DROP TABLE`/`DROP COLUMN` statements after the `-- DOWN` marker. Any plain SQL runner (psql `\i`, pgAdmin) would execute them. 051: `DROP TABLE IF EXISTS engine_health_snapshots;` (line 19). 059: `DROP INDEX × 2` + `ALTER TABLE permits DROP COLUMN` (lines 18-20). 060: `DROP TABLE IF EXISTS scraper_queue;` (line 23). Should be commented out for consistency with all other migrations. | migrations/051, 059, 060 |
| LOW | WF2 DOWN Blocks 2026-04-24 Reviewer B | **Migration 039 DOWN block has no actionable rollback SQL** — The UP block makes 7 categories of change (PostGIS geom columns, GiST indexes, FK constraints, partial indexes, seed rows). The DOWN block says "restore from backup" with no named objects. An on-call engineer cannot derive which FK constraints, indexes, or columns to drop individually. Pre-existing (039 was excluded from this WF2 batch). Improve by listing reversible steps as commented SQL. | migrations/039_schema_hardening.sql |
| LOW | WF2 DOWN Blocks 2026-04-24 Reviewer B | **Migration 040 CREATE INDEX on `wsib_registry` without CONCURRENTLY** — All 4 indexes in 040 lack `CONCURRENTLY` and `IF NOT EXISTS`. `wsib_registry` is in `LARGE_TABLES` in validate-migration.js. On a fresh-apply these would table-lock `wsib_registry` during construction. Pre-existing (040 was committed before CONCURRENTLY enforcement was in place for this table). | migrations/040_wsib_registry.sql |
| LOW | WF2 48 2026-04-24 Gemini+DeepSeek | **`observe-chain.js` `review-database-followup.md` unbounded growth** — `fs.appendFileSync` adds a section on every run with no log rotation or size cap. Over weeks, the file will grow without bound. Fix options: (a) rotate to `review-database-followup-YYYY-MM-DD.md` per day, (b) cap at 10MB and rename old file before appending. Both reviewers flagged this; Gemini classified HIGH. Deferred because the file is developer-facing (manual review tool, not a production system) and disk space on the dev machine is not currently constrained. | Future WF2 observe-chain.js |
| LOW | WF2 48 2026-04-24 Gemini | **`observe-chain.js` AI output written to Markdown without HTML sanitization** — Gemini classified CRITICAL: if the DeepSeek model returns `<script>` or `<img onerror>` tags, a Markdown previewer that renders HTML (VS Code preview, GitHub) could execute them. Actual risk is LOW: (1) the file is local and only opened by trusted developers, (2) VS Code default preview sandbox blocks `<script>`. Deferred; would be HIGH if the report were ever served via HTTP or rendered in a web UI. | Future hardening — sanitize if report is ever web-served |
| LOW | WF2 48 2026-04-24 DeepSeek | **`observe-chain.js` primary DB queries (chain row, step rows, baseline) not individually try/caught** — DeepSeek classified MEDIUM. These three `pool.query()` calls are inside the `withAdvisoryLock` callback which is called from `pipeline.run`. `pipeline.run` has a top-level try/catch that catches any unhandled rejection, logs `pipeline.log.warn`, and exits 0. The observer-level error isolation is therefore guaranteed by the framework, not per-query try/catch. Deferred because adding per-query try/catch would duplicate the framework's own isolation without adding correctness guarantees. | Pattern documentation in spec 48 §3.4 |
## WF5 prod backend — 2026-04-24 audit deferred items

| Severity | Source | Item | Planned Home |
|----------|--------|------|--------------|
| LOW | WF5 prod SC1 | **5 test files missing SPEC LINK header** — `src/tests/factories.ts`, `src/tests/fixtures/sample-permits.ts`, `src/tests/market-metrics.logic.test.ts`, `src/tests/scope.logic.test.ts`, `src/tests/setup.ts`. Protocol requires SPEC LINK in every test file header. | Add headers in next touching WF2/WF3 on those files |
| LOW | WF5 prod T4 | **`diff-narrator.logic.test.ts` has 5 Windows platform-skipped tests** — sits outside `src/tests/db/` (the only accepted skip zone). Either move to `db/` if DB-dependent, or establish a `.skip.platform.ts` convention. | Mobile/tools cleanup WF2 |
| LOW | WF5 prod OP1 | **Spec 07 OP1 expected chain counts are stale** — spec says permits=14, coa=6, sources=14; actual manifest: permits=27, coa=12, sources=15 (plus entities=2, wsib=1, deep_scrapes=7). Update the pass criteria in `docs/specs/00-architecture/07_backend_prod_eval.md`. | Update spec next session |
| LOW | WF5 prod R1/R2/R4/SC2 | **Spec 07 exclusion patterns for R1, R2, R4, SC2 are stale** — grep commands flag tooling scripts (deepseek-review, gemini-review, diff-narrator, extract-stryker-survivors, local-cron) that are not pipeline scripts and legitimately don't use pipeline.run / withTransaction / withAdvisoryLock / no-process-exit. Add explicit `grep -v` patterns for these. Also: C3 exclusion should cover `scripts/analysis/`, `scripts/backfill/`, `scripts/seeds/`, `scripts/migrate.js`. | Update spec next session |
| MED | WF5 prod T5 | **~12 operational pipeline scripts lack dedicated guardrail tests** — `backfill-permits-location.js`, `close-stale-permits.js`, `compute-centroids.js`, `compute-timing-calibration-v2.js`, `create-pre-permits.js`, `link-massing.js`, `link-neighbourhoods.js`, `link-similar.js`, `link-wsib.js`, `load-address-points.js`, `observe-chain.js`, `refresh-snapshot.js`. Coverage exists through chain.logic.test.ts and pipeline-sdk.logic.test.ts at the integration level, but no unit-level guardrail per script. | WF1 testing sprint |
| MED | WF5 prod O2+M3 | **`observe-chain.js` not in manifest and missing emitMeta** — script is running as a live pipeline step (has ADVISORY_LOCK_ID) but not registered in `scripts/manifest.json`. Also missing `emitMeta()` call per spec 47 O-series protocol. | WF2 on observe-chain.js |
| MED | WF5 prod M3 | **`backfill-permits-location.js` not in manifest** — script has `withAdvisoryLock` but is not tracked in `pipeline_runs` via manifest registration. If it's a one-time backfill, move to `scripts/backfill/` and exclude from M3 check. | Clarify intent; register or relocate |

## WF3 — WF5 Audit Bug Batch B1-B5 (commit: TBD, 2026-04-25)

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| MEDIUM | Code Reviewer | **observe-chain.js SPEC LINK mismatch** — SPEC LINK points to spec 48 but `ADVISORY_LOCK_ID = 113`; §3 requires SPEC LINK spec number to match ADVISORY_LOCK_ID. Lock 113 assigned sequentially per §A.5 Bundle G scheme (not by spec number); no spec 113 exists. Fix: add inline comment `// Lock ID 113 assigned sequentially per §A.5 (governed by spec 48)` or create spec 113. | scripts/observe-chain.js line 2 |
| MEDIUM | DeepSeek | **purge-lead-views.js missing audit_table** — `emitSummary` emits no `audit_table` field; SDK injects stub `UNKNOWN` verdict, causing admin dashboard to show green regardless of deletion count. Should emit `audit_table` with `rows_deleted`, `stale_before`, and PASS verdict. | scripts/purge-lead-views.js |
| MEDIUM | DeepSeek | **purge-lead-views.js `records_updated` misused for deletions** — Spec §11 counter semantics: `records_updated` = rows modified in-place; deletions must appear as a named metric in `audit_table.rows`. Set `records_updated: 0` and move delete count to `audit_table`. | scripts/purge-lead-views.js |
| LOW | Gemini | **observe-chain.js `parseFloat(...) \|\| null` hides 0ms baseline** — If avg_duration_ms is legitimately 0, the `\|\|` coerces it to null and baseline comparison shows "no baseline". Use `Number.isFinite` guard instead. | scripts/observe-chain.js lines 118,144 |
| LOW | Gemini | **observe-chain.js duplicate delta-string calculation** — Duration delta built once for DeepSeek context (line 144) and again for the Markdown table (line 212). Should be computed once in the `stepSummaries` map and reused. | scripts/observe-chain.js |

## WF3 — G1-G4 D1-D4 Bug Batch (commit: TBD, 2026-04-25)

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| LOW | Code Reviewer (confidence 85) | **spec 47 §A.2 Tier 2 registry not updated after D3 externalized RETENTION_DAYS** — `purge-lead-views.js` was registered in the §A.2 Tier 2 registry with `RETENTION_DAYS=90` as a hardcoded threshold; D3 fix moved it to DB logic_variables but the registry entry still implies a hardcoded value. §A.2 explicitly says Tier 2 entries require legal sign-off before externalizing — the registry text should be updated to reflect that the value is now sourced from `logic_variables.lead_view_retention_days` (with the 90-day default preserved in the seed JSON). | Update spec 47 §A.2 registry entry in next WF2 touching spec 47 |

## WF3 — WF5 Audit P1/P2/P3 Bug Batch (commit: TBD, 2026-04-25)

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| LOW | Code Reviewer (confidence 82) | **P1 touch-gate test only checks absent string, not unconditional behavior** — `close-stale-permits.infra.test.ts` verifies `INTERVAL '1 hour'` is absent but does not assert that no other time-conditional guard was added to the touch query. A future developer could introduce `AND permits.last_seen_at < $RUN_AT - INTERVAL '10 minutes'` and the test would still pass. Add `expect(LOAD_SRC).not.toMatch(/UPDATE permits SET last_seen_at[\s\S]*?WHERE[\s\S]*?INTERVAL/)` to lock unconditional behavior. | Add to close-stale-permits.infra.test.ts in next WF touching load-permits.js |

## WF5 prod backend — 2026-04-25 Audit Deferred Items

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| HIGH | WF5 prod backend OP4 | **backup_db has never run** — `pipeline_runs` shows 0 rows for `backup_db`. `scripts/backup-db.js` and manifest entry both exist, but the script has never been triggered in the pipeline chain or standalone. No production DB backup is on record. | Urgent operational WF — trigger backup-db.js and verify GCS upload before next schema change |
| MED | WF5 prod backend T5 | **9 operational scripts have no direct guardrail test** — scripts without a corresponding infra/logic test file: `compute-centroids.js`, `link-similar.js`, `link-neighbourhoods.js`, `load-address-points.js`, `load-neighbourhoods.js`, `load-parcels.js`, `observe-chain.js`, `reclassify-all.js`, `enrich-web-search.js`. Chain integration in `chain.logic.test.ts` provides partial coverage but no spec-specific assertions. | File WF2 guardrail sweep for the load/link scripts |
| LOW | WF5 prod backend SC1 | **5 test helper files missing SPEC LINK header** — `src/tests/factories.ts`, `src/tests/fixtures/sample-permits.ts`, `src/tests/market-metrics.logic.test.ts`, `src/tests/scope.logic.test.ts`, `src/tests/setup.ts` all lack the `// SPEC LINK:` comment required by §3 Traceability. | Add SPEC LINK headers in next WF touching each file |
| LOW | WF5 prod backend M3 | **reclassify-all.js not in manifest** — `scripts/reclassify-all.js` exists on disk but has no entry in `scripts/manifest.json`. The script is a bulk utility (full reclassification on schema changes) and can't be triggered from the admin panel. Either add it to the manifest as `type: "utility"` or document it as intentionally unregistered. | Add manifest entry or explicit exclusion comment |
| LOW | WF5 prod backend O4 | **console.warn instead of logError in pipeline_runs tracking catch** — `src/app/api/admin/pipelines/[slug]/route.ts:165-167`: the catch block for `INSERT INTO pipeline_runs` uses `console.warn` (graceful degradation: "table not available"). Per §6 Logging, all catch blocks in API routes must use `logError`. Even for a deliberate no-op catch, this should be `logWarn` from `src/lib/logger.ts`. Also: lines 255/265 are intentional swallows (`catch { /* malformed summary/meta */ }`) — add a logWarn call with truncated payload for observability. | Next WF touching [slug]/route.ts |
| LOW | WF5 prod backend T4 | **diff-narrator.logic.test.ts has 5 skipped tests outside db/** — these 5 tests are marked skip in `src/tests/diff-narrator.logic.test.ts` (not in `src/tests/db/`). The test runner shows them as skipped on every run, inflating the skip count. Either un-skip them (provide a real diff fixture) or move to `src/tests/db/` if they require an environment. | Next WF touching diff-narrator.js |
| LOW | WF5 prod backend R1/R2/R4 | **Tooling scripts missing pipeline SDK wrappers** — `deepseek-review.js`, `diff-narrator.js`, `extract-stryker-survivors.js`, `gemini-review.js`, `local-cron.js`, `migrate.js` do not use `pipeline.run`, `withTransaction`, or `withAdvisoryLock`. By the literal grep criterion these are FAIL, but all are review/tooling scripts with no DB-mutation responsibilities. The spec exclusion list (`grep -v spike | grep -v ai-env`) does not cover them. Consider adding these to the R1/R2/R4 grep exclusions in spec 07. | Update spec 07 exclusion lists to add: `grep -v "deepseek\|gemini\|diff-narrator\|extract-stryker\|local-cron"` |
| LOW | WF5 prod backend SC2 | **process.exit in tooling scripts** — `deepseek-review.js`, `diff-narrator.js`, `gemini-review.js`, `local-cron.js`, `migrate.js`, `run-chain.js`, `validate-migration.js` all use `process.exit()`. None are mainline pipeline scripts; all are CLI tooling where `process.exit` is the correct pattern. The SC2 check's grep command does not exclude them. Consider adding the same tooling exclusions to the SC2 grep in spec 07. | Update spec 07 SC2 grep exclusions |
| LOW | WF3 2026-04-25 C4 residual | **6 pre-existing API routes still use dynamic `conditions.join()` WHERE** — After WF3 fixed `runs/route.ts` (nullable-param static WHERE) and narrowed the C4 grep to SQL-structural keywords, 18 matches remain across: `builders/route.ts` (3), `coa/route.ts` (3), `entities/route.ts` (4), `notifications/route.ts` (2), `permits/geo/route.ts` (2), `permits/route.ts` (3), `admin/rules/route.ts` (1). All values are parameterized (`$1`/`$2`) — no injection risk — but the WHERE *structure* is dynamic (template literal). Refactor each to nullable-param static WHERE pattern (same fix applied to `runs/route.ts`). | File WF2 for each route: refactor to static nullable-param WHERE. Priority: builders → coa → entities → permits (most traffic first) |
| LOW | WF3 2026-04-25 P1 backup-db C2 | **`emitSummary` records_total: null coerced to 0 by SDK** — `scripts/backup-db.js` SKIP path passes `records_total: null` matching the Observer archetype spec, but `scripts/lib/pipeline.js:237` uses `?? 0` so the emitted PIPELINE_SUMMARY has `"records_total":0`. Pre-existing SDK behavior for all 44 Observer scripts. backup_db is an infra step so gate-skip is moot. No behavioral impact. Code clarity fix: pass `records_total: 0` explicitly at the SKIP call site to align source with wire reality. | Future cosmetic cleanup — align SKIP call site with `?? 0` SDK behavior |

## WF2 — Spec 93 Step 0 Pre-flight (2026-04-29)

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| MED | Independent reviewer | **`@sentry/react-native ~7.2.0` — verify iOS EAS Build** — GitHub issue getsentry/sentry-react-native#5222 reports `~7.2.0` failing `npx expo prebuild --platform ios` on Expo SDK 54 (`SentryUserFeedbackIntegration unavailable`). Expo installer chose this version so it may be a resolved patch; verify on first iOS EAS Build. Fix: re-run `npx expo install @sentry/react-native` or pin to confirmed-good patch. | Verify on first EAS iOS build — before Spec 93 Step 2 ships to iOS |
| MED | Independent reviewer | **`input-otp-native@0.6.0` API mismatch with Spec 93 §4** — Spec documents `<OTPInput pinCount={6} cellStyle={...} focusedCellStyle={...} />` but the package uses a render-prop API: `<OTPInput maxLength={N} render={({ slots }) => ...} />`. Props `pinCount`, `cellStyle`, `focusedCellStyle` do not exist — TS compile error at Step 4 implementation. Fix: update Spec 93 §4 OTP Entry to the render-prop pattern, or substitute `react-native-otp-entry`. | Resolve before Spec 93 Step 4 implementation |
| LOW | Independent reviewer | **Android `intentFilters` missing `path: "/oauth2redirect"`** — Canonical `expo-auth-session` Google OAuth intentFilter includes `"path": "/oauth2redirect"` in the data block. Current entry has only `scheme`. May cause redirect handling failure on Android with some Google OAuth client configurations. | Add `"path": "/oauth2redirect"` when substituting real Android Client ID |
| LOW | Independent reviewer | **`SENTRY_AUTH_TOKEN` EAS Secret not yet configured** — Required at EAS Build time for Sentry source map upload. Not blocking development builds. | Add via `eas secret:create --name SENTRY_AUTH_TOKEN` before first production EAS Build |

## WF3 2026-04-26 — Spec 07 Relevance Scoping + V11 (commit: TBD)

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| LOW | V11 FR2 | **`leads/flight-board` FR2 N/A — parameterless route surfaces in Zod check** — `src/app/api/leads/flight-board/route.ts` takes no user-supplied query params (it derives trade_slug from auth context). The FR2 check pattern surfaces it as missing Zod because no `z.` reference appears in the file. This is an N/A, not a FAIL — the route has nothing to validate. All four Expo-consumed routes comply with FR1 (withApiEnvelope present). | Add `flight-board` to the FR2 pass-criteria note as "parameterless — N/A". Done in spec 07 V1.1. No code change needed. |
| LOW | V11 T5 | **`reclassify-all.js` guardrail test coverage unconfirmed** — the script was added to `manifest.json` in this WF3. The T5 check (every operational script has a guardrail test) is a manual cross-reference. Chain integration in `chain.logic.test.ts` provides minimal coverage but no spec-specific assertions for reclassify-all. | Verify T5 in next WF5; file WF2 if coverage gap confirmed |

## WF1 — Spec 93 Mobile Authentication (2026-04-29)

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| MED | Independent reviewer | **AuthGate full profile-check BLOCKED on Spec 95** — Stub does binary signed-in/signed-out routing only. Five-outcome profile-check (200+complete → app, 200+incomplete → onboarding, 404 → onboarding, 403 → reactivation modal, network failure → full-screen error) is documented as TODO in `mobile/app/_layout.tsx` AuthGate. | Wire after Spec 95 lands `/api/user-profile`; uncomment 4 skipped tests in `mobile/__tests__/useAuth.test.ts` |
| MED | Independent reviewer | ~~**`paywallStore.clear()` and `userProfileStore.reset()` calls missing from `signOut()`** — TODO comments in `mobile/src/store/authStore.ts` flag forward dependencies on Spec 96 (paywallStore) and Spec 95 (userProfileStore). Without `paywallStore.clear()` a user who dismissed the paywall and signed out on a shared device leaves `dismissed: true` in memory for the next user.~~ — **STALE 2026-05-03**: `authStore.ts:114-117` now calls `.getState().reset()` on filter/notification/onboarding/userProfile stores; paywallStore exposes `reset()` (not `.clear()`) and is covered by `mobile/__tests__/storeReset.coverage.test.ts` (Spec 99 §B5). | Wire `paywallStore.clear()` in Spec 96 implementation; wire `userProfileStore.reset()` in Spec 95 implementation |
| MED | Independent reviewer | **handleLinkExisting mode-switch only handles email/phone — Google/Apple existing-method conflicts close the sheet with no visual cue** — When the existing method is Google or Apple, the sheet closes and the user lands on the idle button stack with no explicit prompt to tap that provider's button. | Add a transient toast/banner when sheet closes ("Tap Google to continue") for Google/Apple existing-method paths |
| LOW | Gemini adversarial | **Phone validation is hard-coded for Canadian E.164 (`length < 12`)** — Custom `PhoneInputField` is intentionally CA-only at launch. International support would require library-based validation (`libphonenumber-js`). | Revisit when expanding beyond Canadian market |
| LOW | DeepSeek adversarial | **Phone number unmasked in OTP screen body text** — `"6-digit code sent to {phoneNumber}"` shows full E.164. Privacy concern on shared devices, though spec does not require masking. | Mask middle digits if user-research surfaces a privacy complaint |
| LOW | DeepSeek adversarial | **`fetchSignInMethodsForEmail` failure defaults to `'email'`** — In the linking sheet, if the lookup fails (network), the user sees "Sign in with email" as the prompt regardless of the actual existing method. | Replace fallback string with generic "your other method"; or treat as a hard error and ask the user to try again |

## WF1 — Spec 93 Round-2 Multi-Agent Review (2026-04-29)

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| MED | Independent reviewer | **`fetchSignInMethodsForEmail` is deprecated and returns empty under Firebase email enumeration protection** — In Firebase projects with email enumeration protection enabled (default since Sept 2023), this call returns `[]` regardless of the email's actual providers. Current fallback defaults to 'email' which mislabels the linking sheet. Suggested: drop the call and show a generic message ("An account with this email already exists. Try a different method."). | Remove call when redesigning linking flow; spec update needed |
| MED | Independent reviewer | **Google Android OAuth scheme is a literal placeholder (`REPLACE_WITH_GOOGLE_ANDROID_CLIENT_ID`)** — Android Google sign-in is dead until replaced. Already noted in WF2 deferral but flagged again — must be resolved before any Android EAS Build. | Replace with real reverse client ID via EAS Secret before first Android build |
| LOW | Independent reviewer | **Account-linking sheet has no UI cue when existing method is Apple/Google** — Sheet closes with no indicator that the user should re-tap that provider's button. Email/phone branches now correctly route into their input UI, but Apple/Google branches just close. | Add a transient toast ("Tap Google to continue") when sheet closes for those providers |
| LOW | DeepSeek logic | **OTP loading flag persists if user navigates away mid-verification** — `otpLoading=true` not cleared on phone-input back navigation; cosmetic spinner residue. | Add `setOtpLoading(false)` to phone sheet onChange dismiss handler |
| LOW | Gemini logic | **Phone validation uses hardcoded length check `< 12`** — Already noted (CA-only at launch). Re-flagged for international expansion. | Revisit when expanding beyond CA |
| LOW | Gemini logic | **Empty `mapFirebaseError` return for unknown codes triggers blank UI** — Current code: `if (message) setErrorMessage(message)`. Cancellation case ('') is intentional, but unknown codes do return a generic string so the conditional currently works. Re-verify if `mapFirebaseError` is ever extended to return more empty strings. | Audit when adding new error codes |
| LOW | All adversarials | **Custom `PhoneInputField` and `OtpInputField` deviate from spec library names** — Both are intentional substitutions (supply-chain attack and API mismatch respectively). Spec text still references the original libraries. | Update spec 93 §4 to document the substitution decisions |

## WF1 — Spec 94 Mobile Onboarding Multi-Agent Review (2026-04-29)

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| LOW | Code reviewer | **`filterStore.ts` MMKV adapter lacks try-catch** — Unlike `authStore.ts` and `onboardingStore.ts`, `filterStore.ts` adapter methods (`getString`, `set`, `remove`) are called without try-catch. A device out-of-space error would propagate through Zustand's `onRehydrateStorage` with no handler, leaving hydration state indeterminate. Pre-existing issue (not introduced by Spec 94). | Add try-catch to `filterStore.ts` MMKV adapter in a follow-up WF2 task |
| LOW | Code reviewer | **`fetchWithAuth` missing generic type parameter on PATCH call sites** — `profession.tsx`, `address.tsx`, `terms.tsx`, `complete.tsx` call `fetchWithAuth(...)` without `<T>` on PATCH calls where the response body is discarded. TypeScript infers `unknown` and the result is unused. `fetchWithAuth<void>(...)` would make the intent explicit. Minor consistency issue; no runtime impact. | Fix in next onboarding WF2 pass |
| CRIT | API/DB reviewer | **`/api/user-profile` PATCH route missing + user_profiles columns missing** — All 7 onboarding PATCH calls hit a route that does not yet exist (`src/app/api/user-profile/route.ts`). None of `home_base_lat`, `home_base_lng`, `location_mode`, `supplier_selection`, `tos_accepted_at`, `default_tab`, `onboarding_complete` exist in the DB schema (migration 075 only has `user_id, trade_slug, display_name, created_at, updated_at`). End-to-end onboarding is blocked until Spec 95 ships the migration and route. | Spec 95 scope — implement before any E2E testing of the onboarding flow |
| CRIT | API/DB reviewer | **`trade_slug NOT NULL` blocks manufacturer INSERT** — `migrations/075_user_profiles.sql` line 27: `trade_slug VARCHAR(50) NOT NULL`. Spec 95 §2.5 requires `trade_slug = NULL` for manufacturer accounts. No ALTER migration exists to make it nullable. | Spec 95 scope |
| MED | API/DB reviewer | **`withApiEnvelope` error responses not fully wrapped** — `suppliers/route.ts` manual `NextResponse.json()` 401/400/500 responses produce `{ error: '...' }` shapes, while the envelope normally produces `{ data: null, error: {...}, meta: null }`. Mobile client reads `res.status` for errors so no current breakage, but future envelope-aware consumers would see malformed shapes. | Fix in suppliers route WF2 pass |
| LOW | Design reviewer | **`font-sans` resolves to Inter, not DM Sans** — `tailwind.config.js` line 23 maps `sans` to `['Inter', 'system-ui']`. Spec 94 §9 refers to "DM Sans" as the intended font for the design system. Pre-existing config decision (affects all screens). Either update config to load DM Sans via expo-font, or update spec to reflect Inter as the chosen font. | Design system WF2 decision — confirm with design before changing |
| LOW | Design reviewer | **No `dark:` NativeWind variants systemwide** — Spec 90 §2 requires dark mode support. The entire app uses hardcoded zinc-950 dark palette with no `dark:` variants. Users in System Light Mode see an invariably dark UI. Either implement `dark:` variants throughout or add `colorScheme="dark"` to the Expo root to explicitly force dark mode. | Systemwide WF2 sweep (pre-existing gap) |

## WF5 Manual Audit — Spec 94 Mobile Onboarding (2026-04-29)

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| LOW | WF5 audit | **`profession.tsx` idempotency catch dead code** — `err.message.includes('already')` in the PATCH catch block is unreachable because Spec 95 returns HTTP 200 for same-value `trade_slug` retry (idempotency exception), not 400. The catch only fires for genuine change attempts, where the error message will never contain "already". Dead code harmless but misleading. | Remove in a follow-up WF2 (now server behavior is confirmed by Spec 95 implementation) |

## WF1 Spec 95 Mobile User Profiles — Deferred Review Items (2026-04-29)

13-item adversarial triage resolved in commit 4eeaf96 (2026-04-29). Remaining deferred items below.

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| MED | Adversarial | **`tos_accepted_at` writable post-onboarding** — `UserProfileUpdateSchema` always accepts `tos_accepted_at`; spec requires stripping it if sent independently post-onboarding. Requires reading `onboarding_complete` from the pre-fetch SELECT and rejecting the field if already true. Compliance/legal field — low exploit risk since clients set it once; no UI in Spec 97 allows modifying it. | Spec 97 Settings — add guard when editable fields are wired |
| MED | Adversarial | **`trade_slug: null` manufacturer filterStore** — `filterStore.hydrate()` maps `null` to `''` (empty string) for manufacturer accounts; `trade_slugs_override` is not surfaced in filterStore. Manufacturer feed requires multi-trade query logic that is not yet specced. | Manufacturer feed spec (future) |
| MED | Adversarial | **Delete route not in DB transaction** — `UPDATE user_profiles` is a bare `query()` call, not wrapped in `withTransaction`. The spec mandates an atomic transaction wrapping the update and Stripe cancellation. Currently safe since Stripe is deferred (Spec 96), but needs the wrapper added when Stripe is wired. | Spec 96 — add `withTransaction` wrapper when Stripe is installed |
| MED | Integration | **Radius slider writes to filterStore only, no PATCH to server** — `setRadiusKm()` updates local state but the radius change is not persisted to `user_profiles.radius_km` via PATCH. Spec 97 Settings screen is the natural home for the save action. | Spec 97 Settings |
| LOW | Spec Compliance | **`chk_location_mode` constraint redundant** — migration 114 adds both `chk_location_mode` (CHECK enum values) and `chk_location_mode_coords` (three-state coherence). PostgreSQL NULL-in-CHECK semantics make `chk_location_mode` correctly allow NULL despite not being explicit. Redundant but not broken. | Low-priority cleanup if migration is revisited |
| LOW | Integration | ~~**signOut does not clear `user-profile-cache` MMKV key on second device** — `clearUserProfileCache()` is now called in signOut, but the `user-profile-cache` instance in `useUserProfile.ts` is created at module scope; a second logged-in device may still see stale cache until next launch. Spec 96 auth hardening pass should include token-rotation-triggered cache invalidation.~~ — **STALE 2026-05-03**: per Spec 99 §9.1 the `user-profile-cache` MMKV blob was eliminated; only the one-time cleanup migration in `mobile/src/lib/migrations/userProfileCacheCleanup.ts` survives. TanStack Query is the single profile cache. | Spec 96 |

## WF3 — AuthGate Server-Driven Routing (commit: TBD, 2026-04-29)

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| ~~LOW~~ | ~~Code Reviewer~~ | ~~**`apiClient.test.ts` — missing `expect.assertions(N)` on try/catch assertion tests**~~ | **RESOLVED 2026-04-29** — converted both tests to `.rejects.toMatchObject()` idiom |
| LOW | Code Reviewer | ~~**`useUserProfile.ts` `markComplete()` bridge** — BLOCKED: `(onboarding)/_layout.tsx:9+15` and `IncompleteBanner.tsx:10+13` both read `isComplete` for routing/display. Removing the bridge without migrating these consumers would break the onboarding-complete guard and banner on new-device reinstall. Clarifying comment added to `useUserProfile.ts`. Full removal requires replacing all `isComplete` routing consumers with server-state reads.~~ — **STALE 2026-05-03**: per Spec 99 §9.2a/b/c the `markComplete()` bridge and `isComplete` field were both removed; `IncompleteBanner.tsx:42` now reads `profile?.onboarding_complete === true` directly from the server profile. The persist `migrate` deleted `isComplete` from existing MMKV state. | Spec 97 Settings / Spec 94 onboarding cleanup (full consumer migration) |

## WF3 Set 2 — Mobile Hydration & Network Hardening (commit: TBD, 2026-04-29)

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| LOW | Code Reviewer | **`apiClient.ts` concurrent 401 mutex** — Two inflight requests that both receive 401 each independently call `getIdToken(true)` and `setAuth`. Firebase deduplicates the token refresh network call, but the Zustand store receives two `setAuth` writes with potentially different timestamps. Low risk in practice (rare to have two simultaneous requests when a token expires); a module-level `refreshPromise` mutex (`let refreshPromise: Promise<string> \| null = null`) would eliminate the race entirely. | Spec 93 auth hardening or next mobile hardening pass |

## WF1 Spec 96 Subscription — Deferred Review Items (2026-04-30)

Multi-agent review (Code Reviewer + Gemini + DeepSeek) ran on the new endpoints. Critical and high-confidence findings were fixed inline before commit. Remaining items deferred:

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| LOW | DeepSeek | **`subscribe/session` race window between SELECT and INSERT** — A concurrent webhook (e.g., `customer.subscription.created`) could set status to `'active'` after the SELECT returns `null`/`expired` but before the INSERT commits. The web checkout server-to-server re-validates status before charging, so the wasted nonce is a UX issue, not a billing one. Fix would be `SELECT ... FOR UPDATE` or wrap in a transaction. Acceptable given 15-min TTL. | Future hardening pass |
| LOW | DeepSeek | **`subscribe/session` hardcoded production fallback URL** — `'https://buildo.com/subscribe'` is the default when `SUBSCRIBE_CHECKOUT_BASE_URL` env is unset. A misconfigured staging/dev environment would route to prod. Either make the env var required (throw on startup) or default per `NODE_ENV`. | Deployment hardening |
| LOW | Code Reviewer | **`useSubscribeCheckout` re-declares URL response shape locally instead of importing from `src/app/api/subscribe/session/types.ts`** — Mobile workspace cannot import server-side types without a `packages/shared-types` workspace (Spec 90 §7 deferred). Zod schema is the runtime guard so drift fails loud, but the TS type lives in two places. | Spec 90 §7 monorepo workspace |
| LOW | Pre-existing | **Mobile typecheck fails on `AnimatedFlashList` ref/data/keyExtractor/renderItem types** — `Animated.createAnimatedComponent(FlashList)` strips the generic, props become `unknown`. Introduced in WF3 Set 3 (commit `15292eb`). Root typecheck (the gate) is clean; mobile typecheck warnings only. Fix requires either Reanimated v4 type fixes or a typed wrapper component. | Mobile hardening pass |
| LOW | Pre-existing | **`mobile/app/_layout.tsx:38` Notification API type drift** — `expo-notifications` recent versions added `shouldShowBanner` and `shouldShowList` to `NotificationBehavior`. Existing handler returns the legacy 3-field shape. Notifications still work; types are stale. | Mobile dependency upgrade pass |

## WF5 Spec 96 — 6-Reviewer Adversarial Sweep (2026-04-30)

Five-axis review (logic, observability, compliance to specs 96+90, design, wiring/db/api). Six reviewers ran in parallel: Gemini ×2, DeepSeek ×2, Code Reviewer agent ×2. Triage applied 10 inline fixes (CRITICAL + HIGH + select MED/LOW); the items below are the deferred MED/LOW set.

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| HIGH | Observability | **Subscription funnel has zero PostHog events** — no `paywall_shown`, `paywall_dismissed`, `subscribe_session_requested`, `subscribe_session_failed`, `refresh_status_tapped`, `subscription_restored`, `account_deletion_signout`. `ALLOWED_KEYS` whitelist in `mobile/src/lib/analytics.ts:34-41` also lacks `subscription_status`, `days_on_trial`, `refresh_count`, `checkout_error_kind`. Without these, the entire subscription funnel is a black box — product cannot measure conversion, webhook health, or post-payment latency. Spec 90 §11 mandates funnel telemetry. Scoped task: add 7 event names + 4 properties to whitelist + ~7 `track()` call sites. | Spec 96 follow-up — telemetry pass |
| MED | DeepSeek (compliance) | **No fade-out on `expired→active` transition** — Spec 96 §6 explicit: "The paywall screen should fade out (opacity `1 → 0`, `withTiming(0, { duration: 200 })`) before unmounting." Current code unmounts `PaywallMount` immediately on status flip, producing a jarring cut. Requires lifting an exit-fade shared value to the parent layout and delaying unmount until the fade completes. | Future polish pass |
| MED | Design (Code Reviewer) | **Settings `!profile` guard hides whole subscription section during loading** — `settings.tsx:71` returns `null` for the entire section when `profile` is `null`. When the profile resolves, the section pops in causing a layout shift in the Settings scroll. Spec is silent on the loading skeleton for this row, so this is polish, not a spec violation. Fix: render a section-header skeleton for the Subscription block during `isLoading`. | Settings polish pass |
| MED | DeepSeek (compliance) | ~~**`IncompleteBanner` rendered unconditionally in `_layout.tsx`** — pre-existing from Spec 94. Should only render when `profile?.onboarding_complete === false`. Currently mounted for all users on every authenticated screen, producing a misleading banner when irrelevant. Pre-dates Spec 96.~~ — **STALE 2026-05-03**: per Spec 99 §9.2a the component reads server `profile.onboarding_complete` and returns `null` when explicitly true (`IncompleteBanner.tsx:42`). The mount is unconditional but the visibility decision is now correct; the "misleading banner when irrelevant" symptom is resolved. | Spec 94 onboarding cleanup |
| MED | Gemini (logic) | **Webhook event payload uses unsafe type assertions** — `event.data.object as Stripe.Subscription` in `webhooks/stripe/route.ts:103, 113, 122`. The `event.type` switch limits the risk, but a Stripe API change or misconfigured webhook could produce malformed payloads that throw `TypeError`. Hardening: add Zod parse for the event-object shape inside each branch. Not a current bug; defensive only. | Future hardening pass |
| MED | DeepSeek (database) | **`expiration.ts` PostgreSQL-specific syntax** — `NOW()`, `INTERVAL '14 days'` won't run on SQLite or other dialects. Project is locked to Postgres on Cloud SQL, so this is theoretical. | Document-only for now |
| MED | Observability | **`cancelled_pending_deletion` sign-out fires no observable signal** — `_layout.tsx:130-141` calls `signOut()` and redirects with no PostHog `track('account_deletion_signout')` or Sentry breadcrumb. Last observable moment before user leaves the app permanently. Tied to the funnel-events HIGH item above. | Bundle with telemetry pass |
| LOW | DeepSeek (database) | **`expiration.ts` inconsistent-state edge case** — if PATCH wrote `subscription_status = 'trial'` but failed mid-statement before `trial_started_at`, the row would have status without a start date, and `applyFallbackTrialInitIfNeeded` won't fix it (`subscription_status IS NULL` predicate is false). Very narrow window — current PATCH writes both fields atomically in the same SET clause, so this is theoretical. Fix: extend the fallback predicate to also catch `subscription_status = 'trial' AND trial_started_at IS NULL`. | Defensive hardening pass |
| LOW | Gemini (logic) | **Webhook log tag inconsistency** — `signature_verification_failed` log uses `{ event: 'signature_verification_failed' }` (excellent for filtering), but the dedup-fail and unmatched-row logs use unstructured tags. Adopt the `event:` field consistently across all `logError` calls in the webhook handler. | Cosmetic logging pass |
| LOW | DeepSeek (database) | **`expiration.ts` time-zone handling** — `NOW()` and `trial_started_at` are both DB-server timestamps. If the DB session is configured non-UTC, expirations could fire several hours early/late. Cloud SQL defaults to UTC; document the assumption. | Operational documentation |
| LOW | DeepSeek (database) | **`expiration.ts` return type `Record<string, unknown>` loses column-level type safety** — Future column renames in `user_profiles` won't be caught by TypeScript. Define a typed row interface. | Type-tightening pass |
| LOW | Gemini (wiring) | **Subscribe-session DB-default vs spec snippet mismatch** — Spec §10 Step 4b shows `expires_at: new Date(Date.now() + 15 * 60 * 1000)` (computed in app code). Implementation relies on the DB default `NOW() + INTERVAL '15 minutes'` (correct — avoids clock skew). Update spec to match implementation, or add an inline comment explaining the deviation. | Spec document update |
| LOW | DeepSeek (compliance) | **`prevStatusRef` placement** — currently inside the same effect that fires the cleanup; works but slightly fragile. Move ref update to a separate unconditional `useEffect` for clarity. | Refactor pass |

## WF5 Spec 96 — Security-Focused Adversarial Sweep (2026-04-30)

Three-reviewer security pass (Gemini webhook + DeepSeek subscribe-session + Code Reviewer full surface). 13 unique findings; 5 fixed inline (1 CRITICAL + 4 HIGH), 8 deferred below. The critical CSRF and operational concerns are deferred because they're either web-side dependencies or require broader patterns not yet established in the codebase.

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| HIGH | Code Reviewer (A4) | **No CSRF defense on cookie-auth POST to `/api/subscribe/session`** — `SameSite=Lax` does not block credentialed cross-origin `fetch` from same-site subdomains. Practical exploit impact is LOW because: (a) the response body is unreadable to the attacker (SOP), (b) any nonce created is reused by the legitimate user via the FOR UPDATE-locked reuse path. Defense-in-depth Origin allow-list or `SameSite=Strict` upgrade should land before web admin UI uses cookie auth. Mobile uses Bearer with no Origin header so the immediate exposure is minimal. | Web-admin auth hardening pass |
| HIGH | Code Reviewer (B2) | **Stripe webhook secret rotation runbook missing** — `STRIPE_WEBHOOK_SECRET` is read per-request (rotation-friendly, no restart needed), but no operational runbook exists for: rotating the secret in Stripe dashboard, updating the env var, the rollover window, or alerting on signature-verification failure rate spikes. Also: `stripe.webhooks.constructEvent` uses Stripe SDK's default 300s tolerance — make explicit via `{ tolerance: 300 }` so a future SDK default change cannot silently widen the replay window. | Operational runbook + minor code change (explicit tolerance) |
| HIGH | Code Reviewer (C2) | **Nonce URL referrer leakage on the web side** — `https://buildo.com/subscribe?nonce=X` opens via `WebBrowser.openBrowserAsync`. Within the 15-min TTL the nonce is a payment-initiation bearer credential. If the buildo.com/subscribe page loads any third-party resource (Stripe.js, analytics) without `Referrer-Policy: no-referrer`, the nonce can leak via Referer header. The session route comment asserts referrer-safety; the assertion is only true if the web page sets the policy. | Web checkout endpoint task (separate spec) |
| MED | Gemini (logic) | **Webhook event payload uses unsafe type assertions** — `event.data.object as Stripe.Subscription` etc. Switch on `event.type` provides some confidence but Stripe API drift could throw `TypeError`. Hardening: Zod parse the event-object shape inside each branch. Not a current bug. | Future hardening pass |
| MED | DeepSeek (security) | **NULL `subscription_status` allowed to call subscribe-session** — A user with `subscription_status IS NULL` (e.g., before trial init somehow) can request a checkout URL and subscribe without ever starting a trial, leaving `trial_started_at` NULL. Trial-init helpers should populate the row on the GET path before this case can arise, but the subscribe-session endpoint should defensively treat NULL as `expired` (or block) rather than allowing the bypass. | Defensive hardening pass |
| LOW | DeepSeek (security) | **Missing `else` on `result.kind` switch in subscribe-session** — Future enum addition could fall through without returning a response. Add a final exhaustiveness check + 500. | Refactor pass |
| LOW | Gemini (logic) | **Stale `'canceled'` mention in webhook log message context** (already partly fixed) — Comment reads cleanly but log messages still treat all rowCount===0 cases identically. The current "no_row_matched" event tag is grep-friendly but doesn't disambiguate the three sub-causes (missing user, customer mismatch, stale event). Splitting into separate log lines per cause would help support debug specific Stripe events. | Logging hygiene pass |
| LOW | Gemini (NIT) | **`withApiEnvelope` wrapper name is misleading on the webhook route** — The webhook returns `{ received: true }` not the standard envelope, so the wrapper is acting as an error-only catch-all. Either rename to `withApiErrorHandler` or document the dual mode. | Refactor naming pass |

## WF2 Spec 93 RNFirebase migration (commit: TBD, 2026-04-30)

Replaced Firebase JS SDK + `expo-firebase-recaptcha` with `@react-native-firebase/auth` to fix the Gradle 8 build break in `expo-firebase-core@6.0.0`. Findings below are **pre-existing** in `sign-in.tsx` / `sign-up.tsx` (not migration-introduced) — surfaced because the auth surface got fresh adversarial scrutiny during review. Triage retained two BUG fixes inline (`expo-secure-store` removal + `auth/missing-verification-id` mapping); rest deferred here.

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| HIGH-sec | Gemini CRITICAL (re-scoped) | **Account-linking should require `currentUser.emailVerified`** before calling `linkWithCredential` — guards against email-squatting where an attacker creates an unverified email/password Firebase account using the victim's email. The migration preserved the existing `linkingExpectedEmail` guard but did not add a verification check. (Note: Gemini's specific hijack scenario assumes the attacker can forge a Google ID token email claim — Google OAuth verifies emails, so that path is closed; the squatting path remains open.) | Auth security hardening WF |
| HIGH | Code-Reviewer + DeepSeek | **`handleLinkExisting` has no action path for Apple/Google as the existing method** — sign-in.tsx:391-407 routes only `'email'` and `'phone'`. When existing method is Apple or Google, the linking sheet closes and the user lands on the idle button stack with no instruction. Inline comment "tracked in review_followups.md" was previously not actually tracked — now it is. | Auth UX polish WF |
| HIGH | Code-Reviewer | **`sign-up.tsx` does not handle `auth/account-exists-with-different-credential`** — Spec 93 §3.2 requires linking flow on "all four paths"; sign-up.tsx phone + email paths fall through to generic error copy. Pre-existing; sign-up never had linking handling. | Auth completeness WF |
| HIGH | DeepSeek | **Email/password not trimmed before sign-in** — `auth().signInWithEmailAndPassword(email, password)` rejects whitespace in the email field with confusing error. Add `.trim()` to email; preserve password verbatim. | Auth UX polish WF |
| MED | Gemini | **Component state should use `useReducer`** — 15 separate useState hooks allow invalid combinations (e.g. `emailLoading=true` while `mode='phone-otp'`). Architectural critique. | Refactor (large) — defer until subscription gate stabilises |
| MED | Gemini | **"or" divider rendered unconditionally on Android** — Apple button is iOS-only but the divider above it always renders, creating an awkward 3-button stack with a leading "or" line. Conditional render on `Platform.OS === 'ios'`. | Auth UX polish WF |
| MED | Gemini | **`fetchSignInMethodsForEmail` empty-array default is unsafe** — `methods[0] ?? 'password'` misleads users when the array is empty (provider disabled in console, legacy state). Show generic copy instead. | Auth UX polish WF |
| MED | DeepSeek | **"Back" + "Sign up" Pressables missing `accessibilityRole="button"`** — VoiceOver doesn't treat them as buttons. Pre-existing accessibility gap. | A11y sweep WF |
| MED | DeepSeek | **`AccountLinkingSheet` `onDismiss` does not clear stale `pendingCredential`** — comment intentionally leaves it for the linking flow; minor memory leak if user dismisses then signs in to a fresh account. Email guard prevents misattachment but the credential lingers in state. | Auth UX polish WF |
| LOW | DeepSeek | **`otpError` cleared on input change but `errorMessage` is not** — visual mismatch (red border gone, error text lingers). | Auth UX polish WF |
| LOW | DeepSeek | **Apple `ERR_REQUEST_CANCELED` and Google `'cancel'` lack telemetry** — `track('auth_method_cancelled', { method: ... })` would close the funnel. | Telemetry pass |
| LOW | DeepSeek | **No `Keyboard.dismiss()` after email sign-in success** — minor polish; phone path already dismisses. | Auth UX polish WF |
| LOW | Gemini + DeepSeek | **Phone validation is `phoneNumber.length < 12`** — accepts non-digits and rejects valid short international numbers. Use `libphonenumber-js` or the input library's own validation. | Auth UX polish WF |
| LOW | Gemini | **"or" divider styling missing `font-mono` and `tracking-widest`** — Spec §4 mandates these classes; current implementation uses base styling. | Auth UX polish WF |
| LOW | Gemini | **`isMountedRef` pattern is legacy** — should use AbortController for fetches and useEffect cleanup; current pattern doesn't actually cancel underlying work. | Refactor (small) — bundle with state mgmt rework |
| NIT | Gemini | **Email + password preserved on "Back" tap** — minor privacy concern on shared devices. Comment justifies it (likely-return UX). | Optional cleanup |
| NIT | Gemini | **Google OAuth flow uses ref-based dedup against re-renders** — symptom of broad effect dependency array; cleaner pattern would be async/await in onPress handler instead of effect-watching the response object. | Refactor — bundle with state mgmt rework |

## WF2 Spec 93 RNFirebase migration — Round 2 review (commit: TBD, 2026-04-30)

Second-round adversarial pass with the user's 6-area focus (logic / security / dependencies / compliance / wiring / overall). Round 2 surfaced additional findings — most pre-existing in `authStore.ts` / `sign-up.tsx` and surfaced because the auth surface got fresh scrutiny. **Three CRITICAL pre-existing issues warrant their own WF3 in a future session** (called out below). Two migration-introduced findings were fixed inline (Spec 93 §5 phone + Apple nonce tests; jest `transformIgnorePatterns` for `@react-native-firebase`).

### WF3 candidates — pre-existing CRITICAL items (file separately, do not block this WF2 commit)

| Severity | Source | Item | Notes |
|----------|--------|------|------|
| CRITICAL | Code-Reviewer + DeepSeek | **Backup-email persistence bridge missing** — `mobile/app/(auth)/sign-up.tsx:156-180` `handleSubmitBackupEmail` validates and tracks the value but never writes it to any store, route param, or API. Spec 93 §3.3 / Spec 94 §3.3 SMS recovery path is non-functional from day one. The TODO comment ("onboarding reads this from a temporary store / route param") was present pre-migration; the bridge was never built. **WF3** with reproduction test that asserts `backupEmail` survives sheet-close and is readable by the onboarding store. |
| 🔥 PROMOTED 2026-05-03 CRITICAL | Gemini | **Auth-state reset placement leaks data on forced sign-out** — `mobile/src/store/authStore.ts` resets peer stores (filter/notification/onboarding/userProfile) inside `signOut()`, but Spec 93 §3.1 forced sign-out paths (admin disable, password change on another device, project token revocation) fire `onAuthStateChanged(null)` directly without going through `signOut()`. On a shared device, the next user sees the previous user's filter + notification + profile state until server hydration completes. Move all peer-store resets into the listener's null branch. **WF3** with test asserting forced-signout via `authStateHandler?.(null)` clears every peer store. **VERIFIED ACTIONABLE 2026-05-03**: listener `else` branch at `authStore.ts:242` calls `clearAuth()` (line 79: `set({ user: null, idToken: null, isLoading: false })`) only — peer-store resets at lines 114-117 live in `signOut()` and are skipped on this path. Spec 99 §B5 storeReset coverage test does not exercise the listener path. |
| CRITICAL | Code-Reviewer | **`@sentry/react-native@~7.2.0` known broken on RN 0.81 + New Architecture** — GitHub issue #5161; affects Xcode build (iOS only). Android build path unaffected today, but iOS EAS will fail. Upgrade to `^8.0.0` and run the v7→v8 migration. **WF3** scoped to Sentry version bump + migration. |

### DEFER — Round-2 follow-ups

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| HIGH | Code-Reviewer + Gemini | **`getIdToken()` race in onAuthStateChanged listener** — sign-out (forced or user) during in-flight token fetch causes `.then()` to resurrect the user via stale closure. Fix: re-check `auth().currentUser?.uid === firebaseUser.uid` inside `.then()` before calling `setAuth`. | Auth race-condition hardening WF |
| HIGH | Code-Reviewer | **`getIdToken()` cold-boot network failure clears auth silently** — Spec 93 §3.5 promises cached MMKV continuity offline; current catch-all clears auth on any error. Distinguish revoke codes (`auth/user-token-expired`, `auth/invalid-user-token`) from transient network failures. | Auth offline-continuity WF |
| HIGH | Code-Reviewer + DeepSeek | **OTP Resend Pressable has no `disabled`/loading guard** — `sign-in.tsx:622`, `sign-up.tsx:339`. Double-tap during `resendCooldown === 0` race window spawns two concurrent `signInWithPhoneNumber` calls before React state propagates. Add `disabled={phoneLoading || isAuthenticating}` + `accessibilityRole="button"`. | Auth UX polish WF (bundle with round-1 a11y items) |
| MED | Code-Reviewer | **Stale `user` snapshot on 401 retry race** — `apiClient.ts:100-103` captures `user` before `await getIdToken(true)`; if `clearAuth()` fires concurrently, `setAuth(staleUser, newToken)` resurrects a revoked user momentarily. Briefly visible to AuthGate. | Auth race-condition hardening WF |
| MED | Gemini | **MMKV `getItem` swallows errors silently** — `authStore.ts:24-30` returns `null` on read failure with no logging. If MMKV consistently fails (OS upgrade, library bug, corrupted storage), users sign out on every cold boot with no signal to debug. Add `Sentry.captureException` before fallback. | Observability hardening WF |
| MED | Code-Reviewer | **`fetchSignInMethodsForEmail` provider-array could leak via Sentry breadcrumbs** — `sign-in.tsx:184` returns the existing-account's enrolled methods; if the linking-error path is captured to Sentry, the provider list (PII per Spec 90 §11) goes with it. Scrub via Sentry `beforeSend` hook. | Sentry hardening WF |
| MED | Code-Reviewer | **Apple `id_token` in error path could land in Sentry** — `sign-in.tsx:223-243` if `signInWithCredential` throws, RNFirebase's error object can embed credential payload in `nativeErrorMessage`/`userInfo` on Android. Same Sentry scrubber needed. | Sentry hardening WF |
| MED | Code-Reviewer | **Spec 93 §5 says `@sentry/react-native/app-plugin` but `app.json` uses `@sentry/react-native`** — both resolve at runtime in v7.2.0 but the spec text and code disagree. Either align spec to code or update plugin entry. Latent if the package's exports map changes. | Spec/config sync — bundle with Sentry version bump WF3 |
| MED | DeepSeek | **BottomSheet `enablePanDownToClose` allows backup-email-stage dismissal** — `sign-up.tsx:131`; user can pan down at the backup-email stage and become authenticated without a recovery email. Conditionally disable when `phoneStage === 'backup-email'`. | Auth UX polish WF |
| MED | DeepSeek | **`errorMessage` not cleared on method toggle in sign-up** — `sign-up.tsx:100`; "Passwords do not match" leaks into the phone form when toggling. | Auth UX polish WF |
| LOW | DeepSeek | **Backup-email error not cleared on input edit** — `sign-up.tsx:169` field's `onChangeText` doesn't reset `errorMessage`. | Auth UX polish WF |
| LOW | DeepSeek | **No client-side email format validation in sign-up** — `sign-up.tsx:106` only checks presence; user sees raw Firebase error for `auth/invalid-email`. | Auth UX polish WF |
| NIT | DeepSeek | **Hardcoded `phoneNumber.length < 12` magic number** — same finding as round 1; `sign-up.tsx:108` mirror. | Auth UX polish WF |
| NIT | Gemini | **`partialize` ternary in `authStore.ts:113` is verbose but correct** — Gemini self-rescinded; no change needed. | No action |

## WF2 — Firebase Admin SDK init in dev (commit: TBD, 2026-05-01)

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| HIGH | Code-reviewer | **EACCES vs JSON parse error indistinguishable in dev** — `firebase-admin.ts loadFromPath` single catch swallows fs and JSON errors as one warning; a file present but unreadable silently falls through. DX issue. | Auth hardening WF (separate) |
| MED | DeepSeek | **DEFAULT_KEY_PATH is relative to process.cwd()** — `./secrets/firebase-admin-sdk.json` may not resolve to project root in all runtime contexts. Works in current dev/prod patterns; harden if a deployment context shifts cwd. | Auth hardening WF |
| MED | DeepSeek | **Module-level cache cannot be invalidated for credential rotation** — once `cachedApp` is set, env-var or file changes are ignored until process restart. Credential rotation requires either restart or hash-based cache invalidation. | Production runtime WF |
| MED | DeepSeek | **Silent dev fallback when explicit FIREBASE_ADMIN_KEY_PATH is malformed** — when the user explicitly sets the path env var but the file is bad, dev falls through to the default path silently. Should be louder. Partially mitigated by improved logging in this WF; full fix overlaps with EACCES separation above. | Auth hardening WF |
| LOW | Gemini | **Permanent-failure cache strategy floods logs** — every request that re-enters the init path on permanent-failure (e.g., malformed JSON) re-runs the full resolution and re-logs the warning. Cache the failure for ~1min or until restart. | Production runtime WF |
| NIT | DeepSeek | **NODE_ENV reliability for production check** — `isProduction()` reads `NODE_ENV === 'production'`; some PaaS use different conventions. Consider a build-time `IS_PRODUCTION` constant. | Build infrastructure WF |

## WF3 — Address deferred items from Firebase Admin WF2 (commit: TBD, 2026-05-01)

| Severity | Source | Item | Resolution |
|----------|--------|------|-----------|
| MEDIUM | Code-reviewer | #9 Spec 13 §5 missing new files | RESOLVED in 403adcc; verified in this WF3 |
| MEDIUM | DeepSeek | #10 DEFAULT_KEY_PATH relative to process.cwd() | RESOLVED — `path.resolve(process.cwd(), 'secrets', 'firebase-admin-sdk.json')` at module load |
| MEDIUM | DeepSeek | #11 Cache cannot invalidate on credential rotation | RESOLVED — source-identity tracking (SHA-256 hash for env, mtime for file). Production cache forever (rotation = redeploy). |
| MEDIUM | DeepSeek | #12 Silent fallback when explicit KEY_PATH malformed | RESOLVED — `loadFromPath` takes `isExplicit`; explicit failures escalate to logError |
| CRITICAL (from this WF3 review) | Code-reviewer | Stale-app reuse on rotation when firebase-admin has default registered | RESOLVED in this WF3 — `initializationAttempted` flag gates existing-default adoption; rotation-after-first-init path attempts re-init (which firebase-admin rejects) and returns null in dev rather than silently serving stale credentials. New regression test covers this scenario. |
| MEDIUM | Code-reviewer (this WF3) | TOCTOU: `existsSync` true but `statSync` throws → 'unstattable' sentinel treated as stable identity | DEFER — narrow race window (microseconds), requires two consecutive races. Fix path: replace stable 'unstattable' sentinel with per-call sentinel (e.g., `unstattable:${randomUUID()}`) so consecutive failures never match cache. Tracked for production runtime WF. |

## WF5 — Migration UP/DOWN audit (report: docs/reports/migration_down_audit_2026-05-01.md, 2026-05-01)

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| HIGH | Static-analysis sweep | **15 migrations have uncommented DDL under `-- DOWN`** — same bug class as 113/115/116 fixed in commit 68643b3. Files: 041, 042, 044, 045, 046, 051, 059, 060, 100, 101, 102, 103, 108, 111, 112. Production DB happens to be correct (effects re-applied by hand or later migrations), but a fresh checkout running migrations from zero would land in a broken schema. | WF3 batch fix (comment out DOWN sections in all 15) |
| HIGH | Spec 05 §4 routing escalation | **Pre-commit lint rule for migration UP/DOWN bug class** — 15 findings exceeds the 3+ threshold. Add `scripts/hooks/check-migration-down-comments.sh` chained into `.husky/pre-commit` to reject any new migration with uncommented DDL under `-- DOWN`. | WF2 (new lint rule infrastructure) |

## WF3 — Auth resolution order (commit: TBD, 2026-05-02 — Bearer-vs-cookie precedence fix)

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| CRITICAL | Gemini | **Timing attack on `cookie === DEV_SESSION_COOKIE` comparison** — Standard `===` terminates early on first mismatched character. Pre-existing in `verifyIdTokenCookie:58`; not introduced by this WF3 (only the auth ordering changed). Fix: `crypto.timingSafeEqual`. | Auth hardening WF |
| HIGH | Gemini | **ID token used as long-lived session cookie (architectural mismatch)** — Project uses `verifyIdToken` against the `__session` cookie (~1hr TTL) rather than the standard `createSessionCookie` / `verifySessionCookie` flow (14-day TTL). Spec 13 §3 documents both flows; the project chose the ID token path. Re-architecting requires PM/spec discussion. | Auth architecture WF (separate, requires spec decision) |
| HIGH | Gemini | **Silent 401 storm if firebase-admin not initialized in production** — Returns null instead of throwing. Already partially addressed in commit 403adcc which makes `getFirebaseAdmin()` throw at boot in production. The runtime call site (verifyIdTokenCookie:68-77) still returns null after logError. | Auth hardening WF |
| HIGH | DeepSeek | **Dev-mode bypass needs additional `NODE_ENV !== 'production'` guard** — Currently relies only on `isDevMode()` reading `DEV_MODE` env var. A misconfigured prod with `DEV_MODE=true` would bypass auth for the well-known `dev.buildo.local` cookie. Defense-in-depth: also check `NODE_ENV !== 'production'`. | Auth hardening WF |
| CRITICAL | DeepSeek | **`verifyIdToken(cookie)` does NOT check revocation** — Without `checkRevoked: true` second arg, Firebase Admin doesn't throw `auth/id-token-revoked`. Revoked tokens (e.g., post-password-change, admin-disabled accounts) work indefinitely. Spec 93 §3.1 says forced sign-out fires `onAuthStateChanged(null)` client-side, but server-side enforcement is missing. | Auth hardening WF (likely tied to architecture decision in HIGH item above) |
| MEDIUM | DeepSeek | **No length limit on Bearer/cookie token strings** — Attacker could send 1MB JWT to consume CPU/memory before Firebase rejects. Add reject for tokens >8KB before the shape check. | Auth hardening WF |
| MEDIUM | DeepSeek | **Init-failure logging happens on every call (hot path)** — Could flood logs during a transient init failure. Move check to `register()` in instrumentation.ts (already done in commit 403adcc for the eager init); the runtime check is now defense-in-depth and could be quieter. | Auth hardening WF |
| LOW | Gemini | **JWT 3-segment shape check is naive and brittle** — Would reject JWE (5-segment encrypted) tokens if ever needed; offers minimal security benefit on top of `admin.auth().verifyIdToken()`'s own validation. | Auth simplification WF |
| NIT | DeepSeek | Order of dev-bypass check vs shape check — micro-opt to short-circuit the well-known `DEV_SESSION_COOKIE` constant before splitting/checking. | Future cleanup |
| NIT | DeepSeek | `import type { NextRequest }` is only used in `getUserIdFromSession`; could be relocated. | Future cleanup |

## WF2 — AuthGate resume routing per Spec 94 §10 Step 11 (commit: TBD, 2026-05-02)

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| MEDIUM | Code-reviewer | **`'complete'` in OnboardingStep union is a future-bug trap** — Resolved inline via documentation in `getResumePath.ts STEP_TO_SCREEN`. The convention (markComplete() not setStep('complete')) is enforced by code review only, not the type system. Could be hardened further by removing 'complete' from the OnboardingStep union — would require auditing every consumer of the type. | Type narrowing WF (when convenient — convention enforcement is sufficient short-term) |
| LOW | Code-reviewer | **Realtor fallback `/(onboarding)/complete` return path is unreachable** — only fires if all of `currentStep=null`, `trade_slug='realtor'`, `location_mode` set, `tos_accepted_at` set, but `onboarding_complete=false`. The caller only invokes getResumePath when onboarding_complete=false, but the realtor screens patch onboarding_complete atomically with the other fields. Dead branch but harmless. | Code simplification WF (low priority) |
| LOW | Code-reviewer | **`onboardingCurrentStep` dependency causes extra effect runs mid-flow** — All re-runs are no-ops due to the `inOnboardingGroup` guard. No loop, no perf issue at this scale. | Performance pass (when relevant) |
| LOW | Code-reviewer | **`path.tsx` and `first-permit.tsx` don't call setStep** — Works in practice (prior screen's setStep value is correct on resume), but per a strict reading of Spec 94 §10 Step 11 every screen should explicitly advance currentStep on entry/exit. | Onboarding consistency WF |

## WF2 §9.14 Phase D — Adversarial trio review deferred items (commit 656e985, 2026-05-04)

The Phase D adversarial trio review (Gemini + DeepSeek + code-reviewer on commits `03c3af2`, `8e7539d`, `73c42f6` of the `notification_prefs` JSONB-flatten WF) surfaced 8 CRITICAL + 28 HIGH findings. 7 were applied inline (commit `656e985`); the items below are out-of-scope deferrals — pre-existing issues not introduced by §9.14 — captured here so they don't get lost in commit log search.

| Severity | Source | Item | Planned Home |
|----------|--------|------|-------------|
| CRITICAL | DeepSeek | **`src/app/api/user-profile/route.ts` PATCH `trade_slug` first-write race** — Two concurrent PATCH requests both see `trade_slug IS NULL`, both succeed, second wins. Violates "immutable after first write" contract. Pre-existing. | WF3 — atomic UPDATE with `WHERE user_id = $1 AND trade_slug IS NULL` precondition + `rowCount === 1` check, or wrap in transaction with SELECT FOR UPDATE |
| CRITICAL | DeepSeek | **`/api/user-profile` GET + PATCH use `SELECT *` / `RETURNING *`** — exposes internal fields (`stripe_customer_id`, `radius_cap_km`, `trade_slugs_override`) to the client. Any future internal column added to `user_profiles` is leaked automatically. | WF3 — explicit `CLIENT_SAFE_COLUMNS` whitelist shared with notifications/preferences route |
| HIGH | DeepSeek | **`/api/user-profile` PATCH `trade_slug` validation bypass** — `typeof rawBody.trade_slug === 'string'` check runs *before* `safeParse`, so `trade_slug` value bypasses Zod validation entirely. Future schema constraints (length, charset regex) won't be enforced. | WF3 — move trade_slug handling after `safeParse` and use `parsed.data.trade_slug` |
| HIGH | DeepSeek | **`/api/user-profile` GET issues writes via `applyFallbackTrialInitIfNeeded` + `applyTrialExpirationIfNeeded`** — violates HTTP semantics; caching proxies / `If-None-Match` / crawlers will trigger writes. If a helper fails (e.g., unique constraint), GET returns 500. | Future HTTP-semantics hardening WF — move side effects off GET path or attach `Cache-Control: no-store` |
| CRITICAL | DeepSeek | **`scripts/classify-lifecycle-phase.js` `dispatchPhaseChangePushes` N+1 query pattern** — one DB query per transition AND per stalled permit, inside the advisory lock. With thousands of phase changes daily, tips Cloud SQL into connection exhaustion. | WF3 — batch with `WHERE (permit_num, revision_num) = ANY($1::record[])` |
| CRITICAL | DeepSeek | **`scripts/classify-lifecycle-phase.js` `dispatchStartDateUrgentPushes` `SELECT DISTINCT ON` without matching `ORDER BY`** — PostgreSQL returns an arbitrary row per group; silently drops legitimate subscribers or returns wrong predicted_start. | WF3 — add `ORDER BY` clause matching the DISTINCT ON tuple plus a tiebreaker (`tf.predicted_start DESC`?) |
| HIGH | DeepSeek | **`callExpoPushApi` resolves regardless of `res.statusCode`** — A 4xx/5xx from Expo (rate limit, auth failure, malformed payload) is parsed as success. Silent push-loss class bug. | WF3 — reject promise (or return error envelope) when `statusCode < 200 || >= 300` |
| HIGH | Gemini | **`predicted_start BETWEEN INTERVAL '6 days' AND INTERVAL '7 days'` window is a sliding 24-hour timestamp range** — script runs once per day; permits earlier in the day fall outside on day N (~5.9 days away) and again on day N+1 (~4.9 days away). Notifications silently dropped. | WF3 — date-boundary comparison: `predicted_start::date = (NOW() + INTERVAL '7 days')::date` |
| HIGH | DeepSeek | **`classify-lifecycle-phase.js` `flushPermitBatch` filter skips `r.phase === null` transitions** — transitions from non-null phase to null (permit becoming unclassifiable after re-classification) are not recorded in `permit_phase_transitions`. Hides classifier regressions. | WF3 — change filter to `r.phase === r.old_phase` |
| HIGH | DeepSeek | **`classify-lifecycle-phase.js` push dispatch awaited inside `withAdvisoryLock` callback** — Network I/O to Expo (potentially 9K+ messages per batch) extends lock duration, blocks concurrent pipelines. | WF3 — move push dispatch outside the lock, or fire-and-forget with `.catch(logError)` |
| HIGH | DeepSeek + Gemini | **`mobile/app/(app)/settings.tsx` `localRadius` snapshot stale** — `const [localRadius, setLocalRadius] = useState(radiusKm)` initialises once and never re-syncs. If `radiusKm` updates from elsewhere (LeadFilterSheet, hydrate), the slider shows stale value. | Mobile UX hardening WF — derive from store or `useEffect(() => setLocalRadius(radiusKm), [radiusKm])` |
| HIGH | DeepSeek | **`mobile/app/(app)/settings.tsx` sign-out failure UX** — `signOut()` rejection only triggers an error haptic; no toast, no alert. Spec 99 §B5 requires user-visible feedback. | Mobile UX hardening WF — `Alert.alert("Sign out failed", ...)` in catch |
| HIGH | DeepSeek | **`mobile/app/(app)/settings.tsx` `useQuery` `isError` not handled** — On fetch failure, `data` is `undefined` and code falls back to `DEFAULT_PREFS`. User sees "all defaults" with no error indication; toggling one switch produces silent UI/server divergence. | Mobile UX hardening WF — read `isError`, render load-failure banner, disable switches until fetch succeeds |
| HIGH | DeepSeek + Gemini | **`mobile/src/lib/userProfile.schema.ts` (mobile + server) validation gaps** — bare `z.string()` for timestamps (should be `.datetime()`), bare numbers for `home_base_lat/lng` (should have `.min/.max` bounds + NaN guard), bare `z.number().int()` for `lead_views_count` (should be `.nullable().default(0)` for new accounts that LEFT JOIN to NULL). Mobile is more exposed because TanStack persists to MMKV across cold-boots. | Schema-tightening WF — add Zod refinements to both schemas |
| CRITICAL | DeepSeek | **`mobile/src/lib/userProfile.schema.ts` `stripe_customer_id` field persisted to MMKV** — TanStack persister writes the user-profile cache to MMKV. Any field in the schema lands unencrypted on disk. `stripe_customer_id` is PII per §2.1 which mandates Layer 4b SecureStore for PII, NEVER Layer 4a MMKV. | PII hardening WF — either remove `stripe_customer_id` from mobile schema (only needed server-side for portal redirect), or add `dehydrateOptions.shouldDehydrateQuery` filter that strips it before persistence |
| HIGH | DeepSeek | **`src/tests/user-profiles.security.test.ts` whitelist-stripping tests don't inspect SQL** — assert response JSON doesn't echo stripped fields, but never check the actual SQL UPDATE statement sent to the DB. A regression that adds a forbidden field to UPDATE SET clause would still pass as long as mocked SELECT-back returns the original value. Also: only 4 of ~20 server-authoritative fields tested for stripping (`onboarding_complete`, `tos_accepted_at`, `trial_started_at`, `lead_views_count`, `radius_cap_km`, `account_deleted_at`, `trade_slugs_override` all untested — `onboarding_complete` could let a user PATCH to `true` and skip onboarding). | Test hardening WF — assert against `mockQuery.mock.calls` SET clause; parameterise over every restricted field from §3.1 matrix |
| HIGH | DeepSeek | **`src/tests/user-profiles.security.test.ts` no unauthenticated-request test** — `*.security.test.ts` file with zero coverage of "missing session → 401" path. The most basic auth boundary has no regression lock. | Test hardening WF — add at minimum one PATCH and one GET test where `getUserIdFromSession` returns `null`, asserting 401 |
| HIGH | DeepSeek | **`src/tests/user-profiles.infra.test.ts` blanket default `mockResolvedValue([])`** — silently fills any unmocked query. Tests that expect exactly N queries pass even if implementation adds an N+1th query. Also: `applyFallbackTrialInitIfNeeded` + `applyTrialExpirationIfNeeded` mocked to no-op in every test, so the actual mutation paths these helpers run are entirely uncovered. | Test hardening WF — make default throw `Unexpected query`; add tests with profile state matching each helper's WHERE clause |
| HIGH | DeepSeek | **`mobile/__tests__/authGate.test.ts` corrupt-profile guard scoped only to `(auth)`** — Spec §5.2 says guard is group-agnostic. If implementation regresses to scope it to `(auth)`, corrupt profile in `(onboarding)` or `(app)` would route through with wrong-user data (PIPEDA risk). Also: no test covers `currentStep: null` → could produce `'/(onboarding)/null'` infinite redirect. | Test hardening WF — add corrupt-profile tests for `(onboarding)` + `(app)` segments + null currentStep cases |
| HIGH | DeepSeek | **`mobile/__tests__/bridges.test.ts` B2-B5 cross-reference guard is shallow** — only `expect(existsSync(...))` for sibling test files. If `storeIdempotency.test.ts` is gutted to `expect(true).toBe(true)`, this guard still passes. | Test hardening WF — static-analyse for required `describe`/`it` names, or remove the guard (CI runs all tests anyway) |
| MEDIUM | Gemini | **`mobile/__tests__/bridges.test.ts:121` redefines `sampleProfileBytes` as 4-field shape inside the §9.7b suite** — outer suite at line 45 uses the full 10-field shape. Both currently work but a structuralSharing regression on the full shape would not be caught by the inner suite. | Test consolidation — lift the outer definition to module scope, drop the inner |


---

## WF5 Spec 99 Audit — Findings to Triage (audit commit: TBD, 2026-05-04)

Source: `docs/reports/audit_spec99_2026-05-04.md`. Verdict was **GO** — no CRITICAL findings, all 16 §9 P0/P1 items verified DONE in HEAD. The items below are the 5 HIGHs + 3 MEDIUMs surfaced by the audit; pattern-routed proposals (Pattern A → §9.21, Pattern B → §9.22) are tracked separately in the audit report's Phase 5.

| Severity | Source | Item | Planned Home |
|---|---|---|---|
| HIGH | WF5 H1 | **§6.5 narrow `isFetching` carve-out at `(app)/_layout.tsx:216`** — `if (isFetching && profile.subscription_status === 'expired')` exists with good rationale (post-payment expired→active anti-flicker) but spec §6.5 forbids `isFetching` in gate conditions categorically. Resolve EITHER by amending §6.5 to permit narrow carve-outs gated by stable status fields, OR by refactoring (e.g., separate "post-payment-refetch-in-flight" flag set by a useMutation rather than reading the query loading state). | WF3 — pick (a) spec amendment or (b) refactor |
| HIGH | WF5 H2 | **§8.3 gate-stability tests MISSING** — zero tests across `mobile/__tests__/*.test.ts` reference `isFetching`. Spec mandates each render gate condition (per §6.5) MUST have a test asserting `isFetching` toggle does NOT flip the gate. Coupled with H1: the narrow carve-out is untested AND the broad-stripping has no regression guard. | WF3 — add `subscriptionGate.test.ts` cases for the carve-out + the broad gate |
| HIGH | WF5 H3 | **§7.3 router decision telemetry largely unimplemented** — zero `track('route_decision', ...)` calls anywhere in `mobile/`. Of the 4 mandated production-only events, only `signOut → /(auth)/sign-in` is partially covered (via `track('signout_initiated')`); the other 3 are missing: `cancelled_pending_deletion → forced sign-out` (compliance signal), `expired → active transition` (subscription conversion event), `reactivation_modal_shown` (compliance-critical — proves users saw the 30-day window prompt). | WF3 — wire `route_decision` (DEV) + the 3 missing production-only events |
| HIGH | WF5 H4 | **Mobile typecheck has 15 errors (pre-existing, multi-cause)** — buckets: `__tests__/bridges.test.ts` QueryObserver generic typing (×2), `__tests__/offline.test.ts` missing `@tanstack/query-sync-storage-persister` module (×1), `app/(app)/flight-board.tsx` + `app/(app)/index.tsx` FlashList v2 typing (×~10), `app/_layout.tsx` expo-notifications NotificationBehavior shape change + expo-router string→typed-path narrowing (×2). WF5 mandates 0 typecheck errors. | WF3 — type-only cleanup; not all are Spec 99 surface but blocks future WF5 GO verdict |
| HIGH | WF5 H5 | **MEMORY.md numeric claims dramatically stale** — `~/.claude/projects/.../memory/MEMORY.md` claims 41 migrations (actual 114), 36 specs (actual 67), 1823 tests (actual ~4990 root + 316 mobile). Per the auto-memory protocol, MEMORY.md is supposed to be a one-line-per-entry index, not a snapshot blob. The current shape was drift-prone by construction; the numbers being off by orders of magnitude is the symptom. | WF3 — full memory audit per the "MEMORY.md is an index, not a memory" protocol; migrate snapshot facts out of the index file |
| MEDIUM | WF5 M1 | **§9.10 / §B5 spec text says `removeQueries({queryKey:['user-profile']})`, code uses broader `queryClient.clear()`** — defensible (more conservative purge) but spec text is now stale. | WF2 doc-only — update spec to match implementation, OR refactor code back to scoped removal |
| MEDIUM | WF5 M2 | **§5.2 spec rule is `user_id !== user?.uid`, code has stricter `!user_id || user_id !== uid`** — falsy-uid guard added per Gemini WF2 §9.6 F1 + DeepSeek #2 consensus (security: corrupted/poisoned cache attesting to completion would otherwise bypass the guard). Stricter than spec, not weaker — needs spec amendment to document the falsy-uid case. | WF2 doc-only — add falsy-uid case to §5.2 |
| MEDIUM | WF5 M3 | **§9 Migration Plan table missing rows for 7 post-spec WF3 commits** — `3fa96a1` (cursor backward-compat → §9.17), `671aa87`+`202a9aa` (PII MMKV strip → §9.18), `381a0c9`+`f2f7147` (forced-signout cleanup unification → §9.19), `7bcb681` (dead-code sweep → §9.20). Spec is drifting from implementation by ~2 weeks of WF3 work. | WF2 doc-only — add §9.17-§9.20 |

**Pattern-routed (in audit report Phase 5, not filed individually here):**
- **§9.21 proposal** — `mobile/__tests__/spec99.mandates.lint.test.ts` that statically grep-asserts every spec mandate has implementation evidence (closes Pattern A from H2 + H3)
- **§9.22 proposal** — quarterly Spec 99 sync WF + `## Hardening Beyond Spec` subsections in §B5 / §5.2 (closes Pattern B from M1 + M2)
