# Review Follow-ups Log

**Append-only.** Tracks deferred items from independent + adversarial reviews so they don't get lost across WFs. Items move from the active table to the **Closed** section at the bottom when resolved.

**How to use:**
- Each WF that runs reviews appends a new section with its commit SHA(s) and date.
- One row per deferred item: severity, source, item, planned home, status.
- When closing an item, change `OPEN` to `closed-in-<commit-sha>` and move the row to the Closed section.
- Future independent review agents should read this file BEFORE generating their own checklist so they don't repeat known-deferred items.

**Severity legend:** CRITICAL · HIGH · MED · LOW · NIT
**Source legend:** Gemini · DeepSeek · Independent · Self
**Status legend:** OPEN · closed-in-<sha> · WONTFIX (with reason in parentheses)

---

## Active

| Sev | Source | Item | Planned home | Status |
|-----|--------|------|--------------|--------|
| MED | Gemini+DeepSeek | `LeadFeedItem` is a flat interface with nullable fields per lead type, not a discriminated union. Matches the SQL UNION ALL row shape but forces defensive null checks in consumers. | Phase 1 close | closed-in-(this commit) — refactored to `PermitLeadFeedItem \| BuilderLeadFeedItem` discriminated union; mapRow narrows on `lead_type`; consumers can safely access lead-type-specific fields without null checks |
| MED | DeepSeek | `TradeTimingEstimate` `confidence` and `tier` are independent string/number fields — impossible combinations like `{confidence:'high', tier:3}` are representable. Consider a discriminated union keyed on `tier`. | Phase 1 close | closed-in-(this commit) — refactored to a 3-branch DU keyed on `tier`. Tier 1 allows confidence `'high'` or `'low'` (the staleness fallback), Tier 2 is locked to `'medium'`, Tier 3 is locked to `'low'`. |
| MED | Gemini+DeepSeek | `cost-model.ts` string-based permit categorization via `.toLowerCase().includes(...)` is brittle against misspellings or unusual permit types. Matches the existing codebase pattern in `classifier.ts`. Extract to a shared categorization function in a future hardening WF. | Future hardening WF | OPEN |
| MED | Gemini+DeepSeek | `determineBaseRate` defaults unknown new-build structure types (e.g., "Institutional", "Industrial") to SFD rate. Arbitrary choice — spec 72 doesn't cover these categories. Document the default or add explicit Institutional/Industrial rates. | Future spec 72 update | DOCUMENTED — explicit JSDoc on `determineBaseRate` in this commit explains the best-effort default and points to this followup row. Adding explicit rates still requires a spec 72 update + dual code path matching constants. |
| MED | Gemini+DeepSeek | Cliff effect at `tenure_renter_pct = 50%` boundary (0.4 → 0.7 coverage). A 0.2% change in rent-pct flips a 75% cost difference. Spec-compliant but brittle. Smooth to interpolation in a future refinement. | V2 cost model refinement | OPEN |
| MED | DeepSeek | `compute-cost-estimates.js` uses single-row INSERTs inside the transaction (5000 sequential queries per batch). Bulk `INSERT ... VALUES` or `UNNEST` would be 50-100x faster. Fine for correctness; optimize later if nightly run time becomes an issue. | Phase 2+ perf optimization | OPEN |
| LOW | Gemini+DeepSeek | Tests for exported constants (`expect(BASE_RATES.sfd).toBe(3000)`) are snapshot-style change detectors, not behavior tests. Kept as spec-conformance tests; they lock the values to spec 72 table. Behavioral tests for each base rate added in commit `909b3d5`. | N/A (kept intentionally) | WONTFIX |
| LOW | Gemini+DeepSeek | `lat`/`lng`/score fields in `types.ts` are unconstrained `number`. Validation happens at Phase 2's `/api/leads/feed` Zod layer per spec 70. | Phase 2 API route | OPEN |
| LOW | Gemini | `model_version` hardcoded to 1. Will be incremented manually when formula changes; current = initial version. | On next formula change | OPEN |
| LOW | DeepSeek | Duplicate scope tags (e.g., `['pool', 'pool']`) would stack the addition twice. Extremely unlikely in real data; defer to source data hygiene. | Reactive | OPEN |
| LOW | Independent (retry pending) | Independent review agent hit 529 API overload twice on commit `a460904`. Dual code path was verified manually inline via constant-by-constant audit + 3 sample-input walkthroughs (documented in commit message and triage response). Full self-generated checklist deferred to next agent availability. | Re-run on next sub-WF or standalone | OPEN |
| MED | Independent | Spec 70 §Database Schema says "DOWN migration required (migration 067)" but the actual file is migration 070. Doc-only inconsistency. | Doc-only WF3 | closed-in-(this commit) — spec 70 line 62 updated to "(migration 070, after the spec-drift correction)" |
| MED | Independent | Spec 71 §Database Schema lists `Unique: (stage_name, trade_slug)` but the seed data + spec prose require precedence in the unique key. | Doc-only WF3 | closed-in-(this commit) — spec 71 line 29 updated to `(stage_name, trade_slug, precedence)` with prose explaining painting's dual-stage entry |
| MED | Independent | `db:generate` deferred during Phase 1a because local `npm run migrate` fails at pre-existing migration 030 (`column "updated_at" of relation "trade_mapping_rules" does not exist`). Drizzle types may drift from the new lead_views/cost_estimates/inspection_stage_map/timing_calibration tables. | Phase 1b (run after CI clean DB OR repair local migration 030) | OPEN — db:generate ran successfully in Phase 6 session for snapshot columns (migration 080). cost_estimates/timing_calibration tables still not in Drizzle schema but only accessed via raw SQL in CommonJS pipeline scripts (no TS consumer). Non-blocking. |
| LOW | Independent | No DB-roundtrip integration tests for the new CHECK constraints, FK CASCADEs, or XOR logic on lead_views. File-shape regex tests verify SQL structure but cannot catch a runtime constraint failure. Only safe to rely on once CI runs against a clean DB. | Phase 1b CI step | OPEN |
| LOW | Independent | No idx on `(trade_slug, precedence)` in inspection_stage_map. Fine for 21 seed rows; would matter if seed grows to thousands. | Reactive — only if seed grows | OPEN |
| LOW | Gemini | `src/lib/permits/types.ts` `PermitChange.old_value`/`new_value` typed as `string \| null` — loses type fidelity for numeric/date/boolean fields. Pre-existing, not touched by Phase 1a. | Future types-hardening WF | OPEN |
| LOW | Gemini | `src/lib/permits/types.ts` `PermitFilter.sort_by` typed as `string` — should be `keyof Permit` for compile-time safety + SQL injection defense. Pre-existing. | Future types-hardening WF | OPEN |
| LOW | Gemini | `src/lib/permits/types.ts` `Inspection.inspection_date` and `scraped_at` typed as `string` — inconsistent with `Permit` interface using `Date`. Pre-existing. | Future types-hardening WF | OPEN |
| LOW | Gemini | `src/lib/permits/types.ts` `Permit.dwelling_units_created`, `dwelling_units_lost`, `housing_units`, `storeys` typed as `number` (non-nullable) but ingested from raw strings — no protection against `""` / `"N/A"` producing `NaN`. Pre-existing. | Future types-hardening WF | OPEN |
| LOW | Gemini | `src/lib/permits/types.ts` `TradeMappingRule.match_field` typed as `string` — should be `keyof RawPermitRecord` to prevent typos. Pre-existing. | Future types-hardening WF | OPEN |
| LOW | Gemini | `src/lib/permits/types.ts` `Permit.location` typed as `unknown \| null` — no GeoJSON shape definition. Pre-existing. | Future types-hardening WF | OPEN |
| LOW | Gemini | `src/lib/permits/types.ts` `SyncRun.status` typed as generic `string` — should be a literal union. Pre-existing. | Future types-hardening WF | OPEN |
| LOW | Gemini | `src/lib/permits/types.ts` `Entity.is_wsib_registered: boolean` cannot represent the "unknown / not yet checked" state — should be `boolean \| null`. Pre-existing. | Future types-hardening WF | OPEN |
| MED | Stryker baseline 2026-04-08 | `cost-model.ts` mutation score 64.14% — 117 surviving mutants. Highest-leverage gaps: branch coverage on `determineBaseRate` permit_type fallback chain, `computeComplexityScore` scope tag stacking, `buildDisplay` formatting branches. Threshold is ≥ 50% (not failing) but the score has the most headroom of the 4 targets. | Targeted test additions, weekly Stryker run will track | closed-in-(this commit) — +42 boundary tests added; 64.14% → 79.01% (51 more mutants killed) |
| MED | Stryker baseline 2026-04-08 | `builder-query.ts` mutation score 50.00% — 11 surviving mutants. The SQL CASE expressions in the file are mostly tested via regex shape, not via behavior, so Stryker can flip thresholds (e.g., `>= 5 → > 5`) with no test failure. Add behavioral tests that exercise the SQL with mocked rows at each band boundary. | Targeted test additions | closed-in-(this commit) — +14 boundary tests added (toNumberOrNull, mapRow identity, parameter order, exact CASE band values); 50.00% → 63.64% (3 more mutants killed). Remaining 8 survivors are Stryker StringLiteral mutants on the multi-line SQL template which are hard to kill from the JS side — would need constant extraction. Deferred as LOW per the 63.64%→75% cost/benefit ratio. |
| LOW | Stryker baseline 2026-04-08 | `record-lead-view.ts` mutation score 91.84% — 4 surviving mutants. Inspect after the cost-model + builder-query gaps are closed. | Reactive | OPEN (still 91.84% after WF3; 4 survivors are all on the error-path logError string, low value to kill) |
| LOW | Stryker WF3 2026-04-08 | `builder-query.ts` remains at 63.64% after the boundary test additions. The 8 remaining survivors are StringLiteral mutants on the multi-line SQL template (Stryker treats the whole SQL as one literal, so mutating interior text like `'5'` inside the template isn't trivially killed by JS-side tests). Future fix: extract the band threshold constants out of the SQL template into a JS object and interpolate, then test the constants directly. | Future hardening | OPEN |
| MED | WF3 2026-04-08 | **Stryker break threshold raised 50 → 75.** Aggregate mutation score is now 80.87%; GH Actions mutation workflow fails any future regression below 75%. | N/A (policy change) | closed-in-(this commit) |
| LOW | WF3 2026-04-08 Pre-Review Self-Checklist | `scripts/compute-cost-estimates.js` (the JS sibling of `cost-model.ts` per ADR 001 dual code path) has NO matching JS-side boundary tests. The TS tests added in this WF lock the spec 72 constants via `contracts.infra.test.ts` and the JS file re-declares the same constants. Drift would still be caught by the shared constants contract, but a JS-side boundary test file (`compute-cost-estimates.infra.test.ts` additions) would close the gap completely. | Future hardening WF | OPEN |
| MED | WF3 2026-04-10 Self-checklist | `/api/admin/stats` fires 37 parallel `pool.query(...)` calls. Latent same-class pool-exhaustion risk as the `/api/admin/leads/health` bug fixed in this WF3. Queries are individually fast and have not triggered the timeout in production, but a future slowdown in any one of them could starve the pool. Either consolidate via `COUNT FILTER` or adopt the `/api/admin/leads/health` pattern. | Future hardening WF | OPEN |
| MED | WF3 2026-04-10 Self-checklist | 7+ sibling admin API routes return canned opaque 500 error strings (`market-metrics`, `rules`, `stats`, `pipelines/history`, `sync`, `builders`, etc.). Same class as the bug that hid the pool exhaustion for 3+ commits. Apply the `sanitizePgErrorMessage` + NODE_ENV-gated pattern to every admin route as a sweep. | WF6 sweep | OPEN |
| MED | WF3 2026-04-10 Adversarial (Bug #3) | `builders_feed_eligible` in `getLeadFeedReadiness` counts all globally-WSIB-eligible builders (via the wsib_per_entity CTE filters), but the actual feed additionally applies `ST_DWithin` against the querying user's coordinates. A dashboard showing "618 builders feed eligible" may overstate how many a user actually sees. Pre-existing from commit `8528f16`, not introduced by this fix. Field name is accurate per JSDoc ("intersection of wsib_per_entity CTE filters") but potentially misleading at the UI layer. Either rename to `builders_wsib_eligible` or add a prominent caveat. | Future UX/naming WF | OPEN |
| LOW | WF3 2026-04-10 Adversarial | `src/lib/db/client.ts:26` — `parseInt(process.env.PG_PORT || '5432', 10)` has the same NaN/zero/negative trap as the WF3-fixed `PG_POOL_MAX` / `PG_CONNECTION_TIMEOUT_MS` parsers. Pre-existing and not introduced here. Apply `parsePositiveIntEnv` to `PG_PORT` as part of a future config-hardening WF. | Future config hardening | OPEN |
| LOW | WF3 Phase 1 Adversarial + Independent | `getCostCoverage` in `src/lib/admin/lead-feed-health.ts` initializes `coverage_pct_vs_active_permits: 0` as a placeholder that the route handler overrides. JSDoc explains the contract but nothing at the TypeScript level prevents a future caller from using `getCostCoverage(pool)` directly and silently consuming the 0. Type-safe fix: have `getCostCoverage` return `Omit<CostCoverage, 'coverage_pct_vs_active_permits'>` and only the route handler adds the field. | Future type-safety hardening | OPEN |
| LOW | WF3 Phase 1 Independent | `permits_with_cost` in `getLeadFeedReadiness` counts cost_estimates with `estimated_cost IS NOT NULL` regardless of permit status, while `active_permits` filters to ADMIN_ACTIVE statuses. The derived `coverage_pct_vs_active_permits` can exceed 100% if cost_estimates lags permit cancellations. Phase 1 decision: display honestly (no cap) because >100% is an actionable signal that the cache has drifted. Future fix option: add a permit-status JOIN filter to the numerator query, scoped to ADMIN_ACTIVE. | Future query hardening | OPEN |
| NIT | WF3 Phase 1 Independent | `getTrafficLight` boundary tests cover 48.0h and 72h but not 0h (impossibly fresh) or 47.9h. 48.0 is the critical boundary; the others would be redundant with the fix. | Skip unless a regression recurs | WONTFIX |
| LOW | WF3 Phase 2 Adversarial | `getCachedLeadFeedHealth(pool, ttlMs?)` accepts a `pool` parameter but the cache is a module-level singleton keyed by nothing. If a future caller passes a different `Pool` instance within the TTL window, both see data fetched by whichever pool populated the cache first. Production has one shared pool so it's invisible today; a multi-pool setup would need a cache key derived from pool identity. JSDoc now documents the limitation. | Future multi-pool WF | OPEN |
| LOW | WF3 Phase 2 Adversarial | `getCachedLeadFeedHealth` returns the cached `LeadFeedHealthResponse` by reference — no defensive clone. If any caller mutates the returned object, subsequent cache hits within the TTL window see the mutation. Current route handler passes it directly to `NextResponse.json()` without mutation; this is a latent trap, not a production bug. JSDoc now warns. Fix option: `structuredClone()` on cache read (cheap for this payload shape). | Future hardening | OPEN |
| LOW | WF3 Phase 2 Independent | No cache-bypass mechanism in production. `getCachedLeadFeedHealth` has no `force: true` option — a future CLI health check or debug script wanting fresh data would need to call `getLeadFeedReadiness`/`getCostCoverage`/`getEngagement` directly. `__resetLeadFeedHealthCacheForTests` exists but is labelled test-only. | Future debug tooling WF | OPEN |
| NIT | WF3 Phase 2 Independent | Next.js dev HMR wipes module state on hot reload, orphaning `inFlight` promises across reloads. The `finally` block prevents wedging but the behavior isn't mechanically tested. Hard to test without a Next.js dev harness. Documented in code comment. | Skip unless a regression recurs | WONTFIX |
| NIT | WF3 Phase 2 Adversarial | `HEALTH_CACHE_TTL_MS=0` cannot disable caching at runtime — `parsePositiveIntEnv` silently falls back to the 30s default. An operator expecting "0 = off" would be confused. JSDoc now documents this. Fix option: special-case 0 to mean "no cache" (would require overloading parsePositiveIntEnv semantics, higher cost than the benefit). | Skip | WONTFIX |
| LOW | WF3 Phase 3 Adversarial | No DB-level CHECK constraint enforces the `(saved = false ⟺ saved_at IS NULL)` invariant on lead_views. Current enforcement is application-layer only (record-lead-view.ts CASE expression + recorder JSDoc). A future pipeline script writing `UPDATE lead_views SET saved = true WHERE ...` without populating saved_at would produce rows invisible to `saves_7d`/`saves_today` metrics. Fix option: `ALTER TABLE lead_views ADD CONSTRAINT lead_views_saved_at_invariant CHECK ((saved = false AND saved_at IS NULL) OR (saved = true AND saved_at IS NOT NULL))`. Deferred because there is currently exactly one writer (record-lead-view.ts) which enforces the invariant correctly, and a CHECK constraint would reject ad-hoc debug UPDATEs. | Future pipeline-hardening WF | OPEN |
| LOW | WF3 Phase 3 Adversarial | `getEngagement` daily aggregation removes the outer `WHERE viewed_at >= CURRENT_DATE - 7d` clause, forcing a full-table sequential scan on `lead_views`. At current table size this is imperceptible; at 1M+ rows (plausible at scale) the COUNT(*) FILTER aggregate could degrade to 50-200ms. BRIN index on viewed_at and the new partial index on saved_at cannot accelerate a predicate-free aggregate. Mitigation option: add `WHERE viewed_at >= CURRENT_DATE - 7d OR saved_at >= CURRENT_DATE - 7d` which allows the BRIN to skip pre-window pages while still catching saves on old-viewed leads. | Future perf hardening | OPEN |
| LOW | WF3 Phase 3 Adversarial | No DB-roundtrip integration test exercises the Phase 3 recorder/query SQL against a real pg instance. All new tests use `vi.fn()` mocks — a syntax error in the CASE expression would pass all tests and only fail at runtime. The project has a `db.test.ts` infra pattern; Phase 3 has no counterpart. | Phase 1b CI step (same deferral as prior DB integration tests) | OPEN |
| NIT | WF3 Phase 3 Independent | Pre-existing Drizzle schema.ts bug: `idx_lead_views_lead_trade_viewed` covering index uses `op("timestamptz_ops")` on `tradeSlug` (varchar) — should be `text_ops`. Not introduced by Phase 3, carried forward by the Phase 3 regen. | Future Drizzle hygiene WF | OPEN |
| LOW | WF3 2026-04-11 Independent | `/api/leads/feed` (user-facing) also calls `getLeadFeed` but has no `isPostgisAvailable` pre-flight. A dev hitting the user feed without PostGIS gets a 500 with sanitized error message (already in place) but not the actionable 503 + install instructions. The test-feed route gets the pre-flight; the user feed doesn't. Not a production issue (Cloud SQL has PostGIS) but a dev experience gap for whoever works on the user feed next. | Future dev-experience WF | OPEN |
| LOW | WF3 2026-04-11 Independent | `sanitizePgErrorMessage` strips credentials from `error.message` but doesn't touch pg error object fields `detail`, `hint`, `where`, `file`, `routine`. The route handler only serializes `error.message` to the client (so no leak today), but nothing prevents a future refactor from spreading `error` or accessing `error.detail` and leaking schema/column names. Consider a type-safe wrapper or an explicit test asserting only `message` is in the response body. | Future hardening | OPEN |
| LOW | WF3 2026-04-11 Adversarial | 500 error envelope shape mismatch between sister routes: `/api/admin/leads/health` returns `{ error: message }` (bare string) while `/api/admin/leads/test-feed` returns `{ data: null, error: { code, message }, meta: null }` (structured). Pre-existing; not introduced by this WF3. The dashboard's `extractErrorMessage` helper (added earlier this session) already handles both shapes, so the UI is not broken. Harmonize in a future cleanup — probably move the health route to the structured envelope. | Future admin envelope harmonization | OPEN |
| LOW | WF3 2026-04-11 Adversarial | No behavioral integration test asserts that `getLeadFeed` is NEVER called when `isPostgisAvailable` returns false. The file-shape test now includes a positional assertion (`isPostgisAvailable(pool)` index < `getLeadFeed(` index) which catches the most obvious regression, but a true behavioral test would mock the helper and assert the route handler short-circuits. Left as file-shape because route-handler-level tests require a different harness. | Future integration test pattern | OPEN |
| LOW | WF1 Phase 7 2026-04-11 Adversarial near-miss | Error-path observability gap in `/api/leads/feed`: perf marks are captured during the try block but NOT passed through to `internalError()` on the catch path, and are NOT emitted on 401/403/503/429 early returns. Operationally, a slow-path failure that takes 3 seconds in `getLeadFeed` shows a `duration_ms` in logs but no `perf_marks` breakdown — exactly the data you'd want to debug the failure. Fix is a one-liner on the catch path (`return internalError(cause, { route: ..., perf_marks: perf.toLog() })`) plus threading `perf.toLog()` through the early-return helpers. Deferred because the early-return helpers have tight contracts and the change touches `error-mapping.ts` for every leads route. | Future observability WF | OPEN |
| NIT | WF1 Phase 7 2026-04-11 Independent | `LeadFeedHeader.tsx` filter trigger button has no `aria-controls="lead-filter-sheet"` linkage to the DrawerContent's `id`. The code comment at LeadFilterSheet.tsx:105 acknowledges the expected linkage but it isn't wired. Pre-existing, not introduced by Phase 7. Minor a11y completeness gap; screen readers use implicit focus flow instead. | Future a11y polish | OPEN |
| NIT | WF1 Phase 7 2026-04-11 Independent | No integration test verifying that an OS-level `prefers-reduced-motion` toggle mid-session takes effect on the next `hapticTap` call. The unit test (`haptics.logic.test.ts:92-107`) locks the behavior via mock `matchMedia`, but no real-browser test covers the toggle. Acceptable for Phase 7 (unit coverage is sufficient), worth noting for QA. | QA manual test | OPEN |
| LOW | WF3 2026-04-11 Value-check | Local dev Test Feed Tool returns `503 DEV_ENV_MISSING_POSTGIS` — migrations 039/067/078 marked applied but PostGIS-dependent content never ran. Full drift context preserved in git history + prior commit 53dcb29. | Migration 083 | closed-in-(this commit) — PostGIS 3.6.2 installed at OS level + migration 083 replays 039/067/078 content with 221,559 permits backfilled (100% match). Live verified: `/api/leads/feed` now returns HTTP 200 with real data. |
| LOW | WF3 2026-04-11 migration 083 Independent | Migration 074 (entities.photo_url + photo_validated_at + CHECK constraint) had the SAME historical drift class as 039/067/078 — marked applied but columns missing. Surfaced as `column e.photo_url does not exist` when 083's live verification ran the feed. | Migration 084 | closed-in-(this commit) — migration 084 replays 074's columns and CHECK constraint with idempotent pg_constraint lookup. |
| HIGH | WF3 2026-04-11 migration 083 Adversarial | `scripts/migrate.js:179` used a bare `/\bCONCURRENTLY\b/i.test(sql)` regex that matched the keyword anywhere in the file INCLUDING comments. Migration 083's operator-runbook comments mentioned `CREATE INDEX CONCURRENTLY` multiple times, which routed the entire migration through the non-transactional `splitTopLevelStatements` path. Consequence: the DROP TRIGGER → CREATE TRIGGER cycle was not atomic (a crash between the two leaves the trigger absent permanently) and the 221K-row backfill ran outside any transaction. The 083 run happened to succeed, but the pattern is a production regression waiting to happen on any future migration that documents CONCURRENTLY without using it. | `scripts/migrate.js` | closed-in-(this commit) — `migrate.js` now strips `--` line comments, `/* */` block comments, and `$tag$...$tag$` dollar-quoted bodies BEFORE testing for CONCURRENTLY. Regression test in `migrate-runner.infra.test.ts` locks the strip logic. |
| LOW | WF3 2026-04-11 migration 083 Adversarial | Migration 084's `pg_constraint` lookup (`WHERE conname = 'entities_photo_url_https'`) scopes by constraint name only, not by `conrelid`. A same-named constraint on a DIFFERENT table would cause a false skip. Pre-existing pattern inherited from migration 039's FK adds. Low practical risk because constraint names in this repo are distinctive, but the defensive form `AND conrelid = 'entities'::regclass` would be stricter. | Future migration hygiene | OPEN |
| LOW | WF3 2026-04-11 migration 083 Adversarial | Migration 080 has PARTIAL drift — `cost_estimates_null_cost` and `timing_calibration_avg_sample` columns on `data_quality_snapshots` are marked added in the migration file but missing from the live schema. Not blocking the feed, so not repaired in the 083/084 cycle. Could affect quality/health dashboard edge cases. | Future drift sweep | OPEN |
| NIT | WF3 2026-04-11 migration 083 Adversarial | Migration 083 uses `ST_GeomFromGeoJSON(geometry::text)` on `parcels.geometry` / `neighbourhoods.geometry` jsonb without a pre-validation guard. Production rows are already migrated so the backfill WHERE clause catches zero rows in prod. On dev, malformed jsonb would abort the entire migration. Low risk given data source, but a defensive `WHERE jsonb_typeof(geometry) = 'object'` guard would be safer. | Future migration hygiene | OPEN |
| LOW | WF3 2026-04-11 migration 083 Independent + Adversarial | Broader drift audit: unknown how many other migrations in 040-082 have the same drift class. Only 067/078 had defensive `RETURN` guards that could silently no-op; others would have errored loudly at apply time. 039/074/080 drifts are historical (DB restore, manual tampering). A comprehensive audit would cross-reference every migration's column adds against `information_schema.columns`. | Future drift audit WF | OPEN |
| NIT | WF3 2026-04-11 migration 083 Drizzle regen | Drizzle-kit codegen emits `unknown(...)` (uncompilable) for PostGIS system view columns typed `name`. Fixed by adding `tablesFilter: ['!geometry_columns', '!geography_columns', '!spatial_ref_sys']` to `drizzle.config.ts`. Nothing in src/ references these views. | `drizzle.config.ts` | closed-in-(this commit) |
| HIGH | WF2 2026-04-11 Lifecycle Adversarial (C5) | `aic-scraper-nodriver.py` (the Python inspection scraper) writes new rows to `permit_inspections` but does NOT bump `permits.last_seen_at`. The lifecycle classifier's incremental predicate is `last_seen_at > lifecycle_classified_at`, so a permit that just passed a new inspection stage would NOT be picked up by the next classifier run until the downstream `classify-inspection-status.js` step bumps `last_seen_at`. Latency is bounded by deep_scrapes chain cadence, not a correctness bug but a staleness gap for the P9-P17 construction sub-stage signal. Fix: bump `permits.last_seen_at` on every inspection insert in the Python scraper, OR change the classifier's dirty predicate to also check `EXISTS (SELECT 1 FROM permit_inspections WHERE permit_num = p.permit_num AND created_at > p.lifecycle_classified_at)`. | `scripts/aic-scraper-nodriver.py` | OPEN |
| MED | WF2 2026-04-11 Lifecycle Adversarial (H1) | `daysBetween()` JS uses `Math.floor((later.getTime() - earlier.getTime()) / MS_PER_DAY)` while SQL reproducer uses `(NOW()::date - issued_date)`. If Postgres session timezone and Node process timezone differ (e.g., pg session = UTC but Node = America/Toronto), the two can disagree at day boundaries — a permit issued "exactly 30 days ago" could be P7a in JS and P7b in SQL. Production runs both on the same host with the same TZ so it's latent, but a future cloud deployment or DST change could surface it. Fix: normalize both sides to UTC date or force `SET TIME ZONE 'UTC'` at the top of the reproducer. | Future timezone hardening | OPEN |
| MED | WF2 2026-04-11 Lifecycle Adversarial (H6) | Migration 085's DOWN block is commented out (forward-only per project convention), but nothing prevents a future operator from running a manual `ALTER TABLE permits DROP COLUMN lifecycle_phase` — which would immediately break `/api/leads/feed` because `get-lead-feed.ts:123-124` hardcodes `p.lifecycle_phase` / `p.lifecycle_stalled` in the SQL. There is no `COALESCE(lifecycle_phase, 'P18')` defensive fallback, no feature flag env var. Defender options: (a) wrap the projection in `COALESCE(p.lifecycle_phase, NULL)` and have mapRow fall back to `TIMING_DISPLAY_BY_CONFIDENCE`, (b) add `LIFECYCLE_PHASE_ENABLED=1` env gate, (c) document as "do not drop" in the schema doc. Deferred because the project convention is forward-only migrations and the DOWN block being commented is the enforcement mechanism. | Future rollback hardening | OPEN |
| MED | WF2 2026-04-11 Lifecycle Adversarial (M5) | `scripts/quality/assert-lifecycle-phase-distribution.js` exists as a Tier 2 CQA check (per spec §3.3) but is NOT wired into any chain in `manifest.json`. The classifier's own `unclassified_count <= 100` gate runs inline, so the assertion script is the only thing checking per-phase distribution bands — and nothing fires it. Wire it into both permits and coa chains as the step immediately after `classify_lifecycle_phase`. | Followup WF3 | OPEN |
| MED | WF2 2026-04-11 Lifecycle Adversarial (M6) | `EXPECTED_BANDS` in `assert-lifecycle-phase-distribution.js` are hardcoded against the 2026-04-11 snapshot. Three months from now after CKAN normalization drift, these bands will be stale and the assertion will false-positive. The P9-P17 combined band is also `min: 0, max: 80000` which accepts any value, making the assertion useless for those phases. Fix: rebuild the bands with a 10-day moving average + last-reviewed date, OR scrap the bands in favour of a day-over-day delta check. | Future CQA refinement | OPEN |
| MED | WF2 2026-04-11 Lifecycle Adversarial (M1-M4) | Test coverage gaps in `src/tests/lifecycle-phase.logic.test.ts`: (M1) no combinatorial test for orphan + every dead/P20/P19 status — only `Cancelled` is explicitly tested in combination. (M2) no test for `Permit Issued` + `has_passed_inspection=true` + `latest_passed_stage=null` (the defensive P18 branch). (M3) no test pinning the semantics of a 3-year-old permit with a recent passing inspection (P18 vs stalled?). (M4) the `computeStalled` Signal 3 test uses `has_passed_inspection: true` but the signal only requires `status='Inspection' + latest_inspection_date>180d`. Add targeted tests for these gaps. | Future test hardening | OPEN |
| LOW | WF2 2026-04-11 Lifecycle Independent | `O4` (orphan closed) is declared in `VALID_PHASES` and `LIFECYCLE_PHASE_DISPLAY` but is architecturally unreachable — the decision tree short-circuits closed statuses to `P20` before the orphan branch runs. The test at line 207 of lifecycle-phase.logic.test.ts explicitly pins `P20 not O4` for this case. The `LIFECYCLE_PHASE_DISPLAY.O4` entry at `src/features/leads/lib/lifecycle-phase-display.ts:57` is dead code but not a bug. Either remove the entry or add an explicit "unreachable by design" comment next to it. | Future cleanup | OPEN |
| LOW | WF2 2026-04-11 Lifecycle Independent | CoA `NORMALIZED_DEAD_DECISIONS` includes `'closed'` and `'delegated consent refused'` which are NOT documented in spec §1.1. The SQL reproducer was updated to match, so the round-trip gate won't catch the divergence. These were presumably found empirically in the live DB but there's no test comment or spec note explaining the provenance. A CoA row with `decision = 'Closed'` (the lone 'closed') gets killed silently. Document in the spec or add an inline comment citing the source. | Future spec update | OPEN |
| LOW | WF2 2026-04-11 Lifecycle Adversarial (M8) | `lifecycle_phase` is `VARCHAR(10)` per migration 085, which is a tight constraint. The current phase domain uses 2-3 character codes (P7a, P20, O3) so there's headroom, but any future hyphenated or stateful phase codes (e.g., `'O3-stalled'`) would fail with `value too long`. Document the limit or widen to VARCHAR(20) preemptively. | Future spec refinement | OPEN |
| LOW | WF2 2026-04-11 Lifecycle Adversarial (M9) | `scripts/link-coa.js:339` uses `INTERVAL '1 second'` buffer on the `permits.last_seen_at` bump idempotency guard. This only prevents rapid-fire retries (<1s apart); two cron firings 2+ seconds apart would both pass the guard and double-bump. Production has one cron instance so the guard is sufficient today, but a distributed scheduler would need an advisory lock. | Future distributed-cron hardening | OPEN |
| LOW | WF2 2026-04-11 Lifecycle Adversarial (M10) | `TIMING_DISPLAY_BY_CONFIDENCE` is still exported from `src/features/leads/lib/get-lead-feed.ts` and imported by `get-lead-feed.logic.test.ts:7` even though `mapRow` no longer reads it. The old legacy describe block still runs. Remove the export + test block in a followup sweep. | Future cleanup | OPEN |
| LOW | WF2 2026-04-11 Lifecycle Independent | No infra test simulates the `lifecycle_classified_at` stamp failure path — the per-batch `withTransaction` rewrite (this commit) ensures phase + stamp commit atomically, but a behavioral test that injects a transient pg error mid-transaction and asserts no partial state would be stronger than regex shape tests. | Future test pattern | OPEN |
| HIGH | WF1 Phase 2 2026-04-12 Adversarial (HIGH-2) | Stale `old_phase` from JS memory can log incorrect `from_phase` in transitions if a concurrent writer (load-permits.js, link-coa.js) changes the permit's phase between the dirty SELECT and the batch UPDATE. Advisory lock prevents concurrent classifiers but not other pipeline scripts. Risk is low (narrow window, self-correcting on next run) but bad calibration data is hard to detect. Fix: use RETURNING from the batch UPDATE to capture the pre-update phase, or accept and document. | Future hardening | OPEN |
| MED | WF1 Phase 2 2026-04-12 Adversarial (MEDIUM-1) | The 242K-row `phase_started_at` backfill UPDATE runs as a single un-batched statement, locking all rows simultaneously. One-time cost (self-healing via `WHERE phase_started_at IS NULL` guard), but blocks concurrent writers for ~30-60s on the first run. Consider chunking into 10K batches in a future hardening pass. | Future hardening | OPEN |
| MED | WF1 Phase 2 2026-04-12 Independent (Item 10) | Phase 2 state-machine transition detection has no behavioral logic tests — only regex shape tests. A `lifecycle-phase.logic.test.ts` entry should exercise: (a) first classification produces from_phase=null transition, (b) stalled-only change produces no transition, (c) P7a→P7b is suppressed, (d) P7a→P20 is logged. | Future test hardening | OPEN |
| MED | WF1 Phase 2 2026-04-12 Independent (Item 9) | Terminal-state transitions (any-phase → null/dead) are intentionally suppressed by the `r.phase !== null` filter. The calibration engine cannot measure "time from active phase to closure" from transitions alone without joining back to permits. Document this design choice in the calibration engine spec (Phase 3). | Phase 3 spec | OPEN |
| HIGH | WF1 Phase 3 2026-04-12 Adversarial (Probe 4) | `TRADE_TARGET_PHASE` maps each trade to ONE lifecycle phase, but MEP trades (plumbing, hvac, fire-protection) have TWO active phases: P12 (rough-in) and P15 (final). A plumber subscribing to leads would miss "plumbing final coming in N days." The flight tracker (Phase 4) needs either `Map<string, string[]>` or a `TRADE_SECONDARY_PHASE` mapping to generate predictions for both phases. | Phase 4 design | OPEN |
| MED | WF1 Phase 3 2026-04-12 Adversarial (Probe 5) | The flight tracker's fallback for uncalibrated phase pairs is a hardcoded 30-day default. If the actual median for a rare pair is 150 days, the prediction is 120 days too optimistic. Better: two-tier fallback (specific → all-types aggregate → configurable default), or store a `default_gap_days` in the calibration table. | Phase 4 design | OPEN |
| MED | WF1 Phase 3 2026-04-12 Adversarial (Probe 1) | `PREV_STAGE_TO_PHASE_SQL` built via `.replace(/stage_name/g, 'prev_stage')` — works today but fragile if a future edit adds `stage_name` in a comment or string literal. Refactor to a template function `stageToPhaseCase(colName)`. | Future hardening | OPEN |
| LOW | WF1 Phase 3 2026-04-12 Adversarial (Probe 2) | Per-row UPSERT (207 individual `pool.query()` calls) is 200x more round-trips than necessary. A single multi-row `INSERT...VALUES ON CONFLICT` would be cleaner. Not blocking at 1.1s for a daily batch. | Future perf cleanup | OPEN |
| LOW | WF1 Phase 3 2026-04-12 Independent (Gap 1) | No concrete stage-name parity test: infra test checks structural patterns but never executes SQL CASE against known inputs and compares to `mapInspectionStageToPhase()` output. | Future test hardening | OPEN |

---

## Closed

| Sev | Source | Item | Closed in | Notes |
|-----|--------|------|-----------|-------|
| CRITICAL | Gemini | Migration 072: missing `CHECK (min_lag_days <= max_lag_days)` allowing nonsensical lag windows | `909b3d5` | Also confirmed by DeepSeek MED |
| CRITICAL | DeepSeek | Migration 072: `stage_sequence` had no enforcement of the known-vocabulary constraint, allowing rogue sequence values that would break Tier 1 timing engine sequence-gap math | `909b3d5` | Closed via `CHECK (stage_sequence IN (10,20,30,40,50,60,70))` |
| HIGH | DeepSeek | Migration 071: `premium_factor DECIMAL(3,2)` permitted negatives, violating spec 72's documented 1.0–2.0 range | `909b3d5` | Closed via `CHECK (premium_factor >= 1.0)` |
| MED | DeepSeek | Migration 072: `precedence` lacked positive-value enforcement | `909b3d5` | Closed via `CHECK (precedence > 0)` |
| LOW | DeepSeek + Independent | Migration 071: no integrity check that `cost_range_low <= cost_range_high` when both populated | `909b3d5` | Closed via composite CHECK |
| HIGH | Gemini | Migration 070: UNIQUE `(user_id, lead_key, trade_slug)` "blocks re-viewing" — flagged as bug | n/a | WONTFIX — per spec 70 this is a state table with upsert semantics; the `saved` column makes the design explicit |
| HIGH | Gemini + DeepSeek | Migration 070: DOWN block is non-functional (commented out) | n/a | WONTFIX — codebase pattern (065/066/069 follow same convention); table is brand-new with zero data, rollback risk is purely structural |
| HIGH | Gemini | Migration 070: ON DELETE CASCADE on FKs to permits/entities is "dangerous" | n/a | WONTFIX — explicitly per spec 70 §Database Schema with cleanup-strategy discussion |
| HIGH | Gemini + DeepSeek | Migration 070: `user_id` lacks FK to a users table | n/a | WONTFIX — per spec 70 explicitly: "user_id NOT a FK (Firebase UID)" |
| HIGH | DeepSeek | Migration 070: XOR CHECK constraint "logically broken" | n/a | FALSE ALARM — DeepSeek misread; the CHECK is logically tight (verified by manual case analysis) |
| CRITICAL | DeepSeek | Migration 070: FK to `permits(permit_num, revision_num)` "assumes composite PK that may not exist" | n/a | FALSE ALARM — verified `migrations/001_permits.sql` PK is `(permit_num, revision_num)` composite |
| LOW | DeepSeek | Migration 070: VARCHAR(30)/VARCHAR(10) lengths "may not match permits table" | n/a | FALSE ALARM — verified, lengths match exactly |
| MED | Gemini | Migration 072: stage_name/trade_slug have no FK to lookup tables | n/a | WONTFIX — codebase-wide convention; trade slugs are referenced by string everywhere |
| MED | Gemini + DeepSeek | All migrations: should use ENUM types instead of VARCHAR + CHECK | n/a | WONTFIX — codebase consistency; CHECK approach is established |
| HIGH | Gemini | Migration 070: polymorphic table is an anti-pattern; should split into permit_lead_views + builder_lead_views | n/a | WONTFIX — per spec 70 §Database Schema explicit design decision |
| HIGH | Gemini | `src/lib/permits/types.ts` 7 pre-existing issues (sort_by, PermitChange, Inspection dates, etc.) | n/a | OUT OF SCOPE — this WF didn't touch those fields; tracked above as LOW for future hardening WF |

## 2026-04-08 — Phase 1b-i Cost Model (commits a460904 + Phase 1b-i review fixes)

| Sev | Source | Item | Closed in | Notes |
|-----|--------|------|-----------|-------|
| HIGH | Self (test caught it) | `determineBaseRate` returned `addition` rate ($2000) for `permit_type='Interior Alteration'` because `'alteration'` substring matched before any `'interior'` check. Spec 72 distinguishes "Addition/alteration" from "Interior renovation" — they should NOT collapse. | this commit | Fixed in BOTH cost-model.ts and compute-cost-estimates.js (dual code path) by adding `'interior'` branch BEFORE `'alteration'`. Test caught it. |
| HIGH | Gemini | `formatDistanceForDisplay` rounded 999.5 → "1000m" (shadows the 1.0km format) | this commit | Fixed via `Math.floor` for sub-km values |
| MED | Gemini+DeepSeek | `formatDistanceForDisplay` had no defense against NaN/Infinity/negative inputs | this commit | Fixed via `Number.isFinite` guard returning "—" placeholder |
| HIGH | Gemini | cost-model SFD test used loose `toBeGreaterThan(1_000_000)` instead of exact value | this commit | Replaced with `toBeCloseTo(1_380_000, 0)` |
| HIGH | Gemini | cost-model residential vs commercial test used `not.toBe()` (no direction check) | this commit | Replaced with explicit `toBeCloseTo` for both + `toBeGreaterThan` direction assertion |
| HIGH | Gemini | cost-model only had behavioral test for SFD base rate; missing for semi/town, multi-res, addition, commercial, interior_reno | this commit | Added 5 explicit behavioral tests with exact expected values |
| LOW | Gemini | cost-model fallback range test wrapped assertions in `if (!== null)` blocks that silently skip | this commit | Replaced with `expect().not.toBeNull()` then unconditional spread calc |
| HIGH | DeepSeek | `compute-cost-estimates.js` advisory lock could "leak forever on crash" | this commit | Added inline comment explaining `pg_try_advisory_lock` is session-scoped — Postgres auto-releases on connection close, which the SDK does on script exit. No code change needed; clarification only. |
| CRITICAL | Gemini | "dual code path is a time bomb" — manual sync between TS and JS guaranteed to drift | n/a | WONTFIX — per CLAUDE.md §7 Backend Mode rule 8, dual code path is the established pattern for classification/scoring/scope. Cross-reference comments + infra test asserting both files reference the same constant names + manual byte-for-byte audit in commit message. Future hardening WF can extract to shared JSON. |
| CRITICAL | Gemini+DeepSeek | `LeadFeedItem` "grab-bag of nullable fields" violates discriminated union | n/a | WONTFIX for Phase 1b-i — flat shape mirrors the SQL UNION ALL row. Tracked above as MED for Phase 2 refactor when consumer patterns are known. |
| HIGH | Gemini+DeepSeek | `model_version` hardcoded to 1 "defeats versioning" | n/a | WONTFIX — version 1 IS the current model version; bumped manually on formula change. Tracked above as LOW. |
| HIGH | Gemini+DeepSeek | `determineBaseRate` defaults unknown new builds to SFD | n/a | Tracked above as MED — needs spec 72 update to cover Institutional/Industrial categories. Code defensible as best-effort default. |
| HIGH | Gemini+DeepSeek | `computeComplexityScore` stacks +10 per scope tag, "double-counts" | n/a | DEFENSIBLE — spec 72 literally says "Complex scope (underpinning, elevator, pool): +10 each". "Each" = each present scope. |
| HIGH | DeepSeek | `compute-cost-estimates.js` `flushBatch` does sequential single-row INSERTs | n/a | Tracked above as MED for Phase 2 perf optimization. Correct, just slow. |
| HIGH | DeepSeek | `LATERAL SELECT parcel_id ORDER BY ASC LIMIT 1` arbitrary parcel pick | n/a | DEFENSIBLE — already documented in Phase 1b-i plan Risk Note 3 as known limitation. Permits with multiple parcels are rare. |
| HIGH | Gemini+DeepSeek | `tenure_renter_pct > 50` cliff effect | n/a | Tracked above as MED for V2 cost model refinement. |
| HIGH | DeepSeek | `PLACEHOLDER_COST_THRESHOLD = 1000` rejects legit small jobs | n/a | DEFENSIBLE — per spec 72 explicit decision: ">1000 threshold filters out placeholder values like $1". |
| HIGH | DeepSeek | premium_factor not applied to scope_additions ("$80K pool same in low-income as premium") | n/a | DEFENSIBLE — per spec 72 "Scope additions ... additive AFTER the area × rate × premium calculation, never multiplied". |
| HIGH | DeepSeek | premium tier off-by-one boundary | n/a | FALSE ALARM — verified manually: `income=60000` matches tier 2 (`60000 >= 60000 && 60000 < 100000`). |
| HIGH | DeepSeek | `est_const_cost::float8` cast fails on non-numeric strings | n/a | FALSE ALARM — source column is `DECIMAL(15,2)` per migration 001, not text. Cast is safe. |
| MED | DeepSeek | `batch = []` not cleared on flushBatch throw, retries indefinitely | n/a | FALSE ALARM — `batch = []` is OUTSIDE the inner try/catch, runs regardless of throw. |
| HIGH | Gemini | infra tests "tautological / box-ticking" | n/a | DEFENSIBLE — file-shape tests are a deliberate fallback because local DB is broken at migration 030 (Phase 1a known blocker). They lock the SQL structure against drift; behavioral tests will run in CI against a clean DB. |
| MED | Gemini | distance.test "tests for constants are change-detector noise" | n/a | DEFENSIBLE — constants ARE the spec 72 contract; tests serve as spec-conformance locks. Behavioral tests for the constants USING them now also added. |

## 2026-04-08 — Phase 1b-ii Timing Engine (commits 13657da + Phase 1b-ii review fixes)

| Sev | Source | Item | Closed in | Notes |
|-----|--------|------|-----------|-------|
| CRITICAL | Gemini | timing.ts `pickBestCandidate` siblings query had no ORDER BY → non-deterministic fallback when no phase match | this commit | Fixed via `ORDER BY p.issued_date DESC NULLS LAST, p.permit_num ASC` for stable sort |
| CRITICAL | Gemini+DeepSeek | compute-timing-calibration.js JOIN ignored revision_num — every revision of a permit shared the same first_inspection_date, biasing percentiles | this commit | Fixed via `permit_root` CTE that collapses permits by `permit_num` (DISTINCT ON, earliest issued_date) BEFORE the inspections join. permit_inspections has no revision_num column so we can't filter on it; collapsing at the source is the correct approach. |
| HIGH | Gemini+DeepSeek | timing.ts `getGlobalMedianCalibration` was unweighted arithmetic mean of percentiles — statistically unsound | this commit | Fixed: now sample-weighted by `row.sample_size`. Permit types with 10K observations correctly outweigh those with 25. |
| HIGH | DeepSeek | timing.ts cache load failure set `calibrationLoadedAt = now`, blocking retry for 5 min | this commit | Fixed: leave `calibrationLoadedAt` at 0 on failure so the next call retries. Comment added explaining the deliberate behavior. |
| MEDIUM | Gemini+DeepSeek | compute-timing-calibration.js `PERCENTILE_CONT(...)::int` truncated fractional days, biased downward | this commit | Fixed via `ROUND(PERCENTILE_CONT(...))::int` for all 3 percentiles |
| MEDIUM | DeepSeek | timing.ts Tier 2 when `elapsedDays > p75` produced misleading "0-0 weeks remaining" | this commit | Fixed: explicit overdue branch returns "your trade should be active now or recently completed" or "your trade window may have passed" depending on phase match |
| HIGH | Gemini | timing.ts cache load race condition (2 concurrent first-callers) | n/a | DEFENSIBLE — both writes converge to the same Map. Worst case is one redundant DB query during cold start. Documented as future hardening if calibration_query becomes a hotspot. |
| HIGH | Gemini | timing.ts `daysBetween` DST off-by-one | n/a | FALSE ALARM — `Date.getTime()` returns UTC ms; division by `86_400_000` is exact in UTC. |
| MEDIUM | Gemini | timing.ts `getStageSequence` substring match fragile (e.g., "Interior Final Framing" matches both interior-final and framing) | n/a | DEFENSIBLE — stage names are deterministic from the AIC portal scraper, not free-form text. The 7 known stage_names are the universe. Substring order in the function reflects spec 71's stage taxonomy. |
| HIGH | DeepSeek | timing.ts staleness only checks PASSED inspections (permits with all-failed inspections never go stale) | n/a | DEFENSIBLE — spec 71 explicitly: "If the latest passed stage is more than 180 days old". Per-spec by design. |
| HIGH | DeepSeek | compute-timing-calibration.js `pp_self.revision_num` filter missing | n/a | FALSE ALARM — query is on permits/permit_inspections, not permit_parcels. permit_inspections has no revision_num. Different concern resolved by the permit_root CTE fix above. |
| HIGH | Gemini+DeepSeek | compute-timing-calibration.js stale permit_types not deleted (table accumulates obsolete rows over time) | n/a | DEFERRED — Tier 2 fallback handles missing/stale rows via the global median + 30-day staleness check. Cleanup is V2 hardening. |
| MEDIUM | Gemini | compute-timing-calibration.js N+1 inserts (50-200 single-row UPSERTs per run) | n/a | DEFENSIBLE — dataset is small (50-200 rows max). Bulk INSERT is V2 perf optimization. |
| CRITICAL | Gemini | compute-timing-calibration.js try/catch inside transaction loop "defeats atomicity" | n/a | DEFENSIBLE per spec — partial calibration is acceptable because consumers (timing.ts Tier 2) tolerate missing rows via the global median fallback. Spec 71 says "Failure recovery: per-batch try/catch — next nightly run picks up failures". |
| HIGH | DeepSeek | `HAVING COUNT >= 5` too low for stable percentiles | n/a | DEFENSIBLE — threshold is a coverage/stability tradeoff. Spec 71's calibration audit shows median sample sizes well above 5 for production permit_types; 5 is a floor, not a target. |
| MEDIUM | DeepSeek | `xmax = 0` insert detection unreliable | n/a | DEFENSIBLE — PostgreSQL-internal but well-known idiom. Same pattern used in `compute-cost-estimates.js` (Phase 1b-i) and the existing pipeline scripts. Cosmetic for the records_new/records_updated metric. |
| MEDIUM | DeepSeek | timing.ts test mock query order coupling | n/a | DEFENSIBLE — explicit ordering documented in the `stub` helper. Real DB integration tests in CI will provide behavior coverage when local DB is repaired. |
| HIGH | Gemini+DeepSeek | timing.ts test "missing Tier 2 coverage" | n/a | DEFERRED to Phase 1b-iii follow-up test pass — current 20 tests cover the structural correctness; comprehensive Tier 2 calibration scenarios will land alongside `get-lead-feed.ts` tests in Phase 1b-iii. |
| LOW | Independent (deferred) | Independent review agent hit Anthropic 529 overload twice on commit 13657da | Phase 1b-iii or standalone | Adversarial reviews (8 of 8) provided coverage. Dual code path verification (cost-model only) and structural walkthroughs done inline. Sample input traces verified manually below. |

## 2026-04-09 — Comprehensive Review Pass (DeepSeek + Sonnet Independent across Phases 0-3, commit TBD)

Seven-review pass (DeepSeek phases 0/1/2/3/overall + Sonnet Phase 3 holistic + Sonnet overall holistic) against commit `d360e0a`. DeepSeek phase 3 and overall hit reasoning-token overflow on large bundles and returned empty bodies — covered by the Sonnet reviews instead. Total 5 usable reviews + 60 existing OPEN rows triaged.

**Fixed in this commit (18 rows):**

| Sev | Source | Item | Closed in |
|-----|--------|------|-----------|
| HIGH | DS-P0 | Migration 067 trigger missing lat/lng WGS84 range validation | this commit (migration 077) |
| HIGH | DS-P0 | Migration 074 HTTPS CHECK allows malformed URLs (`https://`, `https:///`) | this commit (migration 077 regex tightening) |
| HIGH | DS-P1 | `buildLeadKey` `padStart('000', 2, '0')` returns `'000'` but PG `LPAD('000', 2, '0')` truncates to `'00'` — silent mismatch for revision_num length > 2 | this commit (`.slice(-2)` normalization) |
| HIGH | DS-P1 | `buildDisplay` `>= 1.35` hardcoded vs `PREMIUM_TIERS[2].multiplier` drift | this commit (index reference) |
| CRIT | DS-P2 | POST /api/leads/view missing Content-Type validation — client can send text/plain and trip `request.json()` into cryptic errors | this commit (415 INVALID_CONTENT_TYPE + 3 tests) |
| HIGH | DS-P2 | `getCurrentUserContext` may return non-string `trade_slug` despite TypeScript contract | this commit (defensive null/empty guard + logError) |
| MED | DS-P2 | `envelope.err()` doesn't safely serialize `details` — circular refs crash NextResponse.json | this commit (JSON.stringify round-trip fallback) |
| LOW | DS-P2 | `logRequestComplete` negative duration from clock skew | this commit (`Math.max(0, ...)`) |
| CRIT | Sonnet-P3 | `--color-timing-past` tone from spec 74 never implemented — past leads render as distant gray instead of red | this commit (token + Past branch + test) |
| HIGH | Sonnet-P3 | `--color-timing-future` naming drift vs spec 74 `--timing-distant` | this commit (renamed token + Tone label) |
| MED | Sonnet-P3 | SaveButton `aria-pressed` + dynamic `aria-label` double-announcement | this commit (stable aria-label "Save lead") |
| MED | Sonnet-P3 | `haversine.ts` in `hooks/` should be in `lib/` per spec 75 §1.1 | this commit (file move + 2 import updates) |
| HIGH | Sonnet-OV | `TimingBadge` requires `confidence` but `PermitLeadFeedItem` never had it | this commit (SQL CASE + type + test fixtures) |
| HIGH | Sonnet-OV | `OpportunityBadge` requires `OpportunityType` but feed only returns raw permit_type | this commit (SQL CASE ILIKE classifier + type) |
| MED | Sonnet-OV | `FORCED_REFETCH_THRESHOLD_M` + `COORD_PRECISION` missing from `_contracts.json` | this commit (contracts + 2 new assertions) |
| MED | Sonnet-OV | `DEFAULT_FEED_LIMIT = 15` hardcoded twice in `useLeadFeed` instead of imported | this commit (import from get-lead-feed) |
| MED | Sonnet-OV | `captureEvent` fires before mutation → phantom telemetry on 403/429 rejection | this commit (compensating `lead_feed.lead_save_failed` event) |
| MED | Existing followup line 138 | `scripts/purge-lead-views.js` unimplemented (PIPEDA 90-day retention release blocker) | this commit (batched retention script with dry-run + reconcile stub) |

**Dismissed as false alarms (10 rows):**

| Source | Claim | Why dismissed |
|--------|-------|---------------|
| DS-P0 CRIT | FK `revision_num INTEGER` vs `VARCHAR(10)` mismatch | Verified: `permits.revision_num IS VARCHAR(10)`. DeepSeek hallucinated integer type. |
| DS-P0 HIGH | `route-guard.isValidSessionCookie` only shape-checks JWT | Edge middleware is intentionally shape-only; full `verifyIdToken` via firebase-admin runs in `src/lib/auth/get-user.ts`. |
| DS-P0 HIGH | In-memory rate-limit production race | Prod fallback already logs critical error (line 108-115) + documented as bypass risk. |
| DS-P0 MED | Upstash cache memory leak | Cache is keyed by `(limit, windowSec)` not per-user; max 2 entries ever for feed+view. |
| DS-P1 HIGH | `mapRow` silent drop on malformed UNION rows | Already `logWarn`'d per Phase 0+1+2 fix (commit 449fb2a). |
| DS-P1 HIGH | Cursor tuple comparison subtle | Works correctly per prior Phase 2 holistic verification. |
| DS-P1 CRIT | `LPAD('A', 2, '0')` breaks uniqueness for non-numeric revision_num | `'0A'` is still a valid lead_id; permit_num is the discriminator. |
| DS-P1 MED | `tier2IssuedHeuristic` negative elapsedDays | Already clamped at `timing.ts:406` via `Math.max(0, ...)`. |
| DS-P2 HIGH | Trade slug comparison case-sensitive | Both sides come from the DB via the same insertion path; drift not possible without an explicit migration. |
| DS-P2 MED | POST route body size limit | Next.js default is 1MB. |

**Deferred to follow-up (24 rows):**

| Sev | Source | Item | Planned home |
|-----|--------|------|--------------|
| MED | DS-P0 | Migration 072 `stage_sequence IN (10..70)` brittle enum | Future V2 migration |
| MED | DS-P0 | Migration 067 PostGIS silent skip in dev | Documentation update |
| LOW | DS-P0 | `session.ts` `--` separator collision risk | Reactive |
| MED | DS-P1 | `computed_at: now()` per-call batch variation | Phase 2+ perf optimization |
| MED | DS-P1 | `isCommercial` keyword gap (industrial/warehouse/hotel) | Spec 72 update |
| MED | DS-P2 | Trade slug case collation | Deferred — both sides from DB |
| MED | Sonnet-P3 | Button/Card primitives hardcoded to Buildo dark palette | ADR 7 draft (accept as leads-scoped) |
| HIGH | Sonnet-P3 | `_hasHydrated` gate not consumed by any component yet | Phase 3-iii consumer must wire it |
| HIGH | Sonnet-P3 | Tremor React 19 compat risk (runtime works, peer warning only) | Monitor |
| CRIT | Sonnet-P3 | TimingBadge per-tone icons (spec 74 colorblind safety: lightning/clock/calendar/hourglass/cross) | Phase 3-vi polish |
| MED | Sonnet-P3 | ProgressCircle non-standard `radius`/`strokeWidth` props silently dropped | Phase 3-vi polish |
| MED | Sonnet-P3 | V1 75-card cap not enforced in hook | Phase 3-iv consumer enforcement |
| LOW | Sonnet-P3 | ProgressCircle arc shows scaled 0-100 but label shows raw 0-30 | Phase 3-vi polish |
| HIGH | Sonnet-OV | Feed-vs-view `logRequestComplete` shape asymmetry | Operational runbook note |
| MED | Sonnet-OV | `lead_views` first-save sets `viewed_at=NOW()` even without prior view | Acceptable edge case per spec 70 §4 |
| existing | MED | cost-model permit categorization brittle | Future hardening WF |
| existing | MED | tenure_renter_pct 50% cliff effect | V2 cost model refinement |
| existing | MED | compute-cost-estimates sequential INSERTs | Phase 2+ perf |
| existing | MED | db:generate deferred on pre-existing migration 030 | Closed via prior self-heal commit |
| existing | LOW × 8 | `src/lib/permits/types.ts` pre-existing typing gaps | Future types-hardening WF |
| existing | LOW | Stryker builder-query 63.64% survivors | Future hardening |
| existing | LOW | compute-cost-estimates.js no JS-side boundary tests | Future hardening |
| existing | LOW | purge-lead-views Firebase reconciliation pass | Phase 4+ (stub exists in scripts/purge-lead-views.js via --reconcile flag) |
| existing | LOW | inspection_stage_map 12 unmapped trades | Future seed expansion |

---

## 2026-04-08 — Phase 0+1+2 Cross-Phase Holistic Review (Gemini + DeepSeek + Independent, commit TBD)

Full-bundle review of `23a32a5^..HEAD` (migrations 067-076 + lib/ + api/ + routes). 3 reviewers, self-generated checklists, cross-phase focus (findings that per-file reviews missed).

| Sev | Source | Item | Closed in | Notes |
|-----|--------|------|-----------|-------|
| CRITICAL | Independent | Permit pillar boundaries drifted from spec 70 §4: `value_score` used 0-30 bands and `opportunity_score` used 0-10 bands, but spec lines 234-235 pin both at 0-20. Total `relevance_score` summed to 100 by arithmetic accident, masking the per-pillar contract break. Any UI/test/analytic consuming individual pillars reads the wrong scale. | this commit | Rescaled both CTE CASE expressions in `get-lead-feed.ts`: value mega→20/major→16/large→12/medium→8/small→5/else→3; opportunity Permit Issued→20/Inspection→14/Application→10/else→0. Also rescaled builder CTE's value/opportunity to the same 0-20 boundaries. Header comment + 1 new regression test lock the spec-aligned bands. |
| HIGH | Independent | `builder-query.ts` fit_score could reach **23** (0-20 base + WSIB +3 bonus), pushing `relevance_score` to 103 and breaking spec 70 §4's documented 0-100 ceiling. | this commit | Wrapped the fit CASE + WSIB bonus in `LEAST(..., 20)` to cap at 20. 1 new regression test asserts the LEAST cap survives future refactors. |
| HIGH | Independent | `buildLeadKey` + `LEAD_FEED_SQL.lead_id` had NO normalization for `revision_num`. Verified via psql that the permits table contains BOTH `'0'` and `'00'` values (loader uses `'00'` but earlier ingestions left bare `'0'`). Two paths writing the same permit revision could produce different `lead_key` strings → broken `UNIQUE (user_id, lead_key, trade_slug)` dedup and inconsistent competition counts. | this commit | `buildLeadKey`: `padStart(2, '0')` on permit revisions. `LEAD_FEED_SQL`: `LPAD(p.revision_num, 2, '0')` in the permit lead_id expression. 2 new regression tests (buildLeadKey canonicalization + SQL LPAD presence). |
| HIGH | Gemini | Stale comment in feed route: `// Call the Phase 1 lib function — never throws (try/catch wrapper below is defense in depth)` — contradicted the lib's post-review contract (now throws). Future refactorer could remove the try/catch. | this commit | Comment corrected: now explicitly says the lib throws on DB error and the outer try/catch converts to 500 via `internalError()`. |
| MED | Gemini | `mapRow` defensively returns null on malformed SQL UNION shape (belt-and-suspenders against a hypothetical SQL refactor drift) but silently filters the row — any future bug where rows disappear from the feed would be invisible. | this commit | Added `logWarn` in both null-return branches with `lead_id + lead_type` context. If the defensive null ever fires, it now shows up in logs. |
| MED | Independent | `scripts/purge-lead-views.js` is mandated by spec 70 §Operating Boundaries (90-day retention + weekly Firebase orphan reconciliation) but does NOT exist. PIPEDA/GDPR retention requirement unimplemented. | Phase 3 | closed-in-WF3-followups — file exists (created 2026-04-09), now registered in manifest.json |
| LOW | Independent | `inspection_stage_map` seed (migration 072) covers 21 of 32 trade slugs. The 12 unmapped trades (excavation, shoring, insulation, glazing, elevator, demolition, pool-installation, solar, security, temporary-fencing, caulking, eavestrough-siding) always fall to Tier 2 calibration in `timing.ts` even when inspection data is available. Not a correctness bug (Tier 2 is the documented fallback) but a data-quality gap. | Future seed expansion | OPEN |
| CRITICAL | Gemini | Migration 067 admits `CREATE INDEX CONCURRENTLY` isn't supported → manual production step | n/a | KNOWN — documented in migration 067 comment + Phase 0+1 final review. Operational mitigation in place. |
| CRITICAL | Gemini | Migration 070's `VARCHAR(100)` + UNIQUE constraint created UID truncation collision risk | n/a | CLOSED — migration 076 widened to VARCHAR(128). Historical collision risk is theoretical (no production deploy yet). |
| CRITICAL | Gemini | Dual code path `cost-model.ts` / `compute-cost-estimates.js` is a ticking time bomb | n/a | WONTFIX — CLAUDE.md §7 Backend Mode rule 8 explicitly blesses dual code path for classification/scoring/scope. Constants audit is the discipline. Shared-JSON extraction is a future hardening WF. |
| CRITICAL | DeepSeek | Migration 067 PostGIS extension check fails silently | n/a | FALSE ALARM — the migration uses `CREATE EXTENSION IF NOT EXISTS postgis`, not a conditional check. Trigger/index creation is gated on the column existing via `IF NOT EXISTS`. |
| CRITICAL | DeepSeek | `getLeadFeed` throws but loses SQL error context | n/a | FALSE ALARM — `internalError()` in `error-mapping.ts` calls `logError(cause, context)` with the full stack via `src/lib/logger.ts`. The generic client message is by design (no stack leak). |
| CRITICAL | DeepSeek | `trades.slug` may not be unique → duplicate JOIN rows | n/a | FALSE ALARM — migration 004 line 6: `slug VARCHAR(50) UNIQUE NOT NULL`. Verified. |
| CRITICAL | DeepSeek | Race condition between upsert + competition count read | n/a | DEFENSIBLE — spec 70 §API Endpoints Race condition note explicitly accepts the 1-2 count skew. |
| HIGH | Gemini | `premium_factor DECIMAL(3,2)` caps at 9.99, risking overflow | n/a | FALSE ALARM — `cost-model.ts` PREMIUM_TIERS max is 1.6. Headroom is 6x the largest defined value. |
| HIGH | Gemini | `estimateCost` impure (calls `new Date()` internally) | n/a | DEFERRED — minor testability improvement, out of scope for this review. |
| HIGH | Gemini | `builder-query.ts` positional lat/lng args are a footgun | dc8951a | **CLOSED 2026-04-09** — `builder-query.ts` was deleted entirely as dead code in the Gemini deep-dive WF3 (commit dc8951a). The standalone builder query was orphaned (no API route called it) and its scoring math diverged from the unified `get-lead-feed.ts` builder CTE. The footgun is gone with the file. |
| HIGH | DeepSeek | `determineBaseRate` gaps on institutional/industrial permits | n/a | TRACKED — Phase 1b-i followup, needs spec 72 update. |
| HIGH | DeepSeek | `pickBestCandidate` missing permit_parcels index | n/a | TRACKED — Phase 1b-ii followup. |
| HIGH | DeepSeek | LEAD_FEED_SQL UNION ALL planner inefficiency | n/a | DEFENSIBLE — planner CSEs the distance expression; spec 70's reference sketch uses the same pattern. |
| HIGH | DeepSeek | `cursor_score.int()` vs SQL `$6::int` — fractional score risk | n/a | DOCUMENTED — Phase 2-i followup with explicit inline comment in schemas.ts. |
| MED | DeepSeek | `cost_tier` nullable may violate implicit contract | n/a | DEFENSIBLE — CHECK permits NULL by design; `estimateCost` handles the null path. |
| MED | Gemini | `getStageSequence` substring matching brittle | n/a | TRACKED — Phase 1b-ii followup (DEFENSIBLE: stage names are deterministic from AIC portal). |
| MED | Gemini | `pickBestCandidate` JS-side iteration instead of SQL | n/a | TRACKED — Phase 1b-ii perf followup. |
| MED | Gemini | Cursor mixed DESC is "non-standard" | n/a | DEFENSIBLE — PostgreSQL row-value comparison with `<` on DESC tuples is the documented correct pattern. Phase 2 holistic review verified. |
| MED | DeepSeek | `TradeTimingEstimate` tier 1 'low' confidence could be set when not stale | n/a | DEFENSIBLE — DU allows `tier:1, confidence:'low'` exactly for the staleness fallback. No other code path sets it. |
| MED | DeepSeek | `formatDistanceForDisplay` 999.9m vs 1.0km discontinuity | n/a | CLOSED in Phase 1b-i — `Math.floor` for sub-km values. |
| MED | DeepSeek | `computeBuildingArea` returns 0 when no footprint → null estimate | n/a | DEFENSIBLE — spec 72 explicitly: "permits with no area data get no estimate". Fallback defaults would mislead users. |
| MED | DeepSeek | Calibration cache backoff on DB failure | n/a | DEFENSIBLE — current design (retry on next call, no exponential backoff) is spec-compliant. Retries are 1-per-request, bounded by rate limit. |
| MED | DeepSeek | PII in request logs | n/a | DEFENSIBLE — spec 70 §API Endpoints Observability mandates `{user_id, trade_slug, lat, lng, ...}` logging. Users opted in. |
| MED | DeepSeek | Rate limit cross-trade bypass | n/a | FALSE ALARM — trade_slug must match profile trade; user cannot alternate. |
| LOW | Gemini+DeepSeek | Various nits: IF NOT EXISTS on manual index, `toNumberOrNull` NaN silent, colon in permit_num, `err()` Content-Type | n/a | DEFENSIBLE or false alarms — NextResponse.json sets Content-Type; permit_num uses spaces not colons; toNumberOrNull returning null IS the signal. |

## 2026-04-08 — Phase 2 Holistic Review (Gemini + DeepSeek + Independent, commit TBD)

All Phase 2 sub-phases (2-i foundation, 2-ii feed route, 2-iii view route) reviewed in one pass against `fcbe04a..HEAD` bundle. 3 reviewers, self-generated checklists. Triage below.

| Sev | Source | Item | Closed in | Notes |
|-----|--------|------|-----------|-------|
| HIGH | Independent | `getLeadFeed` silently returned `emptyResult` on DB error — full outage surfaced as HTTP 200 with empty feed, violating spec 70 §API Endpoints "500 on unexpected error". Invisible to clients and monitoring. | this commit | Changed `getLeadFeed` to re-throw after logging. Route's existing try/catch converts to `internalError()` → 500 envelope. Updated file header, updated `get-lead-feed.logic.test.ts` to assert throw instead of empty-on-error. Existing `api-leads-feed.infra.test.ts` regression test (`mockRejectedValueOnce`) already covered the 500 path and now encodes the contract. |
| MED | Independent | `competition_count` counted save/unsave actions as "views" — every save refreshed `viewed_at = NOW()`, artificially keeping leads in the 30-day competition window and violating spec 70 §4 Behavioral Contract line 249: "Only count views, not saves — saves are private." | this commit | Removed `SET viewed_at = NOW()` from the save/unsave ON CONFLICT branch. Only the `view` action refreshes the timestamp now. New regression test locks the semantics: splits the SQL on `DO UPDATE` and asserts `viewed_at = NOW()` does NOT appear in the save/unsave conflict clause. |
| MED | Independent | Double-logging: both `getLeadFeed`/`recordLeadView` (lib layer) AND `logRequestComplete` (route layer) emitted success logs with overlapping PII fields (`user_id`, `lat`, `lng`, `trade_slug`). Every successful request produced 2 log entries. | this commit | Removed the `logInfo` success emission from both lib functions. Route layer owns the single structured success log via `logRequestComplete`. Error paths still use `logError` in both layers (lib for context, route for boundary). |
| HIGH | Gemini + DeepSeek + Independent | `lead_views.user_id VARCHAR(100)` (migration 070) was inconsistent with `user_profiles.user_id VARCHAR(128)` (migration 075). Firebase UIDs >100 chars would authenticate + create a profile but fail every `lead_views` INSERT, breaking view tracking and competition count for affected users. Tracked as OPEN since Phase 2-i (followups line 164). | this commit | Added migration 076 widening `lead_views.user_id` to VARCHAR(128). Metadata-only alter on PostgreSQL (no table rewrite). DOWN is documented but not auto-executed to prevent silent data loss on narrowing. |
| CRITICAL | Gemini | `SuccessStatus` included 204 but `NextResponse.json()` always serializes a body — RFC 7231 §6.3.5 forbids a body on 204 responses. Proxies/CDNs can mishandle. No current route used 204, but the footgun was latent. | this commit | Removed 204 from `SuccessStatus` union. Added comment pointing future callers to `new NextResponse(null, { status: 204 })` if ever needed. |
| HIGH | Gemini + DeepSeek | `ok()` 3-arg overload `(data, meta, status)` was ambiguous with 2-arg form: a call like `ok(data, 201)` would TypeScript-infer `201` as `meta` (because `M` is unconstrained), sending `{data, error:null, meta:201}` with status 200 — completely wrong response. No current caller passed a status, so the footgun was latent. | this commit | Removed the 3-arg overload. `ok()` is now strictly `(data)` or `(data, meta)`, always status 200. If a future route needs non-200 success it must use an explicit options-object refactor. Updated `api-envelope.logic.test.ts` to reflect the removal. |
| MED | Gemini | CSRF protection not explicit on state-mutating view route | n/a | DEFENSIBLE — `__session` is Firebase's httpOnly cookie set with `SameSite=Lax`, and the route requires authenticated session on every call. No cross-site subdomains currently configured. Revisit if CORS ever loosens. |
| MED | DeepSeek | Rate-limit check runs AFTER auth + validation; attacker with valid session could exhaust limit via malformed requests | n/a | DEFENSIBLE — already triaged in Phase 2-ii followups. Validation cost is negligible vs. `getLeadFeed` DB query which rate limit correctly gates. |
| MED | DeepSeek | `recordLeadView` returns `{ok:false}` for both "not found" (client error) and DB failures (server error) — can't distinguish 404 vs 500 | n/a | FALSE ALARM — the function does NOT check existence; it upserts unconditionally. `ok:false` only comes from the catch block (DB failure). No 404 path to distinguish. |
| MED | DeepSeek | No query timeout on `getCurrentUserContext` — slow DB hangs the route | n/a | DEFERRED — statement_timeout is a pool-level concern. Phase 2 deployment doc should set `SET statement_timeout` on the connection config. Tracked for platform hardening. |
| MED | DeepSeek | No request-ID correlation across log layers (auth → validation → DB → logging) | n/a | DEFERRED — structured logs include `user_id + duration_ms + route` which covers ~95% of debug needs. Full request-ID plumbing is a future observability WF. |
| MED | DeepSeek | `.strict()` on view body schema breaks forward-compat if clients add metadata fields | n/a | DEFENSIBLE — intentional per Phase 2-i triage. Strict-mode rejection is a feature, not a bug. |
| LOW | Gemini | `trade_slug` schema duplicated 3x across schemas.ts | n/a | DEFERRED — nit. Will extract if a 4th occurrence appears. |
| LOW | Independent | `updated_at` on `user_profiles` has no auto-update trigger | n/a | DEFERRED — no UPDATE path exists in Phase 2. Future onboarding/profile-edit WF will need to SET it explicitly or add a trigger. |
| LOW | DeepSeek | No index on `user_profiles.trade_slug` for future analytics | n/a | DEFERRED — premature. Current hot path is PK lookup by user_id. Dropped the index in Phase 2-i triage for this exact reason. |
| LOW | DeepSeek | No ON DELETE cleanup strategy for orphaned profiles when Firebase user is deleted | n/a | DEFERRED — Firebase Admin SDK doesn't cascade to Postgres. Needs a nightly sweep script, tracked for platform hardening. |
| NIT | Gemini | Route's explicit 3-field cursor check is redundant with Zod refine | n/a | DEFENSIBLE — defense-in-depth on already-validated input. Cost is 3 undefined checks. |
| HIGH | DeepSeek | `err()` `details: unknown` could leak sensitive internals | n/a | DEFENSIBLE — only `badRequestZod` passes details (`zodError.flatten()` — a structured field-error shape, no internals). No caller passes raw errors. Type-level restriction would duplicate existing runtime discipline. |

## 2026-04-08 — Phase 2-ii Lead Feed Route (commit TBD after this entry)

| Sev | Source | Item | Closed in | Notes |
|-----|--------|------|-----------|-------|
| MED | Independent + Gemini test + DeepSeek test | Test improvements: assert `error.code` on all 400 paths (only one had it), assert `error: null` on 200 paths, lock the 7-field `logRequestComplete` log shape at the route level, prove logRequestComplete is NOT called on error paths, regression-test the `lat=abc` NaN coercion path, document the HTTP parameter pollution behavior | this commit | Added 4 new tests to api-leads-feed.infra.test.ts: (1) logRequestComplete called with 7-field shape on success, (2) logRequestComplete NOT called on error paths, (3) lat=abc → 400 (NaN coercion guard), (4) HTTP parameter pollution determinism (?trade_slug=plumbing&trade_slug=electrical resolves to last value via Object.fromEntries). Beefed up existing 400 tests to also assert `error.code === 'VALIDATION_FAILED'`. |
| CRITICAL | Gemini route | Cursor partial silently treated as page 1 | n/a | FALSE ALARM — Zod's `.refine()` rejects partial cursors at the validation layer. Existing test `returns 400 VALIDATION_FAILED when cursor is partial (missing lead_id)` verifies this. The route's `&&` check is defense-in-depth on already-validated input. |
| HIGH | Gemini route | If `getCurrentUserContext` throws, returns 500 instead of 401 | n/a | DEFENSIBLE — `get-user-context.ts` is hardened against throwing in commit `359bc9f` (Phase 2-i fixes) with its own try/catch around getUserIdFromSession. The "never throws" contract is enforced at the helper level. |
| MED | Gemini route | `Object.fromEntries` silently picks last value on duplicate query params | n/a | DEFENSIBLE per spec 70 — no spec param has repeated semantics. **Now locked in by an explicit test** (item 1 above) so the behavior is documented and any future parser change fails tests. |
| MED | Gemini route | `lat`/`lng` in logs is potentially sensitive PII | n/a | DEFENSIBLE per spec 70 §API Endpoints "Observability" which explicitly requires logging `{user_id, trade_slug, lat, lng, radius_km, result_count, duration_ms}`. The user opted in by using the location-based feed. |
| LOW | Gemini route | "never throws" comment on getLeadFeed call is "dangerous assumption" | n/a | DEFENSIBLE — top-level try/catch wrapper handles any throw. Comment is documentation, not a runtime guarantee. The defensive 500 test path verifies this. |
| HIGH | DeepSeek test | Test for SQL injection via cursor parameters | n/a | FALSE ALARM — `getLeadFeed` parameterizes EVERY value via `$1..$8`. Cursor values are passed as parameters, not interpolated. |
| CRITICAL | DeepSeek test | No test validates relevance_score range/calculation | n/a | OUT OF SCOPE — relevance_score is calculated by Phase 1b-iii `getLeadFeed`, which has its own 24 tests covering the SQL CASE expressions. The route layer just passes the result through. |
| MED | Gemini test | 403 message leaks user's actual trade | n/a | DEFENSIBLE — same finding from Phase 2-i triage. The user knows their own trade (they set it). Surfacing it back is a UX win. |
| MED | DeepSeek test | Race condition test for "user deleted mid-request" | n/a | OUT OF SCOPE — covered by `getLeadFeed`'s pool throws → empty result → 500 path which IS tested. |
| LOW | Independent | No test asserts retryAfterSec format | n/a | DEFERRED — header presence is verified, format is "60" (constant). |
| LOW | Both reviewers | makeRequest helper uses minimal NextRequest stub | n/a | DEFENSIBLE — the route only accesses `request.nextUrl.searchParams`. Adding a real NextRequest would couple tests to Next.js internals without catching real bugs. |

## 2026-04-08 — Phase 2-i API Foundation (commit TBD after this entry)

| Sev | Source | Item | Closed in | Notes |
|-----|--------|------|-----------|-------|
| **CRITICAL** | Gemini + DeepSeek | Migration 075 `user_id VARCHAR(100)` is too short for Firebase UIDs (max 128 chars per Firebase Authentication docs) — would cause INSERT truncation errors for the upper range of UIDs | this commit | Fixed: VARCHAR(100) → VARCHAR(128) on migration 075. **Pre-existing inconsistency:** `lead_views.user_id` from migration 070 is also VARCHAR(100). Tracked as a separate followup since it's not in this WF's scope. |
| **CRITICAL** | Gemini | `internalError()` discarded the underlying cause — observability black hole. Production failures would log only "An unexpected error occurred" with no stack trace or context. | this commit | Fixed: signature is now `internalError(cause?, context?)` — accepts the error and a context object, calls `logError` with both, still returns the generic message to the client (no stack leak). Phase 2 routes call this from their final catch block with `internalError(err, { route, ... })`. |
| **CRITICAL** | Gemini + DeepSeek | `envelope.ok()` had `null as M` cast — type-level lie when `M` is non-nullable | this commit | Fixed: function overloads — `ok(data)`, `ok(data, meta)`, `ok(data, meta, status)`. Status code constraint via `SuccessStatus = 200|201|202|204` and `ErrorStatus = 400|401|403|404|409|422|429|500|502|503` union types. Type-level prevention of "200 OK with error" and "500 with success" misuses. |
| **HIGH** | DeepSeek | `schemas.ts` missing `.finite()` on `z.coerce.number()` fields — `Number("abc")` returns NaN, and `NaN > -90` is false (silently passes min/max gates) | this commit | Fixed: added `.finite()` to lat, lng, radius_km, limit, cursor_score. Rejects NaN/Infinity at the validation boundary. |
| **HIGH** | DeepSeek | `schemas.ts` `entity_id` unbounded — could overflow PostgreSQL INT (2^31-1) and crash the lead_views FK | this commit | Fixed: added `.max(2147483647)` to entity_id. |
| **CRITICAL (DeepSeek)** | DeepSeek | `getCurrentUserContext` did not wrap `getUserIdFromSession` in try/catch — file header promised "never throws" but was unguarded against a future regression in the auth helper | this commit | Fixed: wrapped the call in its own try/catch with `logError` on failure. Defense in depth — Backend Phase 0's `getUserIdFromSession` is documented as never-throws but the contract isn't enforced at the type level. |
| **MED** | Independent | `schemas.ts` discriminated union didn't strict-reject foreign-branch keys — Zod's default `z.object` silently strips unknown keys, so `{lead_type:'permit', entity_id:5, ...}` would parse and quietly drop entity_id. The implementor's comment claimed "fails parsing" — incorrect. | this commit | Fixed: added `.strict()` to both branches. Now rejects entity_id on permit branch, permit_num on builder branch, and any unknown extra keys. Comment updated to match actual Zod behavior. 4 new regression tests lock the strict semantics in. |
| MED | Gemini + DeepSeek | Migration 075 `idx_user_profiles_trade_slug` index is premature — no Phase 2 query uses it, hot path is PK lookup by user_id | this commit | Fixed: dropped the index. Test updated to assert it does NOT exist. Can be added in a future migration if a "list users by trade" feature ships. |
| MED | Gemini | Migration 075 `length(trade_slug) > 0` allows whitespace-only slugs (`'   '`) | this commit | Fixed: `trim(trade_slug) <> ''`. Test updated. |
| LOW | Gemini | `rateLimited()` missing standard `Retry-After` header per RFC 6585 | this commit | Fixed: added `Retry-After: 60` (default, configurable via second parameter). `err()` envelope helper now accepts an optional headers map. |
| LOW | Independent | `cursor_score` coerced to `int` — if scoring ever returns floats, cursor truncation will cause pagination skips at boundaries | this commit | DOCUMENTED — added inline comment explaining the integer assumption and the upgrade path (promote to `.number().finite()` and update SQL cast). Currently safe because all 4 score pillars return integer literals. |
| HIGH | Gemini | Migration 075 lacks updated_at trigger — column will always equal created_at | n/a | DEFENSIBLE — application code is responsible for setting `updated_at = NOW()` on UPDATE statements. Same convention used by other Buildo tables. Adding a trigger is V2 hardening. |
| HIGH | Gemini + DeepSeek | Migration 075 has no FK to a `trades` table for `trade_slug` | n/a | DEFENSIBLE — codebase-wide pattern of referencing trades by string slug without FK constraints (already discussed in Phase 1 followups). The CHECK constraint enforces non-empty/non-whitespace; full slug validation happens at the application layer. |
| HIGH | Gemini | `forbiddenTradeMismatch` "leaks" the user's actual trade in the error message | n/a | DEFENSIBLE — the user already knows their own trade (they set it). Surfacing it back to them is a UX win, not an information leak. |
| HIGH | Gemini | `forbiddenTradeMismatch` JSON injection via unsanitized interpolation | n/a | FALSE ALARM — `NextResponse.json()` properly escapes string values per the JSON spec. No injection possible. |
| HIGH | DeepSeek | `getCurrentUserContext` "trade_slug could be null in DB" violating type contract | n/a | FALSE ALARM — migration 075 declares `trade_slug VARCHAR(50) NOT NULL`. The type contract is honored. |
| HIGH | DeepSeek | `getCurrentUserContext` SQL injection via uid validation | n/a | FALSE ALARM — query is parameterized via `$1`. uid format validation is not necessary. |
| MED | Independent | No `withErrorBoundary` wrapper helper — every Phase 2 route will repeat top-level try/catch boilerplate | Phase 2-ii / 2-iii | DEFERRED — minor DRY opportunity, can be added when patterns emerge in Phase 2-ii. Not blocking. |
| MED | Gemini | `badRequestZod` leaks Zod schema structure via `flatten()` | n/a | DEFENSIBLE per spec 70 — field-level error messages are required for client UX. Per the spec the API is documented as exposing field validation details. |
| MED | DeepSeek | `rateLimited` exposes `remaining` in the response body | n/a | DEFENSIBLE — documented in spec 70. Useful for client retry logic. The privacy concern is theoretical for an authenticated endpoint. |
| MED | Independent | Foundation does not provide a rate-limit DECISION helper (only the response builder) | n/a | FALSE ALARM — the rate limit decision logic exists at `src/lib/auth/rate-limit.ts` (Backend Phase 0 commit `63cbbe3`). Phase 2-ii routes will import `withRateLimit` from there. The independent review agent didn't recon Backend Phase 0. |
| LOW | Independent | `unauthorized()` cannot distinguish "no session" from "no profile row" — onboarding flow will need to redirect differently | Future onboarding WF | DEFERRED — already noted in get-user-context.ts header. Phase 2-ii ships with the unified 401; onboarding flow can introduce a 401-with-onboarding-required code later without breaking the existing contract. |
| LOW | DeepSeek | Pre-existing `lead_views.user_id` is also `VARCHAR(100)` — same Firebase UID truncation risk | closed-in-(Phase 2 holistic review commit) | Migration 076 widens `lead_views.user_id` to VARCHAR(128). See Phase 2 Holistic Review section above. |

## 2026-04-08 — Phase 0+1 Final Gemini Review (commit TBD after this entry)

| Sev | Source | Item | Closed in | Notes |
|-----|--------|------|-----------|-------|
| **CRITICAL** | Gemini phase | `p.location` is stored as `geometry(Point, 4326)` per migration 067 (chosen for GIST index compatibility). All distance expressions in `get-lead-feed.ts` and `builder-query.ts` use `ST_DWithin(p.location, ...::geography, $radius_m)` and `(p.location <-> ...::geography)` — relying on PostGIS function resolution to cast `geometry → geography`. Without an EXPLICIT `::geography` cast on `p.location`, PostGIS may resolve to the geometry overload and interpret `radius_m` as DEGREES (1 degree ≈ 111km), making the radius filter geographically nonsensical. | this commit | Fixed: added explicit `p.location::geography` cast at every distance expression in BOTH files (~14 occurrences). Added regression tests in `builder-query.logic.test.ts` and `get-lead-feed.logic.test.ts` asserting `LEAD_FEED_SQL`/`BUILDER_QUERY_SQL` contain `p.location::geography` and explicitly do NOT contain bare `p.location <->` or `ST_DWithin(p.location,` patterns. **The single highest-value finding from the Gemini phase review.** Per-file reviews missed it because the column type was set in Phase 0 (migration 067) and the queries were written in Phase 1 (5aac708) — the inconsistency was cross-phase, only catchable by a Phase 0+1 holistic review. |
| HIGH | Gemini phase | Singleton execution environment assumption: rate limiter cache + timing calibration cache are in-memory and per-process. Trivially bypassable in serverless. | n/a | KNOWN — already documented in followups for both files. Rate limiter has Upstash backing for production; in-memory is dev fallback only. Timing cache is per-instance with 5-min refresh; acceptable tradeoff. Phase 2 deployment doc should call out the production-Upstash requirement. |
| HIGH | Gemini phase | Migration framework can't run `CREATE INDEX CONCURRENTLY` (multi-statement query in `scripts/migrate.js`). Future indexes on `permits` will require downtime. | n/a | KNOWN — documented inline in migration 067 comment. Operational mitigation: pre-create indexes manually with CONCURRENTLY before applying the migration so the `IF NOT EXISTS` becomes a no-op. The validate-migration.js CONCURRENTLY rule still gates new code from accidentally adding non-CONCURRENT indexes on large tables. |
| MED | Gemini phase | Migration 070 destructive rebuild silently drops dev test data | n/a | DEFENSIBLE — the `ALLOW-DESTRUCTIVE` marker is the documented safety mechanism. Migration validator catches DROP TABLE without marker. Devs running migrations are expected to know what destructive markers mean. |
| MED | Gemini phase | Cursor pagination ties slicing UX (page boundary in middle of identical-score block) | n/a | NOT A BUG — tuple comparison handles this correctly. Page 2 picks up where page 1 left off via the lead_id tiebreak. Documented design. |
| LOW | Gemini phase | Test environment parity: mocked tests can't validate SQL — confirmed by the trade_slug bug | n/a | KNOWN — blocked by pre-existing migration 030 failure. Highest-leverage future addition is `EXPLAIN ` integration tests against a clean DB. |
| LOW | Gemini phase | Dual code path "byte-for-byte" claim is "aspiration not guarantee" | n/a | DEFENSIBLE — manual byte-for-byte audit verified at every commit + phase level. V2 hardening: extract constants to shared JSON. |
| LOW | Gemini phase | Rate limiter input validation incomplete (radius_km not bounded against negative values) | Phase 2 Zod | **CLOSED 2026-04-09** — `src/features/leads/api/schemas.ts:34-39` enforces `z.coerce.number().finite().positive().max(MAX_RADIUS_KM)` on the feed query input. Negative, NaN, infinite, and >50 values all fail validation with HTTP 400 before the route handler runs. Verified live. |

## 2026-04-08 — Phase 1 Holistic Review (commit TBD after this entry)

| Sev | Source | Item | Closed in | Notes |
|-----|--------|------|-----------|-------|
| **CRITICAL** | Independent (worktree) | `get-lead-feed.ts` referenced `pt.trade_slug` on `permit_trades` (lines 117 + 183). Column does not exist on `permit_trades` (only `trade_id INTEGER REFERENCES trades(id)` per migration 006). Unified feed SQL would have failed at runtime against any real database; the catch block would have silently returned empty results. The bug passed every per-file review (Gemini + DeepSeek + per-WF independent) because all tests use mocked pools. | this commit | Fixed in BOTH CTEs (permit_candidates + builder_candidates) by replacing `pt.trade_slug = $1` with `JOIN trades t ON t.id = pt.trade_id` + `t.slug = $1`. Added regression test asserting `LEAD_FEED_SQL` contains `JOIN trades` and explicitly does NOT contain `pt.trade_slug`. **The single highest-value finding of any review in Phase 1.** Per-file reviews missed it because builder-query.ts uses the correct pattern in isolation; only the holistic phase-level review caught the cross-file inconsistency. |
| **HIGH** | Independent | No `recordLeadView` helper for Phase 2's `POST /api/leads/view` — Phase 2 would have to hand-craft INSERT, re-derive `lead_key` format, satisfy migration 070 XOR CHECK, and re-implement the 30-day competition count query. Breaks the Phase 1 promise of "Phase 2 routes need no try/catch". | this commit | Added `src/features/leads/lib/record-lead-view.ts` with `recordLeadView(input, pool) → {ok, competition_count}` and exported `buildLeadKey(input)` helper. View action preserves saved state via no-op upsert; save/unsave actions force the saved column. Race condition between upsert + count read explicitly accepted per spec 70 §API Endpoints. 12 new tests covering all action paths, both lead types, error fallback, NULL handling. |
| MED | Independent | Confidence threshold asymmetry: `permit_candidates` filtered `pt.confidence >= 0.5`; `builder_candidates` and standalone `BUILDER_QUERY_SQL` did not. Same permit could pollute builder activity counts while being excluded from permit counts. | this commit | Added `pt.confidence >= 0.5` filter to BOTH builder paths (the `builder_candidates` CTE in get-lead-feed.ts and the `nearby_permits` CTE in builder-query.ts). Symmetry restored across all 3 lead-discovery code paths. |
| MED | Independent | Tier 2 SQL proxy in feed vs full detail-page engine can tell mutually-inconsistent stories — feed awards `timing_score=30` for a permit in `phase='structural'` even when the trade isn't in `PHASE_TRADE_MAP['structural']`, while detail page returns "wait X weeks" for that same trade | e7c3a05 | **CLOSED 2026-04-09** — Phase 3-iii sidesteps the inconsistency by surfacing a synthetic `timing_display` phrase (mapped from `timing_confidence`) in the feed instead of running the spec-71 engine per row. The detail-view phase will overlay the precise engine output via the `useLeadView` mutation response without modifying `LeadFeedItem`. The two layers no longer compete because the feed's timing string is intentionally generic ("Active build phase" / "Estimated timing" / "Approximate timing"). |
| MED | DeepSeek phase | Spec questions about XOR enforcement, scoring schemes, cursor ties | n/a | DISMISS — XOR CHECK already tight in migration 070 (verified earlier); scoring scheme divergence already documented in builder-query.ts header; cursor lead_id collision already false-alarm (colon disambiguation). |
| LOW | Both | Missing integration tests / "SQL never executed against real DB" | tracked above | KNOWN — local DB broken at migration 030. Followup recommends `pool.query('EXPLAIN ' + SQL, dummyParams)` infra tests once unblocked. Highest-leverage CI addition for the entire Phase 1 surface. |

## 2026-04-08 — Phase 1b-iii Builder Query + Unified Feed (commits 5aac708 + Phase 1b-iii review fixes)

| Sev | Source | Item | Closed in | Notes |
|-----|--------|------|-----------|-------|
| CRITICAL | Gemini ×2 | get-lead-feed.ts `input.limit` not validated → DoS via massive result-set requests | this commit | Fixed via `MAX_FEED_LIMIT = 30` (per spec 70 §API Endpoints) + minimum-1 floor. Both clamps applied BEFORE the empty-result fallback so meta reflects clamped values on error. 2 new tests verify clamps. |
| HIGH | Gemini test | builder-query.ts `(slug, lat, lng, ...)` signature is a footgun — internally swaps to PostGIS `ST_MakePoint(lng, lat)` order | this commit | Fixed via explicit JSDoc warning on the function: documents the lat-first convention matching the rest of the codebase, and warns future maintainers to flip the param array if signature changes. Existing parameter-order test in get-lead-feed.logic.test.ts already catches a regression. |
| LOW | Gemini | get-lead-feed.ts builder `value_score` AVG NULL → 10 (small) — should distinguish "unknown cost" from "small cost" | this commit | Fixed: explicit `IS NULL → 5` branch as the first CASE arm. Builders with no cost data on any nearby permit now sort below the smallest known bucket. |
| CRITICAL | Gemini test | get-lead-feed.ts pagination casts `$6::int` but score "may be float" | n/a | FALSE ALARM verified manually: all 4 pillar CASE expressions return integer literals (10/15/20/25/30 etc), so `relevance_score` is always integer. The `::int` cast is consistent. |
| CRITICAL | Gemini | builder-query.ts ST_DWithin "no spatial index" | n/a | FALSE ALARM — Phase 1a migration 067 created `idx_permits_location_gist` (PostGIS GIST index on permits.location). Verified. |
| HIGH | Gemini ×2 | get-lead-feed.ts `<->` distance computed multiple times in CTE — "extremely inefficient" | n/a | DEFENSIBLE — PostgreSQL planner CSEs the expression per row (verified by independent review). The visual repetition is structural readability cost only. Spec 70's example sketch uses the same pattern. |
| HIGH | Gemini ×2 | get-lead-feed.ts + builder-query.ts errors swallowed → empty result/array | n/a | DEFENSIBLE per spec — design contract is "never throws to caller". Phase 2 routes detect failures via the absence of the success log line + Sentry instrumentation. Documented in file headers. |
| HIGH | Gemini test ×2 | "test only asserts 3 fields, missing 13" + "limit hardcoded vs constant" + "MockPool incomplete" | n/a | DEFENSIBLE — test pattern matches Phase 1b-i and 1b-ii. Behavior coverage focus is on the critical paths (mapping, cursor, clamping, error fallback). Full row deep-equal is V2 thoroughness. |
| HIGH | Gemini | get-lead-feed.ts `input.user_id` only logged, not used for personalization | n/a | DEFENSIBLE per spec 70 — user_id is for observability + Phase 2 trade_slug authorization (`if user.trade !== input.trade_slug → 403`). Personalization (already-viewed exclusion via `lead_views` table) is Phase 2+. |
| HIGH | DeepSeek | get-lead-feed.ts SQL injection via unvalidated trade_slug | n/a | FALSE ALARM — `$1` is parameterized, pg driver handles binding. Trade slug validation at API boundary is Phase 2 (Zod). |
| HIGH | Gemini | get-lead-feed.ts no auth check on user_id | n/a | DEFENSIBLE — auth is the API route's job per spec 70. The lib function trusts its caller (Phase 2 route handler will call `getUserIdFromSession` from Backend Phase 0 BEFORE invoking getLeadFeed). |
| MEDIUM | Gemini | get-lead-feed.ts cursor extraction inside try block could throw on malformed last row | n/a | DEFENSIBLE — `data.length === clampedLimit && data.length > 0` guard plus `if (last)` check inside makes it safe. The whole function body is wrapped in try/catch already. |
| MEDIUM | DeepSeek | LATERAL builder filter "may exclude valid builders with active permits but no website/phone" | n/a | DEFENSIBLE per spec 73 §Data Quality Filter explicit: "Entries with no usable contact info" are filtered out by design. Spec 73 line 26. |
| MEDIUM | DeepSeek | "UNION ALL may produce duplicate lead_ids across types" | n/a | FALSE ALARM verified — permit lead_id always contains `:` (e.g. `'24 101234:01'`), builder lead_id never contains `:` (just entity id). Disjoint by construction. |
| MEDIUM | DeepSeek | "no query timeout — could trigger full table scan" | n/a | DEFERRED — Phase 2 API route can set statement_timeout via the connection. Library doesn't impose timeouts to allow caller flexibility. |
| LOW | Gemini | builder-query.ts `HAVING COUNT(np.permit_num) >= 1` redundant after INNER JOIN | n/a | DEFENSIBLE — cosmetic cleanup, defers to future hardening. |
| LOW | Gemini | builder-query.ts GROUP BY lists columns functionally dependent on e.id | n/a | DEFENSIBLE — PostgreSQL strict-mode requires all non-aggregate columns in GROUP BY. Cosmetic. |
| LOW | Gemini | get-lead-feed.ts LATERAL `ON true` redundant | n/a | DEFENSIBLE — required syntactic noise for LATERAL JOIN, not actually redundant. |
| MED | Gemini test | "regex SQL structure tests are brittle" | n/a | DEFENSIBLE per locked-DB constraint, same triage as Phase 1a/1b-i/1b-ii. |
| MED | DeepSeek | "happy path test only asserts 3 fields" | n/a | DEFENSIBLE — same coverage philosophy as prior sub-WFs. Mapping completeness is verified by typecheck (interface enforcement) + behavior tests on the critical paths. |
| LOW | Independent | builder_candidates in get-lead-feed.ts uses different scoring dimensions than spec 73's standalone builder query | n/a | NOTED — intentional. Spec 70 unified feed uses 30/30/30/10 (proximity/timing/value/opportunity) while spec 73 standalone builder uses 30/30/20/23 (proximity/activity/contact/fit). Documented in builder-query.ts file header that downstream consumers must be aware the same builder gets different scores between the two endpoints. |

## 2026-04-09 — Phase 3-iii backend bundle (commit e7c3a05 — widen feed SQL + types)

Bundle: get-lead-feed.ts, types.ts, get-lead-feed.logic.test.ts, api-leads-feed.infra.test.ts, spec 75 §4.4/§4.5 reconciliation notes.

| Sev | Source | Item | Closed in | Notes |
|-----|--------|------|-----------|-------|
| CRITICAL | Gemini + DeepSeek | Pagination truncation when mapRow drops a row — `data.length === clampedLimit` is false even when more raw rows exist → next_cursor=null → silent feed termination. PRE-EXISTING bug, not introduced by 3-iii. | e7c3a05 | Fixed: derive cursor from `res.rows.length` and the LAST RAW ROW (not post-filter `data`). Both cursor fields are present on the raw row regardless of mapRow's verdict. Regression test added. |
| HIGH | DeepSeek | WSIB LEFT JOIN LATERAL had no tiebreaker on equal `last_enriched_at` → non-deterministic row pick → cursor instability across requests. | e7c3a05 | Fixed: added `, w2.id DESC` as secondary ORDER BY. Test asserts the SQL contains the tiebreaker. |
| MED | DeepSeek | `COUNT(p.permit_num)` in builder CTE could double-count if entity_projects has duplicate `(entity, permit, role)` rows. | e7c3a05 | Fixed: replaced with `COUNT(DISTINCT (p.permit_num, p.revision_num))`. |
| FAIL | Independent | spec 75 §4.5 sample code body still referenced `lead.builder_name`, `lead.phone`, `lead.closest_permit_m`, `lead.wsib_registered` after the reconciliation note was added → readers would copy stale fields. | e7c3a05 | Fixed all sample-code references. Replaced wsib_registered block with REMOVED-in-3-iii comment. |
| FAIL | Independent | samplePermitRow fixture had stale `opportunity_score: 10` / `relevance_score: 90` from a pre-review 0-10 draft. SQL pins 'Permit Issued' at 20. | e7c3a05 | Fixed to 20 / 100. |
| HIGH | Gemini | Distance calc repeated 7 times in proximity_score CASE — extreme perf hit | n/a | DEFENSIBLE — Postgres planner CSEs the expression. Same triage as Phase 1b-iii. Visual repetition is structural readability cost only. |
| HIGH | Gemini | Migration 079 has commented-out DOWN block — all migrations must be reversible | n/a | DEFENSIBLE — project convention per ADR 004 (operator runbook documents the manual DOWN). Same pattern as migration 067/078. |
| HIGH | Gemini | Migration 079 UP runs DROP+CREATE in same migration — guaranteed to fail in production if operator runbook was followed | n/a | DEFENSIBLE — header comment acknowledges this for dev. Production uses the CONCURRENTLY runbook out-of-band; the in-migration form is the dev fallback. |
| MED | DeepSeek | LPAD assumes revision_num is numeric — if it contains letters, padding malforms lead_ids | n/a | DEFENSIBLE — verified against source data, revision_num is always numeric in CKAN feed. Pre-existing assumption from migration 001 + buildLeadKey. |
| HIGH | DeepSeek | LeadFeedRow declares latitude/longitude as `number | string | null` but SQL casts inconsistently | n/a | DEFENSIBLE — toNumberOrNull handles either branch. Cast cleanup is cosmetic. |
| MED | DeepSeek | `business_size IS NOT NULL` in builder WHERE excludes builders without WSIB records | n/a | DOCUMENTED — already noted in types.ts comment + spec 75 reconciliation note. The wsib_registered column is intentionally absent in 3-iii because the WHERE makes it structurally always-true. Re-add when feed widens. |
| HIGH | DeepSeek | Error logging includes PII (user_id, lat, lng) — violates privacy regulations | n/a | DEFERRED — observability vs privacy tradeoff is a project-wide decision, not Phase 3-iii's call. Track as cross-cutting concern. |
| MED | DeepSeek | Re-export of types from `@/lib/permits/types` creates tight coupling | n/a | DEFENSIBLE — standard pattern for Phase 1a barrel exports. |
| LOW | DeepSeek | TIMING_DISPLAY_BY_CONFIDENCE could return undefined for unexpected confidence value | n/a | FALSE ALARM — type-narrowed to `'high' | 'medium' | 'low'` so unreachable. |
| MED | DeepSeek | narrowCostTier returns null for unknown values — silently drops the tier | e7c3a05 | DEFENSIBLE + tested — defensive narrowing is intentional. Test covers the unknown-value case. |

## 2026-04-09 — Phase 3-iii frontend bundle (commit f043dcc — composite cards)

Bundle: PermitLeadCard.tsx, BuilderLeadCard.tsx, format.ts, avatar.tsx, observability/capture.ts, format.logic.test.ts, *LeadCard.ui.test.tsx.

| Sev | Source | Item | Closed in | Notes |
|-----|--------|------|-----------|-------|
| CRITICAL | Independent | PermitLeadCard passing `Math.round(lead.relevance_score)` (0-100 composite) to TimingBadge whose bands + ProgressCircle scaling are calibrated for 0-30 timing pillar. Almost every lead would have rendered NOW regardless of actual urgency. | f043dcc | Fixed to pass `lead.timing_score`. Regression test added: `timing_score=5, relevance_score=85` asserts Distant band, not NOW. |
| HIGH | Independent + Gemini | sanitizeTelHref two real bugs: (a) spec 75 §9 E.164 max not enforced; (b) extensions concatenated as digits — `(416) 555-1234 ext 99` produced `416555123499`, junk number some carriers will dial. | f043dcc | Fixed: `.slice(0, 15)` clamp + 10-digit floor + split on `x|ext\.?|#|,` BEFORE digit pass. 4 new tests cover the matrix. |
| MED | DeepSeek | Hover gating used `=== 'mouse'` which excluded pen/stylus — tablet users got no hover feedback. | f043dcc | Fixed: changed to `!== 'touch'` so mouse, pen, stylus all trigger; only touch is excluded. |
| MED | DeepSeek | Footer div had `onKeyDown={stopPropagation}` on `role="presentation"` element — dead code (presentation divs never receive keyboard focus). | f043dcc | Removed dead handler. Click stopPropagation kept (still required for synthetic event bubbling). |
| ast-grep | rule | sanitizeWebsite originally used try/catch around `new URL()` — tripped silent-catch-fallback rule. | f043dcc | Refactored to `URL.canParse()` (Node 19.9+) so the validator expresses its contract without swallowing exceptions. |
| HIGH | DeepSeek | lat/lng XSS via template literal injection in directionsHref | n/a | FALSE ALARM — `latitude/longitude` are typed as `number | null` and gated by `Number.isFinite()`. Type system prevents string injection. |
| CRITICAL | DeepSeek | empty-string permit_type renders empty `<p>` | n/a | FALSE ALARM — `('' || null)` evaluates to falsy in JS, JSX correctly skips. Verified inline. |
| MED | DeepSeek | URL constructor needs https:// prepend if scheme missing | n/a | DEFENSIBLE — implicit scheme prepending is a footgun. Source data should be canonical URLs from WSIB enrichment. |
| MED | Gemini | BuilderLeadCard rounded-none inconsistency between footer buttons and SaveButton | n/a | DEFENSIBLE — Save button corners get clipped by Card `overflow-hidden` anyway. Cosmetic. |
| MED | Gemini | Full-card click target prevents text selection inside the card | n/a | DEFENSIBLE — full-card-tap is the documented spec 75 pattern. Trade-off accepted. |
| HIGH | DeepSeek | Magic numbers 25/20/10 in getTimingBorderClass | n/a | DEFENSIBLE — already documented inline + grep-tested against spec 74 §Timing. |
| LOW | DeepSeek | Pluralization of `active_permits_nearby === 1` doesn't handle 0 case | n/a | DEFENSIBLE — "0 active permits nearby" is grammatical English with plural. Builder CTE WHERE filter ensures count >= 1 anyway. |
| NIT | Gemini | MotionCard duplicated across PermitLeadCard + BuilderLeadCard files | n/a | DEFENSIBLE — they're in different files, not the same. Sharing would couple unrelated components. |

## 2026-04-09 — Phase 3-iv (commit f619814 — /leads page + LeadFeed container)

Bundle: LeadFeed.tsx, EmptyLeadState.tsx, app/leads/{page,LeadsClientShell,error,loading}.tsx, get-user.ts (verifySessionCookie extraction), route-guard.ts, *.ui.test.tsx.

| Sev | Source | Item | Closed in | Notes |
|-----|--------|------|-----------|-------|
| CRITICAL | Independent | Duplicate `lead_feed.viewed` event: LeadsClientShell fired with `{stage: 'page_mount'}` AND LeadFeed fired with `{item_count, page_count}`. Two events with same name + different shapes — broken PostHog funnels. | f619814 | Removed shell-level emit. LeadFeed-level event is more meaningful (only fires after data loads + carries item_count + page_count). |
| Independent | test | InfiniteScroll mock rendered `endMessage` whenever `!hasMore`, but real lib conditions on `!hasMore && dataLength > 0`. Mock contract gap could give false PASS on empty-list scenarios. | f619814 | Added `dataLength > 0` guard to mock. Mirrors real library contract. |
| HIGH | Gemini + DeepSeek | `next` callback had no `isFetchingNextPage` guard — rapid scrolls past trigger threshold fired multiple `fetchNextPage` calls before first request resolved. | f619814 | Added the guard. |
| MED | Gemini | `key={lead.lead_id}` could collide between permit shape (`'permit_num:revision_num'`) and builder shape (numeric `entity_id`). Low risk but cheap to fix. | f619814 | Composite key `${lead.lead_type}-${lead.lead_id}` guarantees uniqueness. |
| MED | Gemini | EmptyLeadStateProps had `onExpandRadius?` and `onRetry?` both optional — developer could render `no_results` without `onExpandRadius` and silently ship a dead button. | f619814 | Refactored to discriminated union; type system enforces the right callback per variant. Test renders updated to pass required callbacks (typecheck immediately caught the missing ones — exactly the failure mode we wanted). |
| MED | Gemini | Idle/prompt/requesting flicker in client shell — users saw "Loading…" then "Finding leads near you…" as two distinct frames. | f619814 | Collapsed three states into one render branch. |
| MED | Gemini | Cap banner hardcoded `15` for page size — would lie if `DEFAULT_FEED_LIMIT` ever changes. | f619814 | Imported `DEFAULT_FEED_LIMIT` from get-lead-feed.ts. |
| LOW | Gemini | Magic number `5` for radius increment in EmptyLeadState. | f619814 | Extracted `RADIUS_INCREMENT_KM = 5` constant. |
| CRITICAL | DeepSeek | Page cap race: pageCount reaches MAX_PAGES but hasMore is briefly stale, allowing one extra fetchNextPage | n/a | FALSE ALARM — `pageCount` and `hasMore` are computed in the SAME render from the same source (`query.data?.pages.length`). Synchronously consistent. No race. |
| HIGH | DeepSeek | navigator.onLine SSR hydration mismatch — React will throw a hydration error | n/a | FALSE ALARM verified by independent reviewer (PASS w/confidence 82). The `isError` branch only fires post-hydration; LeadFeed is a Client Component anyway. Latent hazard if branch ever moves outside isError — flagged as future hardening (use useSyncExternalStore). |
| HIGH | DeepSeek | `query.isPending && !query.data` returns full feed instead of skeletons during pull-to-refresh | n/a | FALSE ALARM — `isPending` is only true on initial mount with no data. Refetch sets `isFetching` not `isPending`. The existing guard is correct. |
| MED | DeepSeek | Many networks return navigator.onLine===true but block API calls — mislabels network failures as server outages | n/a | MISREAD — that's exactly what the 'unreachable' variant means in our code. Already handled. |
| MED | DeepSeek | Telemetry effect deps cause unnecessary re-runs (items.length, pageCount in deps) | n/a | DEFENSIBLE — deps required so captureEvent emits FRESH `items.length`/`pageCount` values when the quad first matches. Removing them would close over stale values. The early-return guard makes the noise harmless. |
| LOW | DeepSeek | `query.refetch()` refetches all pages, not just first — perf concern | n/a | DEFENSIBLE — that IS the correct pull-to-refresh semantic. User pulls, gets fresh versions of pages they've loaded. |
| LOW | DeepSeek | flatMap assumes every page has `data` property — defensive check needed | n/a | DEFENSIBLE — Zod schema + mapRow guarantee shape. Over-defensive. |
| MED | Gemini | useEffect with `request` dependency could infinite loop if request isn't memoized | n/a | FALSE ALARM verified — `useGeolocation` already memoizes `request` via `useCallback([], [])`. Independent reviewer also verified PASS. |
| NIT | Gemini | Items array recomputed on every render — should useMemo | n/a | DEFENSIBLE — premature optimization. flatMap on a 75-item array max is microseconds. |
| LOW | Gemini | Pull-to-refresh threshold 80px would fire on desktop mouse drag | n/a | FALSE ALARM — `react-infinite-scroll-component`'s pullDownToRefresh is gated to touch events only (touchstart/touchmove/touchend). Desktop mouse drag does not trigger it. |

## 2026-04-09 — Holistic Phase 3 review + WF3 cleanup batch (post-3-iv)

Review surface: all of Phase 3 (3-i through 3-iv inclusive). Three reviewers spawned in parallel against the cumulative Phase 3 bundle: Gemini, DeepSeek, independent worktree reviewer. Plus user-supplied "sliding anchor" + "Zod deadlock" findings. Plus my own self-checklist (written BEFORE consuming reviewer outputs) which independently confirmed two of the same bugs.

| Sev | Source | Item | Closed in | Notes |
|-----|--------|------|-----------|-------|
| HIGH | Self item 19 + Indep C1/CRITICAL 1 | `captureEvent('lead_feed.empty_state_shown', ...)` called directly in render body of `if (query.isError)` and `if (... items.length === 0)` branches → fires on EVERY re-render (Strict Mode double-invoke, background refetch, parent state change) | (this WF3) | Refactored: emits moved to a useEffect with ref-based deduplication keyed on `(trade_slug, variant)`. Mirrors the `lead_feed.viewed` pattern. Regression test added. |
| HIGH | Indep C2/IMPORTANT 3 | `selectedLeadId` not cleared when refetched feed no longer contains the previously-selected lead → phantom marker for Phase 6 map | (this WF3) | Added a useEffect in LeadFeed that detects when `selectedLeadId` is non-null AND the items array no longer contains it, then clears the selection. Gated on `query.isSuccess` so loading states don't clobber. 3 regression tests cover the matrix. |
| HIGH | Indep C8/CRITICAL 2 | `verifySessionCookie` function NAME suggested Firebase session-cookie API but IMPLEMENTATION called `verifyIdToken` → naming confusion that could mislead future maintainers | (this WF3) | Renamed function to `verifyIdTokenCookie` to match the actual Admin SDK call. Added explicit naming-history comment block documenting the two Firebase Admin methods and why we use `verifyIdToken` (the project sets `__session` to a Firebase ID token, not a long-lived session cookie). Updated 2 callers + 1 file-header comment. Behavior unchanged; only the name. |
| HIGH | User review | "Persistent Zod Deadlock": `validatePersistedSlice` accepted `radiusKm` in [1, 100] but server enforces `.max(50)` → corrupted localStorage in 51-100 range deadlocked the feed against 400 VALIDATION_FAILED with no UI failsafe | (this WF3) + Phase 3-v | **Layer 1**: imported `MAX_RADIUS_KM` from `@/features/leads/lib/distance` so the persisted-radius cap and the server validator share a single source of truth. **Layer 2**: the validator now CLAMPS out-of-range values to `DEFAULT_RADIUS_KM` instead of just rejecting the slice — corrupted clients self-heal on next page load. **Layer 3** (UI "Reset filters" failsafe) SHIPPED in Phase 3-v's LeadFilterSheet — the drawer footer now has a "Reset to defaults" button that calls `setRadius(DEFAULT_RADIUS_KM)` + telemetry + closes the sheet. All three layers of the deadlock fix are now in production. |
| HIGH | User review | "Sliding Anchor": claim that snap anchor is dragged forward by `query.isSuccess` → 500m threshold never trips during infinite scroll | (this WF3) | **NOT a bug in current code** — the Phase 3-iii Gemini deep-dive WF3 (commit dc8951a) already replaced the original `lastSuccessfulPositionRef` pattern with the Zustand-snapped pattern that ONLY updates inside the threshold check. Verified by tracing through useLeadFeed.ts:227-242 and grep-confirming no other `setSnappedLocation` callsite. **Locked with a regression test**: walks the snap through 40 × 100m steps over 4km, asserts the snap advances at >500m boundaries (3-15 advances), and asserts that consecutive snap deltas are >= 500m. A second test asserts that 20 same-coord rerenders produce zero snap changes. If a future refactor reintroduces a setSnappedLocation call outside the threshold check, both tests will fail loudly. |
| MED | Gemini | `MAX_PERSISTED_RADIUS_KM = 100` exceeds server `MAX_RADIUS_KM = 50` (root cause of the Zod deadlock above) | (this WF3) | Absorbed by the Zod deadlock fix (Layer 1). The constant is gone — the validator references `MAX_RADIUS_KM` directly. |
| MED | DeepSeek | `TimingBadge` passes negative scores to `ProgressCircle` → negative progress arc (the spec-71 staleness fallback uses negative sentinel scores for the Past tone) | (this WF3) | Fixed: clamped via `Math.min(100, Math.max(0, Math.round((score / 30) * 100)))` before passing to ProgressCircle. The label still shows the raw score so the underlying state is visible. Regression test added. |
| MED | Self item 1 + Indep C7 | Hover state unmount leak — cards directly use `onPointerEnter`/`onPointerLeave` props with no useEffect cleanup. If a card unmounts while hovered (scroll past, list refresh, filter change), `hoveredLeadId` stays set → Phase 6 map phantom hover | (this WF3) | Added a useEffect cleanup in PermitLeadCard + BuilderLeadCard that clears `hoveredLeadId` ONLY IF the unmounting card's own lead_id matches the active hover (otherwise we'd clobber a hover that legitimately moved to a different card during the unmount cycle). |
| MED | Self item 4 | Telemetry gap in `src/app/leads/` for 4 buttons (error.tsx "Try again" + LeadsClientShell "Grant location access" / "Try again" / "Try again") — AST-grep scope is `src/features/leads/`, doesn't cover `src/app/leads/` | (this WF3) | Added `captureEvent` calls to all 4 buttons. error.tsx "Try again" → `lead_feed.refresh` with `source: 'error_boundary'`. LeadsClientShell buttons → `lead_feed.filter_changed` with `field: 'geolocation'` + source discriminator. AST-grep scope expansion to `src/app/leads/` is a future hardening item. |
| MED | Indep C4/IMPORTANT 2 | 5-page cap silently resets on pull-to-refresh (refetch replaces all pages with fresh page-1 → pageCount drops 5→1 → cap clears). Independent reviewer flagged dataLength shift (75→15) as potential confusion for the library's internal scroll position tracking | (this WF3) | Documented as INTENTIONAL UX in the `refreshFunction` callback comment. Pull-to-refresh is a "start over" gesture; the cap reset is the desired behavior. The dataLength shift recovers on the library's next pass. If 3-v's filter sheet introduces a new "reset to top" affordance, the cap-reset semantics should be unified there. |
| LOW | Gemini | `style={{ overflow: 'visible' }}` on InfiniteScroll undocumented — looks like a magic value | (this WF3) | Added comment explaining: prevents the library's default `overflow: auto` from creating a nested scroll container, which would break pull-to-refresh on mobile (the gesture handler attaches to document, not the inner element). |
| LOW | Indep C16 | SQL never produces `'low'` `timing_confidence` (permit CASE has only `'high'`/`'medium'`, builder CTE hardcodes `'high'`) → the `'low'` entry in `TIMING_DISPLAY_BY_CONFIDENCE` is dead code in the feed path | (this WF3) | Documented in get-lead-feed.ts: kept the `'low'` entry because the spec-71 engine (which DOES produce `'low'` for the staleness fallback) will overlay the card via the `useLeadView` mutation response in the detail-view phase. Keeping the entry means that wiring is a one-prop change, not a schema change. Not dead code — forward-compatible. |
| HIGH | Gemini | `useGeolocation` race condition: external `request()` call BEFORE the async `checkPermissionState()` resolves can be overwritten by the resolved state → user sees "denied" UI even though the prompt is showing | n/a | DEFERRED — real race but non-trivial fix (state machine refactor with `isInitializing` guard or `useReducer`). Out of scope for this WF3 cleanup batch. Tracked for a dedicated state-machine WF in the geolocation UX hardening pass. |
| HIGH | Indep IMPORTANT 1 | `SaveButton.initialSaved` always defaults to `false` because `LeadFeedItem` has no `is_saved` field → after pull-to-refresh or page reload, all hearts reset to unsaved regardless of server state | 910d8d3 | **CLOSED 2026-04-09** — Phase 3-vi Part A-D shipped the `is_saved` field through to `LeadFeedItem`. Backend SQL widening: LEFT JOIN `lead_views` in both CTEs (permit branch with `lead_key` equality + decomposed component predicates; builder branch with `lead_key = e.id::text` + `bool_or` aggregation). Frontend wiring: cards pass `initialSaved={lead.is_saved}` to SaveButton. Plus a `mutation.isPending` gate added to SaveButton's initialSaved-sync useEffect to prevent the heart-flicker race when a refetch arrives mid-mutation. NO migration needed — `lead_views.saved` column already existed from migration 070. |
| MED | Gemini | `MAX_PERSISTED_RADIUS_KM` mismatch (already absorbed above) | absorbed | See Zod deadlock entry above. |
| MED | Gemini | `useLeadFeed` effect runs on every GPS update without throttling | n/a | DEFENSIBLE — `haversineMeters` on two coordinate pairs is microseconds. React rendering dwarfs the cost. Throttling adds complexity for no measurable benefit. |
| LOW | Gemini | `URL.canParse` Safari < 17 compatibility | n/a | DEFENSIBLE — project browser support matrix targets Safari 16+. The previous `try/catch` form was rejected by ast-grep `silent-catch-fallback`; reverting to it would require a disable comment which the rule doesn't support inline. |
| NIT | Gemini | SaveButton verbose payload re-validation pattern | n/a | DEFENSIBLE — the re-validation exists to satisfy TypeScript narrowing across the early-return validation branches. Refactor would require a discriminated payload type that doesn't fit the existing API contract shape. Cosmetic. |
| NIT | Gemini | Biome ignore on footer div could be simpler | n/a | DEFENSIBLE — current pattern (wrapper div with `role="presentation"` + `onClick stopPropagation`) is the documented click-bubbling boundary. Removing the wrapper would require duplicating stopPropagation on every footer button child. |
| MED | DeepSeek | `fetchLeadFeedPage` `NETWORK_ERROR` mislabel for malformed JSON (200 with bad body should be `INVALID_RESPONSE`) | n/a | DEFENSIBLE — the next-most-likely cause of unparseable JSON on a 200 response is a transparent proxy or captive portal injecting an HTML interstitial, which IS a network/connectivity issue. The current label is correct for the most common cause. A separate `INVALID_RESPONSE` code is over-classification. |
| HIGH | DeepSeek | `useLeadFeed` snap-advance effect lacks cleanup function | n/a | FALSE ALARM — the effect only calls `setSnappedLocation` (synchronous Zustand action). No async operations, no timers, no subscriptions. No cleanup needed. Independent reviewer also verified PASS (C17). |
| CRITICAL | DeepSeek | `enabled: hasHydrated` race causes double fetch (page 1 with snap=null fallback, then snap seeded, queryKey changes, second fetch) | n/a | FALSE ALARM — independent reviewer verified PASS (C3). Both the `enabled` gate AND the snap-seed effect's `if (!hasHydrated) return` early-return prevent the race. The effect can't seed before `enabled` flips, so the queryKey transition is atomic. |
| HIGH | DeepSeek | `SaveButton` `useEffect` syncs `initialSaved → saved` during mutation pending → optimistic UI clobbered mid-request | n/a | DEFENSIBLE — verified Phase 3-ii fix is in place. The `userInteractedRef` gate prevents the animation from re-firing on parent prop changes; the sync only updates local `saved` state, not the in-flight mutation. The optimistic UI is local to the mutation's lifecycle, not the prop sync. |
| MED | DeepSeek | `lead_save_failed` telemetry inflated by INVALID_INPUT early-return path | n/a | FALSE ALARM — verified Phase 3-ii fix already validates BEFORE any state mutation OR telemetry call. The compensating telemetry only fires from inside `mutation.mutate({ onError })`, never from the validation early return. |
| LOW | DeepSeek | SaveButton `aria-label` + `aria-pressed` double-announce ("Save lead, pressed") | n/a | DEFENSIBLE — Phase 3-ii intentional WCAG toggle pattern. The W3C ARIA Authoring Practices Guide explicitly recommends stable label + `aria-pressed` for toggle buttons; a dynamic label IN ADDITION to `aria-pressed` is the anti-pattern, not the other way around. |
| MED | DeepSeek | `directionsHref` should `encodeURIComponent` lat/lng | n/a | FALSE ALARM — `latitude`/`longitude` are typed as `number | null` and gated by `Number.isFinite()` before interpolation. The type system prevents string injection. |
| HIGH | DeepSeek | Footer div with `onClick stopPropagation` is a keyboard trap | n/a | FALSE ALARM — the buttons inside the div are independently focusable via Tab. The presentation div doesn't intercept keyboard navigation; it only catches the synthetic onClick bubble. Verified manually and via the existing test that fires keyboard events on footer children. |
| MED | DeepSeek | `hasMore` nullish coalescing hides pagination bug if `query.hasNextPage` is undefined | n/a | DEFENSIBLE — `boolean | undefined` collapsing to `false` is the correct behavior. `undefined` means "TanStack Query hasn't determined hasNextPage yet" which is functionally identical to "no more pages right now". |
| LOW | DeepSeek | `endMessage` recomputes on every render → InfiniteScroll re-renders entire component | n/a | DEFENSIBLE — premature optimization. `endMessage` is two `<div>` elements with text. React reconciliation handles this in microseconds. |
| CRITICAL | DeepSeek | `InfiniteScroll` `next` callback lacks debouncing → duplicate fetches | n/a | ALREADY FIXED — Phase 3-iv `f619814` added `if (query.isFetchingNextPage) return` guard. DeepSeek didn't read the latest commit. |
| MED | DeepSeek | `sanitizeTelHref` UK number truncation at 15 digits | n/a | DEFENSIBLE — 15 digits IS the spec (E.164 max). UK numbers fit comfortably in 15 digits including country code. The clamp protects against corrupted source data, not valid international numbers. |
| LOW | DeepSeek | `LeadsClientShell` auto-request effect deps include `request` — could fire repeatedly | n/a | FALSE ALARM — `request` is `useCallback([], [])` stable across the component lifetime. The effect re-runs only when `status.state` changes, and the inner condition `'idle' || 'prompt'` evaluates false after the first request fires. |

## 2026-04-09 — User-supplied Gemini holistic WF3 (8 closed bugs across Phase 1 + Phase 3)

User pasted 9 bugs from a separate Gemini analysis spanning Phase 1 (cost-model, timing, get-lead-feed) and Phase 3 (useGeolocation, format.ts). Validation against the actual code: 6 real, 2 false alarms, 1 defensible UX preference. Then a 3-reviewer adversarial pass on the WIP found 2 more critical issues from the independent reviewer + 1 adjacent fix from Gemini. Final tally: 8 real fixes in this batch, the rest defer or reject.

| Sev | Source | Item | Closed in | Notes |
|-----|--------|------|-----------|-------|
| HIGH | User Gemini "Cost Cache Bypass" | Builder CTE averaged raw `p.est_const_cost` instead of `cost_estimates` cache → parity divergence with permit CTE which uses `ce.cost_tier` | (this WF3) | Added `LEFT JOIN cost_estimates ce_b` + `COALESCE(ce_b.estimated_cost, GUARDED_raw)` in both the avg_project_cost projection and the value_score CASE. The COALESCE fallback to raw is guarded by `CASE WHEN p.est_const_cost > 1000` to prevent placeholder values from leaking in (independent reviewer C5 caught the placeholder threshold gap on the first WIP). |
| HIGH | User Gemini "LATERAL Cartesian Explosion" | LEFT JOIN LATERAL in builder CTE fired once per row of the post-JOIN cross product (entities × entity_projects × permits × permit_trades). With 150 active permits per builder, the lateral fired 150 times per builder. | (this WF3) | Lifted the WSIB lookup into a `wsib_per_entity` CTE keyed by `linked_entity_id` with `DISTINCT ON ... ORDER BY linked_entity_id, last_enriched_at DESC, id DESC`. Same row-pick semantics, fires once per unique entity for the whole query. Replaced the LATERAL with a regular `LEFT JOIN wsib_per_entity w ON w.linked_entity_id = e.id`. 5 new SQL structure tests lock the new shape. |
| MED | User Gemini "Infinity Stale Guard" | `checkStaleness` returned null for permits with zero passed inspections → 15-year-old never-inspected permits fell through to tier2IssuedHeuristic and got the squishy "trade window may have passed" message instead of definitively stalled. | (this WF3) | Extended `checkStaleness` to a two-branch implementation: (1) has-passed-inspection branch (existing); (2) zero-inspection + issued > STALENESS_DAYS branch returns the stalled fallback with a different display string. Moved the call site OUT of the `if (inspections.length > 0)` gate so the guard runs unconditionally. 2 new tests cover both branches. |
| MED-HIGH | User Gemini "Scope Tags Double-Dip" | `cost-model.ts` `sumScopeAdditions` and `computeComplexityScore` iterated `scope_tags` without dedup → `['pool','pool']` added $80K twice. PG `text[]` doesn't enforce uniqueness. | (this WF3) | Wrapped both iterations in `new Set(tags.map(t => t.toLowerCase()))` so duplicate tags collapse to one. 4 new tests cover dedupe (single dup, triple dup, mixed-case dup, multi-tag dup). Existing single-tag tests still pass — `Set(['pool'])` iterates once, behavior unchanged for non-duplicate inputs. |
| MED | User Gemini "Mobile OS Suspend Drift" | iOS WebKit kills the Permissions API change listener when the app is pushed to the background. The user toggles GPS off in Settings, returns to the app, and the change handler never fires → status stuck on pre-suspend value. | (this WF3) | Added a second `useEffect` in `useGeolocation` that listens for `document.visibilitychange` and re-polls `checkPermissionState()` when the page becomes visible. SSR-safe via `typeof document === 'undefined'` early-return. Cancellation guard via `let active = true` flag mirrors the mount effect's pattern (independent reviewer C11 caught the missing guard on the first WIP). 3 new tests cover the visibility transition, the hidden-state bail, and the unmount cleanup. |
| LOW | User Gemini "0-0 Weeks Math Gap" | `tier2IssuedHeuristic` day-based overdue guard `elapsedDays > p75` missed the rounding cliff: when `remainingMax < 4 days`, `Math.round(remainingMax/7)` rounds to 0 → user sees "0-0 weeks remaining". Example: p75=178, elapsed=175 → maxWeeks=0. | (this WF3) | Added `if (maxWeeks <= 0)` guard after the rounding step, routing to the same overdue display branch as the day-based guard. 1 new test asserts the cliff scenario produces the overdue message instead of "0-0 weeks remaining". |
| HIGH | Adversarial review (Gemini holistic line 1165) | `opportunity_score` CASE in builder CTE used `COUNT(p.permit_num)` while `active_permits_nearby` uses `COUNT(DISTINCT (permit_num, revision_num))`. Same entity_projects-duplication issue, asymmetric defense. | (this WF3) | Mirrored the COUNT DISTINCT defense across all 3 CASE arms in opportunity_score. The Phase 3-iii fix that added DISTINCT to active_permits_nearby missed this adjacent CASE. |
| HIGH | Adversarial review (Gemini holistic line 875) | `new Date(candidate.issued_date)` wrap in checkStaleness call site was redundant — `candidate.issued_date` is already typed `Date | null`. The wrap would silently mask a regression that returned a string from the DB layer. | (this WF3) | Simplified to pass `candidate.issued_date` directly. The ternary guard already prevents the `new Date(null) → 1970` crash; removing the wrap makes the type contract explicit. |
| FALSE | User Gemini "Silent UI Death" | recordLeadView swallows constraint violations + frontend optimistic state masks the failure | n/a | FALSE ALARM verified: `view/route.ts:92-99` maps `result.ok=false` → 500 via `internalError()`. SaveButton's mutation onError rolls back the optimistic state + fires `lead_save_failed` telemetry. User DOES see the failure (heart flips back). The deeper "no error code distinguishing constraint violation from network outage" is a low-priority refinement, filed as a separate observability follow-up below. |
| FALSE | User Gemini "Decimal Rounding Bleed" | `formatDistance` produces "10.0km" vs "10km" inconsistency | n/a | FALSE ALARM verified: `format.ts:24-28` uses `toFixed(1)` for ALL distances `>= 1000m`. Both `9999.9m → "10.0km"` and `10001m → "10.0km"`. There is no path that drops the decimal. The user is reading a stale or different version of the helper. |
| DEFENSIBLE | User Gemini "Permission Paradox" | `useGeolocation` initial-denied vs active-deny UX inconsistency | n/a | DEFENSIBLE per inline doc at `useGeolocation.ts:110-117`: initial-denied is indistinguishable from session-denied at the Permissions API layer, so we mark `permanent: false` to allow a re-prompt. The "click and discover" flow is intentional. UX could be smoother (one-click vs zero-click discovery) but it's a preference, not a bug. Filed for the existing geolocation state-machine WF in followups. |

### Pre-existing items flagged by adversarial review (NOT in scope for this WF3 — filed for followup)

| Sev | Source | Item | Notes |
|-----|--------|------|-------|
| CRITICAL | Gemini + DeepSeek | `determineBaseRate` defaults to SFD rate for unknown new builds (industrial, institutional, mixed-use) → orders of magnitude underestimate | Pre-existing spec 72 gap. Tracked for the cost-model spec hardening WF. |
| CRITICAL | DeepSeek | `determineCostTier` boundary off-by-one — `cost = 100_000` exactly returns "small" because `<` not `<=` | Pre-existing. Need to verify against spec 72's "inclusive lower bound" claim. May or may not be a real bug depending on the spec's actual phrasing. Filed for verification. |
| HIGH | DeepSeek | `getGlobalMedianCalibration` averages percentiles across permit types — statistically invalid | Pre-existing. The calibration script could compute a true global median and cache it directly. Tracked for the timing-engine spec hardening WF. |
| HIGH | DeepSeek | `tier1StageBased` integer division by 10 on `stage_sequence` difference — assumes sequential numbering, breaks if a stage is added with sequence 15 | Pre-existing. The mapping should be data-driven (DB lookup), not hardcoded division. |
| HIGH | DeepSeek | Stage name → sequence mapping in `getStageSequence` is hardcoded substring matching | Pre-existing, already in followups. |
| MED | Independent reviewer C4 | `wsib_per_entity` CTE unbounded scan: trades per-row repetition for a full-table CTE scan on every feed request. Net win depends on `wsib_registry(is_gta, last_enriched_at, linked_entity_id)` index coverage | NON-BLOCKING perf concern. Recommend adding a covering index on `wsib_registry(is_gta, linked_entity_id, last_enriched_at DESC)` before scaling. Tracked. |
| LOW | Gemini | `useGeolocation` permanent denial classification on getCurrentPosition error | Pre-existing, already in followups under the geolocation state-machine WF. |
| LOW | Gemini | `PREMIUM_LABEL_THRESHOLD` derived from `PREMIUM_TIERS[2]?.multiplier` (magic index) | Pre-existing nit. Filed for cost-model cleanup. |
| MED | DeepSeek | `tenure_renter_pct > 50` urban/suburban heuristic in `computeBuildingArea` is undocumented | Pre-existing spec 72 design choice. Document or use continuous function. |
| MED | DeepSeek | `useGeolocation` auto-call `request()` on mount drains battery for users who previously granted | Pre-existing UX preference. Caching coords with `maximumAge` is a future optimization. |
| MED | DeepSeek | Visibility change handler aggressively re-polls + re-fetches on every tab focus | Real concern but throttling adds complexity. Filed for the geolocation state-machine WF. |
| LOW | DeepSeek | `recordLeadView` doesn't distinguish constraint violation from DB outage in the error code | The user-supplied "Silent UI Death" claim was a false alarm, but this underlying refinement is real. Filed for an observability/error-classification WF. |

## 2026-04-09 — Phase 3-vi WF1 (is_saved field + observability sibling)

User asked to execute the highest-severity OPEN bug from review_followups.md (the SaveButton.initialSaved one) AND tackle the 3 Phase 3 frontend observability gaps. Cross-Domain WF1 with two atomic commits.

### Part A-D — `is_saved` field (commit 910d8d3)

| Sev | Source | Item | Closed in | Notes |
|-----|--------|------|-----------|-------|
| HIGH | Indep IMPORTANT 1 | SaveButton.initialSaved always false → saved state lost on refetch | 910d8d3 | LEFT JOIN lead_views in both CTEs with COALESCE/bool_or projection. Cards pass `initialSaved={lead.is_saved}`. SaveButton useEffect now gates on `!mutation.isPending` to prevent the heart-flicker race mid-save. |
| CRITICAL | Indep Issue 1 | SQL JOIN safety claim was wrong: lead_views UNIQUE is on `(user_id, lead_key, trade_slug)`, NOT on `(user_id, permit_num, revision_num, trade_slug)`. Pre-LPAD-normalization data could collide. | 910d8d3 | Added explicit `lead_key` equality to BOTH JOINs (matches the actual unique index). Decomposed columns retained for selectivity. |
| Indep Issue 2 | test gap | The `mutation.isPending` gate was implemented but no test exercised the click → refetch → resolve → final-sync sequence | 910d8d3 | Added 2 regression tests in SaveButton.ui.test.tsx covering both halves: (1) in-flight mutation gates the sync, (2) when isPending=false, sync fires normally. |
| CRITICAL | Gemini | Builder lead_id `e.id::text` sorts lexicographically — `'9' < '10'` breaks cursor pagination on relevance ties | n/a | DEFERRED — REAL but PRE-EXISTING (in get-lead-feed.ts since Phase 1b-iii, not introduced by this WF1). Fixing requires changing the on-wire lead_id format (`LPAD(e.id::text, 10, '0')`) which is a cross-version cursor compatibility migration — clients with cached cursors would mismatch. Filed for a dedicated cursor-stability WF. |
| FALSE | DeepSeek | "SQL injection via user_id" | n/a | FALSE — pg parameterized queries are injection-safe regardless of validation. |
| FALSE | DeepSeek | "missing GiST index" | n/a | FALSE — created in migration 078. |
| FALSE | DeepSeek | "next_cursor uses res.rows.length" | n/a | FALSE — already fixed in previous WF3. |
| FALSE | DeepSeek | "mapRow drops rows" | n/a | FALSE — already addressed via the cursor=raw-rows fix. |
| FALSE | Gemini | "repeated CASE calculations" | n/a | DEFENSIBLE — Postgres planner CSE. Same triage as previous reviews. |
| FALSE | Gemini | "lat/lng input validation" | n/a | FALSE — Zod handles in `schemas.ts`. |

### Part E — observability sibling (commit pending)

| Sev | Source | Item | Closed in | Notes |
|-----|--------|------|-----------|-------|
| HIGH | User question | `useLeadFeedState.validatePersistedSlice` silently clamps/resets corrupted localStorage with no telemetry | (Part E) | Added `lead_feed.persisted_state_recovered` event emit per-field clamp/recovery. PII-safe: object/array originals are sanitized to `[object:...]` type tags; long strings (> 50 chars) are sanitized to `[string:N]` tags to prevent coordinate strings from leaking. Whole-slice corruption uses a distinct `__slice__` field sentinel so PostHog facets distinguish it from per-field recovery. 5 new tests. |
| HIGH | User question | `useGeolocation.checkPermissionState` swallows Safari "throws on some permission names" quirk silently | (Part E) | Added `lead_feed.geolocation_query_failed` event emit with `error_message` + `error_name`. 1 new test. |
| HIGH | User question | TanStack Query 4xx errors surface as `unreachable` empty state with no engineering visibility | (Part E) | Added `lead_feed.client_error` event emit via useEffect with ref-based dedup keyed on `(code, message)`. The dedup correctly suppresses duplicate emits when the same query refetches into the same error state. 2 new tests including a refetch-loop regression that proves the ref guard fires (the original test only exercised React's effect-skip behavior, not the ref code path — caught by independent reviewer Critical #1). |
| HIGH | Gemini | `capture.ts` queue memory leak: if `posthog.init()` throws (ad blocker, network failure), the catch silently swallows, `loaded` stays false, every captureEvent queues forever | (Part E) | Added `initFailed` flag set on init catch + missing-key path. captureEvent becomes a no-op after `initFailed`. Also capped queue size at MAX_QUEUE_SIZE=100 with drop-oldest policy as belt-and-suspenders for the slow-load-burst scenario. |
| CRITICAL | Indep Issue 1 | The dedup test for client_error was structurally invalid — `rerender()` with stable props skipped the effect entirely, so the ref guard code was never reached | (Part E) | Rewrote the test to call `query.refetch()` repeatedly, which produces a new `query.error` object reference (different from a stable rerender) so the effect re-runs and the ref check is actually exercised. |
| CRITICAL | Indep Issue 2 | The geolocation_query_failed test used `vi.doMock` + `vi.resetModules()` + dynamic import inside the test body, which clobbers module isolation for the rest of the file | (Part E) | Refactored to use the existing top-level `vi.mock` pattern (hoisted captureEvent mock to module scope). No more `vi.resetModules()`. Test isolation restored. |
| Important | Indep Item 3 | String primitives bypassed PII sanitization in the whole-slice corruption path | (Part E) | Added `PII_STRING_LENGTH_LIMIT = 50` — strings longer than the threshold are sanitized to `[string:N]` tag. Test asserts no coordinate substrings leak. |
| Important | Indep Item 11 | Whole-slice rollup used `field: 'radiusKm'` as sentinel — pollutes PostHog facet with false signal | (Part E) | Distinct `field: '__slice__'` sentinel. Test asserts the sentinel value. |
| MED | Gemini | useGeolocation `permanent: true` on change/visibility handlers vs `permanent: false` on initial mount | n/a | DEFENSIBLE — change/visibility handlers fire on UNAMBIGUOUS user action (the user explicitly toggled permission while the app was running or backgrounded). UX preference, not a bug. |
| MED | Gemini | migrate function not future-proof | n/a | PRE-EXISTING design choice, defer to a separate migrate-pattern WF. |
| LOW | Gemini | Symbol type handling in emitRecovery | n/a | EDGE CASE — Symbols can't survive localStorage round-trip. Defer. |
| LOW | Gemini | UI flicker on permission change → granted → request() → 'requesting' → granted | n/a | UX preference, micro-flicker. Defer. |
| FALSE | DeepSeek | "radiusKm > 0 should be >= 0" | n/a | FALSE — radius 0 is meaningless, server enforces `.positive()`. |
| FALSE | DeepSeek | "snap seeding repeats" | n/a | FALSE — verified handled (snap !== null after first seed). |
| Pre-existing | DeepSeek | useGeolocation initial effect `request()` async cleanup, snap seeding with NaN coords, etc. | n/a | All pre-existing concerns from earlier reviews, already in followups. |

## 2026-04-09 — Phase 3-v WF1 (LeadFeedHeader + LeadFilterSheet)

Phase 3-v ships the sticky header chrome + Vaul bottom drawer filter sheet that were deferred from Phase 3-iv, plus the "Reset filters" CTA that was Layer 3 of the earlier Zod deadlock fix. Three adversarial reviewers (Gemini + DeepSeek + independent worktree) spawned in parallel against the bundle. Self-checklist (15 items) walked against the actual diff BEFORE reviews — all PASS.

### Closed from earlier deferrals

| Sev | Source | Item | Closed in | Notes |
|-----|--------|------|-----------|-------|
| HIGH | User review (earlier WF3) | Zod deadlock Layer 3: "Reset filters" UI failsafe — deferred "to Phase 3-v where the filter sheet naturally hosts the affordance" | (this WF1) | **CLOSED** — LeadFilterSheet ships a "Reset to defaults" footer button that calls `setRadius(DEFAULT_RADIUS_KM)` + `captureEvent('lead_feed.filter_changed', {field:'reset', source:'filter_sheet_reset_cta'})` + `onOpenChange(false)`. Test asserts the sheet closes after reset (terminal action) and that the event fires with the correct payload. |

### Real fixes applied before commit (6)

| Sev | Source | Item | Closed in | Notes |
|-----|--------|------|-----------|-------|
| CRITICAL | Indep Item 1 | The deselect guard test at `LeadFilterSheet.ui.test.tsx` used `fireEvent.click` which doesn't fire the pointer events Radix ToggleGroup listens for. The test passed vacuously (radiusKm stayed 10 because Radix never ran, not because the guard worked). Removing the ref guard would still have passed the test. | (this WF1) | Switched to `userEvent.click` from `@testing-library/user-event` which fires `pointerdown → pointerup → click`. Now the test genuinely exercises Radix's deselect path and the guard is proven to block the NaN poison. Added `@testing-library/user-event` as a dev dep. |
| IMPORTANT | Indep Item 12 | `<Label htmlFor="radius-group">` targeting a Radix `ToggleGroup.Root` (renders as `<div role="group">`) is invalid HTML — `<div>` is not a labelable element. Click-to-focus behavior silently broken for keyboard users. | (this WF1) | Removed both the `htmlFor` and the `id` attribute. Label now renders as a plain visual header. The group is already labelled for screen readers via `aria-label="Search radius in kilometres"` on the ToggleGroup root. |
| IMPORTANT | Indep Item 15 | Header "open sheet" tap emitted `lead_feed.filter_changed` with `action:'open'` — semantically wrong (opening a sheet is UI navigation, not a filter value mutation). Would inflate PostHog `filter_changed` counts by 1 for every sheet open and corrupt funnel analysis. | (this WF1) | Added a dedicated `lead_feed.filter_sheet_opened` event name to the EventName union. Header emits this name with `{source: 'header_tap'}`. Test explicitly asserts the new name fires AND that the old conflated name does NOT fire. |
| HIGH | Gemini | `aria-label="Open filter sheet to change location or radius"` on the header button OVERRIDES the visible dynamic text (`"Near you · 20km"`) for screen readers. Assistive tech users hear the generic static string and miss the contextually useful location/radius readout. | (this WF1) | Removed the static aria-label. The button's accessible name now derives from its visible text content, which includes the dynamic location label + current radius. Added `aria-expanded={filterOpen}` so AT users know whether the sheet is open. Test asserts BOTH: no aria-label override AND aria-expanded reflects state. |
| HIGH | Gemini + DeepSeek | `RADIUS_OPTIONS = ['5','10','20','30','50']` hardcoded the top value. An inline comment admitted "if MAX_RADIUS_KM ever changes, the 50km option at the top needs to update to match" — a maintenance time-bomb. Future developers reading code don't read comments. | (this WF1) | Changed to `['5','10','20','30', String(MAX_RADIUS_KM)]` derived from the single source of truth in `distance.ts`. If MAX_RADIUS_KM changes, the top option follows automatically. |
| NIT → MED | DeepSeek | Header button missing `aria-expanded={filterOpen}` for screen readers to announce whether the sheet is currently open. | (this WF1) | Added. Test asserts `aria-expanded="false"` on the initial render. |

### Rejected as noise or pre-existing

| Sev | Source | Item | Notes |
|-----|--------|------|-------|
| FALSE | DeepSeek | Spread override of `shouldScaleBackground` in Drawer wrapper | FALSE — `shouldScaleBackground` is destructured OUT of props before the spread, so `{...props}` can't contain it. Destructuring order prevents the override. |
| FALSE | DeepSeek | `handleRadiusChange` doesn't check `Number.isNaN(km)` | FALSE — `!Number.isFinite(km)` already rejects NaN (NaN is not finite). Covered. |
| FALSE | DeepSeek | No `Math.min` clamp to MAX_RADIUS_KM in `handleRadiusChange` | FALSE — `RADIUS_OPTIONS` tuple contains only valid values. Belt-and-suspenders is the Zod deadlock fix's `validatePersistedSlice` clamp on rehydration. |
| FALSE | DeepSeek | ToggleGroup missing `orientation` prop | FALSE — Radix defaults to `horizontal`. The wrapper's `...props` forward lets consumers pass it if they want. |
| FALSE | DeepSeek | ToggleGroupItem size not inherited from parent context | FALSE — Shadcn pattern is per-item `cva` variants, not context inheritance. Intentional. |
| FALSE | DeepSeek | `aria-describedby` for DrawerDescription missing | FALSE — Radix Dialog wires this internally via `DrawerDescription` component. No manual wiring needed. |
| FALSE | Gemini | Multiple `'use client'` directives | FALSE — each file has ONE directive at the top. The reviewer was reading the concatenated `/tmp` bundle and seeing them compound. |
| DEFENSIBLE | Gemini | `DrawerContent` composition inflexibility (overlay hardcoded) | DEFENSIBLE — canonical Shadcn pattern. Every Shadcn Drawer wraps overlay+content as a single unit. Changing would fork the upstream convention for no gain. |
| DEFENSIBLE | Gemini | Drag handle hardcoded in DrawerContent | DEFENSIBLE — same as above. Shadcn-canonical. Extractable if a future variant needs it. |
| DEFENSIBLE | Gemini | `z-50` shared for overlay and content | DEFENSIBLE — Shadcn Drawer convention. DOM order handles stacking. A central z-index token system is a separate project-wide refactor. |
| DEFENSIBLE | Gemini | Hardcoded `radius-group` id risk of duplicate if LeadFilterSheet is mounted twice | MOOT — the id was removed as part of the Indep Item 12 fix. Label no longer has htmlFor, no id on the group. If future use cases require `useId`, add then. |
| DEFENSIBLE | DeepSeek | `mt-24` pushes content off-screen on small devices | DEFENSIBLE — 96px top margin on a 567px iPhone SE viewport is 17%, leaves 471px for drawer content. Filter sheet total content is ~200px. Fits comfortably. |
| DEFERRED | Gemini | i18n hardcoded strings (km units, "lead/leads" pluralization) | DEFERRED — out of scope for Phase 3. Toronto-only product, English-only. File for a dedicated i18n WF if/when we expand. |
| DEFERRED | DeepSeek | `{lat: 0, lng: 0}` Null Island check in locationLabel | DEFERRED — we never set `(0, 0)` as a default; locations come from real geolocation or persisted state. Null Island IS valid coords (middle of the Gulf of Guinea). Edge case not worth the complexity. |
| DEFERRED | DeepSeek | Intl.PluralRules for lead count | DEFERRED — same as i18n above. |
| DEFERRED | DeepSeek | DrawerPortal conditional rendering for perf | DEFERRED — Shadcn default. Premature optimization for this use case. |
| DEFERRED | Gemini | `DrawerHandle` extractable component for customization | DEFERRED — YAGNI. Only one Drawer consumer exists today. Extract when a second one has a different need. |

---

## Phase 3 + Phase 0-3 Holistic Review (2026-04-09)

**Anchor:** `bd3c7de` (pre-WF3 HEAD)
**Reviewers:** 2 independent worktree agents (Phase 3 bundle + Phase 0-3 bundle), DeepSeek Phase 3, Gemini Phase 0-3, DeepSeek Phase 0-3. (Gemini Phase 3 returned 503 — re-run skipped, DeepSeek + Independent provided full P3 coverage.)

### Closed in this WF3 (Phase A-F)

| Phase | Sev | Source | Item | Verdict |
|-------|-----|--------|------|---------|
| A | **BLOCK** | Independent P03 | Permit `lead_key` SQL JOIN missing `'permit:'` prefix → `is_saved` structurally always false for permits | CLOSED — `LEAD_FEED_SQL` lv_p predicate now `('permit:' \|\| ...)`. Test at get-lead-feed.logic.test.ts:244 was codifying the wrong format; both fixed. |
| A | **BLOCK** | Independent P03 | Builder `lead_key` JOIN missing `'builder:'` prefix → `is_saved` always false for builders | CLOSED — lv_b predicate now `('builder:' \|\| e.id::text)`. |
| C | HIGH | Gemini P03 | `classifyRoute` default `'public'` (fail-open) | CLOSED — flipped to `'authenticated'` + regression test. |
| C | HIGH | Gemini P03 | `NEXT_PUBLIC_DEV_MODE` leaks server flag into client bundle | CLOSED — `isDevMode()` now reads server-only `DEV_MODE`; client `NEXT_PUBLIC_DEV_MODE` retained for cosmetic login button only (defense in depth: prod misconfig of one var alone can't bypass auth). |
| C | HIGH | Indep P3 C1 | Auth 401 mid-session maps to `NETWORK_ERROR` | CLOSED — `fetchLeadFeedPage` now throws dedicated `AUTH_EXPIRED` code on 401. |
| C | HIGH | Indep P3 C2 | Zustand `buildo-lead-feed` per-device key not cleared on sign-out → user A's `snappedLocation` leaks to user B | CLOSED — `resetLeadFeedState()` exported; `signOut()` calls it after Firebase sign-out. |
| D | HIGH | Indep P3 I1 | No `prefers-reduced-motion` on Motion components (WCAG 2.1 SC 2.3.3) | CLOSED — `useReducedMotion` added to PermitLeadCard, BuilderLeadCard, SaveButton; `whileTap`/`transition` neutralized when reduced. |
| D | MED | Indep P3 I2 | LeadFilterSheet ToggleGroup overflows at 320px (iPhone SE) | CLOSED — `flex-wrap gap-2` on ToggleGroup. |
| D | MED | Indep P3 I3 | Filter trigger missing `aria-controls` / `aria-haspopup` | CLOSED — DrawerContent gets `id="lead-filter-sheet"`, header button gets matching `aria-controls` + `aria-haspopup="dialog"`. |
| D | MED | Indep P3 I4 | `SaveButton.animate` inline object literals churn Motion identity | CLOSED — animate/whileTap/transition now `useMemo`-stable. |
| E | MED | Indep P3 I5 | `lead_feed.client_error` emits unbounded `err.message` (PostHog cardinality) | CLOSED — telemetry now emits only the bounded `code`; dedupe key narrowed to `code`. |
| E | MED | Indep P03 I2 | `get-lead-feed` error-path `logError` logs `user_id + lat + lng` (PIPEDA risk) | CLOSED — context scrubbed to `{ trade_slug, radius_km, limit }`. |
| F | MED | Indep P03 I1 | `formatCostDisplay` Math.round produces "$1000K" at 999_500 | CLOSED — switched to `Math.floor`. Tests updated for 999_500 → "$999K", 999_999 → "$999K", boundary cases. |
| F | LOW | Gemini P03 | `buildDisplay` uses `PREMIUM_TIERS[2]` magic index | CLOSED — replaced with `find(t => t.min === 100_000)` semantic lookup. |

### DEFERRED (real but out of scope for this WF3)

| Sev | Source | Item | Notes |
|-----|--------|------|-------|
| MED | Indep P3 I6 | `lead_feed.lead_clicked` emits `lead_id` (cardinality + PII-adjacent) | DEFERRED — `lead_id` is the core click-attribution analytic; dropping it kills funnel analysis. Product RFC required. |
| MED | DeepSeek P3 | `active_permits_nearby` count vs WHERE status filter | NOT-A-BUG — already documented in `get-lead-feed.ts` lines 247-255. DeepSeek missed the comment. |
| LOW | DeepSeek P03 | `getStageSequence` hardcoded stage strings (timing.ts) | DEFERRED — bundle with next timing-engine WF. |
| LOW | DeepSeek P03 | `user_profiles.updated_at` missing trigger | DEFERRED — verify project convention before adding. Low impact. |
| LOW | DeepSeek P03 | `tier2IssuedHeuristic` permit_type null guard | DEFERRED — bundle with next timing engine cleanup. |
| LOW | Gemini P03 | cost-model dual-path TS↔JS | DEFERRED — acknowledged ADR; re-litigate via spec spike. |
| LOW | DeepSeek P3 | `__opportunityLookup` test export | DEFERRED — minor; refactor when test churn warrants. |

### FALSE POSITIVES / DEFENSIBLE

| Source | Item | Verdict |
|--------|------|---------|
| DeepSeek P3 CRIT | Cursor tuple `<` drops tied relevance scores | FALSE — tuple compare with matching `ORDER BY ... DESC, lead_type DESC, lead_id DESC` cascades to (lead_type, lead_id) tiebreakers. Verified by trace. |
| DeepSeek P3 HIGH | `next_cursor` built pre-filter → ghost page | FALSE — already builds from `res.rows.length` (raw) and last raw row. Existing test "next_cursor uses RAW res.rows.length" locks correct behaviour. DeepSeek inverted the claim. |
| DeepSeek P3 MED | `leadViewBodySchema.strict()` rejects unknown fields | DEFENSIBLE — `.strict()` is intentional XOR enforcement, documented at schemas.ts:79-84. |
| DeepSeek P3 MED | Builder `MIN(distance)` semantics | DEFENSIBLE — spec-intended "closest active permit". |
| DeepSeek P3 MED | Migration 078 `DO` block index | DEFENSIBLE — small table. |
| DeepSeek P3 MED | Haversine 0.5% Earth-radius error | DEFENSIBLE — within 500m snap tolerance. |
| DeepSeek P3 LOW | `useLeadView` invalidate `exact: false` | DEFENSIBLE — TanStack prefix-match is intended. |
| DeepSeek P3 NIT | `TIMING_DISPLAY_BY_CONFIDENCE['low']` dead code | NOT-A-BUG — documented at lines 472-482 as forward-compat for spec-71. |
| DeepSeek P03 CRIT | `buildLeadKey` `padStart.slice(-2)` vs `LPAD` truncation direction | FALSE — DeepSeek mis-described LPAD's truncation direction. In practice every revision_num is ≤ 2 chars, so the truncation branch is unreachable and both implementations agree on the actual data domain. |
| Gemini P03 MED | `trade_slug` not FK to `trades` | DEFENSIBLE — documented ADR. |
| Gemini P03 NIT | `_resetCalibrationCache` test export | DEFENSIBLE — test-only, prefixed. |
| DeepSeek P03 MED | `BASE_RATES.sfd` fallback overpriced | DEFENSIBLE — product decision; spec 72 baseline. |

### Process notes

- **Self-checklist generated BEFORE reviews** (19 items). Two findings that the checklist anticipated also surfaced in independent reviews (auth lifecycle + Zustand per-device leak), validating the pre-review checklist habit.
- **Phase B (cursor + next_cursor) was DROPPED entirely** after verifying both findings as false positives mid-implementation. Verifying claims against the actual code before "fixing" prevented two no-op churn commits.
- All 3256 tests pass; typecheck clean.

---

## WF2: Wire compute-cost-estimates + compute-timing-calibration into permits chain (2026-04-10)

**Commit:** pending
**Reviewers:** Independent code reviewer, Adversarial code reviewer, DeepSeek adversarial plan reviewer

### Fixed during review

| Source | Issue | Resolution |
|--------|-------|------------|
| Independent BLOCKER | `snapClient` used after `.release()` in refresh-snapshot.js | Fixed — changed to `pool.query()` |
| Adversarial HIGH (BUG-2) | Empty catch block in assert-data-bounds.js violates `no-empty` | Fixed — added `piErr` logging |
| Adversarial HIGH (BUG-3) | timing_calibration UPSERT not guarded for missing table (42P01) | Fixed — added try/catch around withTransaction |
| Adversarial MEDIUM (BUG-6) | cost_estimates/timing_calibration missing from refresh-snapshot emitMeta reads | Fixed — added to emitMeta |
| Plan review BLOCKER | `isInfraStep` doesn't cover `compute_*` — gate-skipped on 0-new-permit days | Fixed — added `slug.startsWith('compute_')` to predicate |
| Plan review BLOCKER | Advisory lock early-return skips emitSummary | Fixed — added emitSummary + emitMeta before return |
| Plan review HIGH | snapshotPhase hardcoded 14 → needs 18 | Fixed |
| Plan review HIGH | assert-data-bounds checks must be inside runPermitChecks guard | Fixed |
| Plan review HIGH | compute-timing-calibration fatal on missing permit_inspections | Fixed — defensive 42P01 catch |

### Deferred items

| Severity | Source | Item | Planned home | Status |
|----------|--------|------|--------------|--------|
| HIGH | Adversarial BUG-4 | `compute-cost-estimates.js` batch failures silently omitted from emitSummary counts — misleading chain status. Pre-existing, not introduced by this WF2. | WF2 hardening | closed-in-WF3-followups — added failedBatches/failedRows counters + records_meta tracking |
| MEDIUM | Adversarial BUG-5 | `compute_` prefix in isInfraStep also exempts `compute_centroids` in sources chain — latent if sources gate ever added. Consider slug-specific allow-list or rename to `recompute_centroids`. | WF2 if sources gate added | OPEN |
| LOW | Adversarial BUG-7 | Cost/timing check results in assert-data-bounds not appended to `permitsAuditTable.rows` — invisible in dashboard drill-down. | WF2 polish | OPEN |
| MEDIUM | DeepSeek F9 | compute-cost-estimates 237K single-row UPSERTs take 4-8 min. Future optimization: multi-row VALUES like classify-permits.js (MAX_ROWS_PER_INSERT pattern). | WF2 performance | OPEN |
| MEDIUM | DeepSeek F6 | compute-timing-calibration has no advisory lock — asymmetry with cost script. Safe (small table + UPSERT) but inconsistent. | WF2 hardening | OPEN |
| LOW | Independent | Migration 080 DOWN block commented out. Matches existing convention (071, 073) but rollback path is inoperable. | Design decision | OPEN |

## WF3: Fix compute-cost-estimates SQL join + migration 037 idempotency (2026-04-10)

### Fixed

| Source | Issue | Resolution |
|--------|-------|------------|
| Pipeline run failure | `bf.parcel_id does not exist` — building_footprints has no parcel_id column | Fixed — join through `parcel_buildings` intermediate table with LATERAL LIMIT 1 |
| Independent reviewer | Row multiplication risk from multiple `is_primary = true` rows per parcel | Fixed — LATERAL subquery with LIMIT 1 (defensive, matches permit_parcels pattern) |
| Pre-existing | Migration 037 not idempotent — `ALTER TABLE ADD COLUMN` fails on re-run | Fixed — `IF NOT EXISTS` guards via DO $$ block |
| Pre-existing | `cost_estimates` and `timing_calibration` tables not created (migrations 071/073 never applied) | Fixed — applied directly |

### Deferred items

| Severity | Source | Item | Planned home | Status |
|----------|--------|------|--------------|--------|
| HIGH | Adversarial | `link-massing.js:226` PostGIS path pushes arguments in wrong column order: `is_primary` receives `'spatial_polygon'` (string coerced to true), `structure_type` receives area (float), `match_type` receives boolean. All buildings get `is_primary = true`. Pre-existing bug, separate script. | WF3 link-massing | closed-in-WF3 — PostGIS path rewritten with deterministic primary assignment + correct column order |
| HIGH | Adversarial | `parcel_buildings` has no partial unique index `(parcel_id) WHERE is_primary = true` — allows multiple primary buildings per parcel. Needs migration + link-massing fix together. | WF3 link-massing + migration | closed-in-WF3 — migration 081 adds partial unique index after data repair |
| LOW | Adversarial | No `ORDER BY` on compute-cost-estimates SOURCE_SQL — non-deterministic if fan-out occurs. Mitigated by LATERAL LIMIT 1 but not fully defended. | Future hardening | OPEN |

### WF3 link-massing PostGIS fix review findings (2026-04-10)

| Severity | Source | Item | Planned home | Status |
|----------|--------|------|--------------|--------|
| LOW | Independent | PostGIS path uses confidence 0.90 vs JS fallback 0.95 for same centroid_in_parcel operation. Undocumented inconsistency. | Future harmonization | OPEN |
| LOW | Adversarial | Migration 081 repair uses INNER JOIN to building_footprints — would miss orphan FK rows. FK constraint prevents orphans (no CASCADE) so safe in practice. | WONTFIX (FK enforced) | WONTFIX |

## WF1: Lead Feed Health Dashboard — Phase A (2026-04-10)

### Fixed during review

| Source | Issue | Resolution |
|--------|-------|------------|
| Independent CRITICAL #1 | `permits_geocoded`/`permits_classified` count ALL permits, not just active — inflates readiness | Fixed — added active status filter to both queries |
| Independent CRITICAL #2 | `/api/admin/stats` missing `lead_views_total` and `lead_views_saved` | Fixed — added queries + response fields |
| Independent CRITICAL #3 | `TestFeedDebug` field names don't match spec | Fixed — updated spec to match implementation (`permits_in_results` more accurate than `permits_in_radius`) |

### Deferred items

| Severity | Source | Item | Planned home | Status |
|----------|--------|------|--------------|--------|
| HIGH | Adversarial | `saves_today` counts `viewed_at >= CURRENT_DATE AND saved=true`, not actual save timestamp. `lead_views` has no `saved_at` column — needs schema change for accurate engagement metrics. | WF2 migration + schema | OPEN |
| HIGH | Adversarial | Admin auth is cookie-shape-only (no `getUserIdFromSession` call). Pre-existing across ALL admin routes, not specific to this feature. | Auth hardening WF | OPEN |
| MEDIUM | Independent | `cost_coverage_in_results` and `timing_tier_distribution` missing from TestFeedDebug — LeadFeedItem doesn't carry the data needed. | Phase B (add fields to LeadFeedItem) | OPEN |
| MEDIUM | Adversarial | Health endpoint runs 10 unthrottled DB queries per poll cycle, no cache. | Phase B (add 30s server-side cache) | OPEN |
| LOW | Independent | Median/percentile uses floor-index (biased on even arrays). Test only covers odd-length. | Future hardening | OPEN |
| LOW | Adversarial | `computeTestFeedDebug` parameter typed as inline object instead of `LeadFeedItem[]`. | Type hygiene | OPEN |

## 2026-04-10 — Phase B WF1 (LeadFeedHealthDashboard UI)

Phase B ships the admin dashboard UI component with 4 sections (readiness gauge, cost/timing coverage, engagement, test feed tool) consuming Phase A endpoints (committed as 9bc72e1). Two reviewers spawned: independent (worktree isolation) + adversarial. Self-checklist (18 items) walked against diff BEFORE reviews — all PASS.

### Real fixes applied before commit (6)

| Sev | Source | Item | Fix |
|-----|--------|------|-----|
| CRITICAL | Independent | Traffic light `noCostData` used `feedReadyPct === 0` instead of `cost_coverage.total === 0` — real spec RED condition was unimplemented | Added `costCoverageTotal` parameter to `getTrafficLight`, checks `costCoverageTotal === 0` |
| CRITICAL | Independent | `runTestFeed` had no AbortController or timeout — spec §3.3 requires 30s timeout | Added AbortController with 30s timeout, unmount cleanup via `tfAbortRef` |
| CRITICAL | Independent | Result cards used array index `key={i}` — React anti-pattern for re-rendered lists | Changed to `key={permit_num-i}` composite key |
| IMPORTANT | Independent+Adversarial | `page.tsx` marked `'use client'` with no client code — prevents RSC optimization | Removed `'use client'` directive |
| IMPORTANT | Adversarial+Independent | Stale timing test asserted `/YELLOW|RED/` — accepts wrong answer for spec-defined YELLOW condition | Tightened to `toContain('YELLOW')` |
| IMPORTANT | Independent | No test for `timing_freshness_hours: null` traffic light behavior | Added test: null timing with pct > 80 → GREEN (locked as product decision) |

### Deferred items

| Severity | Source | Item | Planned home | Status |
|----------|--------|------|--------------|--------|
| HIGH | Adversarial | `useEffect` for data fetching in admin dashboard — technically violates §12.2 (TanStack Query). DEFENSIBLE: §12.2 scope is `src/features/leads/` per AST-grep, not `src/components/`. Same pattern as existing `DataQualityDashboard.tsx`. | Future admin TanStack Query migration | OPEN |
| HIGH | Adversarial | Test feed form uses `useState` for form fields — violates §12.3 (React Hook Form). DEFENSIBLE: same scope reasoning as above, admin-only form. | Future admin RHF migration | OPEN |
| MEDIUM | Adversarial | Race condition: overlapping interval fetches can overwrite fresh data with stale. DEFENSIBLE: 10s interval + 10s AbortController timeout means at most one in-flight. Same pattern as DataQualityDashboard. | Future TanStack Query migration (fixes both) | OPEN |
| MEDIUM | Adversarial | `performance` section of `LeadFeedHealthResponse` fetched but not rendered — spec goal "Is the feed responding?" unmet. Spec explicitly marks performance fields as "Phase A: null (not yet instrumented)". | Phase C — when performance metrics instrumented | OPEN |
| MEDIUM | Adversarial | No client-side input validation on test feed form — NaN/out-of-range coords sent to server. Server Zod catches with 400 but no inline UX feedback. | UX polish WF | OPEN |
| MEDIUM | Adversarial | Polling test only verifies `setInterval` registration, never advances timers. Fake timers + async React rendering caused 5s timeout in initial implementation. | Future test hardening | OPEN |
| LOW | Independent | `saves_today` fetched in engagement response but not rendered as stat box. Spec focuses on 7-day window; today's saves is complementary data. | UX enhancement | OPEN |
| LOW | Independent | Retry button in error state has no loading feedback — button text stays "Retry" during fetch. | UX polish | OPEN |
| LOW | Adversarial | GREEN traffic light fires when `timing_freshness_hours` is null (never calibrated). Spec requires `< 48` for GREEN, but null ≠ "< 48". DEFENSIBLE: null timing = "never calibrated" is a separate state; treating as non-stale is a product decision, locked by test. | Spec clarification if product disagrees | OPEN |

## 2026-04-10 — WF3 Cost/Timing Audit + Health 500 (commit TBD)

Three user-reported bugs: (1) `compute-cost-estimates` only showed `sys_*` metrics in admin audit view, (2) same for `compute-timing-calibration`, (3) `/api/admin/leads/health` returned 500. Root causes: scripts never built custom `audit_table.rows`, and migration 070 was never applied to local DB (lead_views stuck on 069 schema). Two reviewers (independent + adversarial) ran in parallel against the fix bundle. Both independently flagged the same 2 HIGH issues.

### Real fixes applied before commit (2 during triage)

| Sev | Source | Item | Fix |
|-----|--------|------|-----|
| HIGH | Both (Independent+Adversarial) | `no_rows` path in timing calibration used `verdict: 'WARN'` — would escalate chain status to `completed_with_warnings` on fresh deploys where `permit_inspections` hasn't been populated yet. Prior behavior was silent (no audit_table → neutral PASS). | Changed verdict to `SKIP` (infra state, not data quality warning) |
| HIGH | Both (Independent+Adversarial) | `coverage_pct` in cost-estimates was semantically `(inserted + updated) / processed` — actually upsert success rate, not model coverage. Always reads 100% when no batch failures, regardless of how many permits got `estimated_cost: null`. Label and threshold were misleading. | Added `nullEstimates` counter; renamed to `model_coverage_pct`; formula now `(processed - nullEstimates) / processed`; threshold lowered to 80% (realistic for a model with fallback nulls); verdict now downgrades to WARN on low coverage |

### Deferred items

| Severity | Source | Item | Planned home | Status |
|----------|--------|------|--------------|--------|
| HIGH | Self (sibling bug) | `scripts/enrich-web-search.js` and `scripts/enrich-wsib.js` have the same observability gap — they emit `emitSummary` without a custom `audit_table`, so the admin UI shows only sys_* rows. Both ARE in active chains (`enrich_wsib_builders`, `enrich_named_builders`, `enrich_wsib_registry`). Scope discipline: user reported steps 14/15 only; fix siblings in a separate WF3. | Dedicated sibling WF3 | OPEN |
| MEDIUM | Both | New audit_table tests are pure file-text regex matching — cannot catch row-construction bugs where string literals exist but the audit_table is in a dead code branch, a comment, or has transposed values. DEFENSIBLE: pre-existing pattern across all `*.infra.test.ts` file-shape tests. A logic-level test (stub `emitSummary`, assert on captured payload) would catch this class. | Future test hardening WF | OPEN |
| MEDIUM | Self | Migration runner (`scripts/migrate.js`) has no tracking — runs all files every time. Migration 070 apparently never ran on local DB despite being on disk, leading to this session's lead_views schema mismatch. No `schema_migrations` table to detect partial-apply state. | Migration runner hardening WF | closed-in-(WF3 2026-04-10 lead feed health 3-bug fix) — schema_migrations tracking table + --verify/--force/--dry-run flags added |
| LOW | Adversarial | `parseInt(r.sample_size, 10)` in `compute-timing-calibration.js` is unnecessary — `COUNT(*)::int` always returns JS number via pg driver. Harmless no-op defensive code. | Type hygiene | OPEN |
| INFO | Self | Issue 3 (health endpoint 500) had NO code fix — resolution was applying migrations 070 + 076 + 079 manually to local DB. Production state may also need reconciliation if the same partial-apply occurred there. No test added (existing `lead-views-schema.infra.test.ts` already locks the expected schema, and it's covered by the migration files themselves). | DB reconciliation for prod if needed | OPEN |

## 2026-04-10 — WF3 Lead Feed Health 3-bug fix (test feed error + timing semantics + expanded readiness)

Three user-reported bugs: (1) test feed tool throws `[object Object]` error on Run Test, (2) "4 permit types calibrated" is uninformative, (3) lead feed readiness is incomplete. Also added `schema_migrations` tracking table to migration runner (closed from prior WF3). Two reviewers (independent + adversarial) ran in parallel. Self-critical checklist (20 items) generated BEFORE reviews; 8 concerns matched agent findings.

### Real fixes applied before commit (6)

| Sev | Source | Item | Fix |
|-----|--------|------|-----|
| CRITICAL | Adversarial | `scripts/migrate.js` apply+record was NOT atomic — a crash between `pool.query(sql)` and `recordApplied` would leave a migration applied but untracked, causing destructive migrations to re-run on next invocation | Wrapped apply+record in single transaction with BEGIN/COMMIT/ROLLBACK + dedicated client. CONCURRENTLY path remains best-effort (Postgres rejects explicit transactions with CONCURRENTLY) with a comment explaining the limitation |
| CRITICAL | Adversarial | `--force` flag silently re-ran ALL migrations including destructive ones (DROP TABLE, TRUNCATE) with no warning. A destructive migration like 070 would drop lead_views and lose data if operator ran `--force` to unstick anything | Added warning banner + required `BUILDO_FORCE_CONFIRM=1` env var to proceed. Without the env var, the runner exits non-zero after printing the warning |
| HIGH | Adversarial | `Feed-Path Coverage` section used `active_permits` (ADMIN inclusion-list predicate) as denominator for bars computed against `FEED_ACTIVE_STATUS_PREDICATE` (exclusion-list). Percentages could exceed 100% and compared apples to oranges | Added `feed_active_permits` field using the FEED predicate. UI now uses this as denominator for all Feed-Path Coverage rows, making all percentages directly comparable |
| HIGH | Adversarial | `permits_with_neighbourhood` used the ADMIN predicate while all other Feed-Path fields used the FEED predicate — same inconsistency problem | Switched to `FEED_ACTIVE_STATUS_PREDICATE` consistent with the rest of the section |
| IMPORTANT | Independent | `tfAbortRef` was declared at line 135 but referenced in a `useEffect` cleanup at line 131 — not a runtime bug (React hooks execute before cleanup fires) but confusing code order that violates "code order = execution order" principle | Moved declaration above the useEffect |
| IMPORTANT | Both | `feedReadyRes` was a sequential query after the Promise.all batch, making 14 queries take 13+1 serial round-trips instead of 14 parallel. Held pool connections longer than necessary | Collapsed into the Promise.all batch (14 parallel queries, still pressure on default-10 pool but best we can do without caching) |
| IMPORTANT | Independent | No test for `{error: null}` body shape — the fallback path was unexercised | Added test asserting HTTP 503 fallback when `body.error === null` |

### Deferred items

| Severity | Source | Item | Planned home | Status |
|----------|--------|------|--------------|--------|
| MEDIUM | Adversarial | No integration test exercises the real 13-query SQL in `getLeadFeedReadiness`. A typo in a column reference would pass all existing file-shape tests. | Future test hardening WF (pre-existing pattern — matches project convention of file-shape tests for infra scripts) | OPEN |
| MEDIUM | Both | Pool connection pressure: 14 parallel queries against default-10 `pg` pool every 10s poll, no cache. No immediate failure but can starve other routes under load. Fix options: (a) add 60s TTL cache on `/api/admin/leads/health`, (b) set explicit `max: 20` on poolConfig, (c) batch queries into a single SQL CTE | Future perf WF — add caching layer to admin endpoints | OPEN |
| MEDIUM | Independent | `permits_feed_eligible` uses `latitude IS NOT NULL` as proxy for `p.location IS NOT NULL`. On PostGIS-absent environments (local dev without PostGIS), the trigger in migration 067 no-ops, so lat/lng can be populated while `location` stays NULL. The count will be INFLATED relative to what the feed actually returns on such envs | Add UI caveat OR switch to direct `location` check (blocks on PostGIS-absent envs) | OPEN |
| LOW | Independent | No tests for traffic light at exactly `feed_ready_pct = 80` or `= 50` boundaries. Spec says GREEN > 80, YELLOW = 50-80, RED < 50 — boundary behaviour is unverified | Future test hardening | OPEN |
| LOW | Self | Spec 76 §2.3 references the card as "Timing Calibration" but we relabeled to "Detail-Page Timing Engine". Spec should be updated or label reverted | Doc WF | OPEN |
| LOW | Self | Feed-Path Coverage section has no explicit 375px mobile viewport test. Uses existing responsive patterns (flex-col md:flex-row, grid-cols-1 md:grid-cols-2) but untested | Future test hardening | OPEN |
| INFO | Self | Migrations 067, 072, 075 were not applied on local DB at the start of this session. 067 gracefully no-ops without PostGIS. 072 + 075 applied successfully. All 78 existing migrations are now backfilled into `schema_migrations` as applied | DB reconciliation | closed-in-(this session) |
| INFO | Self | Test feed will still fail locally until PostGIS is installed (`permits.location` doesn't exist without the extension). The client error message now shows the real error (e.g., `column "p.location" does not exist`) instead of `[object Object]`, so the operator can diagnose. Install PostGIS: Scoop → `scoop install postgresql`, WSL2 → `sudo apt install postgis` + `CREATE EXTENSION postgis` | Dev env setup doc | OPEN |
