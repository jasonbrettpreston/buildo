# Defect Ledger — fleet register (Spec 123 §6 G6 / G8)

> Generated **by hand** for Pilot 1 (`assert_schema`, 2026-08-25) — the first register in the repo. From S5 onward this file is **generator-owned** (`scripts/violations/plan-claims.mjs` family); do not hand-edit rows after that point, regenerate.
> ID scheme: `<STEP-PREFIX>-D<n>`; a row is OPEN until the closing commit lands and the differential points at it (G8: *every explained diff points at a Defect Ledger ID*). Anchors are `file:line` at the HEAD stated in the source assessment and rot with the file — re-derive before citing.

| ID | Step | Anchor (HEAD `8b857169`) | One-line | Status | Closes at | Source |
|---|---|---|---|---|---|---|
| AS-D1 | assert_schema | `scripts/quality/assert-schema.js:546` (pre-conversion) | sources verdict `sourceErrors.length` — not row-derived | **CLOSED · 8b** | **peel 8b** — the cascade became row-derived when the library landed (`scripts/lib/step/verdict.js` `deriveVerdict(rows)`, commit 7 `f843c042`); 8b closes the remaining half by making every error a check-id-keyed ROW (`report(<checkId>, {error})` → `checkRow`), so no verdict input exists outside `audit_table.rows`. Both directions locked by `#199` (no `verdictCascade` / parallel boolean in the compute) and `#165` (sabotage → FAIL row) | [pilot 1 assessment](2026-08-25-pilot1-assert-schema-assessment.md) §4.4 |
| AS-D1b | assert_schema (fleet) | `scripts/lib/` (absent, at filing) | no shared `verdictCascade`; 13 per-script copies | **CLOSED · commit 7 (scoped)** | **commit 7** `f843c042` converts assert_schema onto the shared `scripts/lib/step/verdict.js` (`deriveVerdict`) — the file is now 33 lines (`scripts/quality/assert-schema.js`) with zero duplicate cascade code, confirmed by re-grep (`grep -c verdict` finds only the header comment). assert_schema's own copy is retired. The "13 per-script copies" scope was fleet-wide across the *other*, still-unconverted CQA scripts (Spec 120 §9.3 ①) — that residual is a programme-level item, not a per-pilot G6 blocker for assert_schema; carried to `review_followups.md` (2026-08-29, step:validate remediation) so it is not silently dropped | same |
| AS-D2 | assert_schema | `scripts/quality/assert-schema.js:290-602` (pre-conversion) | process-kill strand inside the 21-request window (throws now closed by `f32b1485`) | **CLOSED · commit 7 + R-B** | **commit 7** `f843c042` moves the strand/`finally` window uniformly onto `scripts/lib/step/index.js` (owned once, for every converted step, per the assessment §2.3 `preserved-in-runner` ruling) — assert_schema no longer opens its own 21-request window outside the library's ownership. **R-B reader** (this same active_task.md, step 7) is the runner-level crashed/stuck-running → FULL detector, proven with a live kill-and-rerun against `link_wsib`; it applies identically to assert_schema since both run through the same `scripts/lib/step/index.js` | same |
| AS-D3 | assert_schema | `scripts/quality/assert-schema.js:282-284` | ledger INSERT failure swallowed, `runId` null | OPEN · PIN | commit 7 (verify library `openLedgerRow` does not swallow) | same |
| AS-D4 | assert_schema | `scripts/quality/assert-schema.js:567` (pre-conversion) | finalize UPDATE failure `.catch(warn)`; window relabels `failed` but real meta lost | **CLOSED · commit 7 + R-B** | **verified**: `scripts/lib/step/ledger.js:84` `finalizeLedgerRow` never swallows silently — it `try/catch`es the UPDATE, logs via `pipeline.log.warn`, and returns `false` on failure (its own docstring: "a swallowed UPDATE failure leaves the row 'running', and the ledger strand window ... is what closes it"); the **R-B reader** (step 7, this task) is exactly that downstream strand-window mechanism, live-proven against `link_wsib`. The pre-conversion "real meta lost" failure mode has no equivalent in the library path | same |
| AS-D5 | assert_schema | `scripts/quality/assert-schema.js:539`, `:536` (pre-conversion) | hand-maintained `sources_checked` = 18 + unrecorded regex-token obligation | **CLOSED · 8c** | **peel 8c** — the literal is gone (the audit table is one row per SELECTED check, built by `verdict.js buildAuditTable`) and so is the token obligation (peel 8b). §5.5's dispatch table makes the remaining half structural: `compute()` iterates `ctx.checks` and `src/tests/step-conformance.infra.test.ts` asserts dispatch keys ≡ descriptor check ids in BOTH directions and in order, so adding a source without adding its check — or vice versa — is a build failure rather than a stale constant | same |
| AS-D6 | assert_schema | `scripts/quality/assert-schema.js:473-483`, `:503-504`, `:535-537` (pre-conversion) | error attribution by substring / alternation regex | **CLOSED · 8b** | **peel 8b** — the `includes('permit'/'coa'/'ckan')` tests and the `zoning\|ravine\|centreline` alternation are retired (`knowingly-retired`, operator, assessment §2.5). Each check owns its try/catch and reports `{error}` under its own id; the "add a source ⇒ add a regex token" obligation no longer exists. The coarse outer catch that suppressed every later check is removed with them | same |
| AS-D7 | assert_schema | `scripts/quality/assert-schema.js:527` | `checks_passed` is `'all'`/`undefined`, never a count | OPEN · PIN | commit 7 (declared diff) | same |
| AS-D8 | assert_schema | `scripts/quality/assert-schema.js:118,162,205,224,251` | 21 HTTP requests, no timeout/retry/abort | OPEN · PIN (`network.timeout: "none"`) | DEFERRED — peel-8c candidate, own lock | same |
| AS-D9 | assert_schema | `scripts/quality/assert-schema.js:605` | lock contention → `return;` with no step-level emit | OPEN · PIN | commit 7 (library SKIP emit + `self_skipped`; declared diff) | same |
| AS-D10 | assert_schema | file (`module.exports` = 0, at filing) | no exports; 13 source-text test sites across 6 files | **CLOSED · commit 7** | **verified**: `scripts/lib/compute/assert-schema.js:458-460` now exports (`module.exports = compute; .compute; .checks`); the 13 source-text sites read the compute module's exports (`step-conformance.infra.test.ts`, `golden-fingerprint.infra.test.ts`), not string-matching `assert-schema.js`'s source text | same |
| AS-D11 | assert_schema | `scripts/lib/compute/assert-schema.js` `fetchCsvHeaders`/`fetchGeoJsonPropertyKeys` | Naive header parsers carried verbatim from the old file: `split(',')` breaks on quoted commas; no UTF-8 BOM strip; `res.text()` reads the full body when a server ignores `Range`; GeoJSON properties parsed by regex inside a fixed 8 KB window | **PIN** (at conversion; Spec 123 §3.1) | fix-after — OUTPUT panel 2026-08-25 (Gemini/DeepSeek, grounder-adjudicated C-class) | same |
| AS-D12 | assert_schema | `scripts/lib/compute/assert-schema.js` `validateTypeSample` | Lenient sample: empty sample → pass; ≥1 parseable `EST_CONST_COST` of 20 → pass (plus the pinned non-OK → `true`, `limitations[0]`) | **PIN** (Spec 123 §3.1) | fix-after — same panel | same |
| AS-D13 | assert_schema | `scripts/lib/compute/assert-schema.js` `checkUrlAccessible` | `HEAD`-only reachability, no `GET Range: bytes=0-0` fallback → false FAIL on HEAD-hostile hosts | **PIN** (Spec 123 §3.1) | fix-after — same panel | same |
| LR-D1 | load_ravines | `scripts/load-ravines.js:441` (HEAD `e94014f9`) | `ravine_geometry_skipped_source_id` WARN pushed once per non-carried feature — audit-table row count unbounded | **CLOSED · 8b** | **peel 8b** — the metric is retired and cannot return: it is not a declared check, and `ctx.report` refuses an undeclared id, so the unbounded shape is structurally unreachable rather than merely absent. The dropped ids travel in ONE row’s `observation.detail` on `ravine_geometry_skipped_pct`, capped at `MAX_DETAIL_KEYS` (50) with the EXACT `dropped_count` and a `dropped_ids_truncated` flag beside them — the row COUNT is bounded by the check count and the row SIZE by the cap, and the reader is never misled about how much was lost. The pass/fail bound is computed from `dropped_count`, never from the truncated list. Locked both directions in `src/tests/steps/load_ravines/violations.test.ts` (3× cap → one report; under-cap → complete list; clean load → bare ratio; the retired id → throws) | [pilot 2 assessment](2026-08-25-pilot2-load-ravines-assessment.md) §4.4 |
| LR-D2 | load_ravines | `scripts/load-ravines.js:291-294` (pre-conversion) | prior-run read `.catch(warn → null)`: all drift guards + both skip tiers silently degrade to "first run"; no audit row | **CLOSED · 8a** | **peel 8a** — the posture is DECLARED, not decided in a catch block: `staleness.on_prior_run_error` (`fail_step` \| `warn_row`, absent = `fail_step`) is read by `scripts/lib/step/staleness.js priorRunErrorPosture` and applied by `readPriorEmitWithPosture`. `load_ravines` declares `fail_step` with a why — the baseline feeds both gate tiers and every drift ratio, so running without it writes a legal-constraint surface with every guard 0-by-definition. The `warn_row` arm proceeds but OWES `priorRunErrorRow()` → a `prior_run_read_failed` WARN row rendered through `buildAuditTable`'s `extraRows` (its first consumer), which moves the row-derived verdict off PASS. There is no arm that returns null quietly. Both postures + both directions locked in `src/tests/step-library.logic.test.ts` | same |
| LR-D3 | load_ravines | `scripts/load-ravines.js:308,371,415,454,553` + `scripts/lib/pipeline.js:475` | FAIL verdict exits 0 (`pipeline.run` discards the return); chain `completed_with_errors` (`run-chain.js:838`), no halt; the `completed` row is what `enrich-ravines.js:34-36` reads | OPEN · PIN (`blocking:false`) | **DEFERRED** — promoting a check to `blocking:true` is its own peel + lock (plan D-8) | same |
| LR-D4 | load_ravines | `scripts/load-ravines.js:459` (pre-conversion) | unreachable `etag` / sha1(last-modified) / `String(runAt)` `source_dataset_version` fallbacks (md5 always present on the write path); last is an unstable Date string | **PIN** (INTENT-UNKNOWN) | **DEFERRED past peel 8c, consciously** — the peel closed the threshold-source half and left this one open on purpose: retiring a fallback is a §7.1 adjudication and no operator ruling was given. The agent that discovers may not retire | same |
| LR-D5 | load_ravines | `scripts/load-ravines.js:176-180`, `:423` | BIGINT `source_id` > 2^53 loses precision (#411); `:423` `Number()` Map key miss → silent `skipped`. Live max 14,041,969 | OPEN · PIN | **DEFERRED** — latent; declared WARN-and-skip check when ids expand | same |
| LR-D6 | load_ravines | `scripts/load-ravines.js:556` (pre-conversion) | lock contention → bare `return;`, no step-level emit / `audit_table` | **CLOSED · 8b** | landed at commit 7 (`skipRecordsMeta` + `RUN_STATUS.SELF_SKIPPED`, declared diff D-10); **CONFIRMED at 8b** against THIS descriptor rather than assert_schema’s: a contended write-class run emits `skipped: true` with an `audit_table` whose verdict is row-derived, the declared `lock_held_elsewhere` terminal carries `status: self_skipped`, the compute never runs, the acquisition seam never reaches the network (it is INSIDE the lock), no statement touches `ravines`, and `records_new`/`records_updated` read null rather than 0. `src/tests/step-library.logic.test.ts` | same |
| LR-D7 | load_ravines | `ravines` live catalog (`relrowsecurity=true`, 0 `pg_policies`; role `postgres` `rolbypassrls=true`) | writes succeed only via RLS bypass; a non-bypass role upserts/deletes 0 rows silently → 0/0/0, PASS, `completed` | **CLOSED · commit 7** | **verified**: `scripts/load-ravines.descriptor.json:117-124` declares `guards.requires[0].kind: "rls_bypass_or_policy"` with `expect.writable: "the role bypasses RLS, or a policy grants the write"`, reported on EVERY run (not only the failing branch, per its own `why`) | same |
| LR-D8 | load_ravines | `scripts/load-ravines.js:46-53` (pre-conversion) | six `ConfigSchema` tunables declared, 0 registered (`logic_variables` 422 rows, 0 `%ravine%`; seeds 0) — Zod-default literals (P4; supersedes #421) | **CLOSED · commit 7** | **verified**: descriptor `config.logic_variables` now declares all 6 (`load_ravines_dataset_age_warn_years`, `_count_drift_fail_pct`, `_geometry_update_warn_pct`, `_invalid_geometry_fail_pct`, `_mass_delete_fail_pct`, `_download_timeout_ms`); `step:validate`'s fast invariant #2 confirms 0 missing from `scripts/seeds/logic_variables.json` | same |
| LR-D9 | load_ravines | `scripts/lib/step/index.js` `runIngestPhase` (post-conversion) — pre-conversion `scripts/load-ravines.js:402-417` (L7) and `:443-454` (L8) at `33786d1a` | **write-before-checks**: the INGESTOR phase order (acquire → validate → write → score) retired Spec 59 L7/L8's "FAIL aborts BEFORE any DB write" guarantee — the pre-conversion loader `return`ed `{failed:true}` above `pool.query(VALIDATION_SQL)` / `withTransaction`, the converted step scored the same checks after the write was committed, so the FAIL row reported bad data that was already in `ravines` | **CLOSED · Fold C** | **introduced at commit 7; closed by the operator §7.1 ruling of 2026-08-26.** `checks[].when` gains `pre_write` (`scripts/steps/_schema/step.schema.json`), the runner scores those checks between the read-only geometry validation and the first write statement (`makePreWriteGate` + `runIngestPhase`'s gate in `scripts/lib/step/index.js`), and an unaccepted FAIL SKIPS the write entirely — no transaction opened, `ravines` untouched, the remaining checks scored over a zeroed `written`, terminal `failed_<check_id>`. Acceptance (ruling A-5) moves the DECISION only: `RAVINE_ACCEPT_FEATURE_COUNT_DRIFT=1` lets the write proceed and never suppresses the FAIL row; L8 declares no override, so its abort is unconditional. Exit semantics unchanged (every check stays `blocking:false`, D-8). Locked both directions — `src/tests/steps/load_ravines/violations.test.ts` (L7/L8 × accepted/unaccepted × the reversion detector: `when:"post"` ⇒ no gate ⇒ the write runs on a FAIL) and `src/tests/step-library.logic.test.ts` (the library gate against a fake pool). Differential: skip path + forced reload all IDENTICAL (D-20) | [pilot 2 assessment](2026-08-25-pilot2-load-ravines-assessment.md) §2.3 / Fold C |
| LR-D10 | load_ravines | `scripts/lib/step/acquire.js` `downloadArchive` (post-conversion) — pre-conversion `scripts/load-ravines.js` had the same shape | **no byte cap on the download.** `execution.needs_disk_mb` is DECLARED and never enforced: `downloadArchive` streams `res.body` to disk through `hashThrough` with no ceiling on `bytes`, and the only limit in force is the network timeout. A publisher that replaces a 7.6 MB archive with a 20 GB one fills the temp volume before any check exists to observe it — the `finally` removes the temp root, so the failure surfaces as an ENOSPC from an unrelated process rather than as a lossy source. Bounded in practice by a frozen 2022 dataset, which is why it is LOW and not the defect that stops the pilot | **PIN** (LOW) | DEFERRED — library-wide (`acquire.js` is step-agnostic): abort the stream once `bytes` exceeds `needs_disk_mb` (or a `max_bytes` sibling), and emit the measured byte count as a declared check so the ceiling is observable on the happy path too. Filed to `docs/reports/review_followups.md` under the pilot 2 OUTPUT-panel DEFERs | pilot 2 OUTPUT panel (2026-08-27, Fold D) |
| LR-D11 | load_ravines | `scripts/load-ravines.descriptor.json` `outputs.writes[0].write_discipline.idempotent_rerun` + `scripts/lib/step/acquire.js` `contentHashSkip` | **`idempotent_rerun: "zero_writes"` is not ASSERTED on a forced same-bytes rerun.** The tier-2 content-hash gate makes the UNFORCED second run skip before the write, so the property the declaration names — run the WRITE twice over identical bytes, the second changes nothing — is never exercised by the gate that appears to cover it. `ravine_rows_changed_ratio` measures `rows_changed / rows_scanned` against `pct <= 0.5` on whatever run happens; nothing compares it against 0 on the specific run where the declaration says it must be 0. D-19 measured the pair by hand (forced run 1: 21 updated; forced run 2: 0/0/0) — a manual observation, not a standing check | **PIN** (LOW) | DEFERRED — needs either a declared check bound to the `force_run` + unchanged-`content_hash` conjunction (`ctx.overrides.force_run && ctx.acquired.content_hash === ctx.prior.content_hash ⇒ rows_changed == 0`) or a golden-harness assertion that runs the forced capture twice. Filed to `docs/reports/review_followups.md` under the pilot 2 OUTPUT-panel DEFERs | pilot 2 OUTPUT panel (2026-08-27, Fold D) |
| LM-D1 | link_massing | `scripts/link-massing.js:709-711` (HEAD `73c90d24`, pre-conversion) | `records_new: 0` is a hardcoded literal (`d2050cfc`, premise "never INSERT" — false since the upsert); `records_total = processed` counts the permanently-unmatchable 1,395-parcel tail; `records_updated = parcelsLinked` counts parcels while `buildings_upserted` counts rows | **CLOSED · 8b** | landed at commit 7, **CONFIRMED at 8b** — the three counters come from `written.e2.{scanned, inserted, updated}` resolved by `deriveCounters` against a PER-TARGET keyed `written` (LG-5), so "records_new" can only mean the upsert's inserts and never the primary-clear's rewrites; the compute returns NO `counters` at all (conformance closes the `c.records_new ??` bypass) and the `:710` literal is gone with the file. The measured steady state now reads `records_total = records_new = records_updated = 0` on an incremental run instead of `1395 / 0 / 0` — 1,395 was the permanently-unmatchable tail reported as work done, and 0 was a literal a guarded upsert's `rowCount` could never have measured | [pilot 3 assessment](2026-08-27-pilot3-link-massing-assessment.md) §4.4 |
| LM-D2 | link_massing | `scripts/link-massing.js:691-693`, `:705-706` | the `:705` cascade IS row-derived (`fc038790`) — the defect is a hand-rolled per-script copy (AS-D1b fleet class) plus the `link_rate` row's SEVERITY chosen by `hasPostGIS && processed > 0`, a boolean no row records (an empty-parcels or JS-path run degrades a broken-geometry signal to WARN) | **CLOSED · 8a** | **peel 8a** — both halves, and the second one structurally. (a) The hand-rolled copy is gone: the step contributes no cascade at all, `scripts/lib/step/verdict.js deriveVerdict(rows)` is the only place a verdict is computed, and `#199` fails if a `verdictCascade` / `.some(r => r.status === …)` shape reappears in the compute. (b) The BOOLEAN-DRIVEN SEVERITY is unreachable: `hasPostGIS` no longer exists (A-8, LM-D7) and `link_rate` carries a DECLARED `severity: "FAIL"` that `checkRow` applies to the reported ratio — measured, a grep for `'FAIL'`/`'WARN'` string literals across `scripts/lib/step/*.js` + `scripts/lib/compute/link-massing.js` returns exactly 6 hits, all of them in `verdict.js` (the lattice + the cascade), `index.js` (reading `row.status` back off the rows) and `staleness.js:215` (`priorRunErrorRow`, itself a row). The empty-work condition the boolean was standing in for is now `guards.empty_source` + the `empty_source_guard` `pre_write` check, which is a ROW with its own severity rather than a modifier on someone else's | same |
| LM-D3 | link_massing | `scripts/link-massing.js:320-323` | E1 `UPDATE … SET is_primary = false WHERE parcel_id = ANY($1) AND is_primary = true` is unguarded (x-banned-for-new) and its `rowCount` is discarded; measured: 0 rows affected on BOTH modes by construction (W1 has emptied the FULL scope; `NOT EXISTS` excludes linked parcels in INCREMENTAL) — fence `5bb31faf` is latent, load-bearing only if the read scope and W1's scope diverge | **CLOSED · 8b** | landed at commit 7, **CONFIRMED at 8b** — E1 is `outputs.writes[0]`, `set_based_scoped`, `guard: "none"` with a `guard_why` that says why a change guard would be WRONG rather than merely redundant (`is_distinct_from` skips exactly the still-primary row whose flag must come down before a DIFFERENT building takes it), grandfathered against `5bb31faf` in `scripts/steps/_schema/grandfathered.json`, and `written.e1.{scanned, updated}` is recorded per target so the measured 0 is an observation rather than an absence. `idempotent_rerun: declared_drift` + `declared_drift` state the bound (at most one row per batch parcel, never the table's row count) instead of claiming a `zero_writes` this target cannot honour | same |
| LM-D4 | link_massing | `scripts/link-massing.js:45` vs `:277` (pre-conversion) | `BATCH_SIZE = 500` (JS path, `66bfd529`) vs `pipeline.BATCH_SIZE = 1000` (PostGIS path, `7c75e92e`) for the same work — undeclared, unexplained divergence | **CLOSED · commit 7 (A-8)** | **verified**: `scripts/link-massing.descriptor.json:269` declares `execution.batch: 1000`, and its `why` (`:603`) is the A-8 operator ruling retiring the entire JS fallback path (BATCH_SIZE 500 named explicitly as retired, along with the §1.6 silent swallow it carried) — one code path, one batch constant, both declared | same |
| LM-D5 | link_massing | `scripts/link-massing.js:691`, `:701` | the verdict-bound link-rate floor is the bare literal `50` in two places (comparison + rendered `'>= 50%'`), `fcd6ff68` with no recorded rationale (INTENT-UNKNOWN on the value); 0 registered `logic_variables` matching `%link_rate%` (428 rows live); live rate 99.71% ⇒ a 49.7-point dead band | OPEN · PIN | **peel 8c** — `link_massing_link_rate_fail_pct` (default 50, 0–100, `on_invalid: fail`, GROUP "Data Quality Thresholds") + `checks[].limit_from_config`; the VALUE is the operator's to move | same |
| LM-D6 | link_massing | `scripts/link-massing.js` (absent counter); `:287` | no link-quality counter: 48,561 of 103,530 `nearest` primaries (46.9%) have `footprint_area_sqm > lot_size_sqm` (33,184 exceed 2×), 13,347 of 381,605 `centroid_in_parcel` (3.5%); 58,050 parcels carry `existing_data_quality_flag = 'footprint_exceeds_lot'` one step downstream; NULL-`geom` parcels are silently dropped by `ST_Contains → NULL` (the PostGIS twin of the Spec 122 §1.6 swallow) | **CLOSED-MEASURED (WF3, 2026-08-30)** | **peel 8b** — `nearest_footprint_gt_lot_count`, a check keyed by `match_type`, reported on every run out of the same `cumulative_sql` round trip as the link rate (no extra statement; +887 ms warm / +2.9–3.6 s cold for the two joins over the 485,135 primaries). Re-measured post-tiebreak: **50,790 of 103,530 nearest primaries (49.06%)** and **13,347 of 381,605 centroid_in_parcel primaries (3.50%)** — and THE TIEBREAK ITSELF MOVED THIS (+2,144 from 48,646/47.0%): resolving a distance tie toward the LARGER footprint puts more primaries above their lot line. Declared, not incidental; the mirror-image drop in shared primaries is on LM-D11, with the denominators in the row so the numerator can be sized. What it closes is that the links were produced here, shipped, and NULLed one chain-step later by enrich-parcels with NOTHING at the link counting them. The NULL-`geom` silent drop is separately declared in `limitations[]` (live count 0). The 0/42 sanity-rule blind spot over `parcel_buildings` remains the filed `review_followups` WF3. **Amended 2026-08-28 (R-H):** peel 8b's INFO-BY-RULING (A-6) disposition conflicts with Spec 48 §4.9 (`tasks/lessons.md:117`) — a standing non-zero metric must be WARN with a declared, machine-observable retighten condition, never INFO-by-taste. Re-dispositioned OPEN, **carried to pilot 4**: `nearest_footprint_gt_lot_count` becomes a WARN check with a declared retighten trigger (defined at pilot 4), superseding the INFO row landed at peel 8b. **Re-verified 2026-08-29: NOT delivered.** `scripts/link-massing.descriptor.json:502-511` still declares `severity: "INFO"`, and the check's own `why` DEFENDS the INFO choice under ruling A-6 with no mention of a pending WARN conversion — pilot 4 (`link_wsib`) shipped without touching `link_massing`'s descriptor. The R-H promise is unmet, not merely unscheduled; filed `docs/reports/review_followups.md` (2026-08-29, HIGH — a broken cross-pilot ruling) | **CLOSED 2026-08-30 (WF3) — the 2026-08-29 "NOT delivered" finding is itself now stale.** Commit `9019c3d3` (2026-08-30, "commit 3 completion", the concurrent Validator v2 WF2's R-T addendum commit 3 — not "pilot 4") flipped `scripts/link-massing.descriptor.json:502-511` `severity: "INFO" → "WARN"` and added `retighten_when: "zero rows"`, re-captured all 4 golden `post/` files, and updated `src/tests/steps/link_massing/violations.test.ts` (`STANDING_WARN_BY_DESIGN`, `#166`). This WF3 was dispatched to build a NEW `limit_from_config` pct-threshold mechanism on top of it — **not built**: it would have downgraded today's honest, always-WARN-while-nonzero row to a threshold defaulted to PASS at 49.06%, regressing the shipped fix. Re-verified live this session: `violations.test.ts` 74/74 green, `golden-fingerprint.infra.test.ts` 27/27 green (0 stale fingerprints), `step:validate --step=link_massing` → 16/17, hard-stop=false |
| LM-D7 | link_massing | `scripts/link-massing.js:391-660` + `:51-87`, `:97-110`, `:111-134`, `:33-35`, `:45-46` | the JS fallback path — 345 lines (46.6%), 0 of 11 audited runs since `7c75e92e` (2026-04-02) took it (the only JS-path row, 1036, predates the dual path); no E1 clear (asymmetric with B-8), `BATCH_SIZE 500`, haversine, reads `geometry` (3857 JSONB) not `geom`, and the `:554-559` silent swallow Spec 122 §1.6 names by anchor | **CLOSED · 8a** | **retired at commit 7, COMPLETED at peel 8a.** Commit 7 landed the `knowingly-retired` ruling (operator, 2026-08-27, A-8 override) and the `{kind: extension, name: postgis, on_missing: fail}` precondition; the COLUMN half of the declared replacement was missing, so a database with postgis installed but a pre-039 `building_footprints` would have failed on a raw `column bf.geom does not exist` from inside the first batch query rather than refusing before the first read. Peel 8a adds `{kind: column, name: "building_footprints.geom"}` and `{kind: column, name: "parcels.geom"}`, both `on_missing: "fail"` — fence `45906c31`'s intent (a schema gap must not select an algorithm) carried forward now that there is no algorithm to select. Probe proven both directions against the live catalog 2026-08-27 (`information_schema.columns`: both present; a fabricated `building_footprints.nope` absent); I/O cost 2 catalog lookups per run, sub-millisecond. C-6 dissolves (nothing pinned because nothing survives); declaration retired commit 9 | same |
| LM-D8 | link_massing | `scripts/link-massing.js:739` | lock-91 contention → bare `return;`; the SDK skip emit carries no `audit_table`; live: 6 `skipped` rows under `permits:link_massing`, every one with `records_meta` NULL | **CLOSED · 8a** | landed at commit 7, **CONFIRMED at 8a against THIS descriptor**: `scripts/lib/step/index.js` `skipRecordsMeta(descriptor, 'advisory_lock_held_elsewhere')` emits `{skipped: true, reason, audit_table}` whose verdict comes from `deriveVerdict(rows)` over two INFO rows — never a bare `return` — and the run is finalized `RUN_STATUS.SELF_SKIPPED`, which is the status the declared `lock_held_elsewhere` terminal names. The 6 live `skipped` rows under `permits:link_massing` all carry `records_meta` NULL; every future one carries a table. The skip meta deliberately carries no `config` stamp: `hoisted_above_gate: true` means an out-of-bounds threshold has already thrown ABOVE the lock, so it cannot hide behind the green SKIPPED summary (A-5 / B-13) ⚠️ **DEMONSTRATED LIVE, by accident, 2026-08-27 19:09Z**: a peel-8b capture launched while an orphaned forced relink still held lock 91 self-skipped in 40 s and emitted exactly the declared shape — `{skipped: true, reason: "advisory_lock_held_elsewhere"}` with a 2-row `audit_table` (`status: SKIPPED`, `reason`), a row-derived PASS verdict, `records_new`/`records_updated` null, exit 0. Pre-conversion that run would have been a bare `return` with `records_meta` NULL, indistinguishable in the ledger from a run that had nothing to do. Evidence retained outside the repo (the capture was discarded because it snapshotted a mid-rebuild junction). One residue noted for follow-up: `records_total` reads 0 on that path while the other two read null. | same |
| LM-D9 | link_massing | `scripts/link-massing.js:735` | `emitMeta` declares `parcels: [… geometry]` and `building_footprints: [… geometry …]`; the live PostGIS path reads `geom` on both tables and `geometry` on neither — the declared read surface describes the retired path | **CLOSED · 8b** | landed at commit 7, **CONFIRMED at 8b** against the captured `PIPELINE_META`: `reads` is derived from `inputs.reads.tables[]` and reads `parcels: [id, centroid_lat, centroid_lng, geom]`, `building_footprints: [id, geom, centroid_lat, centroid_lng, footprint_area_sqm]` and — D-23, a second pre-existing under-declaration — `parcel_buildings: [parcel_id]`, the step's own output table, which its incremental filter and cumulative link-rate query have always read and never declared. `geometry` (the 3857 JSONB the retired path used) appears nowhere | same |
| LM-D10 | link_massing | `scripts/link-massing.js:246-256` | W1 FULL DELETE of 520,492 rows has no empty-source floor and commits in its own transaction (`:247`) before any link is written — an empty/truncated `building_footprints` (the `fc038790` incident shape) on a FULL run deletes everything, links 0, then FAILs `link_rate` after the fact | **CLOSED · 8a** | landed at commit 7, **CONFIRMED at 8a**: `empty_source_guard` is declared `when: "pre_write"`, `severity: "FAIL"`, and `runLinkPhase` scores it BEFORE `writes[0]` — which IS the retraction — returning `writeSkipped: true` with `cumulative: null` and no transaction opened when it fails unaccepted. The post-write half is `mass_retraction_ratio` (`pct <= 0.05` on retracted-and-not-rebuilt), so a run that retracts and rebuilds a fraction can no longer read PASS merely because every statement returned. Both sabotages exist in the `#165` must-fail world (`building_footprints_count = 0` under a resolved full mode; `retracted = 520,492 / inserted = 0`) | same |
| LM-D11 | link_massing | `parcel_buildings` live (66,906 buildings `is_primary` for >1 parcel; 167,443 parcels, 34.5% of linked) | one-parcel-per-primary is neither enforced (`idx_parcel_buildings_one_primary` is the other direction) nor counted — a shared primary structure cannot be the primary of 2+ parcels in the sense the cost model uses it | **CLOSED-MEASURED (WF3, 2026-08-30)** | **peel 8b** — `shared_primary_buildings`, from the same round trip (+322 ms): **65,318 buildings** are the primary structure of **166,591 parcels** (66,807 / 167,329 before the tiebreak relink — the mirror image of LM-D6's rise). `sum(n)` over the shared buildings IS the affected-parcel count precisely because `idx_parcel_buildings_one_primary` makes each parcel contribute at most one primary row. ⚠️ The plan measured **66,906 / 167,443** the same day, BEFORE two forced FULL relinks — that drift is LM-D13's own instability, which is the second reason this belongs in the audit table rather than in a report: a number that moves when nothing changed is only visible if something records it every run. The cost-model consequence stays the `review_followups` "Massing mislink inventory (P13-2, HIGH)" WF3. **Amended 2026-08-28 (R-H):** peel 8b's INFO-BY-RULING (A-6) disposition, same reasoning as LM-D6, conflicts with Spec 48 §4.9 (`tasks/lessons.md:117`). Re-dispositioned OPEN, **carried to pilot 4**: `shared_primary_buildings` becomes a WARN check with a declared retighten trigger, superseding the INFO row landed at peel 8b. **Re-verified 2026-08-29: NOT delivered**, same finding as LM-D6 — `scripts/link-massing.descriptor.json:517-526` still `severity: "INFO"`, defended by A-6, untouched by pilot 4; same `review_followups.md` filing covers both rows | **CLOSED 2026-08-30 (WF3), same citation and same corrected finding as LM-D6** — commit `9019c3d3` flipped `scripts/link-massing.descriptor.json:517-526` to `severity: "WARN"` + `retighten_when: "zero rows"` before this WF3 was dispatched to (redundantly) build it; re-verified green this session (`violations.test.ts` 74/74, `golden-fingerprint.infra.test.ts` 27/27, `step:validate` 16/17 hard-stop=false) |
| LM-D12 | link_massing | `parcel_buildings` live catalog (`relrowsecurity=true`, 0 `pg_policies`; role `postgres` `rolbypassrls=true`) | writes succeed only via RLS bypass; a non-bypass role runs W1/E1/E2 against 0 rows silently → `parcel_buildings_written 0`, verdict PASS, `completed` (LR-D7 class) | **CLOSED · 8a** | landed at commit 7, **CONFIRMED at 8a**: the `rls_bypass_or_policy` requirement names `parcel_buildings`, `write.assertWritePrivileges` MEASURES it before the first write (it is excluded from `assertRequirements`' probe table precisely so the measurement survives onto the happy path), the result is carried as `written.privilege`, and the `write_privilege` check reports it as a row on EVERY run — `bypassrls=true policies=0` on the captured runs — not only on the failing one. A check that exists solely in the failure branch is a check nobody ever sees working | same |
| LM-D13 | link_massing | `scripts/lib/compute/link-massing.js` `buildMatchSql.fallback_match_sql` (pre-conversion `scripts/link-massing.js:357-363`) | **No tiebreak on equal distance** — Postgres kept whichever equidistant row the plan emitted first: **18,252 / 103,530 nearest links (17.6%) could flip building_id between runs; 17,566 tied at distance 0** (a footprint overlapping the lot with its centroid outside it). Two forced FULL relinks hashed `a0023203` vs `0d2ea577` with all 8 invariants identical (U-1, commit 5 `7340fde4`). Primary selection (`5bb31faf`) and classification were already total-ordered. | **CLOSED-MEASURED · 8b** — post-fix recapture 2026-08-27 16:52 (27.3 min, `sources-full-forced-1.json`) and 17:14 (19.4 min, `sources-full-forced-2.json`) hash-EQUAL at `329bbcb6ccd5c32d8b8e9ffca77ff255` on 520,492 rows, all 8 invariants identical | pinned verbatim at commit 7 `9ff540ec` (the fix MOVES ROWS and a no-op differential may not), **FIXED at peel 8b**: the declared rule is `ORDER BY p.id, ST_Distance(...) ASC, bf.footprint_area_sqm DESC, bf.id ASC` — distance FIRST (a tiebreak that re-ranks is not a tiebreak), then the fence-`5bb31faf` order, so the fallback answers "which building is this parcel's" the same way the centroid pass always has: largest footprint wins, lowest `building_id` settles the rest. The rule is stated in the `match_nearest_fallback` check’s `why`, in `notes.json` (`known_bad` + a `fences[]` entry anchored on `9ff540ec`), and in the compute header. Locked BOTH DIRECTIONS twice: **F5** in `src/tests/steps/link_massing/violations.test.ts` over the generated SQL text (a dropped term, a flipped direction, a term hoisted above the distance, no ORDER BY at all — each named), and **behaviourally** in `src/tests/steps/link_massing/nearest-determinism.test.ts`, which runs the PRODUCTION statement 40 times over a randomly ordered scan of a seeded 0-distance tie and asserts ONE winner with the rule and BOTH winners without it. `#150` is rewritten to match: the OLD forced pair must still DIFFER (the pinned defect, kept as evidence — if it ever agreed, the fix would be unproven rather than unnecessary) and the two POST forced FULL relinks must hash-EQUAL. **D-22 retires with it** — a FULL-path table hash is no longer unstable-by-pin | static determinism probe 2026-08-27 + the peel-8b forced pair |
| LM-D14 | link_massing | `scripts/lib/step/staleness.js` `resolveOverrides` (library, all steps) | **`ctx.overrides.force_full` was never populated.** `override.force_full` was read in exactly ONE place — `selectMode`, which uses it to DECIDE the mode — and never reached `ctx.overrides`, which carried only the accept-anomaly flags and `force_run`. So `link_massing`’s declared `override_force_full_present` WARN observed `undefined` and reported CLEAN on every run, INCLUDING forced ones. **Measured 2026-08-27:** the forced FULL relink emitted `full_mode_reason: "force_full_env"`, retracted and rebuilt all 520,492 rows — and its `override_force_full_present` row read **PASS**. The check exists to catch a `LINK_MASSING_FORCE_FULL` left standing in production: a 21.9-minute relink on EVERY subsequent run plus a `linked_at` bump that re-scopes `enrich_parcels` from 1,395 parcels to 485,135. A version of it that cannot fire is the "green because it never looked" class. | **CLOSED · 8b** | **peel 8b** — `resolveOverrides` now returns `force_full` from `forceFullRequested(descriptor, env)`, the SAME source `selectMode` decides on, so the audit row and the decision can no longer disagree. Generic (no step-specific path, `#149`): a descriptor declaring `force_full: "none"` resolves `false` and cannot be armed by any env. Locked both directions in `src/tests/step-library.logic.test.ts` — armed/unarmed, frozen, the never-truthiness set (`"true"`, `"yes"`, `"0"`, `""`, `undefined` → false), `load_ravines` unarmable, and the decision agreeing with the row. **Found by EXECUTING, not by reading**: the row was only visible as wrong because the forced capture was inspected rather than assumed. | peel-8b forced relink capture, 2026-08-27 |
| LM-D15 | link_massing | `scripts/lib/step/config.js` `resolveConfig` (library, all steps) + `scripts/lib/config-loader.js` `loadMarketplaceConfigs` | **A declared variable with NO `logic_variables` row silently resolves to the seed default and is stamped as if operator-set.** `loadMarketplaceConfigs` clones `scripts/seeds/logic_variables.json` and overlays whatever DB rows exist, so `logicVars[name]` is populated for any name the SEED knows regardless of whether a row exists — `resolveConfig`'s `known` check (`logicVars` OR `FALLBACK_LOGIC_VARS`) can therefore never distinguish "seeded" from "seeded AND present in the operator-editable table." Pilot 3's 4 new variables (`link_massing_link_rate_fail_pct`, `link_massing_centroid_confidence`, `link_massing_nearest_confidence`, `link_massing_grid_degrees`) ran through commits 7/8a/8b this way before any row existed. **Found by EXECUTING**: `node -r dotenv/config scripts/seeds/apply-logic-variables.js` inserted 4/413 rows (`ON CONFLICT DO NOTHING`), proving the 4 rows had never been there. | **CLOSED · 8c** | **peel 8c** — `resolveConfig` issues one extra `SELECT variable_key FROM logic_variables WHERE variable_key = ANY($1)` scoped to the declared names and THROWs for any name absent from the result, naming the remedy (`apply-logic-variables.js`) and citing Spec 122 §1.2a P4. Happy-path `values`/`stamp` are byte-identical (goldens unmoved). Locked both directions in `src/tests/step-library.logic.test.ts` (a DB-row var resolves; a seed-only var throws with the remedy; a neither-registry var keeps the pre-existing "exists in NO registry" message). Fix applied locally: `logic_variables` 428 → 432 rows | found by executing peel 8c's insert against the live registry, 2026-08-27 |
| LM-D16 | link_massing (library, all steps) | `scripts/lib/step/verdict.js:210` | **`errors[]` interpolated `row.value` directly into a template literal**, so an object-valued `detail` (`checkRow`, `:171-173`, prefers `observation.detail` as the row value; 9 sites report an object-valued one across `scripts/lib/compute/*.js`) rendered as the literal string `"[object Object]"`, losing the actual value — captured live in `docs/reports/golden/link_massing/post/sources-full-forced-1.json` `summary.records_meta.errors[1]` (`"rows_changed_ratio: [object Object]"`) and rendered verbatim to operators by `src/components/FreshnessTimeline.tsx:1097-1116`. Latent for every step, not link_massing-specific. | **CLOSED · this commit** | a `renderValue` helper renders primitives via `String` unchanged and objects/arrays via `JSON.stringify` over recursively-sorted keys (stable regardless of call-site insertion order), capped at 300 chars so `errors[]` stays bounded; `errors.push` (`:210`) is the only call site and now routes through it. The `audit_table` rows themselves are unchanged — only the rendered message loses nothing now. Locked both directions in `src/tests/step-library.logic.test.ts` (object detail on WARN → stable sorted JSON; primitive detail unchanged; a large detail capped with an ellipsis), proven RED against the unfixed renderer first. | Observability seat, commit `68b8e361` output panel |
| LW-D1 | link_wsib | `scripts/link-wsib.js:503` (pre-conversion) | the `>= 5%` link-rate floor (T2) is an undeclared, verdict-bound literal — the P4 violation for this pilot, parallel to `link_massing`'s T4 | **CLOSED · commit 7** | **verified**: `scripts/link-wsib.descriptor.json:335` declares `limit_from_config: "link_wsib_link_rate_warn_pct"` (registered `:521`, bounds 0-100, `on_invalid: fail`), with a `why` (`:337`) that also records the LW-D18/LW-D19 denominator re-rulings after this pilot shipped | [pilot 4 assessment](2026-08-28-pilot4-link-wsib-assessment.md) §2 |
| LW-D2 | link_wsib | `scripts/manifest.json:39` (`link_wsib` entry, pre-conversion) | `supports_full: true` and `supports_dry_run: false` are BOTH measured FALSE (finding 5) — `--full`/argv full-mode reader does not exist; `--dry-run` is a complete separately-coded simulation branch (`:294-341`) | **CLOSED · commit 7** | **verified**: `scripts/manifest.json:39` now reads `"supports_full": false, "supports_dry_run": true`, with `--full` granted instead via the explicit `chain_args.sources: ["--full"]` — declared-diff manifest correction, no behaviour change | same, §Key Files finding 5 |
| LW-D3 | link_wsib | `scripts/manifest.json:39` `telemetry_tables: ["entities"]` (pre-conversion) | under-declares the write surface — `wsib_registry` is also written (`linked_entity_id`, `match_confidence`, `matched_at`) and is undeclared. Second independent confirmation of the SAME defect Spec 60's own Step Registry row makes at a different layer (G-16, §1) | **CLOSED · commit 7** | **verified**: `scripts/manifest.json:39` now reads `"telemetry_tables": ["entities", "wsib_registry"]`; `docs/specs/01-pipeline/60_shared_steps.md:19`'s Step Registry row corrected together (writes column: `entities, wsib_registry`) | same, §Key Files finding 5 + §1 G-16 |
| LW-D4 | link_wsib | `scripts/link-wsib.js:485` (`med_conf` stats bucket, `>= 0.50 AND < 0.90`) | the `[0.50, 0.60)` sub-range is DEAD — no code path ever writes a `match_confidence` in that band (only 0.95/0.90/0.60 are ever written); the `0.50` lower bound is misleading and will remain so post-conversion unless corrected | OPEN · INCIDENTAL (PH-6), PIN (T5) | **declared limitation** (Spec 123 §3.1 PIN, not FIX) — recorded in `limitations[]` at commit 7, value NOT changed | same, T5 |
| LW-D5 | link_wsib | `scripts/link-wsib.js` `buildTier3Ctes` (fixed by `d704a447`, 2026-04-01) + the monotone `WHERE linked_entity_id IS NULL` guard (G-8) | the article-stripping predicate fix (`d704a447`) could not retroactively repair links written under the pre-fix algorithm — monotone guard means a fixed matching bug never repairs the links it already corrupted. **Measured 2026-08-28: 8,450 / 13,965 linked rows (60.5%) fail today's predicate; 8,450/13,645 (61.9%) of tier-3 links; clean link rate 4.55%, below the 5% WARN floor.** 171 magnet entities concentrate the contamination (worst: MDK CONSTRUCTION 2,118 links). PH-6 (commit 4): 171-magnet copyContacts exposure quantified — 7/171 magnets carry any contact field, but 0 attributable to `copyContacts` (`wsib_registry` carries 0 contact values locally); cloud caveat declared | ✅ **CLOSED · commit 8/8b, live repair executed 2026-08-28** | **REPAIRED.** LG-16 (UPDATE-to-NULL, scoped `match_confidence = 0.60`) + LW-D10's convergence loop (bounded `link_wsib_tier3_full_max_iterations`, T7) ran live via `LINK_WSIB_FORCE_FULL=1` against `127.0.0.1:54322/postgres`: retracted all 13,645 tier-3 links, converged in **10 iterations** (< the 20 bound, `exhausted: false`, `tier3_full_not_converged` read PASS), relinked **8,009** tier-3 rows (+4 tier-2 exact-legal, +0 tier-1) under TODAY's predicate — verified live, `wsib_tier3_current_predicate_pass_rate_pct` **38.1% → 100.0%** (0 of 8,009 tier-3 links now fail the predicate, independently re-queried, not merely trusted from the audit row). `wsib_registry` row count held at 121,116 throughout (links retracted, never rows — the declared LG-16 contract). Net: linked total 13,965 → 8,333 (cumulative link rate 11.53% → 6.88% — a NET DROP, because the repair removes more contamination than the current predicate re-admits, exactly as A-7 predicted); fan-in max 2,118 → 439; magnets (fan-in ≥10) 171 → 129; `entities.is_wsib_registered` 938 → 786. `contacts_cleared_on_retraction` = 0 (matches PH-6's local-zero-exposure finding — no contact values existed to clear). `exit_code: 0`, `verdict: PASS`, duration 1,587.4s (~26.5 min, within the descriptor's declared 35m budget). Full before/after table: [pilot 4 assessment](2026-08-28-pilot4-link-wsib-assessment.md) §8c | same, Fold A/B; measured live 2026-08-28 (contamination found) + 2026-08-28 (repair executed) |
| LW-D6 | link_wsib | `scripts/link-wsib.js` exact-tier length floors (`>= 3`, ×4 sites) vs fuzzy-tier length floor (`>= 5`, `buildTier3Ctes`) (pre-conversion) | asymmetric numeric literals, same semantic role (a minimum comparable-string length), no recorded rationale anywhere in the 17-commit fix corpus — the reasoning (exact match tolerates short strings; fuzzy matching on short strings produces garbage similarity scores) is inferred by this pilot's PH-3 pass, not sourced from any commit | **PIN** (INCIDENTAL, Rule-4 partially owed) | **re-verified 2026-08-29: only PARTIALLY delivered.** The asymmetry rationale IS captured — `scripts/lib/compute/link-wsib.js:63` carries a code comment ("S2 — the length floor on the two EXACT-match tiers (LW-D6: exact tolerates a shorter string than fuzzy)") — but the row's own promise was `checks[].why`, a machine-readable descriptor location, not a code comment; `scripts/link-wsib.descriptor.json` has no `LW-D6`/`asymmetr`/`exact tolerates` match anywhere. Values are unchanged (structural, not tunable) so this is not a behaviour gap, only an under-delivered documentation promise — PIN rather than a fresh defect row | same, §2 (S2) |
| LW-D7 | link_wsib | `docs/reports/review_followups.md:3015` | stale chain-membership claim (finding 1) — `link_wsib` was wrongly cited as a `wsib`-chain member in an earlier R-D followup | **CLOSED · commit 1** | already corrected pre-pilot at `188d7371`/`ad0c1682`; this pilot verified-and-skipped (no edit needed) | same, §1 |
| LW-D8 | link_wsib | `scripts/link-wsib.descriptor.json:448` + `scripts/steps/_schema/step.schema.json` (`properties.database.properties.min_migration`) | `database.min_migration` was written as the DDL migration's FILENAME number (243, for `243_wsib_unlinked_partial_index.sql`) but `scripts/lib/resolve-db.js` `assertDbTarget` (operator ruling P0, `9e2da7b1`) compares against `COUNT(*)` of `schema_migrations`, never a filename — the `migrations/` sequence carries 4 historical filename gaps (43, 49, 50, 158), so COUNT never reaches 243 and the step refused to run against a fully-migrated database (242/242 applied) forever. Found by the G2′ differential (7b agent), reproduced 3× | **CLOSED · this commit** | corrected to 240 — the 1-based position of `243_wsib_unlinked_partial_index.sql` in the sorted `migrations/` directory listing (242 `.sql` files total; live `SELECT count(*) FROM schema_migrations` = 242, matching). `step.schema.json`'s `min_migration` description now states the COUNT semantics and names the footgun by name. New static lock in `src/tests/step-conformance.infra.test.ts` (describe block "database.min_migration — COUNT floor, never a filename number (LW-D8)"): every `converted.json` step AND every `pending` step must declare `min_migration <=` the live `*.sql` file count in `migrations/`, proven RED first against the pre-fix 243, and the schema description is asserted to contain "COUNT". Verified live: `node -r dotenv/config scripts/link-wsib.js --dry-run` now logs `migrations=242 (floor 240)` and completes with verdict PASS (exit 0) | same |
| LW-D9 | link_wsib | `scripts/link-wsib.descriptor.json` (LG-16 null-retract target, `write_discipline.scope`) | **THROWN LIVE, commit 8 first execution attempt (2026-08-28):** the `scope` string for the LG-16 UPDATE-to-NULL target was authored as `"match_confidence = $1 (the fuzzy tier's resolved confidence — tier 3 ONLY; tiers 1/2's exact matches are never retracted, LW-D5's contamination is 100% tier-3)"` — a human-readable parenthetical appended directly onto the predicate. `set_based_null_retract` is in `write.js`'s `SET_BASED_CLASSES`, so `buildWritePlan` renders `scope` VERBATIM into `WHERE ${whereParts.join(' AND ')}` — the parenthetical became live SQL. First real exercise of the LG-16 path (mode never resolved `full` in any commit-7/8a/8b/8c invocation) — no differential or golden capture had ever rendered this `clear_sql`, so no prior test caught it. `executeSetBasedClear` threw `syntax error at or near "("` inside `pipeline.withTransaction`; the whole transaction rolled back — `wsib_registry` row count (121,116) and linked total (13,965) both confirmed unchanged after, no partial write, no orphaned `pipeline_runs` row (chain-driven capture skips its own ledger row) | **CLOSED · commit 8 (R-L/repair-attempt commit)** | `scope` corrected to pure SQL (`"match_confidence = $1"`); the removed rationale is not lost — `write_discipline.declared_drift.text` already states the same tier-3-only bound. Verified: `write.buildWritePlan(nullRetractSpec, descriptor).clear_sql` now renders `UPDATE wsib_registry SET linked_entity_id = null, match_confidence = null, matched_at = null WHERE match_confidence = $1 AND (linked_entity_id IS DISTINCT FROM null);` — parses clean. Scanned every other descriptor's `set_based_scoped`/`set_based_unscoped`/`set_based_null_retract` `scope` field for the same authoring pattern (prose appended to a rendered predicate) — none found. `src/tests/steps/link_wsib/violations.test.ts` (71/71) + `link-wsib-ledger-gate.logic.test.ts` (18/18) + `step-conformance.infra.test.ts` (74/74) green after the fix | [pilot 4 assessment](2026-08-28-pilot4-link-wsib-assessment.md) §8, commit-8 section |
| LW-D10 | link_wsib | `scripts/lib/step/index.js:806-836` (`runCascadePhase`, the LG-16/A-7 mode-`full` block) | ✅ **CLOSED · commit 8b.** ⛔ Was BLOCKING, OPEN. Fold B (Cross-read Adversary, 2026-08-28) RULED the mode-`full` tier-3 pass must LOOP TO CONVERGENCE, bounded by a new tunable `link_wsib_tier3_full_max_iterations` (T7, default 20, WARN `tier3_full_not_converged` on exhaustion) — because `TIER3_SELECT`'s `LIMIT 1000` cap means one bulk pass can relink at most 1,000 of the ~5,195 tier-3 rows expected to survive the repair (plus ~320 tier-1/2 exact matches ⇒ ~5,515 total). Peel 8c's own commit message states "T4–T6 verified wired at 8b" and the descriptor/checks/notes all declare T7 (bounds, WARN severity, a `checks[]` entry, a `notes.json` decision) as if the runtime loop exists. **Measured, commit 8, by direct read (not by execution — the run never reached this code; LW-D9 threw first):** `scripts/lib/step/index.js:834` hardcodes `tier3Full = { retracted, contacts_cleared: contactsCleared, exhausted: false, iterations: 1 }` — a single retract-then-one-pass-per-tier block, NO loop, NO iteration counter, NO read of `link_wsib_tier3_full_max_iterations` anywhere (`grep -rn "convergence\|max_iterations\|tier3_full_max" scripts/lib/step/*.js scripts/lib/compute/link-wsib.js` → 0 matches in runtime code). A forced-FULL run today would retract all 13,645 tier-3 links, relink ≤ ~1,320 (tier1/2 exact + one capped tier-3 pass), leave ~4,000+ links permanently un-relinked with no further attempt, and the audit row would falsely report `exhausted: false` / `iterations: 1` — a converged-looking result that is not converged. **NOT executed at commit 8** — the forced-FULL repair was halted before this code path was reached (LW-D9 threw first); had LW-D9 not thrown, this gap would have produced a silently-incomplete repair under a green audit row | **CLOSED · commit 8b** | RULED and built exactly as specified: `runCascadePhase`'s mode-`full` tier loop now calls a new, extracted, exported `runTierToConvergence(runOnePass, loops, maxIterations)` per tier — `loops` fires ONLY for a tier declaring `max_iterations_from_config` (a generic, per-tier, DECLARED signal; no `'tier3_fuzzy'` string anywhere in the library, preserving Gate 0), repeating while `matched > 0 && iterations < maxIterations` (bound read as `config[tier.max_iterations_from_config]`, never a literal), `exhausted` true only when the bound stops a pass that itself still matched. New INFO check `tier3_full_iterations` `{iterations, relinked_total}` added alongside the WARN `tier3_full_not_converged`. Required a small schema/naming refactor to make "consumed" genuinely runtime-detectable: `tiers[].confidence_config` → `confidence_from_config` (T3/T4/T5) + new `max_iterations_from_config` (T7), both `*_from_config`-suffixed so the existing generic `fromConfigRefs` conformance scan picks them up with zero new detection code — additive to `step.schema.json`, `docs/reports/generated/122-vocabulary.md` regenerated. A SECOND, independent conformance gap found and fixed in the process: the P4 battery's compute-consumed regex matched only `ctx\.config\.<name>`, but link-wsib's compute receives `config` as a bare positional parameter — T1/T3/T4/T5 would ALL have false-positived as dead declarations the instant `link_wsib.js` joined `converted.json`; widened (guarded by `stripComments` after an unguarded first pass immediately false-positived on `assert-schema.js`'s own prose). Locks: `describe('LW-D10 — a declared tunable consumed ONLY via a library *_from_config field...')` (`step-conformance.infra.test.ts`, 4 tests, RED proven against the ACTUAL pre-fix commit `344e9452` via `git show`, not a synthesized fixture) + `describe('LW-D10 — runTierToConvergence: ...')` (`step-library.logic.test.ts`, 5 tests, fixture-pass-driven, the ruling's own worked example 1000/1000/320/0 → 4 iterations/2,320 relinked/not-exhausted). Lesson filed (`tasks/lessons.md`). LOW followup filed for the SET_BASED-scope-is-prose class (LW-D9's root cause), not built this commit | [pilot 4 assessment](2026-08-28-pilot4-link-wsib-assessment.md) §8, commit-8 section; `.cursor/active_task.md` Fold B item 2 (A-7 convergence AMENDED); `docs/reports/review_followups.md` "commit 8 harvest" → RULED and BUILT |
| LW-D11 | link_wsib | `scripts/lib/compute/link-wsib.js` (`entity_fanin_warn`, T6) | **CLOSED · this commit.** `entity_fanin_warn` read a top-level `ctx.fanin` that `scripts/lib/step/index.js`'s `stepCtx` literal NEVER assigns (proven: the object literal that builds `stepCtx` has no `fanin` key, at any archetype) — the check always evaluated `{}`/0 against every real run. Measured live: `docs/reports/golden/link_wsib/post-8-forced/sources-full-forced-2.json`'s `entity_fanin_warn` reported `{"entity_fanin_max":0}` while the SAME capture's invariant `wsib_entity_fanin_max` measured 439. `src/tests/steps/link_wsib/violations.test.ts`'s `runCompute` ctx-builder injected `ctx.fanin` directly (a key no other step's harness used), so the unit suite (71/71) stayed green off a fixture the runtime could never produce — the descriptor's own prose ("Fires immediately on the known-bad population") was false for every real run since commit 7 | **CLOSED · this commit** | Fix per Spec 124 §7 rung (e) — no library change needed: `runCascadePhase` (`scripts/lib/step/index.js`) ALREADY runs this step's own declared `CUMULATIVE_SQL` post-write and merges every non-`linked`/`total` column of its one row generically onto `ctx.matched` (the SAME declared-post-write-observation mechanism `link_rate_warn`/link_massing's `runLinkPhase` use) — `CUMULATIVE_SQL` already selects `entity_fanin_max`/`magnet_entities_fanin_ge_10`, so `entity_fanin_warn` now reads `ctx.matched.entity_fanin_max`/`ctx.matched.magnet_entities_fanin_ge_10` instead of the bogus `ctx.fanin`. **Harness-fidelity lock (generic, LW-D11 class):** `STEP_CTX_KEYS` (new, exported, `scripts/lib/step/index.js`) is the closed list of keys the library ever assigns onto `stepCtx`; `src/tests/step-conformance.infra.test.ts`'s new `describe('LW-D11 — harness-fidelity lock...')` statically AST-parses (TypeScript compiler API, not a lexer — the fixtures' prose breaks brace/paren-counting) every `src/tests/steps/*/violations.test.ts` ctx-builder and asserts every top-level key it sets is ⊆ `STEP_CTX_KEYS`; proven RED first against the pre-fix `link_wsib` harness (named `fanin`, restored via `git stash`/`git stash pop` for the proof, not committed red) and against a synthetic bad fixture, then GREEN. `World`/`healthyWorld`/`SABOTAGE_BY_VAR[T6]` in `link_wsib`'s own `violations.test.ts` moved `fanin`'s fields onto `matched.entity_fanin_max`/`matched.magnet_entities_fanin_ge_10`, mirroring the real runtime shape. **Live re-verification (R-C re-capture, required — editing a converted step's compute changes its `source_fingerprint`):** `permits`/`sources`/`standalone` re-captured against `127.0.0.1:54322/postgres` (242 migrations) — all 3 gated-skip (`no_upstream_changes`, nothing changed in `wsib_registry`/`entities` since the last run) so their own audit rows don't exercise post checks; a fourth, uncommitted verification capture (`--dry-run`, bypasses the SKIP gate only, mode resolves `incremental`, 0 rows changed) DID exercise `entity_fanin_warn` live: `{"metric":"entity_fanin_warn","value":{"entity_fanin_max":439,"magnets_fanin_ge_10":129},"threshold":"viol <= 20","status":"WARN"}`, with `errors:["entity_fanin_warn: {\"entity_fanin_max\":439,\"magnets_fanin_ge_10\":129}"]` rendered correctly via LM-D16's `renderValue` (not `[object Object]`) — matches the live `wsib_entity_fanin_max` invariant (439) exactly, confirming the descriptor's "fires immediately" text is now TRUE. Deleted `docs/reports/golden/link_wsib/post/sources-full-forced-2.json` (byte-identical duplicate of `post-8-forced/sources-full-forced-2.json`, stale fingerprint, not a declared invocation — R-C: "a stale extra scenario capture fails the gate and must be re-run or deleted"; git keeps history). `step-conformance.infra.test.ts` (101/101) + `golden-fingerprint.infra.test.ts` (17/17) + `link_wsib/violations.test.ts` (71/71) + `link_massing`/`assert_schema`/`load_ravines` violations suites (all green) after the fix. Lesson filed (`tasks/lessons.md`) | this report; live capture 2026-08-28 |
| LW-D12 | link_wsib (library, `scripts/lib/pipeline.js#emitSummary`) | `scripts/lib/pipeline.js:321-327` | **CLOSED · this commit.** `emitSummary` coerced `records_total: stats.records_total ?? 0` (both `null` AND `undefined` → 0) while `records_new`/`records_updated` used `!== undefined ? … : 0` (preserves an explicit `null`) — the SAME run's stdout `PIPELINE_SUMMARY` and its `pipeline_runs` row (written by `scripts/lib/step/ledger.js#finalizeLedgerRow`, which deliberately carries NO COALESCE on these three columns) disagreed for a gated-skip's declared-null counters. Measured pre-fix: `docs/reports/golden/link_wsib/post/standalone.json` — `summary.records_total: 0` (number) vs the SAME capture's `pipeline_runs[0].records_total: null`, while `records_new`/`records_updated` read `null` in BOTH places. Blast radius: every converted step's gated-skip path (`scripts/lib/step/index.js` is the only caller that ever passes an explicit `records_total: null` into `emitSummary`, at two sites: the spread of `counters` on a gated/self skip, and the hard-coded null-everything branch) | **CLOSED · this commit** | `records_total` made symmetric with `records_new`/`records_updated` (`!== undefined ? … : 0`). Both-directions red-first lock in `src/tests/pipeline-sdk.logic.test.ts` ("LW-D12" tests: explicit `null` in → `null` out; missing/`undefined` in → `0` out), proven RED against the pre-fix line via `git stash`/`git stash pop` (not committed red). The PRE-EXISTING pinned test at `src/tests/step-library.logic.test.ts` ("the counters read 'not counted', not 'counted zero'") asserted the bug (`records_total` → 0) under a citation of "plan C-11" — corrected: C-11 (`docs/reports/2026-08-25-pilot2-load-ravines-assessment.md:89`) is `run-chain.js`'s OWN DB-write normalisation for the PRE-CONVERSION `load_ravines` script, a different layer that does not license `pipeline.js#emitSummary` doing the same thing asymmetrically to one of three symmetric fields; the test now asserts `records_total` reads `null`, matching `records_new`/`records_updated`. Re-captured `permits`/`sources`/`standalone` (`docs/reports/golden/link_wsib/post/*.json`) against `127.0.0.1:54322/postgres` — `source_fingerprint` unaffected (`pipeline.js` is not one of the 4 fingerprint inputs: step/descriptor/notes/compute), but the captured JSON now shows the true `records_total: null` on every gated-skip run instead of the stale `0`. Consumers checked: `scripts/run-chain.js:661` already read `summary.records_total ?? null` (already null-symmetric, unaffected) and its two `UPDATE … COALESCE($3, records_total)` writes (`:713`,`:733`) already tolerate null by design (preserves the prior row value); grepped `scripts/` + `src/` for `records_total` arithmetic (`+`/reduce/sum) — none found; `src/components/FreshnessTimeline.tsx` already null-guards every render site (`!= null` checks); `src/app/api/admin/pipelines/runs/route.ts`'s `PipelineRunRow.records_total: number` (the one admin route NOT already typed nullable, unlike its 3 siblings) corrected to `number \| null` in the same commit — a pre-existing type-accuracy gap (the DB column was already nullable for many other steps), not new breakage, but adjacent and cheap to fix. `pipeline-sdk.logic.test.ts` (323/323) + `step-library.logic.test.ts` (101/101) green after the fix | this report; live capture 2026-08-28 |
| LW-D13 | link_wsib (library, `scripts/lib/step/index.js`) | pilot 3 output panel §R Reflection (recurring/standard-shaping) — "Ledger-row ownership undeclared" | **DONE (SHOULD-FIX, deferred twice — pilot 3 → pilot 4 plan → this commit) · CLOSED this commit.** `ownsLedgerRow(chainId)` was already computed (`const owns = ownsLedgerRow(chainId)`, `scripts/lib/step/index.js`) and used to gate `openLedgerRow`/`finalizeLedgerRow`, but the fact itself was never made observable in the run's own `records_meta` — an operator reading one run's record could not tell, without cross-referencing `chainId`/log lines, whether THIS process owned the `pipeline_runs` row it just wrote (§1.2a "nothing hidden") | **CLOSED · this commit** | `records_meta.ledger_row` ∈ closed enum `LEDGER_ROW_VALUES = ['owned', 'chain_owned']` (new, exported, `scripts/lib/step/index.js`), stamped unconditionally at BOTH emit sites — the happy-path terminal block and the self_skipped/advisory-lock-contention path (ownership of the CONTEXT is well-defined even when no ledger row was ever opened). Treated as a RUNNER DEFAULT (like `terminal`/`checks_passed`/`config`), not a per-step `emits[]` declaration: `step.schema.json`'s `emits` category is explicitly "records_meta keys BEYOND runner defaults", and this key carries no step-specific data — it applies identically to every converted step, same footing as the three precedent keys. Both-directions red-first lock in `src/tests/step-library.logic.test.ts` (3 "LW-D13" tests: `chain_owned` in-chain, `owned` standalone, both values reachable on the self_skipped path too), proven RED against the pre-fix `index.js` via `git stash`/`git stash pop` (never committed red). Live-verified: `link_wsib` standalone capture → `records_meta.ledger_row: "owned"` (both the stdout summary AND the persisted `pipeline_runs[0].records_meta.ledger_row`); `permits`/`sources` in-chain captures → `"chain_owned"` (`docs/reports/golden/link_wsib/post/*.json`, re-captured). Marked DONE in `docs/reports/2026-08-27-pilot3-link-massing-assessment.md`'s §R recurring table and `.cursor/active_task.md`'s R-F carried-steps list (item 2) — the "DEFERRED-TO pilot 4" text retired. `step-library.logic.test.ts` (104/104) + `step-conformance.infra.test.ts` (94/94) + all 4 `violations.test.ts` suites (288/288) green after the fix | pilot 3 assessment §R; `.cursor/active_task.md` R-F item 2; live capture 2026-08-28 |
| LW-D14 | link_wsib (compute, `scripts/lib/compute/link-wsib.js`) | `scripts/lib/compute/link-wsib.js:88` (`tokenOverlapClause`) + `:250,264,299,313` (`buildFuzzyMatchSql`/`buildFuzzyMatchCountSql` call sites) | **CLOSED · this commit. HIGH.** Tier 3's `similarity() > threshold` + article-stripped first-letter blocking predicate alone let the fan-in/magnet-entity contamination through — measured live (pre-fix, on the corpus the A-7 repair had already produced): only 10.49% (840 of 8,009) of Tier-3 links shared ANY non-generic token between the WSIB name and the matched entity name; the rest matched purely on a shared generic word (CONTRACTING/CONSTRUCTION/…) or nothing at all. This is the fan-in contamination's root cause named at commit 9 (`entity_fanin_max` 439, 129 magnets) — trigram similarity alone cannot tell "CHRISTOPHER MORAND" from "CHRISTOPHER MONIZ" apart from "CONDUCTIVE ELECTRICAL CONTRACTING" from "RESIDENTIAL SOLUTIONS" | **CLOSED · this commit** | Spec 124 §7 rung (e), compute change (a genuine new match-quality predicate, not expressible as a config value alone). `buildFuzzyMatchSql`/`buildFuzzyMatchCountSql` (`scripts/lib/compute/link-wsib.js`) now additionally require `tokenOverlapClause(stopwords, …)` inside each of the two independent tier-3 CTEs (`trade_matches` checks `w.trade_name_normalized` vs `e.name_normalized`; `legal_matches` checks `w.legal_name_normalized` vs `e.name_normalized`) — SQL `regexp_split_to_array` + array `&&` overlap after stripping a declared closed stopword list (`checks[].expect.stopwords`, NOT a compute literal — Rule 1) and purely-numeric tokens. New declared check `tier3_token_overlap` (FAIL below new tunable `link_wsib_tier3_token_overlap_fail_pct`, T8, default 50, seeded + GROUPed + `apply-logic-variables.js` re-run) and new invariant `wsib_tier3_token_overlap_pass_pct` (`docs/reports/golden/link_wsib/invariants.json`). `CUMULATIVE_SQL` widened from a bare string constant to `buildCumulativeSql(descriptor)` (Gate 0: link_wsib is still the only cascade compute `runCascadePhase` calls this for, so the widened contract costs no per-step runner branch). Spec 60 (`docs/specs/01-pipeline/60_shared_steps.md`, "### Link WSIB") amended — NOT Spec 46, which documents `enrich-wsib.js`'s Serper contact flow only, a citation correction made before writing (CLAUDE.md PD #10). **Self-found-and-fixed diagnostic bug (same commit, before any capture was trusted):** the first draft of both `buildCumulativeSql`'s and `invariants.json`'s `tier3_token_overlap_pass_pct` column tokenized ONE field — `COALESCE(NULLIF(w.trade_name_normalized, ''), w.legal_name_normalized)` — against the entity name. Measured 86.2% against the live post-repair corpus, not the expected ~100%. Root cause: `buildFuzzyMatchSql` is a UNION of two INDEPENDENT CTEs; a row written via the legal-name branch can carry a non-empty `trade_name_normalized` that never overlapped anything (it was never the field the write predicate checked), and `COALESCE` always prefers a non-empty trade name over even looking at legal name. Fixed to `OR` the two fields' overlap independently (`COALESCE(overlap(trade,…), false) OR COALESCE(overlap(legal,…), false)`) — mirroring the ALREADY-CORRECT shape of the pre-existing sibling invariant `wsib_tier3_current_predicate_pass_rate_pct`, which this draft should have matched from the start. Verified live via the real (not reproduced) `buildCumulativeSql` output: 100.00%, 0 rows disagree. **Live-fired for real**, twice: `LINK_WSIB_FORCE_FULL=1` run 1 (255.0s, well under the descriptor's `35m` budget and far shorter than the pre-WF3-F A-7 repair's 26.5 min — the token-overlap predicate sharply shrinks the tier-3 candidate pool, the expected reason, confirmed via `pg_stat_activity` mid-run showing the new `trade_matches` CTE genuinely executing) retracted the 8,009 contaminated tier-3 links and relinked 993 clean ones (2 iterations, not exhausted); run 2 (227.5s), re-run AFTER the invariant-formula fix to get a correct capture, retracted the SAME 993 and relinked the SAME 993 (2 iterations) — an idempotency proof (repeat FULL against an unchanged, already-repaired corpus converges to the identical population) as a side effect. Before/after (live, `127.0.0.1:54322/postgres`): `wsib_registry` total rows 121,116 → 121,116 (unchanged, LG-16 contract held); cumulative linked (all tiers) 8,333 → 1,317; tier-3 (0.60) linked 8,009 → 993; `entities.is_wsib_registered` 786 → 587; `entity_fanin_max` 439 → 69; magnets (fan-in ≥ 10) 129 → 20; `tier3_token_overlap_pass_pct` 10.49% → 100.00%. R-M/LW-D17's before-image mechanism produced link_wsib's own FIRST genuine before-image file this run: `docs/reports/golden/link_wsib/before-image/2026-08-29T01-47-22.902Z-wsib_registry.jsonl`, 993 lines (`wc -l`), byte-count-verified equal to the retraction's own reported 993. Re-captured `permits`/`sources`/`standalone` + the forced-full run itself as a 4th file (`docs/reports/golden/link_wsib/post/*.json`, `sources-full-forced-3.json`) — all four hash-identical to each other (`entities:37d345db`, `wsib_registry:44cf7e1d`). `violations.test.ts` (71/71) + `golden-fingerprint.infra.test.ts` (17/17) + `step-library.logic.test.ts` (117/117) + `step-conformance.infra.test.ts` green after the fix | [pilot 4 assessment](2026-08-28-pilot4-link-wsib-assessment.md) §10; this report; live capture 2026-08-28/29 |
| LW-D18 | link_wsib (compute, `scripts/lib/compute/link-wsib.js`) | `scripts/lib/compute/link-wsib.js:87-91` (`tokenOverlapClause`) + `scripts/link-wsib.descriptor.json` (`expect.stopwords`) + `scripts/lib/compute/link-wsib.js` (`link_rate_warn`) | **CLOSED · this commit. HIGH.** A 60-row precision sample of the LW-D14-fixed tier-3 population (`S/lwd14_precision_recall.md`) measured only 15.0%–25.0% genuine precision. 80% of the confirmed-DIFFERENT failures shared one of 15 industry-generic words (GENERAL, RENOVATION(S), MANAGEMENT, DESIGN, BUILD, CUSTOM, HOME, IMPROVEMENT(S), BUILDING, ASSOCIATES, TOP, ALL, QUALITY) the declared stopword list did not cover, and ~16 genuine matches were dropped by the tokenizer splitting on whitespace only (a punctuation-only variant like "T.T.S." vs "TTS" never overlapped) | **CLOSED · this commit** | Spec 124 §7 rung (e), same declared mechanism LW-D14 established, widened. (1) `link-wsib.descriptor.json`'s `tier3_token_overlap` `expect.stopwords` widened 16→31 words. (2) `tokenOverlapClause` now strips `-`/`.`/`'`/`&`/`+` (`regexp_replace(expr, '[-.''&+]', '', 'g')`) before tokenizing — verified live: WSIB #120167 "R.L. URBAN INNOVATIONS" ↔ entity "RL URBAN INNOVATIONS" matched. (3) New `src/tests/db/link-wsib-token-overlap.db.test.ts` (26 tests): one negative-control fixture PER new word class, each proving BOTH that the current list rejects the false match AND that the frozen pre-LW-D18 list would have falsely accepted it (both directions locked permanently) — plus a T.T.S. positive control and a direct `regexp_replace` probe. (4) T2 (`link_rate_warn`) re-ruled: denominator moved from wsib_registry ROWS to ENTITIES (`entities.is_wsib_registered=true` / total entities) via new `buildCumulativeSql` column `entities_with_link_count` — a single magnet's hundreds of rows no longer inflate the ratio; default kept at 5. (5) Spec 60 amended again (not Spec 46 — same citation correction as LW-D14, re-verified). **Self-found diagnostic bug, same pattern as LW-D14's COALESCE bug:** `invariants.json`'s `wsib_tier3_token_overlap_pass_pct` SQL is a hand-duplicated copy of `tokenOverlapClause` (not descriptor-generated) — it still had the pre-widening 16-word list and no punctuation strip, so the first capture measured 97.99% where the real predicate (independently re-verified: 0/548 rows disagree) guaranteed 100%. Regenerated the invariant's SQL PROGRAMMATICALLY from the real `tokenOverlapClause`/`tokenOverlapStopwords` functions (never hand-retyped) and re-captured — confirmed 100.00%. **Live-fired for real, twice (idempotent):** run 1 (313.1s) retracted 993 LW-D14-era tier-3 links, relinked 548 (2 iterations, not exhausted) — captured under the stale invariant, kept as a diagnostic record (`sources-full-forced-1-stale-invariant.json`); run 2 (304.6s), after the invariant fix, retracted the SAME 548 and relinked the SAME 548 — a second independent idempotency proof. `wsib_registry` (121,116) and `entities` (3,948) row counts unchanged both times. Before/after: tier-3 linked 993→548; cumulative linked 1,317→871; `entities.is_wsib_registered` 587→551; `entity_fanin_max` 69→16; magnets (≥10) 20→7; `tier3_token_overlap_pass_pct` 53.88%→100.00%; `entity_fanin_warn` PASSED live for the first time ever (16 ≤ 20). Own before-image: `docs/reports/golden/link_wsib/before-image/2026-08-29T02-50-29.417Z-wsib_registry.jsonl`, 548 lines, byte-verified against the retraction count. Re-captured all 4 `post/` invocations, fingerprint-identical. **Fresh 60-row precision sample (seed `20260828002`, same methodology, tier-3 population 548):** SAME 19 (31.7%), UNSURE 9 (15.0%), DIFFERENT 32 (53.3%) — up to 46.7% counting UNSURE, roughly double the pre-fix rate (full breakdown, pilot-4 assessment §8d). Residual gaps named and filed (MED, `review_followups.md`, not fixed here — out of the coordinator's named 15-word/punctuation scope): the filler word "AND" (4 rows, entity 770), more un-stopworded generic industry words (RESTORATION, PROPERTIES, STRUCTURES, MECHANICAL, ENGINEERING, DRYWALL, CARPENTRY, FIRE, PROTECTION, CONSTRUCTORS, WESTERN — 15 rows), a DEVELOPMENT singular/plural stopword gap (2 rows, the evidence file's own item 2), and a single-letter-initial token collision from the punctuation strip (3 rows, entity 523 "D & D" → bare "D" token — pre-existing since LW-D14, not newly introduced). `violations.test.ts` (71/71) + `golden-fingerprint.infra.test.ts` (17/17) + `step-library.logic.test.ts` (117/117) + `step-conformance.infra.test.ts` (103/103) + the new `.db.test.ts` (26/26) green after the fix | [pilot 4 assessment](2026-08-28-pilot4-link-wsib-assessment.md) §8d; this report; live capture 2026-08-29 |
| LW-D15 | link_wsib + link_massing (library, `scripts/lib/step/index.js` + `staleness.js`) | `scripts/lib/step/staleness.js:305-310` (`dryRunArgPresent`) + `scripts/lib/step/index.js` (`runCascadePhase`/`runLinkPhase`) | **CLOSED · this commit. CRITICAL.** `--dry-run` no longer suppressed writes for ANY converted LINK/MATCHER step. `dryRunArgPresent` was read ONLY to compute `bypassed` (skip the ledger gate) — `resolveOverrides` had no `dry_run` key, so `ctx.overrides.dry_run` was `undefined`; `runCascadePhase`/`runLinkPhase` then ran their write executors (`write.executeSetBasedJoinUpdate`/`executeSetBasedClear`/`executeRetraction`/`executeOrderedWrites`) UNCONDITIONALLY. A `--dry-run` invocation bypassed the ledger gate (correct) and then wrote to `wsib_registry`/`entities`/`parcel_buildings` for real (WRONG) — the opposite of the flag's contract, and the opposite of the pre-conversion `link-wsib.js`'s own behaviour (`5de41cc1`: dry-run simulates via read-only COUNT queries; `2633c1cb` A2: dry-run bypasses the gate so the simulation runs). Fence found by reading both commits, not inferred from the flag's name | **CLOSED · this commit** | Spec 124 §7 rung (d), library fix, every LINK/MATCHER step benefits. `staleness.resolveOverrides` gains `dry_run` (reads the existing `dryRunArgPresent`, no new reader) — `ctx.overrides.dry_run` now exposed on every phase. `runLinkPhase`: the W1 retraction and `executeOrderedWrites` are both skipped when `dry_run`; the match/classify SELECTs still run, so the "would-write" counts are real (`matched.*`), `written` stays genuinely zero (nothing was written). `runCascadePhase`: each tier's write (`wsib_registry` join-update, `entities` flag-flip, `entities` contacts copy) is replaced by a read-only COUNT-query mirror of the EXACT SAME predicate (`buildTierSql`'s new `*_count_sql` siblings, `scripts/lib/compute/link-wsib.js` — SQL text is compute's declared domain, ruling A-2 option 2, matching `5de41cc1`'s own hand-duplicated-COUNT precedent rather than execute-then-ROLLBACK, which would still ISSUE the write statement text); the LG-16 mode=full retraction is skipped too (`priorContacts.rows.length`, already read, is the exact "would retract" count); a full-mode dry-run runs the LW-D10 convergence loop exactly ONE simulated pass (declared limitation — convergence requires rows to actually leave the unmatched scope between passes, which a dry-run structurally cannot do). `records_meta.dry_run: true` stamped + a new INFO audit row `dry_run_no_writes` (`dryRunRow()`, runner-injected like `retired_var_row_present`) whenever `ctx.overrides.dry_run` is true. Two `deviations[]`/`limitations[]` entries added to `link-wsib.descriptor.json` documenting the simulation mechanism and the full-mode single-pass caveat. Both-directions red-first lock in `src/tests/step-library.logic.test.ts` ("LW-D15" describe block): a `--dry-run` run of `runLinkPhase` (link_massing, cloned + `override.dry_run` armed — the real descriptor declares `"none"`) AND of `runCascadePhase` (link_wsib) each issues ZERO statements matching `/^(UPDATE\|INSERT\|DELETE)/i` while the match/count SELECTs still run and report real would-be counts; proven RED against the pre-fix `staleness.js`+`index.js` via `git stash`/`git stash pop` (never committed red — failed on `ctx.overrides.dry_run` being `undefined`, before even reaching the write-statement assertion). `src/tests/load-ravines.logic.test.ts`'s pinned `resolveOverrides` shape test updated (now 5 keys, `dry_run: false` for `load_ravines`, which declares `override.dry_run: "none"`). Live-verified: a `--dry-run` standalone run against `127.0.0.1:54322/postgres` completed with `records_meta.dry_run: true`, the `dry_run_no_writes` INFO row present, and `wsib_registry`/`entities` row counts and content hashes UNCHANGED before/after (table-state hash comparison, golden-capture harness). `step-library.logic.test.ts` (110/110) + `step-conformance.infra.test.ts`/`golden-fingerprint.infra.test.ts`/all 4 `violations.test.ts` suites green after the fix. `runIngestPhase` (INGESTOR archetype) is untouched — no currently-converted INGESTOR step declares an active `override.dry_run` (all three are `"none"`), so its own dry-run gap, if any, is inert today and out of this WF3's scope; filed as a followup, not silently fixed alongside | this report; live capture 2026-08-28 |
| LW-D16 | link_wsib (test infra, `src/tests/db/ledger-gate-callers.db.test.ts`) | `src/tests/db/ledger-gate-callers.db.test.ts:5-10,266` | **CLOSED · this commit. HIGH.** A FALSE CLAIM in a committed test file: the header comment and a D#4 inline comment stated link_wsib's run-ledger-gate coverage was "RE-HOMED, not deleted (A-5, C1 pilot 4 commit 7, 2026-08-28)" to `src/tests/steps/link_wsib/ledger-gate.db.test.ts` — that file was NEVER CREATED (`git log --all -- src/tests/steps/link_wsib/ledger-gate.db.test.ts` returns nothing). Root cause, found the same day: `docs/reports/review_followups.md`'s pre-existing MED item — every converted step declares `database.assert_current_database: "postgres"` and `assertDbTarget` REFUSES any other name, but `setup-testcontainer.ts` always provisions `POSTGRES_DB: 'buildo_test'`, so a live-DB test calling a converted step's real `.run()` refuses immediately. The re-home was never buildable; the comment was left as an aspirational TODO rather than corrected when that was discovered — a genuine coverage gap masquerading as "moved elsewhere." Net effect: `staleness.ledgerGatedSkip` (LG-15, the actual gate `runCascadePhase` calls for link_wsib) had NO direct behavioral test anywhere, live-DB or otherwise — only indirect exercise via `violations.test.ts`'s fixture matrix | **CLOSED · this commit** | Corrected both false-claim sites in `ledger-gate-callers.db.test.ts` to state plainly: no such file exists, the MED testcontainer-database-name item is the reason, and it stays a named followup (not fixed here — test infrastructure, not a step conversion). Added 5 fake-pool (no live DB) behavioral locks for `ledgerGatedSkip` in `src/tests/step-library.logic.test.ts`'s new "LW-D16" describe block: (1) `bypassed:true` never SKIPs even against a baseline that would otherwise SKIP, and issues ZERO queries; (2) a CHANGED `config_version` signal forces `skip:false` even when the raw ledger gate alone reads `skip:true`; (3) no baseline (`ownLastRecordsMeta:null`) reads its config signal as fail-safe `changed:true`, never "nothing to compare so unchanged"; (4) `gate.ownLastRecordsMeta` (a prior SKIP's `consecutive_skips` + a caller-shaped audit row) passes through `ledgerGatedSkip` completely unmodified — a downstream consumer (e.g. `cascade.prior`) sees exactly what the DB held; (5) a hoisted, out-of-bounds `wsib_fuzzy_match_threshold` throws before the ledger gate is ever queried, using the REAL `link-wsib.descriptor.json` + `pipeline.step().run()`, not a synthetic descriptor. Each of the 5 proven RED by temporarily sabotaging the corresponding line in `scripts/lib/step/staleness.js`/`config.js` (never committed red; reverted via re-edit after each, confirmed `git diff` clean before restaging) — (1) neutered the `bypassed` early-return, (2) dropped `!configVersionChanged` from the returned `skip`, (3) hardcoded the per-signal `changed` to `false`, (4) stripped `ownLastRecordsMeta` from the returned `gate`, (5) neutered `config.js`'s `on_invalid:"fail"` throw. `step-library.logic.test.ts` (117/117) + `ledger-gate-callers.db.test.ts` (skips cleanly without `BUILDO_TEST_DB`, unaffected) green after the fix | this report; `docs/reports/review_followups.md` MED item (assert_current_database vs buildo_test) |
| LW-D17 | link_wsib + link_massing (library, `scripts/lib/step/write.js` + `index.js`) | pilot 3 output panel §R Reflection — "Interrupted full retraction leaves a partial table" (R-B's own sibling gap: R-B declares the POSTURE, nothing wrote the EVIDENCE) | **DONE (operator ruling R-M, 2026-08-28) · CLOSED this commit. HIGH.** A destructive retraction (LG-16 UPDATE-to-NULL, or a `retract:"all"` DELETE) had no row-level record of what it was about to remove — if the retraction's own predicate were ever wrong, or an operator needed to know exactly what a repair erased, there was nothing to look at. R-B's `recovery.interrupted` declares the CRASH-RECOVERY posture; nothing declared or produced the AUDIT TRAIL a human would need to verify a destructive repair after the fact | **CLOSED · this commit** | New schema field `recovery.before_image` (`step.schema.json`, mirrors `interrupted`'s exact shape: `"generated"` or `"none"`+`before_image_why`, required on every object-form `recovery`). New library primitives in `scripts/lib/step/write.js`: `buildBeforeImageSelectSql(plan)` mirrors a write plan's OWN `scope`/`keys`/`step_columns` as a read-only `SELECT` (returns `null` when `plan.scope` is falsy — a scope-less retraction, e.g. `retract:"departed"`, is a declared limitation, not a crash); `writeBeforeImage(client, plan, scopeParams, slug, runAt)` runs that SELECT and writes the rows to `docs/reports/golden/<slug>/before-image/<RUN_AT ISO, colon-sanitized>-<table>.jsonl`, unwrapped by any try/catch (a write failure — disk full, permissions — must abort the run BEFORE the retraction, fail loud, never a silent skip). Wired into BOTH retraction sites, on the SAME transaction client, strictly before the retraction call: `runLinkPhase`'s W1 loop (link_massing's `retract:"all"` clear) and `runCascadePhase`'s LG-16 block (link_wsib's `set_based_null_retract`). A new INFO audit row `before_image_written:<table>` (`{path, rows}`) surfaces per target actually written. Declared `before_image: "generated"` in `link-wsib.descriptor.json` + `link-massing.descriptor.json` (both scope-bearing); `before_image: "none"` + a full `before_image_why` in `load-ravines.descriptor.json` (its only retraction is `retract:"departed"` with `write_discipline.scope:"none"` — keyed by a surviving-ids array, not a WHERE predicate, so the generic mechanism structurally cannot mirror it today; a genuinely separate follow-up, not silently fixed or silently ignored). Conformance lock mirroring R-B's exact shape in `step-conformance.infra.test.ts` ("R-M / LG-17" describe block, narrower trigger than R-B's — only a retraction the mechanism CAN cover, i.e. `write_discipline.scope !== "none"`, is required to declare "generated"), with the same built-in RED canaries R-B uses (a synthetic "none"/undefined value reddens against a scope-bearing target). Direct behavioral lock in `src/tests/step-library.logic.test.ts` ("LW-D17" describe block, fake pool for the DB read + a REAL scratch subtree under `docs/reports/golden/_wf3g_test_before_image/` deleted in `afterEach` every time, standing in for a tmp dir since the mechanism's write location is fixed by the schema, not injectable): the mirror SQL matches the plan's own scope/keys/columns; `null` scope returns `{written:false}` with ZERO query and ZERO directory created; a matched-but-empty scope still writes an (empty) JSONL file, never a missing one; proven RED by temporarily stubbing the DB read to return nothing without touching the assertions (never committed red, `git diff` confirmed clean before restaging). golden captures re-taken for all three descriptors whose `recovery` object changed shape (link_wsib, link_massing, load_ravines — the schema amendment changes their fingerprint even though no compute/step file moved): `docs/reports/golden/{link_wsib,link_massing,load_ravines}/post/*.json`, INCLUDING a genuine forced-FULL re-run of `link_massing` (`LINK_MASSING_FORCE_FULL=1`, `sources-full-forced-1.json`, ~load-bearing evidence pinned to `LM_D13_FIXED_HASH` in `violations.test.ts` — re-captured for real, not deleted, after an initial mis-step deleted it as a mistaken "stale extra scenario"; hash-verified against the pinned value before commit). `step-library.logic.test.ts`/`step-conformance.infra.test.ts`/`golden-fingerprint.infra.test.ts`/all 4 `violations.test.ts` suites green after the fix. **LIVE-FIRED FOR REAL**, not merely unit-proven: the `sources-full-forced-1.json` re-capture (`LINK_MASSING_FORCE_FULL=1`, mode FULL, `retracted 520,492 row(s)`) produced the mechanism's FIRST genuine before-image file, `docs/reports/golden/link_massing/before-image/2026-08-29T00-49-52.392Z-parcel_buildings.jsonl` — 520,492 lines (`wc -l`), one per retracted row, byte-verified to match the retraction count exactly, written and confirmed on disk BEFORE the retraction's own log line ("retracted 520,492 row(s)") ever printed. `link_wsib`'s mode never resolved full in this commit's captures; its own first before-image file was produced by WF3-F's live repair run (LW-D14, same session): `docs/reports/golden/link_wsib/before-image/2026-08-29T01-47-22.902Z-wsib_registry.jsonl`, 993 lines, byte-count-verified equal to the retraction's own reported row count — the mechanism's SECOND real-world proof, confirming it generalizes past `link_massing`'s single exercise | pilot 3 assessment §R; this report; live capture 2026-08-28/29 |

| LPA-D1 | link_parcel_addresses (compute `scripts/link-parcel-addresses.js` / future `scripts/lib/compute/link-parcel-addresses.js`) | `scripts/link-parcel-addresses.js:169-177` (the write: `INSERT…SELECT…JOIN ST_Within…ON CONFLICT (parcel_id, address_point_id) DO NOTHING`, `0` `DELETE` anywhere) vs. `scripts/link-parcels.js:282` (consumer comment: *"parcel_address_points guarantees the AP geom is INSIDE the parcel"*) | **OPEN · PIN (Spec 123 §3.1, KNOWN-DEFECT, class D "W3 retraction breach").** Both upstream producers UPDATE `geom` on `ON CONFLICT` for an existing row (`load-parcels.js:293-294`, `load-address-points.js:215`), so a parcel-boundary or address-point correction CAN silently invalidate an already-written bridge row — `ST_Within` no longer holds — with zero mechanism to re-evaluate or remove it. `link-parcels.js:282`'s own in-file comment states the guarantee the code does not actually keep. Strategy 1a (0.97 confidence) and Tier 1a (0.95 confidence, `link-coa-to-parcels.js`) both trust it unconditionally. Live exposure measured 0/511,224 rows (`2026-08-29`, re-confirmed pilot 5 commit 1) — the defect is real and concretely reachable, its current blast radius is zero | OPEN · PIN | pin at commit 7 with a `checks[].why` (Rule 4) naming this row; fix (a retraction/re-evaluation mechanism) is a SEPARATE future commit/WF3, informed by whether live staleness ever moves off 0 — not this pilot's conversion commit | [pilot 5 assessment](2026-08-29-pilot5-link-parcel-addresses-assessment.md) §2 (`LPA-D1`); `.cursor/active_task.md` finding 1 / G6 |
| LPA-D2 | link_parcel_addresses (compute `scripts/link-parcel-addresses.js`) | `scripts/link-parcel-addresses.js:32-34` (header comment) vs. `:148` (`lastParcelId`, a local variable, never persisted) | **OPEN · fix scheduled commit 7.** The header claims *"Resumable: each batch commits independently; operator Ctrl-C leaves the DB in a consistent partial state and a re-run picks up where we left off."* Measured: every invocation restarts the batch loop from `id > -1` (the full population), whether or not a prior run completed. `ON CONFLICT DO NOTHING` makes this idempotent (safe) but NOT resumable in the claimed sense (efficient re-entry) — a self-description bug, not a behavior bug; independently reconfirmed against `docs/reports/2026-08-22-sources-chain-evidence-base.md:285`'s prior finding of the identical claim/code mismatch | **CLOSED · commit 7** | commit 7 — descriptor/`notes.json` states the truth (idempotent-not-resumable); no behavior change, no library growth | [pilot 5 assessment](2026-08-29-pilot5-link-parcel-addresses-assessment.md) §2 (`LPA-D2`); `.cursor/active_task.md` finding 2 |
| LPA-D3 | link_parcel_addresses (descriptor `scripts/link-parcel-addresses.descriptor.json`, checks `parcel_fanout_outliers`/`parcel_fanout_condo_outliers`, library `scripts/lib/step/verdict.js`) | `scripts/link-parcel-addresses.descriptor.json` T4/T5 `checks[].limit` (both authored `"viol == 0"` at commit 7) vs. `scripts/lib/step/verdict.js:63,145` (`VIOL_RE = /^viol (==\|<=) (\d+)$/`; `evaluateLimit`: `m[1] === '==' ? measured === bound : measured <= bound`) | **CLOSED · peel 8b, found while executing #165's must-fail fixture battery.** `resolveLimit` substitutes the RESOLVED config value onto whatever comparator the check declares (`checks[].limit_from_config`) — `"viol == 0"` was written for the LITERAL no-`limit_from_config` idiom ("no violations at all = pass"), but T4/T5 both declared `limit_from_config` too, so the runtime comparator became `"viol == 20"`/`"viol == 400"` — an EXACT-EQUALITY gate a fluctuating COUNT essentially never satisfies. Live consequence, confirmed both by source read and a captured real run (`docs/reports/golden/link_parcel_addresses/post-8a/standalone-forced.json`, peel 8a): T4 (`parcel_fanout_outliers`, 130 non-CONDO outliers vs. default 20) happened to read WARN — the RIGHT status by coincidence (`130 != 20`, any non-matching count reads WARN regardless of direction). T5 (`parcel_fanout_condo_outliers`, 0 CONDO outliers vs. default 400) read WARN — WRONG (`0 != 400`); this check has WARNed on EVERY real run since commit 7 (never a FAIL/blocking gate, so nothing else broke), silently misrepresenting a genuinely clean CONDO population as an outlier every single time | **CLOSED · this commit** | Both `limit` strings changed `"viol == 0"` → `"viol <= 0"` (the config-substituted CEILING form `verdict.js` already implements and documents, `VIOL_RE`'s two branches are intentional, not a bug in the library — the descriptor authored the wrong one). `#165`'s must-fail fixture battery (flipped `it.fails` → `it()` this peel) caught it live: T5's own sabotage fixture (`fanout_condo_gt_threshold: 5`) initially still read PASS post-fix because 5 is nowhere near the 400 default, corrected to `401` (must genuinely exceed the bound, not merely be nonzero) to prove the WARN direction. `src/tests/steps/link_parcel_addresses/violations.test.ts` #165 now proves BOTH directions for T4/T5 post-fix (a clean population PASSes, a genuinely-elevated one WARNs) | this row; [pilot 5 assessment](2026-08-29-pilot5-link-parcel-addresses-assessment.md) Peel 8b |
| LW-D19 | link_wsib (compute `scripts/lib/compute/link-wsib.js` + descriptor `scripts/link-wsib.descriptor.json` + runner `scripts/lib/step/index.js`) | `entities.is_wsib_registered` (descriptor `outputs.writes[1]` fill-true target, `checks[]` id `registered_entities_with_zero_links`, `docs/reports/2026-08-28-pilot4-link-wsib-assessment.md` §8d precision table) | **CLOSED · this commit (operator ruling 2026-08-29). HIGH.** §8d's fresh 60-row precision sample of the LW-D18-hardened tier-3 (fuzzy, 0.60) population measured only 31.7% confirmed-genuine matches (up to 46.7% counting UNSURE) — filed as an OPEN product question in `review_followups.md` ("should tier-3 confer `is_wsib_registered`"). Before this commit the fill-true target flagged `is_wsib_registered = true` identically for all 3 tiers (exact trade 0.95, exact legal 0.90, fuzzy 0.60, 20/20 correct) — a WSIB-registration CLAIM shown to a user or fed into a downstream product surface was, for roughly half of tier-3's rows, more likely wrong than right. Measured live BEFORE the fix: `entities.is_wsib_registered = true` count 551; SQL-computed expectation (`EXISTS >= 1 exact-tier link`) 301 — a 250-row gap, all tier-3-only registrations | **CLOSED · this commit** | Operator ruling, Spec 124 §7 ladder: (a) descriptor — the fill-true target's `write_discipline.scope` restricted from a per-tier `$1` to the closed 2-value set `match_confidence IN ($1, $2)`, where $1/$2 are ALWAYS the two exact tiers' resolved confidences (new `exactTierConfidences(descriptor, config)`, `scripts/lib/compute/link-wsib.js`); (b) compute — the fuzzy tier's own pass issues the IDENTICAL statement with the IDENTICAL params as the exact tiers' passes (no branch, no second code path — a fuzzy-only match is a genuine no-op against this target); (c) a NEW 5th write target (`outputs.writes[4]`, `set_based_scoped`, `set_value: false`, scope `is_wsib_registered = true AND id NOT IN (SELECT linked_entity_id FROM wsib_registry WHERE match_confidence IN ($1, $2))`) runs UNCONDITIONALLY once per invocation (never gated to mode "full", unlike A-7/LG-16's retraction) — the self-heal correction, chosen over a one-off FULL script per the ruling's own "simplest closed form" framing; new declared check `is_wsib_registered_corrected` (INFO, always present) and new golden invariant `wsib_registered_entities_without_exact_tier_link` (expected 0, self-healing). No entities column added — `\d entities` measured only `is_wsib_registered` exists for this purpose; a future product surface needing the tier-3 candidate signal should expose `wsib_registry.match_confidence` via the existing join, per the ruling. Runner fix found while wiring the new target: `written.privilege` was keyed off `specs[specs.length - 1].table` (a fragile "last write target" heuristic) — adding a 5th target silently flipped it from reading `wsib_registry`'s RLS state to `entities`', which the `write_privilege` check's own declared `why.liveness` names as `wsib_registry`; re-anchored on `nullRetractSpec.table` instead (stable regardless of array length). **Live-verified, twice (idempotent), foreground:** `LINK_WSIB_FORCE_FULL=1` run 1 (329.1s) → `entities.is_wsib_registered` 551 → 301 (exact match to the SQL-computed expectation, cited before the run), `wsib_registered_entities_without_exact_tier_link` invariant 0, `wsib_registry` row count unchanged (121,116, LG-16 contract held); run 2 (324.5s) changed 0 additional entity rows (`entities` table hash byte-identical both runs) — idempotency confirmed. **Transparent finding, not silently masked:** `is_wsib_registered_corrected` read 0 in BOTH forced-full runs — NOT because the new target is dead code, but because `LINK_WSIB_FORCE_FULL` resolves mode=full, which ALSO fires A-7/LG-16's PRE-EXISTING, unrelated retraction+unflag cascade (`buildEntitiesUnflagSql`, unconditional "NOT EXISTS any link") — that cascade transiently zero-links every tier-3-only entity the moment its link is retracted, unflagging them BEFORE the tier loop relinks them and BEFORE this new target's own pass ever runs; both mechanisms reach the identical 301 end state in mode=full, by construction. This target's distinct, load-bearing contribution is INCREMENTAL-mode runs (A-8: the overwhelming majority in practice; full is a rare manual repair), where A-7 never fires. Proven directly and live via a throwaway, rolled-back transaction against the real DB (no permanent mutation): picked a genuine tier-3-only entity (id 19) currently `is_wsib_registered = false`, manually set it `true` (simulating the pre-fix bug), ran this exact target's real generated SQL via the real `write.executeSetBasedClear` — 1 row changed, entity flipped back to `false`; a control exact-tier-linked entity (id 1) stayed `true`, untouched; a second identical pass changed 0 rows (idempotent) — then ROLLBACK, DB left unmodified. Both directions and the idempotency claim from ruling item (d) hold: tier-3-only entity is NOT registered (PASS), exact-tier entity IS registered (PASS), idempotent second run changes 0 rows (PASS). Red-first unit locks added in `src/tests/steps/link_wsib/violations.test.ts` (new `describe` block, 6 `it`s): `exactTierConfidences` returns the exact pair; `buildTierSql` returns the SAME pair for all 3 tiers including fuzzy; `ENTITIES_UNFLAG_SCOPE` text shape; the fill-true target's declared scope is the closed set; the 5th write target exists with the right shape; `is_wsib_registered_corrected` renders INFO. `step-library.logic.test.ts`'s existing LW-D15 dry-run test extended with `exactTierConfidences`/`buildEntitiesUnflagCorrectionCountSql` stubs + a `STUB_LW_UNFLAG_COUNT` assertion (dry-run would-be count = 4, wired correctly). Re-captured `docs/reports/golden/link_wsib/post/{permits,sources,standalone,sources-full-forced-4}.json` (fingerprint-current) + 2 fresh forced-full captures under `post-wf3i-forced/`. `violations.test.ts` (77/77) + `golden-fingerprint.infra.test.ts` (17/17) + `step-library.logic.test.ts` (117/117) + `step-conformance.infra.test.ts` (103/103) green after the fix. Spec 46 §3 (Outputs / Core Logic) amended same commit. `docs/reports/review_followups.md`'s "Spec 46 tier-3 product question" entry → RULED (this row) | [pilot 4 assessment](2026-08-28-pilot4-link-wsib-assessment.md) §8d; `docs/reports/review_followups.md` "Spec 46 tier-3 product question"; operator ruling 2026-08-29; live capture 2026-08-29 |
| LW-D20 | link_wsib | `scripts/lib/step/staleness.js` `detectInterruptedRetraction`/`selectMode` + `scripts/lib/step/index.js` `runCascadePhase`/`runLinkPhase` (library, all steps — R-B/LG-19 runtime reader) | R-B's runtime mechanism (Spec 122 R-B) — a `crashed`/stuck-`running` prior `pipeline_runs` row for a step's own producer, more recent than its last `completed` row, must force mode FULL on the next invocation | **CLOSED · this commit** | landed 2026-08-29, proven live: killed a genuine `LINK_WSIB_FORCE_FULL=1` run 5s into real work; the next plain invocation resolved `FULL (recover_interrupted_retraction)`, ran the real tier-3 repair to completion (631.5s, 2 iterations, 548 rows relinked, before-image written per R-M), `verdict:PASS`; post-state verified consistent (`wsib_registry` 121,116 rows, `entities.is_wsib_registered` 301). Two bugs found and fixed by the live proof itself, not by review: (1) a step's own just-opened `running` row self-triggered on every run until excluded via `ownRunId`; (2) the check was unreachable whenever `ledgerGatedSkip` would otherwise SKIP, fixed by checking it before the gate and folding it into `bypassed`. Locked fast (`src/tests/step-library.logic.test.ts`, fake pool) and against real Postgres (`src/tests/db/staleness-interrupted-retraction.db.test.ts`) | [pilot 4 assessment](2026-08-28-pilot4-link-wsib-assessment.md) §R addendum |
| LPA-D4 | link_parcel_addresses (descriptor `scripts/link-parcel-addresses.descriptor.json` + compute `scripts/lib/compute/link-parcel-addresses.js` + runner `scripts/lib/step/index.js`/`scripts/lib/step/staleness.js`) | `scripts/link-parcel-addresses.descriptor.json` `checks[]` (15 declared, ALL `when:"post"`, none `when:"pre"`) vs. `scripts/lib/step/index.js:1596` (`onlyChecks = new Set(descriptor.checks.filter((c) => c.when === 'pre')...)`, the gated-SKIP narrowing) vs. `:1159` (`log.info(tag, 'materialize ledger gate: SKIP (${gatedSkip.reason})')`, never `records_meta`) | **CLOSED · this commit. HIGH.** WF3, Reality-Check output-altitude finding. On a gated SKIP (`staleness.ledgerGatedSkip`, LG-15) the library narrows `stepCtx.checks` to `when:"pre"` ids — this descriptor declared zero, so the persisted `audit_table` had only the 2 always-present `sys_*` rows and `gatedSkip.reason` ('no_upstream_changes') was never observable outside a log line. Live: `pipeline_runs` ids 1709/1710/1714/1717 measured `jsonb_array_length(audit_table->'rows') = 2`, no `reason` anywhere in `records_meta` — a broken always-unchanged gate would have been indistinguishable from a healthy quiet run | **CLOSED · this commit** | Spec 124 §7 rung (a)+(b) then a generic rung (d): (1) new `gate_decision` `when:"pre"` INFO check on `link-parcel-addresses.descriptor.json`, mirroring `link_wsib`'s `gate_decision` exactly (`{mode, reason, gated_skip}`), wired through a new `gate_decision(ctx)` observer in `scripts/lib/compute/link-parcel-addresses.js` reading `ctx.gate` (an existing `STEP_CTX_KEYS` entry, already populated by `runMaterializePhase` — no harness-only field); (2) LIBRARY (generic, one site): new `staleness.gateRecordsMeta(descriptor, gate, gatedSkip)` returns the closed shape `{reason, gated_skip}` (+`own_slugs`/`upstream_slugs`/`own_last_completed_at`/`upstream_non_completed`/`upstream_completed_with_changes`/`upstream_stale_running` when a `ledgerGatedSkip` result is available) — merged onto `records_meta.gate` in `scripts/lib/step/index.js`'s single recordsMeta-build site for EVERY archetype whose gate can genuinely skip (ingest, cascade, materialize); `runLinkPhase` (LINK, e.g. `link_massing`) is DELIBERATELY excluded — it drives `selectMode`'s tri-state full/incremental decision and carries no `terminals[].kind === "skip_gated"` entry in any converted descriptor, so it cannot skip; (3) CONFORMANCE (generic): new `describe('LPA-D4 ...')` in `src/tests/step-conformance.infra.test.ts` — any `CONVERTED` descriptor with a `terminals[]` entry of kind `"skip_gated"` must declare >=1 `checks[].when === "pre"`, proven RED against `git show HEAD:scripts/link-parcel-addresses.descriptor.json` (measured `{pre_checks: 0, has_skip_gated_terminal: true}` at the pre-fix commit) then GREEN post-fix (124/124 green); (4) both-directions locks in `src/tests/steps/link_parcel_addresses/violations.test.ts` — a static lock (skip_gated terminal + 0 pre checks fires; `link_massing`'s own descriptor, which has no skip_gated terminal, stays silent) plus two runtime locks via a new `runComputeDetailed` (accepts a narrowed `checkIds`, mirroring the runner's own `onlyChecks`): a SKIP-narrowed check set (`preIds` only) still reports `gate_decision` with `reason:"no_upstream_changes"`/`gated_skip:true`, and a full (non-skip) check set reports it with `reason:"upstream_changed"`/`gated_skip:false` — plus a `gateRecordsMeta` unit lock (closed shape, both with and without a `gatedSkip.gate`) and a source-grep lock confirming `index.js` calls `staleness.gateRecordsMeta` gated on `ingest \|\| cascade \|\| materialize` (never `link`); (5) **live-verified, foreground, real DB (`127.0.0.1:54322/postgres`, 242 migrations):** ran `node -r dotenv/config scripts/link-parcel-addresses.js` standalone once — genuinely gated-SKIPPED (`no_upstream_changes`, nothing changed in `parcels`/`address_points` since the prior run) — and confirmed the live row directly: `pipeline_runs` id 1719, `jsonb_array_length(audit_table->'rows') = 3` (the new `gate_decision` row first, then the 2 sys_* rows, exactly +1 explained), `records_meta ? 'gate' = true`, `records_meta.gate = {"reason":"no_upstream_changes","gated_skip":true,"own_slugs":[...],"upstream_slugs":[...],"own_last_completed_at":"2026-08-29T13:21:09.658Z","upstream_non_completed":0,"upstream_completed_with_changes":0,"upstream_stale_running":0}` — the exact SKIP-row shape the finding demanded, measured, not inferred; the pre-fix runs (1714/1717) remain `n_rows=2`/`has_gate=false` for direct before/after contrast. **Golden re-capture (`golden-fingerprint.infra.test.ts` genuinely RED first — the descriptor/compute edit moved `source_fingerprint`, exactly as R-C predicts):** re-ran `scripts/analysis/capture-step-golden.js` for `docs/reports/golden/link_parcel_addresses/post/{sources,standalone}.json` (both genuinely gated-SKIP, `no_upstream_changes`) and `post/standalone-forced.json` (`LINK_PARCEL_ADDRESSES_FORCE_FULL=1`, gate bypassed, mode `incremental`, `terminal: materialized_with_warnings` — a genuine WARN on `parcel_fanout_outliers` per R-H, unrelated to this fix) — explained diff on all three: `+1` audit row (`gate_decision`, first row) `+records_meta.gate`, `parcel_address_points` table_state UNCHANGED (511,224 rows, hash `bde2b1c4` on all three — pre/post byte-identical, `--table-columns`/`--table-order` `parcel_id,address_point_id` per the original Fold A NOTE 8 precedent excluding `computed_at`, re-derived explicitly rather than trusting the harness's new descriptor-auto-derivation, which would have wrongly included the `run_at`-sourced `computed_at` column and produced a false diff — caught live: the first capture attempt (before pinning `--table-columns`) hashed `390a6d21`, a genuine miss corrected before commit), `source_fingerprint` now `87c62eb6...` (was stale in the committed files). The forced-FULL capture also hit a transient environment limit twice (`could not resize shared memory segment ... No space left on device`, Postgres parallel-worker DSM pressure inside the local Supabase container, unrelated to this fix's code) — resolved by `PGOPTIONS="-c max_parallel_workers_per_gather=0"` on the child invocation, third attempt succeeded (487 batches, 486,530 records_total, WARN verdict on the pre-existing `parcel_fanout_outliers` R-H standing-WARN, `gate_decision` row correctly `{mode:"incremental",reason:"bypassed",gated_skip:false}`). `golden-fingerprint.infra.test.ts` (20/20) green after. `step-conformance.infra.test.ts` (124/124) + `link_parcel_addresses/violations.test.ts` (80/80) + `link_wsib/violations.test.ts` (77/77) + `link_massing` violations suites (71/71) + `step-library.logic.test.ts` (117/117) + `chain.logic.test.ts`/`pipeline-sdk.logic.test.ts`/`source-version.logic.test.ts`/`pipeline-advisory-lock.infra.test.ts`/`load_ravines/violations.test.ts` all green after the fix — no other converted step's `records_meta` shape regressed (the `gate` key is new/additive, no prior consumer read it). Lesson filed (`tasks/lessons.md`): "a step that can skip must persist WHY it skipped — a pre-check is mandatory, enforced by conformance (LPA-D4)." Spec 124 §5 register: R-P row added (full text folds in the docs commit) | this row; Observability output-altitude finding, 2026-08-29; live capture 2026-08-29 |
| LPA-D5 | link_parcel_addresses (descriptor `scripts/link-parcel-addresses.descriptor.json` + compute `scripts/lib/compute/link-parcel-addresses.js` + seeds `scripts/seeds/logic_variables.json` + admin `GlobalConfigCard.tsx`) | `parcel_link_rate_pct`/`parcels_with_no_address_pct` (single class-blind average) vs. `address_points.address_class_desc` (4 structurally different populations) and `parcel_fanout_outliers` (T4, flat zone-blind 20 ceiling) vs. `parcels.zoning_class` | **CLOSED · this commit.** WF3-B, Reality-Check output-altitude finding, folded WF3-D (unlinked-within-2m INFO metric — measured NOT trivial, killed after >15 min, filed to `review_followups.md`). Measured live 2026-08-29: per-class link rate Land 469,453/478,811 (98.05%), Structure Entrance 12,727/13,399 (94.98%), Structure 28,595/32,508 (87.96%), Land Entrance 449/628 (71.50%) — a single averaged figure cannot distinguish "the corpus is healthy" from "one class quietly degraded while the others hide it." Separately: T4's flat 20-fanout ceiling has no zone-aware sub-bound to say whether a genuinely low-density residential zone (RD/RS) is contributing to its 130-parcel standing WARN population or is clean on its own | **CLOSED · this commit** | Spec 124 §7 rung (a)+(c): three new declared checks on `link-parcel-addresses.descriptor.json` — (1) `address_points_link_rate_by_class` (`kind:"distribution"`, INFO, always reported, mirrors `parcel_fanout_distribution`'s un-thresholded-companion posture) reads a new `json_agg` column (`buildPostSql`) grouping `address_points` by `address_class_desc`; (2) `structure_class_link_rate_warn` (`kind:"bound"`, WARN, `limit:"pct >= 85"` + `limit_from_config`, R-N floor-form precedent) gates the Structure class specifically — this step's own downstream consumers (`link-parcels.js` Strategy 1a, `link-coa-to-parcels.js` Tier 1a) depend on it most; new tunable `link_parcel_addresses_structure_link_rate_warn_pct` (T6, default 85, `on_invalid:"fail"` — verdict-affecting per R-G), measured live 87.96% PASSES with a real 2.96-point margin; (3) `parcel_fanout_rd_rs_outliers` (`kind:"bound"`, WARN, `limit:"viol <= 0"` + `limit_from_config`, same `viol<=N` double-duty shape as T4/T5 per LPA-D3) — a strictly tighter (15 vs T4's 20) per-parcel fan-out ceiling scoped to `parcels.zoning_class IN ('RD','RS')` (`buildInvariantsSql`'s new `$3` param), T4's declared retighten target; new tunable `link_parcel_addresses_fanout_warn_rd_rs` (T7, default 15), measured live 13 RD/RS parcels exceed fan-out 15 (of 258,596 RD/RS-zoned parcels; RD max 35, RS max 64) — PASSES today, close to the margin, not a vacuous always-PASS ceiling. Both T6/T7 registered in `scripts/seeds/logic_variables.json` (seeded live, 2/426 newly inserted, 424 already existed) and `GlobalConfigCard.tsx`'s "Parcel-Address Bridge" GROUP (an unregistered/un-GROUPed seeded var is the same hidden-knob failure P4 exists to close). `docs/reports/golden/link_parcel_addresses/invariants.json` gained `structure_class_link_rate_pct` + `rd_rs_fanout_gt_15_count` (independently-authored mirrors, per the harness's existing hand-duplication convention — same class LW-D18's own followup already names as a drift risk, not newly introduced here). Red-first proof: `#165`'s existing must-fail battery (`src/tests/steps/link_parcel_addresses/violations.test.ts`) auto-covers all 3 new checks generically (it iterates the real descriptor's `d.checks`, not a hardcoded list) — new `SABOTAGE_BY_VAR` entries for T6 (linked count dropped to 50% of total, well under the 85% floor) and T7 (RD/RS outlier count pushed to 16, over the 15 ceiling), a new `SABOTAGE_BY_ID` entry proving `address_points_link_rate_by_class` stays INFO even when every class's `linked` count is zeroed; new `#171`-sibling test cites T6/T7's rationale in the descriptor's own `checks[].why` + seed `description` (T1-T5's `#171` itself stays scoped to the historical pilot-5 report, which predates T6/T7 and correctly does not name them). **Live-verified, foreground, real DB (`127.0.0.1:54322/postgres`, 242 migrations):** `LINK_PARCEL_ADDRESSES_FORCE_FULL=1 PGOPTIONS="-c max_parallel_workers_per_gather=0"` (the same shm-pressure workaround LPA-D4/peel-8a's own filed followup names) → all three new rows fired with the EXACT measured numbers: `address_points_link_rate_by_class` = `[{Land,98.05},{Land Entrance,71.5},{Structure,87.96},{Structure Entrance,94.98}]`, `structure_class_link_rate_warn` = `87.96% >= 85` PASS, `parcel_fanout_rd_rs_outliers` = `13 <= 15` PASS — no code-vs-measurement gap. **Cascading fleet-wide updates a new logic variable requires (caught by the hook, not anticipated up front):** `src/tests/control-panel.logic.test.ts`'s `EXPECTED_LOGIC_VAR_KEYS` (a hand-maintained key-set mirror) gained both names; `docs/reference/logic-variables-registry.md` regenerated (`npm run logic-vars-docs`, 445 vars); `assert-schema.descriptor.json`'s TWO declared fleet-union lists (`checks[].expect` for `declared_logic_variables_present` + `config.probe_presence`, R-D's "cloud parity is a chain-start assertion" — a hand-maintained STATIC copy of `collectDeclaredLogicVariableNames()`'s live derivation, kept in sync by its own lock test) both gained both names in sorted position — which in turn moved `assert_schema`'s OWN `source_fingerprint`, requiring its own `post/{permits,coa,sources,standalone}.json` re-capture (all 4 PASS, `declared_logic_variables_present` reports `{missing:[]}` — both new seeded vars resolve). `violations.test.ts` (81/81, incl. the new LPA-D5 rationale test) + `assert_schema/violations.test.ts` (68/68) + `control-panel.logic.test.ts` (28/28) + `logic-vars-registry.infra.test.ts` (2/2) + `golden-fingerprint.infra.test.ts` (20/20) green after. Lessons/Spec register: no new generic library rule (descriptor-only, rung (a)/(c)), so no Spec 124 register row — LPA-D5 stays pilot-local per §4.6's promotion criterion (recurrence not yet claimed) | this row; Observability/Reality-Check output-altitude finding, 2026-08-29; live capture 2026-08-29; `docs/reports/review_followups.md` "WF3-D, deferred" |
| LPA-D6 | library, all steps (`scripts/lib/step/verdict.js buildAuditTable` + `scripts/lib/step/index.js` recordsMeta build site; found via `link_parcel_addresses`'s forced-FULL capture — coordinator alias "LW/LPA-D6") | `scripts/lib/step/verdict.js:268` (pre-fix: `if (row.status === 'FAIL' \|\| row.status === 'WARN') errors.push(...)`, ONE conflated array) vs. `scripts/lib/step/index.js:1769` (pre-fix: `checks_failed: built.errors.length`) vs. `scripts/quality/assert-data-bounds.js:979-982` (the un-converted precedent: separate `errors`/`warnings` arrays feeding separate `checks_failed`/`checks_warned` counts, already shipping) | **CLOSED · this commit. HIGH.** WF3-C, Reality-Check output-altitude finding. `records_meta.checks_failed` counted BOTH FAIL and WARN rows (`built.errors.length`, where `errors[]` held both severities) — measured live 2026-08-29: `link_parcel_addresses`'s forced-FULL capture reported `"checks_failed":1,"errors":["parcel_fanout_outliers: 130"]` on a run with `audit_table.verdict:"WARN"` and ZERO FAIL rows (the one non-PASS row, `parcel_fanout_outliers`, is severity WARN, R-H standing-WARN by design) — an operator or downstream consumer scanning `checks_failed > 0` as "this run had a genuine failure" would be WRONG on every WARN-only run across the fleet, indistinguishable from an actual FAIL without opening `audit_table.rows` and reading `.status` per row. `src/components/FreshnessTimeline.tsx:1092-1097` ALREADY reads `meta.checks_warned`/`meta.warnings` (dead code today, gated behind `if (hasAuditTable) return null` at :1099 since every converted step carries `audit_table` and the UI's audit-table-aware branch reads `at.verdict`/`at.rows[].status` directly instead) — the fix makes those already-written UI fields correspond to REAL backend semantics for the first time, though the render path itself stays dead until a future UI change reads them. Consumer grep (src/, scripts/, `run-chain.js`, `src/app/api/quality/`) found ZERO live production consumers of `checks_failed`/`errors[]="records_meta"` besides the already-dead FreshnessTimeline branch — independently corroborated by `assert-schema.descriptor.json`'s own prior deviation note ("checks_passed and errors have NO reader anywhere in the estate... the earlier consumer list naming run-chain.js and /api/quality was false") — so the fix carried zero live-consumer risk | **CLOSED · this commit** | Spec 124 §7 rung (d), one generic library site: `verdict.js buildAuditTable` now pushes a FAIL row's rendered string into `errors[]` and a WARN row's into a NEW `warnings[]` array (never both from one row — `SEVERITY_RANK`'s FAIL/WARN branches are mutually exclusive by construction), returned as a new `warnings` field alongside the existing `errors`; `index.js`'s single recordsMeta-build site now stamps `checks_failed: built.errors.length` (FAIL-only), a NEW `checks_warned: built.warnings.length` (WARN-only), `errors`/`warnings` both conditionally present, and `checks_passed: 'all'` requires BOTH arrays empty (parity with `assert-data-bounds.js`'s own `allMessages.length === 0` semantic — a WARN-only run is not "all passed"). `extraRows` (retired-var-row WARNs, dry-run INFO, before-image rows) remain OUTSIDE both arrays, unchanged from pre-fix behavior — a separate, pre-existing, out-of-scope gap (not newly introduced), filed nowhere new here. Red-first: `step-library.logic.test.ts`'s 3 existing LM-D16 tests declared `severity:'WARN'` fixtures and asserted on `.errors[0]` — genuinely wrong post-fix (a WARN row no longer reaches `errors[]`), corrected to read `.warnings[0]` (+ `expect(built.errors).toEqual([])` added, proving the split, not just the rename); a NEW test proves BOTH directions in one build (`severity:'FAIL'` row → `errors` only, `severity:'WARN'` row → `warnings` only, mixed in a single `buildAuditTable` call). Re-captured every converted step's CHEAPEST invocation, all genuinely gated-SKIP or near-zero-cost (no destructive/forced repair re-run): `assert_schema` ×4 (`permits`/`coa`/`sources`/`standalone`), `load_ravines` `standalone`, `link_massing` `permits`, `link_wsib` `permits`, `link_parcel_addresses` `standalone` — EXPLAINED DIFF ONLY on every one: `records_meta` gains `checks_warned` (+`warnings` where a WARN row exists), table_state hashes UNCHANGED on all 9 captures (verified byte-for-byte against `HEAD`'s committed hashes before overwriting: `ravines` `d38b07ad`, `parcel_buildings` `329bbcb6`, `entities`/`wsib_registry` `81ce736f`/`2262efc7`, `parcel_address_points` `bde2b1c4` — zero data drift, observability-only diff). **Found and fixed live while re-capturing:** `link_massing`'s and `link_wsib`'s `permits` captures both needed their ORIGINAL `--table-columns`/`--table-order` args re-supplied explicitly (the harness's newer descriptor-auto-derivation wrongly includes each step's own `run_at`-sourced column — `linked_at`/would-be `matched_at` drift — the exact LPA-D4-adjacent class of bug already caught once this session on `link_parcel_addresses`'s own recapture); both corrected before commit, hashes verified identical to `HEAD`. `step-library.logic.test.ts` (118/118) + `src/tests/steps/*` (643/643 across 6 suites) + `step-conformance.infra.test.ts` (124/124) + `golden-fingerprint.infra.test.ts` (20/20) green after. Lesson filed (`tasks/lessons.md`): "checks_failed means FAIL; WARN is counted separately." Spec 124 §5 register: R-Q row added (full text folds in the docs commit) | this row; Observability/Reality-Check output-altitude finding, 2026-08-29; live capture 2026-08-29 |
| CC-D1 | compute_centroids (compute `scripts/compute-centroids.js` / future `scripts/lib/compute/compute-centroids.js`) | `scripts/compute-centroids.js:117-131` (JS-fallback cursor pagination `id > lastId`, fence `80ac3469`, 2026-03-15: *"Fix infinite loop: cursor pagination replaces `centroid_lat IS NULL` filter which refetched malformed geometries forever"*) vs. `:98-115` (the PostGIS fast path, a single `UPDATE ... RETURNING id` with no loop — no cursor-pagination analogue) | **PIN · knowingly-retired (Fold C S-1 / Fold D Cross-read Adversary item 1).** A-1(a) retires the JS fallback in full (`guards.requires: postgis`, `on_missing: fail`) — the fence protected ONLY that branch, which never fires on this DB (`pgisCheck` always resolves true) or in production. The fence is deleted WITH the branch, not preserved as unexercised dead code, per the `link_massing` A-8 precedent: keeping 70 lines of unverifiable-in-CI fallback alive on an inference is less honest than a declared HALT guard. Evidence trail (commit message + Intent Ledger row) preserved in `notes.json`'s `fences[]` + `checks[].why`, never as live code | **PIN (knowingly-retired)** | pilot 6 commit 7 — `guards.requires: postgis`/`on_missing: fail` declared; `notes.json.fences[]` entry naming `80ac3469` | [pilot 6 assessment](2026-08-29-pilot6-compute-centroids-assessment.md) §2 (`CC-D1`); `.cursor/active_task.md` finding 5, Fold C/D |
| CC-D2 | compute_centroids output, consumed by `link-parcels.js` Tier-3 (`scripts/link-parcels.js:411-426`) | `parcels.centroid_lat/lng` (`ST_Centroid`, correct per Spec 59 R2.5 for concave lots) vs. `link-parcels.js:411-426`'s nearest-centroid `ST_DWithin`/`ST_Distance` join, which trusts centroid PROXIMITY, not polygon CONTAINMENT | **PIN · not a defect in `compute_centroids` itself.** Reality-Check R-1 (Fold C, re-confirmed this commit): of 3,626 out-of-polygon centroids (0.75% of 486,530, pre-existing, Spec 59-sanctioned), 3,130 land inside a DIFFERENT parcel; of 494 `spatial`-tier `permit_parcels` links pointing at a drifted parcel, 276 have the permit's own point actually inside another specific parcel — a mis-attribution signature in `link-parcels.js`'s join strategy, not in this step's centroid computation. `compute_centroids`'s own output is correct per its contract (arithmetic/PostGIS centroid of the supplied geometry); the exposure is downstream. Zero-behaviour-change scope holds — this pilot does not alter the centroid algorithm or the consumer join. `centroid_in_neighbour_parcel_count=3130` re-measured this commit via the materialized-CTE+LATERAL form (2.57s) and pinned in `invariants.json` (commit 5) as an observability row on this step's own invariant set. **Count after (WF3, 2026-08-30 — CC-D3's live FULL-mode repair, same commit):** `outside_polygon_count` 3,626 → **1,547**; `centroid_in_neighbour_parcel_count` 3,130 → **1,344** — both DROPPED because `ST_Centroid` replaced every stale (mostly pre-PostGIS-offload JS arithmetic-mean) centroid, not because this defect's own root cause (a concave-lot centroid landing outside its own polygon, Spec 59 R2.5) was addressed — CC-D2 stays **PIN**, unresolved by CC-D3's repair, still exposing 1,344 potential mis-attributions downstream (a smaller, not zero, population; the HIGH followup against `link_parcels.js` is unaffected and still open) | **PIN** | pilot 6 commit 4 (classification, no code change from this pilot); `invariants.json` commit 5; HIGH followup already filed against `link_parcels.js` (Fold C) | [pilot 6 assessment](2026-08-29-pilot6-compute-centroids-assessment.md) §2 (`CC-D2`); `.cursor/active_task.md` Fold C R-1 / Fold D item 4 |
| CC-D3 | compute_centroids output, consumed by `link-parcels.js` Tier-3 + `link-coa-to-parcels.js` (population, not a compute_centroids bug) | `parcels.centroid_lat/lng` (stored) vs. `ST_Y(ST_Centroid(geom))`/`ST_X(ST_Centroid(geom))` (freshly computed — the EXACT expression `compute-centroids.js:101-104` writes today) on the SAME, unchanged `geom` | **CLOSED-MEASURED (WF3, 2026-08-30 — operator "yes to the recommendations").** Was PIN, 292,587/486,530 (60.1%) stored-vs-fresh distance >1m (2026-08-29). Fix: a declared FULL mode (`compute-centroids.descriptor.json outputs.writes[1]`, class `set_based_scoped`/`set_source:"compute"`, LG-22, `scripts/lib/step/write.js`), guarded `IS DISTINCT FROM` (rounded to the column's own `NUMERIC(10,7)` precision — a real bug found red-first: an unrounded comparison against `ST_Centroid`'s raw double would have flagged every row, including one this exact formula just wrote, as "drifted"; fixed before the live run, `src/tests/db/compute-centroids-full-recompute.db.test.ts`), before-imaged (R-M generalized to a value-overwriting guarded write), reachable ONLY via `override.force_full` (`COMPUTE_CENTROIDS_FORCE_FULL=1`), never scheduled. **Live run** (`127.0.0.1:54322/postgres`, 245 migrations): 395,085/486,530 (81.2%) parcels repaired — the GUARD's own scope (any nonzero drift) is a superset of the >1m observability metric, so more rows changed than the originally-measured 292,587; recompute_max_shift_m 1,476.84m (matches the pilot 6 report's own max exactly). Wall-clock 149.1s (before-image SELECT + 40 batched UPDATEs at batch size 10,000). Before-image: 395,085 rows, 26.7 MB, `docs/reports/golden/compute_centroids/before-image/2026-08-30T16-22-33.602Z-parcels.jsonl`. **Post-repair, measured live:** `centroid_algorithm_drift_gt_1m_count` 292,587 → **0**. `outside_polygon_count` 3,626 → 1,547 and `centroid_in_neighbour_parcel_count` 3,130 → 1,344 (both DROPPED — `ST_Centroid` replaces the retired JS arithmetic-mean, Spec 59 R2.5 concave-lot behaviour, not a residual defect). `parcels` row count unchanged (486,530); migration 245's trigger unchanged (`pg_get_triggerdef` re-verified verbatim). **Idempotency, twice-run:** a second forced-FULL run (`docs/reports/golden/compute_centroids/post/sources-full-forced-1.json`) selected and touched **0** rows (`recompute_rows_changed:0`, 0-byte before-image) — `idempotent_rerun:"zero_writes"` proven, not asserted. **Plausibility sample** (60 random parcels, `setseed(0.20260830001)`, post-repair): 59/60 (98.3%) new centroids land inside their own polygon — consistent with the standing `outside_polygon_count` rate (1,547/486,530 ≈ 0.32%; the sample's 1/60 miss is within noise for that base rate). Table hash CHANGED by design (`94473cfd` → `a12a1a54`, 486,530 rows) — this IS the repair, not a regression; explained in the pilot 6 report §10 and this step's own re-captured `post/sources.json`/`post/standalone.json` (INCREMENTAL path, unaffected, byte-identical `94473cfd`, proving the FULL-mode addition is genuinely additive). `step:validate --step=compute_centroids` re-run after this commit (score/hard-stop recorded in the report's own regenerated scorecard) | **CLOSED-MEASURED** | pilot 6 commit 4 (classification, no code change, PIN); **CLOSED this WF3** — `scripts/lib/step/write.js` (LG-22), `scripts/lib/step/index.js` (`runBackfillFullRecompute`), `scripts/lib/compute/compute-centroids.js` (`buildFullRecomputeSql`), `scripts/compute-centroids.descriptor.json`, `src/tests/db/compute-centroids-full-recompute.db.test.ts` | [pilot 6 assessment](2026-08-29-pilot6-compute-centroids-assessment.md) §4 (`CC-D3`, Finding 7) + §10 (the WF3 repair); `review_followups.md` CC-D3 entry (closed same commit); `scripts/steps/_schema/programme-items.json` `RT-CC3` (updated) |
| CC-D4 | compute_centroids Intent Ledger (`docs/reports/2026-08-29-pilot6-compute-centroids-assessment.md` §2) | 3 `preserved-in-compute` rows (`e4765619` :101, `7c75e92e` :109, `90e3d0f8` :111) with no `why`/`notes.json`/`checks[]` grounding in-row — Rule 4 `checkPreservedInComputeHasWhy` `enforced-red`, "G-2: 3 preserved-in-compute row(s), 3 with no why/notes.json/checks[] grounding" | **CLOSED (WF3, 2026-09-03).** Rows A (`e4765619`) and B (`7c75e92e`) cited in-row: A's accounting shape is `encoded-as-descriptor-field`, declared in `checks[].why` of `parcels_processed`/`centroids_computed`; B's PostGIS-required rule is written down in `notes.json decisions[]` A-1(a) + descriptor `guards.requires`. Row C (`90e3d0f8`) **re-disposed `preserved-in-compute` → `knowingly-retired`** (operator ruling, §4.2 adjudication) — premise refuted live: `grep -rn safeParse scripts/lib/compute/compute-centroids.js scripts/lib/step/*.js` = 0 hits; the old file's 3 call sites (`git show 90e3d0f8:scripts/compute-centroids.js` → `:19,:67,:198,:200`) were never ported. `:67`'s count parse superseded by server-side `COUNT(*)::int`; `:198`/`:200`'s threshold parse superseded by `config.logic_variables[]` min/max + `on_invalid:"fail"` (T1/T2). Fold D's 2026-08-29 G3-vocabulary ruling for this commit amended in place, dated, not silently. `step:validate --step=compute_centroids --write` re-run after this commit — Rule 4 `enforced-green` | **CLOSED** | pilot 6 Fold D G3 vocabulary ruling (item 5, 2026-08-29, superseded); **CLOSED this WF3** — `docs/reports/2026-08-29-pilot6-compute-centroids-assessment.md` §2 rows :101/:109/:111 + item 5 + new §11; no `src/`/`scripts/lib/`/descriptor change | [pilot 6 assessment](2026-08-29-pilot6-compute-centroids-assessment.md) §2 + §11 (`CC-D4`); Spec 124 :73 (G-2 measurement updated) |
| LP-D1 | link_parcels (compute `scripts/link-parcels.js` / future `scripts/lib/compute/link-parcels.js`) | `scripts/link-parcels.js:403-432` (Strategy 3 Step 2, centroid-nearest fallback: `ST_DWithin`/`ORDER BY ST_Distance` on `pa.centroid_lat/centroid_lng`) vs. the file's own header intent (`:12-14`, "nearest parcel") | **OPEN — DEFECT.** Root cause is CC-D2/CC-D3's own exposure surface, PIN there, fixed HERE. Live re-measurement this pilot (commit 4, independent of Fold A/B): 10,616/17,500 (60.6%) of the `spatial`-tier population resolves to a DIFFERENT parcel under THE FIX's own KNN-boundary-distance predicate; pure containment alone flips 6,403/17,504 (36.6%), a subset. Sample plausibility (seed `20260830002`, N=120, stratified by flip-distance delta): 0/120 sampled OLD picks ever achieve containment; `new_contains` rate rises monotonically with delta magnitude (0% to 95%); population-wide containment-upgrade rate 6,403/10,616 (60.3%) | **CLOSED-MEASURED this commit (commit 8).** The declared FULL re-run's before/after delta, measured against the live before-image: **10,707/17,504 (61.2%) flip to a different parcel** — 82 rows (0.77%) above the declared 10,616–10,625 range, flagged as live-DB-churn (the same class already documented at commit 4), not silently absorbed. R-O AFTER-half sample (seed `20260830002`, N=120, same 6 bins, re-run against the ACTUAL committed flips) reproduces the BEFORE-half's plausibility profile: 0/120 `old_contains` (identical to the BEFORE-half), `new_contains` rising 0%→95% by delta bin (identical pattern), population-wide `new_contains` 59.5% (vs. the BEFORE-half's 60.3%) | THE FIX (Spec 124 §7 rung (e)): Strategy 3 Step 2 rewritten to an unconstrained KNN LATERAL on `pa.geom`, declared `, pa.id ASC` tiebreak, cap as scalar post-filter, `WHERE v.lng IS NOT NULL AND v.lat IS NOT NULL` guard (`LP-D6`); locked both directions in `src/tests/steps/link_parcels/violations.test.ts` (commit 6) | [pilot 7 assessment](2026-08-30-pilot7-link-parcels-assessment.md) §0.5/§2 (`LP-D1`)/§4/§8; `.cursor/active_task.md` "THE FIX" section, Fold A/B/C |
| LP-D2 | link_parcels (`permit_parcels`) | `permit_parcels` — 989 `(permit_num, revision_num)` pairs carry 2+ rows, pre-dating the `8a1c7d25` (2026-04-01) retraction-on-relink fence, never backfilled | **OPEN — CONTRACT-adjacent.** Schema-legal (UNIQUE is `(permit_num, revision_num, parcel_id)`, not `(permit_num, revision_num)` alone), violates the step's own post-fence intent. Grounded closure (Fold C item 7): `enrich_permits` (Spec 66 DEC-3) re-derives its dominant-parcel pick fresh on every run (`scopeWhere:'TRUE'`, no incremental filter) — these 989 rows do not permanently corrupt cached state, only the current pick, deterministically, via Spec 66's own DEC-1 tie-break | **CLOSED-MEASURED this commit (commit 8).** `duplicate_permit_pair_count` measured **989 → 0** after the declared FULL re-run — self-heal confirmed, not merely predicted, exactly as Fold A R-F expected | root cause `8a1c7d25` (2026-04-01, "atomic ghost cleanup + changed-match deletion") — the changed-match retraction DELETE it introduced has no backfill against pre-existing duplicates | [pilot 7 assessment](2026-08-30-pilot7-link-parcels-assessment.md) §2 (`LP-D2`)/§4/§8 |
| LP-D3 | link_parcels (`scripts/link-parcels.js:186,660`) | `:186` (`chainId==='sources'?6:9`, origin `2577e694`, 2026-04-21) vs. `:660` (`process.env.PIPELINE_CHAIN==='sources'?6:7`, origin `5baaed5a`, 2026-03-26) — same axis, disagreeing non-`sources` value | **OPEN — DEFECT.** Spec 122 §1.7's own worked example ("`phase` is an explicit map, never a ternary", `:302`,`:505`). This pilot's G3 archaeology establishes the origin order: `5baaed5a`'s `?6:7` came first (a deliberate 3-chain phase-renumbering sweep); `2577e694` independently reinvented the same axis a month later with the unreconciled value `9`. Live empirical confirmation, commit 5: `--compare` of the `permits`/`sources`/`standalone` golden captures shows `permits`/`standalone` both phase 9, `sources` phase 6 — the defect fires today, observably | **CLOSED this commit (commit 7)** — `sharing.varies_by_chain.phase` (`{permits:7, sources:6}`, A-5 RULED against the `link_wsib` precedent `{permits:7, sources:19}`) replaces both ternary sites; live-verified via commit 5's own `--compare` (permits/standalone phase 9→7, sources unchanged 6→6) and commit 7's own re-capture | A-5 RULED, Fold A/Fold C item 8 reconfirmed | [pilot 7 assessment](2026-08-30-pilot7-link-parcels-assessment.md) §2 (`LP-D3`)/§3/§5 |
| LP-D4 | link_parcels (`scripts/link-parcels.js`) | 5 undeclared literal tunables: `0.97`/`0.95`/`0.90`/`0.80` (tier confidences, `:294,324,398,470,339`) + `75` (`link_rate` WARN threshold, `:638`) — never read from `ctx.config` | **OPEN — DEFECT (Spec 124 Rule 3).** `spatial_match_max_distance_m`/`spatial_match_confidence` are ALREADY correctly registered (origin `030a7611`, 2026-04-16) — this row covers the FIVE literals that commit did not touch | **CLOSED this commit (commit 7)** — T1-T5 land as registered `logic_variables` rows (seeded + applied locally, 5/435 newly inserted) + the admin "Parcel Linking" GROUPS entry (consolidating the 2 pre-existing vars); live-verified in `records_meta.config` on the post-conversion golden capture | P4 tunable inventory, governing plan | [pilot 7 assessment](2026-08-30-pilot7-link-parcels-assessment.md) §2 (`LP-D4`) |
| LP-D5 | library, class F (`link_full_retraction`) — found via `link_parcels`, applies to any future class-F step | `scripts/steps/_schema/step.schema.json:389,408` (names the enum) vs. `scripts/lib/step/write.js:63`'s `SET_BASED_CLASSES` (excludes it) — no executor exists | **OPEN — CONTRACT-adjacent (a library gap, not a defect in `link_parcels`'s own logic).** `link_massing` (the OTHER class-F-enumerated step) does NOT actually use the enum's declared upsert+DELETE-stale+DELETE-zero-match mechanic either — it uses `set_based_scoped`+`guarded_upsert` via `is_primary`, which `permit_parcels` cannot reuse (no flag column) | **CLOSED-MEASURED this commit (commit 8).** `LG-24`'s `executeGuardedDeleteByKey` exercised for real with non-zero deletions: the FULL run's per-batch keyed DELETE fired across the ~254-batch loop (e.g. `LP-D6`'s own 4-row retraction, below), before-imaged (Fold A I-3) into the same per-run file. Idempotency additionally proven LIVE (not only structurally): back-to-back FULL runs produce a byte-identical `permit_parcels` table hash (`975fe9e1`), run 2's `records_updated: 0` | Fold A B-1 — `LG-24` (`executeGuardedDeleteByKey`), the first REAL class-F executor either LINK member has shipped | [pilot 7 assessment](2026-08-30-pilot7-link-parcels-assessment.md) §2 (`LP-D5`)/§4/§8; `.cursor/active_task.md` Fold A B-1 |
| LP-D6 | link_parcels (`scripts/link-parcels.js`, Strategy 3 Step 2) | 4 `spatial`-tier permits (`09 165576 HVA`, `13 260505 BLD`, `18 258177 FSU`, `22 104242 BLD` rev `00`) carry NULL `latitude`/`longitude`/`geocoded_at` yet `parcel_linked_at` is set; currently linked to `parcel_id 439990` (confidence 0.65) — corrected evidence, Fold C blocking item 1 (the plan's original "observed: parcel `id=1`" claim was an unverified guess, independently re-queried and corrected this pilot, LP-D6's own REQUIREMENT unaffected) | **OPEN — DEFECT.** A NULL-coordinate permit should never resolve to ANY parcel via a distance-based join — today's naive comparison silently returns an arbitrary row rather than erroring or excluding. No consumer depends on these 4 permits keeping their current link (`enrich_permits` re-derives fresh every run, Fold C item 7) | **CLOSED-MEASURED this commit (commit 8).** All 4 permits (`09 165576 HVA`, `13 260505 BLD`, `18 258177 FSU`, `22 104242 BLD`) confirmed **retracted — zero `permit_parcels` rows, 0/4 relinked** — exactly as declared. The `spatial_null_coordinate_permits` WARN separately fired at **9,628** on the same run — NOT this row's own 4-permit figure; that check's declared scope is broader (every NULL-coordinate permit reaching Strategy 3 Step 2 across the FULL ~254K-permit population, not only this row's narrower pre-existing-mislink population) — the two counts are complementary, not in tension; see assessment §8 item 7, flagged not conflated | Fold B item 2 (Cross-read Adversary) — explicit `WHERE v.lng IS NOT NULL AND v.lat IS NOT NULL` guard; locked both directions in `src/tests/steps/link_parcels/violations.test.ts` (commit 6) | [pilot 7 assessment](2026-08-30-pilot7-link-parcels-assessment.md) §2 (`LP-D6`)/§4/§8; Fold C blocking item 1 |
| LP-D7 | link_parcels output, consumed by `compute-cost-estimates.js` + `link-neighbourhoods.js` (beyond `enrich_permits`) | `scripts/compute-cost-estimates.js:161-167` — an INDEPENDENT dominant-parcel LATERAL (`ORDER BY parcel_id ASC LIMIT 1`, a DIFFERENT tiebreak than whatever `enrich-permits.js` uses), feeding `lot_size_sqm`/`frontage_m` into the cost model; `scripts/link-neighbourhoods.js:88-96,203-211` (same `sources` chain) — a narrower fallback-geometry exposure gated to permits lacking their own lat/lng | **OPEN — CONTRACT.** Fold A/B's own R-E ruling ("`enrich_permits` confirmed the SOLE consumer") is REFUTED — corrected by the Ground-truth grounder (Fold C blocking item 2) and this pilot's own commit-1 boundary-freeze widening of the same `grep -rln "permit_parcels"` search. The commit-8/9 "no phased rollout, sole product surface" framing (Fold B item 6, R-G) undercounts the actual blast radius by at least this second product-facing derivation | **PIN** — not this pilot's own defect to fix (a read-only exposure in a DIFFERENT step's own dominant-parcel selection); filed for commit 8/9's own owner to widen the product-exposure framing, no write-path change required in either consumer. **Grounding added commit 8** (same method the grounder used for `enrich_permits`, Fold C item 7): both `compute-cost-estimates.js` and `refresh-snapshot.js` are confirmed **always-full, no incremental filter** — the ~10,707 relinks self-heal on both consumers' very next run, no staleness/stranding risk, nothing filed to `review_followups.md` | commit 1's new finding, widened commit 2 per Fold C blocking item 2 | [pilot 7 assessment](2026-08-30-pilot7-link-parcels-assessment.md) §1 (commit 1)/§2 (`LP-D7`)/§4/§8; Fold C blocking item 2 |
| LP-D8 | link_parcels (`scripts/lib/compute/link-parcels.js:390`, `buildLinkMeta`) | `matches_tier_3_centroid: m.spatial` — the emitted `records_meta` field NAME literally contained "centroid," the exact terminology THE FIX/`LP-D1`/A-1 retired (Strategy 3 Step 2 is KNN-boundary-distance, not centroid-based, as of commit 7) | **OPEN — DEFECT (naming), found by executing: commit 8's live FULL run surfaced the field in a real `PIPELINE_SUMMARY`.** Declared in `emits[]` with `consumers: []` (no downstream reader, zero external break risk from a rename), but undermined the "nothing hidden, always observable" posture the whole pilot is built on | **CLOSED this commit (commit 9).** Adjudicated at cutover per the operator's explicit instruction not to leave it bare-open: the rename's zero consumer risk and zero behavior change made it cheap enough to fix rather than defer — deferring a known-misleading field name into the pilot's OWN cutover commit would itself violate the "nothing hidden" posture this pilot is built on. Renamed to `matches_tier_3_fallback` (pairs with the sibling `matches_tier_3_polygon` — Step 1 containment vs. Step 2 fallback — and matches the SQL builder's own existing `spatial_fallback_sql` naming). `src/tests/steps/link_parcels/violations.test.ts` + `src/tests/link-parcels.infra.test.ts` green after the rename; golden captures (`post/{permits,sources,standalone}.json`) re-taken | commit 8's live run, `PIPELINE_SUMMARY.records_meta.matches_tier_3_centroid` (pre-rename); fixed commit 9 | [pilot 7 assessment](2026-08-30-pilot7-link-parcels-assessment.md) §8/§9 |
| LP-D9 | link_parcels (`scripts/lib/compute/link-parcels.js`, Strategy 1a `address_points_exact` CTE) | `address_points` carries no street-type column of its own (`linear_name_normalized` is type-less), so the CTE's JOIN matched on house-number + street-name ONLY — no `street_type` predicate, unlike Strategy 1b (`exact`), which has always carried `pa.street_type_normalized = ip.street_type`. A same-house-number-different-street-type collision (measured live: `26 MEADOWVALE RD` vs `26 MEADOWVALE DR`, ~31.6km apart) resolves to whichever candidate wins the class/area/`address_point_id ASC` tiebreak, regardless of the permit's own declared type | **OPEN — DEFECT, pre-existing (not a pilot 7 conversion regression — confirmed identical in the OLD pre-conversion script via a read-only differential against the live DB, 216,531/216,531 address-tier matches identical). Fence: `1ba020bf` (2026-05-23, "WF1 #parcel-address-bridge Phase 2d — link-parcels Strategy 1a address_points bridge") introduced 1a to leverage the MORE AUTHORITATIVE `address_points` bridge, disambiguated by a declared "uniform 3-level rule" — `address_class_desc` > smallest `ST_Area` > `address_point_id ASC` — with no street_type check ever part of that rule (a genuine gap in the original design, not a deliberate omission the commit message argues for). Dormant under 5+ months of incremental-only processing (a permit correctly linked via 1b, before 1a existed or had bridge coverage for its address, was never re-evaluated once linked); fired at scale on commit 8's FIRST-EVER comprehensive FULL run. Measured system-wide (commit 8 investigation): 7,062/215,552 (3.3%) currently-linked `address_points_exact` rows conflict** | **CLOSED — commit 8a.** `p.street_type_normalized = ip.street_type` added to the JOIN + `(ip.street_type = '' OR p.street_type_normalized = ip.street_type)` added to a new WHERE clause, MIRRORING Strategy 1b's own predicate shape byte-for-byte (verified live: 1b's own "empty tolerance" is, in practice, non-permissive — its JOIN's hard equality already requires `pa.street_type_normalized = ''` when `ip.street_type` is empty, 0/8,439 sampled rows ever satisfy it — 1a now reproduces that SAME effectively-strict behavior, not a looser one). Red-first lock (`src/tests/db/link-parcels-address-tier-street-type.db.test.ts`, LP-D9): 2/3 fixture assertions RED on the unfixed compute (wrong parcel wins the id-ASC tiebreak for one of two collision directions; an empty-street_type permit over-matches), all 3/3 GREEN post-fix. New standing observability check `street_type_conflict` (WARN, `viol == 0`, `retighten_when: "zero rows"`, mirrors `spatial_null_coordinate_permits`' own shape) added — a whole-table audit run every invocation via a new `street_type_mismatch_sql`, not merely a run-scoped counter, so a pre-fix residual row an incremental run never revisits (or a future regression of the predicate) is never invisible again. **CONFIRMED-MEASURED post-fix (corrected FULL re-run, same session):** `street_type_mismatch_count` 7,046 → **0** (both the `street_type_conflict` check's own live PASS and an independent direct query agree); the R-O sample REPEATED on the address-tier flips (seed unchanged) reversed from `old_contains` 78.9%/`new_contains` 19.1% (broken) to `old_contains` 7.8%/`new_contains` **88.2%** (corrected, n=5,100) — decisive, not marginal; twice-run idempotency proven live on the corrected code (identical table hash `8a07b229`, `records_updated: 0` on the second run) | fence `1ba020bf` (2026-05-23); found + fixed commit 8a (2026-08-30), same session as the commit-8 FULL-run investigation that surfaced it | [pilot 7 assessment](2026-08-30-pilot7-link-parcels-assessment.md) §8/§8a; `src/tests/db/link-parcels-address-tier-street-type.db.test.ts` |
| LP-D10 | link_parcels (`scripts/lib/step/index.js` `runLinkKeyedPhase`, `scripts/lib/compute/link-parcels.js`) | The pre-conversion script's own `UPDATE permits SET parcel_linked_at = $RUN_AT WHERE (permit_num, revision_num) IN (this batch's own keys)` — fence `a21b7b01` (2026-04-01, "Batch UPDATE parcel_linked_at = NOW() for ALL evaluated permits, regardless of match count. Prevents infinite re-evaluation of unmatchable permits") — existed nowhere in the descriptor's `outputs.writes[]`, the compute, or `runLinkKeyedPhase` as of commit 9 | **OPEN — DEFECT, introduced by commit 7, caught at the WF6 output panel (Regression Guardian seat), not by any commit 1-9 authoring or review pass.** Verified by direct read: `grep -n "parcel_linked_at" scripts/lib/compute/link-parcels.js scripts/lib/step/index.js scripts/link-parcels.descriptor.json` found the column read-only (the incremental filter), never written; the descriptor's own `write_inventory.statements: 2` (pre-fix) explicitly enumerated only `permit_parcels`'s upsert (e1) and LG-24's delete (e2), never the permits watermark. The incremental filter (`parcel_linked_at IS NULL OR (geocoded_at IS NOT NULL AND parcel_linked_at < geocoded_at)`) depends on this write to ever EXCLUDE a no-match permit — without it, an unmatchable permit is re-evaluated on every incremental run forever, exactly the "infinite re-evaluation" class the fence exists to prevent. Live-measured at discovery: 15,849 eligible no-match permits, 0 currently exhibiting the runaway symptom only because this dev DB has had no new permit ingest since before the drop (a live production cron would have begun accumulating the symptom on its very next new unmatchable permit) | **CLOSED this commit (commit 10).** Restored as a THIRD declared write target (e3): `outputs.writes[2]`, table `permits`, class `set_based_scoped` + `set_source:"compute"` (LG-22 precedent — RUN_AT is a per-run bound value, not a declared constant), `guard:"none"` (LG-9/D-5 forbids guarding a run-clock column; `parcel_linked_at` is this target's only declared column, so there is nothing else to guard on — grandfathered.json's existing `link_parcels` entry widened to cover this second `guard:"none"` instance). SQL text is compute-authored (`watermark_update_sql`), executed via `write.executeGuardedUpdate`, ordered LAST in the per-batch transaction (after the upsert and LG-24's delete — a permit's linked/unlinked state is final before it is marked evaluated). New standing observability check `permits_watermarked` (INFO, mirrors `permit_parcels_written`'s own shape) + `records_meta.permits_watermarked_count`. Red-first lock (`src/tests/db/link-parcels-watermark.db.test.ts`, calls `runLinkKeyedPhase` directly — the frozen shell's own `assert_current_database` check refuses the ephemeral test container by design): RED on the unfixed code (both the no-match and the matched-permit fixture's `parcel_linked_at` stayed NULL), GREEN post-fix, both directions, extended to exercise BOTH of LG-24's own delete branches (zero-match cleanup and changed-match retraction) landing correctly alongside the watermark in the same transaction. Live-confirmed on the real dev DB: a genuine no-match fixture permit's `parcel_linked_at` was NULL before, stamped to the run's exact clock value after (`permits_watermarked_count: 1` in the real `PIPELINE_SUMMARY`), zero `permit_parcels` rows (evaluated ≠ linked) | fence `a21b7b01` (2026-04-01); dropped commit 7 (2026-08-30), found WF6 output panel + fixed commit 10 (2026-08-30) | [pilot 7 assessment](2026-08-30-pilot7-link-parcels-assessment.md) §2 item 12 (correction)/§10; `src/tests/db/link-parcels-watermark.db.test.ts` |
| LP-D12 | link_parcels (`scripts/lib/compute/link-parcels.js`, `scripts/link-parcels.descriptor.json`) | W1's FULL-mode mass retraction (`written.e1.retracted`) and LG-24's keyed DELETE stats (`written.e2`) were both computed on every run but never reached `records_meta` — a FULL run that retracted spatial-tier `permit_parcels` rows and then failed to rebuild them would read PASS on every declared row, and a retraction-without-rebuild was structurally invisible | **OPEN — GAP, present since commit 7/10 (both write targets existed; neither's counters were ever surfaced), caught at the WF6 output panel (observability seat FAIL).** `link_massing`'s own `mass_retraction_ratio` (`compute/link-massing.js:520-529`) is the standing precedent for exactly this class ("retracted and NOT rebuilt is the shape of a broken run") — `link_parcels` never got the analogous check when its own `retract:"all"`/`retract_when:"full_only"` (writes[0]) landed | **CLOSED this commit (commit 12).** New check `parcel_retraction_ratio` (bound, FAIL severity, `pct <= 0.05`, mirrors `mass_retraction_ratio`'s `{retracted, rebuilt, unrestored_ratio}` detail shape exactly), reading `written.e1.retracted`/`written.e1.inserted` (e1 IS the upsert target for `link_parcels`, unlike `link_massing` where e2 is upsert — same counters, different target key). New terminal `failed_parcel_retraction_ratio` (`fail_check`), discriminator-safe alongside `failed_write_privilege` (LP-D11) — `selectTerminal`'s `id.includes(discriminator)` correctly isolates each by the failing check's own id, no collision. `buildLinkMeta` now also surfaces `permit_parcels_deleted_count` (`written.e2.rows_changed`) directly in `records_meta`, closing the second half of the gap. One declared asymmetry from `link_massing`'s shape, named in the check's own why-text: the FULL retraction is scoped to `match_type='spatial'` only, while `rebuilt` counts inserts across ALL tiers this run (a retracted permit can legitimately re-land at tier 1/2), so the ratio is a conservative bound, not tier-exact. Live-proved on the real dev DB: an incremental run (unchanged corpus) reported `parcel_retraction_ratio: {retracted:0, rebuilt:0, unrestored_ratio:0}` and `permit_parcels_deleted_count: 0`; a genuine `LINK_PARCELS_FORCE_FULL=1` run reported `{retracted:12697, rebuilt:12697, unrestored_ratio:0}` (PASS) and `permit_parcels_deleted_count: 0` (LG-24's delete legitimately fired 0 times this run — no permit changed its kept `parcel_id`). No new test lock: verified neither `link_massing`'s own `mass_retraction_ratio` nor `failed_mass_retraction_ratio` terminal has one (`grep -rn mass_retraction_ratio src/tests/` = no matches) — matches precedent, not a new gap | present since commit 7/10 (2026-08-30), found WF6 output panel + fixed commit 12 (2026-08-30) | [pilot 7 assessment](2026-08-30-pilot7-link-parcels-assessment.md) §12 |
| LP-D13 | link_parcels (`scripts/link-parcels.descriptor.json` `checks[]`/`invariants[]`) | `spatial_null_coordinate_permits`'s why-text told the LP-D6 4-row historical-bug story exclusively, while the check itself counts every permit reaching Strategy 3 Step 2's LATERAL join with NULL coordinates this run — two different populations conflated into one narrative; an operator reading the why-text had no way to calibrate "normal" for the check that actually gates the WARN | **OPEN — CALIBRATION GAP, present since commit 5/7 (the why-text was copy-authored from LP-D6's own bug report, never generalized once the check's own scope was declared), caught at the WF6 output panel (observability seat WARN).** Live-measured on a real `LINK_PARCELS_FORCE_FULL=1` run (2026-08-30): `null_coordinate_permits: 11613` of 254,045 permits processed — three orders of magnitude larger than the 4-row population the why-text actually described. The SEPARATE `pp_spatial_null_coordinate_count` invariant (`last_measured.value: 4`) IS the narrow LP-D6 population (NULL-coordinate permits that wrongly carry a `match_type='spatial'` row) — its own why-text ("mirroring spatial_null_coordinate_permits above at invariant altitude") actively reinforced the conflation by implying the two rows measure the same thing at different altitudes, when they measure genuinely different things | **CLOSED this commit (commit 13).** Doc/descriptor-text only, no semantics changed — both checks already correctly scope to their own distinct populations; only the why-text was misleading. `spatial_null_coordinate_permits`'s why-text rewritten to describe BOTH populations explicitly, cite the live 11,613 magnitude, and state plainly that non-zero is expected/standing (retighten_when "zero rows" governs a broken-guard regression, not a target to reach). `pp_spatial_null_coordinate_count`'s why-text rewritten to stop claiming it "mirrors ... at invariant altitude" and instead name the real, narrower query it runs. The invariant-vs-check split itself was found to be CORRECT and necessary (two genuinely different SQL predicates, not a redundant duplication) — no re-scoping | present since commit 5/7 (2026-08-30), found WF6 output panel + fixed commit 13 (2026-08-30) | [pilot 7 assessment](2026-08-30-pilot7-link-parcels-assessment.md) §13 |
| LP-D14 | link_parcels (`docs/reports/2026-08-30-pilot7-link-parcels-assessment.md` §2, `scripts/link-parcels.notes.json`) | Rule 4/G-2 enforced-red at direct orchestrator `step:validate --all` run: 12 `preserved-in-compute` rows in §2's Intent Ledger, 11 with no `why`/`notes.json`/`checks[]` grounding cited in the row itself — the worst ungrounded count of any converted step, born this session | **OPEN — GAP, present since commit 2 (the Intent Ledger table was authored before Rule 4/G-2's own mechanical enforcement existed for this step), caught at direct orchestrator validator run.** All 11 violating rows are verbatim/byte-identical historical-fix ports whose original rationale survives as an inline code comment at the cited line range — never a missing rule, only a missing CITATION of where the rule lives | **CLOSED this commit (commit 14).** Added one consolidated `notes.json` `decisions[]` entry naming all 11 rows and the grounding rationale (Rule 2 — the ported code's own inline comment IS the rule's declaration; no separate why/checks[] entry needed for a mechanical, behavior-unchanged port). Appended `grounded in notes.json (why: R2, commit 14)` to each of the 11 rows' citation cell. `G-2` mechanical scorer confirms: 12 preserved-in-compute rows, 0 ungrounded | present since commit 2 (2026-08-30), found orchestrator validator run + fixed commit 14 (2026-08-30) | [pilot 7 assessment](2026-08-30-pilot7-link-parcels-assessment.md) §2; `scripts/link-parcels.notes.json` `decisions[]` |
| LP-D15 | link_parcels (`scripts/link-parcels.descriptor.json` `plausibility`) | `plausibility: "none"` with zero justification — schema-legal but this step has an obvious, well-grounded candidate: spatial-tier links' nearest-boundary distance already bounded by `spatial_match_max_distance_m` at write time, with no standing audit that the bound still holds after the fact | **OPEN — GAP, present since commit 7 (plausibility was never revisited once the initial conversion landed), caught at direct orchestrator validator run.** No genuine defect measured — this is a missing standing audit, not a found violation (live-measured: 0 violations) | **CLOSED this commit (commit 14).** Declared `spatial_fallback_distance_within_cap` (`source:"plausibility"`, WARN, `retighten_when:"zero rows"`), mirroring `link_massing`'s own `linked_parcel_null_centroid_count` row shape exactly — checks `ST_Distance(parcel.geom, permit point) > 100` over `match_type='spatial'` rows, a genuine cross-check against the SAME write's own write-time predicate (`buildMatchSql`'s `spatial_fallback_sql`), catching a row that was compliant when written but is no longer (a cap-config tightening, or the write-time predicate itself regressing). Live-measured: 0 violations, 637ms. `sample_n:1` — a bootstrap disposition under this session's budget, not yet R-T's full >=5-timings/>=2-session median; filed for a future WF3 to accumulate | present since commit 7 (2026-08-30), found orchestrator validator run + fixed commit 14 (2026-08-30) | [pilot 7 assessment](2026-08-30-pilot7-link-parcels-assessment.md) §14; `scripts/link-parcels.descriptor.json` `plausibility[0]` |
| LP-D11 | link_parcels (`scripts/link-parcels.descriptor.json` `terminals[]`) | `pre_write_refused` (`kind:"fail_check"`) was the ONLY `fail_check` terminal, describing "a `when:\"pre_write\"` check FAILED" — but all 14 declared `checks[]` are `when:"post"` (verified live, `node -e` dump of `descriptor.checks.map(c=>c.when)`), so `makePreWriteGate` (which filters on `when==='pre_write'`) returns `null` and the pre-write gate is a permanent no-op for this step — the terminal's own described trigger mechanism is structurally unreachable | **OPEN — DEFECT, introduced commit 7 (copy-pasted from a sibling template without verifying it applies), caught at the WF6 output panel (observability seat).** `selectTerminal`'s own discriminator-miss fallback (`scripts/lib/step/index.js:213-219`: `narrowed = byKind.filter(t => t.id.includes(discriminator))`, `pool = narrowed.length > 0 ? narrowed : byKind`) silently selected `pre_write_refused` whenever ANY `fail_check`-kind outcome fired and no terminal id contained the actual failing check's own id — since `pre_write_refused` was the ONLY `fail_check` terminal, it was ALWAYS the fallback, regardless of which check actually failed. Most plausibly `write_privilege` (the step's only currently-FAIL-severity check), whose real failure mode (RLS enabled, zero policies, role does not bypass — every statement affects 0 rows with no error, the WRITE DID EXECUTE) is the OPPOSITE of what the terminal's own why-text claimed ("no write was issued, permit_parcels is untouched") — an actively false stamp on `records_meta.terminal`, not merely a missing one. `compute_centroids` (0 pre_write checks) correctly declares ZERO `fail_check` terminals; `link_parcel_addresses` (also 0 pre_write checks) declares 2, both correctly discriminator-matched to real checks that exist — confirming the established convention is "declare a `fail_check` terminal only for a check that can actually trigger it," which `link_parcels`'s own `pre_write_refused` violated | **CLOSED this commit (commit 11).** Renamed `pre_write_refused` → `failed_write_privilege`, matching `link-massing.descriptor.json:979-988`/`link-wsib.descriptor.json:648-652`'s own identical shape verbatim (both siblings already correctly declare this exact terminal for this exact failure mode) — `failed_write_privilege`.id now CONTAINS `write_privilege`, so `selectTerminal`'s own discriminator correctly narrows to it specifically when `write_privilege` fails, instead of falling back by elimination. why-text corrected to the true failure mode (RLS-zeroed, write executed and wrote nothing) and records the WF6 finding + the false-claim history in place, not silently. No new test lock added — verified neither sibling pilot (`link_massing`/`link_wsib`) has one for their own `failed_write_privilege` terminal either, so none is owed here beyond matching precedent | fence: none (a descriptor-declaration defect, not a code regression against a prior commit's own intent) — introduced commit 7 (2026-08-30), found WF6 output panel + fixed commit 11 (2026-08-30) | [pilot 7 assessment](2026-08-30-pilot7-link-parcels-assessment.md) §11 |
| RS-D1 | refresh_snapshot (`scripts/refresh-snapshot.js:642-643` pre-conversion; `scripts/refresh-snapshot.descriptor.json` `sharing.varies_by_chain.phase` post-conversion) | `const snapshotPhase = chainId === 'sources' ? 13 : chainId === 'coa' ? 7 : 18` — `deep_scrapes` silently shares `permits`' phase number (18), never given its own branch | **OPEN — GAP, present since `5baaed5a` (2026-03-26, the ternary's own introducing commit — `deep_scrapes` already existed in `manifest.json` per `5c953a61`, 2026-03-11, 15 days earlier, and was never given its own branch by that commit or any successor), caught during PH-3 intent-ledger review (pilot 8 commit 2).** Low blast radius confirmed (only 2 structural/shape test files reference `audit_table.phase`, no admin surface distinguishes it) but a real undeclared magic-shared-literal, Rule 1 ("nothing hidden") | **CLOSED this commit (commit 7).** `sharing.varies_by_chain.phase` now declares `{permits:18, coa:7, sources:13, deep_scrapes:4}`, all 4 chains distinct, read generically by the shared audit-table builder (no per-chain `process.env`/ternary in compute.js at all — Rule 2 also improves as a side effect) | present since `5baaed5a` (2026-03-26), found pilot 8 commit 2 + fixed commit 7 (2026-08-31) | [pilot 8 assessment](2026-08-31-pilot8-refresh-snapshot-assessment.md) §2 (Finding 4), Fold B |
| RS-D2 | refresh_snapshot (`scripts/refresh-snapshot.js:421-494` pre-conversion; `scripts/lib/compute/refresh-snapshot.js` `buildRow()` post-conversion) | `costEst` and `coaFunnel` — the 2 most-recently-added optional query blocks — leave the pre-declared zero/null default on failure instead of calling `getPrevSnapshot()`, unlike the other 4 optional blocks; violates the file's own declared carry-forward policy (`:320-322`, origin `fd14dc53`) | **OPEN — DEFECT, present since `fd14dc53` (the carry-forward policy's own introducing commit added the other 4 blocks' carry-forward but never extended it to `costEst`/`coaFunnel` when those 2 were added later), caught during PH-3 intent-ledger review (pilot 8 commit 2; operator ruling on Ask 1, pre-commit: FIX).** A real, live-measurable inconsistency — on any query failure, a brand-new `data_quality_snapshots` row would write `cost_estimates_total=0` etc., exactly the trend-line-destroying behavior the file's own comment states the pattern exists to prevent | **CLOSED this commit (commit 7).** `costEst`'s failure path now reads the PRIOR `data_quality_snapshots` row's own 4 `cost_estimates_*` columns (mirroring massing/schema/SLA/inspections); `coaFunnel`'s failure path reads the PRIOR RUN's own reported audit values (`prior.audit_table.rows`) since its 7 fields are audit/telemetry only, never written to the table — no matching row column exists to carry forward from. A new declared WARN check (`optional_query_failed`) makes any carried-forward block visible in the audit row every run (nothing-hidden — previously only a `log.warn` line) | present since `fd14dc53`, found pilot 8 commit 2 + fixed commit 7 (2026-08-31) | [pilot 8 assessment](2026-08-31-pilot8-refresh-snapshot-assessment.md) §2 (Finding 5), Fold B |
| LDG-D1 | link_parcels (`scripts/link-parcels.descriptor.json` `inputs.reads.steps` declared `[]`; cross-checked by `src/tests/step-conformance.infra.test.ts` `LDG-4`) | Descriptor declares `inputs.reads.steps: []` but the cross-step ledger derives TWO real converted upstream producers it omits: `compute_centroids` (`parcels.centroid_lat`/`centroid_lng`) and `link_parcel_addresses` (`parcel_address_points.parcel_id`/`address_point_id`, a table `inputs.reads.tables[]` already declares reading) | **OPEN — GAP, measured 2026-09-03 (LDG-4 cross-check, commit 5 of `.cursor/wf1_cross_step_ledger_active_task.md`).** `link_parcel_addresses` is a CURRENT gap; `compute_centroids` is entangled with this branch's own LP-D12..15 fixes (`58664257`) not yet having a completed run recorded anywhere (checked live, local DB + cloud `aws-0-ca-central-1`, `permits:link_parcels` 2026-09-02T15:34:50Z) — may partly self-resolve on merge + re-run, re-measure before landing | **PIN** — out of this WF's scope: fixing changes live `ledgerGatedSkip` staleness-gating behavior for a converted LINK step and needs its own reviewed WF3, not a drive-by edit inside ledger-tooling. Locked exact-equality in `step-conformance.infra.test.ts`'s `KNOWN_GAPS`, never fixed here | measured 2026-09-03 (LDG-4 cross-check), filed `review_followups.md` HIGH row ("LDG-4 cross-check, measured 2026-09-03") | `src/tests/step-conformance.infra.test.ts` `KNOWN_GAPS`/`LDG-4`; `docs/reports/review_followups.md` |
| LDG-D2 | refresh_snapshot (`scripts/refresh-snapshot.descriptor.json` `inputs.reads.steps` declares `link_parcels`/`link_massing`/`link_wsib`; cross-checked by `src/tests/step-conformance.infra.test.ts` `LDG-4`) | Descriptor declares 3 converted upstream steps the ledger's column-overlap derivation does not find, because a RECORDER's dependency on them is an ORDERING/lifecycle wait ("snapshot after these complete"), not a shared-COLUMN data read — a column-lineage-only ledger structurally cannot derive a pure ordering dependency | **NOTED, not a defect — measured 2026-09-03 (LDG-4 cross-check).** Documents a structural ledger limitation (ordering vs. column-lineage dependency), not a found violation | **NO ACTION** — the RECORDER archetype's declared `reads.steps[]` legitimately carries more than column overlap can prove; re-measure if the ledger ever gains a `writes.invalidates`/ordering-edge concept. Locked exact-equality in `step-conformance.infra.test.ts`'s `KNOWN_GAPS` as a named, asserted-exact exemption rather than a silently widened tolerance | measured 2026-09-03 (LDG-4 cross-check), filed `review_followups.md` LOW·NOTED row ("LDG-4 cross-check, measured 2026-09-03") | `src/tests/step-conformance.infra.test.ts` `KNOWN_GAPS`/`LDG-4`; `docs/reports/review_followups.md` |
