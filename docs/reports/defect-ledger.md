# Defect Ledger — fleet register (Spec 123 §6 G6 / G8)

> Generated **by hand** for Pilot 1 (`assert_schema`, 2026-08-25) — the first register in the repo. From S5 onward this file is **generator-owned** (`scripts/violations/plan-claims.mjs` family); do not hand-edit rows after that point, regenerate.
> ID scheme: `<STEP-PREFIX>-D<n>`; a row is OPEN until the closing commit lands and the differential points at it (G8: *every explained diff points at a Defect Ledger ID*). Anchors are `file:line` at the HEAD stated in the source assessment and rot with the file — re-derive before citing.

| ID | Step | Anchor (HEAD `8b857169`) | One-line | Status | Closes at | Source |
|---|---|---|---|---|---|---|
| AS-D1 | assert_schema | `scripts/quality/assert-schema.js:546` (pre-conversion) | sources verdict `sourceErrors.length` — not row-derived | **CLOSED · 8b** | **peel 8b** — the cascade became row-derived when the library landed (`scripts/lib/step/verdict.js` `deriveVerdict(rows)`, commit 7 `f843c042`); 8b closes the remaining half by making every error a check-id-keyed ROW (`report(<checkId>, {error})` → `checkRow`), so no verdict input exists outside `audit_table.rows`. Both directions locked by `#199` (no `verdictCascade` / parallel boolean in the compute) and `#165` (sabotage → FAIL row) | [pilot 1 assessment](2026-08-25-pilot1-assert-schema-assessment.md) §4.4 |
| AS-D1b | assert_schema (fleet) | `scripts/lib/` (absent) | no shared `verdictCascade`; 13 per-script copies | OPEN | DEFERRED — Spec 120 §9.3 ① | same |
| AS-D2 | assert_schema | `scripts/quality/assert-schema.js:290-602` | process-kill strand inside the 21-request window (throws now closed by `f32b1485`) | OPEN (narrowed) | DEFERRED — runner-level | same |
| AS-D3 | assert_schema | `scripts/quality/assert-schema.js:282-284` | ledger INSERT failure swallowed, `runId` null | OPEN · PIN | commit 7 (verify library `openLedgerRow` does not swallow) | same |
| AS-D4 | assert_schema | `scripts/quality/assert-schema.js:567` | finalize UPDATE failure `.catch(warn)`; window relabels `failed` but real meta lost | OPEN (partially mitigated) | commit 7 (verify `finalizeLedgerRow`) | same |
| AS-D5 | assert_schema | `scripts/quality/assert-schema.js:539`, `:536` (pre-conversion) | hand-maintained `sources_checked` = 18 + unrecorded regex-token obligation | **CLOSED · 8c** | **peel 8c** — the literal is gone (the audit table is one row per SELECTED check, built by `verdict.js buildAuditTable`) and so is the token obligation (peel 8b). §5.5's dispatch table makes the remaining half structural: `compute()` iterates `ctx.checks` and `src/tests/step-conformance.infra.test.ts` asserts dispatch keys ≡ descriptor check ids in BOTH directions and in order, so adding a source without adding its check — or vice versa — is a build failure rather than a stale constant | same |
| AS-D6 | assert_schema | `scripts/quality/assert-schema.js:473-483`, `:503-504`, `:535-537` (pre-conversion) | error attribution by substring / alternation regex | **CLOSED · 8b** | **peel 8b** — the `includes('permit'/'coa'/'ckan')` tests and the `zoning\|ravine\|centreline` alternation are retired (`knowingly-retired`, operator, assessment §2.5). Each check owns its try/catch and reports `{error}` under its own id; the "add a source ⇒ add a regex token" obligation no longer exists. The coarse outer catch that suppressed every later check is removed with them | same |
| AS-D7 | assert_schema | `scripts/quality/assert-schema.js:527` | `checks_passed` is `'all'`/`undefined`, never a count | OPEN · PIN | commit 7 (declared diff) | same |
| AS-D8 | assert_schema | `scripts/quality/assert-schema.js:118,162,205,224,251` | 21 HTTP requests, no timeout/retry/abort | OPEN · PIN (`network.timeout: "none"`) | DEFERRED — peel-8c candidate, own lock | same |
| AS-D9 | assert_schema | `scripts/quality/assert-schema.js:605` | lock contention → `return;` with no step-level emit | OPEN · PIN | commit 7 (library SKIP emit + `self_skipped`; declared diff) | same |
| AS-D10 | assert_schema | file (`module.exports` = 0) | no exports; 13 source-text test sites across 6 files | OPEN | commit 7 (compute module; sites re-homed) | same |
| AS-D11 | `scripts/lib/compute/assert-schema.js` `fetchCsvHeaders`/`fetchGeoJsonPropertyKeys` | Naive header parsers carried verbatim from the old file: `split(',')` breaks on quoted commas; no UTF-8 BOM strip; `res.text()` reads the full body when a server ignores `Range`; GeoJSON properties parsed by regex inside a fixed 8 KB window | OPEN (pinned at conversion; Spec 123 §3.1) | fix-after — OUTPUT panel 2026-08-25 (Gemini/DeepSeek, grounder-adjudicated C-class) |
| AS-D12 | `scripts/lib/compute/assert-schema.js` `validateTypeSample` | Lenient sample: empty sample → pass; ≥1 parseable `EST_CONST_COST` of 20 → pass (plus the pinned non-OK → `true`, `limitations[0]`) | OPEN (pinned) | fix-after — same panel |
| AS-D13 | `scripts/lib/compute/assert-schema.js` `checkUrlAccessible` | `HEAD`-only reachability, no `GET Range: bytes=0-0` fallback → false FAIL on HEAD-hostile hosts | OPEN (pinned) | fix-after — same panel |
| LR-D1 | load_ravines | `scripts/load-ravines.js:441` (HEAD `e94014f9`) | `ravine_geometry_skipped_source_id` WARN pushed once per non-carried feature — audit-table row count unbounded | **CLOSED · 8b** | **peel 8b** — the metric is retired and cannot return: it is not a declared check, and `ctx.report` refuses an undeclared id, so the unbounded shape is structurally unreachable rather than merely absent. The dropped ids travel in ONE row’s `observation.detail` on `ravine_geometry_skipped_pct`, capped at `MAX_DETAIL_KEYS` (50) with the EXACT `dropped_count` and a `dropped_ids_truncated` flag beside them — the row COUNT is bounded by the check count and the row SIZE by the cap, and the reader is never misled about how much was lost. The pass/fail bound is computed from `dropped_count`, never from the truncated list. Locked both directions in `src/tests/steps/load_ravines/violations.test.ts` (3× cap → one report; under-cap → complete list; clean load → bare ratio; the retired id → throws) | [pilot 2 assessment](2026-08-25-pilot2-load-ravines-assessment.md) §4.4 |
| LR-D2 | load_ravines | `scripts/load-ravines.js:291-294` (pre-conversion) | prior-run read `.catch(warn → null)`: all drift guards + both skip tiers silently degrade to "first run"; no audit row | **CLOSED · 8a** | **peel 8a** — the posture is DECLARED, not decided in a catch block: `staleness.on_prior_run_error` (`fail_step` \| `warn_row`, absent = `fail_step`) is read by `scripts/lib/step/staleness.js priorRunErrorPosture` and applied by `readPriorEmitWithPosture`. `load_ravines` declares `fail_step` with a why — the baseline feeds both gate tiers and every drift ratio, so running without it writes a legal-constraint surface with every guard 0-by-definition. The `warn_row` arm proceeds but OWES `priorRunErrorRow()` → a `prior_run_read_failed` WARN row rendered through `buildAuditTable`'s `extraRows` (its first consumer), which moves the row-derived verdict off PASS. There is no arm that returns null quietly. Both postures + both directions locked in `src/tests/step-library.logic.test.ts` | same |
| LR-D3 | load_ravines | `scripts/load-ravines.js:308,371,415,454,553` + `scripts/lib/pipeline.js:475` | FAIL verdict exits 0 (`pipeline.run` discards the return); chain `completed_with_errors` (`run-chain.js:838`), no halt; the `completed` row is what `enrich-ravines.js:34-36` reads | OPEN · PIN (`blocking:false`) | **DEFERRED** — promoting a check to `blocking:true` is its own peel + lock (plan D-8) | same |
| LR-D4 | load_ravines | `scripts/load-ravines.js:459` (pre-conversion) | unreachable `etag` / sha1(last-modified) / `String(runAt)` `source_dataset_version` fallbacks (md5 always present on the write path); last is an unstable Date string | OPEN · INTENT-UNKNOWN | **DEFERRED past peel 8c** — the peel closed the threshold-source half and left this one open on purpose: retiring a fallback is a §7.1 adjudication and no operator ruling was given. The agent that discovers may not retire | same |
| LR-D5 | load_ravines | `scripts/load-ravines.js:176-180`, `:423` | BIGINT `source_id` > 2^53 loses precision (#411); `:423` `Number()` Map key miss → silent `skipped`. Live max 14,041,969 | OPEN · PIN | **DEFERRED** — latent; declared WARN-and-skip check when ids expand | same |
| LR-D6 | load_ravines | `scripts/load-ravines.js:556` (pre-conversion) | lock contention → bare `return;`, no step-level emit / `audit_table` | **CLOSED · 8b** | landed at commit 7 (`skipRecordsMeta` + `RUN_STATUS.SELF_SKIPPED`, declared diff D-10); **CONFIRMED at 8b** against THIS descriptor rather than assert_schema’s: a contended write-class run emits `skipped: true` with an `audit_table` whose verdict is row-derived, the declared `lock_held_elsewhere` terminal carries `status: self_skipped`, the compute never runs, the acquisition seam never reaches the network (it is INSIDE the lock), no statement touches `ravines`, and `records_new`/`records_updated` read null rather than 0. `src/tests/step-library.logic.test.ts` | same |
| LR-D7 | load_ravines | `ravines` live catalog (`relrowsecurity=true`, 0 `pg_policies`; role `postgres` `rolbypassrls=true`) | writes succeed only via RLS bypass; a non-bypass role upserts/deletes 0 rows silently → 0/0/0, PASS, `completed` | OPEN | **commit 7** — descriptor `guards` precondition (bypass-or-policy) + `write_discipline.expected_change_ratio` on a forced run (A-3) | same |
| LR-D8 | load_ravines | `scripts/load-ravines.js:46-53` | six `ConfigSchema` tunables declared, 0 registered (`logic_variables` 422 rows, 0 `%ravine%`; seeds 0) — Zod-default literals (P4; supersedes #421) | OPEN | **commit 7** — `config[]` ×6 + seeds + `GlobalConfigCard` group; `config.js` throws on unregistered names | same |
| LR-D9 | load_ravines | `scripts/lib/step/index.js` `runIngestPhase` (post-conversion) — pre-conversion `scripts/load-ravines.js:402-417` (L7) and `:443-454` (L8) at `33786d1a` | **write-before-checks**: the INGESTOR phase order (acquire → validate → write → score) retired Spec 59 L7/L8's "FAIL aborts BEFORE any DB write" guarantee — the pre-conversion loader `return`ed `{failed:true}` above `pool.query(VALIDATION_SQL)` / `withTransaction`, the converted step scored the same checks after the write was committed, so the FAIL row reported bad data that was already in `ravines` | **CLOSED · Fold C** | **introduced at commit 7; closed by the operator §7.1 ruling of 2026-08-26.** `checks[].when` gains `pre_write` (`scripts/steps/_schema/step.schema.json`), the runner scores those checks between the read-only geometry validation and the first write statement (`makePreWriteGate` + `runIngestPhase`'s gate in `scripts/lib/step/index.js`), and an unaccepted FAIL SKIPS the write entirely — no transaction opened, `ravines` untouched, the remaining checks scored over a zeroed `written`, terminal `failed_<check_id>`. Acceptance (ruling A-5) moves the DECISION only: `RAVINE_ACCEPT_FEATURE_COUNT_DRIFT=1` lets the write proceed and never suppresses the FAIL row; L8 declares no override, so its abort is unconditional. Exit semantics unchanged (every check stays `blocking:false`, D-8). Locked both directions — `src/tests/steps/load_ravines/violations.test.ts` (L7/L8 × accepted/unaccepted × the reversion detector: `when:"post"` ⇒ no gate ⇒ the write runs on a FAIL) and `src/tests/step-library.logic.test.ts` (the library gate against a fake pool). Differential: skip path + forced reload all IDENTICAL (D-20) | [pilot 2 assessment](2026-08-25-pilot2-load-ravines-assessment.md) §2.3 / Fold C |
| LR-D10 | load_ravines | `scripts/lib/step/acquire.js` `downloadArchive` (post-conversion) — pre-conversion `scripts/load-ravines.js` had the same shape | **no byte cap on the download.** `execution.needs_disk_mb` is DECLARED and never enforced: `downloadArchive` streams `res.body` to disk through `hashThrough` with no ceiling on `bytes`, and the only limit in force is the network timeout. A publisher that replaces a 7.6 MB archive with a 20 GB one fills the temp volume before any check exists to observe it — the `finally` removes the temp root, so the failure surfaces as an ENOSPC from an unrelated process rather than as a lossy source. Bounded in practice by a frozen 2022 dataset, which is why it is LOW and not the defect that stops the pilot | OPEN · LOW | DEFERRED — library-wide (`acquire.js` is step-agnostic): abort the stream once `bytes` exceeds `needs_disk_mb` (or a `max_bytes` sibling), and emit the measured byte count as a declared check so the ceiling is observable on the happy path too. Filed to `docs/reports/review_followups.md` under the pilot 2 OUTPUT-panel DEFERs | pilot 2 OUTPUT panel (2026-08-27, Fold D) |
| LR-D11 | load_ravines | `scripts/load-ravines.descriptor.json` `outputs.writes[0].write_discipline.idempotent_rerun` + `scripts/lib/step/acquire.js` `contentHashSkip` | **`idempotent_rerun: "zero_writes"` is not ASSERTED on a forced same-bytes rerun.** The tier-2 content-hash gate makes the UNFORCED second run skip before the write, so the property the declaration names — run the WRITE twice over identical bytes, the second changes nothing — is never exercised by the gate that appears to cover it. `ravine_rows_changed_ratio` measures `rows_changed / rows_scanned` against `pct <= 0.5` on whatever run happens; nothing compares it against 0 on the specific run where the declaration says it must be 0. D-19 measured the pair by hand (forced run 1: 21 updated; forced run 2: 0/0/0) — a manual observation, not a standing check | OPEN · LOW | DEFERRED — needs either a declared check bound to the `force_run` + unchanged-`content_hash` conjunction (`ctx.overrides.force_run && ctx.acquired.content_hash === ctx.prior.content_hash ⇒ rows_changed == 0`) or a golden-harness assertion that runs the forced capture twice. Filed to `docs/reports/review_followups.md` under the pilot 2 OUTPUT-panel DEFERs | pilot 2 OUTPUT panel (2026-08-27, Fold D) |
| LM-D1 | link_massing | `scripts/link-massing.js:709-711` (HEAD `73c90d24`, pre-conversion) | `records_new: 0` is a hardcoded literal (`d2050cfc`, premise "never INSERT" — false since the upsert); `records_total = processed` counts the permanently-unmatchable 1,395-parcel tail; `records_updated = parcelsLinked` counts parcels while `buildings_upserted` counts rows | **CLOSED · 8b** | landed at commit 7, **CONFIRMED at 8b** — the three counters come from `written.e2.{scanned, inserted, updated}` resolved by `deriveCounters` against a PER-TARGET keyed `written` (LG-5), so "records_new" can only mean the upsert's inserts and never the primary-clear's rewrites; the compute returns NO `counters` at all (conformance closes the `c.records_new ??` bypass) and the `:710` literal is gone with the file. The measured steady state now reads `records_total = records_new = records_updated = 0` on an incremental run instead of `1395 / 0 / 0` — 1,395 was the permanently-unmatchable tail reported as work done, and 0 was a literal a guarded upsert's `rowCount` could never have measured | [pilot 3 assessment](2026-08-27-pilot3-link-massing-assessment.md) §4.4 |
| LM-D2 | link_massing | `scripts/link-massing.js:691-693`, `:705-706` | the `:705` cascade IS row-derived (`fc038790`) — the defect is a hand-rolled per-script copy (AS-D1b fleet class) plus the `link_rate` row's SEVERITY chosen by `hasPostGIS && processed > 0`, a boolean no row records (an empty-parcels or JS-path run degrades a broken-geometry signal to WARN) | **CLOSED · 8a** | **peel 8a** — both halves, and the second one structurally. (a) The hand-rolled copy is gone: the step contributes no cascade at all, `scripts/lib/step/verdict.js deriveVerdict(rows)` is the only place a verdict is computed, and `#199` fails if a `verdictCascade` / `.some(r => r.status === …)` shape reappears in the compute. (b) The BOOLEAN-DRIVEN SEVERITY is unreachable: `hasPostGIS` no longer exists (A-8, LM-D7) and `link_rate` carries a DECLARED `severity: "FAIL"` that `checkRow` applies to the reported ratio — measured, a grep for `'FAIL'`/`'WARN'` string literals across `scripts/lib/step/*.js` + `scripts/lib/compute/link-massing.js` returns exactly 6 hits, all of them in `verdict.js` (the lattice + the cascade), `index.js` (reading `row.status` back off the rows) and `staleness.js:215` (`priorRunErrorRow`, itself a row). The empty-work condition the boolean was standing in for is now `guards.empty_source` + the `empty_source_guard` `pre_write` check, which is a ROW with its own severity rather than a modifier on someone else's | same |
| LM-D3 | link_massing | `scripts/link-massing.js:320-323` | E1 `UPDATE … SET is_primary = false WHERE parcel_id = ANY($1) AND is_primary = true` is unguarded (x-banned-for-new) and its `rowCount` is discarded; measured: 0 rows affected on BOTH modes by construction (W1 has emptied the FULL scope; `NOT EXISTS` excludes linked parcels in INCREMENTAL) — fence `5bb31faf` is latent, load-bearing only if the read scope and W1's scope diverge | **CLOSED · 8b** | landed at commit 7, **CONFIRMED at 8b** — E1 is `outputs.writes[0]`, `set_based_scoped`, `guard: "none"` with a `guard_why` that says why a change guard would be WRONG rather than merely redundant (`is_distinct_from` skips exactly the still-primary row whose flag must come down before a DIFFERENT building takes it), grandfathered against `5bb31faf` in `scripts/steps/_schema/grandfathered.json`, and `written.e1.{scanned, updated}` is recorded per target so the measured 0 is an observation rather than an absence. `idempotent_rerun: declared_drift` + `declared_drift` state the bound (at most one row per batch parcel, never the table's row count) instead of claiming a `zero_writes` this target cannot honour | same |
| LM-D4 | link_massing | `scripts/link-massing.js:45` vs `:277` | `BATCH_SIZE = 500` (JS path, `66bfd529`) vs `pipeline.BATCH_SIZE = 1000` (PostGIS path, `7c75e92e`) for the same work — undeclared, unexplained divergence | OPEN | **commit 7** — closes with the A-8 retirement of the JS path; one `execution.batch: 1000` with a `why` | same |
| LM-D5 | link_massing | `scripts/link-massing.js:691`, `:701` | the verdict-bound link-rate floor is the bare literal `50` in two places (comparison + rendered `'>= 50%'`), `fcd6ff68` with no recorded rationale (INTENT-UNKNOWN on the value); 0 registered `logic_variables` matching `%link_rate%` (428 rows live); live rate 99.71% ⇒ a 49.7-point dead band | OPEN · PIN | **peel 8c** — `link_massing_link_rate_fail_pct` (default 50, 0–100, `on_invalid: fail`, GROUP "Data Quality Thresholds") + `checks[].limit_from_config`; the VALUE is the operator's to move | same |
| LM-D6 | link_massing | `scripts/link-massing.js` (absent counter); `:287` | no link-quality counter: 48,561 of 103,530 `nearest` primaries (46.9%) have `footprint_area_sqm > lot_size_sqm` (33,184 exceed 2×), 13,347 of 381,605 `centroid_in_parcel` (3.5%); 58,050 parcels carry `existing_data_quality_flag = 'footprint_exceeds_lot'` one step downstream; NULL-`geom` parcels are silently dropped by `ST_Contains → NULL` (the PostGIS twin of the Spec 122 §1.6 swallow) | **OPEN · re-dispositioned (R-H)** | **peel 8b** — `nearest_footprint_gt_lot_count`, a check keyed by `match_type`, reported on every run out of the same `cumulative_sql` round trip as the link rate (no extra statement; +887 ms warm / +2.9–3.6 s cold for the two joins over the 485,135 primaries). Re-measured post-tiebreak: **50,790 of 103,530 nearest primaries (49.06%)** and **13,347 of 381,605 centroid_in_parcel primaries (3.50%)** — and THE TIEBREAK ITSELF MOVED THIS (+2,144 from 48,646/47.0%): resolving a distance tie toward the LARGER footprint puts more primaries above their lot line. Declared, not incidental; the mirror-image drop in shared primaries is on LM-D11, with the denominators in the row so the numerator can be sized. What it closes is that the links were produced here, shipped, and NULLed one chain-step later by enrich-parcels with NOTHING at the link counting them. The NULL-`geom` silent drop is separately declared in `limitations[]` (live count 0). The 0/42 sanity-rule blind spot over `parcel_buildings` remains the filed `review_followups` WF3. **Amended 2026-08-28 (R-H):** peel 8b's INFO-BY-RULING (A-6) disposition conflicts with Spec 48 §4.9 (`tasks/lessons.md:117`) — a standing non-zero metric must be WARN with a declared, machine-observable retighten condition, never INFO-by-taste. Re-dispositioned OPEN, **carried to pilot 4**: `nearest_footprint_gt_lot_count` becomes a WARN check with a declared retighten trigger (defined at pilot 4), superseding the INFO row landed at peel 8b | same |
| LM-D7 | link_massing | `scripts/link-massing.js:391-660` + `:51-87`, `:97-110`, `:111-134`, `:33-35`, `:45-46` | the JS fallback path — 345 lines (46.6%), 0 of 11 audited runs since `7c75e92e` (2026-04-02) took it (the only JS-path row, 1036, predates the dual path); no E1 clear (asymmetric with B-8), `BATCH_SIZE 500`, haversine, reads `geometry` (3857 JSONB) not `geom`, and the `:554-559` silent swallow Spec 122 §1.6 names by anchor | **CLOSED · 8a** | **retired at commit 7, COMPLETED at peel 8a.** Commit 7 landed the `knowingly-retired` ruling (operator, 2026-08-27, A-8 override) and the `{kind: extension, name: postgis, on_missing: fail}` precondition; the COLUMN half of the declared replacement was missing, so a database with postgis installed but a pre-039 `building_footprints` would have failed on a raw `column bf.geom does not exist` from inside the first batch query rather than refusing before the first read. Peel 8a adds `{kind: column, name: "building_footprints.geom"}` and `{kind: column, name: "parcels.geom"}`, both `on_missing: "fail"` — fence `45906c31`'s intent (a schema gap must not select an algorithm) carried forward now that there is no algorithm to select. Probe proven both directions against the live catalog 2026-08-27 (`information_schema.columns`: both present; a fabricated `building_footprints.nope` absent); I/O cost 2 catalog lookups per run, sub-millisecond. C-6 dissolves (nothing pinned because nothing survives); declaration retired commit 9 | same |
| LM-D8 | link_massing | `scripts/link-massing.js:739` | lock-91 contention → bare `return;`; the SDK skip emit carries no `audit_table`; live: 6 `skipped` rows under `permits:link_massing`, every one with `records_meta` NULL | **CLOSED · 8a** | landed at commit 7, **CONFIRMED at 8a against THIS descriptor**: `scripts/lib/step/index.js` `skipRecordsMeta(descriptor, 'advisory_lock_held_elsewhere')` emits `{skipped: true, reason, audit_table}` whose verdict comes from `deriveVerdict(rows)` over two INFO rows — never a bare `return` — and the run is finalized `RUN_STATUS.SELF_SKIPPED`, which is the status the declared `lock_held_elsewhere` terminal names. The 6 live `skipped` rows under `permits:link_massing` all carry `records_meta` NULL; every future one carries a table. The skip meta deliberately carries no `config` stamp: `hoisted_above_gate: true` means an out-of-bounds threshold has already thrown ABOVE the lock, so it cannot hide behind the green SKIPPED summary (A-5 / B-13) ⚠️ **DEMONSTRATED LIVE, by accident, 2026-08-27 19:09Z**: a peel-8b capture launched while an orphaned forced relink still held lock 91 self-skipped in 40 s and emitted exactly the declared shape — `{skipped: true, reason: "advisory_lock_held_elsewhere"}` with a 2-row `audit_table` (`status: SKIPPED`, `reason`), a row-derived PASS verdict, `records_new`/`records_updated` null, exit 0. Pre-conversion that run would have been a bare `return` with `records_meta` NULL, indistinguishable in the ledger from a run that had nothing to do. Evidence retained outside the repo (the capture was discarded because it snapshotted a mid-rebuild junction). One residue noted for follow-up: `records_total` reads 0 on that path while the other two read null. | same |
| LM-D9 | link_massing | `scripts/link-massing.js:735` | `emitMeta` declares `parcels: [… geometry]` and `building_footprints: [… geometry …]`; the live PostGIS path reads `geom` on both tables and `geometry` on neither — the declared read surface describes the retired path | **CLOSED · 8b** | landed at commit 7, **CONFIRMED at 8b** against the captured `PIPELINE_META`: `reads` is derived from `inputs.reads.tables[]` and reads `parcels: [id, centroid_lat, centroid_lng, geom]`, `building_footprints: [id, geom, centroid_lat, centroid_lng, footprint_area_sqm]` and — D-23, a second pre-existing under-declaration — `parcel_buildings: [parcel_id]`, the step's own output table, which its incremental filter and cumulative link-rate query have always read and never declared. `geometry` (the 3857 JSONB the retired path used) appears nowhere | same |
| LM-D10 | link_massing | `scripts/link-massing.js:246-256` | W1 FULL DELETE of 520,492 rows has no empty-source floor and commits in its own transaction (`:247`) before any link is written — an empty/truncated `building_footprints` (the `fc038790` incident shape) on a FULL run deletes everything, links 0, then FAILs `link_rate` after the fact | **CLOSED · 8a** | landed at commit 7, **CONFIRMED at 8a**: `empty_source_guard` is declared `when: "pre_write"`, `severity: "FAIL"`, and `runLinkPhase` scores it BEFORE `writes[0]` — which IS the retraction — returning `writeSkipped: true` with `cumulative: null` and no transaction opened when it fails unaccepted. The post-write half is `mass_retraction_ratio` (`pct <= 0.05` on retracted-and-not-rebuilt), so a run that retracts and rebuilds a fraction can no longer read PASS merely because every statement returned. Both sabotages exist in the `#165` must-fail world (`building_footprints_count = 0` under a resolved full mode; `retracted = 520,492 / inserted = 0`) | same |
| LM-D11 | link_massing | `parcel_buildings` live (66,906 buildings `is_primary` for >1 parcel; 167,443 parcels, 34.5% of linked) | one-parcel-per-primary is neither enforced (`idx_parcel_buildings_one_primary` is the other direction) nor counted — a shared primary structure cannot be the primary of 2+ parcels in the sense the cost model uses it | **OPEN · re-dispositioned (R-H)** | **peel 8b** — `shared_primary_buildings`, from the same round trip (+322 ms): **65,318 buildings** are the primary structure of **166,591 parcels** (66,807 / 167,329 before the tiebreak relink — the mirror image of LM-D6's rise). `sum(n)` over the shared buildings IS the affected-parcel count precisely because `idx_parcel_buildings_one_primary` makes each parcel contribute at most one primary row. ⚠️ The plan measured **66,906 / 167,443** the same day, BEFORE two forced FULL relinks — that drift is LM-D13's own instability, which is the second reason this belongs in the audit table rather than in a report: a number that moves when nothing changed is only visible if something records it every run. The cost-model consequence stays the `review_followups` "Massing mislink inventory (P13-2, HIGH)" WF3. **Amended 2026-08-28 (R-H):** peel 8b's INFO-BY-RULING (A-6) disposition, same reasoning as LM-D6, conflicts with Spec 48 §4.9 (`tasks/lessons.md:117`). Re-dispositioned OPEN, **carried to pilot 4**: `shared_primary_buildings` becomes a WARN check with a declared retighten trigger, superseding the INFO row landed at peel 8b | same |
| LM-D12 | link_massing | `parcel_buildings` live catalog (`relrowsecurity=true`, 0 `pg_policies`; role `postgres` `rolbypassrls=true`) | writes succeed only via RLS bypass; a non-bypass role runs W1/E1/E2 against 0 rows silently → `parcel_buildings_written 0`, verdict PASS, `completed` (LR-D7 class) | **CLOSED · 8a** | landed at commit 7, **CONFIRMED at 8a**: the `rls_bypass_or_policy` requirement names `parcel_buildings`, `write.assertWritePrivileges` MEASURES it before the first write (it is excluded from `assertRequirements`' probe table precisely so the measurement survives onto the happy path), the result is carried as `written.privilege`, and the `write_privilege` check reports it as a row on EVERY run — `bypassrls=true policies=0` on the captured runs — not only on the failing one. A check that exists solely in the failure branch is a check nobody ever sees working | same |
| LM-D13 | `scripts/lib/compute/link-massing.js` `buildMatchSql.fallback_match_sql` (pre-conversion `scripts/link-massing.js:357-363`) | **No tiebreak on equal distance** — Postgres kept whichever equidistant row the plan emitted first: **18,252 / 103,530 nearest links (17.6%) could flip building_id between runs; 17,566 tied at distance 0** (a footprint overlapping the lot with its centroid outside it). Two forced FULL relinks hashed `a0023203` vs `0d2ea577` with all 8 invariants identical (U-1, commit 5 `7340fde4`). Primary selection (`5bb31faf`) and classification were already total-ordered. | **CLOSED-MEASURED · 8b** — post-fix recapture 2026-08-27 16:52 (27.3 min, `sources-full-forced-1.json`) and 17:14 (19.4 min, `sources-full-forced-2.json`) hash-EQUAL at `329bbcb6ccd5c32d8b8e9ffca77ff255` on 520,492 rows, all 8 invariants identical | pinned verbatim at commit 7 `9ff540ec` (the fix MOVES ROWS and a no-op differential may not), **FIXED at peel 8b**: the declared rule is `ORDER BY p.id, ST_Distance(...) ASC, bf.footprint_area_sqm DESC, bf.id ASC` — distance FIRST (a tiebreak that re-ranks is not a tiebreak), then the fence-`5bb31faf` order, so the fallback answers "which building is this parcel's" the same way the centroid pass always has: largest footprint wins, lowest `building_id` settles the rest. The rule is stated in the `match_nearest_fallback` check’s `why`, in `notes.json` (`known_bad` + a `fences[]` entry anchored on `9ff540ec`), and in the compute header. Locked BOTH DIRECTIONS twice: **F5** in `src/tests/steps/link_massing/violations.test.ts` over the generated SQL text (a dropped term, a flipped direction, a term hoisted above the distance, no ORDER BY at all — each named), and **behaviourally** in `src/tests/steps/link_massing/nearest-determinism.test.ts`, which runs the PRODUCTION statement 40 times over a randomly ordered scan of a seeded 0-distance tie and asserts ONE winner with the rule and BOTH winners without it. `#150` is rewritten to match: the OLD forced pair must still DIFFER (the pinned defect, kept as evidence — if it ever agreed, the fix would be unproven rather than unnecessary) and the two POST forced FULL relinks must hash-EQUAL. **D-22 retires with it** — a FULL-path table hash is no longer unstable-by-pin | static determinism probe 2026-08-27 + the peel-8b forced pair |
| LM-D14 | `scripts/lib/step/staleness.js` `resolveOverrides` (library, all steps) | **`ctx.overrides.force_full` was never populated.** `override.force_full` was read in exactly ONE place — `selectMode`, which uses it to DECIDE the mode — and never reached `ctx.overrides`, which carried only the accept-anomaly flags and `force_run`. So `link_massing`’s declared `override_force_full_present` WARN observed `undefined` and reported CLEAN on every run, INCLUDING forced ones. **Measured 2026-08-27:** the forced FULL relink emitted `full_mode_reason: "force_full_env"`, retracted and rebuilt all 520,492 rows — and its `override_force_full_present` row read **PASS**. The check exists to catch a `LINK_MASSING_FORCE_FULL` left standing in production: a 21.9-minute relink on EVERY subsequent run plus a `linked_at` bump that re-scopes `enrich_parcels` from 1,395 parcels to 485,135. A version of it that cannot fire is the "green because it never looked" class. | **CLOSED · 8b** | **peel 8b** — `resolveOverrides` now returns `force_full` from `forceFullRequested(descriptor, env)`, the SAME source `selectMode` decides on, so the audit row and the decision can no longer disagree. Generic (no step-specific path, `#149`): a descriptor declaring `force_full: "none"` resolves `false` and cannot be armed by any env. Locked both directions in `src/tests/step-library.logic.test.ts` — armed/unarmed, frozen, the never-truthiness set (`"true"`, `"yes"`, `"0"`, `""`, `undefined` → false), `load_ravines` unarmable, and the decision agreeing with the row. **Found by EXECUTING, not by reading**: the row was only visible as wrong because the forced capture was inspected rather than assumed. | peel-8b forced relink capture, 2026-08-27 |
| LM-D15 | link_massing | `scripts/lib/step/config.js` `resolveConfig` (library, all steps) + `scripts/lib/config-loader.js` `loadMarketplaceConfigs` | **A declared variable with NO `logic_variables` row silently resolves to the seed default and is stamped as if operator-set.** `loadMarketplaceConfigs` clones `scripts/seeds/logic_variables.json` and overlays whatever DB rows exist, so `logicVars[name]` is populated for any name the SEED knows regardless of whether a row exists — `resolveConfig`'s `known` check (`logicVars` OR `FALLBACK_LOGIC_VARS`) can therefore never distinguish "seeded" from "seeded AND present in the operator-editable table." Pilot 3's 4 new variables (`link_massing_link_rate_fail_pct`, `link_massing_centroid_confidence`, `link_massing_nearest_confidence`, `link_massing_grid_degrees`) ran through commits 7/8a/8b this way before any row existed. **Found by EXECUTING**: `node -r dotenv/config scripts/seeds/apply-logic-variables.js` inserted 4/413 rows (`ON CONFLICT DO NOTHING`), proving the 4 rows had never been there. | **CLOSED · 8c** | **peel 8c** — `resolveConfig` issues one extra `SELECT variable_key FROM logic_variables WHERE variable_key = ANY($1)` scoped to the declared names and THROWs for any name absent from the result, naming the remedy (`apply-logic-variables.js`) and citing Spec 122 §1.2a P4. Happy-path `values`/`stamp` are byte-identical (goldens unmoved). Locked both directions in `src/tests/step-library.logic.test.ts` (a DB-row var resolves; a seed-only var throws with the remedy; a neither-registry var keeps the pre-existing "exists in NO registry" message). Fix applied locally: `logic_variables` 428 → 432 rows | found by executing peel 8c's insert against the live registry, 2026-08-27 |
| LM-D16 | link_massing (library, all steps) | `scripts/lib/step/verdict.js:210` | **`errors[]` interpolated `row.value` directly into a template literal**, so an object-valued `detail` (`checkRow`, `:171-173`, prefers `observation.detail` as the row value; 9 sites report an object-valued one across `scripts/lib/compute/*.js`) rendered as the literal string `"[object Object]"`, losing the actual value — captured live in `docs/reports/golden/link_massing/post/sources-full-forced-1.json` `summary.records_meta.errors[1]` (`"rows_changed_ratio: [object Object]"`) and rendered verbatim to operators by `src/components/FreshnessTimeline.tsx:1097-1116`. Latent for every step, not link_massing-specific. | **CLOSED · this commit** | a `renderValue` helper renders primitives via `String` unchanged and objects/arrays via `JSON.stringify` over recursively-sorted keys (stable regardless of call-site insertion order), capped at 300 chars so `errors[]` stays bounded; `errors.push` (`:210`) is the only call site and now routes through it. The `audit_table` rows themselves are unchanged — only the rendered message loses nothing now. Locked both directions in `src/tests/step-library.logic.test.ts` (object detail on WARN → stable sorted JSON; primitive detail unchanged; a large detail capped with an ellipsis), proven RED against the unfixed renderer first. | Observability seat, commit `68b8e361` output panel |
| LW-D1 | link_wsib | `scripts/link-wsib.js:503` (pre-conversion) | the `>= 5%` link-rate floor (T2) is an undeclared, verdict-bound literal — the P4 violation for this pilot, parallel to `link_massing`'s T4 | OPEN · DEFECT (PH-6) | **commit 7** — `link_wsib_link_rate_warn_pct` (default 5, 0–100, `on_invalid: fail`, GROUP "WSIB Matching") | [pilot 4 assessment](2026-08-28-pilot4-link-wsib-assessment.md) §2 |
| LW-D2 | link_wsib | `scripts/manifest.json:39` (`link_wsib` entry) | `supports_full: true` and `supports_dry_run: false` are BOTH measured FALSE (finding 5) — `--full`/argv full-mode reader does not exist; `--dry-run` is a complete separately-coded simulation branch (`:294-341`) | OPEN · DEFECT (PH-6) | **Spec Update step (commit 7)** — declared-diff manifest correction, not a behaviour change | same, §Key Files finding 5 |
| LW-D3 | link_wsib | `scripts/manifest.json:39` `telemetry_tables: ["entities"]` | under-declares the write surface — `wsib_registry` is also written (`linked_entity_id`, `match_confidence`, `matched_at`) and is undeclared. Second independent confirmation of the SAME defect Spec 60's own Step Registry row makes at a different layer (G-16, §1) | OPEN · DEFECT (PH-6) | **Spec Update step (commit 7)** — manifest + Spec 60 row corrected together | same, §Key Files finding 5 + §1 G-16 |
| LW-D4 | link_wsib | `scripts/link-wsib.js:485` (`med_conf` stats bucket, `>= 0.50 AND < 0.90`) | the `[0.50, 0.60)` sub-range is DEAD — no code path ever writes a `match_confidence` in that band (only 0.95/0.90/0.60 are ever written); the `0.50` lower bound is misleading and will remain so post-conversion unless corrected | OPEN · INCIDENTAL (PH-6), PIN (T5) | **declared limitation** (Spec 123 §3.1 PIN, not FIX) — recorded in `limitations[]` at commit 7, value NOT changed | same, T5 |
| LW-D5 | link_wsib | `scripts/link-wsib.js` `buildTier3Ctes` (fixed by `d704a447`, 2026-04-01) + the monotone `WHERE linked_entity_id IS NULL` guard (G-8) | the article-stripping predicate fix (`d704a447`) could not retroactively repair links written under the pre-fix algorithm — monotone guard means a fixed matching bug never repairs the links it already corrupted. **Measured 2026-08-28: 8,450 / 13,965 linked rows (60.5%) fail today's predicate; 8,450/13,645 (61.9%) of tier-3 links; clean link rate 4.55%, below the 5% WARN floor.** 171 magnet entities concentrate the contamination (worst: MDK CONSTRUCTION 2,118 links). PH-6 (commit 4): 171-magnet copyContacts exposure quantified — 7/171 magnets carry any contact field, but 0 attributable to `copyContacts` (`wsib_registry` carries 0 contact values locally); cloud caveat declared | ✅ **CLOSED · commit 8/8b, live repair executed 2026-08-28** | **REPAIRED.** LG-16 (UPDATE-to-NULL, scoped `match_confidence = 0.60`) + LW-D10's convergence loop (bounded `link_wsib_tier3_full_max_iterations`, T7) ran live via `LINK_WSIB_FORCE_FULL=1` against `127.0.0.1:54322/postgres`: retracted all 13,645 tier-3 links, converged in **10 iterations** (< the 20 bound, `exhausted: false`, `tier3_full_not_converged` read PASS), relinked **8,009** tier-3 rows (+4 tier-2 exact-legal, +0 tier-1) under TODAY's predicate — verified live, `wsib_tier3_current_predicate_pass_rate_pct` **38.1% → 100.0%** (0 of 8,009 tier-3 links now fail the predicate, independently re-queried, not merely trusted from the audit row). `wsib_registry` row count held at 121,116 throughout (links retracted, never rows — the declared LG-16 contract). Net: linked total 13,965 → 8,333 (cumulative link rate 11.53% → 6.88% — a NET DROP, because the repair removes more contamination than the current predicate re-admits, exactly as A-7 predicted); fan-in max 2,118 → 439; magnets (fan-in ≥10) 171 → 129; `entities.is_wsib_registered` 938 → 786. `contacts_cleared_on_retraction` = 0 (matches PH-6's local-zero-exposure finding — no contact values existed to clear). `exit_code: 0`, `verdict: PASS`, duration 1,587.4s (~26.5 min, within the descriptor's declared 35m budget). Full before/after table: [pilot 4 assessment](2026-08-28-pilot4-link-wsib-assessment.md) §8c | same, Fold A/B; measured live 2026-08-28 (contamination found) + 2026-08-28 (repair executed) |
| LW-D6 | link_wsib | `scripts/link-wsib.js` exact-tier length floors (`>= 3`, ×4 sites) vs fuzzy-tier length floor (`>= 5`, `buildTier3Ctes`) | asymmetric numeric literals, same semantic role (a minimum comparable-string length), no recorded rationale anywhere in the 17-commit fix corpus — the reasoning (exact match tolerates short strings; fuzzy matching on short strings produces garbage similarity scores) is inferred by this pilot's PH-3 pass, not sourced from any commit | OPEN · INCIDENTAL (PH-6), Rule-4 owed | **commit 7** — `checks[].why` documents the asymmetry (S2), values unchanged (structural, not tunable) | same, §2 (S2) |
| LW-D7 | link_wsib | `docs/reports/review_followups.md:3015` | stale chain-membership claim (finding 1) — `link_wsib` was wrongly cited as a `wsib`-chain member in an earlier R-D followup | **CLOSED · commit 1** | already corrected pre-pilot at `188d7371`/`ad0c1682`; this pilot verified-and-skipped (no edit needed) | same, §1 |
| LW-D8 | link_wsib | `scripts/link-wsib.descriptor.json:448` + `scripts/steps/_schema/step.schema.json` (`properties.database.properties.min_migration`) | `database.min_migration` was written as the DDL migration's FILENAME number (243, for `243_wsib_unlinked_partial_index.sql`) but `scripts/lib/resolve-db.js` `assertDbTarget` (operator ruling P0, `9e2da7b1`) compares against `COUNT(*)` of `schema_migrations`, never a filename — the `migrations/` sequence carries 4 historical filename gaps (43, 49, 50, 158), so COUNT never reaches 243 and the step refused to run against a fully-migrated database (242/242 applied) forever. Found by the G2′ differential (7b agent), reproduced 3× | **CLOSED · this commit** | corrected to 240 — the 1-based position of `243_wsib_unlinked_partial_index.sql` in the sorted `migrations/` directory listing (242 `.sql` files total; live `SELECT count(*) FROM schema_migrations` = 242, matching). `step.schema.json`'s `min_migration` description now states the COUNT semantics and names the footgun by name. New static lock in `src/tests/step-conformance.infra.test.ts` (describe block "database.min_migration — COUNT floor, never a filename number (LW-D8)"): every `converted.json` step AND every `pending` step must declare `min_migration <=` the live `*.sql` file count in `migrations/`, proven RED first against the pre-fix 243, and the schema description is asserted to contain "COUNT". Verified live: `node -r dotenv/config scripts/link-wsib.js --dry-run` now logs `migrations=242 (floor 240)` and completes with verdict PASS (exit 0) | same |
| LW-D9 | link_wsib | `scripts/link-wsib.descriptor.json` (LG-16 null-retract target, `write_discipline.scope`) | **THROWN LIVE, commit 8 first execution attempt (2026-08-28):** the `scope` string for the LG-16 UPDATE-to-NULL target was authored as `"match_confidence = $1 (the fuzzy tier's resolved confidence — tier 3 ONLY; tiers 1/2's exact matches are never retracted, LW-D5's contamination is 100% tier-3)"` — a human-readable parenthetical appended directly onto the predicate. `set_based_null_retract` is in `write.js`'s `SET_BASED_CLASSES`, so `buildWritePlan` renders `scope` VERBATIM into `WHERE ${whereParts.join(' AND ')}` — the parenthetical became live SQL. First real exercise of the LG-16 path (mode never resolved `full` in any commit-7/8a/8b/8c invocation) — no differential or golden capture had ever rendered this `clear_sql`, so no prior test caught it. `executeSetBasedClear` threw `syntax error at or near "("` inside `pipeline.withTransaction`; the whole transaction rolled back — `wsib_registry` row count (121,116) and linked total (13,965) both confirmed unchanged after, no partial write, no orphaned `pipeline_runs` row (chain-driven capture skips its own ledger row) | **CLOSED · commit 8 (R-L/repair-attempt commit)** | `scope` corrected to pure SQL (`"match_confidence = $1"`); the removed rationale is not lost — `write_discipline.declared_drift.text` already states the same tier-3-only bound. Verified: `write.buildWritePlan(nullRetractSpec, descriptor).clear_sql` now renders `UPDATE wsib_registry SET linked_entity_id = null, match_confidence = null, matched_at = null WHERE match_confidence = $1 AND (linked_entity_id IS DISTINCT FROM null);` — parses clean. Scanned every other descriptor's `set_based_scoped`/`set_based_unscoped`/`set_based_null_retract` `scope` field for the same authoring pattern (prose appended to a rendered predicate) — none found. `src/tests/steps/link_wsib/violations.test.ts` (71/71) + `link-wsib-ledger-gate.logic.test.ts` (18/18) + `step-conformance.infra.test.ts` (74/74) green after the fix | [pilot 4 assessment](2026-08-28-pilot4-link-wsib-assessment.md) §8, commit-8 section |
| LW-D10 | link_wsib | `scripts/lib/step/index.js:806-836` (`runCascadePhase`, the LG-16/A-7 mode-`full` block) | ✅ **CLOSED · commit 8b.** ⛔ Was BLOCKING, OPEN. Fold B (Cross-read Adversary, 2026-08-28) RULED the mode-`full` tier-3 pass must LOOP TO CONVERGENCE, bounded by a new tunable `link_wsib_tier3_full_max_iterations` (T7, default 20, WARN `tier3_full_not_converged` on exhaustion) — because `TIER3_SELECT`'s `LIMIT 1000` cap means one bulk pass can relink at most 1,000 of the ~5,195 tier-3 rows expected to survive the repair (plus ~320 tier-1/2 exact matches ⇒ ~5,515 total). Peel 8c's own commit message states "T4–T6 verified wired at 8b" and the descriptor/checks/notes all declare T7 (bounds, WARN severity, a `checks[]` entry, a `notes.json` decision) as if the runtime loop exists. **Measured, commit 8, by direct read (not by execution — the run never reached this code; LW-D9 threw first):** `scripts/lib/step/index.js:834` hardcodes `tier3Full = { retracted, contacts_cleared: contactsCleared, exhausted: false, iterations: 1 }` — a single retract-then-one-pass-per-tier block, NO loop, NO iteration counter, NO read of `link_wsib_tier3_full_max_iterations` anywhere (`grep -rn "convergence\|max_iterations\|tier3_full_max" scripts/lib/step/*.js scripts/lib/compute/link-wsib.js` → 0 matches in runtime code). A forced-FULL run today would retract all 13,645 tier-3 links, relink ≤ ~1,320 (tier1/2 exact + one capped tier-3 pass), leave ~4,000+ links permanently un-relinked with no further attempt, and the audit row would falsely report `exhausted: false` / `iterations: 1` — a converged-looking result that is not converged. **NOT executed at commit 8** — the forced-FULL repair was halted before this code path was reached (LW-D9 threw first); had LW-D9 not thrown, this gap would have produced a silently-incomplete repair under a green audit row | **CLOSED · commit 8b** | RULED and built exactly as specified: `runCascadePhase`'s mode-`full` tier loop now calls a new, extracted, exported `runTierToConvergence(runOnePass, loops, maxIterations)` per tier — `loops` fires ONLY for a tier declaring `max_iterations_from_config` (a generic, per-tier, DECLARED signal; no `'tier3_fuzzy'` string anywhere in the library, preserving Gate 0), repeating while `matched > 0 && iterations < maxIterations` (bound read as `config[tier.max_iterations_from_config]`, never a literal), `exhausted` true only when the bound stops a pass that itself still matched. New INFO check `tier3_full_iterations` `{iterations, relinked_total}` added alongside the WARN `tier3_full_not_converged`. Required a small schema/naming refactor to make "consumed" genuinely runtime-detectable: `tiers[].confidence_config` → `confidence_from_config` (T3/T4/T5) + new `max_iterations_from_config` (T7), both `*_from_config`-suffixed so the existing generic `fromConfigRefs` conformance scan picks them up with zero new detection code — additive to `step.schema.json`, `docs/reports/generated/122-vocabulary.md` regenerated. A SECOND, independent conformance gap found and fixed in the process: the P4 battery's compute-consumed regex matched only `ctx\.config\.<name>`, but link-wsib's compute receives `config` as a bare positional parameter — T1/T3/T4/T5 would ALL have false-positived as dead declarations the instant `link_wsib.js` joined `converted.json`; widened (guarded by `stripComments` after an unguarded first pass immediately false-positived on `assert-schema.js`'s own prose). Locks: `describe('LW-D10 — a declared tunable consumed ONLY via a library *_from_config field...')` (`step-conformance.infra.test.ts`, 4 tests, RED proven against the ACTUAL pre-fix commit `344e9452` via `git show`, not a synthesized fixture) + `describe('LW-D10 — runTierToConvergence: ...')` (`step-library.logic.test.ts`, 5 tests, fixture-pass-driven, the ruling's own worked example 1000/1000/320/0 → 4 iterations/2,320 relinked/not-exhausted). Lesson filed (`tasks/lessons.md`). LOW followup filed for the SET_BASED-scope-is-prose class (LW-D9's root cause), not built this commit | [pilot 4 assessment](2026-08-28-pilot4-link-wsib-assessment.md) §8, commit-8 section; `.cursor/active_task.md` Fold B item 2 (A-7 convergence AMENDED); `docs/reports/review_followups.md` "commit 8 harvest" → RULED and BUILT |
| LW-D11 | link_wsib | `scripts/lib/compute/link-wsib.js` (`entity_fanin_warn`, T6) | **CLOSED · this commit.** `entity_fanin_warn` read a top-level `ctx.fanin` that `scripts/lib/step/index.js`'s `stepCtx` literal NEVER assigns (proven: the object literal that builds `stepCtx` has no `fanin` key, at any archetype) — the check always evaluated `{}`/0 against every real run. Measured live: `docs/reports/golden/link_wsib/post-8-forced/sources-full-forced-2.json`'s `entity_fanin_warn` reported `{"entity_fanin_max":0}` while the SAME capture's invariant `wsib_entity_fanin_max` measured 439. `src/tests/steps/link_wsib/violations.test.ts`'s `runCompute` ctx-builder injected `ctx.fanin` directly (a key no other step's harness used), so the unit suite (71/71) stayed green off a fixture the runtime could never produce — the descriptor's own prose ("Fires immediately on the known-bad population") was false for every real run since commit 7 | **CLOSED · this commit** | Fix per Spec 124 §7 rung (e) — no library change needed: `runCascadePhase` (`scripts/lib/step/index.js`) ALREADY runs this step's own declared `CUMULATIVE_SQL` post-write and merges every non-`linked`/`total` column of its one row generically onto `ctx.matched` (the SAME declared-post-write-observation mechanism `link_rate_warn`/link_massing's `runLinkPhase` use) — `CUMULATIVE_SQL` already selects `entity_fanin_max`/`magnet_entities_fanin_ge_10`, so `entity_fanin_warn` now reads `ctx.matched.entity_fanin_max`/`ctx.matched.magnet_entities_fanin_ge_10` instead of the bogus `ctx.fanin`. **Harness-fidelity lock (generic, LW-D11 class):** `STEP_CTX_KEYS` (new, exported, `scripts/lib/step/index.js`) is the closed list of keys the library ever assigns onto `stepCtx`; `src/tests/step-conformance.infra.test.ts`'s new `describe('LW-D11 — harness-fidelity lock...')` statically AST-parses (TypeScript compiler API, not a lexer — the fixtures' prose breaks brace/paren-counting) every `src/tests/steps/*/violations.test.ts` ctx-builder and asserts every top-level key it sets is ⊆ `STEP_CTX_KEYS`; proven RED first against the pre-fix `link_wsib` harness (named `fanin`, restored via `git stash`/`git stash pop` for the proof, not committed red) and against a synthetic bad fixture, then GREEN. `World`/`healthyWorld`/`SABOTAGE_BY_VAR[T6]` in `link_wsib`'s own `violations.test.ts` moved `fanin`'s fields onto `matched.entity_fanin_max`/`matched.magnet_entities_fanin_ge_10`, mirroring the real runtime shape. **Live re-verification (R-C re-capture, required — editing a converted step's compute changes its `source_fingerprint`):** `permits`/`sources`/`standalone` re-captured against `127.0.0.1:54322/postgres` (242 migrations) — all 3 gated-skip (`no_upstream_changes`, nothing changed in `wsib_registry`/`entities` since the last run) so their own audit rows don't exercise post checks; a fourth, uncommitted verification capture (`--dry-run`, bypasses the SKIP gate only, mode resolves `incremental`, 0 rows changed) DID exercise `entity_fanin_warn` live: `{"metric":"entity_fanin_warn","value":{"entity_fanin_max":439,"magnets_fanin_ge_10":129},"threshold":"viol <= 20","status":"WARN"}`, with `errors:["entity_fanin_warn: {\"entity_fanin_max\":439,\"magnets_fanin_ge_10\":129}"]` rendered correctly via LM-D16's `renderValue` (not `[object Object]`) — matches the live `wsib_entity_fanin_max` invariant (439) exactly, confirming the descriptor's "fires immediately" text is now TRUE. Deleted `docs/reports/golden/link_wsib/post/sources-full-forced-2.json` (byte-identical duplicate of `post-8-forced/sources-full-forced-2.json`, stale fingerprint, not a declared invocation — R-C: "a stale extra scenario capture fails the gate and must be re-run or deleted"; git keeps history). `step-conformance.infra.test.ts` (101/101) + `golden-fingerprint.infra.test.ts` (17/17) + `link_wsib/violations.test.ts` (71/71) + `link_massing`/`assert_schema`/`load_ravines` violations suites (all green) after the fix. Lesson filed (`tasks/lessons.md`) | this report; live capture 2026-08-28 |
| LW-D12 | link_wsib (library, `scripts/lib/pipeline.js#emitSummary`) | `scripts/lib/pipeline.js:321-327` | **CLOSED · this commit.** `emitSummary` coerced `records_total: stats.records_total ?? 0` (both `null` AND `undefined` → 0) while `records_new`/`records_updated` used `!== undefined ? … : 0` (preserves an explicit `null`) — the SAME run's stdout `PIPELINE_SUMMARY` and its `pipeline_runs` row (written by `scripts/lib/step/ledger.js#finalizeLedgerRow`, which deliberately carries NO COALESCE on these three columns) disagreed for a gated-skip's declared-null counters. Measured pre-fix: `docs/reports/golden/link_wsib/post/standalone.json` — `summary.records_total: 0` (number) vs the SAME capture's `pipeline_runs[0].records_total: null`, while `records_new`/`records_updated` read `null` in BOTH places. Blast radius: every converted step's gated-skip path (`scripts/lib/step/index.js` is the only caller that ever passes an explicit `records_total: null` into `emitSummary`, at two sites: the spread of `counters` on a gated/self skip, and the hard-coded null-everything branch) | **CLOSED · this commit** | `records_total` made symmetric with `records_new`/`records_updated` (`!== undefined ? … : 0`). Both-directions red-first lock in `src/tests/pipeline-sdk.logic.test.ts` ("LW-D12" tests: explicit `null` in → `null` out; missing/`undefined` in → `0` out), proven RED against the pre-fix line via `git stash`/`git stash pop` (not committed red). The PRE-EXISTING pinned test at `src/tests/step-library.logic.test.ts` ("the counters read 'not counted', not 'counted zero'") asserted the bug (`records_total` → 0) under a citation of "plan C-11" — corrected: C-11 (`docs/reports/2026-08-25-pilot2-load-ravines-assessment.md:89`) is `run-chain.js`'s OWN DB-write normalisation for the PRE-CONVERSION `load_ravines` script, a different layer that does not license `pipeline.js#emitSummary` doing the same thing asymmetrically to one of three symmetric fields; the test now asserts `records_total` reads `null`, matching `records_new`/`records_updated`. Re-captured `permits`/`sources`/`standalone` (`docs/reports/golden/link_wsib/post/*.json`) against `127.0.0.1:54322/postgres` — `source_fingerprint` unaffected (`pipeline.js` is not one of the 4 fingerprint inputs: step/descriptor/notes/compute), but the captured JSON now shows the true `records_total: null` on every gated-skip run instead of the stale `0`. Consumers checked: `scripts/run-chain.js:661` already read `summary.records_total ?? null` (already null-symmetric, unaffected) and its two `UPDATE … COALESCE($3, records_total)` writes (`:713`,`:733`) already tolerate null by design (preserves the prior row value); grepped `scripts/` + `src/` for `records_total` arithmetic (`+`/reduce/sum) — none found; `src/components/FreshnessTimeline.tsx` already null-guards every render site (`!= null` checks); `src/app/api/admin/pipelines/runs/route.ts`'s `PipelineRunRow.records_total: number` (the one admin route NOT already typed nullable, unlike its 3 siblings) corrected to `number \| null` in the same commit — a pre-existing type-accuracy gap (the DB column was already nullable for many other steps), not new breakage, but adjacent and cheap to fix. `pipeline-sdk.logic.test.ts` (323/323) + `step-library.logic.test.ts` (101/101) green after the fix | this report; live capture 2026-08-28 |
| LW-D13 | link_wsib (library, `scripts/lib/step/index.js`) | pilot 3 output panel §R Reflection (recurring/standard-shaping) — "Ledger-row ownership undeclared" | **DONE (SHOULD-FIX, deferred twice — pilot 3 → pilot 4 plan → this commit) · CLOSED this commit.** `ownsLedgerRow(chainId)` was already computed (`const owns = ownsLedgerRow(chainId)`, `scripts/lib/step/index.js`) and used to gate `openLedgerRow`/`finalizeLedgerRow`, but the fact itself was never made observable in the run's own `records_meta` — an operator reading one run's record could not tell, without cross-referencing `chainId`/log lines, whether THIS process owned the `pipeline_runs` row it just wrote (§1.2a "nothing hidden") | **CLOSED · this commit** | `records_meta.ledger_row` ∈ closed enum `LEDGER_ROW_VALUES = ['owned', 'chain_owned']` (new, exported, `scripts/lib/step/index.js`), stamped unconditionally at BOTH emit sites — the happy-path terminal block and the self_skipped/advisory-lock-contention path (ownership of the CONTEXT is well-defined even when no ledger row was ever opened). Treated as a RUNNER DEFAULT (like `terminal`/`checks_passed`/`config`), not a per-step `emits[]` declaration: `step.schema.json`'s `emits` category is explicitly "records_meta keys BEYOND runner defaults", and this key carries no step-specific data — it applies identically to every converted step, same footing as the three precedent keys. Both-directions red-first lock in `src/tests/step-library.logic.test.ts` (3 "LW-D13" tests: `chain_owned` in-chain, `owned` standalone, both values reachable on the self_skipped path too), proven RED against the pre-fix `index.js` via `git stash`/`git stash pop` (never committed red). Live-verified: `link_wsib` standalone capture → `records_meta.ledger_row: "owned"` (both the stdout summary AND the persisted `pipeline_runs[0].records_meta.ledger_row`); `permits`/`sources` in-chain captures → `"chain_owned"` (`docs/reports/golden/link_wsib/post/*.json`, re-captured). Marked DONE in `docs/reports/2026-08-27-pilot3-link-massing-assessment.md`'s §R recurring table and `.cursor/active_task.md`'s R-F carried-steps list (item 2) — the "DEFERRED-TO pilot 4" text retired. `step-library.logic.test.ts` (104/104) + `step-conformance.infra.test.ts` (94/94) + all 4 `violations.test.ts` suites (288/288) green after the fix | pilot 3 assessment §R; `.cursor/active_task.md` R-F item 2; live capture 2026-08-28 |
| LW-D14 | link_wsib (compute, `scripts/lib/compute/link-wsib.js`) | `scripts/lib/compute/link-wsib.js:88` (`tokenOverlapClause`) + `:250,264,299,313` (`buildFuzzyMatchSql`/`buildFuzzyMatchCountSql` call sites) | **CLOSED · this commit. HIGH.** Tier 3's `similarity() > threshold` + article-stripped first-letter blocking predicate alone let the fan-in/magnet-entity contamination through — measured live (pre-fix, on the corpus the A-7 repair had already produced): only 10.49% (840 of 8,009) of Tier-3 links shared ANY non-generic token between the WSIB name and the matched entity name; the rest matched purely on a shared generic word (CONTRACTING/CONSTRUCTION/…) or nothing at all. This is the fan-in contamination's root cause named at commit 9 (`entity_fanin_max` 439, 129 magnets) — trigram similarity alone cannot tell "CHRISTOPHER MORAND" from "CHRISTOPHER MONIZ" apart from "CONDUCTIVE ELECTRICAL CONTRACTING" from "RESIDENTIAL SOLUTIONS" | **CLOSED · this commit** | Spec 124 §7 rung (e), compute change (a genuine new match-quality predicate, not expressible as a config value alone). `buildFuzzyMatchSql`/`buildFuzzyMatchCountSql` (`scripts/lib/compute/link-wsib.js`) now additionally require `tokenOverlapClause(stopwords, …)` inside each of the two independent tier-3 CTEs (`trade_matches` checks `w.trade_name_normalized` vs `e.name_normalized`; `legal_matches` checks `w.legal_name_normalized` vs `e.name_normalized`) — SQL `regexp_split_to_array` + array `&&` overlap after stripping a declared closed stopword list (`checks[].expect.stopwords`, NOT a compute literal — Rule 1) and purely-numeric tokens. New declared check `tier3_token_overlap` (FAIL below new tunable `link_wsib_tier3_token_overlap_fail_pct`, T8, default 50, seeded + GROUPed + `apply-logic-variables.js` re-run) and new invariant `wsib_tier3_token_overlap_pass_pct` (`docs/reports/golden/link_wsib/invariants.json`). `CUMULATIVE_SQL` widened from a bare string constant to `buildCumulativeSql(descriptor)` (Gate 0: link_wsib is still the only cascade compute `runCascadePhase` calls this for, so the widened contract costs no per-step runner branch). Spec 60 (`docs/specs/01-pipeline/60_shared_steps.md`, "### Link WSIB") amended — NOT Spec 46, which documents `enrich-wsib.js`'s Serper contact flow only, a citation correction made before writing (CLAUDE.md PD #10). **Self-found-and-fixed diagnostic bug (same commit, before any capture was trusted):** the first draft of both `buildCumulativeSql`'s and `invariants.json`'s `tier3_token_overlap_pass_pct` column tokenized ONE field — `COALESCE(NULLIF(w.trade_name_normalized, ''), w.legal_name_normalized)` — against the entity name. Measured 86.2% against the live post-repair corpus, not the expected ~100%. Root cause: `buildFuzzyMatchSql` is a UNION of two INDEPENDENT CTEs; a row written via the legal-name branch can carry a non-empty `trade_name_normalized` that never overlapped anything (it was never the field the write predicate checked), and `COALESCE` always prefers a non-empty trade name over even looking at legal name. Fixed to `OR` the two fields' overlap independently (`COALESCE(overlap(trade,…), false) OR COALESCE(overlap(legal,…), false)`) — mirroring the ALREADY-CORRECT shape of the pre-existing sibling invariant `wsib_tier3_current_predicate_pass_rate_pct`, which this draft should have matched from the start. Verified live via the real (not reproduced) `buildCumulativeSql` output: 100.00%, 0 rows disagree. **Live-fired for real**, twice: `LINK_WSIB_FORCE_FULL=1` run 1 (255.0s, well under the descriptor's `35m` budget and far shorter than the pre-WF3-F A-7 repair's 26.5 min — the token-overlap predicate sharply shrinks the tier-3 candidate pool, the expected reason, confirmed via `pg_stat_activity` mid-run showing the new `trade_matches` CTE genuinely executing) retracted the 8,009 contaminated tier-3 links and relinked 993 clean ones (2 iterations, not exhausted); run 2 (227.5s), re-run AFTER the invariant-formula fix to get a correct capture, retracted the SAME 993 and relinked the SAME 993 (2 iterations) — an idempotency proof (repeat FULL against an unchanged, already-repaired corpus converges to the identical population) as a side effect. Before/after (live, `127.0.0.1:54322/postgres`): `wsib_registry` total rows 121,116 → 121,116 (unchanged, LG-16 contract held); cumulative linked (all tiers) 8,333 → 1,317; tier-3 (0.60) linked 8,009 → 993; `entities.is_wsib_registered` 786 → 587; `entity_fanin_max` 439 → 69; magnets (fan-in ≥ 10) 129 → 20; `tier3_token_overlap_pass_pct` 10.49% → 100.00%. R-M/LW-D17's before-image mechanism produced link_wsib's own FIRST genuine before-image file this run: `docs/reports/golden/link_wsib/before-image/2026-08-29T01-47-22.902Z-wsib_registry.jsonl`, 993 lines (`wc -l`), byte-count-verified equal to the retraction's own reported 993. Re-captured `permits`/`sources`/`standalone` + the forced-full run itself as a 4th file (`docs/reports/golden/link_wsib/post/*.json`, `sources-full-forced-3.json`) — all four hash-identical to each other (`entities:37d345db`, `wsib_registry:44cf7e1d`). `violations.test.ts` (71/71) + `golden-fingerprint.infra.test.ts` (17/17) + `step-library.logic.test.ts` (117/117) + `step-conformance.infra.test.ts` green after the fix | [pilot 4 assessment](2026-08-28-pilot4-link-wsib-assessment.md) §10; this report; live capture 2026-08-28/29 |
| LW-D15 | link_wsib + link_massing (library, `scripts/lib/step/index.js` + `staleness.js`) | `scripts/lib/step/staleness.js:305-310` (`dryRunArgPresent`) + `scripts/lib/step/index.js` (`runCascadePhase`/`runLinkPhase`) | **CLOSED · this commit. CRITICAL.** `--dry-run` no longer suppressed writes for ANY converted LINK/MATCHER step. `dryRunArgPresent` was read ONLY to compute `bypassed` (skip the ledger gate) — `resolveOverrides` had no `dry_run` key, so `ctx.overrides.dry_run` was `undefined`; `runCascadePhase`/`runLinkPhase` then ran their write executors (`write.executeSetBasedJoinUpdate`/`executeSetBasedClear`/`executeRetraction`/`executeOrderedWrites`) UNCONDITIONALLY. A `--dry-run` invocation bypassed the ledger gate (correct) and then wrote to `wsib_registry`/`entities`/`parcel_buildings` for real (WRONG) — the opposite of the flag's contract, and the opposite of the pre-conversion `link-wsib.js`'s own behaviour (`5de41cc1`: dry-run simulates via read-only COUNT queries; `2633c1cb` A2: dry-run bypasses the gate so the simulation runs). Fence found by reading both commits, not inferred from the flag's name | **CLOSED · this commit** | Spec 124 §7 rung (d), library fix, every LINK/MATCHER step benefits. `staleness.resolveOverrides` gains `dry_run` (reads the existing `dryRunArgPresent`, no new reader) — `ctx.overrides.dry_run` now exposed on every phase. `runLinkPhase`: the W1 retraction and `executeOrderedWrites` are both skipped when `dry_run`; the match/classify SELECTs still run, so the "would-write" counts are real (`matched.*`), `written` stays genuinely zero (nothing was written). `runCascadePhase`: each tier's write (`wsib_registry` join-update, `entities` flag-flip, `entities` contacts copy) is replaced by a read-only COUNT-query mirror of the EXACT SAME predicate (`buildTierSql`'s new `*_count_sql` siblings, `scripts/lib/compute/link-wsib.js` — SQL text is compute's declared domain, ruling A-2 option 2, matching `5de41cc1`'s own hand-duplicated-COUNT precedent rather than execute-then-ROLLBACK, which would still ISSUE the write statement text); the LG-16 mode=full retraction is skipped too (`priorContacts.rows.length`, already read, is the exact "would retract" count); a full-mode dry-run runs the LW-D10 convergence loop exactly ONE simulated pass (declared limitation — convergence requires rows to actually leave the unmatched scope between passes, which a dry-run structurally cannot do). `records_meta.dry_run: true` stamped + a new INFO audit row `dry_run_no_writes` (`dryRunRow()`, runner-injected like `retired_var_row_present`) whenever `ctx.overrides.dry_run` is true. Two `deviations[]`/`limitations[]` entries added to `link-wsib.descriptor.json` documenting the simulation mechanism and the full-mode single-pass caveat. Both-directions red-first lock in `src/tests/step-library.logic.test.ts` ("LW-D15" describe block): a `--dry-run` run of `runLinkPhase` (link_massing, cloned + `override.dry_run` armed — the real descriptor declares `"none"`) AND of `runCascadePhase` (link_wsib) each issues ZERO statements matching `/^(UPDATE\|INSERT\|DELETE)/i` while the match/count SELECTs still run and report real would-be counts; proven RED against the pre-fix `staleness.js`+`index.js` via `git stash`/`git stash pop` (never committed red — failed on `ctx.overrides.dry_run` being `undefined`, before even reaching the write-statement assertion). `src/tests/load-ravines.logic.test.ts`'s pinned `resolveOverrides` shape test updated (now 5 keys, `dry_run: false` for `load_ravines`, which declares `override.dry_run: "none"`). Live-verified: a `--dry-run` standalone run against `127.0.0.1:54322/postgres` completed with `records_meta.dry_run: true`, the `dry_run_no_writes` INFO row present, and `wsib_registry`/`entities` row counts and content hashes UNCHANGED before/after (table-state hash comparison, golden-capture harness). `step-library.logic.test.ts` (110/110) + `step-conformance.infra.test.ts`/`golden-fingerprint.infra.test.ts`/all 4 `violations.test.ts` suites green after the fix. `runIngestPhase` (INGESTOR archetype) is untouched — no currently-converted INGESTOR step declares an active `override.dry_run` (all three are `"none"`), so its own dry-run gap, if any, is inert today and out of this WF3's scope; filed as a followup, not silently fixed alongside | this report; live capture 2026-08-28 |
| LW-D16 | link_wsib (test infra, `src/tests/db/ledger-gate-callers.db.test.ts`) | `src/tests/db/ledger-gate-callers.db.test.ts:5-10,266` | **CLOSED · this commit. HIGH.** A FALSE CLAIM in a committed test file: the header comment and a D#4 inline comment stated link_wsib's run-ledger-gate coverage was "RE-HOMED, not deleted (A-5, C1 pilot 4 commit 7, 2026-08-28)" to `src/tests/steps/link_wsib/ledger-gate.db.test.ts` — that file was NEVER CREATED (`git log --all -- src/tests/steps/link_wsib/ledger-gate.db.test.ts` returns nothing). Root cause, found the same day: `docs/reports/review_followups.md`'s pre-existing MED item — every converted step declares `database.assert_current_database: "postgres"` and `assertDbTarget` REFUSES any other name, but `setup-testcontainer.ts` always provisions `POSTGRES_DB: 'buildo_test'`, so a live-DB test calling a converted step's real `.run()` refuses immediately. The re-home was never buildable; the comment was left as an aspirational TODO rather than corrected when that was discovered — a genuine coverage gap masquerading as "moved elsewhere." Net effect: `staleness.ledgerGatedSkip` (LG-15, the actual gate `runCascadePhase` calls for link_wsib) had NO direct behavioral test anywhere, live-DB or otherwise — only indirect exercise via `violations.test.ts`'s fixture matrix | **CLOSED · this commit** | Corrected both false-claim sites in `ledger-gate-callers.db.test.ts` to state plainly: no such file exists, the MED testcontainer-database-name item is the reason, and it stays a named followup (not fixed here — test infrastructure, not a step conversion). Added 5 fake-pool (no live DB) behavioral locks for `ledgerGatedSkip` in `src/tests/step-library.logic.test.ts`'s new "LW-D16" describe block: (1) `bypassed:true` never SKIPs even against a baseline that would otherwise SKIP, and issues ZERO queries; (2) a CHANGED `config_version` signal forces `skip:false` even when the raw ledger gate alone reads `skip:true`; (3) no baseline (`ownLastRecordsMeta:null`) reads its config signal as fail-safe `changed:true`, never "nothing to compare so unchanged"; (4) `gate.ownLastRecordsMeta` (a prior SKIP's `consecutive_skips` + a caller-shaped audit row) passes through `ledgerGatedSkip` completely unmodified — a downstream consumer (e.g. `cascade.prior`) sees exactly what the DB held; (5) a hoisted, out-of-bounds `wsib_fuzzy_match_threshold` throws before the ledger gate is ever queried, using the REAL `link-wsib.descriptor.json` + `pipeline.step().run()`, not a synthetic descriptor. Each of the 5 proven RED by temporarily sabotaging the corresponding line in `scripts/lib/step/staleness.js`/`config.js` (never committed red; reverted via re-edit after each, confirmed `git diff` clean before restaging) — (1) neutered the `bypassed` early-return, (2) dropped `!configVersionChanged` from the returned `skip`, (3) hardcoded the per-signal `changed` to `false`, (4) stripped `ownLastRecordsMeta` from the returned `gate`, (5) neutered `config.js`'s `on_invalid:"fail"` throw. `step-library.logic.test.ts` (117/117) + `ledger-gate-callers.db.test.ts` (skips cleanly without `BUILDO_TEST_DB`, unaffected) green after the fix | this report; `docs/reports/review_followups.md` MED item (assert_current_database vs buildo_test) |
| LW-D17 | link_wsib + link_massing (library, `scripts/lib/step/write.js` + `index.js`) | pilot 3 output panel §R Reflection — "Interrupted full retraction leaves a partial table" (R-B's own sibling gap: R-B declares the POSTURE, nothing wrote the EVIDENCE) | **DONE (operator ruling R-M, 2026-08-28) · CLOSED this commit. HIGH.** A destructive retraction (LG-16 UPDATE-to-NULL, or a `retract:"all"` DELETE) had no row-level record of what it was about to remove — if the retraction's own predicate were ever wrong, or an operator needed to know exactly what a repair erased, there was nothing to look at. R-B's `recovery.interrupted` declares the CRASH-RECOVERY posture; nothing declared or produced the AUDIT TRAIL a human would need to verify a destructive repair after the fact | **CLOSED · this commit** | New schema field `recovery.before_image` (`step.schema.json`, mirrors `interrupted`'s exact shape: `"generated"` or `"none"`+`before_image_why`, required on every object-form `recovery`). New library primitives in `scripts/lib/step/write.js`: `buildBeforeImageSelectSql(plan)` mirrors a write plan's OWN `scope`/`keys`/`step_columns` as a read-only `SELECT` (returns `null` when `plan.scope` is falsy — a scope-less retraction, e.g. `retract:"departed"`, is a declared limitation, not a crash); `writeBeforeImage(client, plan, scopeParams, slug, runAt)` runs that SELECT and writes the rows to `docs/reports/golden/<slug>/before-image/<RUN_AT ISO, colon-sanitized>-<table>.jsonl`, unwrapped by any try/catch (a write failure — disk full, permissions — must abort the run BEFORE the retraction, fail loud, never a silent skip). Wired into BOTH retraction sites, on the SAME transaction client, strictly before the retraction call: `runLinkPhase`'s W1 loop (link_massing's `retract:"all"` clear) and `runCascadePhase`'s LG-16 block (link_wsib's `set_based_null_retract`). A new INFO audit row `before_image_written:<table>` (`{path, rows}`) surfaces per target actually written. Declared `before_image: "generated"` in `link-wsib.descriptor.json` + `link-massing.descriptor.json` (both scope-bearing); `before_image: "none"` + a full `before_image_why` in `load-ravines.descriptor.json` (its only retraction is `retract:"departed"` with `write_discipline.scope:"none"` — keyed by a surviving-ids array, not a WHERE predicate, so the generic mechanism structurally cannot mirror it today; a genuinely separate follow-up, not silently fixed or silently ignored). Conformance lock mirroring R-B's exact shape in `step-conformance.infra.test.ts` ("R-M / LG-17" describe block, narrower trigger than R-B's — only a retraction the mechanism CAN cover, i.e. `write_discipline.scope !== "none"`, is required to declare "generated"), with the same built-in RED canaries R-B uses (a synthetic "none"/undefined value reddens against a scope-bearing target). Direct behavioral lock in `src/tests/step-library.logic.test.ts` ("LW-D17" describe block, fake pool for the DB read + a REAL scratch subtree under `docs/reports/golden/_wf3g_test_before_image/` deleted in `afterEach` every time, standing in for a tmp dir since the mechanism's write location is fixed by the schema, not injectable): the mirror SQL matches the plan's own scope/keys/columns; `null` scope returns `{written:false}` with ZERO query and ZERO directory created; a matched-but-empty scope still writes an (empty) JSONL file, never a missing one; proven RED by temporarily stubbing the DB read to return nothing without touching the assertions (never committed red, `git diff` confirmed clean before restaging). golden captures re-taken for all three descriptors whose `recovery` object changed shape (link_wsib, link_massing, load_ravines — the schema amendment changes their fingerprint even though no compute/step file moved): `docs/reports/golden/{link_wsib,link_massing,load_ravines}/post/*.json`, INCLUDING a genuine forced-FULL re-run of `link_massing` (`LINK_MASSING_FORCE_FULL=1`, `sources-full-forced-1.json`, ~load-bearing evidence pinned to `LM_D13_FIXED_HASH` in `violations.test.ts` — re-captured for real, not deleted, after an initial mis-step deleted it as a mistaken "stale extra scenario"; hash-verified against the pinned value before commit). `step-library.logic.test.ts`/`step-conformance.infra.test.ts`/`golden-fingerprint.infra.test.ts`/all 4 `violations.test.ts` suites green after the fix. **LIVE-FIRED FOR REAL**, not merely unit-proven: the `sources-full-forced-1.json` re-capture (`LINK_MASSING_FORCE_FULL=1`, mode FULL, `retracted 520,492 row(s)`) produced the mechanism's FIRST genuine before-image file, `docs/reports/golden/link_massing/before-image/2026-08-29T00-49-52.392Z-parcel_buildings.jsonl` — 520,492 lines (`wc -l`), one per retracted row, byte-verified to match the retraction count exactly, written and confirmed on disk BEFORE the retraction's own log line ("retracted 520,492 row(s)") ever printed. `link_wsib`'s mode never resolved full in this commit's captures; its own first before-image file was produced by WF3-F's live repair run (LW-D14, same session): `docs/reports/golden/link_wsib/before-image/2026-08-29T01-47-22.902Z-wsib_registry.jsonl`, 993 lines, byte-count-verified equal to the retraction's own reported row count — the mechanism's SECOND real-world proof, confirming it generalizes past `link_massing`'s single exercise | pilot 3 assessment §R; this report; live capture 2026-08-28/29 |
