# Pilot 3 assessment — `link_massing` (PH-0 · PH-3 · PH-5 · PH-6)

**Target:** `scripts/link-massing.js` — **740 lines** (`wc -l`), lock 91, HEAD `73c90d24` (`git log -1 --format=%h`, the plan commit), file last touched by `2f3d0e4e` (2026-07-08, P11-2 full gate). Archetype **LINK**, writes ONE junction table (`parcel_buildings`) through THREE disciplines in sequence (W1 FULL-only mass DELETE · E1 unguarded scoped UPDATE · E2 guarded upsert), **2 chains + standalone = 3 invocations** (`sources` with `--full`, `permits` bare, standalone bare).
**Plan:** `.cursor/active_task.md` (Pilot 3, Folds A+B; rulings A-1 `runLinkPhase` · A-2 `buildMatchSql` · A-3 explicit `guard_columns` · A-4 column-projected golden · A-5 `hoisted_above_gate` · A-6/A-7 fired · **A-8 OVERRIDDEN by the operator: the JS fallback path is RETIRED, `knowingly-retired`, replaced by `guards.requires {kind: extension, name: postgis, on_missing: fail}`**). **Governing:** Spec 123 §6 / §6.1 / §6.2 / §7 / §7.1; Spec 122 §1.2a P1–P5, §1.4, §1.5, §5.1. Step specs: Spec 56 §2/§3, Spec 60 §3, Spec 43 §15; downstream Spec 65 MB-7.
**Structure:** mirrors `2026-08-25-pilot2-load-ravines-assessment.md` (the violations test reads tables by column header). **Numbers are not inherited** — every figure below was re-derived this session (2026-08-27) from the file at `73c90d24` and the live DB (`scripts/lib/resolve-db.js` → `postgresql://postgres:***@127.0.0.1:54322/postgres`, source `env:DATABASE_URL`, **242** applied migrations, `max(filename)` = `245_parcels_centroid_geom_invalidation.sql`, floor 223). Where a plan figure differed it is flagged `⚠ plan said …`.
**Status:** ASSESSMENT ONLY. No `src/`/`scripts/` code changed. PH-3 dispositions are **RULED — operator, 2026-08-27, §7.1: "Approve all as proposed"** (every §2.4 row carries `adjudicated by = operator (2026-08-27, §7.1)`), plus the A-8 override (JS fallback path → `knowingly-retired`, approver `operator (2026-08-27, §7.1; A-8 override)`). The pass that discovered the fences (Claude, commit 2) did not retire them — discoverer ≠ adjudicator on every row.

> **Operator context (2026-08-27):** every ruling explicit, standardized, observable, data-quality-preserving, I/O-balanced, and unable to silently fail or be casually changed by an LLM. No second code path survives in compute.

---

## 1. PH-0 — Boundary freeze (commit 1 → G0)

**Rule:** nothing outside this section is behaviour.

### 1.1 Grep transcript (the evidence)

```
$ wc -l scripts/link-massing.js                 → 740
$ grep -n "throw new Error"    → 2   :169 (logicVars validation)  :234 (no GiST on parcels.geom — B-4 HALT)
$ grep -n "try {"              → 1   :554        "catch" → 1 :557 (PiP swallow, JS path)      "finally" → 0
$ grep -n "emitSummary"        → 1   :708        "emitMeta" → 1 :734
$ grep -n "fetch("             → 0   (no network seam — first pilot with an empty one)
$ grep -n "pool.query"         → 10  :190 pgis-detect · :230 gist-check · :258 count · :272 keyset batch · :288 MATCH join · :320 E1 UPDATE · :356 NEAREST join · :438 count(JS) · :462 batch(JS) · :677 cumulative link_rate
$ grep -n "client.query"       → 3   :144 E2 upsert · :248 W1 DELETE (PostGIS) · :449 W1 DELETE (JS)
$ grep -n "INSERT INTO"        → 1   :145        "DELETE FROM" → 2 :249 :450        "UPDATE parcel_buildings" → 1 :321
$ grep -n "withTransaction"    → 3   :143 (E2) :247 (W1) :448 (W1-JS)     "withAdvisoryLock" → 1 :165
$ grep -n "streamQuery"        → 3 lines, 1 call :407 (JS path; :210 :403 comments)
$ grep -n "Date.now()"         → 4   :184 startTime · :400 loadStart · :426 loadElapsed · :662 durationMs        "new Date(" → 0
$ grep -n "getDbTimestamp"     → 1   :166 (RUN_AT, inside the lock — Spec 47 §R3.5)
$ grep -n "isFullMode"         → 1   :176 (→ pipeline.js:712-714 process.argv.includes('--full'), ONE FRAME UP)
$ grep -n "process.argv"       → 0        "process.exit" → 0        "console." → 0
$ grep -n "process.env"        → 2   :177 LINK_MASSING_FORCE_FULL · :727 PIPELINE_CHAIN (phase 8/9 ternary)
$ grep -n "pipeline.run("      → 1   :164  ('link-massing' — hyphen, not the slug)
$ grep -n "pipeline.progress"  → 2   :388 :657 (stdout narration every 10,000 parcels)
$ grep -n "pipeline.log.info"  → 10       "pipeline.log.warn" → 2 :201 (geom column missing) :558 (PiP swallow)
$ grep -n "require("           → 7   :29-32 pipeline/safe-math/zod/config-loader · :50 massing-full-gate · :397-398 Turf (lazy, JS path)
$ grep -n "SPEC LINK"          → 1   :27 (56)        "module.exports" → 0  (nothing exported)
$ grep -n "metric: '"          → 9   :695-703  (+2 sys_ injected by emitSummary = 11 live)
$ grep -n "IS DISTINCT FROM"   → 5 lines, 4 guard columns :153-156 (:138 is a comment)
$ grep -n "linked_at = EXCLUDED.linked_at" → 1 :152  (in SET, NOT in the guard — B-12 / finding 2)
$ grep -n "RUN_AT"             → 4   :166 capture · :340 :374 :631 (7th bind param on every INSERT row)
$ grep -n "4326"               → 3   :53 (comment) :286 (comment) :293 (ST_SetSRID)   ⚠ plan said 3 sites incl. ST_Expand; measured: ST_Expand (:360) carries no SRID literal — the 3 are 1 SQL + 2 comments
$ grep -n "0.95"               → 2   :340 :566        "0.60" → 2 :374 :594
$ grep -n ">= 50"              → 2   :691 (verdict) :701 (rendered threshold string)
$ grep -n "BATCH_SIZE"         → 3   :45 (=500, JS) :277 (pipeline.BATCH_SIZE=1000, PostGIS) :468 (500, JS)
$ grep -n "records_new"        → 1   :710  `records_new: 0` LITERAL      "records_total" → 1 :709 (= processed)   "records_updated" → 1 :711 (= parcelsLinked)
$ grep -n "hasPostGIS"         → 8   :199 :206 :224 :688 :689 :693 :695 :696
$ grep -n "return;"            → 1 :739   "break;" → 2 :279 :471   "continue;" → 4 :495(inner ring) :534 :545 :603
$ grep -cF "geom" → 44 lines;  the PostGIS join reads parcels.geom (:292 :293 :294 :360-363) and building_footprints.geom (:292 :360 :361 :363) — NEITHER is declared in emitMeta :735
```

Exit constructs = **8**: 2 `throw` (:169 :234) + 1 `return;` (:739) + 2 `break;` (:279 :471) + 3 parcel-level `continue;` (:534 :545 :603). Plan figures re-measured to the same value: 740 lines · 2 throw · 1 try / 1 catch / 0 finally · 1 emitSummary · 1 emitMeta · 10 + 3 queries · 4 `Date.now()` / 0 `new Date(` · 0 argv · 2 env · 0 fetch · 0 console · 0 exports · 9 metrics · 8 exit constructs. **Two plan phrasings did not re-measure literally:** (a) `4326` "3 sites incl. the ST_Expand join" — the ST_Expand join (:360) has no SRID literal; the three hits are one `ST_SetSRID` (:293) + two comments; (b) the JS path is **345/740 = 46.6%** of the file when its private helpers (:51-87 reprojection, :97-110 haversine, :111-134 grid) are counted, not "~36%" (= :391-660 alone, 270/740 = 36.5%).

### 1.2 Tables and columns written — the three disciplines (SQL anchors)

| Site | Discipline | Statement (verbatim shape) | Scope / mode | Guard | Txn |
|---|---|---|---|---|---|
| **W1** `:249` (PostGIS) · `:450` (JS) | FULL-only step-scoped mass DELETE (= E2's `retract: "all"`, `retract_when: "full_only"`) | `DELETE FROM parcel_buildings WHERE parcel_id IN (SELECT id FROM parcels WHERE centroid_lat IS NOT NULL AND centroid_lng IS NOT NULL)` | `if (FULL_MODE)` `:246`/`:447`; ONE statement BEFORE the loop (B-7); deletes every link of every centroid-bearing parcel = **520,492 rows** on a FULL run | **none** — no floor, no empty-source check (→ **LM-D10**) | `pipeline.withTransaction` `:247`/`:448` |
| **E1** `:320-323` (PostGIS ONLY — the JS path has no E1) | batch-scoped UNGUARDED `set_based_scoped` UPDATE | `UPDATE parcel_buildings SET is_primary = false WHERE parcel_id = ANY($1) AND is_primary = true` (`$1` = the batch's matched parcel ids) | per keyset batch of `pipeline.BATCH_SIZE` = 1000 parcels, matched subset only | **none** (`IS DISTINCT FROM` not applicable); `rowCount` DISCARDED — the result of `pool.query` is never read (→ **LM-D3**) | autocommit (outside any txn) |
| **E2** `:144-158` via `flushInsertBatch` (3 call sites `:346` `:378` `:641/:652`) | `guarded_upsert` on the composite key | `INSERT INTO parcel_buildings (parcel_id, building_id, is_primary, structure_type, match_type, confidence, linked_at) VALUES … ON CONFLICT (parcel_id, building_id) DO UPDATE SET is_primary, structure_type, match_type, confidence, linked_at = EXCLUDED.* WHERE parcel_buildings.is_primary IS DISTINCT FROM EXCLUDED.is_primary OR structure_type … OR match_type … OR confidence …` | per batch; rows built in JS (`0.95`/`'centroid_in_parcel'` at `:340`, `0.60`/`'nearest'`/`is_primary=true`/`'primary'` at `:374`) | `IS DISTINCT FROM` on **exactly 4** columns `:153-156`; **`linked_at` is in the SET (`:152`) and NOT in the guard** (B-12, `b36d0596`) — `guard_columns` MUST be explicit, never `all_declared` | `pipeline.withTransaction` `:143` — one txn per flush |
| via `run-chain.js` (in-chain) | bookkeeping | `pipeline_runs` row under `${chainId}:link_massing` (`run-chain.js:522`, `:601`) | — | the step writes **no** `pipeline_runs` row; standalone writes none either | — |

**Columns written (7 of 8):** `parcel_id`, `building_id`, `is_primary`, `structure_type`, `match_type`, `confidence`, `linked_at` (= `RUN_AT`, DB clock, on INSERT and on UPDATE). `id` is the serial — **reassigned on every FULL run** (W1 deletes, E2 re-inserts). **Never written:** nothing else; but three live column DEFAULTS are values the step never writes — `structure_type 'other'`, `match_type 'polygon'`, `confidence 0.85` — a generated INSERT that omits a column silently adopts them (`match_type='polygon'` is NOT in the live value domain).

**DB reads (all columns, incl. the two undeclared `geom`s):** `parcels.{id, centroid_lat, centroid_lng, geom}` (`:237` filter, `:273` keyset, `:289-294` join, `:357-363` nearest, `:679-680` link_rate denominator) — the JS path additionally reads `parcels.geometry` (`:463`); `building_footprints.{id, geom, centroid_lat, centroid_lng, footprint_area_sqm}` (`:289-293`, `:357-363`) — the JS path reads `geometry` instead of `geom` (`:408`); `parcel_buildings` (`:240` NOT EXISTS scope, `:679` COUNT DISTINCT); `pg_extension` + `information_schema.columns` (`:190-197`); `pg_indexes` (`:230-232`); `logic_variables` via `loadMarketplaceConfigs(pool, 'link-massing')` `:167` (live: 428 rows, the 3 `massing_*` keys present at 20 / 60 / 50 as `variable_value` text; **0** `%link_rate%` keys); `pipeline_runs` + `building_footprints` COUNT via `evaluateMassingFullGate` (`massing-full-gate.js:38-46` — reads the last `status='completed'` row across the 4-form slug array). **`emitMeta` `:735` declares `parcels: [id, centroid_lat, centroid_lng, geometry]` and `building_footprints: [id, geometry, footprint_area_sqm, centroid_lat, centroid_lng]` — `geom` is undeclared on both tables** (→ **LM-D9**, declared diff D-1).

**Live state (2026-08-27):** `parcel_buildings` **520,492 rows · 485,135 distinct `parcel_id` · 417,702 distinct `building_id` · 113 MB · `linked_at` 0 NULL, min = max = `2026-07-07T15:23:54.234Z`** (one FULL watermark, run 1401). `parcels` **486,530** (486,530 with centroid, 486,530 with `geom`, 4,062 MB); **1,395** with `massing_enriched_at IS NULL` = exactly the **1,395** centroid-bearing parcels with zero links (the permanently-unmatchable tail, C-8 confirmed). `building_footprints` **427,077** (all with `geom` + centroid; 0 NULL / 0 zero `footprint_area_sqm`; 511 MB). SRID measured `4326` on both `geom` columns. Live `stale_scope` (`pb.linked_at > p.massing_enriched_at`) = **0** — `enrich_parcels` currently re-scopes nothing on this edge; a `guard_columns: all_declared` port would make it 485,135.

**Live catalog (DB Schema-Fidelity, A-7):** 8 columns ALL `NOT NULL`, 6 with defaults (`id` serial · `is_primary false` · `structure_type 'other'` · `linked_at now()` · `match_type 'polygon'` · `confidence 0.85`); `confidence` is **`numeric(3,2)`**. Constraints: `parcel_buildings_pkey (id)` · `parcel_buildings_parcel_id_building_id_key UNIQUE (parcel_id, building_id)` (the ON CONFLICT target — composite, not the PK) · **FK `parcel_buildings_parcel_id_fkey → parcels(id)`** · **FK `parcel_buildings_building_id_fkey → building_footprints(id)`**. **5 indexes:** `parcel_buildings_pkey` · `parcel_buildings_parcel_id_building_id_key` · `idx_parcel_buildings_one_primary` (**partial UNIQUE `(parcel_id) WHERE is_primary = true`** — the B-8 guard; **0** parcels with >1 primary live) · `idx_parcel_buildings_parcel (parcel_id)` · `idx_parcel_buildings_building (building_id)`. GiST preconditions present: `idx_parcels_geom_gist`, `idx_building_footprints_geom_gist`. PostGIS detection live: `has_ext = true`, `has_geom_col = true`. **RLS:** `relrowsecurity = true`, `relforcerowsecurity = false`, **0 policies**; role `postgres` `rolbypassrls = true` — the LR-D7 class: a non-bypass role would DELETE/UPDATE/UPSERT 0 rows silently (→ **LM-D12**).

**Value domain (live, exhaustive — 5 combinations, no others):** `('centroid_in_parcel', true, 'primary', 0.95)` 381,605 · `('centroid_in_parcel', false, 'other', 0.95)` 23,013 · `('centroid_in_parcel', false, 'shed', 0.95)` 6,539 · `('centroid_in_parcel', false, 'garage', 0.95)` 5,805 · `('nearest', true, 'primary', 0.60)` 103,530.

### 1.3 Audit rows (9 declared + 2 `sys_`) and `records_meta`

`audit_table = { phase: PIPELINE_CHAIN==='sources' ? 8 : 9, name: 'Building Footprint Linking', verdict: massingVerdict, rows }` (`:726-731`); `emitSummary` appends `sys_velocity_rows_sec` + `sys_duration_ms` (11 live rows on every audited run).

| # | Metric | Status | Threshold | Site | Value source |
|---|---|---|---|---|---|
| 1 | `buildings_indexed` | INFO (PostGIS) / PASS·FAIL (JS, `> 0`) | `null` / `'> 0'` | `:695` | `totalBuildings` — **always 0 on the PostGIS path** |
| 2 | `grid_cells` | INFO | `null` | `:696` | `'N/A (PostGIS)'` / `grid.size` |
| 3 | `parcels_processed` | INFO | `null` | `:697` | `processed` (= the 1,395 tail on every incremental run) |
| 4 | `run_matched` | INFO | `null` | `:698` | `parcelsLinked` |
| 5 | `match_centroid_in_parcel` | INFO | `null` | `:699` | `centroidInParcelMatches` |
| 6 | `match_nearest_fallback` | INFO | `null` | `:700` | `nearestMatches` |
| 7 | `link_rate` | PASS / FAIL / WARN | `'>= 50%'` | `:701` (status decided `:691-693`) | cumulative `COUNT(DISTINCT parcel_id) / COUNT(parcels with centroid)` `:677-684`; **FAIL only if `hasPostGIS && processed > 0`, else WARN** |
| 8 | `no_match` | INFO | `null` | `:702` | `noMatch` |
| 9 | `parcel_buildings_written` | INFO | `null` | `:703` | `buildingsUpserted` (sum of E2 `rowCount`s) — **asserted by name in `chain.logic.test.ts:1544`** |

**Measured baseline (run 1479, `sources:link_massing`, 2026-07-08, the last completed sources row):** rows exactly as above with values `0 · N/A (PostGIS) · 1395 · 0 · 0 · 0 · 99.7% · 1395 · 0` + `sys_velocity_rows_sec 177.17` + `sys_duration_ms 7874`; verdict **PASS**; phase **8**; `records_total 1395 / records_new 0 / records_updated 0`; `records_meta` **1,248 B** (run 1572 `permits`: 1,249 B, phase **9**). ⚠ plan said `records_meta` 2,026–2,039 B — `pg_column_size(records_meta)` measures **1,145–1,249 B** on the six most recent completed rows (the plan's figure is presumably `length(records_meta::text)`; the ledger column, not the JSON text, is what P3 costs).

**`records_meta` — 15 keys on run 1479** (`jsonb_object_keys`): `duration_ms` · `code_version` · **`building_footprints_count`** · `full_mode` · `full_mode_reason` · `parcels_processed` · `parcels_linked` · `buildings_matched` · `buildings_upserted` · `matches_centroid_in_parcel` · `matches_nearest` · `no_match_count` · `audit_table` + 2 run-chain-injected (`telemetry`, `pipeline_meta`). **`building_footprints_count` is a STRING** (`jsonb_typeof` → `"string"`, value `"427077"`; produced by `String(...)` at `massing-full-gate.js:38`) and **`code_version`** = `'v2-building-centroid-in-parcel'` — both are **consumed by the NEXT run's own gate** (`evaluateMassingFullGate` `:49-56`, `String(prevCount) !== buildingCount`). Byte-stable, self-referential producer/consumer contract; only **2** live rows carry it (runs 1479, 1572 — post-P11).

**Counters (`:709-711`):** `records_total: processed` · **`records_new: 0` (LITERAL — `d2050cfc` 2026-03-10, "these scripts only update existing rows (never INSERT)", false since the upsert inserts)** · `records_updated: parcelsLinked` (a PARCEL count; `buildingsUpserted` is the ROW count — `chain.logic.test.ts:1544` locks `not records_updated: buildingsUpserted`). → **LM-D1**.

### 1.4 stdout contract

`PIPELINE_SUMMARY:<json>` ×1 (`:708`; or the SDK's skip payload `{records_total:0, records_new:0, records_updated:0, records_meta:{skipped:true, reason:'advisory_lock_held_elsewhere'}}` from `pipeline.js withAdvisoryLock` when lock 91 is held) · `PIPELINE_META:<json>` (`:734`) · `pipeline.progress` lines `  [link-massing] N / T (pct%) — Xs — R rows/s` every 10,000 parcels and at the end (`:387-389`, `:656-658`) · ~7 `pipeline.log.info` lines on the PostGIS path incl. the full-gate line `full-gate: explicitFull=… force=… gate.changed=… (…) → FULL|INCREMENTAL` (`:183`) and `Mode: … [PostGIS]` (`:206`) · `[link-massing] completed in Xs` from `pipeline.run`. No `console.*` in the file.

### 1.5 Exit codes and the terminals

`pipeline.run` (`pipeline.js:463-484`) rethrows on error → Node unhandled-rejection → **exit 1**; every non-throwing path → **exit 0 regardless of verdict** (a FAIL `link_rate` row exits 0; `run-chain.js` records `completed`/`completed_with_errors` — the LR-D3 class, pinned).

| T | Terminal | Anchor | Exit | Ledger / emit |
|---|---|---|---|---|
| T1 | lock 91 held elsewhere | `:165` → `:739` `return;` | 0 | SDK skip emit; no `audit_table`; **6 live `skipped` rows under `permits:link_massing`, `records_meta` NULL** (→ **LM-D8**) |
| T2 | `logicVars` invalid | `:169` throw (INSIDE the lock — B-13) | 1 | `failed` |
| T3 | PostGIS present, no GiST on `parcels.geom` | `:234` throw | 1 | `failed` (B-4 HALT before any join) |
| T4 | PostGIS success (FULL or INCREMENTAL) | `:708` | 0 | `completed`; verdict PASS/WARN/FAIL row-derived |
| T5 | JS-fallback success | `:708` | 0 | `completed` — **0 of 11 audited runs since `7c75e92e` (2026-04-02) took this branch** (§2.3 U-5) |
| T6 | any pg error (pool/txn) | rethrow via `pipeline.run` | 1 | `failed`; a FULL run that throws AFTER W1 leaves `parcel_buildings` empty for the window (W1's txn commits independently of E2's) |

### 1.6 Network fan-out

**None.** 0 `fetch(`; the step is a pure DB→DB join.

### 1.7 Invocation (Spec 123 §7 note, verified)

`manifest.json:30` `link_massing`: `supports_full: true`, `supports_dry_run: false`, **`chain_args: {"sources": ["--full"]}`**, `telemetry_tables: ["parcel_buildings"]`; chain member at `:80` (`permits`, after `link_neighbourhoods`) and `:105` (`sources`, after `massing`). `run-chain.js:638-646`: `stepEnv = {...process.env, PIPELINE_CHAIN: chainId}`, `extraArgs = [...(scriptEntry.chain_args?.[chainId] || [])]`, `spawnStepChild(...)`. Reproductions: **sources** `PIPELINE_CHAIN=sources node scripts/link-massing.js --full` (phase 8; FULL only if the gate says changed or `LINK_MASSING_FORCE_FULL=1`) · **permits** `PIPELINE_CHAIN=permits node scripts/link-massing.js` (phase 9; never FULL unless forced) · **standalone** `node scripts/link-massing.js` (phase **9** — the ternary's else-arm; INCIDENTAL). `manifest.json` declares `chain_args` for exactly two steps (`enrich_parcels`, `link_massing`).

### 1.8 Boundary freeze — tables touched (machine-readable, Appendix H row counts)

Row counts measured this session (`SELECT COUNT(*)` on `127.0.0.1:54322/postgres`, 2026-08-27).

| Table | Rows | Access | Statements |
|---|---|---|---|
| `parcel_buildings` | 520,492 | write (domain; W1 `retract:"all"` full_only · E1 `set_based_scoped` · E2 `guarded_upsert` on `(parcel_id, building_id)`); FK → `parcels`, `building_footprints`; 5 indexes; RLS on, 0 policies | DELETE `:249`/`:450` · UPDATE `:321` · INSERT…ON CONFLICT `:145-156`; reads `:240` `:679` |
| `parcels` | 486,530 | read (`id`, `centroid_lat`, `centroid_lng`, **`geom`**; JS path `geometry`) | `:258` `:272` `:288` `:356` `:438` `:462` `:677` |
| `building_footprints` | 427,077 | read (`id`, **`geom`**, `centroid_lat`, `centroid_lng`, `footprint_area_sqm`; JS path `geometry`); COUNT via the gate | `:288` `:356` `:407`; `massing-full-gate.js:39` |
| `pipeline_runs` | 59 | read (prior-run gate signals `massing-full-gate.js:41-46`; count is the 4-form slug set: `sources:` 14 completed / 3 failed · `permits:` 33 / 1 / **6 skipped** · `link_massing` 1 / 1 · `link-massing` 0) | SELECT only; the row is written by `run-chain.js` |
| `logic_variables` | 428 | read (3 `massing_*` keys; 0 `%link_rate%`) | via `loadMarketplaceConfigs(pool, 'link-massing')` `:167` |
| `pg_extension` / `information_schema.columns` / `pg_indexes` | catalog | read (PostGIS + `geom` column + GiST detection) | `:190` `:230` |

---

## 2. PH-3 — Intent ledger (commit 2 → G3)

### 2.1 G1 archaeology (recomputed)

```
$ git log --oneline -- scripts/link-massing.js | wc -l                    → 43   (--follow also 43; no rename)
$ git log --pretty=%s -- scripts/link-massing.js | grep -c "^fix("        → 30   (fix density 30/43 = 69.8%)
$ git log --format=%B -- scripts/link-massing.js | grep -ci "^severity:"  → 0    (fence density by footer = 0)
$ git log --format=%B -- scripts/link-massing.js | grep -ci "^lesson-routing" → 0
$ git log --oneline -- scripts/lib/massing-full-gate.js | wc -l           → 1    (2f3d0e4e; 0 Severity:)
  range: 66bfd529 2026-02-25 → 2f3d0e4e 2026-07-08
```
**G1 = 43 commits · 30 fix · 69.8% fix density · fence density 0 — identical to the plan.** The 0 is an instrument limit: the newest commit (2026-07-08) predates the Spec 05 §5 footer schema, so the fence corpus is the 30 `fix(` subjects adjudicated by `git log -S` per construct (§2.3). 20% change-coupling: **NOT computed** — no batch artifact or generator exists (U-2, inherited from pilots 1–2).

### 2.1a Risk class (G4, Spec 121 §3 PH-4 — `risk class` = `chance` × `impact`)

**Risk class: A.** Chance is HIGH — 69.8% fix density (30/43, the highest of any pilot to date) and 43 total commits over 5 months is genuine sustained churn, even though the footer-based fence count reads 0 (an instrument artifact, §2.1: the newest commit predates the Spec 05 §5 footer schema — the REAL fence corpus is 30 `git log -S`-adjudicated `fix(` commits, §2.3). Impact is HIGH — `link_massing` is a MATCHER writing `parcel_buildings` (485,135 primary links live), feeds `enrich-parcels.js`'s entire existing-structure block, and its writes are destructive (scoped DELETE + composite-key UPSERT, §4.1 C1/C2); a bad link silently mis-prices a building through the whole downstream cost model. Chance HIGH × impact HIGH lands class A on Spec 121 §3 PH-4's table (A(9) — the highest cell) — test intensity •••, matching the depth this pilot actually delivered (G7: 72 `it()` sites across 7 fences, all both-directions locked, plus a live forced-FULL relink with hash-equal proof, §6).

### 2.2 G2 structure — `ASSESSMENT-INCOMPLETE`

**`ASSESSMENT-INCOMPLETE` is claimed here because** no churn×complexity instrument exists to formally plot the quadrant, even though — unlike pilots 1–2 — the quadrant here is not degenerate: 740 lines × 69.8% fix density × 43 commits is the top-right by any reading, it just has no instrument to say so mechanically. Recorded per Spec 123 §6.2 clause 3. No churn×complexity instrument exists (`ls scripts/analysis | grep -i "churn\|complex\|risk"` → nothing). Not scored.

### 2.3 The fences — evidence and dispositions (RULED — operator, 2026-08-27, §7.1)

Each fence: what the commit guards (from `git show`), the construct it left in the file at `73c90d24` (`git log -S` confirms the introducing SHA), the reversion patch a both-directions lock must detect, and the disposition under Spec 122 §1.2a P1/P5 (vocabulary: Spec 120 §14.3), as proposed by this pass and **ruled by the operator on 2026-08-27 (§7.1, "Approve all as proposed"; F-JS additionally by the A-8 override)**. **Discoverer = this pass (Claude, commit 2). Adjudicator = the operator (2026-08-27, §7.1) on every fence.**

| # | Fence SHA · subject | What it guards (evidence) | Construct at HEAD | Reversion patch (the red direction) | Ruled disposition (operator, 2026-08-27, §7.1) |
|---|---|---|---|---|---|
| F-1 | `b16c036d` fix(56) flip PostGIS predicate to building-centroid-in-parcel | "a detached house covers ~35% of its lot, so the lot centroid lands in the yard" — coverage 57.5% → 99.7%; also added W1 to the PostGIS path, the nearest fallback (bbox `ST_Expand` before `ST_DWithin`), the GiST HALT, `78000.0`, confidence 0.90→0.95 | `ST_Contains(p.geom, ST_SetSRID(ST_MakePoint(bf.centroid_lng, bf.centroid_lat), 4326))` `:293`; `bf.geom && ST_Expand(p.geom, $2)` `:360` before `ST_DWithin` `:361`; `:246-256`; `:230-235`; `:267` | swap the `ST_Contains` arguments (parcel-centroid-in-building); drop `bf.geom && ST_Expand(...)`; drop the `if (FULL_MODE)` DELETE | `preserved-in-compute` (`buildMatchSql`, A-2) with the predicate + B-9/B-10 ordering DECLARED in `notes.json` + a shape lock (P5: the rule must be declared); the GiST HALT → `encoded-as-descriptor-field` (`guards.requires[].kind: index`, on_missing fail); W1 → `encoded-as-descriptor-field` (`retract:"all"`, `retract_when:"full_only"`); `78000.0` → `notes.json` + `guards.srid` |
| F-2 | `e99f6753` fix(28) flip the JS path to building-centroid-in-parcel ("The Yard Problem") | same intent as F-1 on the JS path; 0% → 98.9% | `booleanPointInPolygon(buildingPt, parcelFeature)` `:556` | swap point/polygon | **`knowingly-retired` with F-JS** (the intent survives in F-1's SQL predicate; the JS expression is the retired path) |
| F-3 | `7a0fc5a0` fix(28) reproject EPSG:3857→4326 in the JS path | footprint `geometry` JSONB was Mercator; Turf PiP returned false on every parcel → 0 links | `MERCATOR_ORIGIN` `:57`, `reprojectGeometry` `:68-87` (CRS sentinel `200` `:76`) | remove the reprojection call at `:418` | **`knowingly-retired` with F-JS** — the PostGIS path reads `building_footprints.geom` (SRID 4326 via `fc038790`'s `ST_Transform`), never `geometry`; record the 3857 origin in `notes.json` so the P4 grep does not re-open it |
| F-4 | `fc038790` fix(56) populate `building_footprints.geom` via `ST_Transform` + row-derived verdict + PostGIS-path FAIL gate | `geom` was 0/427K → `ST_Contains` matched 0 → `parcel_buildings` empty → cost model on lot-size fallback, hidden as a WARN | `linkRateStatus` `:691-693` (`hasPostGIS && processed > 0 ? 'FAIL' : 'WARN'`); `massingVerdict` `:705-706` row-derived | make `link_rate` WARN-only on the PostGIS path | `encoded-as-descriptor-field` — check `link_rate` with `limit_from_config` (T4) and severity `fail`; the `processed > 0` guard → `guards.empty_source` (B-2). The cascade itself → `preserved-in-runner` (`verdict.js deriveVerdict`) — LM-D2 |
| F-5 | `45906c31` fix(56) guard PostGIS detection against a missing `geom` column | migration 065 conditionally skipped the column; extension-only detection crashed on `bf.geom does not exist` | `has_geom_col` `:193-199`, warn `:200-204` | detect on `pg_extension` alone | `encoded-as-descriptor-field` — `guards.requires[]` `{kind: extension, name: postgis, on_missing: fail}` + `{kind: column, table: building_footprints, name: geom, on_missing: fail}` (A-8 override: the fallback becomes a fail-loud precondition, not a branch) |
| F-6 | `5bb31faf` fix(60) PostGIS argument order + deterministic primary + partial unique index (**B-8**) | `is_primary` received `'spatial_polygon'` (coerced true for ALL rows); multi-primary corruption; mig 081 partial unique index; pre-upsert `is_primary=false` clear so a primary shift cannot violate it | E1 `:317-323`; sort `area DESC, building_id ASC` `:328`; single-primary enforcement `:334-336` | drop E1; sort by `building_id` only | E1 → `encoded-as-descriptor-field` (`outputs.writes[0]` `set_based_scoped`, `guard:"none"` + `guard_why` citing this SHA, ordered BEFORE E2; `grandfathered.json`); the sort + single-primary rule → `preserved-in-compute` with the tie-break DECLARED in `notes.json` and the partial index in `guards.requires[]`. **Measured this session: E1 affects 0 rows on BOTH modes by construction** (W1 has emptied the scope in FULL; `NOT EXISTS` excludes linked parcels in INCREMENTAL) — the fence is latent, load-bearing only if scoping changes (LM-D3) |
| F-7 | `06320b39` fix(56) `linked_at` NULL on first INSERT (**B-12**, CRITICAL per its body) | `linked_at` was a trailing ON CONFLICT-only param, absent from the INSERT column list → first-time rows NULL | `RUN_AT` as the 7th VALUES param `:339-340` `:373-374` `:626-631`; `linked_at = EXCLUDED.linked_at` `:152` | move `linked_at` back to a trailing bind param | `encoded-as-descriptor-field` — `outputs.writes[1].columns[]` lists `linked_at` with `source: run_at`, `written: step`; the generated INSERT includes it. Lock: the generated SQL's column list contains `linked_at` |
| F-8 | `b36d0596` fix(37) `IS DISTINCT FROM` guard on the upsert (**finding 2**) | "unconditionally updated all ~479K rows and set `linked_at = NOW()` every run" | the 4-column guard `:153-156` with `linked_at` EXCLUDED from it | add `OR parcel_buildings.linked_at IS DISTINCT FROM EXCLUDED.linked_at` — or declare `guard_columns: "all_declared"` | `encoded-as-descriptor-field` — **explicit `guard_columns: [is_primary, structure_type, match_type, confidence]`** (A-3) + the LG-9 validator rule. The both-directions lock is the first one written at commit 6 (D-5): `all_declared` ⇒ 520,492 rows rewritten ⇒ `enrich_parcels` re-scopes 485,135 |
| F-9 | `82e33ad2` fix(31) N+1 → in-memory grid (B13) | 48 min → 11 min in full mode; the sources chain stopped timing out | grid index `:111-134`, `:402-427`, `:511-529` | — | **`knowingly-retired` with F-JS** — the PostGIS GiST join (`7c75e92e`, F-1) superseded it; the performance intent is carried by `guards.requires` on both GiST indexes (B-4) |
| F-10 | `3cc93feb` fix(28) OFFSET → keyset cursor | OFFSET degraded to O(N) per batch past 400K → the sources chain hung; 49 min → 99 s | `lastId` / `AND id > $2 ORDER BY id ASC LIMIT $1` `:270-280` (and `:459-472`) | revert to `OFFSET $2` | `preserved-in-runner` — the LINK read phase pages by keyset (`execution.batch` + `why` naming this SHA); lock: the generated batch SQL contains `id > $` and no `OFFSET` |
| F-11 | `0d5edf40` fix(28) cumulative link rate in the audit table | run-specific rate was a false FAIL (0/5,355) because the incremental pool is permanently unmatchable; removed the FAIL on `parcels_linked = 0` | `:674-684` cumulative query; no `run_matched` threshold `:698` | compute `parcelsLinked / processed` | `encoded-as-descriptor-field` — check `link_rate` declares its numerator/denominator as the cumulative pair (`observation.detail`), `why` citing this SHA; `run_matched` stays INFO |
| F-12 | `c1ef0b73` fix(47) advisory-lock retrofit (lock 91) + `RUN_AT` snapshot | `NOW()` in writes → bound `RUN_AT`; lock 91 in the registry | `ADVISORY_LOCK_ID = 91` `:37`, `withAdvisoryLock` `:165`, `getDbTimestamp` `:166` (B-11) | renumber; capture `RUN_AT` after the first write | `encoded-as-descriptor-field` — `identity.lock: 91` (kept textually, §5.4; `pipeline-advisory-lock.infra.test.ts:77/:246-253/:294-301`); `RUN_AT` → `preserved-in-runner` (`staleness` pre_compute captures it inside the lock) |
| F-13 | `d324ab27` fix(28) WF3-E19 externalize the three massing heuristics | `SHED 20 / GARAGE 60 / NEAREST 50` moved to `logic_variables`; `classifyStructure` takes explicit params | `LOGIC_VARS_SCHEMA` `:39-43`, `:170-172`, `classifyStructure(…, shedThreshold, garageMax)` `:89-96` | re-inline the literals | `encoded-as-descriptor-field` — `config.logic_variables[]` ×3 (already registered; live 20/60/50); compute reads `ctx.config` (P4). Lock: the four-surface battery |
| F-14 | `2f3d0e4e` feat(56) P11-2 `--full` gate (`massing-full-gate.js`, **B-5/B-6**) | always-on `--full` cost ~21.9 min per quarterly run; gate = DATA (footprint count) OR CODE (`LINK_MASSING_CODE_VERSION`) — "a pure data gate would have silently skipped the predicate FLIP itself"; missing pre-P11 signals treated UNCHANGED; `LINK_MASSING_FORCE_FULL=1` escape | `:174-183`; `decideMassingFull`, `evaluateMassingFullGate`, the 4-form slug array, `code_version` + `building_footprints_count` (string) in `records_meta` `:715-716` | make FULL unconditional on `--full`; drop `code_version` from the gate; finalize the ledger row before the gate reads it | `encoded-as-descriptor-field` — `staleness.trigger: [upstream_count, code_version]`, `fingerprint_inputs: ["scripts/lib/massing-full-gate.js"]`, `mode_select: tri_state`, `override.force_full: LINK_MASSING_FORCE_FULL`; the string type of `building_footprints_count` → `emits[]` with `consumers: ["link_massing"]`; the slug array → `sharing.slug_forms: derived` (`knowingly-retired` as code, reason: §1.7 forbids declaring it; `link-massing` has 0 rows) |
| F-15 | `9099a63c` fix(31) incremental mode — skip already-linked parcels | 486K → ~6.7K per permits run; 48 min → seconds | `incrementalFilter` `NOT EXISTS (… pb.parcel_id = parcels.id)` `:238-240` | drop the filter | `preserved-in-runner` — the LINK read phase's scope predicate, declared as `inputs.reads.tables[].scope` with `why`; C-10 bounds it (no `staleness.scope` keyed on centroid columns) |
| F-16 | `6a41143c` fix(31) 4 fixes — MultiPolygon, BBOX scaling, primary tie-break, ghost links (W1 on the JS path), and "empty catch block: log invalid geometry" | the JS-path W1 (`:447-457`); `:483-509` envelope; and **the `:554-559` catch that Spec 122 §1.6 names as a true silent swallow** — this commit turned an EMPTY catch into a `log.warn` with ids; it never added a counter | `:554-559` | remove the warn | **`knowingly-retired` with F-JS** for the JS constructs; the SWALLOW is registered as **LM-D7** so §1.6's "must be fixed, not declared" is honoured by retirement (C-6 resolves: nothing is pinned because nothing survives). The PostGIS equivalent — `ST_Contains(NULL,…)` → NULL → silently dropped (`:287` comment) — has NO counter either and is the live half of the swallow → **LM-D6** |
| F-JS | `7c75e92e` feat(60) PostGIS dual-path (+ `0c6128c6` lazy Turf, `c5ced678` streaming, `45906c31`) | "PostGIS when available (production), JS fallback when not (local dev without PostGIS)" | `:391-660` + helpers `:51-87` `:97-110` `:111-134` `:33-35` `:45-46` — **345 lines, 46.6%** | — | **`knowingly-retired` (RULED — operator, 2026-08-27, §7.1; A-8 override).** Evidence (U-5, measured): of the **12** completed audited rows, **11** carry `grid_cells = 'N/A (PostGIS)'`; the single JS-path row is run **1036** (2026-03-21) — BEFORE `7c75e92e` landed (2026-04-02), so **0 of 11 runs since the dual path existed took the JS branch**; live detection reads `has_ext = true, has_geom_col = true`. Replacement: `guards.requires` PostGIS extension + `geom` column, `on_missing: fail`. What the evidence does NOT cover: the 47 pre-audit-table rows (before `fcd6ff68`, 2026-03-21) and any environment other than the local DB |

**Non-fence constructs with a recovered why (structural, P4 S-series):** `PARAM_FLUSH_THRESHOLD 30000` `:47` (`50a3e41d`, PG 65,535 ÷ 7 with headroom → `execution.partial_fill`); `10000` progress cadence `:387/:656` (`66bfd529`/`7c75e92e` → `ctx.log`, D-9); `phase 8 : 9` `:727` (`5baaed5a` → `sharing.varies_by_chain.phase` map, both values live-verified); `pipeline.BATCH_SIZE` `:277` (`7c75e92e`); `>= 50` `:691/:701` (`fcd6ff68` 2026-03-21, no rationale recorded → **INTENT-UNKNOWN**, LM-D5); `0.95`/`0.60` (`e99f6753` for both; `b16c036d` raised the PostGIS 0.90 → 0.95 "so both paths agree") → T5/T6 registered variables; `'primary'/'garage'/'shed'/'other'` + `'centroid_in_parcel'/'nearest'` (`e99f6753`, `5bb31faf`) → `checks[].kind: vocab_coverage` candidate.

### 2.4 Intent Ledger (Spec 120 §14.3 — machine-readable; G3)

Discoverer ≠ adjudicator on every row (§7.1). Locks land in `src/tests/steps/link_massing/violations.test.ts` (G4d, both directions, commit 6). **Every row is adjudicated by the operator (2026-08-27, §7.1 — "Approve all as proposed"); the `knowingly-retired` rows additionally carry the A-8 override as their human approval (`#153`, `#162`).**

| Construct | Origin | Discovered by | Disposition | Adjudicated by | Where it lands |
|---|---|---|---|---|---|
| Building-centroid-in-parcel predicate + bbox-before-geography nearest fallback (`:288-296`, `:356-365`) | fence `b16c036d` fix(56) | agent pass, commit 2 (Claude) | `preserved-in-compute` (`buildMatchSql`, rule declared in `notes.json` + shape lock) | operator (2026-08-27, §7.1) | compute + `notes.json` |
| `parcels.geom` GiST HALT before the join (`:230-235`, B-4) | fence `b16c036d` | agent pass, commit 2 (Claude) | `encoded-as-descriptor-field` (`guards.requires[].kind: index`, fail) | operator (2026-08-27, §7.1) | descriptor `guards` |
| W1 FULL-mode mass DELETE before the loop (`:246-256`, B-7) | fence `b16c036d` (JS twin `6a41143c`) | agent pass, commit 2 (Claude) | `encoded-as-descriptor-field` (`retract:"all"` + `retract_when:"full_only"` on E2) — empty-source floor → LM-D10 | operator (2026-08-27, §7.1) | descriptor `outputs.writes[1]` |
| JS-path predicate flip (`:556`) | fence `e99f6753` fix(28) | agent pass, commit 2 (Claude) | `knowingly-retired` (with F-JS; intent carried by the SQL predicate) | operator (2026-08-27, §7.1; A-8 override) | LM-D7 |
| EPSG:3857→4326 reprojection + CRS sentinel `200` (`:51-87`) | fence `7a0fc5a0` fix(28) | agent pass, commit 2 (Claude) | `knowingly-retired` (with F-JS; `geom` is 4326 via `fc038790`) — origin recorded in `notes.json` | operator (2026-08-27, §7.1; A-8 override) | LM-D7 + `notes.json` |
| PostGIS-path `link_rate` FAIL gate + `processed > 0` guard (`:691-693`) | fence `fc038790` fix(56) | agent pass, commit 2 (Claude) | `encoded-as-descriptor-field` (check `link_rate`, `limit_from_config`, severity fail; `guards.empty_source`) | operator (2026-08-27, §7.1) | descriptor `checks[]` + `guards` |
| Row-derived verdict cascade (`:705-706`) | fence `fc038790` | agent pass, commit 2 (Claude) | `preserved-in-runner` (`verdict.js deriveVerdict`) (LM-D2) | operator (2026-08-27, §7.1) | library `verdict.js` |
| PostGIS detection requires BOTH extension AND `geom` column (`:190-204`) | fence `45906c31` fix(56) | agent pass, commit 2 (Claude) | `encoded-as-descriptor-field` (`guards.requires[]` extension + column, `on_missing: fail`) | operator (2026-08-27, §7.1) | descriptor `guards` |
| E1 `is_primary=false` pre-clear (`:317-323`, B-8) | fence `5bb31faf` fix(60) | agent pass, commit 2 (Claude) | `encoded-as-descriptor-field` (`writes[0]` `set_based_scoped`, `guard:"none"` + `guard_why`, ordered before E2; `grandfathered.json`) — 0-rows-by-construction recorded as LM-D3 | operator (2026-08-27, §7.1) | descriptor `outputs.writes[0]` |
| Deterministic primary: sort `area DESC, building_id ASC` + single-primary enforcement (`:328-337`) | fence `5bb31faf` (tie-break `6a41143c`) | agent pass, commit 2 (Claude) | `preserved-in-compute` with the tie-break declared in `notes.json`; the partial unique index in `guards.requires[]` | operator (2026-08-27, §7.1) | compute + descriptor `guards` |
| `linked_at = RUN_AT` on INSERT as the 7th param (`:339-340`, `:373-374`, B-12) | fence `06320b39` fix(56) CRITICAL | agent pass, commit 2 (Claude) | `encoded-as-descriptor-field` (`columns[]` `linked_at` `source: run_at`, `written: step`) | operator (2026-08-27, §7.1) | descriptor `outputs.writes[1].columns` |
| 4-column `IS DISTINCT FROM` guard EXCLUDING `linked_at` (`:153-156`, finding 2) | fence `b36d0596` fix(37) | agent pass, commit 2 (Claude) | `encoded-as-descriptor-field` (explicit `guard_columns` ×4, never `all_declared`; LG-9 rule) — first lock at commit 6 | operator (2026-08-27, §7.1) | descriptor `write_discipline.guard_columns` |
| In-memory grid index, B13 (`:111-134`, `:402-427`, `:511-529`) | fence `82e33ad2` fix(31) | agent pass, commit 2 (Claude) | `knowingly-retired` (with F-JS; superseded by the GiST join; intent carried by `guards.requires` on both GiST indexes) | operator (2026-08-27, §7.1; A-8 override) | LM-D7 |
| Keyset cursor `id > $2 ORDER BY id LIMIT $1` (`:270-280`) | fence `3cc93feb` fix(28) | agent pass, commit 2 (Claude) | `preserved-in-runner` (LINK read phase paging; `execution.batch` `why` names the SHA; lock: no `OFFSET`) | operator (2026-08-27, §7.1) | library `runLinkPhase` |
| Cumulative link rate, no FAIL on `parcels_linked = 0` (`:674-684`, `:698`) | fence `0d5edf40` fix(28) | agent pass, commit 2 (Claude) | `encoded-as-descriptor-field` (check `link_rate` declares the cumulative numerator/denominator; `run_matched` INFO) | operator (2026-08-27, §7.1) | descriptor `checks[]` |
| Advisory lock 91 + `RUN_AT` from the DB clock inside the lock (`:37`, `:165-166`, B-11) | fence `c1ef0b73` fix(47) | agent pass, commit 2 (Claude) | `encoded-as-descriptor-field` (`identity.lock: 91`, textual) + `preserved-in-runner` (`staleness` pre_compute captures `RUN_AT`) | operator (2026-08-27, §7.1) | descriptor `identity` + library |
| Three registered massing heuristics via `LOGIC_VARS_SCHEMA` (`:39-43`, `:170-172`) | fence `d324ab27` fix(28) | agent pass, commit 2 (Claude) | `encoded-as-descriptor-field` (`config.logic_variables[]` ×3; `ctx.config`) | operator (2026-08-27, §7.1) | descriptor `config` |
| Two-signal FULL gate (data count OR `LINK_MASSING_CODE_VERSION`), pre-P11 = unchanged, `LINK_MASSING_FORCE_FULL`, gate reads the last `completed` row (`:174-183`, `massing-full-gate.js`, B-5/B-6) | fence `2f3d0e4e` feat(56) | agent pass, commit 2 (Claude) | `encoded-as-descriptor-field` (`staleness.trigger[]` ×2, `fingerprint_inputs`, `mode_select: tri_state`, `override.force_full`; `emits[]` `code_version` + string `building_footprints_count` with `consumers: [link_massing]`) | operator (2026-08-27, §7.1) | descriptor `staleness` + `override` + `emits` |
| 4-form slug array in `evaluateMassingFullGate` | `2f3d0e4e` | agent pass, commit 2 (Claude) | `knowingly-retired` as code (§1.7 `sharing.slug_forms: derived`; `link-massing` form has 0 rows) | operator (2026-08-27, §7.1; A-8 override) | descriptor `sharing` |
| Incremental `NOT EXISTS` scope (`:238-240`) | fence `9099a63c` fix(31) | agent pass, commit 2 (Claude) | `preserved-in-runner` (`inputs.reads.tables[].scope` + `why`; bounded by C-10) | operator (2026-08-27, §7.1) | descriptor `inputs` + library |
| PiP `try/catch` warn-and-drop (`:554-559`) — the Spec 122 §1.6 silent swallow | fence `6a41143c` fix(31) (turned an empty catch into a warn) | agent pass, commit 2 (Claude) | `knowingly-retired` (with F-JS; LM-D7) — the live PostGIS twin (`p.geom IS NULL` / `ST_Contains → NULL` silently dropped, no counter) is LM-D6 | operator (2026-08-27, §7.1; A-8 override) | LM-D6 / LM-D7 |
| **JS fallback path (`:391-660` + helpers; 345 lines)** | `7c75e92e` feat(60) (+ `0c6128c6`, `c5ced678`) | agent pass, commit 2 (Claude) | **`knowingly-retired`** — reason: never executed since it existed (0/11 audited runs; U-5), replaced by fail-loud `guards.requires` PostGIS + `geom` column | operator (2026-08-27, §7.1; A-8 override) | LM-D7; closes at commit 7 |
| `>= 50` link-rate floor ×2 (`:691`, `:701`) | `fcd6ff68` — **INTENT-UNKNOWN** (no rationale in the commit) | agent pass, commit 2 (Claude) | `encoded-as-descriptor-field` (becomes tunable **T4** `link_massing_link_rate_fail_pct`, registered, `limit_from_config`; LM-D5) — the VALUE 50 is carried as the default and is the operator's to move via T4 | operator (2026-08-27, §7.1) | descriptor `config` + `checks[]` |
| `records_new: 0` literal (`:710`) | `d2050cfc` fix(37) — premise "never INSERT" false since the upsert | agent pass, commit 2 (Claude) | `knowingly-retired` (LM-D1; counters become `written.e2.{scanned,inserted,updated}`, Fold B item 6) | operator (2026-08-27, §7.1; A-8 override) | descriptor `counters` |
| `0.95` / `0.60` confidences ×2 sites each; `structure_type` / `match_type` vocabularies | `e99f6753`, `5bb31faf`, `b16c036d` | agent pass, commit 2 (Claude) | `encoded-as-descriptor-field` (T5/T6 registered variables with [0,1] bounds against `numeric(3,2)`; vocab → `checks[].kind: vocab_coverage`) | operator (2026-08-27, §7.1) | descriptor `config` + `checks[]` |
| Structural constants `PARAM_FLUSH_THRESHOLD 30000` · progress `10000` · `phase 8:9` · `pipeline.BATCH_SIZE` · `78000.0` · `4326` | `50a3e41d` · `66bfd529` · `5baaed5a` · `7c75e92e` · `b16c036d` | agent pass, commit 2 (Claude) | `encoded-as-descriptor-field` (`execution.partial_fill`, `execution.batch`, `sharing.varies_by_chain.phase`, `guards.srid`) or `notes.json` (`78000.0`) | operator (2026-08-27, §7.1) | descriptor `execution` / `sharing` / `guards` + `notes.json` |

**Count:** 26 rows · 17 fence SHAs named (`b16c036d` `e99f6753` `7a0fc5a0` `fc038790` `45906c31` `5bb31faf` `06320b39` `b36d0596` `82e33ad2` `3cc93feb` `0d5edf40` `c1ef0b73` `d324ab27` `2f3d0e4e` `9099a63c` `6a41143c` `7c75e92e`) · 1 `INTENT-UNKNOWN` (the value 50 → tunable T4) · **26 rows RULED by the operator (2026-08-27, §7.1 — "Approve all as proposed"; the `knowingly-retired` rows also under the A-8 override) · 0 rows PROPOSED**. Discoverer ≠ adjudicator on every row.

### 2.5 Line accounting (Spec 120 §14.5 Gate 4c — 740 lines, 100%, no overlap)

Categories: `runner-owned | validator-owned | descriptor-encoded | compute | dead (proved) | duplicate`. Measured against the working tree = `HEAD:scripts/link-massing.js` (**740 lines**; untouched since `2f3d0e4e`). Under rulings A-1 (`runLinkPhase`) and A-2 (`buildMatchSql`): *runner-owned* = the library's read / staleness / ordered-write / emit phases; *validator-owned* = declared checks evaluated by `verdict.js`; *compute* = pure classifiers + the SQL-text builder. `dead (proved)` is claimed ONLY for the JS path, and the proof is observational, not by reading: 0 of 11 audited runs since `7c75e92e` took it, live detection reads true/true, and the operator retired it (A-8) — Gate 4f is satisfied by measurement + ruling, not by inspection. Blank lines fold into the adjacent range.

| Lines | Category | Evidence |
|---|---|---|
| 1-28 | descriptor-encoded | header: SPEC LINK `:27` → `identity.spec`; the B13/grid narrative `:5-16` and the observability notes `:19-23` → `notes.json` (the grid narrative describes the retired path and is recorded as history, not interpretation) |
| 29-36 | runner-owned | `require`s: pipeline/safe-math/zod/config-loader `:29-32` → library; lazy Turf slots `:33-35` die with the JS path (LM-D7) |
| 37 | descriptor-encoded | `ADVISORY_LOCK_ID = 91` → `identity.lock` (kept textually, §5.4) |
| 38-43 | descriptor-encoded | `LOGIC_VARS_SCHEMA` ×3 → `config.logic_variables[]` (T1–T3, already registered) |
| 44-47 | descriptor-encoded | `BATCH_SIZE 500` (JS; retired, LM-D4) · `GRID_SIZE` (JS; retired) · `PARAM_FLUSH_THRESHOLD 30000` → `execution.partial_fill` (S2) |
| 48-50 | runner-owned | `massing-full-gate` require → `staleness` phase (`fingerprint_inputs` names the file, S10) |
| 51-87 | dead (proved) | `mercatorToWgs84` / `reprojectRing` / `reprojectGeometry` — `grep -n -e reprojectGeometry -e reprojectRing -e mercatorToWgs84 scripts/link-massing.js` → defs `:58` `:64` `:68`, internal calls `:65` `:81` `:84`, ONE external caller `:418` (inside the `hasPostGIS` else-branch `:391-660`); **0 hits** outside the JS path; the PostGIS path reads `geom` (SRID 4326 live) — F-3 retired with F-JS. **Zero-hit evidence (measured 2026-08-27):** `pipeline_runs` `grid_cells` rows across the 4-form slug set → 11 `'N/A (PostGIS)'` + 1 other; the single non-PostGIS row is run 1036 (2026-03-21, value `7502`), which predates `7c75e92e` ⇒ **0 hits on the JS branch since it existed**; live detection `has_ext = true, has_geom_col = true` |
| 88-96 | compute | `classifyStructure` — pure; thresholds via `ctx.config` |
| 97-110 | dead (proved) | `haversineDistance` — `grep -n haversineDistance scripts/link-massing.js` → `:101` (def) + `:578` (JS path only); **0 hits** outside the JS path. **Zero-hit evidence (measured 2026-08-27):** `pipeline_runs` `grid_cells` rows across the 4-form slug set → 11 `'N/A (PostGIS)'` + 1 other; the single non-PostGIS row is run 1036 (2026-03-21, value `7502`), which predates `7c75e92e` ⇒ **0 hits on the JS branch since it existed**; live detection `has_ext = true, has_geom_col = true` |
| 111-134 | dead (proved) | `gridKey` / `gridNeighbourKeys` — `grep -n -e gridKey -e gridNeighbourKeys scripts/link-massing.js` → `:117` `:124` (defs) + `:414` (`gridKey`, JS path only); `gridNeighbourKeys` has **0 hits** as a caller anywhere in the file. **Zero-hit evidence (measured 2026-08-27):** `pipeline_runs` `grid_cells` rows across the 4-form slug set → 11 `'N/A (PostGIS)'` + 1 other; the single non-PostGIS row is run 1036 (2026-03-21, value `7502`), which predates `7c75e92e` ⇒ **0 hits on the JS branch since it existed**; live detection `has_ext = true, has_geom_col = true` |
| 135-162 | runner-owned | `flushInsertBatch` = E2's generated `guarded_upsert` (`write.js`, composite key LG-2, explicit `guard_columns` A-3, `linked_at` in SET not guard) |
| 163-172 | runner-owned | `pipeline.run` → `pipeline.step`; lock 91; `RUN_AT` (B-11); config load + validate (B-13 → `hoisted_above_gate`, A-5) |
| 173-184 | runner-owned | the two-signal full gate + `fullModeReason` + the gate log line → `staleness` (`mode_select: tri_state`, `override.force_full`, LG-7/LG-10); `startTime` `:184` → `ctx.clock` |
| 185-206 | descriptor-encoded | PostGIS + `geom`-column detection and the fallback warn → `guards.requires[]` (extension, column; `on_missing: fail` — A-8 override); the `Mode:` log line → `ctx.log` |
| 207-222 | runner-owned | counter initialisation → per-target `written` (LG-5) + check observations |
| 223-235 | descriptor-encoded | `parcels.geom` GiST HALT (B-4) → `guards.requires[].kind: index` |
| 236-256 | runner-owned | `baseFilter` / `incrementalFilter` (B-2, F-15 scope) + W1 FULL DELETE (B-7) → read-phase scope + E2 `retract:"all"` `retract_when:"full_only"`; the empty-source floor is LM-D10 |
| 257-262 | runner-owned | work-set COUNT + log → read phase (`expect_nonempty`, `guards.empty_source`) |
| 263-268 | compute | `nearestDegSpan = nearestMaxDistanceM / 78000.0` — S7 declared in `notes.json`; feeds `buildMatchSql` |
| 269-282 | runner-owned | keyset batch loop (F-10; `execution.batch` = `pipeline.BATCH_SIZE` 1000) |
| 283-299 | compute | the MATCH join SQL (F-1 predicate; B-10 bbox prefilter) → `buildMatchSql` (A-2); `matchedParcelIds` |
| 300-316 | compute | group-by-parcel + `footprint_area_sqm` parse |
| 317-324 | runner-owned | E1 `set_based_scoped` clear (F-6, B-8) → `outputs.writes[0]`, `guard:"none"` + `guard_why`; `rowCount` discarded (LM-D3) |
| 325-344 | compute | deterministic sort + single-primary enforcement + `classifyStructure` + row assembly (`0.95`, `'centroid_in_parcel'` → T5 / vocab) |
| 345-349 | runner-owned | E2 flush + counter accumulation → `written.e2` |
| 350-365 | compute | NEAREST SQL (B-9 order, B-10 `ST_Expand` before `ST_DWithin`) → `buildMatchSql` |
| 366-382 | runner-owned | nearest row assembly (`0.60`, `'nearest'`, `is_primary=true` → T6) + E2 flush + counters |
| 383-390 | runner-owned | `processed` / `noMatch` accumulation + `pipeline.progress` (S8 → `ctx.log`, declared diff D-9) |
| 391-660 | dead (proved) | the JS fallback branch: Turf lazy-load, `streamQuery` grid build, JS-side count + W1 twin (`:447-457`), `BATCH_SIZE 500` loop, envelope scaling, PiP + the `:554-559` swallow, haversine fallback, classification, param-threshold flushes — **retired (A-8, LM-D7)**. **Zero-hit evidence (measured 2026-08-27):** `pipeline_runs` `grid_cells` rows across the 4-form slug set → 11 `'N/A (PostGIS)'` + 1 other; the single non-PostGIS row is run 1036 (2026-03-21, value `7502`), which predates `7c75e92e` ⇒ **0 hits on the JS branch since it existed**; live detection `has_ext = true, has_geom_col = true` (§2.3 F-JS, U-5); the JS W1 is a `duplicate` of `:246-256` and the JS classification a `duplicate` of `:325-344`, both subsumed by the retirement |
| 661-672 | runner-owned | `durationMs` (`ctx.clock`) + the `Linking complete` structured log → `ctx.log` |
| 673-693 | validator-owned | cumulative `link_rate` query (F-11) + `linkRateStatus` (F-4; `>= 50` T4 → `limit_from_config`; `hasPostGIS && processed > 0` → `guards.empty_source`) — declared check `link_rate` |
| 694-704 | validator-owned | the 9 metric rows → declared checks (8 INFO + `link_rate`; D-4; `parcel_buildings_written` keeps its id) |
| 705-706 | runner-owned | `massingVerdict` row-derived cascade → `verdict.js deriveVerdict` (LM-D2) |
| 707-733 | runner-owned | `emitSummary`: counters `:709-711` (LM-D1 → `counters`), 12 `records_meta` keys → `emits[]` (incl. the string `building_footprints_count` contract), `phase` ternary `:727` → `sharing.varies_by_chain.phase` (S12), `audit_table` → library emit |
| 734-737 | descriptor-encoded | `emitMeta` reads/writes → `inputs.reads.tables[]` (+ `geom` on both, LM-D9 / D-1) and `outputs.writes[].columns` (D-2) |
| 738-740 | runner-owned | lock-contention `return;` `:739` → declared terminal `lock_held_elsewhere` with a row-derived SKIP emit (LM-D8) |

Coverage: 36 ranges, 1-740 contiguous, sum 740, no line assigned twice. `dead (proved)` = 37 + 14 + 24 + 270 = **345 lines (46.6%)**.

---

## 3. PH-5 — Seam map (commit 3 → G5)

| Seam | Named seam | Anchors | Notes / planned home |
|---|---|---|---|
| **DB seam** | the `pool` injected by `pipeline.run` `:164` / `withAdvisoryLock` `:165`; `client` from `withTransaction` `:143` `:247` `:448` | `pool.query` ×10 (`:190` `:230` `:258` `:272` `:288` `:320` `:356` `:438` `:462` `:677`) · `client.query` ×3 (`:144` `:248` `:449`) · `loadMarketplaceConfigs(pool)` `:167` · `getDbTimestamp(pool)` `:166` · `evaluateMassingFullGate(pool)` `:178` · `streamQuery(pool)` `:407` (JS) | → `ctx.pool`; the join and the three writes move to `runLinkPhase` (A-1); config → `config.js`; the gate's two reads → `staleness.js` (LG-7). **Transaction topology today: W1 in its own txn, E1 autocommit, E2 one txn per flush** — no step-level txn; `execution.txn_scope` must declare this honestly (a FULL run that throws after W1 has already committed the DELETE — §1.5 T6) |
| **Clock seam** | **split.** (a) `pipeline.getDbTimestamp(pool)` `:166` → `RUN_AT`, the ONLY value written to a timestamp column (`linked_at`, 3 sites) — **Spec 47 §R3.5 posture is CLEAN**: DB clock, captured once inside the lock, before any write (B-11); 0 `new Date(`. (b) **`Date.now()` ×4** `:184` `startTime` · `:400` `loadStart` · `:426` `loadElapsed` · `:662` `durationMs` — wall clock for ELAPSED time only; two of the four (`:400` `:426`) die with the JS path | `Date.now()` 4 · `new Date(` 0 · `NOW()` in SQL 0 | → `ctx.clock` for the two surviving elapsed reads (`compute-no-wall-clock` bans `Date.now()` in a compute); `RUN_AT` captured by `staleness` pre_compute inside the lock. `records_meta.duration_ms` `:713` is the only emitted wall-clock value (excluded in §5) |
| **Network** | **none** — 0 `fetch(`, 0 http requires | — | `execution.network: "none"` — the first pilot with an empty network seam |
| **argv seam** | **indirect, one frame up.** `pipeline.isFullMode()` `:176` → `pipeline.js:712-714` `process.argv.includes('--full')`; 0 direct `process.argv` in the file | `:176` consumed at `:179-182` (`explicitFull`) | `--full` arrives ONLY from `manifest.chain_args.sources` (§1.7). → `execution.invocation.<chain>.argv` ≡ `manifest.chain_args` (conformance) + `staleness.mode_select: tri_state`; the library reads argv (LG-10), the compute never does. **No lint can see this read** (`compute-no-process-env` matches `process.env` only — C-12): state it as a rule enforced by review + the conformance assertion |
| **env seam** ×2 | `process.env.LINK_MASSING_FORCE_FULL === '1'` `:177` · `process.env.PIPELINE_CHAIN === 'sources'` `:727` | consumed at `:179-180` (`forceFull`) and `:727` (`phase`) | → `override.force_full: "LINK_MASSING_FORCE_FULL"` (E1) · `sharing.varies_by_chain.phase: {sources: 8, permits: 9}` (S12; standalone = 9 today, INCIDENTAL). No `accept_anomaly` needed — the step has no accept-override |
| **JOIN PREDICATE** (the fifth surface — Spec 123 §6 G5 does not enumerate it; **finding 1**) | **none today** — two domain SQL strings with no declaration slot | MATCH `:288-296` (`bf.geom && p.geom AND ST_Contains(p.geom, ST_SetSRID(ST_MakePoint(bf.centroid_lng, bf.centroid_lat), 4326))`) · NEAREST `:356-365` (`bf.geom && ST_Expand(p.geom, $2) AND ST_DWithin(p.geom::geography, bf.geom::geography, $3) … ORDER BY p.id, ST_Distance(...)`) | **Planned home: compute exports a pure `buildMatchSql(descriptor)` SQL-text builder (A-2, no pool); the library executes it.** B-9 (centroid pass before nearest) and B-10 (bbox before geography) are owned there and locked by a shape test on the emitted SQL text; `guards.srid: 4326` covers the literal; the P1 shortfall (a domain string is not descriptor data) is recorded in `limitations[]` with a `check_id` |

Invocation (verified, §1.7): `run-chain.js:638-646` — `PIPELINE_CHAIN=<chain>` + `chain_args[chain]`; `sources` is the only chain that passes `--full`; the step reads `PIPELINE_CHAIN` itself (`:727`), unlike pilot 2's loader.

---

## 4. PH-6 — Classification (commit 4 → G6)

### 4.1 CONTRACT — must survive conversion

| # | Behaviour | Anchor | Consumer / lock |
|---|---|---|---|
| C1 | `parcel_buildings` write shape: 7 columns, composite conflict target `(parcel_id, building_id)`, 4-column `IS DISTINCT FROM` guard with `linked_at` EXCLUDED, `linked_at = RUN_AT` on INSERT and UPDATE | `:145-158`, `:339-340`, `:373-374` | `enrich-parcels.js buildMassingScopeWhere` `:363-367` (`pb.linked_at > p.massing_enriched_at` — live scope 0; `all_declared` ⇒ 485,135); `pipeline-sdk.logic.test.ts` `IS DISTINCT FROM` lock; `chain.logic.test.ts:1544` |
| C2 | W1 → E1 → E2 order; W1 only in FULL mode; ONE DELETE before the loop scoped to `centroid IS NOT NULL` | `:246-256` → `:320-323` → `:346/:378` | B-7/B-8; `idx_parcel_buildings_one_primary` (0 violations live); `massing-full-gate.logic.test.ts` source contract "a FULL run still runs the ghost-link cleanup" |
| C3 | FULL_MODE = `forceFull || (explicitFull && gate.changed)`; the gate reads the LAST `completed` prior row's `code_version` + string `building_footprints_count`; pre-P11 signals = unchanged | `:176-182`, `massing-full-gate.js:29-58` | the next run's own gate (self-consumed, byte-stable); `massing-full-gate.logic.test.ts` 12 `it` |
| C4 | `records_meta` 12 step-written keys incl. `code_version`, `building_footprints_count` (STRING `"427077"`), `full_mode`, `full_mode_reason` | `:712-732` | `evaluateMassingFullGate` `:49-56`; admin quality UI |
| C5 | `audit_table.phase` 8 (`sources`) / 9 (`permits`), `name 'Building Footprint Linking'`, the 9 metric ids (esp. `parcel_buildings_written`), row-derived verdict | `:694-731` | `run-chain.js`, `/api/quality`; `chain.logic.test.ts:1544` asserts the id `parcel_buildings_written` by name |
| C6 | `link_rate` = cumulative `COUNT(DISTINCT parcel_id) / COUNT(parcels with centroid)`, floor 50, FAIL on the PostGIS path when `processed > 0`, else WARN | `:677-693`, `:701` | F-4, F-11; live 99.71% (485,135 / 486,530) |
| C7 | Incremental scope = centroid-bearing parcels with NO `parcel_buildings` row (the 1,395 tail); incremental never retracts | `:237-240`, `:273-274` | F-15; C-8 (`review_followups` line 13 confirmed: 1,395) |
| C8 | Building-centroid-in-parcel predicate, `bf.geom && p.geom` prefilter, `p.geom IS NOT NULL`, SRID 4326 | `:288-296` | F-1; `massing.logic.test.ts` predicate-flip `describe`; `db/link-massing.db.test.ts` geometric proof |
| C9 | Nearest fallback ONLY over the batch's unmatched parcels, `ST_Expand` bbox BEFORE `ST_DWithin`, `DISTINCT ON (p.id) … ORDER BY ST_Distance`, single row → `is_primary=true`, `'primary'`, `'nearest'`, 0.60 | `:354-381` | B-9/B-10; `tasks/lessons.md`; live 103,530 `nearest` rows |
| C10 | Deterministic primary: sort `footprint_area_sqm DESC, building_id ASC`; exactly one `'primary'` per parcel; others by `classifyStructure` (shed < 20 ≤ garage ≤ 60 < other) with the three registered variables | `:89-96`, `:328-337` | F-6, F-13; mig 081 repair ORDER BY; live vocabulary (5 combos) |
| C11 | GiST-on-`parcels.geom` HALT BEFORE any join query; PostGIS requires BOTH extension AND `geom` column | `:190-204`, `:230-235` | B-4, F-5; `pipeline-sdk.logic.test.ts:1663` `hasPostGIS|pg_extension` loop |
| C12 | `RUN_AT` from the DB clock, captured inside lock 91 before any write; config validated inside the lock | `:165-169` | B-11, B-13 (A-5 declares the move to `hoisted_above_gate` as a diff); `pipeline-advisory-lock.infra.test.ts:77` |
| C13 | `PIPELINE_META` reads/writes as emitted (7 write columns; reads WITHOUT `geom`) | `:734-737` | `run-chain.js` (declared diff D-1/D-2 at commit 7 — `geom` added, LM-D9) |
| C14 | Exit 0 on every non-throwing terminal incl. FAIL verdict; throws on invalid config and missing GiST | `:169`, `:234`, `pipeline.js:463-484` | LR-D3 class (pinned, `blocking:false`); chain `completed_with_errors` |
| C15 | Counters: `records_total = processed`, `records_new = 0`, `records_updated = parcelsLinked` (PARCELS, not rows) | `:709-711` | `chain.logic.test.ts:1544` `not /records_updated\s*:\s*buildingsUpserted/`; `pipeline-sdk.logic.test.ts` `records_updated uses parcelsLinked` — **pinned as-is, replaced by declared `counters` at commit 7 (LM-D1, declared diff D-8)** |

> **Operational failure mode (C2, B-7 — observed commit 9, 2026-08-28):** a forced FULL killed mid-rebuild left `parcel_buildings` at 29,330 of 520,492 rows — the W1 retraction (`retract:"all"`, `retract_when:"full_only"`) commits before the batch loop, identical to the old script's behaviour. Recovery is another forced FULL (`LINK_MASSING_FORCE_FULL=1`). Not a regression; documented, not a defect.

### 4.2 INCIDENTAL — do not assert on

Log-line text (`:183` `:206` `:225` `:254` `:262` `:663`) · `pipeline.progress` cadence and format (`:387-389`, S8 / D-9) · `pipeline.run` tag `'link-massing'` `:164` (D-15) · standalone `phase = 9` (the `:727` else-arm) · `records_meta` key order · audit-row order · the `sys_*` rows · `buildings_indexed = 0` / `grid_cells = 'N/A (PostGIS)'` on the PostGIS path (`:695-696` — retired with F-JS, replaced by nothing: they carried no information on the live path) · `full_mode_reason` string forms `:180-182` (declared, but the exact text is not a consumer contract) · `matchedParcelIds` re-add at `:380` (bookkeeping) · the `4326` in comments · `nearestDegSpan` over-covering E-W (S7 — declared, not asserted numerically) · the JS-path warn at `:201-204`.

### 4.3 DEFECT — pin in current form, fix after (Spec 123 §3.1)

Each candidate re-verified at its anchor this session.

- **LM-D1** `:709-711` — `records_new: 0` is a LITERAL (`d2050cfc`, premise "never INSERT" — false: E2 is an INSERT … ON CONFLICT and `flushInsertBatch` returns a `rowCount` that cannot split insert from update); `records_total = processed` counts the permanently-unmatchable tail (1,395 on every incremental run, verdict PASS); `records_updated = parcelsLinked` counts parcels while `buildings_upserted` counts rows (both 0 on incrementals; 485,135 vs 520,492 on the last FULL, run 1401). Confirmed. **PIN**; closes at commit 7 via `counters` = `written.e2.{scanned, inserted, updated}` (Fold B item 6, declared diff D-8).
- **LM-D2** `:691-693` + `:705-706` — ⚠ **the plan's "parallel-boolean verdict at :705" does NOT re-verify literally**: `massingVerdict` IS row-derived (`massingAuditRows.some(r => r.status === 'FAIL') ? 'FAIL' : … 'WARN' : 'PASS'`, made so by `fc038790`), and `emitSummary` re-derives escalate-only from the same rows. The defect is (a) a hand-rolled per-script copy of the cascade (the AS-D1b fleet class — 13 copies), and (b) the `link_rate` row's SEVERITY is chosen by a boolean outside the rows — `hasPostGIS && processed > 0 ? 'FAIL' : 'WARN'` — so an empty-parcels run and a JS-path run both degrade a broken-geometry signal to WARN by a condition no row records. Confirmed. **PIN**; closes at peel 8b (`verdict.js deriveVerdict`; the guard becomes `guards.empty_source` + a declared check severity).
- **LM-D3** `:320-323` — E1 is an UNGUARDED set-based UPDATE (x-banned-for-new) whose `rowCount` is DISCARDED (`await pool.query(...)`, result unused) — no audit row, no counter. **Measured this session: it affects 0 rows on both modes by construction** — in FULL, W1 (`:249`) has already deleted every row in scope; in INCREMENTAL, `NOT EXISTS` (`:240`) admits only parcels with no rows. The fence (`5bb31faf`) is therefore latent: load-bearing only if the read scope and W1's scope ever diverge (e.g. a centroid re-computed onto a linked parcel). Confirmed. **PIN**; commit 7 declares it (`guard:"none"` + `guard_why` + `grandfathered.json`) and the library records `written.e1.updated` so the 0 becomes observable.
- **LM-D4** `:45` vs `:277` — `BATCH_SIZE = 500` (JS) vs `pipeline.BATCH_SIZE = 1000` (PostGIS) for the same work, undeclared and unexplained (`66bfd529` set 500 in 2026-02; `7c75e92e` used the SDK default on the new path without reconciling). Confirmed. **Closes with the A-8 retirement** at commit 7 — one `execution.batch: 1000` with a `why`; no unification decision is needed because only one path survives.
- **LM-D5** `:691` + `:701` — the verdict-bound link-rate floor is the bare literal `50` in TWO places (the comparison and the rendered `'>= 50%'`), `fcd6ff68` (no rationale — INTENT-UNKNOWN on the value), **0** registered `logic_variables` matching `%link_rate%` (428 rows live). Live rate 99.71% ⇒ a **49.7-point dead band** — the gate cannot fire before a catastrophe. Confirmed. **PIN**; closes at peel 8c (`link_massing_link_rate_fail_pct`, default 50, `limit_from_config` — the VALUE is the operator's to move).
- **LM-D6** — the step emits **no link-quality counter**. Measured: `nearest` primaries **103,530**, of which **48,561 (46.9%)** have `footprint_area_sqm > lot_size_sqm` and **33,184 (32.1%)** exceed it 2×; `centroid_in_parcel` primaries 381,605 with **13,347 (3.5%)** / 4,462 (1.2%); downstream `parcels.existing_data_quality_flag = 'footprint_exceeds_lot'` on **58,050** parcels — produced here, nulled one step later by `enrich-parcels.js`, invisible in this step's audit table. The PostGIS twin of the §1.6 swallow lives here too: `p.geom IS NULL` / `ST_Contains → NULL` parcels are silently dropped (`:287` comment) with no counter. Confirmed. **PIN**; closes at peel 8b as a NEW INFO check by `match_type` with this id (A-6 ruling, C-6 pin-then-add).
- **LM-D7** `:391-660` + `:51-87` `:97-110` `:111-134` `:33-35` `:45-46` — the JS fallback path: **345 lines (46.6%)** never executed since it existed (0/11 audited runs since `7c75e92e`; the only JS-path row, 1036, predates the dual path); has **no E1 clear** (asymmetric with B-8), `BATCH_SIZE 500`, haversine instead of geography, reads `geometry` (3857 JSONB) not `geom`, and carries the **`:554-559` silent swallow** Spec 122 §1.6 names by anchor (invalid geometry → warn → parcel counted as no-match, no counter). Confirmed. **RETIRED by operator ruling (A-8 override) — `knowingly-retired`, closes at commit 7** with `guards.requires` PostGIS + `geom` column, `on_missing: fail`. C-6 (§1.6 "fix" vs §3.1 "pin") dissolves: nothing is pinned because nothing survives.
- **LM-D8** `:739` — lock contention → bare `return;`; the SDK's skip emit carries no `audit_table`. **Live evidence: 6 `skipped` rows under `permits:link_massing`, every one with `records_meta` NULL** (runs 1522, 1018, …). Confirmed. **PIN**; closes at commit 7 (library SKIP summary + `self_skipped` terminal, declared diff D-14).
- **LM-D9** `:735` — `emitMeta` declares `parcels: [id, centroid_lat, centroid_lng, geometry]` and `building_footprints: [id, geometry, footprint_area_sqm, centroid_lat, centroid_lng]`; the live PostGIS path reads **`geom` on both tables and `geometry` on neither**. The declared read surface describes the retired path. Confirmed. **PIN**; closes at commit 7 (`inputs.reads.tables[]` derived — declared diff D-1).
- **LM-D10** `:246-256` — W1 deletes **520,492 rows** with no floor: an empty or truncated `building_footprints` (the `fc038790` incident: `geom` 0/427K) on a FULL run would DELETE everything, then link 0, then FAIL the `link_rate` row — after the deletion has committed in its own transaction. Confirmed by reading + `:143/:247` txn topology. **PIN**; closes at commit 7 as a `when: "pre_write"` check on the retraction (D-20; pilot 2's LR-D9 lesson applied forward, not retrofitted).
- **LM-D11** — **66,906** buildings are `is_primary` for MORE THAN ONE parcel (**167,443** parcels, 34.5% of linked); the partial unique index guarantees one primary per parcel, not one parcel per primary; no counter, no invariant. Confirmed. **PIN**; a declared INFO check candidate in peel 8b alongside LM-D6; the cost-model consequence is the already-filed `review_followups` WF3.
- **LM-D12** (Schema-Fidelity) — `parcel_buildings` RLS enabled, 0 policies; writes succeed only because `postgres` has `rolbypassrls = true`. A non-bypass role would run W1/E1/E2 against 0 rows silently → `parcel_buildings_written 0`, verdict PASS. Confirmed on the live catalog (LR-D7 class). **PIN**; closes at commit 7 (`write.js` `rls_bypass_or_policy` preflight declared in `guards`).

### 4.4 Defect Ledger register — `LM-D1 … LM-D15`

Fleet register: `docs/reports/defect-ledger.md` (rows appended this commit).

| ID | Anchor | One-line | Status | Closes at |
|---|---|---|---|---|
| LM-D1 | `link-massing.js:709-711` | `records_new: 0` literal; `records_total` = the unmatchable tail; `records_updated` counts parcels not rows | **CLOSED · 8b** | landed at commit 7 (`counters` from per-target `written.e2.{scanned,inserted,updated}`, D-8), CONFIRMED at 8b — the steady state reads 0/0/0 instead of 1395/0/0 |
| LM-D2 | `link-massing.js:691-693`, `:705-706` | hand-rolled verdict cascade (row-derived, AS-D1b class) + `link_rate` severity chosen by `hasPostGIS && processed > 0`, a boolean no row records | **CLOSED · 8a** | peel 8a — the step contributes no cascade (`verdict.js deriveVerdict` is the only one, `#199` locks it) and the boolean is unreachable: `hasPostGIS` retired with the JS path, `link_rate` carries a declared FAIL severity, and the empty-work condition is `guards.empty_source` + the `empty_source_guard` `pre_write` ROW |
| LM-D3 | `link-massing.js:320-323` | E1 unguarded set-based UPDATE, `rowCount` discarded; measured 0 rows on both modes by construction (fence `5bb31faf` latent) | **CLOSED · 8b** | landed at commit 7 (E1 `set_based_scoped` + `guard_why` + `grandfathered.json`, `written.e1` recorded), CONFIRMED at 8b |
| LM-D4 | `link-massing.js:45` vs `:277` | batch size 500 (JS) vs 1000 (PostGIS), undeclared divergence | OPEN | commit 7 — closes with the A-8 retirement; one `execution.batch` + `why` |
| LM-D5 | `link-massing.js:691`, `:701` | verdict-bound link-rate floor `50` ×2 literals, 0 registered variables; 49.7-pt dead band | OPEN · PIN | peel 8c — `link_massing_link_rate_fail_pct` + `limit_from_config` |
| LM-D6 | `link-massing.js` (absent counter); `:287` | no link-quality counter: 48,561/103,530 nearest primaries exceed their lot (46.9%), 13,347 centroid; 58,050 flagged downstream; NULL-geom parcels silently dropped | **CLOSED · 8b** | peel 8b — `nearest_footprint_gt_lot_count` INFO by `match_type`, same round trip as the link rate; re-measured post-tiebreak 50,790/103,530 nearest (49.06%), 13,347/381,605 centroid (3.50%) — the tiebreak itself added +2,144, declared |
| LM-D7 | `link-massing.js:391-660` + `:51-87` `:97-110` `:111-134` | JS fallback path: 345 lines, 0/11 runs since `7c75e92e`, no E1, BATCH 500, the `:554-559` §1.6 silent swallow | **CLOSED · 8a** (RETIRED by ruling, A-8 override) | commit 7 — `guards.requires` PostGIS + `geom` column, `on_missing: fail` · **peel 8a completes the replacement**: the COLUMN half was missing at commit 7, so a postgis-installed database with a pre-039 `building_footprints` would have thrown a raw `column bf.geom does not exist` from inside the first batch query instead of refusing before the first read. `guards.requires` now names `building_footprints.geom` and `parcels.geom`, both `on_missing: "fail"` (fence `45906c31`) · T7 (`link_massing_grid_degrees`, the retired path's grid-span tunable) stayed declared-but-dead through 8c; **its DECLARATION was retired at commit 9** after the armed §1.2a P4 gate caught it as read by nothing — removed from the descriptor, the seed, and `GlobalConfigCard` GROUPS; the local `logic_variables` DB row is left orphaned (review_followups, MED) |
| LM-D8 | `link-massing.js:739` | lock contention → bare `return;`; 6 live `skipped` rows with `records_meta` NULL | **CLOSED · 8a** | landed at commit 7 (`skipRecordsMeta` + `RUN_STATUS.SELF_SKIPPED`, D-14), CONFIRMED at 8a against this descriptor's `lock_held_elsewhere` terminal |
| LM-D9 | `link-massing.js:735` | `emitMeta` declares `geometry`, never `geom`; the live path reads `geom` on both tables | **CLOSED · 8b** | landed at commit 7 (`geom` on both read tables + `parcel_buildings` D-23), CONFIRMED at 8b against the captured `PIPELINE_META` |
| LM-D10 | `link-massing.js:246-256` | W1 FULL DELETE of 520,492 rows has no empty-source floor; commits in its own txn before any link is written | **CLOSED · 8a** | landed at commit 7 (`empty_source_guard` at `when: "pre_write"`, scored before `writes[0]`, D-20), CONFIRMED at 8a with the post-write half `mass_retraction_ratio` |
| LM-D11 | `parcel_buildings` live (66,906 buildings primary for >1 parcel; 167,443 parcels) | one-parcel-per-primary is neither enforced nor counted | **CLOSED · 8b** | peel 8b — `shared_primary_buildings` INFO: 65,318 buildings / 166,591 parcels (66,807/167,329 pre-tiebreak) (the plan's 66,906/167,443 drifted across two forced relinks — LM-D13's own instability) |
| LM-D12 | `parcel_buildings` live catalog (RLS on, 0 policies; role bypasses) | non-bypass role would write 0 rows silently → PASS | **CLOSED · 8a** | landed at commit 7 (`rls_bypass_or_policy` preflight in `write.assertWritePrivileges`), CONFIRMED at 8a — the privilege is MEASURED and reported by `write_privilege` on every run, not only the failing one |
| LM-D13 | `buildMatchSql.fallback_match_sql` (pre-conversion `link-massing.js:357-363`) | nearest fallback `DISTINCT ON (p.id) … ORDER BY p.id, ST_Distance(...) ASC` with NO TIEBREAK — 18,252 of 103,530 nearest links (17,566 tied at distance 0) can flip `building_id` between two identical FULL relinks | **CLOSED · 8b** | discovered at Fold C by MEASUREMENT, not by a `fix(` commit (two forced relinks hashed `a0023203` vs `0d2ea577` with all 8 invariants identical); pinned verbatim at commit 7 `9ff540ec`; peel 8b lands the declared rule `… ST_Distance ASC, bf.footprint_area_sqm DESC, bf.id ASC` with F5 (text) + `nearest-determinism.test.ts` (DB) both directions, and D-22 retires |
| LM-D14 | `scripts/lib/step/staleness.js` `resolveOverrides` (library) | `ctx.overrides.force_full` was never populated — `override.force_full` reached `selectMode` (which DECIDES the mode) but not the compute, so the declared `override_force_full_present` WARN observed `undefined` and read CLEAN on every run, forced ones included | **CLOSED · 8b** | found by EXECUTING peel 8b's forced relink and reading its rows: `full_mode_reason: "force_full_env"` beside `override_force_full_present: PASS`. `resolveOverrides` now returns `force_full` from the same source `selectMode` decides on; locked both directions in `step-library.logic.test.ts` |
| LM-D15 | `scripts/lib/step/config.js` `resolveConfig` + `scripts/lib/config-loader.js` `loadMarketplaceConfigs` (library) | a descriptor-declared variable with NO `logic_variables` row resolves through the seed clone silently and is stamped into `records_meta.config` indistinguishable from an operator-edited value — pilot 3's 4 new variables ran this way through commits 7/8a/8b | **CLOSED · 8c** | found by EXECUTING: `node -r dotenv/config scripts/seeds/apply-logic-variables.js` inserted 4/413 rows, proving they had never existed. `resolveConfig` now issues one `SELECT variable_key FROM logic_variables WHERE variable_key = ANY($1)` and THROWs for any declared name absent from the result, naming the remedy command; happy-path `values`/`stamp` unchanged; locked both directions in `step-library.logic.test.ts` |

### 4.5 G6 statement

Every behaviour in §1 is classified above (15 CONTRACT · 12 INCIDENTAL items · 14 DEFECT rows), and every DEFECT carries an `LM-D*` id present in `docs/reports/defect-ledger.md`. G6 is claimable at commit 4: the §2.3/§2.4 dispositions were adjudicated by the operator on 2026-08-27 (§7.1, "Approve all as proposed"; A-8 override for the JS path).

---

## 5. Non-determinism inventory (commit 5 → G1′; Spec 120 §14.2)

Declared BEFORE any old/new diff. Sources: `scripts/analysis/capture-step-golden.js` (`VOLATILE_KEYS` ×11, `VOLATILE_METRIC_PREFIXES` `sys_`, `VOLATILE_PATTERNS` ×7) **plus what a LINK adds** — the harness's default table-state is unusable here (520,492 rows > the 100,000 ceiling ⇒ `skipped_reason: 'over_ceiling'`; and `md5(string_agg(t::text …))` renders `id` + `linked_at`, both of which change on every FULL relink). Commit 5 grows it with the A-4 column projection ordered by the declared unique key. Closed vocabulary: `must-match-exactly | normalize-then-match | excluded-with-reason`.

| Key | Disposition | Reason / how |
|---|---|---|
| `key:pipeline_runs[0].id` | `excluded-with-reason` | serial PK (`VOLATILE_KEYS`) — standalone captures only (in-chain the row is `run-chain`'s) |
| `key:pipeline_runs[0].started_at` · `key:pipeline_runs[0].completed_at` · `key:pipeline_runs[0].duration_ms` | `excluded-with-reason` | wall clock / elapsed (`VOLATILE_KEYS`); written out in full because the inventory is machine-read key-by-key |
| `key:id` · `key:run_id` · `key:timestamp` · `key:elapsed_ms` · `key:elapsed_s` · `key:generated_at` · `key:checked_at` · `key:captured_at` · `key:summary.records_meta.chain_run_id` · `key:pipeline_runs[0].records_meta.chain_run_id` | `excluded-with-reason` | `VOLATILE_KEYS` (harness); `chain_run_id` added 2026-09-03 (WF2 "Rules 10/11/12 mechanical checkers" C2 — a per-invocation chain-run correlation UUID, R-B/R-U, run-scoped by design; stamped on both the emitted summary and the persisted `pipeline_runs` row) |
| `row:sys_duration_ms` · `row:sys_velocity_rows_sec` | `excluded-with-reason` | `VOLATILE_METRIC_PREFIXES` `sys_` — observed on every audited run (11 rows = 9 + 2) |
| `pattern:duration_literal` | `normalize-then-match` | `completed in 8.4s`, the `Linking complete` `duration: '7.9s'`, progress `— 7.8s —` → `<DUR>` |
| `pattern:iso_timestamp` · `pattern:pg_timestamp` · `pattern:rows_per_sec` · `pattern:run_id_literal` · `pattern:pipeline_runs_id_literal` · `pattern:pid_literal` | `normalize-then-match` | harness masks (`<TS>`, `<RATE>` for the progress line's `rows/s`, `<RUN_ID>`, `pipeline_runs <ID>`, `pid=<PID>`) |
| `key:summary.records_meta.duration_ms` (= `meta.duration_ms`) | `excluded-with-reason` | `Date.now() - startTime` `:662` — the only emitted wall-clock value (the harness key form, as the captures declare it) |
| `key:pipeline_runs[0].records_meta.duration_ms` | `excluded-with-reason` | The SAME wall-clock value as the row above, reached by its second path: on a STANDALONE capture the whole `records_meta` block is also persisted into the ledger row, so the harness strips the key twice — once out of the emitted summary, once out of `pipeline_runs[0]`. It appears in the post captures and not the pre ones for a declared reason (D-25): the pre-conversion standalone run wrote its ledger row under the HYPHEN slug `link-massing`, so the harness's slug-keyed `pipeline_runs` capture came back empty and there was no second copy to strip. `pipeline.step()` passes `identity.name`, the row becomes visible under `link_massing`, and this key is the visibility made explicit rather than a new source of noise. |
| `table:parcel_buildings.id` | `excluded-with-reason` | **bigserial, REASSIGNED on every FULL run** (W1 deletes 520,492 rows, E2 re-inserts them) — excluded from the projection; identical logical output, different ids |
| `table:parcel_buildings.linked_at` | `excluded-with-reason` | = `RUN_AT` (DB clock, `:166`) on every row touched; a FULL relink re-stamps all 520,492 (live: one watermark `2026-07-07T15:23:54.234Z`); an incremental touches 0 — excluded from the projection, but `count(*) FILTER (WHERE linked_at >= run.started_at)` is compared as `rows_changed` (0 on incrementals; 520,492 on a FULL) |
| `table:parcel_buildings.{parcel_id, building_id, is_primary, structure_type, match_type, confidence}` ordered by `(parcel_id, building_id)` | `must-match-exactly` | the column-projected content hash (A-4; measured 666 ms) — ORDER BY the declared unique key derived from `outputs.writes[].key`, never the pk. Cross-FULL stability is **U-1 until two FULL relinks are measured** |
| `table:parcel_buildings.count(*)` | `must-match-exactly` | 520,492 — and the incremental rerun must be byte-identical with `parcel_buildings_written = 0` |
| `invariant:one_primary_per_parcel` · `invariant:unique_pair` · `invariant:confidence ∈ {0.95, 0.60}` · `invariant:link_rate ≥ floor with denominator 486,530` | `must-match-exactly` | the 4 pinned invariants (`docs/reports/golden/link_massing/invariants.json`, Fold B item 7), asserted by count, not by trusting the constraint |
| `meta.code_version` · `meta.building_footprints_count` | `must-match-exactly` | the self-consumed gate contract (C3/C4); `building_footprints_count` compared as the STRING `"427077"` — a type change is a real diff, not noise |
| `meta.full_mode` · `meta.full_mode_reason` | `must-match-exactly` | **per invocation** — vary by capture axis, not by run: `sources` incremental → `false` / `incremental:gate_unchanged`; `permits` → `false` / `incremental:no_full_arg`; forced FULL → `true` / `force_full_env` |
| `meta.{parcels_processed, parcels_linked, buildings_matched, buildings_upserted, matches_centroid_in_parcel, matches_nearest, no_match_count}` | `must-match-exactly` | 1395 / 0 / 0 / 0 / 0 / 0 / 1395 on every incremental capture (the tail); FULL values recorded on the forced capture |
| `summary.records_meta.audit_table.{phase, name, verdict, rows[!sys_]}` | `must-match-exactly` | C5; `phase` is 8 or 9 per invocation (the `:727` ternary); rows compared after the `sys_` strip, order preserved (INCIDENTAL order, but stable); `buildings_indexed 0` / `grid_cells 'N/A (PostGIS)'` retire at commit 7 as a DECLARED diff (D-4) |
| `summary.records_{total,new,updated}` | `must-match-exactly` | 1395 / 0 / 0 on incrementals (LM-D1 pinned); the D-8 counter change at commit 7 is a declared diff, not normalised |
| `exit_code` · `signal` · `verdict` · `ledger_status` | `must-match-exactly` | exit 0 on every terminal (C14 pinned); `['completed']` in-chain |
| `meta.reads` · `meta.writes` | `must-match-exactly` | D-1 (`geom` added, `geometry` removed) / D-2 recorded in the post capture, not normalised away |
| `stdout` (after patterns) · `stderr` | `must-match-exactly` | the full-gate line, `Mode:` line, `Parcels to process: 1,395`, the progress lines (D-9 declared at commit 7) — a diff is reviewed |
| `env:LINK_MASSING_FORCE_FULL` · `argv:--full` · `env:PIPELINE_CHAIN` | `excluded-with-reason` | capture AXES, not output: 3 invocations × {incremental, forced FULL}; the forced FULL costs **21.9 min** (`pipeline_runs.duration_ms` 1,314,440 on run 1401) and re-stamps 520,492 `linked_at`s, re-scoping `enrich_parcels` from 0 to 485,135 — a budgeted act with a declared cleanup (D-19) |

---

> ⚠️ **The projected-hash row's `must-match-exactly` was TRUE ONLY OF THE INCREMENTAL CAPTURES until peel 8b.** Commit 5 measured three FULL relinks of the OLD code at `4b15b352` → `a0023203` → `0d2ea577` on 520,492 rows with all eight invariants identical every time — LM-D13, the nearest fallback's missing tiebreak, which left 18,252 of 103,530 links (17,566 tied at distance 0) to be resolved by plan order. It was pinned verbatim at commit 7 (a no-op differential may not move rows) and declared as D-22 “unstable by pin”. **Peel 8b lands the rule** — `ORDER BY p.id, ST_Distance(...) ASC, bf.footprint_area_sqm DESC, bf.id ASC` — and the two POST forced FULL relinks hash-EQUAL, so this row's disposition now holds on ALL THREE invocations and D-22 retires. `#150` asserts both halves: the OLD pair must still differ (the evidence) and the POST pair must agree (the fix).
>
> **MEASURED 2026-08-27, post peel 8b (`3322be5c`):** the POST pair was re-captured live rather than argued — `docs/reports/golden/link_massing/post/sources-full-forced-1.json` (16:52, `duration_ms` 1,635,861 = 27.3 min) and `…-forced-2.json` (17:14, `duration_ms` 1,166,639 = 19.4 min), both `full_mode_reason: "force_full_env"`, both 520,492 rows. Projected `parcel_buildings` hash is IDENTICAL — `329bbcb6ccd5c32d8b8e9ffca77ff255` — on both, and all 8 invariants match exactly (pb_unique_pairs_violations=0, pb_multi_primary_parcels=0, pb_rows=520492, pb_distinct_parcels=485135, parcels_with_centroid=486530, link_rate_pct=99.71, confidence_values=0.60/0.95, nearest_share_pct=19.89). **U-1 and D-22 are CLOSED-MEASURED.** Both verdicts read WARN — `override_force_full_present: true` (D-29-by-design, the env is standing) and a `rows_changed_ratio: [object Object]` message-stringification defect present in both files (filed LOW in `review_followups.md`). ⚠️ The files previously at these two paths (16:05 / 16:17) were invalid: `capture-step-golden.js` reported `records_meta.skipped: true, reason: "advisory_lock_held_elsewhere"` for both — link_massing never ran — yet the harness still hashed whatever `parcel_buildings` state another process's in-flight relink had left behind (239,676 rows / `77897697…` for the first, coincidentally 520,492 / `329bbcb6…` for the second) and still returned `verdict: PASS`. That pair proved nothing; the 16:52/17:14 recapture above is the first valid measurement of two genuine forced-FULL relinks and is filed as a MED harness defect (skip-then-hash-and-PASS) in `review_followups.md`.

### 5.1 Declared diffs ADDED at peel 8b (stated before the capture, §5.3)

| # | Diff | Old (measured) | New | Disposition |
|---|---|---|---|---|
| **D-26** | audit-table rows +2 | 17 declared checks + 2 `sys_` = 19 rows | 21 rows: `nearest_footprint_gt_lot_count` (INFO, by `match_type` — 50,790 of 103,530 nearest primaries, 49.06%; 13,347 of 381,605 centroid, 3.50%; denominators in the row) and `shared_primary_buildings` (INFO — 65,318 buildings over 166,591 parcels) | **NEW, INTENDED, C-6 pin-then-add.** LM-D6 / LM-D11. INFO by the A-6 ruling: standing data-quality populations a forced relink does not move, so they are REPORTED at the step that makes them and never gate it. No extra statement — both ride `cumulative_sql`'s single round trip (+887 ms warm / +2.9–3.6 s cold for the footprint join over 485,135 primaries, +322 ms shared-primary, +64 ms denominators) |
| **D-27** | the junction's projected content hash | `0d2ea577` on 520,492 rows | a NEW hash on the same 520,492 rows, EQUAL across two forced FULL relinks | **THE POINT OF THE PEEL** (LM-D13). Up to 18,252 rows change `building_id` while the row count, the unique-pair count, the one-primary law, the link rate, the confidence domain and the nearest share are all unchanged — the signature of an ASSIGNMENT change, and why the fix could not ride the no-op conversion diff |
| **D-28** | `match_nearest_fallback`'s `why` | the fallback described without a selection rule | the rule in words + the `ORDER BY` | declared documentation change: a reader of the descriptor can learn which building a tied parcel gets without reading the compute |
| **D-29** | `override_force_full_present` on a FORCED run | **PASS** — measured on the 2026-08-27 forced relink, which ran with `full_mode_reason: "force_full_env"` and retracted and rebuilt all 520,492 rows | **WARN** — the row fires whenever the env is standing | **A DEFECT FOUND BY EXECUTING (LM-D14), fixed here.** `staleness.resolveOverrides` returned the accept-anomaly flags and `force_run` but never `force_full`: the flag reached `selectMode` (which DECIDES the mode) and never `ctx.overrides` (which the audit row observes). A check whose entire purpose is catching a `LINK_MASSING_FORCE_FULL` left standing in production — a 21.9-min relink on every subsequent run plus a `linked_at` bump that re-scopes `enrich_parcels` from 1,395 parcels to 485,135 — reported CLEAN on every run, forced ones included. Library-wide and `#149`-clean; `load_ravines` declares `force_full: "none"` and resolves `false`. ⚠️ Consequence for this peel: BOTH forced captures were re-taken after the fix, so the golden pair certifies the committed code rather than the code that happened to be running when they were first measured |

## 6. Commit ledger (§12.16 — every commit names its done-test)

| Commit | Hash | Content | Done-test |
|---|---|---|---|
| 0 | `73c90d24` | Pilot 3 plan (Folds A+B; rulings A-1…A-7 as recommended, A-8 overridden → JS path retired; 13 before/after guarantees; gates 3/17) | plan re-read; every executable claim re-executed in this report (§1.1 — two phrasings did not re-measure literally: the `4326` "3 sites" and the JS-path share 46.6% vs ~36%; §1.3 `records_meta` bytes 1,145–1,249 by `pg_column_size` vs 2,026–2,039; §4.3 LM-D2 the `:705` cascade is row-derived, not a parallel boolean) |
| 1 | `9ff540ec` (this commit) | PH-0 boundary freeze re-derived at 740 lines (§1) — 8 exit constructs, 6 terminals, 9+2 metrics, the three disciplines W1/E1/E2 with SQL anchors, all read columns incl. the undeclared `geom`s, FK pair + 5 indexes + RLS, the STRING `building_footprints_count`, `records_new: 0` at `:710`, stdout/exit, the three invocations | `#6a` boundary-freeze table has `parcel_buildings` with an integer row count (520,492) and `parcels` / `building_footprints` / `pipeline_runs` / `logic_variables` rows |
| 2 | `9ff540ec` (this commit) | PH-3 Intent Ledger RULED (§2.3, §2.4 — 26 rows, 17 fence SHAs; operator §7.1 "Approve all as proposed" 2026-08-27 + A-8 override), line accounting (§2.5, 36 ranges, dead ranges with zero-hit evidence), G1 recomputed, G2 `ASSESSMENT-INCOMPLETE` | `#152` `#153` `#162` `#155` `#157` `#151a` `#6b` in `src/tests/steps/link_massing/violations.test.ts` (commit 6); `#162` green now that `adjudicated by` names the operator on every row |
| 3 | `9ff540ec` (this commit) | PH-5 seam map (§3) incl. the join-predicate surface with no seam, the argv read one frame up, the split clock (Spec 47 §R3.5 clean for writes; 4 `Date.now()` for elapsed) | `#6a` + descriptor `execution.invocation.<chain>.argv` ≡ `manifest.chain_args` (conformance, LG-10) |
| 4 | `9ff540ec` (this commit) | PH-6 classification + LM-D1..D12 appended to `defect-ledger.md` (§4) | `grep -c "^| LM-D" docs/reports/defect-ledger.md` = 14 |
| 5 | `9ff540ec` | golden master ×3 invocations (sources `--full` gated-incremental, permits, standalone) + one forced FULL, with the column-projected `parcel_buildings` hash ordered by `(parcel_id, building_id)` (`--table-order`, ceiling raised with the measured 666 ms) + `invariants.json` (4 pinned) | `node scripts/analysis/capture-step-golden.js --self-test`; `--compare` exit 0 on a repeat INCREMENTAL capture; `#150` two OLD captures normalise identical under §5 |
| 6 | `9ff540ec` | PH-7 test design — 44 55-A + 5 partials (`#181` N/A by subject; `#206` exercisable at k=3; `#160` surfaced as the C1 programme gate) + one both-directions lock per adjudicated fence, **the `guard_columns` lock (F-8 / D-5) written first**, proven red | `npx vitest run src/tests/steps/link_massing/` → RED set recorded |
| 7 | `9ff540ec` | descriptor + `notes.json` + compute verbatim (`buildMatchSql` + pure fns) + library growth LG-1…LG-7, LG-10 (`runLinkPhase`, ordered `writes[]`, composite key, `retract:"all"` + `retract_when`, argv reader) + `grandfathered.json` + 4 seeds + `GlobalConfigCard` GROUP + the 14-file re-home; JS path retired (LM-D7); frozen §5.1 shape | `step-conformance.infra.test.ts` green with 3 converted steps; the commit-7 vitest list in the plan; differential zero-diff vs commit-5 goldens after §5 normalisation on all three incremental invocations; `records_meta.{code_version, building_footprints_count}` byte-identical incl. the string type |
| 8a | `3322be5c` | peel: gating/error paths — the two-signal gate → `staleness` (`trigger[]` ×2, `fingerprint_inputs`, `mode_select: tri_state`, `override.force_full`), `hoisted_above_gate` (A-5) declared; **LM-D2 CLOSED** (no boolean-driven severity survives: `hasPostGIS` retired, the cascade is `verdict.js` only, the empty-work condition is a `pre_write` ROW), **LM-D7 completed** (`guards.requires` gains `building_footprints.geom` + `parcels.geom`, `on_missing: fail` — fence `45906c31`), **LM-D8 / LM-D10 / LM-D12 CONFIRMED** | `#154` + `massing-full-gate.logic.test.ts` 9 pure `it`s green against the lib + the tri-state arm proven both directions + differential green |
| 8b | `68e23678` | peel: verdict/audit — **LM-D13 FIXED** (the declared nearest tiebreak `bf.footprint_area_sqm DESC, bf.id ASC`; two forced FULL relinks now hash-EQUAL, D-22 retires, `#150` rewritten to compare the POST pair); **LM-D6** `nearest_footprint_gt_lot_count` + **LM-D11** `shared_primary_buildings` INFO checks added (pin-then-add, A-6); **LM-D14 FIXED** (`resolveOverrides` never populated `ctx.overrides.force_full`, so `override_force_full_present` could not fire — found by EXECUTING the forced relink and reading `full_mode_reason: "force_full_env"` beside a PASS on that row); LM-D1/D3/D9 CONFIRMED; LM-D8 demonstrated LIVE (a capture launched against a held lock 91 self-skipped with the declared row-derived SKIP table) | `#150` (POST forced pair hash-equal, OLD pair still differing) + F5 both directions in `violations.test.ts` + `nearest-determinism.test.ts` (40 shuffled executions each way, DB) + the `force_full` both-directions lock in `step-library.logic.test.ts` + `#154` + `#165` + differential green (declared diffs D-3/D-4/D-26/D-27/D-28) |
| 8c | `2ced0763` | peel: thresholds/checks — T4 (LM-D5 CLOSED), T5/T6 registered, `limit_from_config`, `pct <=` evaluable at the resolved bound; **LM-D15 FOUND AND CLOSED** (a declared variable with no `logic_variables` row resolved silently through the seed and was stamped as operator-set — `resolveConfig` now issues a presence `SELECT … = ANY($1)` and throws with the remedy command); **T4–T6 verified already wired at 8b** (`limit_from_config: "link_massing_link_rate_fail_pct"` at descriptor `:460`, `ctx.config` reads for the two confidence bounds at `link-massing.js:461` and the shed/garage thresholds at `:370-371`); **T7 retired per A-8** (the JS-fallback path's grid-span tunable has no consumer left to read it) — **the DECLARATION itself was left behind at 8c and retired at commit 9**, after registering `link_massing.js` in `converted.json` armed the §1.2a P4 conformance gate and it caught `link_massing_grid_degrees` as a dead declaration (read by nothing) | `#154` + `#165` + the 8c threshold-source locks + the LM-D15 both-directions lock in `step-library.logic.test.ts` |
| 9 | `68b8e361` | `converted.json` +1 (→ 3/62; `pending` entry deleted); `node scripts/hooks/check-step-shape.mjs` → "3 converted step file(s) enforced", 59/59 unconverted (was 60/60); arming the §1.2a P4 conformance gate found `link_massing_grid_degrees` (T7) DECLARED but consumed nowhere — a dead declaration, ruled a FALSE AFFORDANCE (an operator-editable registry knob with zero effect, not "visible retirement" — retirement visibility lives in **LM-D7**) and retired from descriptor/seeds/GROUPS/tests; the 3 pre-pilot `massing_*` seeds, which lacked `CONSUMED by link_massing` tags, gained them; differential RE-EXECUTED against the shipping tree (never trusted from the 8c message) on **4** invocations — forced FULL (`force_full_env`), permits, sources `--full` (gated incremental), standalone — ALL **520,492 rows**, projected `parcel_buildings` hash IDENTICAL `329bbcb6` on every one, all invariants identical; exactly ONE explained diff vs the 8c baselines (`records_meta.config.link_massing_grid_degrees` `0.003` → `undefined`, the T7 retirement → **LM-D7**) | `#158`; `check-step-shape.mjs` exit 0 with 3 enforced + 3 compute modules; differential green on all four invocations, zero UNEXPLAINED diffs (G8) |
---

## §R Reflection — see rulings R-A..R-F (2026-08-28)

Written 2026-08-28 after cutover `68b8e361` + output panel (Guardian PASS ×8; Observability 1 BLOCKING → LM-D16 `1768ddb3`). Every row is measured in-session, not inherited.

### (a) Low-confidence items

| Item | Why confidence is low (evidence) | What would raise it | Owner |
|---|---|---|---|
| Retirement vs declared-≡-consumed | Two 8c/commit-9 decisions collided; ruled in-session (R-A) without a prior spec rule | R-A schema + conformance landing; one more pilot exercising `config.retired[]` | library (R-A commit) |
| `recovery` category is decorative | descriptor said `resume: checkpoint`; measured kill left 29,330/520,492 rows and the next run went incremental | R-B reader in the staleness gate; a kill-and-rerun test at pilot 4 | pilot 4 (`link_wsib`) |
| Differential as a commit-message claim | 8c message claimed T7 retired; tree still declared it; caught only by re-executing | R-C fingerprint lockfile gate red in `npm run test` | library (R-C commit) |
| Cloud parity untested | LM-D15 throws mid-chain on cloud until `apply-logic-variables.js` runs; 3 chains have no `assert_schema` | R-D check + cloud dry-run before cutover lands there | ops HIGH followup |
| G7 mutation unsatisfiable | stryker `mutate` scoped to 3 `src/features/leads/lib` files; three pilots shipped on prove-red alone | R-E amendment (done) + bounded spike on `scripts/lib/compute/**` | followup spike |
| G4d bounded close | 6 named test files verified survived; full 17-fence semantic cross-check ran in the Guardian pass, not the plan | — (closed by Guardian PASS ×8) | closed |

### (b) Recurring / standard-shaping issues

| Issue | First seen | Expected to recur in | Resolution |
|---|---|---|---|
| Object-valued check details rendered `[object Object]` in `errors[]` | pilot 3, forced FULL capture (`68b8e361`) | every archetype with object `detail` (9 sites today) | RULED · LM-D16 fixed `1768ddb3` |
| Ledger-row ownership undeclared (`ownsLedgerRow` = !chainId) | pilot 3 output panel | every shared step | **DONE · pilot 4 WF3-C (LW-D13).** `records_meta.ledger_row` ∈ `{owned, chain_owned}` now stamped unconditionally by the runner (`scripts/lib/step/index.js`, both the happy-path and the self_skipped/advisory-lock-contention emit sites), from the already-computed `ownsLedgerRow(chainId)`. Both values proven reachable live: standalone `link_wsib` capture → `owned` (`docs/reports/golden/link_wsib/post/standalone.json`), `permits`/`sources` in-chain captures → `chain_owned` |
| Golden harness hashes a SKIPPED run as PASS off another process’s table | pilot 3 8b | any step captured while a lock is held | FOLLOWUP (MED, filed 8b) — R-C should add `skipped` ⇒ capture failure |
| `git_head` recorded as `unknown` when git is busy | pilot 3 forced capture | any capture during a concurrent commit | RULED · R-C hard-fail |
| Interrupted full retraction leaves a partial table | pilot 3 (measured kill) | every `retract_when: full_only` step (LINK, INGESTOR-B, MATERIALIZER) | RULED · R-B; reader DEFERRED-TO pilot 4 as a named step |
| Bash-tool 10-min cap vs 14–27 min forced FULL | pilot 3 | every large-table step | FOLLOWUP: detached-capture runbook line (docs/runbook) |
| Declared-but-unconsumed tunable survives a "retirement" | pilot 3 T7 | any step retiring a code path | RULED · R-A |

**Carried into the pilot 4 plan as named steps:** R-B reader + kill-and-rerun test; `records_meta.ledger_row` stamp; R-A/R-C/R-D exercised on a MATCHER; §R written before pilot 5 is planned.

---

## 7. Differential (commit 9 → G8) — re-derived this session

`node scripts/analysis/capture-step-golden.js --compare=<pre>,<post>` re-run against all four `docs/reports/golden/link_massing/{pre,post}/*.json` pairs that share a filename (`permits` 76, `sources-full` 67, `sources-full-forced-1` 119, `standalone` 69 — `sources-full-forced-2.json` in `pre/` has no post-conversion counterpart). Every difference falls into a named bucket; two needed real investigation (marked below) and both turned out to be real, currently-true facts rather than defects.

- **`stdout_lines`** — `sources-full-forced-1.json` alone shows 58 differences in `stdout_lines` (the largest bucket everywhere) — ad hoc `console.log` prose replaced by structured JSON log lines (`{"level":"INFO","msg":"...","tag":"[link_massing]"}`, Spec 47 §6), the same substitution pilots 1-2's differentials document in full. Every scenario file's `stdout_lines` bucket is the same substitution.
- **`invariants`** — `permits.json` shows 8 differences in `invariants` — the same `capture-step-golden.js` operator-supplied `--invariants=<file>` harness flag pilot 2's differential documents (present on the pre capture, absent on post); not step output.
- **`meta[0].reads.building_footprints[N]`** / **`meta[0].reads.parcels[3]`** — `"geometry"` → `"geom"` — a PIPELINE_META correction: the pre-conversion declaration named a column (`geometry`) that does not exist on either table; the post-conversion `emitMeta` call names the REAL column (`geom`), matching every SQL anchor in §1/§3. This is the declared metadata becoming ACCURATE, not a behaviour change.
- **`meta[0].reads.parcel_buildings`** — new read declared (`["parcel_id"]`) — the ghost-link cleanup / full-gate read of `parcel_buildings` (B-7/B-8, §4.1 C2) is now visible in `PIPELINE_META`, where it was previously undeclared.
- **`meta[0].writes.parcel_buildings[N]`** — column reordering in the declared write-column list (`"parcel_id"` no longer first; `"is_primary"` appears) — matches the descriptor's `outputs.writes[0].columns`, not a behaviour change.
- **`summary.records_meta.audit_table.rows`** — `sources-full-forced-1.json` alone shows 11 differences in `audit_table.rows` (new array entries, indices 9-19) — one new row per newly-declared check (Rule 1), same pattern as pilots 1-2. Every scenario file's `audit_table.rows` bucket is the same substitution (10 new rows on the three non-forced captures, 11 on the forced one).
- **`metric` / `status` / `threshold` / `value`** (existing `audit_table.rows[N]` fields) — surviving rows reorder/rename as new declared-check rows are inserted ahead of them, same as pilots 1-2.
- **`checks_failed`** / **`checks_passed`** / **`checks_warned`** / **`config`** / **`errors`** / **`ledger_row`** / **`terminal`** — the standard step-library `records_meta` fields (pilots 1-2's differentials document the mechanism); `config` surfaces `link_massing_link_rate_fail_pct` and the other declared tunables (Rule 3); `errors` is new to this pilot — an empty array on every capture here (no check errored), present because the standard contract always emits it.
- **`records_total`** / **`records_new`** / **`records_updated`** (`sources-full-forced-1.json` only) — `0`→`null`/real counts, the same Observer/write-archetype convention pilots 1-2 document — this capture is a genuine FULL write (`parcel_buildings` populated), so these carry real counts post-conversion rather than the pre-conversion hardcoded-0 pattern (§2.6's own line-accounting cites `records_new: 0 at :710` as a KNOWN pre-conversion literal).
- **`summary.records_meta.audit_table.phase`** (`standalone.json` only) — **investigated, not assumed**: `9` → `0`. `sharing.varies_by_chain.phase` (descriptor `:735`) declares an EXPLICIT map `{sources: 8, permits: 9}` with no `standalone` entry — this is exactly §3's own S12 finding, already on the record BEFORE this differential was written: *"Both values are live-verified... so the ternary cannot be wrong today"* and the seam map itself flags the pre-conversion standalone value 9 as **INCIDENTAL** (an accident of the old ternary's fallthrough, never a declared contract). The explicit map retiring that fallthrough and resolving an undeclared chain to `0` is the S12 ternary-retirement working as intended, not a regression — nothing downstream reads `audit_table.phase` for a standalone invocation (standalone is a dev/debug invocation type, not a chain).
- **`summary.records_meta.audit_table.verdict`** (`standalone.json` only) — **investigated, not assumed**: `"PASS"` → `"WARN"`. Read the actual WARN row rather than assuming a regression: `retired_var_row_present:link_massing_grid_degrees` fired WARN because the retired T7 logic variable (`link_massing_grid_degrees`, retired commit 9, LM-D7) still has a LIVE `logic_variables` seed row in this DB — a genuine, currently-true housekeeping gap (an operator-editable knob with zero effect left registered), not a step-code defect. The check exists specifically to catch this condition and is doing so correctly; the two captures were simply taken before and after the seed row was left behind.
- **`table_state[0].content_hash`** — differs because the two captures were taken at different points across this pilot's own multi-week timeline (peel 8b's tiebreak fix moved 2,144 links, `standalone.json`'s post hash `329bbcb6` matches the LATER, tiebreak-corrected, forced-FULL-verified state cited throughout §4/§6) — live-table time-skew, the same class pilot 2's differential documents, not a write-path change.
- **`pipeline_runs[0]`** (`standalone.json` only) — gained the full `records_meta` shape described above; mirrored, not a separate finding.

**G8 verdict:** zero unexplained diffs. Two buckets (`audit_table.phase`, `audit_table.verdict`) needed genuine investigation rather than pattern-matching against pilots 1-2's differentials — both resolved to real, already-documented facts (S12's own ternary-retirement note; LM-D7's own retirement leaving a live seed row), not regressions. No new LM-D row needed for a diff bucket.

## 7b. R-T addendum differential (WF2 "The Step Validator, Data-First", commit 3, 2026-08-30)

Spec 124 §2 Rule 13's R-T addendum lands `invariants[]`/`plausibility[]` (5 net-new invariants + 1 plausibility candidate, Finding 3) into this descriptor, plus the LM-D6/LM-D11 severity fix ruling R-H had left undelivered. Re-captured all four `post/` scenarios against this commit; every new diff bucket vs. the §7 baseline is named here, per the same G8 discipline:

- **`source`** (every `audit_table.rows[N]` entry) — new field (Fold B-3): `"check"` for every pre-existing `checks[]` row (a default, not a descriptor change — `checks[]` entries declare no `source` field of their own), `"invariant"`/`"plausibility"` for the 6 new R-T-addendum rows. Lets a consumer reading only the emitted audit table tell DATA rows from PROCESS rows without re-deriving it from array membership.
- **`pb_unique_pairs_violations`, `pb_rows`, `pb_distinct_parcels`, `parcels_with_centroid`, `nearest_share_pct`, `linked_parcel_null_centroid_count`** — 6 new `audit_table.rows` entries (5 `invariants[]` + 1 `plausibility[]`, `source:"invariant"`/`"plausibility"`), all `every_run` (measured this session — single-session medians, 131ms/30ms/55ms/196ms/37ms/677ms — well under the `invariants_every_run_budget_ms` 5000ms default; a ≥2-session grounder re-confirmation is a filed followup, not yet executed). `linked_parcel_null_centroid_count` is this pilot's own worked plausibility example (Fold A-5, PHYSICAL-BOUND): grounded in `scripts/lib/compute/link-massing.js:101`'s `PARCEL_ELIGIBILITY` predicate — a primary-linked parcel with a NULL centroid is structurally impossible under normal operation, checked against `parcels` (compute_centroids' own table), not link_massing's own write.
- **`warnings` / `checks_warned`** (newly non-empty, was `[]`/`0` on every capture in §7's baseline) — **the actual behaviour fix**, not just a relabelling: `nearest_footprint_gt_lot_count`/`shared_primary_buildings` (LM-D6/LM-D11) flip `severity: INFO → WARN` in the descriptor (ruling R-H, 2026-08-28, re-verified 2026-08-29 as undelivered and filed HIGH), AND `scripts/lib/compute/link-massing.js`'s own two compute functions change `violations: 0` (hardcoded) → the REAL combined over-lot / shared-building count. Measured live capturing this exact fix: the severity flip ALONE did nothing (the hardcoded `violations: 0` made `evaluateLimit('viol == 0', {violations:0})` read `ok:true` regardless of declared severity — the row rendered PASS with WARN severity already in force, until the compute-side change landed). Both rows now correctly read WARN on every run (a standing, non-zero population by design — Spec 48 §4.9: WARN + a self-announcing `retighten_when` ("zero rows"), never FAIL, never a silent PASS).
- **`retighten_when`** — new, optional `checks[]` field (extended from `invariants[]`/`plausibility[]`'s own field, same grammar) — populated on LM-D6/LM-D11 only, closing R-H's standing HIGH follow-up.
- **`table_state[0].columns`** — unchanged (still excludes `id`/`linked_at`, matching §7's own A-4/Fold-B-item-5 precedent) — re-captured with the SAME explicit `--table-columns`/`--table-order` this pilot's captures have always used; the auto-derivation `capture-step-golden.js` now supports from `outputs.writes[]` would have wrongly included `linked_at` (it is `written:"step"`, not `db_default`) had it been relied on instead of the explicit flag.
- **`source_fingerprint`** — moves (Fold B-4: the fingerprint algorithm itself now strips `last_measured` from a descriptor's `invariants[]`/`plausibility[]` entries before hashing, so a future re-time alone will NOT move it again — this commit's move is from the real, declared-contract descriptor change, not from `last_measured` churn).

**G8 verdict (R-T addendum pass):** every new diff bucket named above; `warnings`/`checks_warned`/`retighten_when`/`source` cited by name satisfies the generic-wrapper-excluded citation rule (`step-validate.mjs`'s G8 scorer). No unexplained diffs expected from this pass.

---

## Validation scorecard (generated)

> Generated by `node scripts/analysis/step-validate.mjs --step=link_massing --write` — Spec 123 §6, ruling R-R (2026-08-29).
> Regenerate with the same command; a stale block is a conformance-lock finding (`step-conformance.infra.test.ts`).

**Score: 16/17** · G9 Reflection: PASS · G4d fence-lock coverage: PASS · G-shape: PASS · **Hard stop: no**

| Gate | Score | Max | Detail |
|---|---:|---:|---|
| G0 | 1 | 1 | boundary-section=true spec-line=true |
| G1 | 1 | 1 | PH-3 section found=true sha-count=98 |
| G2 | 1 | 1 | 122-churn-complexity.md quadrant=bottom-right window=39313d9 |
| G3 | 1 | 2 | table rows=82 vocab-hit rows=43 |
| G4 | 2 | 2 | risk-class row with chance+impact found=true |
| G5 | 1 | 1 | db=true clock=true network=true argv/env=true |
| G6 | 3 | 3 | 16 ledger row(s), 0 without CLOSED/PIN () |
| G7 | 3 | 3 | file=true fences=7 it-count=75 RED-evidence=true |
| G8 | 3 | 3 | missing-invocations=0 missing-pre-invocations=0 stale-fingerprints=0 unexplained-diffs=0 |
| G9 (binary) | PASS | — | heading=true low-confidence-table=true recurring-table=true |
| G4d (fence<=lock) | PASS | — | fences=7 lock-it-count=75 |
| G-shape | PASS | — | file-clean=true compute-clean=true |

### Fast invariants (always run — the fast descriptor gate)

| # | Scope | Pass | Detail |
|---|---|---|---|
| 1 | link_massing | PASS | min_migration=81 <= migrations count=244 |
| 2 | link_massing | PASS | 6 declared, missing from seeds: none |
| 3 | link_massing | PASS | retired=1 overlap-with-declared=none |
| 7 | link_massing | PASS | SPEC LINK header present=true |
| 8 | link_massing | PASS | G-4: 6 declared, 1 verdict-affecting, 0 violate on_invalid:fail with no deviations[] cover |
| 20 | link_massing | PASS | HB-1: execution.shape="link" — HB-1 applies_when execution.shape=="enrich" only (RS-D-STA); not applicable, never a pass-by-omission |
| 21 | link_massing | PASS | CEIL-1: execution.shape="link" — CEIL-1 applies_when execution.shape=="enrich" only (RS-D-STA); not applicable, never a pass-by-omission |
| 4 | (registry) | PASS | overlap: none |
| 5 | (registry) | PASS | clean (0 it.fails( call sites outside a declared pending slug) |
| 9 | (registry) | PASS | clean (0 converted slugs blocked by an unmet cutover_prereq item; blocks batching: 0) |
| 22 | (registry) | PASS | GOLD-PRE-FRESH: 52 PRE capture(s) across 12 converted step(s) all tracked + clean (git can restore every reference) |
| 23 | (registry) | PASS | COMPRESSED-FORM-ELIGIBLE: not applicable (0 pending slugs declare the compressed form) |
| 24 | (registry) | PASS | COMPRESSED-FORM-DEFAULT: not applicable (0 pending slugs whose archetype is eligible) |

### Captures (item iv)
- missing invocations (POST): none
- missing invocations (PRE, GOLD-PRE): none
- stale fingerprints: none
- compare ran: true · diffs found: 434 · unexplained: 0

### Test suite (item iii)
- 1009/1037 passed (suite success=false)

### Policy coverage matrix (item vi) — Spec 124 Rules 1-13

| Rule | Name | Status | Note |
|---|---|---|---|
| 1 | Nothing hidden | enforced-green | G-1 schema-baseline: schema-baseline clean |
| 2 | Compute is just compute | enforced-green |  |
| 3 | Tunables externalized | enforced-green | G-4: 6 declared, 1 verdict-affecting, 0 violate on_invalid:fail with no deviations[] cover |
| 4 | Compute rule declared | enforced-green | G-2: 4 preserved-in-compute row(s), 0 with no why/notes.json/checks[] grounding |
| 5 | checks >= 1 | enforced-green |  |
| 6 | Omission fails (20 categories) | enforced-green |  |
| 7 | Archetype gates categories | enforced-green |  |
| 8 | Per-target write discipline | enforced-green |  |
| 9 | Banned write needs ledger (+ V7 no_retraction) | enforced-green |  |
| 10 | Verdict row-derived | enforced-green | (a) OK — 11 corpus file(s) scanned, 0 unsanctioned second derivations, 2 sanctioned hit(s) matched SANCTIONED_VERDICT_SITES · (b) OK — SELF_SKIPPED audit table folds to verdict=WARN (!= PASS), row-derived off 1 non-INFO row(s) — VRD-SKIP closed |
| 11 | Phase-order re-derive (declared half, checkOrderGuaranteesCited) | enforced-green | 1 when:"pre_write" check(s), 0 order_guarantee violation(s) — G-3 completeness half stays open |
| 12 | Truthful crash posture (R-B reachability, static + R-M before-image) | enforced-green | R-B (checkInterruptedPostureTruthful): shape=link runner=runLinkPhase: no staleness.ledgerGatedSkip early-return on this path; calls staleness.selectMode unconditionally, which folds detectInterruptedRetraction internally · R-M: prose-only (R-M/LG-17 describe not scoped to this step (vitest not run, or no before-image target)) |
| 13 | A step validates itself | enforced-green | this run of step:validate IS the mechanism |
| P3 | I/O cost adjudication (measured, not gated) | measured | descriptor=68637B notes=17389B checks=19 rows records_meta=4690B (newest post/ capture) |

**Enforced-green: 13/14**

