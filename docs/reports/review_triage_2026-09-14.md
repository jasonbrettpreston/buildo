# Review Queue Triage — 2026-09-14

_Generated by the weekly triage routine. Source: `docs/reports/review_followups.md` (2998 lines).
Do NOT edit `review_followups.md` directly; file new items there per WF close-out protocol._

---

## Queue Snapshot

| Metric | Value |
|--------|-------|
| Total active items (est.) | ~90 |
| HIGH severity | ~15 |
| MED severity | ~25 |
| LOW / NIT | ~50 |
| Filed since 2026-08-01 | ~12 |
| Items with **ACT** disposition | 5 |
| Prior triage report | None (first run) |
| Last commit to repo | 2026-08-27 (18 days ago) |
| Commits in past 14 days | **0** — no activity |

The repo has been quiet since 2026-08-27. All recent work (last 20 commits) is on the
`122_step_optimization` branch, focused on the Spec 122 `pipeline.step()` library and
the `load_ravines` C1 pilot 2.

---

## Stale Items

Items where the codebase was checked for a claimed resolution. **No items were found to be
stale** — all checked items remain open.

| Item | Claimed status | Grep / log evidence | Verdict |
|------|---------------|---------------------|---------|
| `shoelaceArea` dead code in `load-massing.js` | DEFER (2026-05-09) | `grep` confirms function at line 43, constant `SQM_TO_SQFT` at line 26 — both still present | STILL OPEN |
| `clampedLimit = NaN` when `input.limit` undefined (`get-lead-feed.ts`) | DEFER (2026-05-08) | Line 1004: `Math.max(1, input.limit)` — `undefined` input still produces NaN | STILL OPEN |
| `get-lead-feed.ts:121` colon separator in `lead_id` vs `--` expected by `parseLeadId` | DEFER (2026-05-06) | Line 121 still uses `permit_num || ':' || LPAD(...)` | STILL OPEN |
| `parcels_null_address_pct` gate unsatisfiable | ACT — WF3 (2026-08-25) | `git log -S parcels_null_address_pct` → only docs commits; no code fix | STILL OPEN |
| `massing_zero_link_ghost` comment wrong value | ACT — WF3 (2026-08-25) | `git log -S massing_zero_link_ghost` → only docs commits; no code fix | STILL OPEN |

---

## Top 5 This Week

Ranked by: (a) severity, (b) unblocking other items, (c) overlap with active `122_step_optimization` work.

### 1. `parcels_null_address_pct` gate is permanently unsatisfiable (MED)

**Rationale:** Filed 2026-08-25 with **ACT** disposition — the highest-urgency disposition
in the queue for non-critical items. The check WARNs at 100.0% on every cloud run because
`ADDRESS_NUMBER` was deliberately dropped from the source CSV; the threshold can never be
satisfied again. It poisons the WARN classification audit by permanently occupying one of the
eight WARN slots with a structural false alarm, masking real regressions.

**Location:** `scripts/lib/parcels-csv-drift.js:27-35`, `scripts/load-parcels.js:508-525`.

**Fix shape:** Retire the check outright, or rebase it onto
`address_points` / `parcel_address_points` bridge coverage (the documented replacement
for `ADDRESS_NUMBER`). The spec footnote at the filing site already names both options.

---

### 2. `massing_zero_link_ghost` founding comment claims a value that has never been observed (MED)

**Rationale:** Also filed 2026-08-25 with **ACT** disposition. The comment in
`enrich-parcels.js:1896-1901` asserts "Measured day-one value 0," but both cloud rows for
this metric read 1395. Either 1395 represents real orphaned links (a data bug) or the claim
is wrong (a doc bug). Either way the comment is actively misleading — operators will read it
and conclude the forward-looking baseline is 0 when it never was.

**Location:** `scripts/enrich-parcels.js:1896-1901`.

**Fix shape:** Query the ghost population to determine which case applies, then either fix the
orphaned links or correct the comment. Two-step: measure first, decide after.

**Groups with #1 into Sweep WF A** — both touch the same pipeline observation surface and can
ship in one WF3.

---

### 3. `*_dataset_version_when_enriched` stamps have no trigger invalidator (HIGH)

**Rationale:** Filed as Spec 122 P1 (HIGH). Three `_dataset_version_when_enriched` columns
are invalidated only inside `load-parcels.js`'s UPSERT — any other write path that changes
the underlying data leaves the stamps stale without notification. The fix shape is already
proven by the centroid-gap migration 245: add arms to the existing
`trg_parcels_invalidate_on_geom_change()` trigger. The filing note requires measuring the
current stale population first to judge urgency (migration 245 found 0/486,530 — a latent
defect). No enrich-parcels full re-run has executed since 2026-06-10, so this measurement
must be the first step.

**Location:** `scripts/load-parcels.js:353-361`, the `trg_parcels_invalidate_on_geom_change`
trigger definition (migration ≤245), `scripts/enrich-parcels.js` consumer.

**Unblocks:** The full-data re-run needed to verify #1/#2 would also measure this population.

---

### 4. `cost-model-shared.js` falsy-`0` triad — three HIGH bugs, one file (HIGH × 3)

**Rationale:** Three separate HIGH findings from Gemini (WF2 #3, 2026-05-08) all share the
same root cause (`||` vs `??`) in one file, making them a natural single WF3 sweep:

- `computeGfa` `(row.storeys || 1)` → inflates GFA for 0-storey foundation permits.
- `computeEffectiveArea` `pct > 0` gate → defaults zero-allocation rows to 1.0 (full GFA).
- `computeTradeValue` `complexity_factor || 1.0` → overrides operator-set `0` to 1.0.

All three silently inflate cost estimates. They have been deferred since May 2026.

**Location:** `src/features/leads/lib/cost-model-shared.js` lines 188, 227, 286.

**Fix shape:** Three `||` → `??` substitutions + tests. Under 10 LoC of code change.
**Groups into Sweep WF B.**

---

### 5. `get-lead-feed.ts` NaN clamp + silent INNER JOIN — three HIGH bugs, one file (HIGH × 3)

**Rationale:** Three HIGH bugs in `get-lead-feed.ts` from DeepSeek review (2026-05-08),
all deferred but all confirmed present against HEAD:

- `clampedLimit = NaN` when `input.limit` undefined → LIMIT NaN → Postgres error.
- `clampedKm = NaN` when `input.radius_km` undefined → `ST_DWithin` returns false → empty feed.
- `LEFT JOIN wsib_per_entity WHERE w.business_size IS NOT NULL` acts as INNER JOIN → silently
  drops 30-50% of builder leads (new contractors, GTA-condition failures).

**Location:** `src/features/leads/lib/get-lead-feed.ts` lines 1003-1005, ~line 600 area.

**Fix shape:** Add `?? DEFAULT_FEED_LIMIT` / `?? DEFAULT_RADIUS_KM` before the clamp;
remove the `WHERE w.business_size IS NOT NULL` predicate (feed query handles NULL at render).
**Groups into Sweep WF C.**

---

## Proposed Sweep WFs

### Sweep A — Pipeline WARN calibration (WF3)

**Name:** `fix/pipeline-warn-calibration`
**Scope:** `scripts/lib/parcels-csv-drift.js`, `scripts/load-parcels.js`, `scripts/enrich-parcels.js`
**Items:** 2 (both MED, both ACT disposition, both 2026-08-25)
**Effort:** Small — retire one check constant; measure + fix/document one comment
**Gate:** Re-run `parcel-sanity-audit.js` after; WARN count should drop by ≥1

Covers items #1 and #2 from the Top 5. These are the only **ACT** items in the entire queue
that have no in-progress commits.

---

### Sweep B — Cost-model falsy-0 (WF3)

**Name:** `fix/cost-model-falsy-zero`
**Scope:** `src/features/leads/lib/cost-model-shared.js`
**Items:** 3 HIGH (`computeGfa`, `computeEffectiveArea`, `computeTradeValue`) + 1 LOW
(`BULK_COLUMN_COUNT` derivation) + 1 MED observability calibration
**Effort:** Small — three `||` → `??` substitutions; existing cost-estimate test suite gates correctness
**Gate:** Run `npx vitest related src/features/leads/lib/cost-model-shared.js --run`

Covers item #4. The falsy-0 triad has been queue-resident since May 2026; bundling is the
cheapest way to clear three HIGHs.

---

### Sweep C — Lead-feed query bugs (WF3)

**Name:** `fix/lead-feed-query-bugs`
**Scope:** `src/features/leads/lib/get-lead-feed.ts`
**Items:** 3 HIGH (NaN clamp ×2, INNER JOIN) + 1 MED (cursor NULL)
**Effort:** Small — `?? DEFAULT` on two clamps; remove one WHERE predicate; add COALESCE to cursor
**Gate:** `compute-cost-estimates.db.test.ts` (add if missing) + typecheck

Covers item #5. The INNER-JOIN builder-lead drop (30-50%) is the most user-visible of the
three; shipping these together keeps the blast radius small (single file, all read-path).

---

## Recommended Sequencing

1. **Sweep A first** — clears the only ACT items; unblocks the full enrich-parcels re-run
   needed to measure item #3 staleness. Small scope, no DB schema change.
2. **Sweep B** — one-file, three HIGHs cleared. Run before any pilot cost-model work.
3. **Sweep C** — one-file, three HIGHs cleared. Gate on a live-DB test for builder-lead drop.
4. **Item #3** (invalidator trigger) — needs staleness measurement from the re-run in step 1.
   File as its own WF3 once the population count is known.

---

## Items Noted but Not in Top 5

**Deliberately excluded (high severity but deferred by design):**

- `createPool()` localhost default retirement (HIGH, Spec 122 §P0) — blast radius across all
  27 manifest steps; requires its own measured commit. The filing note explicitly guards this
  behind "verify every invocation path first." Not actionable without that measurement pass.
- Non-blocking FAIL invisible to `/api/quality` (MED, Spec 122 §S2-min) — DEFER disposition:
  must not be resolved until a pilot declares a non-blocking FAIL check. No unblocking action
  available this week.
- Parcels 1944170/1944175 massing-mislink ($105.24M cost) (HIGH) — the T2 bounds in the
  archetype plan neutralize the COST consequence; the parcel-side fix requires its own WF3
  with a Reality-Check pre-run. Higher complexity than the Top 5; schedule after Sweep A/B/C.

---

_Report generated 2026-09-14 by the weekly triage routine. Next scheduled triage: 2026-09-21._
