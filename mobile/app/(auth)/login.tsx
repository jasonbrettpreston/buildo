import { View, Text } from 'react-native';

export default function LoginScreen() {
  return (
    <View className="flex-1 bg-bg-feed items-center justify-center">
      <Text className="text-text-primary text-2xl font-bold">Buildo</Text>
      <Text className="text-text-secondary mt-2">Phase 1 — Auth Shell</Text>
    </View>
  );
}
