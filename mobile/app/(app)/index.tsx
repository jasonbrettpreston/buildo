import { View, Text } from 'react-native';

export default function LeadFeedScreen() {
  return (
    <View className="flex-1 bg-bg-feed items-center justify-center">
      <Text className="text-text-primary text-xl font-semibold">Lead Feed</Text>
      <Text className="text-text-secondary mt-1 text-sm">Phase 3 — Spec 91</Text>
    </View>
  );
}
