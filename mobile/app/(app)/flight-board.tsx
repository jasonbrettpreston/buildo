import { View, Text } from 'react-native';

export default function FlightBoardScreen() {
  return (
    <View className="flex-1 bg-bg-feed items-center justify-center">
      <Text className="text-text-primary text-xl font-semibold">Flight Board</Text>
      <Text className="text-text-secondary mt-1 text-sm">Phase 5 — Spec 77</Text>
    </View>
  );
}
