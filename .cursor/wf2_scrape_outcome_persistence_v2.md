# Active Task: WF2 — hold per-permit scrape outcomes in the database (v3 — 8-reviewer plan panel folded)
**Status:** Planning — panel COMPLETE (Gemini · DeepSeek · Code Reviewer · Observability · Integration · Regression Guardian · Schema-Fidelity · Reality-Check) + fold-verification final gate PASSED (3 corrections applied in place: Spec-114 no-catalog F4-1, browser kind-mapping completion, runbook README index F4-2). **AUTHORIZED by operator 2026-07-31 ("y and proceed with your recommendations on D1 and D2"). D1 RULED: 90-day raw + rollup, pg_cron prune (235-hardened shape). D2 RULED: dedicated connection with reconnect/backoff; SAVEPOINT fallback. Status: Implementation.**
**Domain Mode:** Backend/Pipeline. Parent: `.cursor/wf2_deep_scrapes_restore.md` §QUEUED WORK item 4.
**Provenance:** v1 implemented-before-planned, rejected by 9-reviewer panel, reverted 2026-07-31; adjudication in `.cursor/wf2_scrape_outcome_persistence.md`. v2 (this file's prior revision) built on the ten binding corrections C1–C10; the v3 fold below is the 8-reviewer panel's output on v2. C1–C10 remain binding and are not re-litigated.

## Context
* **Goal (operator-directed):** *"understand why the inspections data failed for a permit — so we could take corrective action — we need to hold the data in the database."* Per-permit, append-only, diagnosable after the run ends, identical from both transports (`SCRAPER_TRANSPORT=http|browser`).
* **Target Spec:** Spec 44 §3/§5 · Spec 47 §R11 + §7.8 (Tier 3 ledger) · Spec 48 §3.6/§3.7 · Spec 114 (RLS) · Spec 115 §5 (pg_cron catalog) · Standards §3/§11.
* **Key Files:** `migrations/236_permit_scrape_outcomes.sql` (new), `scripts/aic-scraper-nodriver.py`, `scripts/aic-orchestrator.py`, `scripts/tests/test_scrape_outcome_persistence.py` (new), `docs/specs/_contracts.json` + `src/tests/contracts.infra.test.ts`, `src/tests/db/`, `docs/runbook/` (§3.7 artifact).
* **Out of scope:** the `populate_queue` re-queue-forever defect (item 5, lifecycle epic) — but see D1's re-evaluation follow-up.

## ⚠ THE STRUCTURAL FOLD — classification threading is NEW WORK the v2 plan wrongly assumed existed
**Four reviewers converged (Code Reviewer CRITICAL conf-90 · Integration F1 · Regression Guardian Fence-1 UNADDRESSED · DeepSeek taxonomy MED):** the `transport_error`/`waf_blocked` distinction exists ONLY inside `HttpTransport.call` (`:1935` vs `:1938-1942`, plus `unparseable :1945-1946`). Every one of the four call sites in `fetch_permit_chain_http` (`:2019-2021, :2031-2032, :2042-2043, :2056-2057`) and every browser-path parse error in `fetch_permit_chain` (`:2115, :2154, :2181, :2212` — including all `fetch_error:*`) collapses to `{'waf_blocked': True}`; `scrape_year_sequence:2242-2243` erases it again with an untyped `raise`; `scrape_with_retry:2516-2523` returns only `retry_exhausted`. **Shipping v2 as scoped would persist misclassifications as durable DB facts — every network hiccup recorded as `waf_blocked` forever — WORSE than today's in-memory-only mislabel (Guardian's framing).** No existing test pins the distinction at the chain-result layer (only at raw `t.call()`), which is how the collapse survived.
**RULED (jury): thread the classification — new Execution Plan step 4a.** The alternative (drop `transport_error` from the vocabulary, ship later) was rejected: the operator's goal is *why it failed*; a vocabulary that cannot distinguish a proxy blip from a WAF ban does not answer it, and a CHECK value the code can never emit is a standing lie (Guardian's own dichotomy). Scope, stated honestly: the `fetch_permit_chain*` return contract gains a `failure_kind` (+ `detail`) carried through `chain_result` → typed exception (or result field) through `scrape_year_sequence` → preserved by `scrape_with_retry` — 8 call sites + 2 functions, touching the K7a fence lines (`:2113-2119`) on both transports. Kind mapping: `transport_error`→`transport_error` (browser `fetch_error:*` → `transport_error` with `detail`), `waf_blocked`→`waf_blocked`, `unparseable`→`waf_blocked` (the Akamai hollow/HTML signature is its known cause); browser `html_or_empty`/`json_decode_error`/`non_string_result:*` → `waf_blocked` (same Akamai-hollow rationale as HTTP `unparseable`). **New chain-result-layer pytest lock: a `transport_error` kind must NOT surface as `waf_blocked` — the lock 5dc577f2 never got.** K7a logging sites preserved byte-for-byte (they log, never route).

## Technical Implementation (v3)

### Migration 236 — `permit_scrape_outcomes` + `permit_scrape_outcome_rollup`
* `permit_scrape_outcomes`:
  `id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY` ·
  `permit_num VARCHAR(30)` (nullable — see resolution fallback; matches permits-PK width, the locked `permit_num_max=30` contract; SF-F6) ·
  `year_seq VARCHAR(30)` (nullable — the raw queue key, always populated for batch-derived outcomes) ·
  `CHECK (num_nonnulls(permit_num, year_seq) >= 1)` ·
  `outcome TEXT NOT NULL CONSTRAINT chk_permit_scrape_outcomes_outcome CHECK (...)` (named per SF-F5; vocabulary below) ·
  `detail VARCHAR(500)` (sanitized + truncated — see writer) ·
  `transport TEXT NOT NULL CONSTRAINT chk_permit_scrape_outcomes_transport CHECK (transport IN ('http','browser'))` (NOT NULL per DeepSeek) ·
  `run_id TEXT` (nullable — run correlation per DeepSeek HIGH: orchestrator exports `SCRAPER_RUN_ID` = `GITHUB_RUN_ID` or a generated uuid to workers; standalone runs stamp their own) ·
  `observed_at TIMESTAMPTZ NOT NULL DEFAULT now()`.
  No FK on permit_num (C1 reasoning stands) — `-- FK-EXEMPT` rationale comment (218 precedent). **Revision-grain ambiguity accepted knowingly (RC):** `permit_num` is not unique in `permits` (revisions are rows; 21,281 permit_nums have >1); this ledger is deliberately permit_num-grain. Duplicate outcome rows from a retried ambiguous insert are tolerated-and-documented (append-only diagnostic ledger; writer does not retry within an attempt).
* Indexes: `(permit_num, observed_at DESC)` + **`(observed_at)` for the prune** (DeepSeek). Plain CREATE INDEX (new tables, not LARGE_TABLES — validated).
* **`ENABLE ROW LEVEL SECURITY` on BOTH tables** (SF-F1 — the Spec 114 §11 definitional pgTAP guard fails any new public table without it; zero policies = Class B default deny, `admin_backup_codes` 231 precedent).
* `permit_scrape_outcome_rollup`: **PK `(permit_num, outcome, transport)`** (Gemini CRITICAL — transport must survive the horizon), `occurrences BIGINT`, `first_at`, `last_at`.
* DOWN block **comment-only** (validator Rule 6 + hook; SF-F4/Int-F3), `-- DROP TABLE IF EXISTS ...` documentation form. No apostrophes in comments (validator hygiene).
* **Database Impact: YES.** New tables only; no backfill (stated N/A).

### D1 — retention/rollup (numbers CORRECTED by Reality-Check — v2's were a 2.4× overcount of the wrong population)
Eligible population = the 3 `ALL_TARGET_TYPES` only: **~11.6K permits** (cloud 11,620 / dev 11,595 measured), not 28,004 (that figure lacked the `permit_type` filter). Worst-case (re-queue-forever): **~605K rows/yr**; 90-day raw steady-state **~150K**; rollup bound ~11.6K × 8 ≈ **93K**.
* **RECOMMENDED (a): 90-day raw + prune-time rollup.** Prune job follows the **migration-235-hardened** pg_cron shape (SF-F2): SECURITY DEFINER `SET search_path = pg_catalog`, fully schema-qualified, per-row `WHEN OTHERS`, durable `pipeline_runs` summary row, `REVOKE FROM PUBLIC/anon/authenticated`; function unguarded (CI-testable), `cron.schedule()` in a `pg_extension`-guarded DO-block with NOTICE-skip + unschedule-by-name idempotency (233 structure) — keeps the step-2 testcontainer green. Spec 115 §5 catalog row added (eligibility: never-must-succeed — a prune qualifies).
* **Prune is ATOMIC + IDEMPOTENT (Gemini CRITICAL + DeepSeek):** one transaction — `DELETE ... WHERE observed_at < cutoff RETURNING *` into the upsert `INSERT ... ON CONFLICT (permit_num, outcome, transport) DO UPDATE SET occurrences = rollup.occurrences + excluded.occurrences, first_at = LEAST(...), last_at = GREATEST(...)`. Mid-failure rolls back both halves; re-run cannot double-count. Concurrency-safe vs live inserts (cutoff predicate). Tested.
* **Re-evaluation follow-up (Gemini MED):** the worst-case rests on the re-queue defect; when item 5 lands, re-evaluate retention length (likely extend) — filed in review_followups at implementation.

### The writer (C2/C3/C10 + panel folds)
* **C2:** dedicated psycopg2 connection per worker, lazily opened, autocommit, **stored on the worker and closed at teardown** (Gemini); **on failure: close + lazily reopen with bounded backoff — the writer must RECOVER on the next insert, not die for the worker's life** (DeepSeek HIGH); a recovery test proves it. SAVEPOINT remains the documented fallback (D2).
* **C3:** stamping untouched (`:2278-2358`, de2e4b75 — Guardian: KNOWINGLY PRESERVES, mechanism verified). **Explicit (Gemini): `address_not_found` is NO-STAMP** — a permit deleted from source must stay visible to staleness monitoring; test added. Failure outcomes (`waf_blocked`/`transport_error`/`retry_exhausted`): outcome row, no stamp.
* **`detail` sanitization at the writer (Gemini HIGH + DeepSeek):** allowlist of known-safe prefixes (`fetch_error:<ExceptionClass>`, classifier strings); anything else truncated to class-name only; hard 500-char cap; defense-in-depth over the `d295188b` relay redaction, never instead of it.
* **C9:** resolution = the orchestrator `populate_queue` SUBSTRING derivation (**`aic-orchestrator.py:154`** — v2's `:3122` cite was the scraper's standalone-mode copy; both exist), once per claimed batch, **`SELECT DISTINCT permit_num`** (RC — revision rows duplicate permit_nums; 4 already in the eligible set). **Zero-resolution fallback (DeepSeek HIGH): the row is written anyway with `permit_num NULL, year_seq = <raw key>`** — the anomalous outcome the feature exists for must never vanish; `outcome_resolution_failures` counter added; tests for empty and multi-row resolution. Non-queue paths (standalone/batch-file): per-permit outcomes write directly; batch-level outcomes carry `year_seq`.
* **C10:** swallow-and-count; counter added to **BOTH** hand-maintained lists — the worker merge (`:271-284`) **and `aggregate_telemetry()` (`:339-379`)** (Obs — one without the other silently defeats the audit row).
* **F6c:** `scrape_with_retry` returns `outcome: 'retry_exhausted'`; `accumulate_result` handles it explicitly. **Execution-plan line (Guardian, CONDITIONALLY PRESERVED): `:2651-2654` (WAF-trap forcing) stays a separate top-level `if`, independent of the outcome-key `elif` chain** — folding it in silently retires the forced rotation.

### Vocabulary (C5) — verified real by RC; mechanism ruled
`{scraped, no_stages, no_inspection_link, no_target_folders, address_not_found, waf_blocked, transport_error, retry_exhausted}` — all 8 are reachable code strings at verified lines; 5 observed live, 3 unexercised-but-real (RC: "unverified live, not absent"). `fetch_error:*` = `detail` under `transport_error` (made true by step 4a's threading; DeepSeek's taxonomy objection resolves because the distinction now genuinely survives). **Mechanism (Integration F4, ruled): literal frozenset in the scraper + test-time grep-pin from `_contracts.json`** (the mig-228 `entitlement_products` pattern; first Python-side consumer — the fs-read+regex mechanism handles `.py`), NOT a runtime JSON reader. Live-CHECK equality via the `BUILDO_TEST_DB=1` integration test querying `pg_constraint`. (DeepSeek's `BUILDO_TEST_DB` typo NIT — refuted, real var.)

### Observability (C7/C8 + panel folds)
* **C7:** audit rows in the orchestrator aggregate (`:560-604`, row-derived cascade verified). **Total-outage tripwire (3-reviewer convergence + in-repo precedent `:575/:580`): `FAIL iff permits_attempted > 0 AND outcome_write_failures >= permits_attempted; WARN iff >= 1; permits_attempted == 0 is its own (non-FAIL) case.`** Same-denominator rule: both counters counted per expected outcome row.
* **C8 — scope CORRECTED (Obs CRITICAL + Integration F2): the ORCHESTRATOR's `emit_meta` (`aic-orchestrator.py:606-616`) is the one production lineage sees** (run-chain.js takes the LAST `PIPELINE_META`; orchestrator re-prints workers then emits last). All THREE emit_meta sites updated (orchestrator + scraper `:2973-2977`/`:3210-3214`), each truthing up the pre-existing omission (`permits.enriched_status/last_scraped_at`, `scraper_queue`) and declaring `permit_scrape_outcomes`.
* **Lineage regen ordering (SF-F7):** dev run of the modified scraper → `lineage-docs --refresh` (snapshot) → render; otherwise the drift-guard pins stale lineage.
* **Spec 48 §3.7 mandatory artifact (Obs):** new Tier-3 ledger writer ⇒ first-deploy-spike runbook entry (expected spike shape, convergence query, exit criteria — `I1_first_deploy_spike.md` mirror) so observe-chain's baseline doesn't CRITICAL the new rows.

### Migration deploy ordering — via `apply-migrations.yml` (D3 RESOLVED)
After OUTPUT panel + gates: dispatch `apply-migrations.yml` from this branch (`dry_run=false`, operator approves the `production-db` environment gate), final `--verify` + INVALID-index gate green → merge to main in the same sitting. This manual sequence **minimizes** (not "zero by construction" — Gemini NIT accepted) the window where main is ahead of cloud; the operator verifies both steps. 236 is additive and unread by deployed code until merge, so early apply is forward-safe.

## Standards Compliance
* **Try-Catch:** writer best-effort behind its own guard on its own connection with bounded-backoff recovery; can never raise into or abort the scrape path. Threading step changes classification, never control flow of retries.
* **Unhappy Path Tests (failing-first):** outcome-write raises → stage writes still commit (v1 reproduction) · `waf_blocked`/`address_not_found` write rows and do NOT stamp · **`transport_error` survives to the chain-result layer and persists as itself (the new 5dc577f2 lock)** · `retry_exhausted` carries its outcome key end-to-end; WAF-trap `+THRESHOLD` forcing preserved (mutation-style check) · dead connection → scrape succeeds, counter increments, audit WARNs, **writer recovers on next insert** · zero-resolution batch outcome lands with `year_seq`, `outcome_resolution_failures` increments · resolution uses DISTINCT (duplicate-revision fixture) · CHECK rejects unknown outcome (DB) · vocabulary triple-agreement (contracts.json ↔ named CHECK ↔ frozenset) · detail sanitizer truncates + allowlists · prune: atomic, idempotent on re-run, LEAST/GREATEST correct (DB test) · both transports produce identical rows per terminal branch · RLS enabled (pgTAP guard passes).
* **logError:** N/A (Python) — structured log; every swallow counted (C10).
* **UI:** N/A.
* **§11:** UP+DOWN(comment-only) ✓ · backfill N/A stated ✓ · db:generate + typecheck ✓ · factories entry (TS db-test consumes) ✓ · emit_meta ×3 (Spec 47 §R11) ✓ · Cross-Layer Contracts ✓ · SQLFluff/validate-migration/hooks ✓ · `01_database_schema.md` **via `npm run db:docs` regen after dev apply** (SF-F3 — hand-edits inside markers get clobbered) ✓.

## Multi-Agent Review
* **PLAN panel — DONE (this fold):** Gemini + DeepSeek CLIs, Code Reviewer, Observability, Integration, Regression Guardian, Schema-Fidelity, Reality-Check. Adjudication log below.
* **OUTPUT panel:** the 5 + Regression Guardian (step 4a touches the 5dc577f2/K7a fence lines — RG's priority) **+ Schema-Fidelity (operator-ruled: both altitudes)** + Reality-Check on the dev-run rows (first live outcome data).

## Panel Adjudication Log (v3, 2026-07-31 — 8 reviewers)
* **FOLDED:** classification threading step 4a (CR-CRIT + Int-F1 + RG-Fence1 + DS) · orchestrator emit_meta in C8 (Obs-CRIT + Int-F2) · RLS both tables + Spec 114 catalog (SF-F1) · rollup PK gains transport + atomic idempotent prune SQL (Gemini-CRIT + DS) · 235-shape prune + Spec 115 §5 row (SF-F2) · D1 numbers corrected 2.4× (RC) + retention re-eval follow-up (Gemini) · run_id correlation column (DS) · year_seq zero-resolution fallback + counter (DS) · DISTINCT resolution (RC) · reconnect-with-backoff (DS) + teardown close (Gemini) · detail allowlist + 500-cap (Gemini + DS) · transport NOT NULL, (observed_at) index, named CHECKs, VARCHAR(30), FK-EXEMPT comment, comment-only DOWN (DS/SF) · FAIL-condition zero-guard + same-denominator (Gemini + DS + Obs, precedent `:575/:580`) · counter in both merge lists (Obs) · §3.7 runbook artifact (Obs) · db:docs regen (SF-F3) · lineage snapshot ordering (SF-F7) · contracts mechanism = literal + grep-pin (Int-F4) · address_not_found no-stamp explicit (Gemini) · WAF-trap separate-if line (RG) · citation fixes: `scrape_year_sequence`, orchestrator `:154` (CR/Int/RG) · "minimizes, not zero" wording (Gemini NIT) · revision-grain + duplicate-tolerance documented (RC + DS).
* **REFUTED/REJECTED with reason:** DS "BUILDO_TEST_DB typo" — real env var, repo-wide. DS "keep fetch_error first-class outcome" — resolved instead by threading: `fetch_error:*` is genuinely a transport_error detail once the kind survives; two-taxonomy risk dies with the collapse. Drop-transport_error-from-v2 alternative (RG's option b) — rejected, stated goal requires the distinction; threading is scoped and fenced instead. RC re-queue-magnitude measurement — correctly reported not-yet-measurable (278 stamps, hours old), not folded as a number.
* **RC live alert, closed:** runs 1953/1961 (`attempted=0, pending=10838, preflight_failures=1`) are the already-triaged stale-code cron failures on main — resolved by today's merge (cron disabled with the new code).

## Operator rulings at PLAN LOCKED
* **D1 — retention:** recommend (a) 90-day raw + rollup, on the CORRECTED numbers (~150K raw steady-state, ~93K rollup bound); knobs = length + prune vehicle (recommend pg_cron per 235 shape). Re-eval follow-up filed when item 5 lands.
* **D2 — writer isolation:** recommend dedicated connection (+reconnect/backoff, teardown close); SAVEPOINT fallback.
* **D3 — RESOLVED:** `apply-migrations.yml` (shipped this session) is the apply vehicle.
* **Scope acknowledgment:** step 4a (classification threading) adds ~8 call sites + 2 function contracts beyond v2's estimate — the panel's unanimous price of honest data.

## Execution Plan
- [x] 1. PLAN panel (8 reviewers) — DONE, folded above.
- [x] 2. **PLAN LOCKED — halt for authorization (D1, D2, scope ack).**
- [x] 3. **Failing tests first** (list above): `scripts/tests/test_scrape_outcome_persistence.py` + chain-result-layer classification lock + DB integration tests (`BUILDO_TEST_DB=1`; testcontainer applies 236). Confirm red.
- [x] 4a. **Classification threading:** `fetch_permit_chain_http` (4 sites) + `fetch_permit_chain` (4 sites) propagate `failure_kind`+`detail` through `chain_result`; `scrape_year_sequence` raises typed; `scrape_with_retry` preserves kind; K7a lines untouched. Threading locks green.
- [x] 4b. **Migration 236** per design (RLS, named CHECKs, year_seq, run_id, indexes, rollup, comment-only DOWN). SQLFluff + validate + hooks. Apply to dev only.
- [x] 4c. **Writer + call sites** (dedicated conn w/ recovery, sanitizer, DISTINCT resolution + year_seq fallback, no-stamp rules, `:2651-2654` stays separate top-level if). Tests green.
- [x] 5. **Orchestrator:** audit rows (guarded FAIL condition), counter in BOTH merge lists, `SCRAPER_RUN_ID` export, orchestrator `emit_meta` truth-up; scraper emit_meta ×2; dev run → `lineage-docs --refresh` → render.
- [x] 6. **Retention prune** per D1 (235-shape function + guarded schedule + Spec 115 §5 row) + atomicity/idempotency DB tests.
- [x] 7. **Docs — explicit list, every file named:**
      · `docs/specs/01-pipeline/44_chain_deep_scrapes.md` — §3 gains the persistence contract (taxonomy table incl. threading semantics, no-stamp rules, transport-identical guarantee, year_seq fallback); §5 gains `test_scrape_outcome_persistence.py` in the lock roster.
      · `docs/specs/00-architecture/01_database_schema.md` — regenerated via `npm run db:docs` AFTER the dev apply (SF-F3: the marker-fenced listing is generated; hand-edits get clobbered); prose outside the markers (table-count narrative) hand-updated.
      · `docs/specs/00-architecture/114_rls_policy_catalog.md` — **no catalog entry required** (fold-verifier F4-1: Class B is definitional per §2/§4 — Spec 114 explicitly rejects an enumerated Class B catalog as a staleness hazard; the §11 pgTAP introspection guard covers the new tables automatically). Optionally add both to the §4 illustrative list.
      · `docs/specs/00-architecture/115_scheduling.md` §5 — prune-job catalog row.
      · `docs/specs/_contracts.json` — outcome-vocabulary entry (+ `src/tests/contracts.infra.test.ts` consumer rows).
      · `docs/reference/data-lineage-map.md` — regenerated (dev run → `lineage-docs --refresh` → render).
      · `docs/runbook/` — Spec 48 §3.7 first-deploy-spike artifact + **`docs/runbook/README.md` index row** for it (F4-2).
      · `docs/specs/01-pipeline/44_chain_deep_scrapes.md` §Operating Boundaries — add `migrations/236_*.sql` + the new test file to Target Files (drift fence).
      · `docs/specs/01-pipeline/47_pipeline_script_protocol.md` §7.8 — add `permit_scrape_outcomes` to the Tier-3 example roster (illustrative; one line).
      · `docs/reports/review_followups.md` — retention re-eval entry (blocked on item 5).
- [x] 8. Full gate: `npm run test:py`, `npm run test`, `BUILDO_TEST_DB=1`, db:generate + typecheck, lint. §11 self-checklist on the diff.
- [x] 9. **OUTPUT panel — COMPLETE (2026-07-31 EOD, 8 reviewers).** Verdict: implementation matches the plan fold-for-fold. Schema-Fidelity PASS (2 accepted LOWs) · Observability PASS zero findings ("unusually tight") · Integration 8/8 PASS incl. live testcontainer replay of 237 · Regression Guardian all 5 fences PRESERVED (+ mechanical confirmation: scripts/tests/ diff is additions-only) · Code Reviewer fold-for-fold PASS + ONE finding FOLDED (`d675a5a1`: HTTP transport_error carried no detail — now `transport_error:<ExceptionClass>`, red-first + parity locks) · Reality-Check: first 3 ledger rows SANE (vocab/RLS/CHECKs/run_id/stamping all live-verified), pg_cron prune registered; RC also CORRECTED the wrong-DB incident's root cause (hardcoded legacy PG_* fallbacks, NOT .env — filed as WF3 candidate) and named the breakdown-vs-ledger undercount blind spot (filed). Gemini/DeepSeek whole-file passes harvested pre-existing defects → all filed in review_followups (2026-07-31 entries).
- [ ] 10. **⏰ MORNING (2026-08-01): Cloud apply via `apply-migrations.yml`** — dispatch from THIS branch (`wf2/deep-scrapes-restore-l0`), `dry_run=false`, operator approves the production-db gate; expect 236+237 in the dry-run listing, final verify + INVALID-index gate green → merge to main, same sitting → watch next scheduled coa-permits pre-flight (`--verify` must stay green with 234 files) + first cloud outcome rows on the next deep-scrapes dispatch. Also morning: export the C7-firing evidence row (pipeline_runs id 1603/1604) from the legacy 5432 DB before any cleanup.

> **PLAN LOCKED. Do you authorize this WF2 plan? (y/n)**
> §11 note: step 4a (classification threading) is the panel-forced scope addition — without it the CHECK vocabulary would constrain values the code can never emit, and misclassifications would become durable DB facts. D1/D2 rulings requested above.
