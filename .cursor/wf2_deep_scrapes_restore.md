# Active Task: WF2 — deep-scrapes scraper: restore the proven artifact as the default path, port ONLY the cloud minimum
**Status:** **PLAN LOCKED — authorized by the operator 2026-07-30** ("take your recommendations — tag the backup, try P3 — beyond that move forward"). Panel round 1 complete (7 lenses: Gemini, DeepSeek ×2, Integration, Ground-truth, Observability, Yield-and-Economics); all findings adjudicated below.

## Operator rulings (2026-07-30)
* **P1 RULED — annotated git tag, no on-disk copy.** `deep-scrapes-cloud-attempt-2026-07-30` **created**, pointing at `539468b5`. Recover with `git show deep-scrapes-cloud-attempt-2026-07-30:scripts/aic-scraper-nodriver.py`. Zero rot, zero search pollution, permanent. (Also moots Integration's finding that the on-disk copy's stated mitigations were no-ops.)
* **P2 RULED (Gemini, adopted):** attach mode kept as the gated cloud path; no monkeypatch.
* **P3 RULED — cloud uses the EXTENSION path; the relay becomes a local-only opt-in.** Premise corrected by G14: the runner ships unbranded Chromium 150, exempt from the `--load-extension` removal, and CI logs show both extension service workers loading. The relay's only real user is the operator's **local branded Chrome 150**, where it is the sole way to proxy at all. Deletes the relay, forced-headless, and the egress-tripwire cascade from the cloud path. **Binding constraint carried over from Yield CRITICAL-2: whichever mode is selected must rotate the exit IP WITHOUT recycling the browser** — the extension path must gain that capability (it rotates by reloading extension credentials, not by killing Chrome), or the split is lost.
* **P4 RULED (Ground-truth, adopted):** restore `20/2000/3` as module defaults, conditional on K6 env-wrapping and the workflow pinning cloud values.

## Blocking verification CLOSED (Observability F4) — the crash-to-verdict path survives
Diffed the three functions G1 did not cover, `de3ff6dd` vs HEAD: **`preflight_stealth_check` is byte-identical (0-line diff)**. `bootstrap_with_retry` differs only by relay plumbing, `stop_and_terminate` (attach-mode teardown), and the K7c launch-log dump — the `PREFLIGHT_FAIL` → `raise` → `preflight_passed=False` chain is structurally unchanged, and `tel['preflight_passed'] = False` is present in both (`orig:1200`, `HEAD:2071`). On restore, `stop_and_terminate` correctly reverts to `browser.stop()` (right once we no longer attach) and the diagnostic is a K7 keeper. **The orchestrator's honest-verdict guard therefore keeps its input.**
**Domain Mode:** Backend/Pipeline
**Note:** dedicated file; `.cursor/active_task.md` holds the Supabase program plan. Supersedes `.cursor/wf3_aic_cloud_capture.md` (the geo/transport WF3 drafted 2026-07-30) — that file's F1 geo finding is folded here as C6; its F2 raw-HTTP transport is explicitly OUT of scope (see §Non-Goals).

## Context
* **Goal:** Undo the 2026-07-29 scope explosion. Restore `de3ff6dd`'s scraper as the **default runtime path**, keep the current cloud version as an on-disk backup, and re-apply only the changes that a GitHub Actions runner genuinely forces — each one **environment-gated, default OFF**, so the artifact the operator proved locally is what runs locally.
* **Target Spec:** `docs/specs/01-pipeline/44_chain_deep_scrapes.md` (script behaviour) + `docs/specs/00-architecture/115_scheduling.md` §2.4 (runner/workflow contract). ⚠ **Both were rewritten on 2026-07-30 by the errant session to codify the approach this task reverses** — see G7. The **pre-drift** text (`3c780e88` for Spec 44, `6e639db7` for Spec 115) is the authoritative baseline; the 2026-07-30 additions are *evidence to be adjudicated*, not binding requirements.
* **Prior art (read, do not re-derive):** `docs/reports/2026-07-30-deep-scrapes-original-vs-head-comparison.md` — the recovered full `de3ff6dd`→HEAD classification (45 differences, four buckets, per-commit, with line numbers). This plan is that report's targeted-revert recommendation, formalised.
* **⚠ PROVENANCE CORRECTION (Ground-truth F3, live DB, cloud + local identical).** The nodriver scraper's success is **operator-attested, not data-verifiable.** `permit_inspections` holds **792 rows, all from 2026-03-14/15**, and nothing in the last 90 days; the nodriver scraper did not exist until `bee15998` (2026-03-29), so **every inspection row in the database was produced by the deleted Playwright scraper** (`poc-aic-scraper-v2.js`). Across all of `pipeline_runs`, `chain_deep_scrapes` shows **`records_new = 0` on every row ever recorded**; `scraper_queue` = 10,862 pending / 111 completed / 8 failed, with the errant session's runs marking items complete having inserted nothing. **No GH-runner scrape has ever produced a row, and neither has any nodriver run.** What *is* verifiable: `git log --follow` shows the scraper substantively **frozen 2026-04-01 → 2026-07-29**, so whatever the operator ran in the attested April–June window IS substantively the `de3ff6dd` artifact — G11's reconciliation holds. **Required wording discipline from here on: "operator-attested," never "proven working."** Benefit (a) below is "provably runnable," which is fair; "proven working" would be a provenance overclaim.
* **Boundary note (Ground-truth F4):** `de3ff6dd` **does not touch the scraper at all** — it changes only the workflow (AppArmor sysctl) and the orchestrator (verdict rows). The scraper content at `de3ff6dd` is identical to its parent `86868387`. The file-level restore recipe is therefore correct, and implicitly retains `de3ff6dd`'s own sysctl and verdict fences — but the boundary commit is itself cloud-adaptation work, not a pre-cloud tag.
* **Baseline honesty:** the attested local run was **unproxied, headed, from the operator's own Toronto residential IP** — it worked partly *because* the MV3 proxy extension was silently dropped by branded Chrome ≥137. Cloud has neither property. **This task is code hygiene, not a cloud fix** (see §What this task does NOT claim).

## Ground truth (measured; sourced, not inferred)
**G1 — The data path was never touched.** Mechanically verified function-by-function at `de3ff6dd` vs HEAD: `fetch_permit_chain` (the four `/jaxrs/` calls, headers, 13-field POST body, 15 000 ms abort), `scrape_year_sequence`, `scrape_with_retry`, `safe_json_parse`, `normalize_status`/`compute_enriched_status`/`parse_inspection_date`, all upserts and the queue claim/complete SQL — **IDENTICAL**. The only insertion is two `_log_step1_body()` calls that no branch reads. **The request chain must not be rewritten** (`review_followups.md:2802`).

**G2 — Scale of the drift.** `scripts/aic-scraper-nodriver.py` **1 267 → 2 140 lines (+927/−54)** across 28 commits. `scripts/aic-orchestrator.py` **UNCHANGED**. `.github/workflows/chain-deep-scrapes.yml` **UNCHANGED by the drift** — *note: this task nonetheless modifies it, for C2's cache deletion and the C-gate env block (DeepSeek NIT).* New: `scripts/proxy-relay.mjs` (137), `scripts/tests/` (5 files + conftest), `proxy-chain` in `package.json`.

**G3 — The genuine cloud minimum is ~20 lines of Python + ~5 workflow lines.** Roughly **620 of the 927 added lines fall outside it.** Two contestable architectural choices generated ~400 of those 620: the relay (137 `.mjs` + ~90 Python, chosen over ~4 workflow lines of Chrome-for-Testing) and attach mode (~200 lines, chosen over a ~5-line monkeypatch of nodriver's retry loop), the latter cascading into ~200 more lines of process-ownership code.

**G4 — Unconditional local-behaviour regressions (fire regardless of `PROXY_HOST`):** `WAF_TRAP_THRESHOLD` 20→3 (**and `accumulate()` counts legitimate not-founds toward `consecutive_empty`, so 3 genuine misses now force a browser+IP rotation**), `RETRY_BASE_MS` 2 000→90 000, `MAX_RETRIES` 3→2, `MAX_BROWSER_LAUNCHES=12` hard ceiling, 15 `BANDWIDTH_GUARD` launch switches, attach mode's two new fatal `raise`s, `--enable-logging`.

**G5 — `PROXY_HOST` is set in the operator's local `.env`**, so every `if PROXY_HOST:` branch is live on the operator's machine. The single largest behavioural change is therefore **local: unproxied+headed → proxied+headless**, through metered Decodo.

**G6 — Two live defects in the current file** (independent of the revert, both disappear with it): **B11** — `b1bc91e9`'s "relay start moved inside the try" is unreachable dead code (lines 1898/1903 already cover every `PROXY_HOST` case), so a relay failure still escapes `main()` uncaught, with no `PIPELINE_SUMMARY` and `preflight_passed` left `True`. **B10** — the `DISPLAY` guard is dead code, sitting inside `if proxy_ext_dir:` where `build_proxy_extension()` is never called.

**G7 — The specs were rewritten to match the errant approach — by ONE commit, `52691fae` (2026-07-29 22:34).** *(Narrowed per Ground-truth F5: `7c8db089` is NOT §2.4 drift — it touches the architecture env block and the chain→workflow map with true, unrelated WSIB/inert-path content, and it predates `de3ff6dd`. **Step 8 must not revert or re-litigate it.**)* `52691fae` added to Spec 44 (+29/−5) and Spec 115 §2.4 the attach-mode launch ownership, the relay contract, the Decodo credential contract, the egress tripwire, and **replaced** the pre-drift headed-Chrome/`xvfb-run` deployment note with a headless amendment. **Two of its own statements are already false against HEAD code:** item 10(b)'s allowlist (F-REL), and Spec 44 §3's "Chrome killed after each batch" — falsified 48 minutes after the spec commit by `15c7417b`. Conversely its cron-schedule line is TRUE and must survive Step 8, and pre-drift Spec 115's *"the residential proxy carries ALL AIC traffic"* and *"the proxy forces headed Chrome"* are **both false and must not come back**. Pre-drift Spec 115 §2.4 states plainly: *"Because the proxy forces headed Chrome (P3-G4), the workflow runs the orchestrator under `xvfb-run`"* and mandates `actions/cache` stealth-profile restore — the very cache that `8c3dd193` proved bricks Chrome (`tasks/lessons.md:79`). **Neither the pre-drift nor the post-drift spec text is trustworthy as-is**; both are truthed-up in Phase 4 against what this task actually ships.

**G8 — Portal recon (2026-07-30, measured — survives the revert because it is about the PORTAL, not our code).** No cookies/session/CSRF: step 1 returns 200 from a cold client. The blocker is **Akamai cumulative client reputation**: ~12 requests / ~10 min, then every request 403s with a ~430 B HTML page, clearing after ~5 min idle, **sticky**. Measured with curl's TLS fingerprint = a **LOWER bound**. ~5.3 KB/permit of JSON; `setup.do` alone is 75 885 B (14× the data chain). **`build_proxy_username` emits no `country-ca`** — exit IPs drawn included **186.225.225.102 (Brazil)** while scraping a Toronto municipal portal.

**G9 — Test-surface coupling.** `scripts/tests/test_scraper_bootstrap.py`, `test_scraper_config.py`, `test_scraper_egress.py`, `test_scraper_launch_attach.py` import `build_browser_args`, `wait_for_devtools`, `verify_proxied_egress`, `resolve_proxy_port`, `build_proxy_username` — **functions that exist only at HEAD**. Spec 44 §5 mandates pytest locks (*"Any new scraper logic must land with locks here rather than be validated by dispatch"*), so the tests must be **rewritten in the same commit**, never deleted wholesale.

**G11 — The apparent contradiction with yesterday's recorded operator ruling is resolvable, and the panel must confirm it.** `review_followups.md:2811-2817` records an operator correction: *"The delivered, working scraper **is** the current nodriver one… Therefore this task is **BUILD-AND-PROVE, not RESTORE-AND-DIFF**."* Today's directive is restore-from-the-version-that-worked. **These agree**: the version that worked *is* the current nodriver design **at `de3ff6dd`, before the 2026-07-29 drift** — restoring it is not reverting to a pre-nodriver architecture. What remains true from the correction is the §"does NOT claim" statement: **cloud execution is a NEW capability that has never once succeeded**, so no part of this task may be described as fixing a regression. *If the panel finds these genuinely irreconcilable, it must say so — that is an operator decision, not mine.*

**G12 — Recovered Spec 38 §3.8 contradicts the headed-Chrome premise both specs rest on.** It states nodriver launched Chrome **headless** (`--headless=new`) and *"**Proxy: Optional. Direct connection works** (nodriver bypasses WAF without proxy)."* Pre-drift Spec 115 §2.4's *"the proxy forces headed Chrome"* is therefore a consequence of the **extension**, not of proxying. This is direct evidence for P3 and against treating headed mode as inherent.

**G13 — ~~The relay's allowlist is a live CRITICAL latent defect~~ → RETRACTED; the defect is in the DOCUMENTATION, not the code.** *(Corrected in panel round 1 — see F-REL in the adjudication log. My original claim, inherited from `review_followups.md:2804` D1 and Spec 115 §2.4 item 10, was that the relay is a deny-by-default allowlist admitting only `toronto.ca` + `api.ipify.org`, which would refuse the WAF/CDN challenge host and reproduce the `d138bb04` shadow-ban.)* **Source-verified false:** `scripts/proxy-relay.mjs:46-55` states *"BLOCK-list, deliberately NOT an allowlist. Deny-by-default was reviewed as a … an allowlist of `*.toronto.ca` cannot be known to be complete."* It blocks 16 named Google/update hosts and passes everything else. **Standing conclusion:** D1's CRITICAL/LATENT rating is void; Spec 115 §2.4 item 10 and `review_followups.md` D1 both describe a mechanism the code does not implement, and Step 8 corrects both. C7 still defaults OFF — as a cost guard, not a shadow-ban risk.

**G14 — ⚠ THE BIGGEST CORRECTION OF THE PANEL: the CI runner ships UNBRANDED Chromium, which loads the MV3 extension fine. The relay was never a cloud requirement.** Verified: `review_followups.md:2798` records it as **evidenced, not assumed** — run `30493773408`'s `chrome_diagnostics` resolved `/bin/chromium` → Chromium **150.0.7871.0, unbranded, exempt from the 137/142 `--load-extension` removals**, and run `30498062060`'s `browser_targets` listed **BOTH extension service workers loaded in CI**. Consequences: (a) the comparison report's cloud-minimum item #4 ("install Chrome for Testing because the runner ships branded Chrome") is **factually wrong — no browser install is needed**; (b) the relay's *only* real justification is the **operator's local branded Chrome 150**, and locally the operator scraped unproxied anyway; (c) the whole cascade the relay forced — headless, no extension, the 190-line egress tripwire — was built to solve a problem the cloud does not have. **This rules P3** (see below) and shrinks the cloud-required set again.

**G15 — ⚠ THE CRON IS LIVE, so "default OFF" is not a safe no-op.** `.github/workflows/chain-deep-scrapes.yml:38-39` has `schedule: - cron: '0 15,18,21 * * 1-5'` **uncommented and active** (the file's own header comment at line 8 saying "committed COMMENTED OUT" is stale). The workflow sets `PROXY_HOST/PORT/USER/PASS` from secrets (185-192) but **none** of the C-gates. So on the first scheduled fire after merge, the cloud would run the *default* path — nodriver-launched Chrome against a cold cacheless profile, straight into the 2.25 s handshake failure — or, surviving that, proxy-configured with the tripwire OFF and no way to detect unproxied scraping from a datacenter IP. **A restore that only touches Python ships a broken scheduled job.**

**G10 — `de3ff6dd` retains the empty-string env-var crash fix** (`86868387` is its immediate predecessor; `git blame` confirms the `int(os.environ.get('X') or 'N')` guards at its lines 84, 85, 130, 229). Reverting to it loses nothing on that front.

## What this task does NOT claim (jury statement — read before authorizing)
Restoring the proven artifact **will not by itself make cloud scraping work.** The local run succeeded because it was unproxied, headed, and exited from a Toronto residential IP. A GitHub-hosted runner has a datacenter IP and must proxy; G8 says a proxied/datacenter path hits an Akamai ceiling of ~12 requests per ~10 minutes *regardless of how clean the code is*. This WF2 buys three things and only three: (a) the operator's local capability is restored and provably runnable, (b) the artifact becomes a legible baseline that a cloud experiment can be measured *against*, (c) ~620 lines of unmeasured drift and two live defects leave the tree. **The cloud throughput question is deliberately deferred to a follow-on WF** whose first experiment is C6 geo-targeting, and whose open question is transport (browser vs raw HTTP) — see §Non-Goals.

## Non-Goals (explicit — scope fence)
* **No raw-HTTP transport.** `wf3_aic_cloud_capture.md`'s F2 is a separate WF, after this one lands. Mixing a transport rewrite into a restore is how yesterday's 927 lines happened.
* **No request-chain change**, including the tempting `setup.do` removal and step-4 skip (G8) — both are throughput work for the follow-on WF.
* **No cadence/schedule change.** Spec 115 §2 owns it.
* **~~No workflow-file rewrite beyond C2/C4/C7~~ — AMENDED (Integration F1 + Observability F1, converging CRITICALs).** The original fence was unsafe: G15 shows the cron is **live**, so shipping Python-only leaves the scheduled job running the *default* path on a runner that needs the cloud path — a designed-in, thrice-weekly CI failure. **This task now owns the workflow's C-gate `env:` block** (`SCRAPER_ATTACH_MODE`, `SCRAPER_PROXY_MODE`, `SCRAPER_VERIFY_EGRESS`, `SCRAPER_PROXY_COUNTRY`, `SCRAPER_DEVTOOLS_TIMEOUT_S`) in addition to C2's cache deletion — otherwise "default OFF" is not a no-op, it is a regression. Everything else in Spec 115 §2.4's workflow contract (cadence, runner, secrets, concurrency, verdict check) stays untouched.
* **No re-dispatch to validate.** Spec 44 §5 is explicit: land logic with pytest locks, do not validate by dispatch. Dispatch belongs to the follow-on WF that has a hypothesis to test.

## Technical Implementation

### Strategy: restore-as-default, cloud-as-opt-in
`scripts/aic-scraper-nodriver.py` is rebuilt from `git show de3ff6dd:scripts/aic-scraper-nodriver.py` as the base, then the **Keep set (K1–K4)** and the **cloud set (C1–C7)** are applied on top. Every C-item is env-gated with the **pre-drift behaviour as the default**; the workflow sets the flags. Result: `python3 scripts/aic-scraper-nodriver.py` with no cloud env vars behaves as it did at `de3ff6dd`.

* **Backup (operator's explicit requirement):** the current 2 140-line file is preserved verbatim at **`scripts/reference/aic-scraper-nodriver.cloud-2026-07-30.py`**, with a header comment stating it is a non-executing reference of the 2026-07-29/30 cloud attempt, its HEAD commit hash, and a pointer to the comparison report. It is added to `knip.json` ignore + excluded from pytest collection so it can never be imported or run. *(Panel question P1: is an on-disk copy right, or does `git show` + the comparison report suffice? Operator asked for a backup; I am honouring that literally and flagging the duplication cost.)*

### K — Keep from the drift (re-apply by hand onto the restored base, ~40 lines)
* **K1 — Decodo credential fix** (`3583d824`, `ef9bbab2`): `user-` prefix (Decodo only parses the username when it begins with the literal token `user-`; verified live: bare account → 200, `account-session-x` → **407**), **alphanumeric-only session IDs — operator-flagged 2026-07-30 as a real cost of yesterday.** The mechanism: Decodo reads the username as a **hyphen-delimited key–value list**, so a hyphen *inside a value* ends that value early — our `buildo-worker-1-<ts>` made the parser read `session=buildo` and then choke on `worker` as an unknown key. **Our own product name contains the delimiter**, which is why this bit us and would bite anyone reusing a `buildo-*` identifier in a proxy credential. Session IDs are now `w1t<ts>`-shaped. Captured in `tasks/lessons.md:84` and Spec 44 §3 with this exact example; the digest's corrections register carries it too. `PROXY_SCHEME` default `https` (plain-http CONNECT tunnels are RESET; Chrome cannot authenticate to SOCKS at all). Without these the proxy 407s **anywhere**, and a 407 renders as "site can't be reached" — invisible. `tasks/lessons.md:84`, `:85`.
* **K2 — `clear_stale_profile_locks()`** (`8c3dd193`, ~28 lines): strips host-scoped `Singleton*` locks. Kept **and** paired with C2 (dropping the cache that creates them) — belt and braces, because `tasks/lessons.md:79` notes the poisoned entry is restored *forever* once honest verdicts stop saving new caches. Guarded so it cannot remove a lock held by a **live** Chrome (the one local downside the comparison report names).
* **K3 — step-1 body sampling** (`4124a0f7`, ~25 lines, capped at 8, pure logging): the only instrument that distinguishes *"AIC has no such permit"* (`[]`) from *"the session is shadow-banned"* (a ~430 B HTML page). That ambiguity cost the diagnostic day. `review_followups.md:2809`.
* **K4 — per-worker sticky ports** (`c3dff232`, `resolve_proxy_port()` = `base + N − 1`, ~25 lines): a sticky-proxy PORT pins the exit IP and **overrides** any per-session username suffix, so all workers previously shared one IP. Load-bearing whenever `SCRAPER_WORKERS > 1`, which the operator's `.env` sets. `tasks/lessons.md:86`.
* **K5 — `nodriver==0.48.1` exact pin** (`requirements.txt`): `>=0.48` meant CI silently resolved 0.50.3 — **the cloud never once ran the code that was tested.** The single cheapest and highest-value line in the whole batch. `tasks/lessons.md:88`.

### K7 — **DIAGNOSTICS: baked in, not gated** (operator-directed 2026-07-30)
> *"Rather than see a fail — we knew why it failed and at which point. We did this through trial and error that took a long time; let's bake it in."*

The comparison report classified these as bucket-4 "pure diagnostic" and my draft filed them behind `SCRAPER_CHROME_DIAGNOSTICS=1`, default OFF. **Overruled: they are first-class keepers, ON by default.** They are what converted an opaque red run into a located, explained failure, and they were paid for in ~13 CI cycles and a day. Gating them off means re-buying that knowledge the next time something breaks.

* **K7a — WAF/response visibility (already K3, restated as the anchor):** `_log_step1_body()`, 8-sample cap. The *only* instrument that separates `[]` (genuine not-found) from a ~420–450 B Akamai "Access Denied" page. Both currently feed the same `consecutive_empty` counter — the conflation that makes a WAF threshold sabotage a healthy queue. **Keep, and additionally log the classification** (`not_found` vs `waf_blocked`) with the byte length, so the distinction is visible without re-reading bodies.
* **K7b — `log_chrome_diagnostics()`:** resolves the executable, `realpath`, `chrome --version`. This is what proved the runner ships **unbranded Chromium 150** — the single fact that voided the relay's cloud justification (G14). One subprocess per process.
* **K7c — `dump_chrome_launch_log()`:** tails Chrome's own log on bootstrap failure. Turns "Failed to connect to browser" into a cause.
* **K7d — descriptive readiness errors:** the timeout `RuntimeError` names port, elapsed time and process return code — the deliberate opposite of nodriver's cause-agnostic message. Keep even on the non-attach path.
* **K7e — structured lifecycle events:** `devtools_ready`, `devtools_port_drift`, `stale_profile_lock_removed`, `proxy_relay_ready`/`terminated`, `log_browser_targets()`. Each marks *which stage* failed. `log_browser_targets` must **never gate** (its docstring records a false negative it once caused in run 30496893882).
* **K7f — `check-chain-running.js` guard observability** (`27e39948`, +11): makes the skip/run decision visible instead of silent.
* **Caveat, carried from the prior WF3's G10:** `--enable-logging` on a **piped, undrained** stdio path can fill a 64 KB buffer and stall Chrome's startup. Keep the diagnostic, but only with stdio to `DEVNULL` or a drained pipe — never re-create the amplifier.
* **Telemetry, not just stdout:** per the Observability findings, these must reach `records_meta`/`scraper_telemetry` where a verdict reader can see them — a diagnostic that lives only in a GitHub Actions log nobody reads is not observability.

### C — Cloud set (env-gated, default OFF = pre-drift behaviour)
| # | Change | Gate | Default | Runner property that forces it |
|---|---|---|---|---|
| C1 | Attach-mode launch (`launch_chrome` + `wait_for_devtools` + process-group teardown) **or** the ~5-line monkeypatch of nodriver's 2.25 s retry budget — **P2 below** | `SCRAPER_ATTACH_MODE=1` | off → `uc.start(headless=…, browser_args=…, user_data_dir=…)` | A cold profile on a CI runner is still creating its DBs when nodriver's hardcoded ~2.25 s budget expires (`tasks/lessons.md:87`) |
| C2 | **Delete the `actions/cache` step** for `~/.buildo-scraper` | workflow-only | n/a | A cache-restored `SingletonLock` names a dead host/PID and bricks Chrome (`tasks/lessons.md:79`) |
| C3 | Proxy mechanism — relay vs Chrome-for-Testing + extension — **P3 below** | `SCRAPER_PROXY_MODE=relay\|extension\|none` | `none` when `PROXY_HOST` empty; otherwise the P3 ruling | Runner ships branded Chrome ≥137, which drops `--load-extension` |
| C4 | Chrome availability on the runner (whichever binary P3 selects) | workflow-only | n/a | Runner's default browser is not the operator's |
| C5 | Proxied-egress tripwire + TTL | `SCRAPER_VERIFY_EGRESS=1` | off | Only meaningful when proxying; *"is the mechanism configured?" ≠ "is it working?"* (`tasks/lessons.md:90`) |
| ~~C6~~ | ⚠ **REWRITTEN — my C6 was built on a false premise (Yield HIGH-4).** `aedd4cd1` (2026-03-15) records a *measured* ruling: Decodo geo-targeting is selected by **country-specific HOSTNAME + port band, not by a username parameter**, and that commit explicitly *"removed incorrect `;country=CA` username append logic"*. `ca.decodo.com:20001+` is the Canadian lane; **`gate.decodo.com:10001` is geo-fenced by the AIC portal**. So the correct mechanism is already in place via `PROXY_HOST`, and I had made an unverified username format into a **mandatory fatal startup gate** — the eager-fix antibody failing on my own plan. **Replacement C6: (a) refuse to run against `gate.decodo.com` (measured, and the actually-missing guard); (b) assert the resolved proxy host is the Canadian lane; (c) if a username country key is wanted, verify it live with curl FIRST — as the `user-` prefix fact was.** *(Note: this also refutes yesterday's `wf3_aic_cloud_capture.md` F1, which proposed exactly the username append.)* | see (a)/(b) | on-when-proxying | Original (superseded) text follows |
| ~~C6-orig~~ | ~~**Geo-target Ontario** — add `country-ca` (± `city-toronto`) to `build_proxy_username`, and~~ **assert the returned exit IP geolocates to Canada before scraping; fail loud, never silently scrape from Brazil** | **NOT opt-in — mandatory whenever proxying** (`SCRAPER_PROXY_COUNTRY`, default `ca`); a proxied run with no country target **fails at startup** | on-when-proxying | We are demonstrably not geo-targeting; a Toronto municipal portal hit from Brazil is a first-order bot signal (G8). **NEW cloud-required item the comparison report's minimum set omits**, because at `de3ff6dd` the proxy was silently broken so geo never mattered. *(v2 — Gemini CRITICAL accepted: an unset default would codify the known-bad behaviour as the default for every proxied run. The safe state is the default; only the unproxied path is exempt.)* |
| C7 | Bandwidth guard (15 launch switches) + relay host blocklist | `SCRAPER_BANDWIDTH_GUARD=1` / `SCRAPER_PROXY_BLOCKLIST` (empty = no blocking) | off | Caps the $6.60/1.76 GB Chrome-talks-to-Google exposure (`tasks/lessons.md:91`) — real, but a cost guard, not a correctness requirement |

### R — Restore pre-drift defaults, **but keep the drift's env-wrapping** (Ground-truth F2 — REFUTED my original wording)
⚠ At `de3ff6dd` these constants are **bare literals with no env reads** (`de3ff6dd:61-63`); `MAX_BROWSER_LAUNCHES` does not exist at all. The `SCRAPER_*` override knobs were introduced by the very commits being reverted (`ae5f7c26`, `68a2f55e`). A verbatim restore would therefore **delete the override mechanism that §R's own ruling and the P4 ruling both depend on.** **K6 (new keeper): retain the env-wrapping, with pre-drift defaults** — `int(os.environ.get('SCRAPER_X') or '<pre-drift>')`. Without this the follow-on cloud WF cannot tune retry/WAF behaviour from workflow env and would need another code change.
`MAX_RETRIES` 2→**3**, `RETRY_BASE_MS` 90 000→**2 000**, `WAF_TRAP_THRESHOLD` 3→**20**, `MAX_BROWSER_LAUNCHES` 12→**unlimited (as a module default only — the workflow PINS 12)**.

⚠ **REVERSED — per-batch browser recycle is NOT restored (Yield CRITICAL-2, overturning my own ruling).** `15c7417b` deliberately **separated two axes the phrase "1 batch = 1 IP" fuses**: it *kept* the IP rotation and *removed only the browser churn*, rotating the Decodo session on a pinned local relay port so the exit IP changes **without a cold Chrome**. A wholesale revert re-fuses them, because `de3ff6dd` has no relay and therefore cannot rotate the IP any other way. Decisive provenance: per-batch recycle arrived in `368bd878` (**2026-03-29**) — **14 days after the last real `permit_inspections` row** — so it has **never coexisted with a successful scrape**, and the productive v2 scraper used one session for **200 permits**. Measured cost: at batch size 10 it adds ≈29 KB/permit of bootstrap overhead (**5.5× the entire 5.3 KB data chain**); amortised over 200 permits the same overhead is 1.4 KB/permit — a **~20× multiplier**. **Ruling: preserve the rotate-IP-without-recycling-the-browser split (H13) as an explicit requirement of whichever proxy mode P3 selects.** ⚠ **Recorded tension, for the panel:** G8's recon argues 2 000 ms retries fire *inside* Akamai's ~5-minute recovery window and that 20 consecutive-empties burns 20 requests against an already-blocked edge — i.e. the pre-drift values may be wrong *for a blocked cloud edge*. They are nonetheless right for the proven local path, and were retuned globally from a measurement taken through a proxy, from a runner, against an already-blocked edge — a context that is not the operator's. **Ruling: restore the defaults, and let the follow-on WF set cloud values in the workflow env once it has measured the ceiling it is tuning for.**

* **Deletions:** `proxy-relay.mjs` and `proxy-chain` survive only if P3 rules for the relay; otherwise both are removed and the dependency dropped. Dead code (G6 B10/B11) does not come back.
* **Database Impact:** **NO.**

## Open decisions the panel must rule on (I hold a position on each; try to break it)
* **P1 — Backup form.** On-disk `scripts/reference/…py` (operator asked for it, costs a duplicated 2 140-line file that will rot) vs `git show` + the comparison report (zero rot, needs a command). *My position: honour the operator's literal request, but make it inert and dated.*
* **P2 — Attach mode vs monkeypatch.** Attach mode is ~200 lines + ~200 lines of forced process ownership; the monkeypatch is ~5 lines but reaches into a third-party private retry loop that the exact pin (K5) makes safe. *My position: gate attach mode OFF and ship the monkeypatch as the gated cloud path — but attach mode is already written, tested and locally proven (G12 of the prior WF3), so discarding it may be waste. Panel decides.*
* **P3 — RE-FRAMED by G14; the original question was built on a false premise.** Both my position and the comparison report's assumed *"the runner ships branded Chrome, which drops `--load-extension`"* — **evidenced false**: the runner ships unbranded Chromium 150, exempt from the removal, and CI logs show both extension service workers loading. So **the relay was never cloud-required, and no browser install (C4) is needed either.** The live question is now narrower: the extension path works in CI but carries the MV3 idle-service-worker eviction risk (`tasks/lessons.md:89`); the relay avoids eviction but cost ~230 lines and cascaded into headless + the tripwire. *My revised position: **cloud uses the extension path** (proven to load there, preserves headed+xvfb per the existing workflow, deletes the cascade); the relay survives as an opt-in `SCRAPER_PROXY_MODE=relay` for the operator's local branded Chrome, where it is the only way to proxy at all.* **Original framing, now superseded:** relay vs Chrome-for-Testing + extension. The comparison report favours Chrome for Testing (~4 workflow lines, 0 Python, **preserves headed mode and the proven fingerprint**). But `tasks/lessons.md:89` is measured and damning: an **idle MV3 service worker is EVICTED**, taking `onAuthRequired` with it while `chrome.proxy` settings persist — the browser keeps routing through the proxy while unable to authenticate, *intermittently and invisibly*. *My position: keep the relay, but **decouple it from headless** — the relay forced headless only incidentally; a relay + headed Chrome under `xvfb-run` preserves the proven fingerprint AND avoids the eviction failure mode, and collapses the tripwire cascade. Neither reviewer proposed this; attack it.*
* **P4 — RULED by Ground-truth (F7), adopted.** Restore `WAF_TRAP_THRESHOLD=20` as the default. **For:** 20/2000/3 are the *only* constants ever associated with real rows in any database (`poc-aic-scraper-v2.js` used exactly these when it produced the 792 rows of 2026-03-14/15 from a residential IP), and they were the nodriver defaults unchanged through the whole attested local window; HEAD's `3` is affirmatively harmful on a healthy path because `accumulate()` counts legitimate not-founds. **Against:** the recon is real — but it was measured with curl's TLS fingerprint (a stated lower bound), through a proxy, from a datacenter, against an edge *already* in reputation block; it says nothing about `20` on the unproxied residential default path. **CONDITIONS, both binding:** (1) K6 keeps the constants env-overridable — without it Ground-truth rejects the R set as written; (2) the follow-on cloud WF is **on record as REQUIRED to override these before any dispatch**.

## Standards Compliance (§11)
* **Try-Catch Boundary:** no C-item may introduce a bootstrap-fatal `raise` on the **default** path — that is the precise defect being reverted (8 new fatal gates, several reachable locally). Fatal gates are permitted only inside an explicitly-enabled C-gate. C6's geo assertion is fatal **by design** when `SCRAPER_PROXY_COUNTRY` is set: a wrong-country exit IP must stop the run, never scrape.
* **Unhappy Path Tests** (`npm run test:py`, Spec 44 §5 — G9 requires same-commit rewrite of the 4 coupled files): every gate defaults OFF and the default path takes the pre-drift branch · `build_proxy_username` carries `user-` prefix, alnum session, and `country-ca` only when C6 is on · the geo assertion rejects a non-Canadian IP · `resolve_proxy_port` gives worker N `base+N−1` · `clear_stale_profile_locks` refuses to strip a live Chrome's lock · restored constants are the module defaults · a relay/launch failure surfaces as a caught failure with `PIPELINE_SUMMARY` emitted and `preflight_passed=False` (the G6/B11 regression lock).
* **Regression locks:** G6/B11 and G4's `WAF_TRAP_THRESHOLD`-vs-`accumulate()` interaction each get a named lock; the Regression Guardian owns confirming every deleted line's fence.
* **logError Mandate:** N/A (Python) — existing structured `log()` helper; JS touchpoints unchanged.
* **UI Layout:** N/A.
* **Observability — SCOPED IN (Observability F2, CRITICAL, a direct consequence of my own Round-1 ruling):** `proxy_configured` is computed as `bool(os.environ.get('PROXY_HOST'))` (`aic-scraper-nodriver.py:1856`, `aic-orchestrator.py:482`), and the workflow always sets `PROXY_HOST` from secrets. Under the `SCRAPER_PROXY_MODE=none` default, every cloud run would therefore write **`proxy_configured=true` while genuinely scraping unproxied from a datacenter IP** — recreating the project's headline historical bug (`review_followups.md:2797`) in a new, *code-deliberate* form. **Required in this task:** `proxy_configured` is redefined to derive from the **resolved proxy mode**, not from an env var's presence; `scraper_telemetry` additionally emits the resolved `SCRAPER_PROXY_MODE` (so a reader can tell "deliberately unproxied by policy" from "misconfigured"), C5's verification result, the resolved exit-IP country, and the relay's `blocked`/`allowed` counters (Observability F3 — today they die on the relay's stderr and never reach `records_meta`, which is exactly the metric needed to diagnose a wrongly-blocked host). The **`assert-network-health.js` gate change stays out** (different surface) but its blindness is no longer compounded by a lying field.
* **G1 SCOPE LIMIT — must be closed before PLAN LOCKED (Observability F4).** G1's byte-identical proof covers the *data path*; it does **not** cover `main()`, `bootstrap_with_retry()`, or `preflight_stealth_check()` — the crash-to-verdict path that `de3ff6dd`'s honest-verdict guard depends on, and the code most rewritten by the drift. I used G1 to justify skipping Reality-Check, so this gap is mine. **Action: diff those three functions against `de3ff6dd` and confirm the broad-exception → `preflight_passed=False` pattern survives verbatim** — an unverified claim here is precisely the defect class Spec 08 §4.1.4 names.
* **NOTED (Observability F6):** the worker-level `compute_summary()` verdict at `aic-scraper-nodriver.py:1866` is a parallel boolean (`'FAIL' if miss_status == 'FAIL' else 'PASS'`), not Spec 47's row-derived cascade. Inert today (the orchestrator never parses the worker's `audit_table`), but the file is being rebuilt — fix it in passing rather than re-enshrining the pattern.
* **Observability:** the `assert-network-health` gap stays **open and filed** (`review_followups.md:2799` — it reads `proxy_configured` nowhere, so a fully direct run reports `proxy_errors=0` and PASSES). Closing it means feeding C5's result into `scraper_telemetry`, which is only meaningful when C5 is on; **out of scope here, re-filed with this task's ID.**

## Execution Plan
- [ ] **Step 1 — PLAN PANEL (fail-first, this file is the target).** Spec 08 §6.4 Backend/Pipeline WF2 PLAN roster: DeepSeek **lens set** (spec · idempotency · error-paths) + Gemini `plan`, both CLI; **Integration** (`general-purpose`, MAIN tree — verifies K/C/R against the live tree, `manifest.json` wiring, the real workflow file, and the pytest imports); **Ground-truth** (`general-purpose`, MAIN tree + git — audits every G-claim and every commit hash in this file, and rules P4); **Observability** (`feature-dev:code-reviewer`, worktree — verdict/telemetry contract, Spec 47 §R10); **Yield-and-Economics** (`general-purpose`, MAIN tree — **operator-requested lens, 2026-07-30**: (Q1) did the plan harvest everything genuinely valuable that yesterday's research and testing produced, or does the revert throw away measured findings and instruments? (Q2) does it preserve the properties that made the original **cheap and fast** — the ~0.3 MB/permit budget behind 11K permits/week on one 3 GB plan, and the throughput? It must also rule whether restoring per-batch browser recycling re-arms the cost bug `review_followups.md` D5 blames for 116 cold starts and $6.60, and **establish from git which code was actually running during the cheap, fast weeks** — since if CDP resource blocking is absent at `de3ff6dd`, the "proven" artifact may not be the economical one). Adjudicate every finding with its premise verified; ~⅓ have false premises (Spec 08 §4.1.6). Record rejections with reasons. **Reality-Check SKIPPED with reason — panel may overturn:** `CLAUDE.md:132`'s literal trigger is *"any enriched/derived **parcel** field"* and the agent's mandated instruments (`parcel-sanity-audit.js`, `parcel-field-dump.js`) are parcel-scoped; `Database Impact: NO` and this task changes no derivation logic (G1). **Counterweight the panel must weigh:** Spec 08 `:47`/`:201`/`:282` state RC's trigger generically (*"when values/derived fields are in play"*), and the scraper does derive `permits.enriched_status`. Since G1 proves that derivation is byte-identical before and after, I rule the trigger unmet — **overturn me if the restored path changes any written value.** **Schema-Fidelity SKIPPED:** no migration, no schema change, no contract field.
- [ ] **Step 2 — Round-2** (Spec 08 §7.3: always, for pipeline-critical work) on the findings, and operator rulings on P1–P4. Fold → v2 of this file → **PLAN LOCKED**.
- [ ] **Step 3 —** Preserve the backup per the P1 ruling; restore the base from `de3ff6dd`.
- [ ] **Step 4 —** Apply K1–K5, then C1–C7 gated per the P2/P3 rulings, then the R defaults.
- [ ] **Step 5 —** Rewrite the 4 coupled pytest files + add the Standards-Compliance locks; `npm run test:py` + `npm run test` + `npm run typecheck` + `npm run lint` + ruff green.
- [ ] **Step 6 —** Local smoke, both arms. **(a) Default path**, with the operator's real `.env` untouched (`PROXY_HOST` still set): prove it runs **explicitly unproxied** and pre-drift-shaped — i.e. the same observable behaviour `de3ff6dd` produced, now by declaration rather than by a silently-broken extension. **(b) Proxied path**: `SCRAPER_PROXY_MODE` set → C6's geo assertion fires and a non-Canadian exit IP aborts the run.
- [ ] **Step 6b — OPERATOR-FACING behaviour change, must be communicated, not just shipped** *(DeepSeek HIGH accepted).* After this task, **`PROXY_HOST` alone no longer enables proxying** — `SCRAPER_PROXY_MODE` does. The operator's local `.env` has `PROXY_HOST` set today, so without this note their next local run would be unproxied while they believed otherwise: the *same silent-belief-mismatch class* as `tasks/lessons.md:80`, merely with the sign flipped. Deliverable: a commented `.env` example block + a line in `docs/runbook/README.md` stating the new switch and that unproxied is now a deliberate, declared state.
- [ ] **Step 7 — OUTPUT PANEL** on the diff (Spec 08 §6.4 OUTPUT roster): **Integration + Code Reviewer + Observability + Regression Guardian** (MAIN tree — mandatory for WF2 by definition; owns the fence for every one of the ~620 deleted lines and must state, per deletion, "existed because X; still covered / knowingly retired") + Round-2. Triage: BUG → WF3 before Green Light; DEFER → `review_followups.md`.
- [ ] **Step 8 — Spec truth-up (G7), CORRECTIONS ONLY.** Rewrite Spec 44 §3 + §6 and Spec 115 §2.4 to describe **what this task actually ships** — neither the pre-drift text (headed-Chrome-because-extension, `actions/cache` profiles) nor the 2026-07-30 text (attach mode, relay, tripwire as unconditional contract) is currently true. Record the gates and their defaults as the contract. **Named required edits (F-REL): (i) Spec 115 §2.4 item 10 — rewrite "deny-by-default allowlist (`toronto.ca`, `api.ipify.org`)" to describe the actual 16-host cost BLOCKlist; (ii) `review_followups.md` D1 — downgrade from CRITICAL/LATENT to a documentation defect, closed by (i); (iii) record which configuration (headed+extension vs headless+direct) produced the last real `permit_inspections` rows, per Ground-truth, resolving the Spec 38 / Spec 115 conflict (G12).** ⚠ **Spec 38 restoration REMOVED from this task** — *Gemini CRITICAL accepted:* the recovered session-entry rules (status-lifecycle skip, missing-Status-link/TPS edge cases, per-type inspection regimes, the tighter `assert_data_bounds` matrix) are **untested business logic that changes the data path**, directly contradicting G1's premise and reproducing the exact scope-creep failure this WF exists to undo. Filed as a separate task in Step 9.
- [ ] **Step 8b — KNOWLEDGE HARVEST (operator-directed, 2026-07-30; blocks Green Light).** *"Even though the outcome was terrible, we learned — about how to use the cloud, and about the Toronto site."* Reverting ~620 lines must not silently discard a day of expensively-bought understanding. Two inventories, written **durably** before the code is deleted, each item landing in a named file or being explicitly ruled not-worth-keeping:
  * **(A) Cloud-execution knowledge** → `tasks/lessons.md` (extend, don't duplicate): Ubuntu 24 AppArmor/userns, `actions/cache` + Singleton profile locks, branded-vs-unbranded Chrome and `--load-extension`, nodriver's hardcoded 2.25 s CDP budget **and** `>=0.48` silently resolving 0.50.3, attach-mode process ownership/teardown, xvfb vs headless, DBUS stalls, Decodo auth/scheme/port mechanics + geo-fencing, the metered-bandwidth blowout, CI-vs-local DB divergence, and pytest-not-dispatch as the validation loop.
  * **(B) Toronto AIC portal knowledge** → Spec 44 (a `## Known Failure Modes` / portal-behaviour section). **This is the least replaceable class: it was bought with real requests against a live WAF, and re-learning it costs reputation damage.** Akamai cumulative rate-reputation (~12 requests/~10 min, ~5 min idle recovery, sticky, ~420–450 B Access-Denied body), no session/cookies/CSRF, `Accept-Encoding` absence = instant 403, ~5.3 KB/permit data chain vs `setup.do` at 75 885 B, step 3 already embedding `folderProcessAttempts[]` (step 4 possibly skippable), the ~3-permits-per-10-min-per-exit-IP ceiling, the geo signal, and **how a shadow-ban looks in our telemetry versus a legitimate not-found** (the ambiguity K3 exists to resolve).
  * Items already captured but stated **inaccurately** count as gaps — F-REL is the proof that yesterday's documentation cannot be trusted unverified.
- [ ] **Step 9 —** Route to `review_followups.md`: the `assert-network-health` proxy-blindness gap, the nodriver upstream connect-budget issue, `DBUS_SESSION_BUS_ADDRESS=/dev/null`, and the follow-on cloud-throughput WF (C6 geo experiment first, then the browser-vs-raw-HTTP A/B). Lessons: fold the *"cloud adaptation must be additive and environment-gated — changing the artifact destroys the proven baseline"* lesson into `tasks/lessons.md`.
- [ ] **Step 10 —** WF6 exit gate: commit per phase, `npm run verify`, Husky green.

---

## Panel Adjudication Log — Round 1 (CLI lenses: Gemini `plan`, DeepSeek `spec`)
*Grounder agents (Integration, Ground-truth, Observability) pending; their findings append here before PLAN LOCKED.*

**ACCEPTED — Gemini CRITICAL (C6 geo must not be opt-in).** Correct and damning: an unset default codifies the bot-signalling behaviour as the default for every proxied run. Folded into C6 — mandatory whenever proxying, fail-loud at startup if a proxied run has no country target.

**ACCEPTED — Gemini CRITICAL (Spec 38 restoration is scope creep).** Correct, and it is the *same* failure mode this WF undoes: untested business logic that changes the data path, smuggled in under "truth-up". Removed from Step 8; filed separately.

**ACCEPTED, REVERSING MY OWN POSITION — Gemini HIGH (P2: keep attach mode, drop the monkeypatch).** My position was line-count-driven. Gemini's is better: attach mode is *owned, tested, locally-proven* code (prior WF3's G12 probe), while a 5-line monkeypatch of a third-party private retry loop is a time bomb welded to the exact pin — the day `nodriver` must be upgraded, it breaks with no institutional memory of why it existed. **P2 RULED: attach mode is the gated cloud path; the monkeypatch is not written.** It remains gated OFF so the local default path is untouched.

**ACCEPTED (partial) — Gemini MED (scope the egress telemetry in).** The plumbing only: record C5's verification result **and the resolved exit-IP country** into `scraper_telemetry` / the run summary, so a misconfigured proxied run is visible rather than reporting `proxy_errors=0` PASS. **Changing the `assert-network-health.js` gate itself stays out** — different surface (JS quality script), and gating on a field the default path does not emit would redden every honest local run.

**ACCEPTED — Gemini LOW / DeepSeek MED (B11 must not be re-introduced).** The restored base predates the relay, so the correct-relay-start pattern must be *written*, not inherited. Standards Compliance already requires the lock; making it explicit: relay/launch failure → caught → `PIPELINE_SUMMARY` emitted → `preflight_passed=False`.

**ACCEPTED — DeepSeek HIGH (the `PROXY_HOST` default-path ambiguity) — this is the sharpest finding of the round.** At `de3ff6dd`, `PROXY_HOST` set → the extension path → *silently dropped by branded Chrome* → accidentally unproxied. **Restoring that is restoring an accident, not a capability**, and it re-arms the exact silent-unproxied-scraping failure `tasks/lessons.md:80` was written about. **RULED: the default path is explicitly `SCRAPER_PROXY_MODE=none` — genuinely, deliberately unproxied — regardless of whether `PROXY_HOST` is set.** Proxying is reached only by explicitly selecting a mode, and selecting one activates C6's mandatory geo assertion. This preserves the operator's *observed* local behaviour (unproxied) while removing the silent-failure mechanism that produced it.

**REJECTED (premise verified false) — DeepSeek CRITICAL (relay allowlist will reproduce the shadow-ban).** `scripts/proxy-relay.mjs:46-55` is explicit: *"BLOCK-list, deliberately NOT an allowlist. Deny-by-default was reviewed as a … an allowlist of `*.toronto.ca` cannot be known to be complete."* It blocks 16 named Google/update hosts and passes everything else, so it cannot refuse a WAF/CDN challenge host. **F-REL — this refutation has teeth beyond the finding:** Spec 115 §2.4 item 10 describes a *"deny-by-default allowlist … `toronto.ca, api.ipify.org`"* and `review_followups.md` D1 rates that mechanism **CRITICAL/LATENT — "WILL reproduce the documented shadow-ban on the next run"**. **Both describe a mechanism the code does not implement.** D1 is downgraded to a documentation defect; Step 8 corrects both. *(This is Spec 08 §9's CLI-blind-spot rule earning its keep — and a live illustration of why yesterday's spec text cannot be trusted as ground truth.)*

**REJECTED (premise false) — DeepSeek MED (a dead relay lets Chrome fall back to direct).** Chrome does not silently fall back to DIRECT when a `--proxy-server` is unreachable; it fails the request with `ERR_PROXY_CONNECTION_FAILED`. Spec 44's edge-case list already states this. The C5 tripwire is nonetheless what *proves* it rather than assuming it — which is why C5 exists.

**REJECTED with reason — Gemini MED (delete K2 `clear_stale_profile_locks`).** The layering objection is fair, but `tasks/lessons.md:79` records that once verdicts are honest, failed runs never save caches, so **one poisoned entry is restored forever** — and the function also protects the operator's local profile after a crashed Chrome, which no workflow change can. Belt-and-braces is justified when the failure is silent and expensive. *Folded from the finding:* C2 adds a prominent workflow comment stating why the cache was removed and must not be restored.

**PARTIALLY REJECTED — Gemini HIGH (ship cloud constants in the workflow now).** Agreed that shipping known-wrong config for the deployment target is bad. But the retuned values are **not known-right either** — they were measured through a proxy, from a runner, against an already-blocked Akamai edge (§R). Setting them speculatively repeats the original error with the opposite sign. **RULED: restore local defaults; the workflow sets no scraper constants until the follow-on WF has measured the ceiling it is tuning for.** Recorded as a standing objection.

**DEFERRED to the operator — Gemini MED (P1: reject the on-disk backup).** Gemini is technically right that an inert 2 140-line file rots, pollutes `grep`, and offers false security, and that git is the backup. The operator asked for one explicitly. **My recommendation, which satisfies both:** an annotated **git tag** `deep-scrapes-cloud-attempt-2026-07-30` on the current HEAD, referenced from the comparison report — a named, permanent, zero-rot backup that needs no file. Operator rules at authorization.

### Round 1, lens 2 (DeepSeek `error-paths`, context `tasks/lessons.md`)
**ACCEPTED — HIGH (G13 left an uncorrected phantom CRITICAL in the ground-truth section).** Correct and embarrassing: I rejected the finding in the log but left the false claim standing in G13, so any reader skipping the log would inherit a phantom bug. **G13 is now retracted in place** with the source quote. Mechanical, but exactly the kind of stale-claim rot that made yesterday's specs untrustworthy.
**ACCEPTED — HIGH (the operator's own environment changes silently).** The `SCRAPER_PROXY_MODE` ruling means `PROXY_HOST` alone stops enabling proxying, so the operator's existing `.env` would produce an unproxied run while they believed otherwise — the same belief-mismatch class as `tasks/lessons.md:80` with the sign flipped. **New Step 6b** owns communicating it.
**ACCEPTED — MED (Step 8 must name the spec amendment explicitly).** Folded: Step 8 now names Spec 115 §2.4 item 10 and `review_followups.md` D1 as required edits, not implied ones.
**ACCEPTED — MED (test-rewrite risk is understated).** Fair. The precise per-symbol mapping is exactly what the Integration reviewer was tasked to produce; Step 5 consumes its inventory rather than improvising. Findings fold on arrival.
**ACCEPTED — LOW (Spec 38 headless vs Spec 115 headed).** Both were true at different times: recovered Spec 38 (~2026-04) describes headless + optional proxy, while at `de3ff6dd` `use_headless=False` whenever the extension path was selected. Step 8 must record **which configuration produced the last real rows**, not pick a favourite — assigned to Ground-truth.
**REJECTED with reason — MED ("probe the portal before restoring; the endpoints may have changed").** Already measured, and more recently than this plan: the 2026-07-30 recon (G8) hit the live chain from a cold client and got 200s with no cookies, session, or CSRF, and `review_followups.md:2802` concludes *"Do not rewrite the request chain."* A fresh probe would spend Akamai reputation to re-learn a known fact.
**NOTED — LOW (C6's `country-ca` is Decodo-specific).** True; the format is a provider protocol fact. Recorded in the C6 row rather than abstracted behind a `PROXY_PROVIDER` indirection no second provider yet justifies.

**NOTED — DeepSeek NIT (`DBUS_SESSION_BUS_ADDRESS=/dev/null`).** Already filed (`review_followups.md:2793`); belongs to the cloud path, so it rides C1/C4 in the follow-on rather than this restore.

### Round 1, grounders (Integration · Ground-truth · Observability — all MAIN tree)
**FOLDED — 2 converging CRITICALs (Integration F1 + Observability F1): the live cron makes "default OFF" a regression, not a no-op.** Two independent reviewers reached this from different directions. Non-Goals amended: this task now owns the workflow's C-gate `env:` block. Without it the scheduled job runs the default path on a runner that needs the cloud path, three times a weekday, indefinitely.

**FOLDED — CRITICAL (Observability F2): my own Round-1 ruling would have made `proxy_configured` lie.** The field derives from `bool(os.environ.get('PROXY_HOST'))` and the workflow always sets `PROXY_HOST`; under `SCRAPER_PROXY_MODE=none` every cloud run would report `proxy_configured=true` while scraping unproxied from a datacenter IP — the project's headline historical bug, re-created deliberately. Redefinition scoped in, with the resolved mode, C5's result, exit-IP country and the relay's block counters.

**FOLDED — HIGH (Integration F3 → G14): the runner ships UNBRANDED Chromium and loads the extension.** The largest single correction of the panel. It voids the relay's cloud justification, voids C4 entirely, voids the comparison report's minimum item #4, and re-frames P3. *This is the second time in one review that a premise everyone had accepted turned out to be documented but never verified.*

**FOLDED — HIGH (Ground-truth F2): §R's "override via existing env" was false.** The knobs are drift artifacts; `de3ff6dd` has bare literals and no `MAX_BROWSER_LAUNCHES` at all. New keeper **K6** retains the env-wrapping with pre-drift defaults — a binding precondition of the P4 ruling.

**FOLDED — MED-HIGH (Ground-truth F3): "proven" is a provenance overclaim.** The DB has never recorded a single row from the nodriver scraper; all 792 inspection rows predate it and belong to the deleted Playwright scraper. The reconciliation still holds on the git-freeze evidence, but the artifact is **operator-attested, data-unverifiable** — wording corrected throughout.

**FOLDED — MED (Ground-truth F5): G7 over-attributed the spec drift.** Narrowed to `52691fae` alone; `7c8db089` is unrelated true content that Step 8 must leave alone.

**FOLDED — MED-HIGH (Integration F5): C6 was fiction as written.** No geo-IP mechanism exists in the repo (`api.ipify.org` returns an IP, not a country), and the only code reading the browser's exit IP is C5's. **C6 must name its geo-IP mechanism and declare its hard dependency on C5** — a country assertion with nothing to assert against is theatre.

**FOLDED — HIGH (Integration F4): C3's default could re-proxy the operator's machine.** Local default is pinned independent of whatever P3 selects for cloud.

**FOLDED — MED (Integration F8): the K-set is ~130–160 lines, not ~40**, and K2's "refuses to strip a live Chrome's lock" guard is **new cross-platform development**, not a re-application. Estimate corrected; Step 4 sized accordingly.

**FOLDED — MED (Integration F9): C7's "empty = no blocking" silently reverses today's semantics** (empty currently means *the 16-host default block*), re-opening the $6.60 exposure on any proxied run without the env var. Keep today's semantics; use an explicit sentinel to disable.

**FOLDED — MED (Integration F6): enumerate the gate combinations and mark proven vs novel.** Only two have ever executed. C1-off + C3-relay and "relay + headed under xvfb" are both new; the plan must not present them as restorations.

**FOLDED — MED (Integration F7): K4 is entangled with P3.** Sticky ports are consumed only on the relay path; wiring per-worker ports into the extension body is new, never-committed code.

**FOLDED — LOW/MED (Integration F10): "ruff green" is not currently achievable** — the restored file yields 33 ruff errors (HEAD yields 35), invisible on main because the lint job is PR-triggered only. Budget a noqa pass or drop the claim; do not discover it at Step 5.

**FOLDED — HIGH (Observability F4): G1's byte-identical proof does not cover `main()`, `bootstrap_with_retry()`, `preflight_stealth_check()`** — the crash-to-verdict path, and the code the drift rewrote most. I leaned on G1 to skip Reality-Check, so this gap is mine; it must be closed before PLAN LOCKED.

**CONFIRMED by two independent grounders:** G1's data-path identity (Ground-truth re-derived it rather than re-reading the report), G2's counts and the zero-diff orchestrator/workflow/manifest, G4, G5, G6/B10/B11, G9's exactly-four coupled test files (`test_orchestrator_verdict.py` survives), G10, G12, the pre-drift spec baselines, and every K-set commit hash. **K5 is a no-op** — `requirements.txt:6` already pins `nodriver==0.48.1`. **P1's stated mitigations are no-ops** (pytest's `testpaths` and knip's `src/**` scope already exclude it) — the backup is safe as proposed, but the mitigation text is fiction and must be corrected rather than executed.

**NOTED — Integration:** `test_scraper_config.py:308,321` are regression locks pinning the **drift** values (`RETRY_BASE_MS >= 60_000`, `WAF_TRAP_THRESHOLD <= 5`). The R set retires them; the Regression Guardian must record that as a *knowing* retirement, not an accident.

### Round 1, Yield-and-Economics (operator-requested lens) — the most consequential findings of the panel
**FOLDED — CRITICAL-1 (cost): as originally written, this plan would have cost ~$99/week, unattended.** §R restored **all four cost drivers at once** (WAF threshold 3→20, launch ceiling 12→unlimited, per-batch recycle, retry 90 s→2 s) with C7's bandwidth guard defaulting OFF, **into a live 15-runs/week cron**. Measured unit cost of the incident configuration is 1.81 GB / $6.60 **per run** ⇒ ~27 GB/week against a **3 GB/month** plan; one proxied run alone consumes 59% of the monthly plan. **Mandatory: the workflow env block pins `SCRAPER_BANDWIDTH_GUARD=1`, `SCRAPER_MAX_BROWSER_LAUNCHES=12`, cloud-tuned `SCRAPER_WAF_TRAP_THRESHOLD`/`SCRAPER_RETRY_BASE_MS`, and an explicit `SCRAPER_PROXY_MODE` — or the cron is re-commented — BEFORE the restore lands.** This also resolves MED-9 and closes Gemini's partially-rejected HIGH in Gemini's favour: **module defaults pre-drift, measured values in the workflow.**
**FOLDED — CRITICAL-2 (overturns my own §R ruling): per-batch browser recycle is not restored.** See §R. `368bd878` introduced it 14 days *after* the last real row; it has never coexisted with a successful scrape; `15c7417b` deliberately split IP-rotation from browser-churn and a wholesale revert re-fuses them. ~20× byte multiplier.
**FOLDED — CRITICAL-3 (knowledge): `xfail`, never delete, `TestAkamaiTunedBackoff`** (`test_scraper_config.py:293-321`). It is not scraper plumbing — it is **the measured Akamai model in executable form**, asserting properties (≥60 s base, ≥240 s ladder span, threshold ≤5) rather than literals. §R fails all three, and my Step 5 wording ("assert restored constants are the module defaults") would have replaced the model with literal locks on the disproven values. Deleting it deletes the only enforcement of knowledge whose prose lives solely in a backlog file.
**FOLDED — HIGH-4: C6 rewritten** (see the C-table) — geo is hostname+port, not username; my mandatory fatal gate rested on an unverified premise that a 2026-03-15 commit had already investigated and *removed as incorrect*.
**FOLDED — HIGH-6: the restored artifact is the EXPENSIVE one.** Neither `de3ff6dd` nor HEAD has CDP resource blocking; the deleted v2 scraper did (`poc-aic-scraper-v2.js:497-500`, allowing `script` deliberately so the WAF challenge runs), giving ~4 KB/permit against ~1.5 MB — a **375× reduction**. **Spec 38 §3.6's entire 3 GB/month budget was inherited from v2's blocking and never re-derived for the nodriver port.** Record in Spec 44 that the §3.6 budget does not apply; file resource-blocking as the follow-on throughput WF's first item.
**FOLDED — HIGH-7: `de3ff6dd`'s stealth machinery accelerates the ban it was built to avoid.** `ENTRY_URLS`/`NOISE_URLS` spend 2 extra navigations per 4 permits out of a ~12-request/10-min budget, and `NOISE_URLS` includes `setup.do` at **75,885 B**, adding ≥19 KB/permit against a 5.3 KB data chain. Fencing it out of a *restore* is defensible; shipping it into a live cron is not. Recorded in Spec 44 §3 for the follow-on.
**FOLDED — HIGH-5: the workflow env block must be enumerated as a table before PLAN LOCKED.** The C3 row and the adjudicated `SCRAPER_PROXY_MODE=none` ruling still contradict each other, and nothing sets the mode in the workflow — so today nobody can say whether the next scheduled run is proxied. Under the ruling it would scrape **unproxied from a GitHub datacenter IP**, burning fresh Akamai reputation on an IP shared with every runner.
**FOLDED — MED-8: no throughput claim survives.** Derived from `de3ff6dd`'s own sleep constants: ~600–720 permits/hour/worker, against Spec 38's ~3,600/hr (a v2 estimate) and the measured Akamai ceiling of **~18/hour/exit-IP**. The `~11,000/week` figure is **`de3ff6dd`'s `10,981 pending` queue depth, not throughput.** Spec 44 must carry a nodriver-specific number; §3.6's table is marked v2-only.
**FOLDED — HARVEST (H1–H13): 11 of 12 portal facts live in exactly ONE place** — `review_followups.md`, a 2,800-line append-only backlog with no spec authority — plus commit bodies. Step 8b's inventory (B) is upgraded from "write it down somewhere" to a **named Spec 44 §3 subsection, "Portal behaviour (measured 2026-07-30)"**, carrying the Akamai model with its lower-bound caveat, the `Accept-Encoding` trap, the byte table, step-4 redundancy, the per-IP ceiling, the block signature, the geo rule, and **the disproven list** (so nobody re-spends reputation on `setup.do` warm-ups, `X-Requested-With`, or CSRF). A1 (Ubuntu 24 userns sysctl) goes to `tasks/lessons.md` — today it exists only in a workflow comment.
**ACCEPTED — the headline reframing.** Removing 620 unmeasured lines is still right, but the plan may not claim `de3ff6dd`'s constants are "right for the proven local path" — that path produced zero rows. **This is build-and-prove for cloud AND local; `de3ff6dd` is a legible baseline, not a proven one.**

### Evidence preservation (done during the panel)
The comparison report, this plan, and the geo evidence were all **untracked** and would vanish on a clean checkout; the CI run logs lived only in a temp directory scheduled for pruning. Rescued into the repo: `docs/reports/deep-scrapes-evidence-2026-07-29/` (8 CI run logs, runs 30485096998–30501840576, plus the portal's own `as-interface.js` — the 4,307-byte file that proves there is no token handling). ⚠ **Run 30506470111 — the only genuinely-proxied run, and the source of the 654-requests/0-rows/102-traps signature — is NOT among them**; fetch it from GitHub before it ages out.

> **PLAN LOCKED. Do you authorize this WF2 plan? (y/n)** — *not yet; grounder findings (Integration, Ground-truth, Observability) + Round-2 must land first, and P1/P3/P4 need operator rulings.*
