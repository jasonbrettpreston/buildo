### C4 — data-bounds: two-array split ONLY  *(scope RESOLVED by operator 2026-08-11)*

> **⚠ RE-VERIFIED 2026-08-12 BEFORE IMPLEMENTATION — citations HOLD, but the PREMISE IS REFUTED.**
> **HOLDS:** `assert-data-bounds.js` is untouched by C1/C2/C3/C6 (last commit `e99ae61a`, predates this WF). All six cited lines resolve **exactly**: `:52` `pipeline.run('assert-data-bounds', …)` · `:243` the verdict ternary · `:679`/`:810`/`:941` the three exception-derived `errors.push` · `:998` `if (hasErrors) throw`. **27 total `errors.push` sites = 24 threshold-derived + 3 exception-derived**, matching the 8/11 ground-truth correction.
> **⛔ REFUTED — "Strands 11 permits steps incl. `backup_db` today — the second live copy of this outage."** Measured on cloud over 2026-08-10 → 08-12: `assert_data_bounds` **completed on EVERY run** — `permits` WARN, `coa` PASS, `deep_scrapes` PASS. It halts nothing and strands nothing. The chains that failed died at `assert_lifecycle_phase_distribution` (step 25), which runs **after** data-bounds (step 22) — so data-bounds ran, passed, and the *lifecycle* assert stranded the tail. **C4 closes a LATENT copy of the class-blind throw, not a live outage.**
> **Consequence:** the justification changes from "stop a second live outage" to "retire a latent risk", which affects urgency, and may affect scope (a latent fix has no forcing deadline and could be batched with the filed `assert-network-health` / `assert-staleness` sweep). **Operator call.** Also note the plan's own §5 lesson: this is the *fifth* count/claim in this WF asserted as live fact without re-measurement.
> **NOT YET DONE:** no tests exist.

#### ⚖ C4 PLAN PANEL (Integration + Observability, 2026-08-12) — **9 required corrections, 1 BLOCKING**

**✅ THE CENTRAL PREMISE IS VERIFIED, NOT ASSUMED BY ANALOGY TO C1.** Observability traced all five hops on the live file: `emitSummary` (`:983`) fires unconditionally **before** the `:998` throw → `run-chain.js:519-520` extracts `audit_table.verdict` **on the SUCCESS path, not gated on exit code** → `:636-643` `hasVerdictFails` → `completed_with_errors` → `check-chain-verdict.js:89` `OK_STATUSES` excludes it, with `:103-105` independently scanning `step_verdicts`. Two independent mechanisms, generic per-step machinery.
**✅ The 24/3 partition is CORRECT** — all 27 sites read and classified, no fourth category. Exception: `:679`, `:810`, `:941`. Threshold: `:119, :177, :189, :203, :235, :261, :306, :317, :387, :419, :432, :441, :454, :473, :492, :505, :517, :539, :545, :566, :629, :642, :666, :724`. (`:526`/`:556`/`:576` re-throw into `:941`, inheriting fatal correctly.)
**✅ No AD1-style consumer work needed** — C4 produces the same terminal state C1 already introduced, AD1 already landed at `run-chain.js:225-235`, all four chains carry a verdict step, and no consumer keys on step-level `status='failed'` for this slug.
**✅ Fence surface is near-ZERO** — unlike C1, **nothing repo-wide pins `if (hasErrors) throw`**. `assert-data-bounds.infra.test.ts` locks only threshold externalization. No test to retire.

**⛔ BLOCKING — `:119` `cost_outliers` FAILS C4's OWN HARD GATE. Found INDEPENDENTLY by both reviewers.**
`:118` pushes an error at `costOutliers > 0`; its audit row `:211` is `status: costOutliers >= 20 ? 'WARN' : 'PASS'` — **it can never reach FAIL, and the two conditions disagree over the entire 1–19 range.** Post-C4 trace, end to end: error → non-halting → exit 0 → verdict PASS → `run-chain.js:511` records `PASS` → no `completed_with_errors` → `check-chain-verdict.js:99` green → watchdog green. **Completely silent.** Today it throws loudly. No secondary channel exists — `records_meta.checks_failed` (`:952`) has exactly ONE consumer repo-wide, `src/components/FreshnessTimeline.tsx:1073`, display only.
**Latent today** (live: `cost_outlier_ceiling_cad = $2,000,000,000`, `cost_outliers = 0`, negative-cost permits = 0) — but the partition is unsound at this site and C4's whole safety argument IS the partition.
**FIX (inside "split only"):** change `:211` to `threshold: '== 0', status: costOutliers > 0 ? 'FAIL' : 'PASS'` — strictly LESS severe than today (today `>0` halts the chain). **Alternative:** keep `:119` in the fatal array, making the split **23/4** — but that leaves a *data* condition halting a chain, which is what C4 exists to stop. **Do not ship 24/3 as written.**

**⛔ THE MECHANISM THE PLAN NEVER STATED — why the 3 exceptions MUST stay fatal.** Each audit table is assigned only at the **END** of its block (`permitsAuditTable :240`, `coaAuditTable :393`, `sourcesAuditTable :603`). A mid-block exception means the table is **never assigned**, so the chain-exclusive selector at `:957-968` falls through to **`return {}`** — **no `audit_table` at all.** Exception-derived failures therefore have **ZERO verdict representation by construction**. Keeping them fatal is not prudence, it is load-bearing. **Put this in the plan AND the test-file header.**

**⛔ BLAST RADIUS — 3 of 4 numbers REFUTED** (measured from `scripts/manifest.json`):
| Chain | Plan said | **Actual** | Latent strand |
|---|---|---|---|
| permits | #22, strands 11 | **#22 of 33** ✔ | **11** ✔ (incl. `backup_db` #33) |
| coa | #8, strands 8 | **#11 of 16** ⛔ | **5** ⛔ |
| sources | #26, strands 2 | **#26 of 27** ✔ | **1** ⛔ |
| deep_scrapes | #5, strands 3 | **#5 of 7** ✔ | **2** ⛔ |
**Corrected total latent strand = 19 steps, not 24.** `backup_db` exists only in permits, at #33. **Also delete the residual live-claim sentence further down this section** — it survives *below* the re-verification block that already refuted it.

**⛔ THE OPERATOR'S "SPLIT ONLY" RULING — ALL THREE SUPPORTING CLAIMS ARE NOW FALSE. This is the SIXTH stale premise in this WF.**
| Claim (2026-08-11) | Now |
|---|---|
| "exactly ONE script in `scripts/quality/` carries guard+export (`audit-fk-orphans.js`)" | ⛔ **TWO** — `audit-fk-orphans.js:305/:307` **and `assert-lifecycle-phase-distribution.js:142/:147`, created by C1 IN THIS WF** |
| "no repo precedent for extracting a pure halt-classifier from a quality script" | ⛔ **C1 created exactly that** — `classifyHaltDecision` at `:142` |
| "a guard means re-indenting the entire ~946-line file" | ⛔ **FALSE** — both precedents use a **non-indenting** guard: `if (require.main === module) {` on its own line, `pipeline.run(` left at original indentation, closed `} // require.main === module` (`audit-fk-orphans.js:307/:431`, `assert-lifecycle-phase-distribution.js:147/:930`). **The whole-file-diff cost does not exist.** |
**Our own C1 invalidated the evidence for a ruling made the same day.** The *conclusion* can still stand as JUDGMENT (the classifier here reduces to `fatalErrors.length > 0`, and unlike C1 there is no second source of truth to reconcile) — but it must be **re-stated as judgment, not evidence, and re-affirmed by the operator on the corrected basis.**

**⛔ THE TEST CLAIM DOES NOT SURVIVE CONTACT.** TEST DISCIPLINE says "the 3 exception-derived sites asserted **still fatal**". Only **`:941` is behaviourally provable** — `PIPELINE_CHAIN=permits` + `DROP TABLE permit_trades CASCADE` → `:171` throws → outer catch, and `permitsAuditTable` is never assigned so `records_meta` carries **no `audit_table`**: the strongest possible Case B, proving fatality AND why. **`:679`/`:810` are NOT reachable** without destructive shared-container DDL — the `'does not exist'` substring filters at `:676`/`:807` catch every cheap provocation (dropping the table, dropping `entities`, any type mismatch → `operator does not exist`) and route to **SKIP**, not to the error branch. **Honest plan:** behavioural for the threshold case + `:941`; a **stated-ceiling source assertion** that all three catch-derived pushes target the fatal array (TEST DISCIPLINE permits regex "where forced, state what it cannot catch"). **Do NOT claim all three are behaviourally asserted.**
**Threshold fixture is trivial:** `PIPELINE_CHAIN=permits` on an empty testcontainer is an all-PASS baseline; seed ONE `permits` row with `permit_type='Pre-Permit'` → `:235` fires AND row `:228` is FAIL → post-fix exit **0**, verdict **FAIL**.

**Other required corrections:** ① **Do NOT touch `:947`** (`status = hasErrors ? 'failed'`) — it must stay keyed on the FULL `errors` array; it is the only DB-visible signal on the standalone path (`:972-979`). ② **Document the standalone ceiling** — post-C4, `node scripts/quality/assert-data-bounds.js` with a non-permits-scope failure exits 0 (`:957-968` emits only one, permits-preferred, table). ③ **Name a spec target — C4 has NONE.** No spec text anywhere describes `assert_data_bounds`'s halting posture (grepped); and Observability found two lines that become newly inaccurate: `41_chain_permits.md:141` (*"Any FAIL verdict is logged but does not halt"*) and `30_pipeline_architecture.md:7` (*"a FAIL verdict does NOT halt a chain"*) — both already false today and still false after C4 for the 3 fatal sites. ④ **File, don't fix:** the over-broad `'does not exist'` filters at `:676`/`:807` — a genuinely missing `entities` table silently SKIPs the whole WSIB block, and a missing `permits` table SKIPs the whole inspection block: two "I could not check" conditions masquerading as "nothing to check". ⑤ Implementation fidelity: keep `errors[]` as the full source so `checks_failed` stays accurate; add a **separate `fatalErrors[]`** populated only at `:679`/`:810`/`:941`; `:998` gates on that alone.

**⇒ PANEL RECOMMENDATION: do C4, but BATCH it with the filed `assert-network-health` / `assert-staleness` sweep.** Rationale (Integration): the `:119` discovery is a defect in the **existing audit-row wiring**, not in C4's design — and running the same audit-row audit across the other two scripts *"will almost certainly find more `cost_outliers`-shaped holes. Finding them one script at a time is how the gate gets applied inconsistently."* Zero measured `backup_db` coverage is gained today (latent), so there is no forcing deadline.
**SCOPE RULING — supersedes `:107`'s "same two layers as C1".** C4 gets the **two-array split and nothing else**: NO `require.main` guard, NO `module.exports`, NO extracted classifier, NO `.logic.test.ts`.
**Why (evidence, not preference):** exactly ONE script in `scripts/quality/` carries the guard+export pattern — `audit-fk-orphans.js`, the very precedent C1 cites at `:66` — and it exports a **data constant** (`RELATIONSHIPS`, `:305`), *not* a classifier. There is **no repo precedent** for extracting a pure halt-classifier from a quality script. Meanwhile `assert-data-bounds.js:52` calls `pipeline.run(...)` at top level with all ~946 lines inline, so the guard means **re-indenting the entire file** — a whole-file diff on a file this commit already edits, to enable a helper that reduces to `fatalErrors.length > 0`. C1's export stays mandatory (`:66`) and is NOT in question; the symmetry is what was dropped.
`scripts/quality/assert-data-bounds.js`. ~24 threshold-derived sites go non-halting; **the 3 exception-DERIVED sites (`:679`, `:810`, `:941`) STAY FATAL** — push to both arrays; `:998` becomes `if (fatalErrors.length > 0) throw`. Verdict can reach FAIL (`:243` etc.) → hard gate satisfied. Strands 11 permits steps incl. `backup_db` today — the second live copy of this outage.
**`assert-engine-health.js` is EXCLUDED** (two reviewers independently): its only `errors.push` is the top-level `catch`; every data finding is WARN-only and its verdict at `:282` can **never** be FAIL. Converting it = a dead DB silently continuing + a green job. **Its throw is already correct.**
**Corrected hard gate for any future sweep:** the failure must be *threshold-derived* AND the audit verdict must be *able to reach FAIL*. (Field-presence alone is insufficient — that is what let engine-health through.)
**Remaining sweep (filed, not in scope):** `assert-network-health`, `assert-staleness`. `assert-schema` STAYS halting (step 1 of every chain, fail-fast before any write) — record that decision in the spec.
