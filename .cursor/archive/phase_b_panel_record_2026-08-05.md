# Active Task: Phase B — Sources Incremental Architecture (WF2)
**Status:** Planning — v2, seven 2026-08-04 panel verdicts folded + four operator rulings applied 2026-08-05
**Domain Mode:** Backend/Pipeline (`scripts/`, `scripts/lib/`, `migrations/`, `manifest.json`, `.github/workflows/`) — **B6 is CROSS-DOMAIN** (it touches `src/app/api/admin/pipelines/schedules/route.ts` + `src/components/DataQualityDashboard.tsx` + `src/lib/.../funnel.ts`); read `.claude/domain-crossdomain.md` before B6, not before B1.
**Workflow:** WF2 (Enhance). One commit per step, Red Light before Fix in every step.
**Design authority:** `docs/reports/2026-08-04-sources-incremental-architecture.md`.
**Predecessor:** Phase A COMPLETE (`d6eb9f31`, `2fa3b2e7`) + post-Phase-A residuals WF3 COMPLETE (`6bae172d`, `b01e1f74`, `b4351297`) — record at `.cursor/wf3_deep_scrapes_drain_finalization_record.md`. **Residuals WF3 has two OPEN operator items: migration 238's cloud apply (run 31034937479 awaiting the production-db approval) and the next-nightly proof.**
**Rollback Anchor:** `b4351297`.

## Context
* **Goal:** The sources chain does ~85% of its work to change 4 rows, and then dies. Run 30861473506 hit the 180-min step ceiling with `enrich_parcels --full` still running, so `compute_parcel_cost_estimates`, all four assert gates and the snapshot **never ran at all** — the chain has been silently skipping its own safety net whenever the ceiling fires. Make every step do work only when its inputs actually changed, and split the chain so enrichment gets its own budget.
* **Target Specs:** `43_chain_sources.md` (primary) · `115_scheduling.md` §2.2/§2.5/§6 · per-source `54/55/56/58/59/61/62` · `65_enrich_parcels.md` + `78_optimal_lot_config.md` (pass 4-5 scope) · `40_pipeline_system.md` (manifest/chain) · `47_pipeline_script_protocol.md` · `48_pipeline_observability.md`.
* **Non-negotiable inherited constraint (GT3, HARD):** `enrich-permits.js:438-444` HALTs the DAILY permits/coa chains unless a **completed** `enrich_centreline` run post-dates the latest `load-parcels`. Every new skip path MUST emit a completed run row with re-stamped versions (`emitReducedSummary`, `enrich-centreline.js:445-484`). A skip that emits nothing breaks the nightly chains, not just this one.

## Operator rulings (carried + new)
* **Prior (2026-08-04/05):** full architecture only, no interim quick-wins patch · sources cadence → **bi-weekly** (overrides the report's weekly) · full safety pass → **quarterly** (overrides monthly) · no sources rerun before this lands · parcel enrichment may stay out-of-sync meanwhile · `chain-sources` is `disabled_manually` in GitHub and **B6 must re-enable it THERE as well as in the cron text**.
* **D1 — massing invalidation: ADD A WATERMARK COLUMN** (`parcels.massing_version_when_enriched`), the 5th beside the 4 that exist. Not a `parcels_dirty` side table. Rationale: edits an existing pattern instead of standing up a parallel one; massing is an ANNUAL dataset so the 486K-row invalidation UPDATE fires ~once a year. Link-massing's moved-parcel gap is closed separately by version-stamping its incremental mode.
* **~~D2 — split the chain into two manifest chain definitions~~ — REVOKED by the operator 2026-08-05 after the panel priced it. REPLACED BY D2′.**
* **D2′ — NO SPLIT. Keep ONE `sources` chain. The quarterly full pass becomes a variant of that same chain** — same chain id, same slugs, same consumers — as a `workflow_dispatch`/scheduled workflow with `timeout-minutes` raised toward the 360-min platform ceiling, plus an explicit `--force-full` channel (which B2 must reintroduce anyway, since it removes the only existing lever: DeepSeek DS5 + Integration IN14).
  **Why revoked — the split solved a problem the incremental work already solves.** Bi-weekly steady state is 20-45 min against a 180-min step ceiling (~4× headroom); only the quarterly pass (150-210 min) and the few-times-a-year worst case (270-300 min) need more room, and a raised ceiling gives them that without renaming anything. **What it cost:** 2 Integration BLOCKERS + 1 CRITICAL, ~30 consumers, a silent-permanent-PASS on the GT3 halt (GTB1), 10 hardcoded `sources:` literals reading frozen rows, `PIPELINE_CHAIN` branching breaking across 8 scripts/13 sites (assert-schema goes invisibly green; coverage/data-bounds fall through to the PERMITS profile), a `pipeline_schedules_chain_id_check` migration, 9-14 pinned test files, and Spec 58's FROZEN §9 contract asserting `load_zoning` and `enrich_parcels` are "in the same chain" (GTB4). **What it would have destroyed:** `run-chain.js`'s stop-on-first-failure guarantee that enrichment never runs against a failed load — a stronger guarantee than the failure-isolation the split was sold on (IN3).
  **COLLAPSED TO NOTHING by this ruling:** IN1, IN2, IN3, IN6, IN8/SFB-F2, IN11, IN15, SFB1's `chainFamily` remedy, OB2's chain-identity enumeration, GTB1, and most of GTB4's doc sweep. The cadence-STRING consumers (5 sites + a live DB row) survive and still belong to B6.
  **Contingency, stated honestly:** `enrich_parcels --full` has never been observed to FINISH (killed at 123 min), so the quarterly pass's true duration is unknown. If one measured quarterly run exceeds what a single job can give, the split returns — as a decision made on a measurement rather than an estimate.
* **D3 — TWO-TIER gate for the daily-regenerated CSVs:** poll `package_show` first (unchanged `last_modified` → skip free); if it moved, download and **content-hash**, and skip parse+upsert when the hash matches. Removes the 2-week observation dependency entirely — the hash is authoritative whether or not the timestamp is noisy.
* **D4 — comps/optconfig stay in `enrich_parcels`, with a SCOPED refresh** (parcels near a permit/CoA decision landed since the last completed run, ∪ `comp_count IS NULL`). Not moved to the permits chain; no Spec 65/78 boundary change.

## State Verification (measured 2026-08-05 against cloud `aws-0-ca-central-1.pooler.supabase.com`)
* **Watermark columns on `parcels`:** `centreline_dataset_version_when_enriched`, `heritage_dataset_version_when_enriched`, `ravine_dataset_version_when_enriched`, `zoning_enriched_at`. **No massing watermark** — confirms the D1 gap.
* **The stuck set is real and is NOT converging:** 14,526 of 486,530 valid-geom parcels carry a NULL centreline stamp today vs the report's 14,510 on 2026-08-04 — four weeks of weekly recomputes moved it by +16. Reality-Check's "STUCK, not small churn" is the correct characterisation (B4).
* **`enrich-heritage.js` is write-only as reported:** `main()` reads the producer contract and passes `datasetVersion` into `enrichHeritage` (which stamps it), with **no skip predicate anywhere in the path** — 8.8 min/run doing provably nothing.
* **Manifest `--full` pins:** exactly two, both `chain_args.sources` — `enrich_parcels` (`manifest.json:18`) and `link_massing` (`:30`). SF-A3 confirmed: the link_massing pin defeats its own gate's purpose every run.
* **Sources cron is `0 13 * * 0`** (GT factual fix — the earlier plan said `0 14`), and the workflow is currently `disabled_manually`.

## Design (report §6, with the four rulings applied)
* **B1 tier-1 skip:** one shared `scripts/lib/source-version.js` (GT4 / report item 11) consolidating the **four divergent `skipCheckDecision` copies** — ravines `:163-171`, heritage `:186-193`, centreline `:247-255` (adds contentHash to the no-validators bail), zoning `:328-337` (CKAN metadata equality + a 730-day force-reload). Extend the poll to the Group-B loaders (address-points, parcels, massing) with the D3 two-tier gate.
* **B2 enrich-parcels:** drop BOTH manifest `--full` pins; add the D1 massing watermark so passes 2-3 stop being blind; all five passes then run their existing incremental predicates. Pass 4 gains the D4 scoped refresh.
* **B3:** port the heritage skip (all three required pieces already exist); gate `link_parcel_addresses` (10.2 min, zero skip logic) and `compute_parcel_cost_estimates` (streams ~380-437K parcels every run); add `link_wsib`'s attempted-watermark (~121K permanently-unmatchable rows re-trigram-matched weekly).
* **B4:** diagnose-then-fix the 14,526 stuck parcels.
* **B5:** runtime CKAN resource resolution for massing (kills the 404-rotation class that has bitten twice).
* **B6:** the D2 split + bi-weekly cadence + quarterly full-pass variant + every consumer of both.
* **Compute target:** bi-weekly steady state ~20-25 min (report §8), quarterly full ~2.5-3.5 h.

## Spec Updates (NAMED DELIVERABLE — this WF does not close until every line is edited)
Phase A shipped code with zero spec edits and the residuals WF3 had to clean it up; that is not happening twice. Per `00_engineering_standards.md` §14 each spec edit rides ITS OWN step's commit, not a trailing doc commit.

| Spec | What is wrong TODAY | Rides |
|---|---|---|
| `43_chain_sources.md` §1 + §2 `**Schedule:**` | Says **"quarterly chain"** / "Quarterly (address_points, parcels, massing, zoning...)" while Spec 115 §2 row 2 and the cron said **weekly** `0 13 * * 0`. Pre-existing spec-vs-spec contradiction, discovered while planning this WF | B6 |
| `43` §2 step table + §2 Chain-Specific Arguments + §3 Core Logic 9 | Describes ONE 27-step chain and the two `--full` pins as load-bearing; both pins are being dropped and the chain becomes two definitions | B2, B6 |
| `43` §2 Runtime paragraph | Cites the P11 projection (~61 min, `enrich_parcels --full` dominant residual) — superseded | B6 |
| `115_scheduling.md` §2 row 2, §2.5, §6, plus `:393`/`:773`/`:780`/`:1051` (GT) | Weekly cadence, `chain_sources` freshness 204h, and the `pipeline_schedules` row — all must move to bi-weekly AND to two chain slugs | B6 |
| `56_source_massing.md` / `62`, `61`, `59` | Per-source skip semantics change when the shared module lands | B1, B5 |
| `65_enrich_parcels.md` + `78_optimal_lot_config.md` | Pass-4 comp refresh scope (D4) | B2 |
| `48_pipeline_observability.md` | The new skip-row + convergence-row contracts | B1, B4 |
| `docs/runbook/README.md` | Quarterly full-pass operator procedure | B6 |

## Standards Compliance
* **Database Impact: YES** — B1 (a watermark/hash store per source; prefer `pipeline_runs.records_meta`, which is the existing version store — **no `source_versions` table unless the panel forces one**, SF ruled the pattern generalizes) and B2/D1 (one nullable TEXT column on `parcels`, metadata-only ALTER, no rewrite). UP+DOWN per §3.2, DOWN all-comments per validate-migration Rule 6. `BUILDO_TEST_DB=1` DB test per migration.
* **Spec 47 §R8 (C4 ruling):** `scripts/lib/source-version.js` is a **library, not a pipeline script** — R8-exempt, no advisory-lock id, no `pipeline.run` skeleton. Any NEW standalone script would need the full skeleton + a lock id from the §A.5 registry; this plan adds none.
* **Cross-Layer Contracts (§11):** the cadence strings ('Bi-weekly') cross `manifest`/`seed-pipeline-schedules.js`/the admin validator/the dashboard. There is no `_contracts.json` entry for cadence and no DB CHECK (verified 2026-08-05: `pipeline_schedules.cadence` is plain TEXT). **Ruling: add the new cadence to all four consumers in B6's single commit** rather than minting a contracts entry — the enumeration count is the problem, not the absence of a registry. B6 must also fix `ScheduleEditModal`'s dropdown (filed 2026-08-05 as the third independent enumeration).
* **Idempotency:** every gate/poll is a read-only decision; loaders stay `IS DISTINCT FROM` upserts; watermark writes are guarded.
* **Unhappy paths (Red Lights) — per step, below.** Every skip path's red test asserts the GT3 completed-row invariant.

## Execution Plan (one commit per step; Red Light BEFORE Fix; spec edits ride their own step)
- [ ] **B0 — Spec Review + State Verification** (C2's named step): re-read Specs 43/115/56/65/78 against live code; record per-step live counts (dirty sets, watermark coverage, last-run `records_meta` shapes) BEFORE any change.
- [ ] **B1 — shared `source-version.js` + tier-1 skip.** *Red:* a decision table asserting **each of the four divergent branches SURVIVES consolidation** (RG1 — ravines/heritage contentHash, centreline's contentHash-in-no-validators bail, zoning's 730-day force-reload), plus CKAN-unreachable → **fail-safe LOAD**, plus the D3 two-tier cases (metadata unchanged → skip; metadata moved + hash equal → skip parse; hash differs → load), plus **per source: a skip emits a COMPLETED row that CARRIES FORWARD the prior `records_meta` versions** (GT3 + SF-A2 + OBS3). Port `load-zoning`'s `skip_check_error` catch+row (OBS3b). Storage key shape decided here (SF-A1).
- [ ] **B2 — enrich-parcels.** *Red:* massing watermark gate (changed → passes 2-3 bite; unchanged → incremental), manifest-pin-absence locks for **BOTH** `enrich_parcels` and `link_massing` (SF-A3), pass-4 scoped-refresh predicate (D4), and each of the 5 passes' existing predicates verified against live data. Migration for the D1 column + its DB test.
- [ ] **B3 — the three unconditional recomputes.** *Red, enumerated (C3):* heritage skip fires on unchanged version and emits its completed row; `link_parcel_addresses` skips when BOTH `address_points` and `parcels` report 0 ins/upd; `compute_parcel_cost_estimates` skips when enrich updated 0 rows AND the rate tables are unchanged; `link_wsib` stops re-attempting permanently-unmatchable rows.
- [ ] **B4 — the 14,526 stuck parcels. DIAGNOSE FIRST** (C3: the red light is untestable until the cause is known). Produce the cause, THEN a red test that the set shrinks, THEN the fix. Emit a **bounded convergence row — WARN if the count is ≥ the previous run's** (OBS4), not a bare INFO.
- [ ] **B5 — runtime CKAN resource resolution for massing.** *Red:* resolver picks the newest matching `3DMassingShapefile_*_WGS84.zip`; a stale/404 id is handled, not fatal.
- [ ] **B6 — the split + cadence (CROSS-DOMAIN).** Two manifest chains; two workflows (or one with two jobs) each at 180 min; bi-weekly week-parity guard (DST-safe); quarterly full-pass variant; **re-enable the workflow in GitHub, not just in cron text**; watchdog `chain_sources` 204h → bi-weekly + slack **and split across the two new slugs** (RG2); the four cadence consumers + `ScheduleEditModal`. *Red:* week-parity fires the correct weeks incl. DST boundaries; infra locks on both workflow shapes; a freshness lock per new slug.
- [ ] **B7 — OUTPUT panel + proving runs.** Pipeline WF2 roster incl. Reality-Check (enriched parcel fields change) + Schema-Fidelity + Observability + Integration + Regression Guardian. Proving: one bi-weekly-shape run green end-to-end **including steps 21-27, which have never completed on cloud**, and one quarterly-shape run green in its window.
- [ ] **B8 — Close-out.** Lessons (metadata-gated loads; a watermark that is written but never read is not a gate; four copies of one decision function is three too many), memory, and the Spec Updates table above signed off line by line.

## Known risks
* **The GT3 invariant is the sharpest edge in this plan.** Six new skip paths each have to emit a completed, version-re-stamped run row or the DAILY chains halt. Every red light asserts it; B7's proving run is the real proof.
* **Consolidating four divergent decision functions is where a silent regression would hide** (RG1). The red-light table asserts each source's branch individually, not the consolidated happy path.
* **The chain split changes `pipeline_runs` slugs**, which the watchdog freshness check and the admin dashboard both key off — the same cadence-coupling that nearly shipped a false red this morning. B6 owns every consumer.
* **B4 may find the stuck set is a data defect, not a gate defect** — in which case its fix could be larger than a WF2 step and should be re-scoped rather than forced.
* Quarterly full pass leans harder on the invalidation fences being airtight than a monthly one would (report §9.5, operator-ruled).

## Panel verdicts — Phase B plan altitude (2026-08-05)
* **DeepSeek (adversarial, plan altitude) — APPROVE WITH CHANGES. 12 findings; 4 are landmines that would reproduce the original failure or break the DAILY chains.**
  * **DS4 CRITICAL — the GT3 skip-row rule, as I wrote it, can BREAK THE NIGHTLY CHAINS.** `enrich-permits.js:438-444` HALTs unless a completed `enrich_centreline` run post-dates the latest `load-parcels`. If `load_parcels` SKIPS but still emits a completed row (which my B1 invariant REQUIRES), that row's timestamp is NEW — so it now post-dates the last `enrich_centreline` row, and the daily chains halt. The invariant needs re-scoping: a skip row must carry forward the prior VERSION without advancing the recency comparison the nightly HALT depends on. This is the plan creating the exact class of failure it exists to prevent.
  * **DS1 HIGH — first-run semantics of the new watermark are undefined.** `parcels.massing_version_when_enriched` is NULL for all 486,530 rows on day one, so the gate sees "everything stale" and the first run after B2 is a FULL enrich — the 123-min step that started all of this. Must either backfill the column at migration time or explicitly size and schedule that one-time full pass.
  * **DS2 HIGH — B5 must precede B2** (or merge into it). B2's massing gate compares a massing version that B5 is what makes correct; running B2 first gates on a value that can rotate out from under it.
  * **DS5 HIGH — the quarterly full pass has no mechanism.** B2 removes both `--full` pins; nothing in the plan then defines how the quarterly run forces a full recompute. Needs an explicit force channel (`--force-full` chain arg or a `LINK_MASSING_FORCE_FULL`-style env) AND a red test.
  * **DS11 MED — B3's skip conditions test the wrong thing.** "Skip when address_points and parcels report 0 ins/upd" must also require those loader runs COMPLETED. A failed loader emits no completed row, and a downstream skip would then silently mask the failure. Same for `compute_parcel_cost_estimates` vs a failed `enrich_parcels`.
  * **DS10 MED — B1's fail-safe branches are unstated:** no prior `records_meta` → LOAD; malformed/partial `records_meta` → LOAD; CKAN unreachable → LOAD (that one is in the plan). Also names a real hole in D3: when `last_modified` is UNCHANGED we never download or hash, so a content change without a timestamp change is missed. Acceptable, but it must be an explicit stated assumption rather than an unexamined one.
  * **DS12 MED — B4's "set shrinks" red light is confounded** by B2/B3, which may themselves shrink the stuck set before B4 starts. Re-baseline after B2/B3, before diagnosis.
  * **DS3 / DS6 / DS7 / DS8 / DS9 (MED-LOW):** link_massing's moved-parcel closure is named in D1's rationale but appears in no step · `link_wsib`'s unmatchable watermark needs invalidation semantics (when does a permanently-unmatchable row get re-tried?) · B6 needs a MIGRATION for the existing `pipeline_schedules` row plus a red test per old-slug consumer · B7's proving run must genuinely exercise steps 21-27 rather than assert them · B6 needs a rollback step for the GitHub workflow-enable.
  * **DS10b — migration 238 (residuals WF3) is a hard prerequisite** for B1/B2, and the migration/code deploy ordering must be stated: code reading `massing_version_when_enriched` before the migration applies will crash.

* **Compliance — CONDITIONAL FAIL, not lockable. 3 HIGH + 1 MED-HIGH blocking, 8 lesser.**
  * **CF1 HIGH — the plan's skeleton is a WF3's, not a WF2's, and the root cause is VERIFIED not inferred:** the predecessor file heads its checklist "per `.claude/workflows.md` **WF3 discipline** applied to each step" and asks "authorize this **WF3** plan?". v2 corrected the label but never re-derived the step list from `workflows.md:67-95`. **Missing WF2-only steps: Contract Definition · Schema Evolution's four commands + factories · UI Regression Check · Pre-Review Self-Checklist · Green Light.** Consequences named: no Contract Definition means the chain-slug/cadence contract crossing `pipeline_runs` → watchdog → admin ships undefined (the very seam my own Known Risks calls out); no Green Light means the WF could close without `npm run test` ever running; and the Pre-Review Self-Checklist is a **regression** — the predecessor HAD the sibling sweep (other write-only watermarks, other hardcoded CKAN ids, other never-converging dirty sets), which is exactly the class-not-symptom check B3/B5 need.
  * **CF2 HIGH — B6's cross-domain surface is declared but not complied with.** Try-Catch Boundary, logError Mandate and UI Layout are absent from Standards Compliance; §11's API-Route block is 0/8 and its UI-Component block 0/5. All targets verified on disk (`schedules/route.ts`, `DataQualityDashboard.tsx`, `ScheduleEditModal.tsx`, `funnel.ts`). Minimum missing test named: invalid cadence → 400, which is precisely where a fifth enumeration site would reject `'Bi-weekly'` in production.
  * **CF3 HIGH — the 486K-row backfill question is unanswered (converges with DeepSeek DS1).** "Metadata-only ALTER, no rewrite" answers *locking*, not *backfill*. Both branches are bad and it is a PLAN decision: NULL ⇒ stale means the first run re-enriches all 486,530 parcels (the step that already blew the ceiling); NULL ⇒ fresh means the gate never fires until massing next rotates (annual) and D1 buys nothing for up to a year. Also: rule `factories.ts`/`db:generate` explicitly rather than silently — precedent is MIXED (`zoning_enriched_at` IS in `types.ts:58` + `factories.ts:121`, the three `*_dataset_version_when_enriched` are in neither).
  * **CF4 MED-HIGH — B7's OUTPUT roster is under-populated:** missing the base Code Reviewer, Gemini, the DeepSeek 4-lens set, and Round-2 (Spec 08 §7 rule 3 makes Round-2 mandatory for pipeline-critical work), plus the triage rule (BUG → WF3 before Green Light; DEFER → review_followups).
  * **CF5-CF12 (MED→LOW):** no B-specific PLAN-roster record (the 2026-08-04 panel was ruled to cover A only — B's rides its own ceremony, i.e. THIS one) · §14 Prevention Loop unapplied: my "update all four consumers in one commit" is the same commit-time discipline that has now failed three times, and §5.2's **parity battery** would reduce enforced divergences to zero without adding a source of truth · no SPEC LINK commitment for ~8 new test surfaces · `npm run system-map` not planned despite ~8 spec edits · cross-domain reading deferred to B6 is ILLEGITIMATE (`domain-crossdomain.md:4` says read before generating the active task) and its Scenario-A handoff/JSON-contract note is missing · Spec 05 §5 six-field footer not committed to (husky `commit-msg` enforces it — 8 commits would be blocked or waved through) · **Spec 47 §R8 MIScitation: the exemption is REAL but §R8 is "Computation — pure functions from scripts/lib/", which ENDORSES libs; the correct citation is Spec 47's `:5` scope line ("runs as a step in any pipeline chain")** · SQLFluff, §9.5 stream-hash rather than buffering a full daily CSV, pre-flight, and `scripts/CLAUDE.md` required-reading record.
  * **CONFIRMED STRONG:** State Verification (live, dated, and it disconfirms its own prior number) · the GT3 completed-row invariant treatment, called "the single best compliance artifact in the document" · Prime Directive #10 "met to an unusually high standard" · the Cross-Domain classification of B6 · the Spec 47 library exemption itself · one-commit-per-step · the Spec Updates table as a named deliverable with per-line owning steps.

* **Observability — 1 CRITICAL, 4 IMPORTANT. It refutes this plan's central safety claim.**
  * **OB1 CRITICAL — "the GT3 invariant protects all six skip paths" is FALSE; it reaches two, both weakly.** `enrich-permits.js` has FIVE guard shapes, not one: `assertCentrelineEnriched` (`:301-332`) is the ONLY strict-recency guard · `assertRavinesEnriched`/`assertHeritageEnriched` (`:141-156`) and `assertWf2Ran` are **existence-only** (`COUNT(*) WHERE stamp IS NOT NULL > 0`) — trivially satisfied forever once any parcel was ever stamped, with no recency component at all · the cost/optconfig/max-build asserts are **column-existence** only, proving a migration ran, not that data was refreshed. And a grep for `wsib`, `parcel_addresses`, `cost_estimate` returns **zero guards**. So for `link_parcel_addresses`, `compute_parcel_cost_estimates` and `link_wsib`, a skip that emits nothing produces **no HALT anywhere in the codebase** — precisely the silent staleness this WF exists to cure, now behind a gate I claimed was safety-netted. **The protection must come from B1/B3's own red lights + B7's proving run, NOT from an inherited HALT that does not reach them.** My Known Risks paragraph must be rewritten, not softened.
  * **OB1b — my citation was stale and the agents caught it by checking:** `enrich-permits.js:438-444` is inside a `WITH ag AS (...)` CTE, not the guard. The real one is `:301-332`. (Second bad citation this panel — see Compliance's §R8 catch.)
  * **ADJUDICATION vs DeepSeek DS4:** OB confirms `load_parcels` (step 4) and `enrich_centreline` (step 13) BOTH sit in `sources_load` under the split, so within a run centreline's completed row always post-dates parcels' — DS4's "skip advances recency and halts the nightly chains" is **largely mitigated by the plan's own emit-a-completed-row rule plus the split keeping the two steps together**. It remains real ONLY if `enrich_centreline`'s own skip path emits nothing. Downgrade DS4 from CRITICAL to HIGH and state the condition explicitly rather than deleting it.
  * **OB2 IMPORTANT — the chain-IDENTITY enumeration is broader than the cadence-STRING one I scoped, and fixing the string fixes none of it.** Unlisted consumers, all verified: `admin/pipelines/[slug]/route.ts:22-40` `CHAIN_WORKFLOWS`/`WORKFLOW_CHAINS` (dispatch AND cancel-row-marking) · `admin/stats/route.ts:316,338` — its own `CHAIN_SLUGS` array **and a third independently-hardcoded `chain_sources: 192` freshness constant**, distinct from `check-pipeline-freshness.js`'s 204h · `DataQualityDashboard.tsx` — **two** separate identity arrays (`:111-117` HealthBanner `CHAINS`, `:215-221` `CHAIN_FRESHNESS_LABELS`) · `chain-sources.yml:66,73,85` invoking `check-chain-running`/`run-chain`/`check-chain-verdict` with the literal `sources` argument. **Failure scenario to keep verbatim:** the dashboard keeps showing one Sources tile while `sources_enrich` — which carries the cost estimates, all four asserts and the snapshot, i.e. the exact safety net this WF exists to restore — is invisible to both the health banner and the admin dispatch button. "The identical shape of bug this WF was triggered by, recreated one layer up, in the UI that is supposed to catch it."
  * **OB3 — the two-tier gate needs three DISTINGUISHABLE audit outcomes, not three decision cases.** Precedent emits one shared reason (`unchanged_last_modified`, `no_op_refresh`). Without a tier-specific reason value, the report's own Q2 ("is `last_modified` noisy?") becomes permanently unanswerable from the audit trail — the hash tier exists to moot that question, but only if an operator can see which tier decided.
  * **OB4 — B4's convergence row needs the missing-previous-value fallback.** The cited precedent `readLastEnrichedVersion` (`:289-301`) deliberately does NOT trust a single field lookup; it falls back to scanning `audit_table.rows`. A bare `current >= previous` with an undefined previous evaluates FALSE in JS — silently suppressing the WARN on the first post-deploy run, exactly when the anchor is least trustworthy.
  * **OB5 — Spec 48 §3.7 (regularly-REDUCED step) must ride B2 and B3, not just B1/B4.** B2 turns the chain's largest step from always-full to usually-skipped and B3 adds four more near-zero steps; without the §3.7 artifacts, `observe-chain.js`'s narrative flags five steps' shrinkage as anomalies for several runs — the false-CRITICAL noise class §3.7 already had to solve once.
  * **CONFIRMED SOUND:** the plan correctly distinguishes a data-driven skip from `withAdvisoryLock`'s lock-contention SKIP payload · the verdict cascade stays row-derived · the chain split does NOT break centreline's recency ordering · `observe-chain.js`'s chain-scoped lock self-heals for new chain ids (suspected gap, ruled out).

* **Integration — 2 BLOCKERS, 1 CRITICAL, 4 HIGH, 8 MED. Cloud host printed. THE SPLIT AS SCOPED IS WRONG.**
  * **IN1 BLOCKER — the split renames the exact slugs the GT3 HALT matches on.** `enrich-permits.js:307-308` matches `pipeline IN ('sources:enrich_centreline','enrich_centreline')` and `('sources:parcels','parcels')`. `run-chain.js:375` composes slugs as `` `${chainId}:${slug}` ``, so after B6 they become `sources_load:*` and **neither literal matches**. `enrichedAt` then resolves to the frozen 2026-08-04 row (verified live), so the first `load-parcels` after the split makes `enrichedAt < parcelsAt` → `:314-316` throws → **the DAILY permits AND coa chains both halt, permanently**, because no future run can advance a `sources:`-prefixed row. I defended GT3 against skip paths and not against my own rename.
  * **IN2 BLOCKER — 10 files hardcode `sources:` literals** for producer/self contracts (`enrich-ravines:19`, `enrich-heritage:27,28`, `enrich-centreline:23,27,292`, `enrich-parcels:54,55`, `load-zoning:41`, `load-ravines:37`, `load-heritage:38`, `load-centreline:43`, `massing-full-gate:43`). Post-split they read **frozen historical rows silently — no throw**, because the old rows still exist. `enrich-centreline:292`'s `SELF_NAME` is the deliberate "run ledger, not the per-parcel column" design; post-split it compares against a permanently-stale version forever. **Worse than a crash: the gates keep returning a verdict, just the wrong one.**
  * **IN3 CRITICAL — the split destroys mutual exclusion and ordering, both of which are currently free.** The chain advisory lock is `hashtext('chain_' || $1)` — keyed on chain id, so the two halves take DIFFERENT locks; `check-chain-running` is per-slug; GH `concurrency.group` is per-workflow-name. A `sources_enrich` dispatch could run `enrich_parcels` against `parcels` mid-upsert. Today `run-chain.js` serialises this and stop-on-first-failure guarantees enrich never runs on a failed load. The plan adds no cross-chain lock and no ordering fence.
  * **IN4 HIGH — MY PLAN CONTAINS A FALSE PREMISE.** I wrote "SF-A3 confirmed: the link_massing pin defeats its own gate every run." The code says the opposite: `FULL_MODE = forceFull || (explicitFull && gateChanged)` — **the pin is the PERMISSION, the gate is the DECISION**, and the report's own run log shows it working (`--full` self-downgraded to `incremental:gate_unchanged`). Dropping the pin sets `explicitFull=false` permanently, so the annual massing vintage change **never triggers a full relink**, and incremental mode is link-existence-only, so changed footprints on already-linked parcels are never re-evaluated. **The two pins are NOT symmetric and must not be dropped together.**
  * **IN5 HIGH — `massing-full-gate.js` does not generalize as the B1/B3 template.** Its `COUNT(*)` proxy exists *because* load-massing has no dataset version — B5 adds one, so copying the proxy propagates a workaround past its cause. It only ever DOWNGRADES; every new gate must also ESCALATE. And `prevCode !== undefined` means **missing prior signal → treat as unchanged → skip: FAIL-OPEN**, the exact inverse of B1's stated "CKAN-unreachable → fail-safe LOAD".
  * **IN6 HIGH — `PIPELINE_CHAIN` branching breaks silently across 8 scripts / 13 sites.** `assert-schema.js:287` is `CHAIN_ID === 'sources'` → under `sources_load` **all source schema-drift probes stop running and the assert goes invisibly green**. `assert-global-coverage.js:97` and `assert-data-bounds.js:97,959` fall through to the **permits** profile, producing wrong coverage rows against the wrong table. Plus phase-number drift in 10 more sites.
  * **IN7 HIGH — B3's gate predicate is unsafe against this chain's actual failure history.** Live evidence of mid-chain deaths (ids 2073 `Stopped at step: address_points`, 2133 `Stopped at step: massing`, 2158 the timeout). Run N inserts 50K address rows then dies before `link_parcel_addresses`; run N+1's loader legitimately reports 0 ins/upd → **the gate skips and those 50K are never linked, permanently**. The predicate must be a WATERMARK (last completed `link_parcel_addresses` vs last loader run that WROTE), not a latest-run counter. Same for the cost step.
  * **IN8 MED-HIGH — undeclared migration:** `pipeline_schedules_chain_id_check` (verified live) is `CHECK (chain_id = ANY (ARRAY['permits','coa','sources','entities']) OR chain_id IS NULL)`. New chain ids can never be scoped there without a migration.
  * **IN9 — the consumer enumeration is worse than I scoped, and my cadence count is wrong.** Unlisted: `FreshnessTimeline.tsx:172` `CHAIN_META` + `:530` `DISPATCHABLE_CHAINS` (**`chain.logic.test.ts:900-916` hard-fails on any manifest chain absent from CHAIN_META**), `local-cron.js:78`, `observe-chain.js:63`. And **`manifest.json` carries NO cadence field at all** — the real cadence set is 5 code sites plus a live DB row, not the 4 I named.
  * **IN10 MED — LIVE FINDING ABOUT WORK SHIPPED THIS MORNING: editing the seed does not change cloud.** Cloud `pipeline_schedules` still reads `deep_scrapes` = `'Weekdays (3x Daily)'` / `'0 15,18,21 * * 1-5'` while the seed file says 1x since `2fa3b2e7`. **Actionable now, independent of Phase B.**
  * **IN11-IN15 MED:** neither two-job shape is free (shared `concurrency.group` + `cancel-in-progress:false` queues the second behind the first; two workflows diverge and re-open IN3) · **the week-parity guard emits a false RED every off-week** — the verdict step isn't gated by parity, so it reads the 2-week-old row, which today is id 2158 `status='failed'` · no `CHAIN_DURATION_BUDGET_MINUTES` is set, so `check-chain-verdict`'s duration tripwire — built for exactly the creep-then-timeout class this WF exists to fix — is dead here · the quarterly full-pass mechanism is missing (converges with DeepSeek DS5) · 9 test files pin the current names.
  * **CONFIRMED SOUND:** both manifest pins are exactly as stated and are the ONLY `chain_args` entries in the manifest — so dropping them empties the rename landmine, **which makes B2-before-B6 order-critical** · the four `skipCheckDecision` copies are at the cited lines · **`records_meta` genuinely works as the version store; no new table needed** · the D1 gap is real (verified live) · pass-1's predicate is type-sound (`timestamptz` both sides), so dropping `enrich_parcels`' pin is defensible **provided D1+D4 ship in the same commit** · **`funnel.ts` needs no change and should be DROPPED from B6's scope** (`resolveLastRun` matches `key.endsWith(':'+slug)`, prefix-agnostic) · two chains CAN structurally coexist in `manifest.chains`.

* **Schema-Fidelity — 1 CRITICAL, 4 HIGH, 3 MED, 1 LOW. Cloud host printed. It supplies the fix for the split AND breaks the D1 ruling.**
  * **SFB1 CRITICAL + ITS REMEDY (converges with IN1/IN2/OB1).** Same slug-rename catastrophe, independently derived — and the remedy the other agents did not give: **decouple the slug FAMILY from the chain KEY.** Add an explicit `slugPrefix`/`chainFamily: 'sources'` to BOTH new manifest chain definitions and have `run-chain.js` use it for `scopedSlug` (`:307,338,375`) and `PIPELINE_CHAIN` (`:410`), so only the chain-level row (`chain_sources_load`/`chain_sources_enrich`) changes and every `sources:`-prefixed literal keeps matching. **This is what makes D2 viable at all.**
  * **SFB2 HIGH — THE D1 RULING CANNOT WORK AS DESIGNED. There is no massing version to store.** Verified live: the last `sources:massing` run's `records_meta` holds only `errors`/`telemetry`/`audit_table`/`features_*` — **no dataset version, no last_modified, no hash** — and `massing-full-gate.js:11-12` says so in prose. B5 would supply one but is sequenced AFTER B2. So B2 as ruled lands a TEXT column its producer emits nothing to write into: the stamp is constant, the predicate is constant-false (gate never fires, identical to today's blindness) or constant-true (full 486K recompute every run) — literally the B8 lesson "a watermark written but never read is not a gate". **Better shape, already in the schema: `massing_enriched_at TIMESTAMPTZ` compared against `parcel_buildings.linked_at` (exists, NOT NULL, indexed) via `EXISTS (… pb.linked_at > p.massing_enriched_at)`** — mirrors the zoning pattern at `enrich-parcels.js:179-182` and needs no producer version at all. **This supersedes D1-A on the evidence and needs an operator re-ruling.**
  * **SFB3 HIGH — and it eliminates a cost I mispriced.** (a) CONFIRMED SAFE: `load-parcels.js:341-361` is NOT a standalone 486K UPDATE — the NULLing arms are CASE expressions inside the batched `ON CONFLICT DO UPDATE`, gated on geometry inequality. Adding a 4th arm is three lines. My "486K-row UPDATE churn once a year" was wrong in the harmless direction. (b) GAP: a massing-DATASET change needs a separate all-parcels UPDATE the plan never locates. Live cost: `parcels` is **3610 MB heap + 1645 MB TOAST + 306 MB indexes over 496,422 rows**; a full-column UPDATE rewrites every row under MVCC, and the **server session `statement_timeout` is 2min**, so the same statement from a migration is cancelled at 120 s. SFB2's predicate shape removes this UPDATE entirely.
  * **SFB4 HIGH — D4 does not close the staleness it targets.** Passes 4/5 key ONLY on their own NULL markers (`comp_count IS NULL` `:982`, `opt_config_confidence IS NULL` `:1093`) and are NOT fed by the temp-table cascade that links passes 1→2→3. So a massing-driven pass-3 rewrite leaves `comp_build_ratio_p50` and `comp_fsi_p50` pinned to the OLD footprint forever, and D4's permit-proximity predicate is orthogonal to that. Live blast radius: **444,483 of 496,422 parcels (89.5%) are already in the never-refreshes state.** Fix: passes 4/5 must UNION the pass-3 temp-table `EXISTS` the way 2/3 already do — D4 alone is insufficient.
  * **SFB5 MED — D4's spatial predicate has no index to use.** `parcels` carries a **geometry** GiST index only; `ST_DWithin(p.geom::geography, …)` cannot use it and seq-scans 3610 MB. The codebase already solved this at `enrich-parcels.js:196-200` with a `metres ÷ 78000` bbox pre-filter. Cheaper still — skip spatial entirely: `permit_parcels` (243,125 rows, indexed) and `coa_applications.zoning_dominant_parcel_id` resolve decision→parcel directly.
  * **SFB6 MED — the link_wsib watermark has no precedent and my number was wrong.** Zero `%attempt%` columns exist database-wide. The unmatchable set is **107,143 unlinked of 121,116 total** — my "~121K unmatchable" was the table total. Needs a partial index (`WHERE linked_entity_id IS NULL`) and the predicate added to all three tier CTEs, not just the count query.
  * **SFB7 LOW but factual — the watermark inventory is FIVE, not four, and is not one pattern.** I missed `zoning_base_source_dataset_version TIMESTAMPTZ` (mig 165). Three are TEXT, two TIMESTAMPTZ. **And none of them is indexed** — every enrich predicate seq-scans the 3610 MB heap, so B2's "0 rows changed ⇒ cheap" still pays a full scan without a partial index. Rule on it rather than inherit it.
  * **CONFIRMED CORRECT:** `records_meta` as version store is sound on size (largest live value 5,302 bytes vs jsonb's 255 MB ceiling) · **the "last completed run" read is indexed and cheap** — `idx_pipeline_runs_lookup (pipeline, started_at DESC)`, 0.026 ms; six new gates per run is not a real cost — **but the `completed_at DESC` variant is NOT covered (17.3 ms) and is semantically different, so consolidating into `source-version.js` is the moment to standardise on `started_at DESC`** · `pipeline_runs.pipeline` has no schema obstacle to new values · `pipeline_schedules.cadence` is plain TEXT, so the §11 cadence ruling holds · exactly one sources schedule row, `('sources','Weekly','0 13 * * 0')`, confirming the GT cron correction · the D1 ALTER is metadata-only · `parcels` is clean (496,422 live, 0 dead).
  * **SFB8 — sequencing:** migration 238 is still unapplied on cloud, so B2's would be **239 and the second unapplied migration in flight** — B2 needs an explicit ordering statement.

* **Ground-truth — 1 CRITICAL, 3 HIGH, 7 lesser. Cloud host printed. It falsifies my own B4 premise.**
  * **GTB2 HIGH — "14,526 vs 14,510, NOT converging" IS FALSE, and I amplified it into State Verification as evidence.** The two numbers are **the same set measured two ways on the same day**: `countStaleParcels` filters `geom IS NOT NULL AND ST_IsValid(geom)`, there are exactly **16 invalid-geom parcels, all inside the NULL-stamp set**, and 14,526 − 16 = 14,510. There were also **no weekly recomputes to converge** — `enrich_centreline` ran 07-07, 07-08, then 08-03/04 and nothing between, because the chain is `disabled_manually`. The real trend is 14,528 → 14,528 → **14,526 — DOWN by 2**, not up by 16.
    **And the cause is already diagnosed, in code and in the run meta.** `enrich-centreline.js:89` builds `parcel_ids_intersecting` from `parcel_segments`; the temp table is built FROM it and the UPDATE joins on it — **so a parcel intersecting ZERO centreline segments never enters the temp table and its stamp is never written. Permanently NULL by construction.** The 2026-08-03 run emitted `parcels_with_zero_centreline_intersections_count: 14526` exactly, and `enrich-permits.js:71-72` already calls it "the ~3% zero-intersection floor."
    **→ B4 is NOT a diagnosis task. It is a one-line design decision:** stamp zero-intersection parcels with the version (writing their `false/false/NULL/false` defaults) so they converge, OR keep them unstamped and exclude them from `countStaleParcels`. Rewrite the State Verification bullet; drop the "stuck" framing I inherited and amplified.
    Also: `IS DISTINCT FROM` and `IS NULL` agree only because `nonnull_stale = 0` today — after any centreline republish the former counts all 486,530. B4's convergence row must use `IS DISTINCT FROM` or the metric silently changes meaning.
  * **GTB5 HIGH — the GT3 constraint has a SECOND arm I never named.** `assertCentrelineEnriched` HALTs on recency (L24b) **and on coverage (L24c): stamped / valid-geom ≥ `centreline_propagation_coverage_min`, default 0.90.** Measured today **97.01% — 7 points of headroom.** Any mass-NULL event (a boundary redraw firing the `load-parcels` geometry fence, or a B4 remedy that UNstamps rather than stamps) can cross 0.90 and **hard-HALT the daily permits and coa chains.** This directly constrains which B4 remedy is safe.
  * **GTB3 HIGH — D3's tier-1 is near-useless for the two loaders it exists to fix, and the compute target is wrong.** CKAN polled TODAY: address-points `last_modified 2026-08-05T11:43`, property-boundaries `2026-08-04T03:59` — **both moved within 48h while the Aug-3 run recorded 0 ins / 0 upd for each.** So tier-1 reports "moved" on essentially every run and **tier-2 carries 100% of the weight — the ~185 MB + ~327 MB downloads still happen every run.** The report's "~20-25 min/wk" assumed the DOWNLOAD was avoided; only parse+upsert is. Realistic floor ≈ **30-40 min**. **But the hash tier itself is VINDICATED:** `load_centreline` produced an identical `source_dataset_version` on two runs 5h apart despite daily regeneration — content hash is stable where timestamps are not. Suggests an HTTP `Range`/`Content-Length` tier-1.5 before committing to a full download.
  * **GTB1 CRITICAL — the slug rename (4th independent finding), with a worse failure mode than the others described:** not a halt but a **silent permanent PASS**. The historical `sources:*` rows still exist with enrich (Aug 4) > parcels (Aug 3), so the recency check keeps reading a frozen pre-split baseline and never re-arms — "a green zombie". Regression-locked at `enrich-permits-centreline.logic.test.ts:83-84`. Also newly named: `enrich-parcels.js:55` `PRODUCER_NAME='sources:load_zoning'` implements **Spec 58's FROZEN §9 consumer protocol**.
  * **GTB4 MED-HIGH — 30+ stale doc lines, two correctness-relevant, and one is ANOTHER argument against the split:** `58_source_zoning_bylaw.md:11,196` assert `load_zoning` and `enrich_parcels` are **"in the same chain"** — the split makes that FALSE (steps 20 and 21 land either side of the boundary), removing a sequential-atomicity guarantee a documented decision rests on. Also `49_data_completeness_profiling.md:23,26,29` documents the `PIPELINE_CHAIN === 'sources'` branch. **The Spec 43 cadence contradiction is FOUR-way, not two:** Spec 43 ("quarterly", 3 places), Spec 30 (`:88` "Quarterly, 08:00 ET on the 1st of Jan/Apr/Jul/Oct"), `DataQualityDashboard.tsx` — which **contradicts itself** (`:115` 'Weekly' vs `:18-37` 'Quarterly') — versus Spec 115 + the live cron + the DB row, which agree on weekly. **Spec 115 is right.** Add Spec 30. Unlisted test locks that will fail: `admin.ui.test.tsx:600-619,673-693,962` and `quality.infra.test.ts:248` assert 'Quarterly'.
  * **GTB9 — `funnel.ts` fails OPEN on an unknown cadence:** `'Bi-weekly'` falls through to the 48h Daily default, so the sources chain would read `stale` two days after every run, silently. The red light must assert the fall-through; better, make the mapping exhaustive-or-throw. (Note this contradicts Integration's "drop funnel.ts from scope" on the CADENCE axis — Integration ruled only on the chain-slug axis. Both are right: keep funnel.ts for cadence, drop it for slugs.)
  * **GTB6/7/8/10/11 (MED-LOW):** my `:438-444` citation is wrong (3rd agent to catch it) — real HALT is `:301-320` · watermark inventory is 5 today, massing would be the **6th**, and the geometry fence covers 3 of them, so D1 does not "restore parity" · **B5 is too narrow AND not urgent** — massing's pinned id has NOT rotated since Aug 3, while `load-parcels.js:37` and `load-address-points.js:38` pin ids of the same class (nearly free to resolve in the `package_show` call D3 already makes) · the report's `sys_duration_ms` table systematically understates vs wall-clock (steps 1-20 = 57.2 min; the "85%" claim re-derives to 80.6%, directionally true) · zoning's `skipCheckDecision` compares `Date.parse()` numerically where the other three use string equality, and emits different reason strings that flow into observability rows.
  * **VERIFIED TRUE:** the cron IS `0 13 * * 0` · `chain-sources` IS `disabled_manually` (every other chain workflow is active) · heritage IS write-only (604s for 0 rows) · exactly two manifest pins · the geometry fence exists, is documented as "the LOAD-BEARING precondition", and is trivially extendable — **D1's premise holds** · GT3 exists and behaves as described · no massing watermark · cadence is plain TEXT · watchdog 204h · **the residuals record is accurate: `gh run view 31034937479` → status `waiting`, awaiting the production-db approval** · ceilings are job 210 / step 180 · **every CKAN dataset and pinned resource id still exists today; nothing has rotated.**

* **Reality-Check — CONDITIONAL PASS on architecture; 3 findings that must be resolved first. Cloud host + timestamp printed.**
  * **RC0 PROCESS — the wrong-DB defect bit a FOURTH time, inside the audit tooling itself.** `parcel-sanity-audit.js` and `parcel-field-dump.js` both fall back to a hardcoded `localhost:5432/buildo` when `DATABASE_URL` is unset. The agent caught it from the script's own log line and re-ran against cloud — **and the numbers differ materially: 10/32 checks tripped local vs 14/32 cloud; 1,346 vs 5,318 HIGH/MED violations.** Cloud is meaningfully worse than local, so any prior audit run without an explicit `DATABASE_URL` graded the wrong database. **ESCALATE the filed PG_*-fallback WF3 again and add these two scripts to its scope.**
  * **RC1 HIGH — the 2026-08-03 parcels anomaly, live, unexplained, two days before this review, on the exact mechanism D1 depends on.** `sources:parcels` recorded **`records_updated=485,567` of 495,459 (98%), `records_unchanged=0`** on an otherwise-stable table whose runs between 06-25 and 07-08 were all clean 0/0, with **no commit touching `load-parcels.js`'s geometry comparison** in that window (`git log --since=2026-07-28` empty). The write-guard is `geometry::jsonb IS DISTINCT FROM EXCLUDED.geometry::jsonb` — exactly the comparison class vulnerable to CKAN's daily-regenerated-CSV re-encoding noise that the design report flags as unverified (§9 Q2). It propagated: the same date shows full 485-486K rewrites in `enrich_ravines` AND `enrich_heritage`, both consuming that same fence. **If this is serialization noise rather than a real boundary redraw, the identical false-positive invalidation would defeat the incremental savings this entire plan exists to deliver, on an unpredictable cadence — and D1's watermark is modeled on the same pattern. B0 must byte-diff a sample parcel's stored geometry against a fresh CKAN download and root-cause this BEFORE B1/D1 ship.**
  * **RC2 HIGH — D4 describes a mechanism that does not exist in the code.** `enrichComparableBuilds` (`enrich-parcels.js:940-1037`) is subject-driven kNN (`ORDER BY c.geom <-> s.geom LIMIT 50`) with **no `ST_DWithin` / radius cutoff anywhere**. D4 is not a parameter to flip; it is new spatial-join code to write. **Plus a hard gap: `comp_count IS NULL` is 0 rows across all 440,267 eligible parcels today** — so D4's `∪ comp_count IS NULL` clause is a **no-op on day one** — while **88,524 parcels (20.1%) sit at `comp_count = 0`**, which that clause does NOT recapture. Today's forced `--full` resets them to NULL and re-tries weekly; under D4 as specced they go permanently un-retried until the quarterly pass. **D4 must fold `comp_count = 0` into its scope.** Radius sizing, measured: 100 m → 36,141 subjects (8.2%), 250 m → 115,341 (26.2%), 500 m → 235,927 (53.6%); trailing-14-day decisions = 338 permits + 426 CoA over 598 distinct parcels. **Neither tiny nor citywide — at 500 m more than half the city refreshes and the scoping buys little; at 100 m, 92% stay stale for the full window.**
  * **RC3 MED — a live cross-pass invariant break that conditional gating would freeze.** `opt_aor_gfa_gt_max_buildable_gfa` = **461/424,025**: pass 5's as-of-right GFA exceeds pass 2's max-build envelope, which should be impossible. Sample `1036` (RM corner, lot 371.49 m²): `max_buildable_footprint_sqm=22.81`, `max_build_width_m=**1.26 m**` — a physically degenerate sliver, almost certainly a setback-geometry bug in pass 2 — against `opt_aor_gfa_sqm=245.18`. **Pre-existing, but today's weekly `--full` gives it a chance to self-correct; under conditional gating it stops self-healing.** B2's red light must assert pass 5 never runs against a pass-2 output older than its own basis, and this invariant should move into the audit's GATED set once B2 ships. **Audit blind spot found by the eyeball: there is no BOUND check on `max_build_width_m`/`max_build_length_m` individually — only the derived footprint/GFA.**
  * **RC4 — B4's root cause CONFIRMED at 300/300 sample: `CENTRELINE_PROXIMITY_M = 20`.** Every stuck parcel has **zero centreline segments within 20 m** (`ST_DWithin(...::geography, 20)`, `enrich-centreline.js:39`). Structural exclusion by radius, not data quality (only 16 of 14,526 have invalid geometry). Fix is widening the radius for outliers or a nearest-segment fallback — **not a data cleanup**. *(Adjudication with Ground-truth: GT's arithmetic stands — 14,510 valid-geom + 16 invalid = 14,526, one set measured two ways, and my "+16 drift" was wrong. RC confirms the set is STABLE and structurally unable to converge. "Not converging" is true by construction; "growing" was false.)*
  * **RC5 — the stuck parcels are NOT globally unenriched:** 100% carry zoning/ravine/heritage stamps, 71.5% have `max_buildable_gfa_sqm`, 84.7% have `lot_size_sqm`; zone spread is proportional to the citywide mix. So the centreline gap is isolated, not a symptom of broader failure.
  * **RC6 — the quarterly ruling cannot be validated from data, and the safety net is untested:** **no `compute_parcel_cost_estimates` run has completed since 2026-07-08** — the chain has died before reaching it on every attempt since. RC recommends the quarterly cadence stand **conditional on B0 clearing the RC1 root cause**; if 08-03 turns out to be recurring noise, monthly (or extending content-hashing to the write-comparison layer) is the safer call.
  * **BASELINE THE PLAN MUST NOT DEGRADE (cloud):** 14/32 checks tripped, **0 FAIL-GATED**, 5,318 HIGH/MED violations over 437,305 residential parcels. **All hard zero-baseline invariants hold at 0** — `opt_aor_gfa_gt_opt_coa_gfa`, `new_build_cost_gt_coa_build_cost`, `footprint_gt_lot_x105`, `existing_floor_gt_lot_x105`, `heritage_basis_footprint_gt_lot`, `cost_fb_on_footprint_gt_lot`, `greenspace_out_of_range`. Watch-items B3's cost gate would make permanent rather than self-correcting: `nulllot_on_gfa_or_cost_bearing` 3,330 (0.79%), `stale_cost_fsi_without_gfa` 121, `newbuild_cost_per_sqm_out_of_band` 63.

> **PLAN LOCKED. Do you authorize this WF2 plan? (y/n)**
> §11 note: the cadence-string enumeration is fixed by updating all four consumers in B6's commit rather than minting a `_contracts.json` entry — the defect is that the enumeration exists four times, and a fifth registry would not reduce that.
