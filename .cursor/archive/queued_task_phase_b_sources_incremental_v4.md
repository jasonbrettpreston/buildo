# Active Task: Phase B — Sources Incremental Architecture (WF2)
**Status:** Planning — **v4**, rewritten 2026-08-06 after the ROUND-2 four-agent re-review found v3 not-ready (record: `.cursor/phase_b_round2_panel_2026-08-06.md`; round-1 record: `.cursor/phase_b_panel_record_2026-08-05.md`; superseded plan: `.cursor/queued_task_phase_b_sources_incremental_v3.md`). Round-2 headline: **every measured number in v3 reproduced exact on cloud** — the defects are design gaps, not facts.
**Domain Mode:** **Cross-Domain** (declared at task start; `domain-admin.md` + `scripts/CLAUDE.md` read before generation). Primary surface Backend/Pipeline; B6 touches `src/app/api/admin/pipelines/schedules/route.ts`, `src/app/api/admin/stats/route.ts` (**added — R2 fold 1**), `src/components/ScheduleEditModal.tsx`, `src/components/DataQualityDashboard.tsx`, `src/components/FreshnessTimeline.tsx`, `src/lib/admin/funnel.ts`, `src/tests/chain.logic.test.ts`.
**Workflow:** WF2 (Enhance), step list per `.claude/workflows.md:67-95`.
**Design authority:** `docs/reports/2026-08-04-sources-incremental-architecture.md`, corrected by both panels where drifted.
**Predecessor gate (DS-8, from Phase 1 v3):** this plan is re-verified against the post-Phase-1 `max_build_*` contract before implementation starts — Phase 1's D-C changes the emission contract (NULL envelopes + `ravine_constrained`/`coverage_only` basis) that B2's pass-4/5 scope and RC3's freeze-risk invariant read.
**Rollback Anchor:** `b4351297`. **Pre-Flight:** `node scripts/ai-env-check.mjs` before B0.

## IMMEDIATE OPERATOR ITEMS (independent of Phase B — restored per IN10; do not wait for B6)
1. **Fix the cloud `pipeline_schedules.deep_scrapes` row now** — still 'Weekdays (3x Daily)' / `0 15,18,21 * * 1-5` (updated 07-22) despite `2fa3b2e7`; seed edits provably don't reach cloud (ON CONFLICT DO NOTHING).
2. Migration 238's cloud apply + next-nightly proof (carried from the predecessor).

## Context
* **Goal:** unchanged — the chain does ~80% of its work to change 4 rows and dies at the 180-min ceiling; **no `compute_parcel_cost_estimates` run has completed since 2026-07-08** (verified again 08-06: last completed id 1486). Make every step do work only when its inputs actually changed — **and make the runs where inputs DID change survive** (R2-B1, the round-2 centerpiece).
* **Target Specs:** as v3 (`43` · `115` · `30` · `40` · `47` · `48` · `49` · `58` FROZEN §9 · `60` · `65` · `78` · per-source 54/55/56/59/61/62).

### The GT3 constraint (unchanged from v3, line range corrected)
`assertCentrelineEnriched` at `enrich-permits.js:301-332` (called `:613`): L24b recency + L24c coverage (97.01% vs 0.90 floor — 7 points of headroom). It reaches only 2 of the 6 new skip paths, both weakly; `link_parcel_addresses`, `compute_parcel_cost_estimates`, `link_wsib` have NO downstream halt — their protection is B1/B3's red lights, **B3's skip-staleness assertion (new, R2)**, and B7's proving run.
**DS4 condition, stated explicitly (restored):** the L24b mitigation — "a skip emits a completed row carrying versions forward, so recency doesn't halt the nightly chains" — holds ONLY IF `enrich_centreline`'s own skip path emits that completed row. B1's red light pins this per-step; it is a condition, not an assumption.

## Operator rulings
* **D1′ (revised per R2):** `parcels.massing_enriched_at TIMESTAMPTZ` vs `parcel_buildings.linked_at` via EXISTS — with four corrections: **(a)** `linked_at` is **NOT indexed** (round-2 verified) — B2's index ruling covers it alongside the watermark partial-index question; **(b)** the gate predicate is `massing_enriched_at IS NULL OR EXISTS(… pb.linked_at > p.massing_enriched_at)` — the NULL arm is mandatory (`> NULL` is never true); **(c)** the migration backfill is per-parcel **`MAX(linked_at)`** (heterogeneous link rows; an arbitrary-row join misfires day one); **(d)** two stated limitations: a **heights-only** massing refresh (footprint count unchanged → neither gate nor `linked_at` moves) leaves passes 2-3 stale until the quarterly — accepted, documented; **zero-link ghosts** (link set → 0) never refire — add a WARN row counting stamped parcels with zero `parcel_buildings` rows.
* **D2′ (extended per R2-B1 — the CRITICAL):** one `sources` chain, quarterly = same-chain variant with raised `timeout-minutes` + env-shaped `--force-full` channel (manifest can't distinguish variants under one id; `run-chain.js:411`; scripts read `pipeline.isFullMode()` — the env→argv plumbing is named B2/B6 work). **NEW — escalation routing:** the bi-weekly run must SURVIVE the runs where inputs actually changed (zoning release ≈ citywide cascade; massing vintage → full relink → D1′ fires for ~440K). Mechanism: **each gated step computes its scope count BEFORE working; if the projected scope exceeds its stated escalation threshold, the step (and the chain) self-stops CLEANLY at the next step boundary with a distinct `deferred_to_full` outcome, and the operator (or the next quarterly window) runs the 360-min force-full variant.** A soft time-budget self-stop (deep_scrapes `d6eb9f31` precedent) backstops mid-step overruns. A platform kill is never the stop mechanism (`lessons.md:99`). `CHAIN_DURATION_BUDGET_MINUTES` set (B6) so the duration tripwire lives. Quarterly cron mechanics: DOM/DOW are OR'd — in-job date check; job ceiling (`chain-sources.yml:20`) moves with the step ceiling; a quarterly Sunday on an off-parity week is not skipped; **the first `--force-full` is dispatch-supervised, never the cron's maiden run** (a 360-min DNF rolls back the pass-1-4 transaction atomically). Contingency unchanged: if a measured quarterly exceeds a single job, the split returns.
* **D3 (unchanged + CF12 restored):** two-tier metadata→content-hash; tier-1 will report "moved" nearly every run (re-confirmed 08-06: both CKAN timestamps moved again while 0 rows changed); realistic floor ≈ 30-40 min — **a projection until B7 measures it**; HTTP Range/Content-Length tier-1.5; stated miss-window assumption. **Tier-2 hashing is STREAMED (§9.5)** — a ~327 MB download must hash without buffering into memory.
* **D4′ (revised per R2):** scope = `comp_count IS NULL` **OR (`comp_count = 0` AND a new decision landed within the candidate join since the last completed run — NOT unconditional: 88,524 zero-comp parcels are terminal markers (`:1057-1060`) and unconditional re-inclusion is a new permanent re-kNN churn set; the quarterly retries them all)** OR parcel had a decision land since the last completed run (**keyed on `permit_parcels.linked_at` / `coa_applications.parcel_linked_at` vs the last completed run's `started_at` — NEVER `decision_date`,** a late-backfilling business DATE) OR parcel in the pass-3 refresh scope — which **must be MATERIALIZED** (persistent scratch table or recorded id set written inside the transaction): pass 5 runs post-commit on a separate connection (`:1405-1408`) and the ON-COMMIT-DROP temp tables are structurally invisible to it (R2-B4). A skipped pass 3 writes an EMPTY materialized scope — pass 4/5 then run on their own predicates only, stated, not silently shrunk.
* **Prior rulings unchanged:** full architecture · bi-weekly cadence · quarterly safety pass (conditional on B0) · no sources rerun before this lands · re-enable in GitHub as well as cron text.

## State Verification (all round-2 re-verified EXACT on cloud 2026-08-06 unless noted)
As v3, with corrections: watermark columns five, none indexed, fence NULLs three · stuck set 14,526 = one set, trend shrinking (14,528→14,528→14,526), 100% otherwise-stamped, 71.5% with GFA, root cause `CENTRELINE_PROXIMITY_M = 20` · manifest two pins · link_wsib 107,143/121,116, no %attempt%, index inverted · cadence contradiction four-way, Spec 115 right · sanity baseline 14/32, 0 FAIL-GATED, 5,318 HIGH/MED **([RES scope])** · records_meta 5,302 B max; `started_at DESC` covered, `completed_at DESC` not — **and NO code reads `started_at DESC` today** (`massing-full-gate.js:45` and `enrich-permits.js:305-308` both use completed_at): B1's standardization explicitly INCLUDES those two consumer-side readers or states their exclusion · CKAN ids valid, massing not rotated · **freeze-risk counts are RESIDENTIAL-scoped: 461 / 3,330 / 121 (all-parcels: 658 / 5,953 / 123)** · Spec 30 quarterly at `:88,:101` (not `:222`); `:101`'s lock adds `chain.logic.test.ts` to B6 · massing-full-gate is fail-open via `prevCode === undefined` — **deliberate and documented (`:16-18`), a design stance not a latent bug; still not the template** · chain deaths 2073/2133/2158 confirmed live.

## B0 — HARD GATE (unchanged + one addition)
* Root-cause the 2026-08-03 parcels anomaly (rows re-verified exact: 485,567 of 495,459 updated, 0 unchanged; propagated to 485-486K ravine/heritage rewrites). Byte-diff stored vs fresh CKAN geometry. Quarterly-cadence ruling conditional on this. **B0 cuts both ways (R2-B5): once false-positive invalidation stops, GENUINE geometry changes must still invalidate — B2's fence extension is the other half.**
* Spec Review + per-step live counts before any change.

## Spec Updates (each edit rides ITS OWN step's commit)
As v3's table, plus: `30_pipeline_architecture.md` rows corrected to `:88,:101` · B6 file list gains `stats/route.ts` + `chain.logic.test.ts` · Spec 48 §3.7 rides B1-B4 · **B6 also documents the escalation/deferral outcome (`deferred_to_full`) in Spec 43 + 48**.

## Standards Compliance
* **Database Impact: YES.** B2: `parcels.massing_enriched_at` (nullable ALTER) + **backfill via per-parcel `MAX(linked_at)`** batched or via pipeline pool (`statement_timeout=0`, `pipeline.js:43-70`) + index ruling covering BOTH the watermark partial index AND `parcel_buildings.linked_at`. UP+DOWN, Rule 6, `npm run migrate`, `db:generate`, `typecheck`, `BUILDO_TEST_DB=1 npm run test:db`, **SQLFluff (restored)**. factories.ts N/A (watermarks raw-SQL-only by precedent). Migration ordering: numbered at implementation time behind whatever Phase 1 lands (v3's "239" assumed Phase B ran first; Phase 1 now owns 239) — state the order in the migration header; code reading the column must not deploy before it applies.
* **Cross-Domain (B6):** as v3 (try-catch, logError, invalid-cadence→400 + 404/500, desktop-first md:, §10.2 funnel related-tests) **with the test-impact sentence corrected (R2 Integration F2): `admin.ui.test.tsx:595-625,668-699` are TEST-LOCAL copies that cannot fail from component changes — B6 EDITS them deliberately; `:956-965` + `quality.infra.test.ts:242-250` fail only if 'Quarterly' is removed from source. The tautological-local hazard is itself evidence for the parity test.** Scenario-A handoff/JSON-contract note (CF9, restored): B6's API/UI changes ship with the schedules PUT contract restated in the plan of record.
* **Cadence enumeration:** **6 code sites** — the five from v3 PLUS `stats/route.ts:338` (`chain_sources: 192`, a cadence-bearing freshness window that goes falsely stale every off-week under bi-weekly) — plus the DB row and `DataQualityDashboard.tsx:18-37` per-source labels (explicitly assigned, not implicit). Parity test asserts all of them; `funnel.ts` exhaustive-or-throw.
* **RC0 deferral BROADENED (round-2 data F2):** the analysis-script defect is two-headed — localhost fallback (4th bite; escalate) AND the CLI pool dying on cloud's 2-min statement_timeout (`parcel-sanity-audit.js:30-35` bare pg.Pool; only `runSanity()` via the pipeline pool works). Both filed in one review_followups entry; **B7's Reality-Check instrument uses the pipeline-pool path until fixed.**
* **Idempotency:** every gate a read-only decision; loaders stay IS DISTINCT FROM; watermark writes guarded; **watermark ordering rule (R2-B2): the stamp is written in the SAME transaction as the work it stamps, never before.**
* SPEC LINK headers; `npm run system-map`; Spec 05 §5 six-field footers.

## Execution Plan — WF2 shape, one commit per step
- [ ] **B0 — State Verification + Spec Review + THE HARD GATE.** No B1/B2 code until the 08-03 anomaly is root-caused (both directions — see B0 section).
- [ ] **B1 — `scripts/lib/source-version.js` + tier gates.** *Contract Definition* as v3. *Red (v3's list plus):* the four divergent branches survive · fail-safe LOAD on CKAN-unreachable / no-prior-meta / malformed-meta · **stream-hash (no full-buffer)** · three distinguishable audit outcomes · a skip emits a completed row (per-step — this IS the DS4 condition) · **`started_at DESC` standardization scope names the two consumer-side completed_at readers (in or out, stated)**.
- [ ] **B2 — enrich-parcels + D1′ + D4′ + the fence extension.** Migration (column + MAX-backfill + both index rulings). Drop ONLY the `enrich_parcels` pin; KEEP `link_massing`'s. **Extend the DEC-FENCE2 geometry fence to also NULL `zoning_enriched_at` + `massing_enriched_at` (R2-B5 — closes DS3's moved-parcel gap; conditional on B0 clearing).** Materialize the pass-3 scope. **Escalation thresholds stated per pass** (D2′ routing). *Red:* massing gate fires on `IS NULL OR linked_at > watermark` and not otherwise · zero-link-ghost WARN row · pass-4/5 scope includes the bounded comp_count=0 rule + the MATERIALIZED cascade (a skipped pass 3 → empty scope, stated) · decision-scope keys on linked_at columns, a late-backfilled decision_date row is still caught · manifest-pin-absence lock for enrich_parcels only · pass 5 never runs against a pass-2 output older than its basis (RC3) · **scope-count-then-defer: a simulated citywide invalidation defers cleanly with `deferred_to_full`, not a 180-min death** · geometry-change fixture invalidates zoning+massing stamps.
- [ ] **B3 — heritage skip · link_parcel_addresses · compute_parcel_cost_estimates · link_wsib.** *Red (rewritten per R2-B2):* gates key off a watermark where one exists; where none exists (addresses/cost/wsib) the run ledger IS the watermark and the gate **scans every upstream attempt of ANY status since ITS OWN last completed run — skip only if all completed with 0 changes** (fixtures: loader-died-mid-upsert; enrich-died-between-commit-and-summary) · `link_wsib` partial index (`WHERE linked_entity_id IS NULL`) + predicate in all three tier CTEs + stated invalidation semantics · **skip-staleness assertion: per-step "age since last non-skip execution" ≤ 2× expected real-work cadence, WARN beyond (healthy-skip vs wedged-gate — R2 Adversarial 8; precedent `assert_staleness`)**.
- [ ] **B4 — the zero-intersection floor.** As v3 (design decision on the 20 m radius; L24c 97.01%-vs-0.90 constraint; IS-DISTINCT-FROM convergence row; WARN-if-≥-previous with the missing-previous fallback; re-baseline after B2/B3; report the coverage ratio).
- [ ] **B5 — runtime CKAN resource resolution.** As v3 (all three pinned loaders; sort by last_modified; Shapefile glob; no year parsing).
- [ ] **B6 — cadence + escalation surface (CROSS-DOMAIN).** **Epoch-day parity `floor(epochDays/14) % 2` (NOT ISO week — week 53 breaks winter 2026/27), phase ANCHORED (the re-enable commit states which dates are on-weeks; operator note says whether the first Sunday runs)**; `workflow_dispatch` bypasses the parity skip, keeps verdict gating; the D2′ quarterly variant + env-shaped `--force-full` plumbing; re-enable in GitHub; the **6** cadence sites + per-source dashboard labels + DB row + parity test; watchdog 204h → bi-weekly + slack; **`stats/route.ts:338` 192h → cadence-derived**; `funnel.ts` exhaustive-or-throw; `CHAIN_DURATION_BUDGET_MINUTES` set; `chain.logic.test.ts` updated; the tautological UI-test locals edited. *Red:* parity guard gates the VERDICT step too (off-week must not read run 2158's failed row) · **force-full red test (DS5, restored): a dispatch with the force channel set reaches `pipeline.isFullMode() === true` in enrich_parcels** · a GitHub-disable rollback step · **week-53/year-boundary parity fixture (Dec 2026 → Jan 2027 dates)**.
- [ ] **B7 — OUTPUT panel (full roster) + proving runs.** As v3 (full roster + Round-2 adjudication; RC uses the pipeline-pool path per the broadened RC0). Proving: one bi-weekly-shape run green **including steps 21-27**, one quarterly-shape run genuinely exercising `--force-full` (dispatch-supervised), **and one simulated-escalation run proving the clean `deferred_to_full` stop (R2-B1)**. The 30-40-min floor claim is MEASURED here, retiring its projection label.
- [ ] **B8 — Self-Checklist + Green Light + close-out.** Sibling sweep: other write-only watermarks · other hardcoded CKAN ids · other never-converging dirty sets · other fail-OPEN gates · **other stamp-before-work orderings · other tautological test-local copies**. **RC3 tail (restored): move `opt_aor_gfa ≤ max_buildable_gfa` into the audit's GATED set once B2 ships and Phase 1's re-run has zeroed it.** Green Light: `typecheck && lint && test && test:py && test:db`. Lessons, memory, system-map, six-field footers.

## Known risks
* RC1 (the 08-03 anomaly) is still the plan-killer if unresolved — B0 gates, both directions.
* **The escalated-run path (R2-B1) is now designed but unproven until B7's simulated-escalation run** — it is the highest-consequence residual.
* Three of six skip paths still have no downstream halt — protection is red lights + the skip-staleness assertion + B7.
* Conditional gating freezes pre-existing divergences (461/3,330/121 residential-scope) until the quarterly or Phase 1's re-run heals them — RC3's gate promotion (B8) is the backstop.
* D1′'s two stated limitations (heights-only refresh; zero-link ghosts) are accepted staleness windows bounded by the quarterly.

## Panel disposition ledger — round 2 (round-1 ledger unchanged in v3 §; full text `.cursor/phase_b_round2_panel_2026-08-06.md`)
**FOLDED:** R2-B1 (D2′ escalation routing + B2 thresholds + B7 proving run) · R2-B2 (B3 red rewritten; watermark-ordering rule) · R2-B3 (epoch-day parity, anchored, dispatch semantics, week-53 fixture) · R2-B4 (materialized pass-3 scope) · R2-B5 (fence extension; closes DS3) · IntF1 (linked_at not indexed → index ruling) · IntF2 (UI-test sentence corrected; locals edited) · IntF3-F6 (wording/line/path corrections) · Adv6 (D1′ four corrections + WARN row) · Adv7 (linked_at timestamps; reader-scope statement) · Adv8 (skip-staleness assertion) · Adv9 (bounded comp_count=0) · Adv10 (quarterly mechanics, five items) · Fold1 (stats-route 192h → B6, 6th site) · Fold2 (DS4 stated) · Fold4 (stream-hash + SQLFluff) · Fold5 (CF9 Scenario-A note) · Fold6 (IN10 immediate operator item) · Fold7 (DS5 force-full red) · Fold11 (RC3 tail → B8) · DataF2 (RC0 broadened) · DataF3/F5 (scope labels; projection labels).
**NOT ADOPTED (with reason):** none — every round-2 finding is folded or restored.

> **PLAN LOCKED. Do you authorize this WF2 plan? (y/n)**
> §11 note: two non-obvious round-2 choices — escalation is handled by **scope-count-then-defer** (a clean `deferred_to_full` stop routed to the supervised force-full variant) rather than raising the bi-weekly ceiling, preserving the small steady-state budget as the tripwire; and the parity guard moved off ISO weeks to an **anchored epoch-day bucket** because ISO week 53 provably breaks the first winter after ship.
