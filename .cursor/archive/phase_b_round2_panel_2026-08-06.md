# Round-2 PLAN panel record — Phase B Sources Incremental v3 (2026-08-06)

Four-agent re-review of `.cursor/queued_task_phase_b_sources_incremental_v3.md`, operator-ordered.
Roster: **Integration** (code grounding, live tree + cloud DB) · **Data Grounding** (cloud DB + CKAN + GitHub, all live) · **Fold Auditor** (v3 vs `.cursor/phase_b_panel_record_2026-08-05.md`) · **Fresh Adversarial** (blinded to the round-1 record).

## Verdicts
| Agent | Verdict |
|---|---|
| Integration | SOUND-WITH-CORRECTIONS |
| Data Grounding | SOUND-WITH-CORRECTIONS (**every load-bearing number reproduced exact on cloud** — the rewrite transcribed nothing wrongly) |
| Fold Auditor | FOLD FAITHFUL WITH GAPS (all 9 D2′ collapse claims verified genuinely moot) |
| Fresh Adversarial | **NOT-READY** |

## Blocking findings (drive the v4 rewrite)

**R2-B1 (CRITICAL, Adversarial 1): the architecture is sized for runs where nothing changed.** A zoning release fires pass-1 citywide (`enrich-parcels.js:179-182` true for ~every parcel) cascading to 2-5 + cost; an annual massing vintage → FULL relink rewrites every link with fresh `linked_at` (`link-massing.js:246-256,145-160`) → D1′ fires for ~440K parcels. Either lands an ordinary BI-WEEKLY run on the unchanged 180-min ceiling (`chain-sources.yml:72`; design report §8 worst case 4.5-5h) — the founding incident (steps 21-27 silently skipped) recurs by construction, with the weekly `--full` heal-path REMOVED. `CHAIN_DURATION_BUDGET_MINUTES` only WARNs; a platform kill is not a stop mechanism (`tasks/lessons.md:99`; SIGTERM handler has failed to terminalize twice). Need: escalation detection → clean self-defer to the 360-min force-full variant, or a soft time-budget self-stop (deep_scrapes `d6eb9f31` precedent).

**R2-B2 (HIGH, Adversarial 2): the two B3 red criteria compose incorrectly — partial commits still strand.** (a) loader killed mid-upsert: rows exist that no completed run reports; next completed run says 0 changes → naive gate skips them FOREVER. (b) `enrich_parcels` commits data+stamps in one txn BEFORE emitting its summary (`:1389-1403`) — killed between commit and summary, next run finds ~0 stale, completes 0-change, cost gate sees clean upstream: 400K changed parcels never priced, no red light. Correct semantics, to be pinned as a red criterion: **scan every upstream attempt of ANY status since MY OWN last completed run; skip only if all completed with 0 changes.** Also: `link_parcel_addresses`/`compute_parcel_cost_estimates` have NO row watermark (`parcels` has no updated_at; bridge is ON CONFLICT DO NOTHING `link-parcel-addresses.js:110-117`) — the run ledger IS the watermark; state it.

**R2-B3 (HIGH, Adversarial 3): ISO-week parity breaks winter 2026/27.** ISO 2026 has 53 weeks: 12-27=W52(even), 01-03=W53(odd), 01-10=W1(odd) — two consecutive same-parity Sundays → 3-week gap (false-red or desensitized watchdog) or back-to-back fulls. Use **epoch-day bucket `floor(epochDays/14) % 2`** (DST-free, week-53-free); ANCHOR the phase (state which bucket is the on-week at re-enable, so the operator knows whether the first Sunday runs); define `workflow_dispatch` semantics (proving runs/quarterly/ad-hoc must bypass the parity skip but keep the verdict gating).

**R2-B4 (HIGH, Adversarial 4): D4′'s pass-3-temp-table clause is unimplementable for pass 5.** Pass 5 runs post-commit on a separate connection (`enrich-parcels.js:1405-1408`); the temp tables are ON COMMIT DROP, session-local (`:1393-1399`). The scope silently evaporates for exactly the pass (opt-config, 89.5% never-refreshing) D4′ exists to fix. Materialize it (persistent scratch table / recorded id set / marker column) and say so.

**R2-B5 (HIGH, Adversarial 5): geometry-change invalidation reaches NO enrich pass once `--full` is dropped.** The DEC-FENCE2 fence NULLs only ravine/heritage/centreline stamps (`load-parcels.js:353-361`); a redrawn parcel keeps `zoning_enriched_at` (pass 1 keys on zoning version), keeps `lot_size_confidence` (pass 2 skips), is invisible to link_massing incremental (never-linked-only, `link-massing.js:238-240` — the documented moved-parcel gap = DS3, which v3 silently dropped) and to D4′ (decision-keyed). Yet the loader rewrites lot_size/frontage/depth/geom — max-build's direct inputs. Extend the fence: NULL `zoning_enriched_at` + `massing_enriched_at` on geometry change (B0's anomaly fix is prerequisite in BOTH directions: false positives must stop, real changes must still invalidate). This closes DS3 too.

## Corrective findings
- **Integration F1:** `parcel_buildings.linked_at` is **NOT indexed** (verified cloud pg_indexes — only parcel_id/building_id/unique-pair/is_primary-partial). D1′'s "is indexed" premise false; B2's index ruling must cover `linked_at` explicitly.
- **Integration F2:** "the UI tests will fail" is backwards — `admin.ui.test.tsx:595-625,668-699` assert TEST-LOCAL copies (can't fail from component changes); `:956-965` + `quality.infra.test.ts:242-250` are `toContain('Quarterly')` on source (fail only if the string is REMOVED). Real hazard = tautological tests silently passing; B6 must EDIT the local copies + rely on the parity test.
- **Integration F3/F4/F5/F6:** massing-full-gate parenthetical inverted (fail-open arm is `prevCode === undefined`; documented as deliberate `:16-18` — a design stance, not a latent bug); `assertCentrelineEnriched` spans `:301-332`; Spec 30 quarterly at `:88,:101` only (`:222` is chain-placement; `:101`'s lock means B6 also touches `src/tests/chain.logic.test.ts`); path is `scripts/lib/massing-full-gate.js`.
- **Adversarial 6 (D1′ holes):** (a) `linked_at` only advances when link CLASSIFICATION changes (`link-massing.js:147-156`) — a heights-only massing refresh (IS DISTINCT FROM `max_height_m`, `load-massing.js:295`) bumps neither the gate (count/code_version only) nor `linked_at` → passes 2-3 stale until quarterly: STATE the limitation. (b) `linked_at > NULL` never true — the red criterion needs the `massing_enriched_at IS NULL OR …` arm, and the backfill must be per-parcel `MAX(linked_at)` (heterogeneous rows). (c) zero-link ghosts (link set drops to 0) → EXISTS false forever → add a WARN row counting stamped parcels with zero pb rows.
- **Adversarial 7:** D4′'s timestamp = **`permit_parcels.linked_at` / `coa_applications.parcel_linked_at` vs last completed run `started_at`** — NOT `decision_date` (a business DATE that backfills late; non-monotonic vs run boundary). Note: NO code today reads `started_at DESC` (`massing-full-gate.js:45` uses completed_at; `enrich-permits.js:305-308` max(completed_at)) — B1's standardization must state whether the two consumer-side readers are in scope.
- **Adversarial 8:** after skips are steady-state, nothing distinguishes healthy-skip from wedged-gate (watchdog is chain-level; a constant-true gate bug reproduces the founding month-long silent skip, all green). Add per-step "age since last non-skip execution" assertion (≤2× expected real-work cadence) or skip-streak WARN — precedent: `assert_staleness`, `manifest.json:116`.
- **Adversarial 9:** `comp_count = 0` re-inclusion creates a NEW permanent churn set — 88,524 parcels re-kNN'd every run forever (zero-fill is a terminal marker, `:1057-1060`). Bound it: retry only when a new decision landed within the candidate join, or on the quarterly.
- **Adversarial 10:** quarterly mechanics — cron DOM/DOW are OR'd (needs an in-job date check); `--force-full` must be env-shaped (manifest can't distinguish variants under one chain id; `run-chain.js:411` injects per-chain args; scripts read argv `pipeline.isFullMode()`) — net-new plumbing to name; job ceiling `chain-sources.yml:20` moves with the step ceiling; quarterly-Sunday-on-off-week must not be parity-skipped; first `--force-full` is dispatch-supervised, never the cron's maiden run (a 360-min DNF rolls back the pass-1-4 txn atomically).
- **Fold 1 (HIGH):** OB2 over-collapsed — `src/app/api/admin/stats/route.ts:338` `chain_sources: 192` is a cadence-bearing freshness window (verified live), NOT an identity literal; missing from B6 + the cadence enumeration → falsely-stale admin chip every off-week.
- **Fold 2/3:** DS4 (state the skip-recency condition explicitly — mitigation holds only if enrich_centreline's own skip emits a completed row) and DS3 (moved-parcel closure) were silently dropped — DS4 restored as stated text; DS3 closed by R2-B5's fence extension.
- **Fold 4/5/6/7:** CF12's §9.5 **stream-hash** (~327 MB downloads must hash without buffering) + SQLFluff dropped; CF9's Scenario-A handoff/JSON-contract note missing; IN10's "actionable NOW" cloud `deep_scrapes` row demoted — restore as an immediate operator item; DS5's force-full RED test absent from B6's red list.
- **Fold 9/10/11:** "three core premises" undercounts (record has ≥4 false-premise verdicts — OB1, IN4, GTB2, SFB2); 30-45 vs 30-40 wobble; `DataQualityDashboard.tsx:18-37` per-source labels named in State Verification but unassigned in the site list; RC3's "move invariant to GATED set post-B2" tail has no home.
- **Data F2 (MED):** the plan's own Reality-Check instrument dies on cloud — `parcel-sanity-audit.js` CLI pool (`:30-35`, bare pg.Pool) hits the 2-min statement_timeout; only `runSanity()` via the pipeline pool works. Broaden the RC0 deferral to cover BOTH defects (localhost fallback + CLI-pool timeout) or B7's RC instrument is blocked.
- **Data F3/F4/F5 [cloud]:** freeze-risk counts are RESIDENTIAL-scoped (461/3,330/121; all-parcels 658/5,953/123 — label them); CKAN last_modified moved again 08-06 while 0 rows changed (reinforces D3); "30-45 min steady state" is a PROJECTION (label it); "80% to change 4 rows" fair (57.2 min for steps 1-20, net 4 changed rows + 158 unconditional neighbourhood rewrites); massing not rotated (B5 preventive stands).

## Verified-exact (no action)
No-cost-run-since-07-08 (last completed 1486, 07-08T16:18Z) · L24c 472,004/486,530 = 97.014% vs 0.90 · stuck set 14,510+16=14,526, trend 14,528→14,528→14,526 (shrinking), 100% zoning/ravine/heritage-stamped, 71.47% with GFA · D4′ scope (comp_count NULL=0 of 440,267; =0 → 88,524 = 20.11%; 89.54% never-refresh) · link_wsib 107,143/121,116, no %attempt% column, index covers IS NOT NULL · 08-03 anomaly rows (2137: 9,892+485,567=495,459 ⇒ 0 unchanged; ravines 2152 485,567 upd 4,987 s; heritage 2153 485,551; evening heritage 604 s/0 rows) · sanity baseline 14/32, 0 FAIL-GATED, 5,318 HIGH/MED, 437,305 RES · records_meta max 5,302 B; only pkey + `idx_pipeline_runs_lookup(pipeline, started_at DESC)` · cloud `deep_scrapes` row still 'Weekdays (3x Daily)' (updated 07-22) · `chain-sources disabled_manually` (GitHub) while the DB row is Weekly-enabled · manifest exactly two `--full` pins · `sources:enrich_parcels` latest run failed (08-04) — "never observed to finish" stands.
