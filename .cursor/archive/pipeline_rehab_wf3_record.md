# Active Task: Pipeline Rehab — every scheduled cloud workflow concludes GREEN
**Status:** Implementation — AUTHORIZED by operator 2026-08-03 ("proceed"; full panel on OUTPUT; CLI pair cancelled). P0 DONE 2026-08-03 (ids 1756/2045/2097 → 'failed', read-back verified).
**Prior status:** Planning — v3 (2026-08-03). v2 = 6-reviewer PLAN panel folded (Observability, Schema-Fidelity, Regression Guardian, Code Reviewer, Integration, Reality-Check). v3 adds: Ground-truth doc-surface inventory folded (9 missing items + 2 hazards) + WF3 protocol structure enforced (fail-first per `.claude/workflows.md` WF3). Adversarial CLIs outstanding — fold on arrival or cut at implementation start. Adjudication log at bottom.
**Domain Mode:** Backend/Pipeline (`scripts/`, `.github/workflows/`)
**Workflow:** WF3 (Fix) — bundled by operator direction 2026-08-03; per-phase commits; **each phase runs Reproduction → Red Light → Fix internally** (protocol steps below).

## Context
* **Goal:** All five scheduled cloud workflows (chain-sources, chain-coa-permits, chain-entities, chain-deep-scrapes, pipeline-watchdog) conclude GREEN on their schedules. chain-entities is green; the other four are red for six panel-corrected root causes (diagnosis below).
* **Target Spec:** `docs/specs/00-architecture/115_scheduling.md` + `docs/specs/01-pipeline/44_chain_deep_scrapes.md` + `docs/specs/01-pipeline/48_pipeline_observability.md` (§4.5/§4.6/§4.9 govern P4) + Spec 41 (entity-tracing gate) + Spec 49 (coverage matrix) + Spec 112 §6 (backup fallback).
* **Key Files:** `scripts/check-chain-verdict.js`, `scripts/load-address-points.js`, `scripts/load-parcels.js`, `scripts/load-neighbourhoods.js`, `scripts/load-massing.js`, `scripts/quality/assert-global-coverage.js`, `scripts/quality/assert-entity-tracing.js` (paths fold-verified), `scripts/aic-orchestrator.py`, `scripts/aic-scraper-nodriver.py`, `scripts/local-cron.js`, `.github/workflows/chain-coa-permits.yml`, `.github/workflows/pipeline-watchdog.yml`, `.github/workflows/chain-deep-scrapes.yml`, `src/tests/check-chain-verdict.logic.test.ts`, `src/tests/chain.logic.test.ts`, `scripts/tests/test_scraper_outcomes.py`.

## Diagnosis (triage panel 2026-08-03, panel-corrected — six root causes)
1. **chain-sources CODE BUG (recurring):** gitignored `data/` absent on fresh checkout; **FOUR** loaders write into it without mkdir: `load-address-points.js:45`, `load-parcels.js:183`, `load-neighbourhoods.js:54` (two call sites :640/:652), `load-massing.js:87` (zip download precedes its only mkdir :142, which creates extractDir). Sweep of all 27 chains.sources scripts confirms list complete. centreline/ravines/heritage use os.tmpdir mkdtemp — safe.
2. **chain-permits TIMEOUT (NEW since Aug 2):** duration crept to 78+ min (78.8/78.4/78.2 on 07-29/31/08-01) vs 90-min step ceiling; 08-02 + 08-03 killed mid `compute_trade_forecasts`. Kills nightly `backup_db` (final step) → **backups 50.7h stale vs 25h SLA (live)**. ⚠ No completed run >90 min exists — 120 is an estimate, not validated headroom.
3. **Verdict-check FALSE-GREEN (CONFIRMED LIVE):** killed chains leave `status='running'` rows — **THREE orphaned rows: ids 1756 (07-30), 2045 (08-02), 2097 (08-03)**. Not in `FAIL_STATUSES` → the 08-03 verdict check PASSED against a dead chain. `status` is unconstrained TEXT (mig 033, no CHECK); `cancelled` is ALSO ok-classified today (second hole). **No auto-expiry exists** — the 12h TTL in `chain-concurrency.js:35-38` is a read-side filter only. Real fence (run-chain.js:28-79 SIGINT/SIGTERM handler) did not land its UPDATE on the GH kills — likely SIGKILL beat the async UPDATE; filed as latent.
4. **coa-permits baseline reds:** 07-31/08-01 purely the 2 accepted gates — coa `assert_global_coverage` estimated_cost **61.1–61.2%, 8/8 FAIL** vs ≥90; permits `assert_entity_tracing` opportunity_score **79.9–80.0, 8/8 FAIL, never passed** (persistently near-threshold, NOT flapping). Wiring verified: run-chain.js exits 0 on gate-only failures (:679); nightly red comes SOLELY from the verdict-check steps (live-DB confirmed).
5. **pipeline-watchdog (correct alarm, mute):** `>> "$GITHUB_OUTPUT"` at yml:163/178 swallows the `::error` naming stale chains (script self-writes outputs via `writeOutput()` :112-119; line-178 outputs consumed by nothing; filed review_followups:~2833). **Backup fallback gated on `chains_fresh=='true'` (:168-171) is LIVE-SUPPRESSING backup protection during this exact outage.**
6. **deep-scrapes DATA-GATE flake:** n=3 probes vs `anomalous_miss_rate <20%` — binomial at assumed 10% baseline: false-FAIL 27.1% at n=3, 13.3% at n=20, **7.3% at n=30**. The 10% baseline is a pre-ledger ASSUMPTION (ledger has only today's 3 rows) — measure from P7. Gate emitted in TWO files: `aic-orchestrator.py:534-535/636` + `aic-scraper-nodriver.py:3293`. Gate is 4 days old (`de2e4b75`, fail-safe-never-silently-excused; sibling `de3ff6dd` fixed wipeout false-PASS) — softening must not reopen the wipeout class. 07-31 scheduled reds = pre-merge CDP runs (explained; `schedule:` commented on main). **WF2 outcome ledger VERIFIED on cloud** (3 rows, run_id 30829832464, http, C7 PASS; queue 10,920 pending / 231 completed).

## Technical Implementation
* **Database Impact:** NO migrations. One manual ops UPDATE (P0 orphaned rows). WARN rows ride existing JSONB `records_meta` (mig 041, unconstrained).
* **records_meta contract facts:** `step_verdicts` keys = BARE manifest slugs (run-chain.js:213/477/550, persisted :611-623); may be ABSENT entirely; FAIL verdict can coexist with `status='failed'`.

## Standards Compliance
* **Try-Catch/logError:** no new catch blocks expected; mkdir throws surface via existing chain error paths.
* **Unhappy Path Tests:** enumerated per-phase in the Red Light steps below.
* **Idempotency:** per-phase check (WF3 protocol step) — all touched scripts must stay re-run-safe.
* **UI Layout:** N/A.
* **Cross-Layer Contracts (§11):** WAIVER recorded — the new thresholds (120-min ceiling, 80%-budget tripwire, n≥30 gate floor, acceptance thresholds) live in yml/python/gate-script surfaces OUTSIDE the `_contracts.json` spec↔SQL↔Zod harness; no contract rows added. Waiver re-examined at P8 output review.

## WF3 Protocol Steps (verbatim per `.claude/workflows.md`; phase-level Red Lights nested in the plan)
- [x] **Rollback Anchor:** `372bc8bc90c72b708ca4e29d9394b1297a80043d` (recorded at P1 implementation start, 2026-08-03).
- [x] **State Verification:** done — live cloud-DB verified by Integration + Reality-Check (3 orphaned rows; 8/8 gate FAILs; ledger 3 rows; queue 10,920; backup 50.7h stale). Data available: pipeline_runs + records_meta. Data ASSUMED (not available): step-level timings (gap filed), real miss-rate baseline (assumption, pending P7).
- [x] **Spec Review:** done — Specs 115/44/48/41/49/112 read by panel; ground-truth inventory below.
- [x] **Reproduction + Red Light:** per-phase — DONE P1-P6 (red evidence captured each phase before the fix; see commits).
- [x] **Fix:** per-phase — DONE P1-P6 (commits `2f513cd0` / `1be8d767` / `469723ee` / `1a2d586d` / `b84eae8b` / `eed9f918`).
- [x] **Idempotency Check:** per-phase — P1/P3 verdict script read-only; P2 mkdirSync recursive idempotent, loaders otherwise untouched; P4 gates stay read-only probes; P5 yml-only; P6 telemetry/pure-logic only.
- [x] **Pre-Review Self-Checklist (2026-08-03):** ① repo-wide createWriteStream sweep — only the 4 fixed loaders + load-centreline (mkdtemp root, safe) ② remaining `>> "$GITHUB_OUTPUT"` = check-chain-running invocations only, VERIFIED redundant (writeOutput self-write :56-62), no third class; watchdog's two deleted ③ FAIL_STATUSES consumers re-verified none outside script+test (P3) ④ no other small-n rate gates found in audit rosters (C7 outcome gate already carries an absolute floor) ⑤ `|| '3'` / `|| '1'` fallbacks confirmed at chain-deep-scrapes.yml:298/:301 — P7 scope, untouched.
- [x] **Independent Review + Regression Guardian:** OUTPUT panel DONE 2026-08-03 — 6 reviewers (Independent, Regression Guardian, Observability, Schema-Fidelity, Integration, Reality-Check). Verdict: ONE code BUG, quad-confirmed (SF+Int+Indep+RC): raw-vs-rounded operand drift in assert-entity-tracing.js knife-edge band [0.7995,0.80) — FIXED red-first in `ea3b86ee` (osStatus now decided on the rounded osPct, matching the coa gate's semantics). RG: all 7 fences PRESERVED. Obs: Spec 48 §4.9 conformance PASS. Integration: fromJSON(env) timeout syntax VALID (precedented at chain-deep-scrapes.yml:195); independently re-ran suites green. RC: live values match plan (61.1% / 79.9%); ran classify_miss_rate live (WARN/PASS/FAIL all correct); found+ops-closed 4 orphaned STEP-level rows (1783/1785/2072/2124, P0-extension); flagged ops-patched durations. 8 DEFERs filed in review_followups (2026-08-03 section). §11 contracts waiver re-examined: upheld (zero schema surface confirmed by SF §6).
- [ ] **Green Light:** `npm run test && npm run lint -- --fix` + `npm run test:py` (python emitters touched) + evidence pasted. → WF6.

## Execution Plan (ordered; one commit per phase; each phase = Red Light → Fix → Idempotency)
- [x] **P0 — ops unblock (no commit):** `UPDATE pipeline_runs SET status='failed', completed_at=now(), error_message='orphaned by GH step-timeout kill (2026-08-03 rehab P0)' WHERE id IN (1756,2045,2097) AND status='running'`. Required before any same-day manual permits dispatch (row 2097 blocks `check-chain-running` until ~01:54 UTC). Read-back verify.
- [x] **P1 (DONE `2f513cd0`) — permits timeout (STOPGAP + tripwire):**
  *Red Light:* new test asserting the duration-tripwire `::warning` fires at >80% budget (fails — tripwire doesn't exist); update `chain.logic.test.ts:417` lock to 120 (red until local-cron changes).
  *Fix:* `chain-coa-permits.yml:125` permits step 90→120; `:22` job 210→240; coa step `:96` stays 90 (explicit decision). `local-cron.js:42` CHAIN_TIMEOUT_MS 90→120 same commit. Duration tripwire in `check-chain-verdict.js` (duration from the row it already reads; budget via env from the same YAML value — no second hardcode).
  *Docs (ground-truthed):* Spec 115 sweep = lines ~95, 114 (verify sweep SKIPS it — coa stays 90), 127, 164 (entities stays 90; **sources is ALREADY 180 in the yml since 2026-07-25 — reconcile the spec's pre-existing drift to 180, don't preserve 90**), **350 (reconcile to P7's deep-scrapes value, NOT 120 — pre-existing drift vs yml's job-45)**, 485-490, **792, 799 (§7 CHAIN_TIMEOUT_MS mentions)**. Known-limit note: 120 unvalidated (no >90 completion exists); step-timing gap → review_followups.
- [x] **P2 (DONE `1be8d767`; dispatch/watch NOT run — operator/orchestrator step) — sources mkdir (4 loaders):**
  *Red Light:* test running a loader's `downloadFile()` against a fresh temp cwd with no `data/` — asserts ENOENT today (red), passes post-fix. Cover all four.
  *Fix:* `fs.mkdirSync(path.dirname(destPath),{recursive:true})` inside each `downloadFile()` helper (covers load-neighbourhoods' two call sites once): address-points, parcels, neighbourhoods, **massing** (:137 download precedes :142 mkdir). Plus `mkdir -p data` in chain-sources.yml (~:65) — CI belt; loader fix still required for local fresh clones.
  *Idempotency:* mkdirSync recursive is idempotent; loaders already re-run-safe (verify unchanged).
  *Docs:* Spec 115 §2.2 one line (mkdir step in sources anatomy).
  *Then:* manual `workflow_dispatch` of chain-sources; **watch to terminal RAN_STATUSES completion** — steps 3–27 haven't completed in 26 days (last id 1464, 07-08); ENOENT was only the FIRST bug. One completion resets the watchdog's 204h window.
- [x] **P3 (DONE `469723ee`) — verdict-check hardening (GREEN-ALLOWLIST inversion; MUST precede P4):**
  *Red Light:* new tests — `status='running'` → ok:false; `status='cancelled'` → ok:false; unknown status → ok:false; `completed_with_errors` + ABSENT `step_verdicts` → ok:false. All red today (:running/cancelled currently classify ok). Deliberate lock retirements: `check-chain-verdict.logic.test.ts:16,27,88-90` (exact FAIL_STATUSES set + export shape); PRESERVE :42 (`completed_with_warnings` ok) and :61-70 (`completed_with_errors` red).
  *Fix:* `classifyVerdict()` ok ONLY for `status ∈ {completed, completed_with_warnings}` (step_verdicts FAIL prong kept) — denylist unprovable on unconstrained TEXT. Rewrite file header contract. In-code note: first-line fence is run-chain's signal handler (failed on GH kill — latent filed); this is belt-and-suspenders. Test-pinned knowing behavior: latest-row-by-started_at means a concurrent manual dispatch's `running` row reddens a scheduled check.
  *Docs (ground-truthed):* Spec 115 `:338-347` + `:376-380` + `:911-917` (denylist → allowlist contract text) + `:926-950` ("no signal / manual UPDATE" truth-up — P0/P3 change both facts) + **§10 Operating Boundaries: ADD `scripts/check-chain-verdict.js` to Target Files**. Spec 48 `:15` optional one-liner.
- [x] **P4 (DONE `1a2d586d`) — baseline acceptance, PRODUCER-SIDE (adjudicated flip; replaces v1's checker allowlist — Spec 48 §4.6/§4.9 violation):**
  *Red Light:* red-first tests on both gates — accepted-WARN row present with live value + threshold + self-documenting acceptance string; companion re-tighten INFO row; **value ≥ threshold ⇒ acceptance self-retires** (gate reverts to plain PASS). Red today (rows don't exist).
  *Fix:* accepted-WARN pattern IN THE GATES: coa `assert_global_coverage` estimated_cost FAIL→accepted-WARN; permits `assert_entity_tracing` opportunity_score same. **NEW metric names — `coa_audit_gate_warn_accepted` is TAKEN (Spec 85, mig 211, compute-trade-forecasts, locked by gate-policy infra test); collision would conflate dashboards/tests (ground-truth hazard).** Chains land `completed_with_warnings` → verdict green with ZERO checker changes; acceptance durable in records_meta, dashboard-visible. **Scope guard:** permits-chain `assert_global_coverage` profile currently PASSes — must NOT be relaxed (chain-aware via PIPELINE_CHAIN, assert-global-coverage.js:95). Alerting retirement = knowing; visibility preserved via WARN rows + annotations.
  *Idempotency:* gates are read-only probes (Spec 47 registry) — must stay so.
  *Docs (ground-truthed):* Spec 48 **§4.5 catalog entry for the accepted-WARN row shape** + **§4.9 exemplar additions (both new acceptances)**; Spec 41 `:199` (opportunity_score row → accepted-WARN posture); Spec 49 `:181` (estimated_cost matrix row → acceptance annotation **+ fix pre-existing ≥80-vs-90/70 drift**); Spec 42 `:187` optional. review_followups `:2554` cross-ref (opportunity_score basis-aware filing superseded/deferred).
- [x] **P5 (DONE `b84eae8b`) — watchdog un-mute + backup fallback (ELEVATED — live incident):**
  *Red Light:* infra test asserting pipeline-watchdog.yml freshness steps carry NO `>> "$GITHUB_OUTPUT"` redirect (red today) + backup_fallback `if:` not gated on `chains_fresh` (red today), `permits_running` race guard retained.
  *Fix:* delete redirects at `:163`/`:178` ONLY (fence CORRECTED by fold-verifier: `check-chain-running.js` ALSO self-writes via `writeOutput()` :56-62, so its redirects at chain-coa-permits.yml:89/:119 are redundant, not load-bearing — still out of scope for THIS fix; cleaning them is a separate follow-up, verify the fact at impl). Decouple `backup_fallback` from `chains_fresh` per Spec 112 §6 (spec already describes the decoupled behavior — yml deviates; P5 restores conformance).
  *Docs:* Spec 115 `:386-397` optional incident-rationale note; review_followups `~:2833` closure annotation (watchdog swallow resolved).
- [x] **P6 (DONE `eed9f918`) — miss-rate gate statistics (BOTH emitters, wipeout-safe):**
  *Red Light:* pytest red-first — n=3 with 1 miss → WARN not FAIL (red today); n=30 with 7 misses (23.3%) → FAIL; **production-shaped run (no cap / early-abort fired) with small n → still FAIL** (wipeout class stays closed). Add to `test_scraper_outcomes.py`.
  *Fix:* `aic-orchestrator.py:534-535/636` AND `aic-scraper-nodriver.py:3293` (parity, or explicitly rule the standalone path out with rationale). FAIL-eligible only at `permits_attempted >= 30`; below 30 WARN **except** when the 90%-anomalous early-abort fired or absolute anomalous count is wipeout-shaped. Threshold string self-documents the regime. 10% baseline cited as assumption (pre-ledger), re-measured from P7.
  *Docs (ground-truthed):* Spec 44 `:171-176` (whole gate paragraph, not just :173) + `:14` schedule-status line (stale NOW — schedule is commented; changes again at P7) + Testing Mandate `:230-241` (new pytest locks).
- [x] **P7 — DONE 2026-08-05** (spun out as its own WF3 — record: `.cursor/wf3_deep_scrapes_drain_finalization_record.md`). Shipped in two commits: **`d6eb9f31`** (F1 soft time-budget self-stop — the drain stops claiming 10 min before the GH kill and finalizes normally; F2 stale-claim reclaim locked red-first) and **`2fa3b2e7`** (F3 cron re-enable). Entry gate satisfied by proving slice **31009693871**: 1,151 year_seqs attempted, 1,086 queue rows retired, anomalous miss-rate 3.7%, zero WAF blocks, budget-stop at 141.0 min vs the 140-min budget, `completed_with_warnings` with no FAIL verdict, zero orphans, zero stuck claims. Job ceiling raised 45→170 (`315dae5d`). `|| '3'`/`|| '1'` fallbacks fixed — the schedule path now resolves `max_permits=0` / `max_retries=2` / `chain_timeout_minutes=150`. WAF waiver recorded in-file per the 2026-08-03 operator ruling. **Cadence landed at ONE weekday slot (`0 15 * * 1-5`), not the 3 this entry assumed** — operator ruling 2026-08-05, sized to ~10K permits over two weeks. Real miss-rate baseline measured: 3.7% anomalous over 1,151 attempts (feeds the P6 re-eval; note the year_seq-vs-ledger-row grain trap now recorded in Spec 44's gate paragraph). ***Docs deliverable DISCHARGED 2026-08-05*** (it was still open when the code shipped — the residuals WF3 closed it): Spec 44 `:14` rewritten to the live one-slot ENABLED cadence + the gate-unit lesson; Spec 115 §2 row 4, §2.5 freshness windows (72/26 → 80/30, moved WITH the cadence), §6 table, §5 prune-window prose, and §9 (reaper scope correction + the 2158/2179 stranding entry) all reconciled.
- [ ] ~~P7 original scope (superseded by the above)~~ **deep-scrapes throughput + cron re-enable (entry-scoped; may spin out as own WF):** staged dispatches `max_permits=100` → observe → `0`/full. **Requires raising job `timeout-minutes: 45` (chain-deep-scrapes.yml:82).** Before uncommenting `schedule:`: (a) probe-proven constants cited to run id (:46-50 contract), (b) `:286-290` condition — **RULED 2026-08-03 (operator): WAIVED in writing** — zero blocks across 509 requests (runs 30829832464 + 30843114683); constants stay conservative, re-derive if/when a block occurs naturally; comment amendment rides the re-enable commit, (c) fix `|| '3'` / `|| '1'` fallbacks (:298/:301) so the schedule path gets PRODUCTION values. Measure real miss-rate baseline (feeds P6 re-eval). *Docs:* Spec 44 `:14` re-enable note; Spec 115 `:350`/`:953` line-ref reconciliation (with P1's sweep).
- [ ] **P8 — Multi-Agent OUTPUT Review:** Independent (`feature-dev:code-reviewer`, worktree) + Regression Guardian (main tree) per WF3 protocol, + Observability (P4 acceptance rows vs Spec 48 §4.9) + Reality-Check on the first post-fix green board. BUG → fix before Green Light; DEFER → review_followups.
- [ ] **P9 — Verify green board + lessons:** one full schedule cycle green (nightly coa-permits, watchdog after sources completion + backup restored, deep-scrapes staged dispatch); record run ids here. Route lessons (mkdir-on-fresh-checkout; unconstrained-status denylists; consumer-side acceptance is a spec smell; probe-shaped defaults leaking into cron) per Spec 05 §5. File: step-timing observability gap; SIGKILL-vs-async-UPDATE latent; adversarial CLI results if landed.

## Known Failure Modes guarded
* P4 producer-WARN masking a further coverage REGRESSION — accepted-WARN row carries the live value every run; history makes a drop visible.
* 120-min ceiling unvalidated — duration tripwire at 80% budget + revisit clause; step-timing gap filed.
* P6 softening reopening wipeout false-PASS — cap/early-abort override keeps production wipeouts FAIL.
* P5 over-deletion — check-chain-running redirects load-bearing; scope pinned to watchdog :163/:178.
* P4 metric-name collision — new names mandatory; `coa_audit_gate_warn_accepted` reserved by Spec 85.

## Panel Adjudication Log
* **v2 FOLDED:** load-massing 4th loader (CR+Int) · 3 orphaned rows + no-auto-expiry truth (SF+CR+Int+RC) · P3 green-allowlist + cancelled hole (SF) · P4 → producer-side accepted-WARN per Spec 48 §4.6/§4.9 (Obs; CR/Int verified allowlist would function — spec-conformance + durability + less code decides) · P1 duration tripwire (Obs) · local-cron + test lock + Spec 115 sweep (RG) · P6 dual emitters (CR/Int) + n≥30 (RC) + wipeout override (RG) · P7 job-ceiling (CR) + WAF re-derive + `||` trap (RG) · P5 elevation (RC) + check-chain-running fence (Obs/RG) · watch-sources-to-completion (RC) · knife-edge reframe (RC) · coa :96 explicit (Int).
* **v3 FOLDED (Ground-truth):** Spec 115 :792/:799 named · Spec 115 verdict-contract rewrite (:338-347/:376-380/:911-917) + :926-950 truth-up · Spec 115 §10 Target Files += check-chain-verdict.js · Spec 115 §2.2 mkdir line · Spec 48 §4.5 catalog + §4.9 exemplars · Spec 41 :199 · Spec 49 :181 (+80-vs-90/70 drift fix) · Spec 44 :14 + Testing Mandate · review_followups closures (~:2833, :2554) · **P4 metric-name collision hazard** · Spec 115 :350 reconciled to P7 value. Plus WF3 protocol scaffold verbatim (operator directive): per-phase Red Lights, idempotency checks, sibling-bug self-checklist, rollback anchor.
* **REJECTED with reason:** v1 checker-side allowlist (Spec 48 §4.6/§4.9 violation; superseded) · RC's "row 2097 exceeds 120min" as evidence-against-120 (orphaned row wall-time ≠ chain runtime; valid kernel folded as known-limit note).
* **CUT (operator, 2026-08-03 at authorization):** Gemini/DeepSeek CLI outputs — pair cancelled; runner's groundwork already harvested (dual-emitter find, independently confirmed).
