# Active Task: Phase B — Sources Incremental Architecture (WF2)
> **⚠ RESUMED 2026-08-14 UNDER SPEC 08 §11 (Grounded Verification Protocol — operator-directed).** **B0 ITEM 7 IN FLIGHT:** four Phase A commits postdate item 6's re-derivation (C1 `cdaea415` lands AD1 INSIDE B2-owned `run-chain.js:225-232` · C4 `1fad5ee4` · C7 `4c598dd8` · envelope `20fa17f2`) — every B2 file/line/lock claim is being RE-EXECUTED against today's tree, plus the NEW unruled composition question: **does `deferred_to_full` count as a failed predecessor under AD1?** (if yes → post-defer run is UNGATED = longer — arguably the wrong response to "scope too big"). Red-first case specs for the remaining B2 suite are being drafted INTO the plan per §11.4; fold-validation (grounder + Idempotency + Cross-read Adversary) follows the fold, before any code.
> **⚠ C5 MERGED IN (operator-directed 2026-08-14):** Phase A's last item — `step_completeness` (`{expected, executed, died_at, skipped_gate, skipped_budget}` + `classifyStepCompleteness`, consulted ONLY inside OK_STATUSES, §4.9 annotate→`_retighten` rollout) — lands WITH B2's defer mechanism as ONE contract: same files, interlocking semantics (`deferred_to_full` = legitimate `executed < expected`; the field set must say so). C5's binding design text stays in `.cursor/active_task.md` §C5; the grounder is mapping both + ruling the unified field-set shape. **This closes the operator's "full CoA and permits steps accounted for and tracked" requirement** — post-landing, a green-looking chain that silently ran fewer steps than its manifest expects is machine-detected per run.
**Status:** Implementation — AUTHORIZED by operator 2026-08-09 ("proceed"; B0 gate cleared, folds applied) — **v5**, rewritten 2026-08-06 after the ROUND-3 lean panel found v4 not-ready (records: `.cursor/phase_b_round3_panel_2026-08-06.md` · round 2 `phase_b_round2_panel_2026-08-06.md` · round 1 `phase_b_panel_record_2026-08-05.md`; supersedes `queued_task_phase_b_sources_incremental_v4.md`, `_v3.md`). Round-3 headline: the round-2 panel itself minted a wrong remedy (the parity formula) that v4 folded faithfully — **folded remedies are re-verified claims from now on.**
**Domain Mode:** **Cross-Domain.** B6 touches `schedules/route.ts`, `stats/route.ts`, `ScheduleEditModal.tsx`, `DataQualityDashboard.tsx`, `FreshnessTimeline.tsx`, `funnel.ts`, `chain.logic.test.ts`. **B2 owns (R3-B2)** `scripts/run-chain.js`, `scripts/check-chain-verdict.js`, `scripts/check-pipeline-freshness.js` and their exact-pin locks `check-chain-verdict.logic.test.ts:101-102`, `check-pipeline-freshness.logic.test.ts:56`.
**Workflow:** WF2 per `.claude/workflows.md:67-95` — **Self-Checklist runs BEFORE the B7 review panel** (order restored).
**Design authority:** `docs/reports/2026-08-04-sources-incremental-architecture.md`, corrected by three panels.
**Predecessor gate (DS-8) — now a PERFORMED step:** B0 items 3-5 below re-verify this plan against Phase 1's post-fix contract. Phase 1 (max-build v4) runs first; nothing here lands between Phase 1's steps 13-14.
**Rollback Anchor:** `b4351297`. **Pre-Flight:** `node scripts/ai-env-check.mjs` before B0.

## IMMEDIATE OPERATOR ITEMS (operator-executed; not part of this task's authorization)
1. **Cloud `pipeline_schedules.deep_scrapes` row** still 'Weekdays (3x Daily)' — fix via an `apply-migrations.yml` migration or the documented runbook one-off procedure; **never an ad-hoc prod UPDATE** (vehicle restored per round-3 GT F6).
2. Migration 238 cloud apply + next-nightly proof (carried).

## Context
* **Goal:** unchanged — make every step work only when inputs changed, AND make the runs where inputs DID change survive. Founding incident re-confirmed verbatim (run 2179: 180-min kill, SIGTERM handler never fired). `enrich_parcels` is literally step 21 of 27; steps 22-27 are the cost + assert + snapshot set.
* **Target Specs:** as v4 **plus Spec 40 (status vocabulary), Spec 47 (a third step ending), Spec 115 §2.5 (RAN/watchdog semantics)** — the `deferred_to_full` documentation rides **B2's commit** (behavior ships there); B6 documents only variant/cadence (GT F7).

### GT3 constraint + DS4 condition — unchanged from v4 (`assertCentrelineEnriched` at `enrich-permits.js:301-332`).

## Operator rulings
* **D1′ (corrections folded):** `massing_enriched_at` vs `parcel_buildings.linked_at` via `IS NULL OR EXISTS(linked_at > watermark)`. Backfill = per-parcel `MAX(linked_at)`, **BATCHED (~50K id-range batches — `migrate.js` has no statement_timeout override; a single statement dies at the 2-min default)**; premise corrected: today 0 parcels have >1 distinct linked_at (one full relink stamped all) — MAX is future-proofing, not a day-one fix. **Indexes RULED (Int F6):** NO new parcel_buildings index (probe is index-supported; measured trivial); ONE partial index `ON parcels(id) WHERE massing_enriched_at IS NULL` (day-one ≈11.3K entries — the zero-pb parcels backfill NULL and form a bounded first-run scope). Zero-link-ghost WARN row: day-one value **0** (measured) — forward-looking. Heights-only staleness: measured real-but-negligible (4/427,077 per run) and **bounded by the quarterly ONLY IF the quarterly also sets `LINK_MASSING_FORCE_FULL=1`** (see D2′ — without it the massing gate vetoes on unchanged count/code_version and the bound is false, R3-B4/Adv F6).
* **D2′ — escalation, defer lifecycle DESIGNED (closes R3-B2/B3):**
  - **Scope-count-then-defer:** each gated step computes its scope BEFORE working; over threshold → the chain stops cleanly at the **current** step boundary with outcome **`deferred_to_full`** (a NEW `pipeline_runs.status` value — TEXT column, no migration needed). **The mid-step soft-stop claim is DELETED** (R3-B3): enrich-parcels passes 1-4 are one transaction; `d6eb9f31` transfers only to per-item-commit loops. Pre-step counting is the whole mechanism.
  - **Status fork CHOSEN (R3-B2):** `deferred_to_full` is a distinct status. Consumer classification, all four hops: `check-chain-verdict.js` treats it as **green + a `::warning` GitHub annotation** (deliberate edit of `OK_STATUSES` + its exact-pin lock) · `check-pipeline-freshness.js` counts it as **RAN** (absence detection; deliberate edit of `RAN_STATUSES` + lock) · both admin renderers gain the status (distinct chip, not default styling) · **a deferred STEP row does NOT satisfy B3's "completed with 0 changes"** (gates exclude the status explicitly).
  - **Loop breaker + nag (R3-B2/Adv F3):** a **defer-streak rule** — 2 consecutive `deferred_to_full` on the same step ⇒ the verdict step exits 1 (red) with an explicit "supervised force-full required" message; plus a stated staleness ceiling (one missed bi-weekly window) after which the operator dispatches the 360-min variant. The fence×defer interaction is thereby the designed path: citywide redraw → citywide NULL → first run defers with a warning → second would-be defer goes red → supervised force-full.
  - **Force-full channel (R3-B4):** per-script env vars — `ENRICH_PARCELS_FORCE_FULL` OR'd into `enrich-parcels.js:1317`'s argv check (NOT into `pipeline.isFullMode()`, which would flip `link_parcels`); the new cost gate honors its own var; **the quarterly dispatch sets `LINK_MASSING_FORCE_FULL=1` as well**; force envs are step-scoped `env:` on the chain step only (no `$GITHUB_ENV` leak to the verdict step). DS5's red test targets `:1317`.
  - **Thresholds (R3-B8, evidence-based):** initial **50,000 rows/pass**, stored in `logic_variables` — 10-15× measured-normal (decision scope ≈3,461/14d), ~9× under measured-escalated (full 5-pass citywide 46.5-53 min; full relink 485K = 14.7-21.9 min ×3 runs). **Prerequisite: B2 adds per-pass duration audit rows** (today one timer covers all five passes — thresholds are a labeled projection until B7 measures). **Every run emits `scope_count / threshold / ratio` per pass + WARN at ≥80%** (the operator sees the cliff coming, Adv F11).
  - Quarterly mechanics: as v4 (OR'd cron DOM/DOW in-job date check; job ceiling moves; quarterly Sunday never parity-skipped; first force-full dispatch-supervised). **Accepted operating cost, stated for ratification: worst-case ~92 days staleness** if a release lands just after a quarterly and no operator dispatches sooner.
* **D3:** as v4 (stream-hash, tier-1.5, stated assumption; 30-40 min floor is a projection).
* **D4′:** as v4 (linked_at-keyed decision scope; bounded comp_count=0 retry — direct component measured ≤3.9%/window; **the candidate-join multiplier is an OPEN number — B7 measures it via EXPLAIN ANALYZE on the first real run**; scope MATERIALIZED) with round-3 semantics (R3-B7): scope table **keyed by run_id + a consumed marker**; pass 5 unions **unconsumed** prior scopes (crash between scope-write and pass-5 read no longer orphans the set); cleanup rule for failed runs; retention bound. Concrete shape (Int F8): permanent table `enrich_parcels_pass3_scope` via migration + **RLS Class-B (mig 227 pattern)** + a **backup-db `EXCLUDED_TABLES` ruling**; DELETE+INSERT in-txn; chain lock + enrich xact lock serialize access.
* **Parity (R3-B1 — the triple-confirmed blocker):** the guard is **`floor(epochDays / 7) % 2 === ANCHOR_PARITY`** — parity of weeks-since-epoch, which strictly alternates for weekly-sampled Sundays, ISO-week-53-free, DST-free (UTC epoch days). **Anchor: 2026-08-09 is an ON week (20674 / 7 = 2953 → parity 1)**; `PARITY_ANCHOR` lives in the workflow `env:` block with the formula in a comment; the re-enable commit message restates it. Implemented as a first-class guard step after `env_guard` emitting `on_week` to `$GITHUB_OUTPUT`, gating the run step AND the verdict step AND the freshness expectations. `workflow_dispatch` bypasses the parity skip, keeps verdict gating. **Red test: strict ON/OFF alternation across ≥8 consecutive Sundays spanning Dec 2026 → Jan 2027** (a year-boundary-only fixture passes the broken `/14` formula — insufficient by proof).
* Prior rulings unchanged: full architecture · bi-weekly · quarterly conditional on B0 · no sources rerun before landing · GitHub re-enable.

## State Verification
As v4 (all round-2/round-3 verified) with corrections: parcels **496,422** · `parcels` **bloat: dead_ratio 0.64-0.73** (~1.2-1.3M dead tuples — all ceiling math measured against a degraded table; bloat/autovacuum check added to B0 + B8 sweep) · new column surfaces in `parcels-columns.snapshot.json` + `parcel-lookup.infra.test.ts` (deliberate edits) · `zoning_base_source_dataset_version` is **NOT fenced** (ruled: provenance-only, no consumers, truthful while the stamp is NULL) · freeze-risk counts residential-scoped · "30-45 min" a projection.

## B0 — HARD GATE (now five items; items 3-5 ARE the DS-8 performance, R3-B6)
1. Root-cause the 08-03 parcels anomaly (both directions: false positives stop; real changes still invalidate).
2. Spec Review + per-step live counts + **the `parcels` bloat/autovacuum check**.
3. **Post-Phase-1 re-baseline:** re-measure 14/32 · 5,318 HIGH/MED · 461/3,330/121 (`stale_cost_fsi_without_gfa` inflates by construction after Phase 1's NULLs; the pinned baselines are invalidated).
4. **Vacuous-zero companion check:** add/verify `opt_aor_gfa_sqm IS NOT NULL AND max_buildable_gfa_sqm IS NULL` (the audit's RC3 invariant applies only when max_buildable is non-NULL — Phase 1's NULLed parcels LEAVE its population; zero must mean healed, not vacated). Phase 1's `ravine_constrained_carries_priced_cost` invariant covers the constrained class; this covers the rest.
5. **Pass-4/5 eligibility under NULL envelopes:** state whether NULL-envelope parcels are excluded from comp/opt scope (expected: yes, by existing eligibility) and record the coverage consequence.

## Spec Updates
As v4's table with round-3 additions: Spec 40/47/115 §2.5 (defer semantics — **rides B2**) · Spec 30 rows `:88,:101` (not `:222`) · B6 gains `chain.logic.test.ts` + `stats/route.ts`.

## Standards Compliance (inlined — no incorporation-by-reference, GT F8)
* **Database Impact: YES.** Migration (numbered behind Phase 1's 239): `massing_enriched_at` ALTER + batched MAX backfill + the partial index + the `enrich_parcels_pass3_scope` table (+RLS). UP+DOWN all-comments, `validate-migration.js`, SQLFluff, `npm run migrate`, `db:generate`, `typecheck`, `BUILDO_TEST_DB=1 npm run test:db`. factories.ts N/A (watermarks raw-SQL by precedent); `parcels-columns.snapshot.json` edit named.
* **Cross-Domain (B6), answered here:** *Try-Catch:* schedules PUT keeps its overarching try-catch; stats route change adds no new boundary; any new catch uses `logError(tag, err, context)`. *Unhappy paths:* invalid cadence → 400, plus 404/500; **`funnel.ts` AND the stats-route freshness map both exhaustive-or-throw on unknown cadence** (the 192h constant becomes cadence-derived with its own fail-closed ruling). *UI Layout:* admin desktop-first `md:`; enum/status content only. *UI Regression:* `npx vitest run src/tests/*.ui.test.tsx` — the tautological test-LOCAL copies in `admin.ui.test.tsx:595-625,668-699` are EDITED deliberately; `:956-965` + `quality.infra.test.ts:242-250` fail only on string removal. *§10.2:* `npx vitest related src/lib/admin/funnel.ts`. *Scenario-A handoff note (CF9, landed):* the schedules PUT contract is: `PUT /api/admin/pipelines/schedules` body `{id, cadence, cron, enabled}`, cadence ∈ the 6-site enumeration, validated server-side (400 on unknown), DB row is authority, seed is bootstrap-only (`ON CONFLICT DO NOTHING`); UI reads the row, never the seed. *§11 N/A items stated:* envelope/middleware/types/375px — N/A (no new route, no mobile surface).
* **Cadence enumeration:** 6 code sites + DB row + per-source dashboard labels; parity test (code-vs-code) **plus the runtime cloud-row assertion** (R3-B5): the verdict step (and a B7 checklist line) asserts the live `pipeline_schedules` row for chain_sources against the expected cron/cadence values **passed to the step as `env:`** (round-4 F7: the script cannot see the workflow's constants otherwise) — closing the live-drift loop the CI test cannot see. The verdict step already has DB access (`*pipeline-env`).
* **RC0 deferral (three heads now):** localhost fallback · CLI-pool statement_timeout death · **orphaned server backends on client kill** (13 found and terminated this session — analysis sessions must cancel server-side on abort). One review_followups entry; B7's RC uses the pipeline pool.
* **Idempotency:** gates read-only; loaders IS DISTINCT FROM; **watermark written in the SAME transaction as the work it stamps, never before**; scope-table writes in-txn with run_id.
* SPEC LINK headers · `npm run system-map` · Spec 05 §5 six-field footers.

## Execution Plan — WF2 shape, one commit per step
- [ ] **B0 — the five-item HARD GATE above.** No B1/B2 code until items 1-5 clear.
- [x] **B1 — `source-version.js` + tier gates.** DONE `0b230472` (2026-08-10). All six v4 items landed. Note for B7: the content-hash branch existed in all four gates pre-B1 but **no call site ever fed it** — tier-2 is newly *live*, not merely refactored, so B0 item 1's "the hash tier stops the common false signal ✔" only becomes true at this commit. Heritage tier-1 keeps its explicit spread (`load-heritage.infra.test.ts:91-93` pins the ordering textually — not a B1-authorized lock edit). As v4 (four branches; fail-safe LOAD; **stream-hash**; three distinguishable outcomes; skip emits completed row = the DS4 condition; `started_at DESC` standardization NAMES the two consumer-side completed_at readers as in-scope or excluded).
- [ ] **B2 — enrich-parcels + D1′ + D4′ + fence + defer plumbing.** **IN PROGRESS.** Migration DONE `21db5d88` (mig 240: `massing_enriched_at` + backfill 485,135 + partial index + `enrich_parcels_pass3_scope` + RLS) — validated, applied to dev, idempotency-replayed. **B0 item 6 re-verified against the live tree BEFORE any code**: `:545` `'completed'` literal ✔ · ladder `:635-641` (defer branch goes between `:636` completed_with_errors and `:639` budgetStopped) ✔ · `OK_STATUSES` `check-chain-verdict.js:89`, double-locked at `check-chain-verdict.logic.test.ts:102` + `run-chain-budget.logic.test.ts:81` (regex tail `\]\)` DOES break on append) ✔ · `RAN_STATUSES` `check-pipeline-freshness.js:84`, lock `:56` ✔ · argv `--full` at `enrich-parcels.js:1378` ✔. **Int F4/F5 batching UPHELD and implemented literally — a self-correction is recorded here because the first attempt overrode it wrongly.** I initially replaced the ~50K batches with one `SET LOCAL`-guarded statement, reasoning that SET LOCAL is the house pattern (mig 138/139/141/145/169/172). Then I measured: a single-statement backfill is **94.06 s** against the 2-min cloud ceiling — ~26 s of headroom, on a box with FEWER parcels than cloud (486,530 vs 496,422) and the same bloated heap round-3 measured. Batched, the worst statement is **10.79 s** (~11× headroom). The panel was right. Two sub-claims also retracted: **"~10 batches" was never in the plan or the panel** — the panel said "~50K/**batch**" (rows) and named no count, so the backfill batches by ROW COUNT (`LIMIT 50000`), which also makes the sparse-id fact (max id 1,944,530) irrelevant since it only complicates *id-range* batching; and **Int F6 was CONFIRMED, not corrected** (EXPLAIN shows the EXISTS subplan on `idx_parcel_buildings_parcel`). `SET LOCAL` is retained as defence-in-depth only — verified to override a session timeout mid-txn, but batching does not depend on it landing through Supabase's pooler. A completeness gate RAISEs if 12×50K ever under-covers, so a half-stamped watermark can never pass silently. **Lesson: measure before overriding a panel ruling — the ruling's stated reason was thin ("dies at 2 min"), but it was right, and my substitute reason was theory.** **REMAINING:** fence extension · per-pass duration + scope/threshold/ratio rows · the `deferred_to_full` mechanism (run-chain marker→step-row rewrite→third break var→chain row + `deferred_step` meta) · verdict green+`::warning` + 2-defer streak red · `RAN_STATUSES` · force-full envs (`:1378` + cost gate) · comps `resetIneligible` lot-half fix · `opt_aor_without_max_gfa` row · D1′/D4′ wiring · Spec 40/47/115/43/48 docs · the red suite. Migration (column/backfill/index/scope-table). Fence extension (zoning + massing stamps; NOT the provenance column). **Per-pass duration audit rows + per-pass scope_count/threshold/ratio rows (every run).** The `deferred_to_full` mechanism, row inventory SPECIFIED (round-4 F3/F4): the deferring script emits a `records_meta` marker and exits 0 → `run-chain.js` parses it, **rewrites the deferring STEP row to `deferred_to_full` (diverting the `:506-516` unconditional `completed` UPDATE — otherwise B3's status-exclusion is vacuous)**, breaks the loop via its **own variable (NOT `failedStep`**, which exits 1 and maps to chain `failed`), and writes chain row `deferred_to_full` + a **`deferred_step` slug in chain `records_meta`** (streak detection needs it). Prior steps' rows stay `completed`; downstream steps have NO rows. Verdict streak detection = the last **2** chain rows (`LIMIT 1 → 2`) compared on `deferred_step`. The `OK_STATUSES` edit is global across chains — dormant elsewhere since only sources emits the status (one-sentence note in the lock edit). **`run-chain.js`, `check-chain-verdict.js`, `check-pipeline-freshness.js` + both exact-pin locks are THIS step's files**; Spec 40/47/115/43/48 defer documentation rides this commit. Force-full env plumbing (`:1317` + cost gate). *Red:* massing gate NULL-arm + fires-on-linked_at-and-not-otherwise · zero-link WARN row · bounded comp_count=0 rule + materialized cascade (run_id + consumed marker; crash fixture: unconsumed scope is unioned by the next pass 5) · decision scope on linked_at (late-backfilled decision_date still caught) · pin-absence lock (enrich_parcels only) · pass-5-basis invariant (RC3) · **simulated citywide scope → clean `deferred_to_full` at the current boundary, verdict green + warning, freshness counts RAN, B3 gates do NOT read it as 0-change** · **defer-streak fixture: second consecutive defer → verdict exit 1** · geometry-change fixture invalidates both stamps.
- [ ] **B3 — heritage skip · link_parcel_addresses · compute_parcel_cost_estimates · link_wsib.** *Red:* run-ledger gates scan every attempt of ANY status since own last completed run · **no-completed-run-ever arm: fail-safe RUN (fixture: cost step, nothing completed since 07-08)** · dual-chain slug enumeration (`sources:link_wsib` + `entities:link_wsib` per the record; each script's actual chain memberships verified at implementation, not assumed; massing-full-gate IN-list precedent) · deferred rows excluded · wsib partial index + all three tier CTEs + invalidation semantics.
- [ ] **B4 — zero-intersection floor.** As v4 (design decision; L24c constraint; IS-DISTINCT-FROM convergence row + missing-previous fallback; re-baseline after B2/B3; coverage ratio reported).
- [ ] **B4.5 — comparable_builds blanket write (NEW 2026-08-19, ahead of B5 — it is on the critical path B5 is not).** `buildComparableBuildsUpdateSql` (`enrich-parcels.js:1114`) rewrites ~351,899 parcels EVERY run with no change detection — the last unguarded blanket writer in a file that uses `IS DISTINCT FROM` 13 times (pattern founded by `7e130bff`, Severity HIGH, `lessons:28`). **GROUNDED STATUS — it HAS been looked at, and the ruling answered a different question:** fold D (`a81c6a7c`) excluded comps from the `records_updated` aggregate *"by design: compute-parcel-cost-estimates.js reads ZERO of its columns … guarding it would still overcount the counter"* — a sound COUNTER-HONESTY argument that says nothing about write cost. The separate *"a guard spanning all 11 columns would rescue nothing"* line is about **optconfig's `nearby_builds_summary`** (differs 88,575/88,575 every run by design, rolling 5-yr window), NOT comps. The write-amplification risk was raised **independently by the Regression Guardian and Reality-Check seats**, filed MED, and **never measured**. **GATING MEASUREMENT (do this BEFORE planning a fix — the C2 discipline):** snapshot `comparable_builds`/`comp_count`/`comp_dominant_build` for a parcel sample, re-run the pass, diff. **If they genuinely differ every run, a guard rescues nothing and B4.5 is CLOSED as not-a-defect.** If largely stable, the guard suppresses ~351,899 row-rewrites/run on `parcels` — 5,806 MB at **38.9% cache-hit**, which with `permits` is 87% of all 915M disk block reads. **Why it outranks B5:** `enrich_parcels` averages **387 min** (measured, cloud) and is the chain's largest cost; B5 saves zero minutes (its loaders cost 1-2 min each). **Also re-opens Spec 118 §9.1's recorded NEGATIVE result** — the enrich-parcels blanket-UPDATE → heap-decorrelation hypothesis was logged UNSUPPORTED *solely* because *"the incident occurred on the cloud instance, which the analysis session could not reach"*; §9.1's own stated condition for re-testing (correlate against cloud write-history) is now satisfiable.
- [ ] **B5 — runtime CKAN resolution** (three loaders; last_modified sort; Shapefile glob; no year parsing).
- [ ] **B6 — cadence + defer surface (CROSS-DOMAIN).** The **corrected parity guard** (formula/anchor/placement per Operator rulings) + week-alternation red test (≥8 Sundays, Dec→Jan) · quarterly variant + per-script force envs **+ `LINK_MASSING_FORCE_FULL=1`** · GitHub re-enable + disable-rollback step · 6 cadence sites + per-source labels + DB row + parity test + **runtime cloud-row assertion** · watchdog window → bi-weekly + slack (structural 14-day gap, not 21) · admin status rendering for `deferred_to_full` · `CHAIN_DURATION_BUDGET_MINUTES` set · funnel + stats-route exhaustive-or-throw · tautological UI-test locals edited · `chain.logic.test.ts`.
- [ ] **B6.5 — skip-staleness assert as a WORKFLOW step (round-4 F6 correction: a manifest tail step CANNOT run after a defer — the loop breaks before it; the correct placement is a standalone workflow step after the chain step, verdict-step pattern `chain-sources.yml:83-86`, `if: always() && skip != 'true'`, `*pipeline-env` for DB access):** per-step expected-cadence table in `logic_variables`; WARN at 2×, **FAIL at 4×**; a defer does NOT reset a step's age (it attempted, did no work). This is the wedged-gate tripwire AND the defer-loop backstop.
- [ ] **B6.6 — ⚠ PRIORITY (added 2026-08-16, from the B3 output-panel grounding). Stranded `running` rows are a live reliability defect that the admin stats route has been silently masking for months — and they wedge B3's gates.** GROUNDED, not theorised: a real fault injection (`node scripts/load-wsib.js` against a schema-drifted CSV) leaves `pipeline_runs` at `status='running', completed_at=NULL`; the B3 gate's `COALESCE(completed_at,'infinity') > own_started` then beats **every future anchor**, so `nonCompleted` stays ≥1 permanently — reproduced across 5 simulated later runs, and cured only by deleting the row. **The "it has never happened" premise is REFUTED: it has fired 19 times.** Today's live count reads 0 ONLY because `src/app/api/admin/stats/route.ts:188-199` runs `UPDATE pipeline_runs SET status='failed' … WHERE status='running' AND started_at < NOW() - INTERVAL '2 hours'` on **every admin-stats request** — 19 rows carry `interrupted: stale run auto-cleaned`, including `load_wsib` itself and `sources:link_parcels` stranded **39 days**; 10 were reaped in a single burst on 2026-07-19, one page load clearing ~4 months of accumulated false state. **Consequences, both of which land squarely on B7:** (1) the reaper requires a HUMAN TO OPEN A PAGE, so under unattended GH Actions cron — exactly B7's operating mode — the wedge is *permanent*, not 2-hourly; (2) for months the dashboard has been erasing the evidence of a real crash/SIGKILL reliability problem instead of surfacing it, so nobody has ever seen the signal. **Scope:** six scripts share the `INSERT status='running'` + no-`try/finally` + happy-path-only-finalize shape — `load-wsib.js:145/390` (**the only one that also swallows its finalize error**, `.catch(() => {})`), `enrich-web-search.js:314/550`, `enrich-wsib.js:493/842`, `assert-data-bounds.js:83/991`, `assert-engine-health.js:43/287`, `assert-schema.js:269/547`; `run-chain.js:331/595` shares the INSERT but IS defended (SIGINT/SIGTERM handlers `:47-79` + failure UPDATEs) — though those still do not cover SIGKILL / hard crash / container stop, which is how the chain-scoped strands (`sources:link_parcels`, `chain_permits` ×5, …) happened. `assert-schema.js:119/207/225/252` throw BEFORE their finalize and will strand; the three HALT throws sit AFTER theirs and will not. **Required work:** (a) `try/finally` + non-swallowed finalize across the six, (b) a **TTL/staleness guard inside the gate itself** so a stranded upstream row self-expires rather than wedging forever — precedent `scripts/lib/chain-concurrency.js:36` already uses a 12h TTL for exactly this class, and the B3 gate has none, (c) decide the reaper's future: it must not remain the only mechanism, and it should RECORD what it reaps (a counter/audit row) instead of silently rewriting history. **Must close before B7** — a wedged or silently-reaped gate during the proving runs produces precisely the false confidence those runs exist to eliminate. `load-wsib.js` alone is fixed inside the B3 WF3 remediation (commit E); the other five and (b)/(c) are THIS step.
- [ ] **B7 — Pre-Review Self-Checklist, THEN the OUTPUT panel (order per workflows.md), then proving runs:** one bi-weekly-shape green including steps 21-27 · one quarterly-shape genuinely exercising all force channels (dispatch-supervised) · **one simulated-escalation run proving the defer lifecycle end-to-end (status at all four hops + the streak red)** · measure: the candidate-join multiplier, real per-pass durations (retire the threshold projection label), the 30-40-min floor.
- [ ] **B8 — Green Light + close-out.** Sibling sweep (v4's + stamp-before-work orderings + tautological test-locals + **bloat/autovacuum on big tables**). RC3-tail gate promotion (with B0 item 4's companion check green). Full command battery; lessons (incl. the folded-remedy re-verification lesson); memory; system-map; six-field footers.

## Known risks
As v4, restated: RC1 anomaly still the plan-killer (B0 both directions) · the defer lifecycle is designed but unproven until B7's simulated run · **92-day worst-case defer staleness is an accepted operating cost — operator ratifies it by authorizing** (GT F12 restated: if B0's anomaly ruling downgrades the quarterly to MONTHLY, the D1′ staleness windows and this worst case shrink to ~30 days and are restated in the plan) · three skip paths still halt-free (red lights + B6.5 + B7 are their cover) · D1′'s two staleness windows accepted (heights-only now genuinely quarterly-bounded via `LINK_MASSING_FORCE_FULL`) · thresholds are projections until B7 · `parcels` bloat may move all duration math.

## Panel disposition ledger — round 3 (rounds 1-2 ledgers in the superseded files; full text `.cursor/phase_b_round3_panel_2026-08-06.md`)
**FOLDED:** R3-B1 (parity formula `/7` + anchor + 8-Sunday red) · R3-B2 (status fork chosen: distinct status, green+warning verdict, RAN freshness, admin chips, B3 exclusion, build surface + locks in B2, spec surface 40/47/115 riding B2) · R3-B3 (mid-step claim deleted; d6eb9f31 non-transfers stated) · R3-B4 (per-script envs at :1317; quarterly sets LINK_MASSING_FORCE_FULL; step-scoped env) · R3-B5 (runtime cloud-row assertion) · R3-B6 (DS-8 as B0 items 3-5; companion check; re-baseline) · R3-B7 (run_id + consumed marker + union + cleanup + RLS + backup exclusion) · R3-B8 (50K thresholds + per-pass telemetry prerequisite + every-run ratio rows + WARN 80%) · Adv F3 (defer-streak red + staleness ceiling) · Adv F9/Int F9 (no-completed-ever arm; dual-chain slugs) · Adv F10/GT F10 (B6.5 chain-tail; 2×/4×; defer doesn't reset age) · GT F6 (operator vehicle) · GT F7 (spec edit rides B2) · GT F8 (compliance inlined; Scenario-A note landed; stats-route ruling; §11 N/A stated) · GT F9 (Self-Checklist before panel; UI regression named) · GT F12 (wording; conditional bound restated) · Int F4/F5/F6/F7 (backfill premise/batching; indexes; fence position; snapshot fixture) · RC 1-6 (bloat check; candidate-join open number; ghost day-one 0; heights-only characterization; 92-day cost stated).
**NOT ADOPTED:** none.

> **PLAN LOCKED. Do you authorize this WF2 plan? (y/n)**
> §11 note: three round-3 rulings — the parity guard moved to **weeks-since-epoch parity** (the round-2 remedy was arithmetically wrong — triple-confirmed); the defer fork resolved as a **distinct status, green-with-warning verdict, defer-streak red at 2** (silent-green and standing-red both rejected); and the quarterly now **explicitly forces the massing relink** (`LINK_MASSING_FORCE_FULL=1`) — without it the "quarterly heals" bound on D1′'s accepted staleness was false.

## B0 item 6 (ADDED 2026-08-09 — post-chain-budget-WF3 re-verification, per this plan's own folded-remedies lesson)
The chain-budget WF3 (commits 8556c992/d5a58168/6db54559, merged to main) modified B2's owned files AFTER this plan's round-3: run-chain.js gained the soft-budget block (loop shape changed; the ":506-516 unconditional completed UPDATE" citation drifted; a SECOND early-stop path with status completed_with_warnings now exists and the defer mechanism must COMPOSE with it — distinct statuses, distinct break variables, cancel-precedes-budget precedence already locked); new lock files exist (run-chain-budget.logic.test.ts, chain-coa-permits-workflow.infra.test.ts); Spec 115 §2.2 carries the budget contract B2's defer docs must sit beside. B0 item 6 = re-derive every B2 file/line/lock assumption against the post-WF3 tree before B1/B2 code.

## B0 — PERFORMED 2026-08-09 (all six items; GATE CLEARS)
1. **08-03 anomaly ROOT-CAUSED both directions**: a ONE-TIME upstream CKAN toolchain re-export (schema cut 12→6 cols; STATEDAREA re-rounded to 2dp; sub-mm coordinate jitter on every vertex → IS DISTINCT FROM fired citywide, 485,567). NOT recurring (5 loads/28d byte-stable vs daily metadata churn; post-event regeneration numerically identical). (a) The COMMON false signal (metadata churn, identical bytes) is stopped by the D3 hash tier ✔. A RECURRENCE of the 08-03 class is a genuine value change no exact comparator can eat — the designed cover is D2′ scope-defer (485K ≫ 50K → clean deferred_to_full, replacing the 180-min kill). **Epsilon-aware geometry compare (~7dp ≈ 1cm pre-compare) = the only way to EAT the class — ruled OUT of Phase B scope (once in 5 months; defer covers it); filed to review_followups.** (b) Real changes still invalidate — every layer live-proven in the ledger. Quarterly cadence STANDS (GT F12 monthly-downgrade condition not triggered: the anomaly is not upstream-recurring). Residual: 963 orphaned DB rows (in DB, absent from the new file) retain July-vintage data — noted for B1's row-set handling.
2. **Counts + bloat**: per-step table captured (agent record). Bloat RECONCILED: 0.64-0.73 and 0 were both true (1.47M dead tuples reclaimed by autovacuum + assert auto-VACUUM 08-07 22:46/23:04); the 3.7GB heap FOOTPRINT remains — duration math stays degraded-heap-based; every 08-03-class event re-accrues ~1.5M dead.
3. **Re-pins (cloud, post-Phase-1)**: audit 19/42 tripped · 0 FAIL-gated · **5,036 HIGH/MED** (was 5,318) · trio 461/3,330/121 → **0 / 2,608 / 0** · `stale_cost_fsi_without_gfa` = **0** (the plan's inflation prediction WRONG — parcel-cost already re-ran post-Phase-1 and nulled max_build_fsi consistently; fresh data, not stale).
4. **Companion check = 0/437,305 ✔** — SHIP as a new gated zero-baseline audit row in B2 (`opt_aor_without_max_gfa`): covers the 13,746 non-ravine NULL-envelope parcels the class-scoped ravine invariant is blind to.
5. **Pass-4/5 eligibility airtight for optconfig** (0 stray, 0 backlog; 407,984 = eligible exactly). **NEW pre-existing bug (RC eyeball)**: comps `resetIneligible` (:1090-1093) checks only the footprint half of its own eligibility — **2,583 parcels with lot NULL/≤0 keep live stale comp_count/comp_fsi_p50** (the D-D mirror gap; Phase 1 fixed optconfig's reset with `(lot > 0) IS NOT TRUE`, comps never got it). **FOLD into B2**: extend comps resetIneligible identically + the stale rows also pollute the unscoped comp_fsi audit stats (scope note).
6. **Post-WF3 correction map (folded into B2/B6)**: completed-UPDATE now `:542-555` (divert the `'completed'` literal `:545`) · defer break = a THIRD variable, ladder branch BETWEEN `completed_with_errors` and `budgetStopped` (both existing pins stay green; the budget line stays textually intact) · defer leaves downstream steps with NO rows (deliberate divergence from budget's 'skipped' rows — comment it) · **OK_STATUSES double-locked**: `check-chain-verdict.logic.test.ts:102` AND `run-chain-budget.logic.test.ts:79-82` (regex tail breaks on append — edit BOTH); RAN_STATUSES lock `:56` still exact · verdict green+::warning code lands in `run()` ~:181-192 beside the tripwire pattern · B6 sets BOTH envs on chain-sources (`CHAIN_DURATION_BUDGET_MINUTES` on verdict + `CHAIN_TIME_BUDGET_MINUTES` shell-computed on the chain step — Spec 115 :180-181 assigns the sources re-size to Phase B) · Spec 115 §2.2's "generic and inert without the env" sentence stays true (defer is marker-driven) — defer docs sit beside it as a DISTINCT mechanism · watchdog test's `lastIndexOf` pins: B6's window edits must not add text after the end-gate mentioning `freshness_recheck` or the verify outcome · force-full argv target `:1317` → **`:1378`**.

---
## B0 ITEM 7 — ✅ EXECUTED 2026-08-14 (§11.1 grounding; every claim below was RUN, not read) + B2/C5 v6 FOLD

**Tree fact:** branch `4fb0f5c4` carries duplicate-SHA copies of all 9 main commits — `git diff origin/main HEAD` on B2's files is EMPTY; all cites valid on both trees. **Exactly ONE commit touched B2-owned files since item 6: C1 `cdaea415`** (+7 lines at `:225`). C4/C7/envelope touched none of them.

**CORRECTED MAP (supersedes item 6's):** completed-UPDATE `:549-562`, `'completed'` literal **`:552`** · ladder **`:640-648`** (cancelled :641 · failed :642 · completed_with_errors :643 · budget :646 · verdict-warns :647 · completed :648) — **defer branch between `:643` and `:646`** · state vars confirmed (`failedStep :210` set at `:318` cancel/`:403,:410` config/`:629` step-fail; `budgetStopped :298` never sets it — locked) · AD1 `:225-235`, predicate `:231-232`, **its "sole consumer :565" self-cite is STALE — real read `:572`** (fix in the B2 edit) · chainError `:652-658` · metaObj `:661-671` (budget shape `:664-670`) · jsonb merge `:673-681` (expr `:677`) · **run-chain.js has NO exports and runs on require — B2 adds the `require.main` guard + exports `resolveChainStatus`/`parseDeferMarker` (C1 precedent)** · verdict: OK_STATUSES `:89` (+ header `:31`), green+::warning lands `:181-192`, exports `:205`, streak needs LIMIT 1→2 at `:168` · freshness RAN `:84`, lock `:54-58` · enrich `--full` argv `:1378` ✔ · **comps lot-half gap STILL LIVE, now `:1094-1097`** (mirror fix at `:1328-1334`) · budget-file pins B2 must not trip: `:33-36` (defer insertion between the pinned indices is SAFE), `:53-55`, `:61-65`; cascade lock `:44-46` safe.

**RULING 1 — AD1×defer:** **add `deferred_to_full` to the `:232` predicate.** Facts: prevChainFailed's only effect is suppressing `gateSkipped` at `:572`; `manifest.chain_gates :120-123` = `{permits, coa}` ONLY → **AD1 is behaviorally inert for sources** (dormant one-liner today); on any future gated chain, NOT matching would gate-skip the very step carrying the deferred backlog AND starve the streak detector — AD1's named failure class verbatim. Ladder: FAIL-verdicts beat defer (red audit reads red; AD1 covers the backlog); defer beats budget/verdict-warn (a WARN can't hide a defer from the streak). No cancel/budget precedence arbitration needed — control flow makes the three mutually exclusive (cancel/budget pre-step, defer post-step-break); comment + lock it (⑦b).

**RULING 2 — the ONE CONTRACT (C5×B2):** `records_meta.step_completeness = {expected, executed, died_at, skipped_gate, skipped_budget, deferred_at}`. **v5's separate `deferred_step` chain-meta key is RETIRED** (two carriers of one fact drift). B2 ships first and creates the object with `deferred_at`; C5 extends the same writer. `classifyStepCompleteness` gains a MANDATORY defer arm (defer sits INSIDE OK_STATUSES so completeness IS consulted on deferred rows): `status='deferred_to_full' ⟺ deferred_at present` (both-or-neither = drift tripwires both directions); missing step rows legitimate iff at-or-after `deferred_at` in manifest order. `died_at = wasCancelled ? null : failedStep` (the `:318` cancel setter the C5 text didn't name). Streak detection reads `step_completeness.deferred_at` over the last-2 chain rows. D3(iii): `:244` `::notice`→`::error` (exit gate already `:256-260`); the `:248-254` race-guard notice UNCHANGED; watchdog race guard is `pipeline-watchdog.yml:191-193` (C5's :190-192 was off-by-one).

**RULING 3 — status-vocab census for `deferred_to_full` (B6 scope WIDER than v5 says):** B2 edits the two scripts + BOTH OK_STATUSES locks (verdict.logic `:101-103` + budget.logic `:78-82` — both break on append, both edited in the B2 commit) + RAN lock. B6 owns: `stats/route.ts:321` hard IN-list (deferred chain currently invisible) · **`FreshnessTimeline.tsx` FOUR hard-coded status lists (`:326,:478,:615,:737`) + color maps (`:965,:977`)** · `DataQualityDashboard.tsx:128`. ⑦e locks the producer-gate exclusion structurally (`source-version.js:91` filters `status='completed'` — a defer can never satisfy a producer gate).

**CLOUD STATE:** mig 238 ✅ APPLIED (operator item 2's apply half DONE; next-nightly proof pending) · **deep_scrapes `pipeline_schedules` row STILL '(3x Daily)' `0 15,18,21 * * 1-5' — OPERATOR ITEM 1 STILL OPEN** · chain-sources workflow still `disabled_manually` ✔ · mig 240 objects NOT on cloud (expected — rides B2's deploy) · advisory lock 44 now TAKEN (C3) — B2 plans no new lock, no collision.

**RED-FIRST SUITE (tests-in-plan per §11.4; ⓔ = net-new capability, cannot red behaviorally — export/row-absence is the diagnostic, lock proven both directions post-impl per §11.3):**
| # | Case | Mode | Red? |
|---|---|---|---|
| ① | Massing gate NULL-arm; fires on linked_at only (P1 null-stamp IN, P2 newer-linked_at IN, P3 older OUT) | db.test, exported scope-builder | ⓔ |
| ② | Zero-link ghost WARN row (count 1; day-one live 0) | audit pass | ✓red row-absence |
| ③ | Scope cascade: crashed run A's unconsumed rows unioned by run B, flipped consumed, in-txn; retry bound excludes beyond-bound | db.test | ⓔ |
| ④ | Decision scope keyed on linked_at (late-backfilled decision_date caught) | exported SQL | ✓red/ⓔ resolve at impl |
| ⑤ | Force-full pins: env OR'd at `:1378` (✓red regex) · `isFullMode` does NOT match it (g/b — the link_parcels fence) · cost gate own var | logic.test | ✓red (a) |
| ⑥ | `opt_aor_without_max_gfa` zero-baseline row (B0 item 4's 0/437,305) | db.test | ✓red row-absence |
| ⑦a | Citywide scope → clean defer marker `{step, scope_count, threshold, ratio}`, exit 0 | child stdout | ⓔ |
| ⑦b | Ladder precedence + step-row rewrite diverts `:552` + no `failedStep=` in defer block + `deferred_at` written | pure `resolveChainStatus` + locks | ⓔ |
| ⑦c | **Verdict green+warning on `deferred_to_full` — THE FLAGSHIP TRUE RED: `classifyVerdict` (already exported) returns ok:false TODAY** | pure | **✓red** |
| ⑦d | RAN_STATUSES 4-element set | lock edit | ✓red |
| ⑦e | Producer-completed gates exclude defer rows | `source-version.js:91` via client | g/b |
| ⑧ | Defer-streak → exit 1 ("supervised force-full required"); mixed pair ok | pure `classifyDeferStreak` + LIMIT 1→2 lock | ⓔ |
| ⑨ | Geometry change NULLs both stamps | loader fence path db.test | ✓red massing half |
| ⑩ | AD1 disjunct present (regex `:232`) + g/b companion: `chain_gates` keys = exactly `['permits','coa']` (dormancy documented; re-ruling forced if sources gains a gate) | logic.test | ✓red |
| C5-1..4 | died_at-on-green fails · gate-skip/budget/advisory-SKIP all PASS (informational fields never consumed by verdict) | pure `classifyStepCompleteness` | ⓔ |
| C5-5 | Absent field → `{ok:true, annotate:true}` + §4.9 pair (annotate row w/ live count + naming threshold; `_retighten` INFO w/ machine-observable flip; flip = operator follow-up commit filed in-commit) | pure + rows | ⓔ |
| C5-6 | Defer arm: legit-incomplete PASS · `completed`+deferred_at FAIL · defer w/o deferred_at FAIL | pure | ⓔ |
| C5-7 | D3(iii) `:244` `::error` (✓red — says `::notice` today) + g/b: `:248-254` race notice unchanged | logic.test | ✓red |
Every ✓red case must red at the DESIGNED assertion, never a missing import.

**Next per §11.2: fold-validation (Idempotency Lens + Cross-read Adversary) on THIS fold, then operator y/n on the B2+C5 combined implementation.**

## ⚖ FOLD-VALIDATION VERDICTS + v6.1 CORRECTIONS (2026-08-14 — Idempotency: NOT READY on B-1 · Cross-read: FOLD DEFECTS ×6 · ALL FOLDED BELOW, supersedes v6/v5 where conflicting)

**⛔ B-1 (BLOCKING, folded) — DEFER DECISIONS ARE PRE-TRANSACTION ONLY.** Pass-2's natural scope is knowable only in-txn (`ON COMMIT DROP` temp table at `:1451-1465`) — an implementation that commits pass 1 and defers before pass 2 stamps zoning watermarks that permanently hide those parcels from passes 2-4: the founding silent-staleness class inside the mechanism built to kill it. **PIN: every defer decision in enrich-parcels is made BEFORE `withTransaction`, from upfront-computable predicates (pass-1 stale-zoning + D1′ massing + D4′ decision-scope; passes 2-4 conservatively bounded by pass-1 ∪ massing). Any hypothetical in-txn defer THROWS → rollback. ⑦a additionally asserts the deferring run wrote ZERO watermark stamps and ZERO scope-table rows.** Per-pass ratio rows for 2-4 are post-hoc telemetry, never the defer trigger.
**S-2 (folded):** the pass-5 `consumed_at` flip has NO txn to share (autocommit 500-row batches, `flushOptConfigBatch:1284-1312`) — **PIN: work-before-stamp (flip per-batch in the batch's own statement, or once after the stream completes); NEVER flip-before-stream.** Fixture ③ gains: after a simulated mid-stream crash, the unprocessed remainder's `consumed_at IS NULL` (as drafted, ③ passes GREEN against the broken implementation).
**S-3 (folded):** the C5 `_retighten` flip ("zero absent across all five chains") is STRUCTURALLY UNREACHABLE until B6 re-enables chain-sources — the threshold text MUST name that dependency; the absent-count query is PINNED to latest-row-per-chain (legacy rows in a lookback window would hold it nonzero forever — the silent-relaxation drift §4.9 exists to prevent).
**X-2 (folded) — the tripwire collision:** my `deferred_to_full ⟺ deferred_at` biconditional is violated by my own ladder (FAIL-verdicts beat defer → a defer-then-FAIL run legally carries `deferred_at` under `completed_with_errors`). **RESTATED: the ⟺ tripwire is scoped to OK_STATUSES rows** (C5-6's fixtures already encoded the narrow form). **DECLARED semantic change from v5:** streak detection keys on `deferred_at`, so a defer-that-also-FAILed COUNTS toward the streak — adopted deliberately (it IS a real defer; hiding it behind the red would starve the escalation).
**X-3 (folded) — rollout stated:** **ONE combined B2+C5 commit.** The annotate window opens at its deploy and closes after one full cycle of all five scheduled chains — which structurally means after B6 (S-3); the §4.9 follow-up entry is filed in THAT commit. §C5's "in the C5 commit itself" = this commit.
**X-1 (folded) — tree-fact corrected:** the branch carries CHERRY-PICK PAIRS of the 9 main commits (patch-identical, DIFFERENT SHAs — e.g. C1 = `67663a81` branch / `cdaea415` main), not "duplicate SHAs". And "cites valid on both trees" holds ONLY for the three B2-owned scripts — **`source-version.js` (+ its lock) is BRANCH-ONLY (B1 `0b230472`)**; ⑦e's target does not exist on main.
**X-5 (folded) — suite upgrades:** ⑧ SPLIT: the `LIMIT 1→2` source lock at `:168` is ✓red TODAY (same class as ⑦d); only `classifyDeferStreak` is ⓔ · **⑦a and ③ UPGRADED to behavioral reds** via the C1 child-process pattern (seed small scope + low threshold in logic_variables → run enrich-parcels as child → marker absent in stdout / `consumed_at` never flipped = red at the designed assertion TODAY).
**X-6 (folded) — B6 census +1:** FreshnessTimeline has a FIFTH hard-coded status site — the per-step "Status:" text-color ladder at **`:1356-1363`** — and B2's step-row rewrite means a deferred STEP renders "Deferred_to_full" in default gray there: exactly the "default styling" v5:25 forbids. B6 scope: five sites + two color maps + DataQualityDashboard `:128` + stats route `:321`.
**N-6 (folded):** the defer arm WRITES `chainError` — `"Deferred to full at step <slug> (scope N >= threshold T)"` — in the `:652-658` ternary (the budget precedent: meta-alone is invisible on the admin surface until B6).
**N-5 (folded, documented behavior):** the pass-5 threshold count INCLUDES unioned unconsumed prior scope — a crashed run's leftover can push the next run over threshold (two crashes → streak red). Correct (it is the true work size); now stated in D4′.
**P-7 (recorded):** backfill ceiling re-verified LIVE on cloud 2026-08-14: parcels 496,422 = 485,135 pb-distinct + 11,287 no-pb (exact partition) · statement_timeout '2min' confirmed · 10 batches used, 2 spare (23.7% margin) · cloud heap dead-tuple-free · completeness gate RAISEs on under-coverage.
**STALE v5/§C5 LINES — STRUCK (do not follow):** v5:59's "`deferred_step` meta"/"compared on `deferred_step`" (→ `step_completeness.deferred_at`), its "`:506-516`" (→ `:549-562`/`:552`), its late "`:1317`" (→ `:1378`) · v5:27's two "`:1317`" cites incl. DS5's target (→ `:1378`) · v5:25 "both admin renderers, distinct chip" (→ the X-6 census) · v5:5's lock cite `:101-102` (→ `:101-103`) + ownership list gains `run-chain-budget.logic.test.ts` + `chain-cascade.integration.test.ts` (must-not-trip) · v5:46 Spec Updates gains **Spec 48 §3.7 (step_completeness contract) + §4.9 (annotate pair)** · `.cursor/active_task.md` §C5's "`:645-661`"/"`:664-670`" (→ metaObj `:661-671`, merge `:673-681`) · in-commit comment strikes: `check-pipeline-freshness.js:82` + `stats/route.ts:304` both cite the dead "run-chain.js:584-589" ladder.

**FOLD-SET CROSS-READ (the 12 corrections pairwise): B-1's pre-txn pin × ⑦a's behavioral upgrade — compose (the child-run fixture asserts both the marker AND zero-stamps) · S-2's flip ordering × N-5's leftover-counts-toward-threshold — compose (unflipped remainder is EXACTLY what the count should include) · X-2's OK-scoped tripwire × C5-6 fixtures — already aligned · X-3's one-commit × S-3's B6-gated flip — consistent (window opens at deploy, closes post-B6, text says so). No collisions found.**

> **PLAN LOCKED (B2+C5 v6.1). Operator y/n authorizes: the combined single commit (defer mechanism + step_completeness one-contract + fence extension + per-pass telemetry + force-full envs + comps lot-half fix + the 21-case suite red-first-where-possible) followed by B3-B8 per v5.**

**SPEC-LAYER LANDED 2026-08-14 (agent-implemented, code untouched):** Spec 40 §3.1.2 (status vocabulary — BUILT FROM SCRATCH, no such section existed; the amendment target was fiction) · Spec 47 §8.7 (defer marker + B-1 pre-txn rule) · **Spec 48: the completeness contract is §3.9, NOT §3.7 (occupied by first-deploy spikes) — every §3.7 cite for step_completeness in this file and §C5 reads §3.9 now** · §4.9 pair instance w/ B6 dependency · Spec 115 §2.5 4-status set · Spec 43 defer lifecycle · seed `enrich_parcels_defer_threshold_rows` (50000; registry regen 419 vars; consumer "—" until B2's code lands, precedent road_overlay_distance_m) · dead-ladder comment cites fixed in freshness `:82` + stats route `:304`.

**PRE-REVIEW SELF-CHECKLIST (B2+C5, walked against the actual diff 2026-08-14):**
1. Defer decisions PRE-TRANSACTION only (B-1) — `computeDeferScope` runs before `withTransaction`; deferring runs write zero stamps/scope rows (⑦a pins, now behaviorally green) — PASS
2. One contract: `step_completeness` all 6 fields, written every run, `deferred_at` only on defers; the separate `deferred_step` key never shipped (Ruling 2) — PASS
3. ⟺ tripwire OK-scoped (X-2); completeness consulted only inside OK_STATUSES; streak keys on `deferred_at` (declared semantic change) — PASS (C5-6 + ⑧ green)
4. Ladder precedence: FAIL>defer>budget/warn; no `failedStep` from defer; budget locks 9/9 + cascade 6/6 untouched-green — PASS
5. AD1 disjunct + stale cite fixed; dormancy documented; ⑩ green both halves — PASS
6. All four consumer hops: OK_STATUSES (+both locks same-commit), RAN (+lock), ::warning, producer-gate exclusion (⑦e) — PASS
7. S-2 flip ordering (work-before-stamp) implemented in `consumePendingScope`; ③ green incl. the crash clause — PASS
8. Registry-bounds discipline held under pressure: the implementer REFUSED to weaken the Zod floor to satisfy a stale test guess; the fixture came to the spec (threshold at legal floor + 1,001-row scope) — PASS, and the incident is the §11 posture working
9. Spec truth FIRST (40 §3.1.2 new, 47 §8.7, 48 §3.9/§4.9, 115 §2.5, 43) + registry + emitMeta declarations — PASS
10. Migration 242 (geom-invalidation trigger, net-new beyond the plan's file map — required because ⑨ bypasses the loader) — validated + container-applied; FLAGGED to the output Guardian for the C6 trigger-scope lesson check — PASS pending Guardian
11. Acceptance 144/144 · battery exit 0 · typecheck 0 · lint clean — PASS

**✅ B2+C5 COMMITTED `e8793c8f` (2026-08-14) + Spec 118 `6a7d2241` (spec cherry-picked to main; B2+C5 BRANCH-ONLY per the deploy contract — migs 240/242 not on cloud; arrival = B7/B8).** Output-panel round: 2 blind-converged criticals + 9 items, ALL landed test-locked; footgun gate caught 2 DB-clock slips + its own text-matching quirk (recorded). NEXT: B3 (run-ledger gates) onward per v5; the deep-scrapes refresh_snapshot WF3 (Spec 118 §7) awaits the operator's interim decision — next slot MONDAY (weekday cron), no weekend urgency.

## ⚖ B3 GROUNDING FOLDED (2026-08-15 — headline corrections; full report in the grounding output)
* **CORRECTED: "heritage skip" = `enrich-heritage.js`** (NOT load-heritage, which B1 already gated): watermark written (`:191`) never read; port the ravines-#418 Layer-1 skip. **⛔ THE WEDGE-OPEN TRAP (live-grounded): 16 invalid-geom parcels are excluded from stamping by ENRICH_SQL's `ST_IsValid` (`:143`) FOREVER — a verbatim-ravines probe returns 16≠0 and NEVER SKIPS (dead gate, green suite). The probe MUST mirror the enrich SQL's full eligibility.** Probe costs 69.9s cold — carry an index ruling (partial btree on the watermark WHERE geom IS NOT NULL) or accept explicitly.
* **CORRECTED: `link_wsib` dual-chain = `sources` + `permits`** (manifest :78/:104; ZERO `entities:link_wsib` rows ever) — v5:60's "entities" refuted. Own slug set: `{sources:link_wsib, permits:link_wsib, link_wsib, link-wsib}`.
* **REFUTED fixture premise:** the cost step now HAS a completed run (id 2389, 08-07, 83,488 updated — Phase 1's scoped completion). No-completed-ever arm = seeded fixture only; stop citing the live instance.
* **Gate SQL drafted + EXPLAIN'd** (own-last CTE + ANY-status-since + completed-with-changes; SKIP iff own_completed ∧ non_completed=0 ∧ changes=0): cost ≤121, ~5ms — free. `deferred_to_full` excluded structurally (≠'completed') + explicit fixture. Notes ruled INTO the plan: `'skipped'` rows ⇒ RUN (first post-scoped-run never skips — fail-safe, stated) · link_wsib cross-chain overlap → upstream predicate `COALESCE(completed_at,'infinity') > own_started` · **cost step gains canonical ISO `rates_as_of`/`index_updated_at` meta keys SAME commit** (today's only version signal is a `Date.toString()` blob) · **wsib invalidation = links are MONOTONE** (nothing ever re-nulls `linked_entity_id`; load-wsib upsert never touches it) — stated in gate comment + spec.
* **Net-new confirmed:** wsib partial index `WHERE linked_entity_id IS NULL` (only the OPPOSITE predicate exists, mig 044); tier CTEs already carry the predicate (lock, not code). **`link-parcel-addresses.js` + `link-wsib.js` execute on require** — both gain guard+exports (I1 ✓red).
* **RED-FIRST CASES (in-plan):** G1 no-completed⇒RUN(ⓔ) · G2 any-status⇒RUN incl. running/failed(ⓔ) · G3 skip-iff-all-0-change(ⓔ) · G4 defer-row⇒RUN(ⓔ) · G5 skip-emits-summary/DS4(ⓔ child) · W1 dual-chain slug lock(+no-entities g/b) · **W2 partial index ✓red** · W3 monotone-link lock · **H1 invalid-geom trap ✓red (the naive port fails it)** · H2 version-bump⇒RUN(ⓔ) · H3 skip-emits(ⓔ) · H4 L14-halt-on-skip-branch(ⓔ) · **C1 ISO-keys ✓red** · C2 rate-bump⇒RUN(ⓔ) · C3 force-full env(ⓔ) · **I1 importable ✓red ×2** · X1 must-not-trip battery (20 link-pa pins, cost db×5, chain.logic order pins, source-version lock — gate lands as `runLedgerGateDecision` sibling export in source-version.js = deliberate same-commit lock edit).
**Next: ONE fold-validation pair covers THIS fold + the WF3 fold (cross-workstream cross-read included — shared surfaces enumerated), then operator y/n on both.**

## ✅ JOINT FOLD-VALIDATION (2026-08-15) — B3 corrections FOLDED (verdict: FOLD DEFECTS → all resolved here)
* **B5: I1's pre-impl red = SOURCE-SCAN (guard-absent regex, the ⑦b precedent) — NEVER a literal `require()`** (an unguarded `pipeline.run` in a shared vitest worker attempts a real pool + process.exit — the exact fenced hazard).
* B6 labels: **C3 → ✓red source-regex** (same class as ⑤a); **H1 mode stated:** pre-impl = textual mirror-lock (probe SQL must contain the ST_IsValid eligibility terms), post-impl = the adversarial invalid-geom fixture.
* B7: the source-version.js sibling export is ADDITIVE — no existing lock breaks; don't hunt for one.
* **T2: pipeline_runs fixture discipline:** `runLedgerGateDecision(slugSets, …)` takes slug sets as PARAMETERS; suites seed FX-prefixed slugs + DELETE-by-prefix.
* Sequencing: B3 lands AFTER WF3 (re-grep manifest `:78/:104` post-F2 — line drift expected).
