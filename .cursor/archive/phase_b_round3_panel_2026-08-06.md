# Round-3 PLAN panel record — Phase B v4 (2026-08-06, lean 4-agent roster)

Roster: Integration+Schema · Reality-Check · Compliance+Ground-truth · Adversarial+Observability. Target: the v4 REMEDIES (round-2-verified numbers settled). Verdicts: SOUND-WITH-CORRECTIONS / SOUND-WITH-CORRECTIONS / NOT-READY / NOT-READY. Full agent texts in the session transcript.

## BLOCKER
**R3-B1 (TRIPLE-confirmed: Adv F1, Int F1, GT F1) — the folded epoch-day parity formula is arithmetically wrong.** `floor(epochDays/14) % 2` puts TWO consecutive Sundays in every 14-day bucket (Sundays ≡ 3 and 10 mod 14) → on,on,off,off / 21-and-7-day gaps — breaks the cadence, the "bi-weekly + slack" watchdog window, and the anchor note at once. Anchoring does not fix it. The round-2 panel MINTED this remedy; the fold transcribed it faithfully — folded remedies are claims to re-verify, not settled facts (process lesson). CORRECT FORM: parity of weeks-since-epoch — `floor(epochDays/7) % 2 === ANCHOR_PARITY` (2026-08-09 = epoch day 20674, 20674/7=2953 → parity 1 = ON). RED TEST: strict alternation across ≥8 consecutive Sundays spanning Dec 2026→Jan 2027 (the year-boundary-only fixture PASSES the broken formula).

## CRITICAL
**R3-B2 (Adv F2 + Int F2 + GT F2) — the defer lifecycle is undesigned: status fork unchosen, build surface unnamed, no loop breaker, no nag.**
- Consumers verified: `check-chain-verdict.js:89` OK-allowlist {completed, completed_with_warnings} — novel status = RED; `check-pipeline-freshness.js:84` RAN_STATUSES — novel status = ABSENT (watchdog red); mapping onto completed_with_warnings = verdict green + watchdog green = **silent citywide staleness up to ~13 weeks** (the founding failure class). Both exact-pin test locks verified: `check-chain-verdict.logic.test.ts:101-102`, `check-pipeline-freshness.logic.test.ts:56`.
- Step→orchestrator channel: exit code + PIPELINE_SUMMARY only; "clean stop at step boundary" = NEW `run-chain.js` plumbing (parse `records_meta.deferred_to_full` → step row status, break loop, new chain-status arm :597-602). `run-chain.js` is in no step's file list.
- No max-defer-count red light; quarterly DNF (360-min rollback) → re-defer next quarter; the extended fence firing on a GENUINE citywide redraw = permanent defer loop, unsignalled (Adv F3 — the single most dangerous residual).
- A deferred STEP row must not satisfy B3's "completed with 0 changes" gates (wedges cost forever otherwise).
- `pipeline_runs.status` is unconstrained TEXT (mig 033 + live) — no migration needed for the new value; admin renderers (`FreshnessTimeline.tsx:325-982` region, `DataQualityDashboard.tsx:125-134`) enumerate statuses and need the addition.
- Spec surface: 40 (status vocabulary) + 47 (a third ending) + 115 §2.5 (RAN/watchdog) + 43 + 48; rides B2's commit (behavior ships there), B6 documents variant/cadence only (GT F7).

## HIGH
- **R3-B3 (Adv F4 + GT F5):** the mid-step soft-budget claim contradicts pass-1-4 single-txn atomicity — `d6eb9f31` transfers only to per-item-commit loops (verified via git show); a mid-step stop in enrich_parcels rolls back ALL work (DNF loop) or strands silently. DELETE the mid-step claim; scope-count-then-defer at step boundaries is the mechanism (defer happens at the CURRENT boundary — before work).
- **R3-B4 (Int F3):** `pipeline.isFullMode()` is argv-only (:712-714) and **`enrich-parcels.js:1317` reads argv directly**; `compute-coa-cost-estimates.js:130` likewise; cost has no mode at all. Env-in-isFullMode accidentally flips `link_parcels` (:133). RULING: per-script env vars (`ENRICH_PARCELS_FORCE_FULL` OR'd at :1317; the new cost gate honors its own var) on the `LINK_MASSING_FORCE_FULL` precedent; DS5's red test targets :1317, not isFullMode. **Quarterly must ALSO set `LINK_MASSING_FORCE_FULL=1`** (Adv F6: the massing gate vetoes on unchanged count/code_version regardless of `--full` — without this, heights-only staleness is UNBOUNDED, not quarterly-bounded). Force env step-scoped in the yml.
- **R3-B5 (Adv F7):** parity test is code-vs-code — the LIVE cloud row escapes it (the deep_scrapes drift class, live today). Add a runtime hop: the verdict step (or B7 checklist) asserts the cloud `pipeline_schedules` row for chain_sources against the workflow's own cron/cadence constants.
- **R3-B6 (GT F3+F4):** DS-8 predecessor gate has NO performing step; and Phase 1's NULLs make RC3's invariant VACUOUSLY satisfiable (audit :128 applies only when max_buildable non-NULL — NULLed parcels leave the population; stale opt_aor persists with nothing re-firing it (pass-5 confidence already set; no scope arm reaches them)). B0 gains explicit line-items: companion check `opt_aor_gfa_sqm IS NOT NULL AND max_buildable_gfa_sqm IS NULL`; re-baseline 14/32 · 5,318 · 461/3,330/121 post-Phase-1 (`stale_cost_fsi_without_gfa` inflates by construction); pass-4 eligibility under NULL envelopes.
- **R3-B7 (Adv F8 + Int F8):** materialized scope table needs run_id keying + consumed marker (crash between scope-write and pass-5 read orphans the scope; next run's empty scope overwrites it — D4′'s pathology recurs through the remedy); pass 5 unions unconsumed scopes; cleanup rule for failed runs; growth bound. Concrete shape (Int): permanent table via migration, RLS Class-B (mig 227 pattern), backup-db EXCLUDED_TABLES ruling; chain advisory lock + enrich's own xact lock serialize access.
- **R3-B8 (RC 1, corrections):** NO per-pass duration telemetry exists (one timer :1318→:1530) — B2 adds per-pass duration audit rows BEFORE thresholds can be validated; thresholds labeled projection until B7. Proposed initial threshold: **50,000 rows/pass** (evidence: full 5-pass citywide 46.5-53.0 min; pass-1-only citywide 17.75 min; link_massing full relink 485,135 rows measured 3× at 14.7-21.9 min; normal decision scope ≈3,461/14d; even 5 simultaneous threshold-hits ≈8-10 min vs 180). Emit scope_count/threshold/ratio EVERY run + WARN ≥80% (Adv F11). Founding-incident row 2179 confirmed verbatim (180-min kill, SIGTERM handler didn't fire).

## MED / corrective
- Adv F9 + Int F9: scan-all-attempts — the no-completed-run-ever arm (NULL window → vacuous skip → skip forever on exactly the starved class): fail-safe RUN + fixture. Dual-chain slugs: `link_wsib` in BOTH entities (:78) and sources (:104); gates enumerate scoped slugs (massing-full-gate :43 IN-list precedent). pipeline_runs 2,328 rows — query trivially cheap, index-supported.
- Adv F10 + GT F10: skip-staleness assertion → a chain-TAIL assert step (runs on the defer path too; inside gated steps it's dormant exactly during a defer loop); per-step expected-cadence table homed in logic_variables; WARN at 2× / FAIL at 4× (WARN-only never reaches GitHub through the green allowlist); a defer does NOT reset the age (it attempted, did no work).
- GT F6: IMMEDIATE OPERATOR ITEM 1 names executor + vehicle: operator-executed via `apply-migrations.yml` migration or documented runbook one-off — never an ad-hoc prod UPDATE.
- GT F8/F9: B6's cross-domain answers INLINED (not "as v3"); Scenario-A schedules-PUT contract note lands IN the plan; stats-route 192h change gets its own exhaustive-or-throw ruling; §11 N/A items stated per-item. Self-Checklist moves BEFORE B7 (workflows.md order); UI Regression command named in B6.
- GT F12: "self-stops at the NEXT step boundary" → CURRENT; D1′ staleness bounds restated if B0 forces monthly.
- Int F4/F5/F6/F7: backfill premise corrected (0 multi-linked_at parcels today — one full relink stamped all; keep MAX as future-proofing); backfill BATCHED (~50K/batch — migrate.js has NO statement_timeout override; single-statement dies at 2 min); aggregate itself 244 ms. Indexes: NO new pb index (probe index-supported; full seq ~1s); ONE partial `ON parcels(id) WHERE massing_enriched_at IS NULL` (~11.3K entries day one — the zero-pb backfill-NULL set, a bounded first-run scope). Fence: do NOT fence `zoning_base_source_dataset_version` (provenance-only, no consumers; truthful even while stamp NULL); consumers survive (enrich-permits halts only on 100% wipe; Spec 58 §9 reads records_meta, not stamps). Fence×defer interaction stated: citywide redraw → citywide NULL → defer → the breaker + supervised force-full IS the designed path.
- RC 2-6: candidate-join multiplier UNMEASURED (3 spatial attempts timed out) — open number, B7 measures via EXPLAIN ANALYZE; direct-decision component ≤3.9%/window (bound is meaningfully tight). Zero-link-ghost day-one count = **0** (clean). Heights-only churn measured real-but-negligible: 4/427,077 per run ("accepted, documented" fair — but see R3-B4's quarterly-doesn't-relink correction for why the bound needed fixing). Bi-weekly pass-4/5 scope ≈5-10K rows ≪1 min. Defer worst case = **92 days** staleness (quarterly gaps 90/91/92/92) — an accepted operating cost the operator must knowingly ratify. `parcels` BLOAT: dead_ratio 0.64-0.73 (~1.2-1.3M dead tuples) — all ceiling math measured against a degraded table; add autovacuum/bloat check to B0/B8 sweep. parcels now 496,422; `parcels-columns.snapshot.json` + `parcel-lookup.infra.test.ts` named as deliberate edits for the new column.

## Confirmed sound (no action)
Scope-table mechanism necessary (temp tables ON COMMIT DROP :250/:417/:768/:945; pass 5 post-commit :1405-1408; no cascade arm in :1093) · fence extension mechanical at load-parcels :341-361 · scan-all-attempts index-supported · assert_staleness precedent valid · lessons :99/:100/:102 honored · all round-2 restorations present in v4 text · disposition ledger reconciles · Spec 58 FROZEN §9 NOT violated by a defer (its only freshness bound is the 730-day force-reload) — the strained promise is Spec 115's · epoch-day ISO-week-53 diagnosis itself correct (2027-01-03 = W53) · D2′ chain_args citation exact · every enrich-parcels line citation exact.
