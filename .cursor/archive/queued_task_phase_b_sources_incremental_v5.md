# Active Task: Phase B — Sources Incremental Architecture (WF2)
**Status:** Planning — **v5**, rewritten 2026-08-06 after the ROUND-3 lean panel found v4 not-ready (records: `.cursor/phase_b_round3_panel_2026-08-06.md` · round 2 `phase_b_round2_panel_2026-08-06.md` · round 1 `phase_b_panel_record_2026-08-05.md`; supersedes `queued_task_phase_b_sources_incremental_v4.md`, `_v3.md`). Round-3 headline: the round-2 panel itself minted a wrong remedy (the parity formula) that v4 folded faithfully — **folded remedies are re-verified claims from now on.**
**Domain Mode:** **Cross-Domain.** B6 touches `schedules/route.ts`, `stats/route.ts`, `ScheduleEditModal.tsx`, `DataQualityDashboard.tsx`, `FreshnessTimeline.tsx`, `funnel.ts`, `chain.logic.test.ts`. **B2 owns (R3-B2)** `scripts/run-chain.js`, `scripts/check-chain-verdict.js`, `scripts/check-pipeline-freshness.js` and their exact-pin locks `check-chain-verdict.logic.test.ts:101-102`, `check-pipeline-freshness.logic.test.ts:56`.
**Workflow:** WF2 per `.claude/workflows.md:67-95` — **Self-Checklist runs BEFORE the B7 review panel** (order restored).
**Design authority:** `docs/reports/2026-08-04-sources-incremental-architecture.md`, corrected by three panels.
**Predecessor gate (DS-8) — now a PERFORMED step:** B0 items 3-5 below re-verify this plan against Phase 1's post-fix contract. Phase 1 (max-build v4) runs first; nothing here lands between Phase 1's steps 13-14.
**Rollback Anchor:** `b4351297`. **Pre-Flight:** `node scripts/ai-env-check.mjs` before B0.

## IMMEDIATE OPERATOR ITEMS (operator-executed; not part of this task's authorization)
1. **Cloud `pipeline_schedules.deep_scrapes` row** still 'Weekdays (3x Daily)' — fix via an `apply-migrations.yml` migration or the documented runbook one-off procedure; **never an ad-hoc prod UPDATE** (vehicle restored per round-3 GT F6).
2. Migration 238 cloud apply + next-nightly proof (carried).

## Context
* **Goal:** unchanged — make every step work only when inputs changed, AND make the runs where inputs DID change survive. Founding incident re-confirmed verbatim (run 2179: 180-min kill, SIGTERM handler never fired). `enrich_parcels` is literally step 21 of 27; steps 22-27 are the cost + assert + snapshot set.
* **Target Specs:** as v4 **plus Spec 40 (status vocabulary), Spec 47 (a third step ending), Spec 115 §2.5 (RAN/watchdog semantics)** — the `deferred_to_full` documentation rides **B2's commit** (behavior ships there); B6 documents only variant/cadence (GT F7).

### GT3 constraint + DS4 condition — unchanged from v4 (`assertCentrelineEnriched` at `enrich-permits.js:301-332`).

## Operator rulings
* **D1′ (corrections folded):** `massing_enriched_at` vs `parcel_buildings.linked_at` via `IS NULL OR EXISTS(linked_at > watermark)`. Backfill = per-parcel `MAX(linked_at)`, **BATCHED (~50K id-range batches — `migrate.js` has no statement_timeout override; a single statement dies at the 2-min default)**; premise corrected: today 0 parcels have >1 distinct linked_at (one full relink stamped all) — MAX is future-proofing, not a day-one fix. **Indexes RULED (Int F6):** NO new parcel_buildings index (probe is index-supported; measured trivial); ONE partial index `ON parcels(id) WHERE massing_enriched_at IS NULL` (day-one ≈11.3K entries — the zero-pb parcels backfill NULL and form a bounded first-run scope). Zero-link-ghost WARN row: day-one value **0** (measured) — forward-looking. Heights-only staleness: measured real-but-negligible (4/427,077 per run) and **bounded by the quarterly ONLY IF the quarterly also sets `LINK_MASSING_FORCE_FULL=1`** (see D2′ — without it the massing gate vetoes on unchanged count/code_version and the bound is false, R3-B4/Adv F6).
* **D2′ — escalation, defer lifecycle DESIGNED (closes R3-B2/B3):**
  - **Scope-count-then-defer:** each gated step computes its scope BEFORE working; over threshold → the chain stops cleanly at the **current** step boundary with outcome **`deferred_to_full`** (a NEW `pipeline_runs.status` value — TEXT column, no migration needed). **The mid-step soft-stop claim is DELETED** (R3-B3): enrich-parcels passes 1-4 are one transaction; `d6eb9f31` transfers only to per-item-commit loops. Pre-step counting is the whole mechanism.
  - **Status fork CHOSEN (R3-B2):** `deferred_to_full` is a distinct status. Consumer classification, all four hops: `check-chain-verdict.js` treats it as **green + a `::warning` GitHub annotation** (deliberate edit of `OK_STATUSES` + its exact-pin lock) · `check-pipeline-freshness.js` counts it as **RAN** (absence detection; deliberate edit of `RAN_STATUSES` + lock) · both admin renderers gain the status (distinct chip, not default styling) · **a deferred STEP row does NOT satisfy B3's "completed with 0 changes"** (gates exclude the status explicitly).
  - **Loop breaker + nag (R3-B2/Adv F3):** a **defer-streak rule** — 2 consecutive `deferred_to_full` on the same step ⇒ the verdict step exits 1 (red) with an explicit "supervised force-full required" message; plus a stated staleness ceiling (one missed bi-weekly window) after which the operator dispatches the 360-min variant. The fence×defer interaction is thereby the designed path: citywide redraw → citywide NULL → first run defers with a warning → second would-be defer goes red → supervised force-full.
  - **Force-full channel (R3-B4):** per-script env vars — `ENRICH_PARCELS_FORCE_FULL` OR'd into `enrich-parcels.js:1317`'s argv check (NOT into `pipeline.isFullMode()`, which would flip `link_parcels`); the new cost gate honors its own var; **the quarterly dispatch sets `LINK_MASSING_FORCE_FULL=1` as well**; force envs are step-scoped `env:` on the chain step only (no `$GITHUB_ENV` leak to the verdict step). DS5's red test targets `:1317`.
  - **Thresholds (R3-B8, evidence-based):** initial **50,000 rows/pass**, stored in `logic_variables` — 10-15× measured-normal (decision scope ≈3,461/14d), ~9× under measured-escalated (full 5-pass citywide 46.5-53 min; full relink 485K = 14.7-21.9 min ×3 runs). **Prerequisite: B2 adds per-pass duration audit rows** (today one timer covers all five passes — thresholds are a labeled projection until B7 measures). **Every run emits `scope_count / threshold / ratio` per pass + WARN at ≥80%** (the operator sees the cliff coming, Adv F11).
  - Quarterly mechanics: as v4 (OR'd cron DOM/DOW in-job date check; job ceiling moves; quarterly Sunday never parity-skipped; first force-full dispatch-supervised). **Accepted operating cost, stated for ratification: worst-case ~92 days staleness** if a release lands just after a quarterly and no operator dispatches sooner.
* **D3:** as v4 (stream-hash, tier-1.5, stated assumption; 30-40 min floor is a projection).
* **D4′:** as v4 (linked_at-keyed decision scope; bounded comp_count=0 retry — direct component measured ≤3.9%/window; **the candidate-join multiplier is an OPEN number — B7 measures it via EXPLAIN ANALYZE on the first real run**; scope MATERIALIZED) with round-3 semantics (R3-B7): scope table **keyed by run_id + a consumed marker**; pass 5 unions **unconsumed** prior scopes (crash between scope-write and pass-5 read no longer orphans the set); cleanup rule for failed runs; retention bound. Concrete shape (Int F8): permanent table `enrich_parcels_pass3_scope` via migration + **RLS Class-B (mig 227 pattern)** + a **backup-db `EXCLUDED_TABLES` ruling**; DELETE+INSERT in-txn; chain lock + enrich xact lock serialize access.
* **Parity (R3-B1 — the triple-confirmed blocker):** the guard is **`floor(epochDays / 7) % 2 === ANCHOR_PARITY`** — parity of weeks-since-epoch, which strictly alternates for weekly-sampled Sundays, ISO-week-53-free, DST-free (UTC epoch days). **Anchor: 2026-08-09 is an ON week (20674 / 7 = 2953 → parity 1)**; `PARITY_ANCHOR` lives in the workflow `env:` block with the formula in a comment; the re-enable commit message restates it. Implemented as a first-class guard step after `env_guard` emitting `on_week` to `$GITHUB_OUTPUT`, gating the run step AND the verdict step AND the freshness expectations. `workflow_dispatch` bypasses the parity skip, keeps verdict gating. **Red test: strict ON/OFF alternation across ≥8 consecutive Sundays spanning Dec 2026 → Jan 2027** (a year-boundary-only fixture passes the broken `/14` formula — insufficient by proof).
* Prior rulings unchanged: full architecture · bi-weekly · quarterly conditional on B0 · no sources rerun before landing · GitHub re-enable.

## State Verification
As v4 (all round-2/round-3 verified) with corrections: parcels **496,422** · `parcels` **bloat: dead_ratio 0.64-0.73** (~1.2-1.3M dead tuples — all ceiling math measured against a degraded table; bloat/autovacuum check added to B0 + B8 sweep) · new column surfaces in `parcels-columns.snapshot.json` + `parcel-lookup.infra.test.ts` (deliberate edits) · `zoning_base_source_dataset_version` is **NOT fenced** (ruled: provenance-only, no consumers, truthful while the stamp is NULL) · freeze-risk counts residential-scoped · "30-45 min" a projection.

## B0 — HARD GATE (now five items; items 3-5 ARE the DS-8 performance, R3-B6)
1. Root-cause the 08-03 parcels anomaly (both directions: false positives stop; real changes still invalidate).
2. Spec Review + per-step live counts + **the `parcels` bloat/autovacuum check**.
3. **Post-Phase-1 re-baseline:** re-measure 14/32 · 5,318 HIGH/MED · 461/3,330/121 (`stale_cost_fsi_without_gfa` inflates by construction after Phase 1's NULLs; the pinned baselines are invalidated).
4. **Vacuous-zero companion check:** add/verify `opt_aor_gfa_sqm IS NOT NULL AND max_buildable_gfa_sqm IS NULL` (the audit's RC3 invariant applies only when max_buildable is non-NULL — Phase 1's NULLed parcels LEAVE its population; zero must mean healed, not vacated). Phase 1's `ravine_constrained_carries_priced_cost` invariant covers the constrained class; this covers the rest.
5. **Pass-4/5 eligibility under NULL envelopes:** state whether NULL-envelope parcels are excluded from comp/opt scope (expected: yes, by existing eligibility) and record the coverage consequence.

## Spec Updates
As v4's table with round-3 additions: Spec 40/47/115 §2.5 (defer semantics — **rides B2**) · Spec 30 rows `:88,:101` (not `:222`) · B6 gains `chain.logic.test.ts` + `stats/route.ts`.

## Standards Compliance (inlined — no incorporation-by-reference, GT F8)
* **Database Impact: YES.** Migration (numbered behind Phase 1's 239): `massing_enriched_at` ALTER + batched MAX backfill + the partial index + the `enrich_parcels_pass3_scope` table (+RLS). UP+DOWN all-comments, `validate-migration.js`, SQLFluff, `npm run migrate`, `db:generate`, `typecheck`, `BUILDO_TEST_DB=1 npm run test:db`. factories.ts N/A (watermarks raw-SQL by precedent); `parcels-columns.snapshot.json` edit named.
* **Cross-Domain (B6), answered here:** *Try-Catch:* schedules PUT keeps its overarching try-catch; stats route change adds no new boundary; any new catch uses `logError(tag, err, context)`. *Unhappy paths:* invalid cadence → 400, plus 404/500; **`funnel.ts` AND the stats-route freshness map both exhaustive-or-throw on unknown cadence** (the 192h constant becomes cadence-derived with its own fail-closed ruling). *UI Layout:* admin desktop-first `md:`; enum/status content only. *UI Regression:* `npx vitest run src/tests/*.ui.test.tsx` — the tautological test-LOCAL copies in `admin.ui.test.tsx:595-625,668-699` are EDITED deliberately; `:956-965` + `quality.infra.test.ts:242-250` fail only on string removal. *§10.2:* `npx vitest related src/lib/admin/funnel.ts`. *Scenario-A handoff note (CF9, landed):* the schedules PUT contract is: `PUT /api/admin/pipelines/schedules` body `{id, cadence, cron, enabled}`, cadence ∈ the 6-site enumeration, validated server-side (400 on unknown), DB row is authority, seed is bootstrap-only (`ON CONFLICT DO NOTHING`); UI reads the row, never the seed. *§11 N/A items stated:* envelope/middleware/types/375px — N/A (no new route, no mobile surface).
* **Cadence enumeration:** 6 code sites + DB row + per-source dashboard labels; parity test (code-vs-code) **plus the runtime cloud-row assertion** (R3-B5): the verdict step (and a B7 checklist line) asserts the live `pipeline_schedules` row for chain_sources against the expected cron/cadence values **passed to the step as `env:`** (round-4 F7: the script cannot see the workflow's constants otherwise) — closing the live-drift loop the CI test cannot see. The verdict step already has DB access (`*pipeline-env`).
* **RC0 deferral (three heads now):** localhost fallback · CLI-pool statement_timeout death · **orphaned server backends on client kill** (13 found and terminated this session — analysis sessions must cancel server-side on abort). One review_followups entry; B7's RC uses the pipeline pool.
* **Idempotency:** gates read-only; loaders IS DISTINCT FROM; **watermark written in the SAME transaction as the work it stamps, never before**; scope-table writes in-txn with run_id.
* SPEC LINK headers · `npm run system-map` · Spec 05 §5 six-field footers.

## Execution Plan — WF2 shape, one commit per step
- [ ] **B0 — the five-item HARD GATE above.** No B1/B2 code until items 1-5 clear.
- [ ] **B1 — `source-version.js` + tier gates.** As v4 (four branches; fail-safe LOAD; **stream-hash**; three distinguishable outcomes; skip emits completed row = the DS4 condition; `started_at DESC` standardization NAMES the two consumer-side completed_at readers as in-scope or excluded).
- [ ] **B2 — enrich-parcels + D1′ + D4′ + fence + defer plumbing.** Migration (column/backfill/index/scope-table). Fence extension (zoning + massing stamps; NOT the provenance column). **Per-pass duration audit rows + per-pass scope_count/threshold/ratio rows (every run).** The `deferred_to_full` mechanism, row inventory SPECIFIED (round-4 F3/F4): the deferring script emits a `records_meta` marker and exits 0 → `run-chain.js` parses it, **rewrites the deferring STEP row to `deferred_to_full` (diverting the `:506-516` unconditional `completed` UPDATE — otherwise B3's status-exclusion is vacuous)**, breaks the loop via its **own variable (NOT `failedStep`**, which exits 1 and maps to chain `failed`), and writes chain row `deferred_to_full` + a **`deferred_step` slug in chain `records_meta`** (streak detection needs it). Prior steps' rows stay `completed`; downstream steps have NO rows. Verdict streak detection = the last **2** chain rows (`LIMIT 1 → 2`) compared on `deferred_step`. The `OK_STATUSES` edit is global across chains — dormant elsewhere since only sources emits the status (one-sentence note in the lock edit). **`run-chain.js`, `check-chain-verdict.js`, `check-pipeline-freshness.js` + both exact-pin locks are THIS step's files**; Spec 40/47/115/43/48 defer documentation rides this commit. Force-full env plumbing (`:1317` + cost gate). *Red:* massing gate NULL-arm + fires-on-linked_at-and-not-otherwise · zero-link WARN row · bounded comp_count=0 rule + materialized cascade (run_id + consumed marker; crash fixture: unconsumed scope is unioned by the next pass 5) · decision scope on linked_at (late-backfilled decision_date still caught) · pin-absence lock (enrich_parcels only) · pass-5-basis invariant (RC3) · **simulated citywide scope → clean `deferred_to_full` at the current boundary, verdict green + warning, freshness counts RAN, B3 gates do NOT read it as 0-change** · **defer-streak fixture: second consecutive defer → verdict exit 1** · geometry-change fixture invalidates both stamps.
- [ ] **B3 — heritage skip · link_parcel_addresses · compute_parcel_cost_estimates · link_wsib.** *Red:* run-ledger gates scan every attempt of ANY status since own last completed run · **no-completed-run-ever arm: fail-safe RUN (fixture: cost step, nothing completed since 07-08)** · dual-chain slug enumeration (`sources:link_wsib` + `entities:link_wsib` per the record; each script's actual chain memberships verified at implementation, not assumed; massing-full-gate IN-list precedent) · deferred rows excluded · wsib partial index + all three tier CTEs + invalidation semantics.
- [ ] **B4 — zero-intersection floor.** As v4 (design decision; L24c constraint; IS-DISTINCT-FROM convergence row + missing-previous fallback; re-baseline after B2/B3; coverage ratio reported).
- [ ] **B5 — runtime CKAN resolution** (three loaders; last_modified sort; Shapefile glob; no year parsing).
- [ ] **B6 — cadence + defer surface (CROSS-DOMAIN).** The **corrected parity guard** (formula/anchor/placement per Operator rulings) + week-alternation red test (≥8 Sundays, Dec→Jan) · quarterly variant + per-script force envs **+ `LINK_MASSING_FORCE_FULL=1`** · GitHub re-enable + disable-rollback step · 6 cadence sites + per-source labels + DB row + parity test + **runtime cloud-row assertion** · watchdog window → bi-weekly + slack (structural 14-day gap, not 21) · admin status rendering for `deferred_to_full` · `CHAIN_DURATION_BUDGET_MINUTES` set · funnel + stats-route exhaustive-or-throw · tautological UI-test locals edited · `chain.logic.test.ts`.
- [ ] **B6.5 — skip-staleness assert as a WORKFLOW step (round-4 F6 correction: a manifest tail step CANNOT run after a defer — the loop breaks before it; the correct placement is a standalone workflow step after the chain step, verdict-step pattern `chain-sources.yml:83-86`, `if: always() && skip != 'true'`, `*pipeline-env` for DB access):** per-step expected-cadence table in `logic_variables`; WARN at 2×, **FAIL at 4×**; a defer does NOT reset a step's age (it attempted, did no work). This is the wedged-gate tripwire AND the defer-loop backstop.
- [ ] **B7 — Pre-Review Self-Checklist, THEN the OUTPUT panel (order per workflows.md), then proving runs:** one bi-weekly-shape green including steps 21-27 · one quarterly-shape genuinely exercising all force channels (dispatch-supervised) · **one simulated-escalation run proving the defer lifecycle end-to-end (status at all four hops + the streak red)** · measure: the candidate-join multiplier, real per-pass durations (retire the threshold projection label), the 30-40-min floor.
- [ ] **B8 — Green Light + close-out.** Sibling sweep (v4's + stamp-before-work orderings + tautological test-locals + **bloat/autovacuum on big tables**). RC3-tail gate promotion (with B0 item 4's companion check green). Full command battery; lessons (incl. the folded-remedy re-verification lesson); memory; system-map; six-field footers.

## Known risks
As v4, restated: RC1 anomaly still the plan-killer (B0 both directions) · the defer lifecycle is designed but unproven until B7's simulated run · **92-day worst-case defer staleness is an accepted operating cost — operator ratifies it by authorizing** (GT F12 restated: if B0's anomaly ruling downgrades the quarterly to MONTHLY, the D1′ staleness windows and this worst case shrink to ~30 days and are restated in the plan) · three skip paths still halt-free (red lights + B6.5 + B7 are their cover) · D1′'s two staleness windows accepted (heights-only now genuinely quarterly-bounded via `LINK_MASSING_FORCE_FULL`) · thresholds are projections until B7 · `parcels` bloat may move all duration math.

## Panel disposition ledger — round 3 (rounds 1-2 ledgers in the superseded files; full text `.cursor/phase_b_round3_panel_2026-08-06.md`)
**FOLDED:** R3-B1 (parity formula `/7` + anchor + 8-Sunday red) · R3-B2 (status fork chosen: distinct status, green+warning verdict, RAN freshness, admin chips, B3 exclusion, build surface + locks in B2, spec surface 40/47/115 riding B2) · R3-B3 (mid-step claim deleted; d6eb9f31 non-transfers stated) · R3-B4 (per-script envs at :1317; quarterly sets LINK_MASSING_FORCE_FULL; step-scoped env) · R3-B5 (runtime cloud-row assertion) · R3-B6 (DS-8 as B0 items 3-5; companion check; re-baseline) · R3-B7 (run_id + consumed marker + union + cleanup + RLS + backup exclusion) · R3-B8 (50K thresholds + per-pass telemetry prerequisite + every-run ratio rows + WARN 80%) · Adv F3 (defer-streak red + staleness ceiling) · Adv F9/Int F9 (no-completed-ever arm; dual-chain slugs) · Adv F10/GT F10 (B6.5 chain-tail; 2×/4×; defer doesn't reset age) · GT F6 (operator vehicle) · GT F7 (spec edit rides B2) · GT F8 (compliance inlined; Scenario-A note landed; stats-route ruling; §11 N/A stated) · GT F9 (Self-Checklist before panel; UI regression named) · GT F12 (wording; conditional bound restated) · Int F4/F5/F6/F7 (backfill premise/batching; indexes; fence position; snapshot fixture) · RC 1-6 (bloat check; candidate-join open number; ghost day-one 0; heights-only characterization; 92-day cost stated).
**NOT ADOPTED:** none.

> **PLAN LOCKED. Do you authorize this WF2 plan? (y/n)**
> §11 note: three round-3 rulings — the parity guard moved to **weeks-since-epoch parity** (the round-2 remedy was arithmetically wrong — triple-confirmed); the defer fork resolved as a **distinct status, green-with-warning verdict, defer-streak red at 2** (silent-green and standing-red both rejected); and the quarterly now **explicitly forces the massing relink** (`LINK_MASSING_FORCE_FULL=1`) — without it the "quarterly heals" bound on D1′'s accepted staleness was false.
