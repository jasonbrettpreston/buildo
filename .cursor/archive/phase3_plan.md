# Active Task: Supabase Migration — Phase 3 (Functions Deletion / Scheduling / Backups)
**Status:** Implementation — authorized 2026-07-20 (operator "Yes — proceed"). **P3-D1 RULED: deep_scrapes on GITHUB-HOSTED runners** (Decodo proxy carries all AIC traffic so the runner's datacenter IP is never WAF-visible — the v2 self-hosted lean over-weighted that; xvfb virtual display for the proxy-forced headed Chrome; `actions/cache` on `~/.buildo-scraper/` stealth profiles; Decodo creds in GH encrypted secrets per the §11 carve-out; concurrency group prevents overlap; operator's decisive factor: headed Chrome windows disrupt the local workday. Gemini's objection thereby satisfied; self-hosted public-repo risk vanishes.) **P3-D2 RULED: Option A object storage** — wire S3-COMPATIBLE (`BACKUP_S3_ENDPOINT`/`BACKUP_S3_BUCKET`/`BACKUP_S3_ACCESS_KEY_ID`/`BACKUP_S3_SECRET_ACCESS_KEY` — resolves Spec 112 §4.2's placeholder), vendor (B2 vs R2) finalized when the operator creates the bucket; both are S3-compatible so no code fork.
**Domain Mode:** Backend/Pipeline (`scripts/`, `migrations/`, `.github/workflows/`, manifest) — `scripts/CLAUDE.md` read; runbook deploy-ordering rules §3 folded (rules 2+3 were violated by v1 — fixed below). ONE scoped cross-domain touch: the freshness-watchdog dashboard surfacing (P3-D9) edits `src/app/api/admin/stats` + `DataQualityDashboard.tsx` (read-only display of pipeline_runs freshness).
**Workflow:** WF1 sub-phase plan-lock for `.cursor/active_task.md` Phase 3 (steps 3.1–3.4). Abort: git revert per commit.
**Governing specs:** Spec 115 (scheduling — amended per §Amendments below), Spec 113 §8/§9/§11, Spec 112, Spec 47. Program authority: `.cursor/active_task.md` v2.2 Phase 3 + operator cadence rulings 2026-07-20 (nightly ×7 coa→permits; sources weekly; **deep_scrapes weekdays-only, business hours, no weekends**).

---

## Ground truth (scout + panel-verified; v2 corrections marked)

**P3-G1 — `functions/` has ZERO live references.** 5 Cloud Functions; only scaffolding refs: `knip.json:5,9`, `eslint.config.mjs:17`, own package/tsconfig, doc mentions. **[panel-fold: Integration]** `knip.json:33-35` `ignoreDependencies` split: `@google-cloud/functions-framework` + `@google-cloud/pubsub` go at F1; **`@google-cloud/storage` MUST survive F1** (still imported by `backup-db.js`, root `package.json:53`) and is removed at F6 with the backup rewrite.

**P3-G2 — 3.1 pre-delete check PASSES.** `syncTrigger` = Toronto Open Data Active Building Permits only; fully covered by the permits chain (`load-permits.js` + classify steps). `/api/sync` stays as the manual trigger.

**P3-G3 — `local-cron.js` mechanics** as v1 (3 schedules, exact 12h isChainRunning query, 90-min SIGKILL, full-env spawn of run-chain). **[panel-fold: Integration]** `chain.logic.test.ts:341-417` pins: file exists; content contains the 4 chain IDs (contains-only — adding `deep_scrapes` passes); coa-before-permits array shape; **source-scan literals of the loop/try-catch/`child.kill('SIGKILL')` INSIDE local-cron.js** → the shared-helper extraction is LIMITED to `isChainRunning`; `triggerChain` + scheduler loop STAY in the file, and the SIGKILL literal changes per P3-D5's SIGTERM-parity fold, so those specific pins get updated same-commit (they are WF2-P8 behavior locks — Regression Guardian attention at output review).

**P3-G4 (CORRECTED — [panel-fold: Ground-truth HIGH-5]) — `deep_scrapes` is a 7-STEP chain, not single-step:** `inspections` (aic-orchestrator.py), `classify_inspection_status`, `assert_network_health`, `refresh_snapshot`, `assert_data_bounds`, `assert_engine_health`, `assert_staleness` (`manifest.json:115-118`). **`refresh_snapshot`/`assert_data_bounds`/`assert_engine_health` are SHARED with permits+coa** — shared-step advisory locks SKIP on contention (runbook rule 3), so deep_scrapes slots MUST clear the nightly chain window (11:00 UTC + up to ~3h worst case → first slot ≥15:00 UTC, P3-D3). Orchestrator env/reclaim per v1; `requirements.txt` exists (`nodriver>=0.48`, `psycopg2-binary`); **proxy mode forces HEADED Chrome → any Linux runner needs `xvfb-run`** (guarded RuntimeError, not silent); persistent stealth profiles (`~/.buildo-scraper/profile-worker-N`) reset on ephemeral runners (fingerprint burn — weighs against GH-hosted). **[panel-fold: Integration HIGH-2] `aic-orchestrator.py` EXITS 0 on scrape-level failure by design** (verdict-only FAIL → run-chain `completed_with_errors` → exit 0; only hard crashes exit non-zero) — the deep_scrapes workflow's failure detection MUST read the chain's `pipeline_runs` status/verdict, not the process exit code.

**P3-G5 (CORRECTED) — Spec 115 pins old cadences; the FULL amendment sweep** ([panel-fold: Ground-truth HIGH-4]; v1's "§2.1 weekday wording" claim was wrong — §2.1 has no such sentence): §1 L20 ("the 3 workflow definitions"), §2 table + §2.2, **§2.1 (rewritten for the DST reconciliation, P3-D3)**, new §2.4 (deep_scrapes), §5 L282 (mv-refresh timing comment — pre-existing oddity: 09:30 UTC is BEFORE the 11:00 chain; re-time to after the nightly window), §6 values table, §7 L334/L346 (incl. the mandated header-comment template's "same 3 chain schedules" → count-correct text), §8 L377 ("all three"), **§10 Target Files (+`chain-deep-scrapes.yml`, +watchdog workflow — v1 would have authored files outside the spec's own Operating Boundaries)**.

**P3-G6 — backup-db.js** as v1 (GCS streaming, SKIP-if-unset, lock 112, manifest :71/:91). **[panel-fold: Integration]** run-chain.js:281 lists `backup_db` in the `isInfraStep` exemption — keep the slug. Test surface = `backup-db.logic.test.ts` (11 source-scan tests; ≥4 break on rewrite) + peripheral pins in `pipeline-advisory-lock.infra`/`quality.logic`/`assert-global-coverage.infra`/`chain.logic` (survive if slug+lock kept — verify). **Step rows are written under the scoped slug `permits:backup_db`** (run-chain.js:321) and standalone runs under `backup-db`'s own slug — the 25h watchdog query must match BOTH shapes, not the spec's bare `backup_db` prose.

**P3-G7 — purge-lead-views deletion surface** as v1. **[panel-fold: Ground-truth LOW-11]** ADR-007:122-123 ordered deletion "same commit as the D6 migration" — mig 229 landed in Phase 1 and the script survived: the ruling is ALREADY violated/overdue. This plan re-anchors deletion to the pg_cron catalog (ADR-007's own rationale: retention must never gap) and records that acknowledgment in the ADR at F6. **[panel-fold: Schema-Fidelity F5]** `logic_variables.variable_value` is NUMERIC → the retention job SQL needs `make_interval(days => variable_value::int)`; the seed row's description ("purged nightly by purge-lead-views.js") is re-described at F6 (`seeds/logic_variables.json:887` + registry regen).

**P3-G8 (REWRITTEN — [panel-fold: Schema-Fidelity F1 + Integration, refuting the program-plan bullet's mechanism]) — the "pin pg_cron/pg_net to `extensions`" migration is IMPOSSIBLE as worded and UNNECESSARY for call sites.** Live evidence: pg_cron 1.6.4 `extnamespace=pg_catalog`, `extrelocatable=false`, control-file-fixed — CANNOT be moved, ever; its callable surface is hardcoded in the **`cron`** schema. pg_net 0.20.3 already in `extensions`, also non-relocatable; functions hardcoded in **`net`**. Both are search_path-independent when call sites schema-qualify. Mig 224's missing SCHEMA clause is CORRECT, not a defect. Replacement mechanic (P3-D4): a **schema-determinism migration** — assertion/NOTICE of the live layout + `SCHEMA extensions` on the guarded pg_net CREATE for fresh installs (guarding on the `extensions` schema existing — it doesn't on Docker/CI images) — plus the RULE that every catalog call site is written `cron.*`/`net.*` qualified. Program-plan 3.2's bullet gets amended to this mechanism. Preconditions verified live: `shared_preload_libraries` has both; `cron.database_name=postgres` matches; `cron.job` empty. HEAD=231.

**P3-G9 — existing workflows** as v1; **[panel-fold: Integration]** repo is PUBLIC; `schedule:` triggers fire ONLY from the default branch (main) — inert pre-merge, LIVE the moment the YAML lands on main, so the P3-D6 gate must be correct in the merge commit, not retrofitted at 4.3. A missing GH secret does NOT fail a workflow (empty-string interpolation) — inertness never rests on that. Public-repo wrinkles: scheduled workflows auto-disable after 60 days repo inactivity; **GitHub explicitly warns against self-hosted runners on public repos** (fork-PR code execution) — P3-D1 mitigation requirement.

**P3-G10 — restore-db.js exists** (Phase 0); not rebuilt here.

**P3-G11 (NEW — [panel-fold: Schema-Fidelity F2 + Integration]) — `pipeline_schedules` live truth:** 27 step-level rows, ALL `chain_id=NULL`, ALL `cron_expression=NULL`; NO `sources`/`entities`/`deep_scrapes` rows (plain UPDATE silently no-ops for 3 of 4). The 038 PK on `pipeline` is GONE (095 replaced it with unique expression index `idx_pipeline_schedules_scope (pipeline, COALESCE(chain_id,'__ALL__'))`) → **048's `ON CONFLICT (pipeline)` precedent FAILS at runtime today**; the seed must `INSERT ... ON CONFLICT (pipeline, COALESCE(chain_id,'__ALL__')) DO UPDATE` (route.ts:80-86 is the working precedent). Admin writers never touch `cron_expression` (no collision). **Cadence enum gap:** PUT validates `['Daily','Quarterly','Annual']` (route.ts:34) — 'Weekly' (sources) needs the enum extended in the same change or the admin PUT silently reverts it. 095's `chain_id` CHECK excludes `'deep_scrapes'` — new rows use `chain_id=NULL` (global), no CHECK change needed; noted for any future per-chain scoping.

**P3-G12 (NEW — [panel-fold: Schema-Fidelity F3/F4/F6]) — offboarding + runtime schema facts:** `account_deleted_at` lives ONLY on `user_profiles` (mig 114:32; no auth.users metadata mirror — Spec 115 §5's sample predicate is wrong and gets amended). FK topology verified: 11 public + 8 auth-internal CASCADEs from `auth.users`; `admin_watchlist` SET NULL; **`admin_audit_log` RESTRICT — a deliberate fence (mig 229:96-106): a batch `DELETE FROM auth.users` ABORTS ENTIRELY if any swept user authored audit rows**, blocking every other user's deletion forever with the failure visible only in `cron.job_run_details`. The sweep must therefore run per-user with exception handling, or exclude-and-surface audit-authoring users for manual RTBF scrub (P3-D4). `mv_monthly_permit_stats` HAS the unique index CONCURRENTLY needs (closed). `pipeline_runs` has `error_message` (SIGTERM UPDATE fine) but **NO `step_name` column** — Spec 112 §7's quoted OP4 psql check (`WHERE step_name='backup_db'`) cannot run as written; the OP4 prose fix corrects it to the real column (`pipeline` + scoped-slug matching). `scraper_queue` exists (0 rows = scraper never ran against the rebuilt DB; rows persist after runs).

---

## Key Decisions

* **P3-D1 (OPERATOR DECISION — deep_scrapes runner; Spec 113 §8.2 amendment).** Chains (coa-permits / sources / entities / watchdog) → **GitHub-hosted, Network Restrictions OFF + strong auth** (§8.2 option 3). deep_scrapes options — **(a) self-hosted GH runner on the operator box** (Chrome/Python/proxies/profiles already there; box must be on weekday business hours — which it typically is; REQUIRED mitigations on this PUBLIC repo [P3-G9]: Actions setting "Require approval for all outside collaborators" + runner allowed only on the deep-scrapes workflow + never on PR-triggered workflows), **(b) keep deep_scrapes on the demoted local-cron** (zero new infra; no GH-side alerting; dies silently with the dev box), **(c) GH-hosted + xvfb + proxy-routed** (works mechanically per P3-G4 but burns proxy spend + resets stealth profiles every run + Gemini's reliability preference; the WAF history is empirical, not perceived). **Recommendation: (a) with the mitigations; (b) as the fallback if always-on isn't acceptable.** Gemini's standing objection (option c, "solvable engineering problem") recorded.
* **P3-D2 (OPERATOR DECISION — portable backup destination; Spec 112 §2.1 amendment).** **Recommended: A (Backblaze B2 or Cloudflare R2)** — decoupled from runner choice and dev-box uptime; Option B viable only under P3-D1(a).
* **P3-D3 (operator cadences, final).** coa→permits `0 11 * * *` UTC nightly ×7; sources weekly `0 13 * * 0` (Sun — day confirmable); **deep_scrapes `0 15,18,21 * * 1-5`** UTC = 10 AM/1 PM/4 PM EST · 11 AM/2 PM/5 PM EDT — weekdays only, business hours both DST regimes, **first slot clears the 11:00 UTC nightly window + 3h worst case** (P3-G4 shared-step hazard; runbook rule 3). Entities `0 8 * * *` unchanged. **DST reconciliation ([panel-fold: Ground-truth BLOCKING-2] — program panel decision "single UTC cron + in-job ET check" vs Spec 115 §2.1 accepted-drift):** implemented as BOTH — single UTC cron entries (no dual lines) + the guard step logs the current ET time and emits a `::notice` drift annotation (the in-job ET check, observability-grade — it never skips, honoring §2.1's rationale that late-not-early drift is safe). Spec 115 §2.1 amended to record the panel decision + this reconciliation.
* **P3-D4 (pg_cron catalog + schema-determinism).** Two migrations: ① schema-determinism (P3-G8 mechanism — NOT a pin); ② catalog: `mv_monthly_permit_stats_refresh` (re-timed post-nightly-window, index verified), `lead_views_retention_purge` (`make_interval` cast per P3-G12), `offboarding_sweep_30day` — **predicate on `user_profiles.account_deleted_at` (P3-G12), per-user loop with per-row exception handling: audit-authoring users (RESTRICT) are SKIPPED and surfaced** (INSERT into a sweep-skipped notice mechanism — minimally a `RAISE WARNING` + a row in `cron.job_run_details`; manual RTBF scrub per the P24 pattern) — plus a partial index on `user_profiles(account_deleted_at) WHERE account_deleted_at IS NOT NULL` [Gemini MED, cheap insurance] **+ a `BUILDO_TEST_DB` cascade-contract test** (dummy user with rows in every CASCADE table + one audit row → assert cascades fire, audit-user skipped, others deleted) [Gemini LOW-7 + SF F3]. All call sites `cron.*`/`net.*` qualified; both migrations NOTICE-skip on extension-less images.
* **P3-D5 (local-cron demotion, scoped).** Header rewrite (count-correct text per P3-G5); `isChainRunning` → shared `scripts/lib/chain-concurrency.js` (**extraction limited to it** — P3-G3 source-scan locks); **timeout SIGKILL → SIGTERM-then-SIGKILL-after-grace** for prod parity [Gemini MED] (the pinned literals + `chain.logic.test.ts` updated same commit); deep_scrapes schedule entry added **ONLY under P3-D1(b)** [DeepSeek MED — explicitly gated].
* **P3-D6 (workflow inertness, REVISED — [panel-fold: Gemini HIGH + DeepSeek MED converged + Integration]).** All chain workflows are committed with the **`schedule:` block PRESENT BUT COMMENTED OUT** + active `workflow_dispatch` (manual testing). Phase 4.3 activation = one PR uncommenting the blocks. No failed-run noise, no runner minutes, no secret-dependence for inertness (missing secrets don't fail workflows anyway — P3-G9). The guard step still verifies `SUPABASE_DATABASE_URL` non-empty and exits 1 loudly if absent (protects manual dispatches).
* **P3-D7 (run-chain terminal-status handler).** Handles **SIGINT AND SIGTERM** (GH sends SIGINT first, SIGTERM ~7.5s later — Integration LOW-7): terminal `failed` UPDATE per Spec 115 §4 item 6 (`error_message` exists; no step_name — scoped-slug rows updated by id).
* **P3-D8 (NEW — check-chain-running DB-error contract, Spec 115 §4 item 3 AMENDMENT — [panel-fold: Gemini HIGH]).** On DB-check error: `skip=true` **AND `exit 1`** — the chain still doesn't fire (no double-run; preserves the fail-safe posture) but the job goes RED and notifies (an unreachable DB is an outage signal, not a silent green skip). Spec 115 §4 item 3 + §9 amended.
* **P3-D9 (NEW — freshness watchdog, restoring the DROPPED program mandate — [panel-fold: Ground-truth BLOCKING-1]).** One workflow `pipeline-watchdog.yml` (daily `30 15 * * *` UTC, after the nightly window): checks `pipeline_runs` for **(i)** a completed `chain_permits` + `chain_coa` run within 25h — missing → `exit 1` (red + GH notification; catches crons that NEVER FIRED, which GH's own notifications structurally cannot); **(ii)** a completed backup (scoped-slug shapes per P3-G6) within 25h — missing AND permits chain not currently running (race guard, [Gemini LOW-6]) → invoke `backup-db.js` directly (the Spec 112 §6 safety-net, MERGED into this workflow); still missing after → red. **Dashboard surfacing:** `GET /api/admin/stats` payload + `DataQualityDashboard.tsx` gain a per-chain `last_completed_at` freshness block (the scoped cross-domain touch — read-only display).

---

## Technical Implementation (delta from v1 — v1 items stand except as amended)

**Item 1 (F1):** as v1 + the `ignoreDependencies` split (P3-G1); `@google-cloud/storage` survives to F6.

**Item 2 (3.2):**
- Spec amendments per the FULL P3-G5 sweep + §4 item 3 (P3-D8) + §2.1 DST reconciliation + §5 predicate/timing fixes + §10 Target Files + Spec 113 §8.2 (ruling) + §8.4 (deep_scrapes joins the GH-Actions-only set — argued not asserted: pg_cron is structurally incapable of running a Python orchestrator regardless of must-succeed classification [GT LOW-12]) + §11 (P3-F7 scope, below) + program-plan 3.2 bullet (schema-determinism rewording).
- Workflow anatomy additions: **PG17-line `postgresql-client-17` install step (PGDG repo)** in every workflow that reaches `pg_dump` — the chain workflow (backup_db step) + watchdog (Spec 112 §5, [GT MED-6]); **`migrate.js --verify` pre-flight step before `run-chain.js`** (runbook rule 2, [GT MED-8]); ET-drift `::notice` (P3-D3).
- `chain-deep-scrapes.yml`: runs-on per P3-D1 ruling; `xvfb-run` under proxy mode on Linux; **failure detection reads the chain's `pipeline_runs` status after the run — `completed_with_errors`/FAIL verdict → exit 1** (P3-G4 exit-0 caveat; the coa-permits red-flip pattern generalized).
- Seed per P3-G11: idempotent `INSERT ... ON CONFLICT (pipeline, COALESCE(chain_id,'__ALL__')) DO UPDATE` (script, re-runnable [DeepSeek HIGH-4]) inserting the missing `sources`/`entities`/`deep_scrapes` rows; **cadence enum extended (`Weekly`, and a display value for multi-daily)** in `subscription... schedules` route PUT validation same-change (P3-G11).
- local-cron demotion per P3-D5.

**Item 3 (3.3):**
- backup-db.js rewrite per v1 + Spec 112 §7's SECOND edit (`$DATABASE_URL`→`SUPABASE_DATABASE_URL` framing) + the OP4 psql-check column correction (P3-G12 no-step_name) [GT LOW-10].
- **Watchdog/safety-net lands in the SAME commit as the backup-db rewrite** (never invocable against the old GCS script — [DeepSeek HIGH-3]).
- purge-lead-views deletion + ADR-007 overdue-acknowledgment note (P3-G7).
- **Vault (P3-F7, scope corrected [GT MED-7]):** ① migration creates the write-RPC (SECURITY DEFINER, statement-logging off for the call) **+ explicit EXECUTE REVOKE from `anon`/`authenticated`/`public`, GRANT to `service_role` only** (Spec 113 §11's access-scope requirement); ② operator-run seeding script generates `CRON_SECRET` randomly and writes via the RPC — the value never appears in a migration file or terminal history [DeepSeek LOW]. CRON_SECRET mandate correctly homed to §8.1 (HTTP-trigger surface — not consumed by the scheduled path). **§11 amendment: CI-runner credentials (SUPABASE_DATABASE_URL, deep-scrapes proxy creds) live in GH encrypted secrets** — the workflow-execution carve-out to §11's Vault posture, recorded not implied.

**Item 4 (3.4):** OUTPUT review — **FULL ROSTER, operator-directed 2026-07-20 (supersedes v2's lean composition):** Gemini + DeepSeek CLIs (DeepSeek as the 4-lens set: spec / security / idempotency / error-paths) + Code Reviewer + **Observability** (audit-row completeness for the new watchdog/check-chain-running surfaces; verdict cascades row-derived; records_meta contracts on the backup-db rewrite) + **Integration** (main tree — workflow YAML vs real GH semantics, seed vs live table, manifest wiring) + **Schema-Fidelity** (catalog/schema-determinism migrations, seed ON CONFLICT target, partial index, cascade contract vs live FKs) + **Regression Guardian** (local-cron literal pins, backup-db test locks, manifest edits, run-chain handler — the WF2-P8 fences) + **Reality-Check** (operator-directed inclusion despite no enriched parcel fields: validates the SEEDED cron values against the ruled cadences, the watchdog's 25h queries against real pipeline_runs rows, and the sweep's per-user behavior against live FK topology — the "are the numbers/queries real" pass) + **Security** (Vault RPC grant surface, GH-secret handling, CRON_SECRET seeding path, workflow injection surface, proxy-cred exposure). Round-2 (DeepSeek + Integration) on the fold. Each CLI pass adjudicated by a grounder (§9 CLI blind spots).

## Execution Plan (v2)
- [ ] P3-F1: functions/ deletion + scaffolding cleanup (storage dep survives) (commit 1).
- [ ] P3-F2: FULL spec amendment sweep (115/113/112 + program bullet) per operator rulings (commit 2). GATES F3–F7.
- [ ] P3-F3: chain-concurrency helper + check-chain-running (P3-D8 contract) + run-chain SIGINT/SIGTERM handler + local-cron demotion (P3-D5) + chain.logic.test updates (commit 3).
- [ ] P3-F4: workflow YAMLs — 4 chain workflows (schedules commented out per P3-D6, PG17 client + migrate-verify + drift-notice steps) + stats/dashboard freshness surfacing (P3-D9 cross-domain touch) (commit 4). *Watchdog YAML itself deferred to F6 (rides with backup-db).*
- [ ] P3-F5: migrations — schema-determinism + pg_cron catalog (P3-D4, incl. partial index + cascade-contract test) + pipeline_schedules seed script + cadence-enum extension; validate + fresh replay + BUILDO_TEST_DB battery (commit 5).
- [ ] P3-F6: backup-db.js rewrite + tests + `pipeline-watchdog.yml` + OP4 prose (both edits + column fix) + purge-lead-views deletion + ADR note + logic-var re-description (commit 6).
- [ ] P3-F7: Vault RPC migration (grants per corrected scope) + operator seeding script (commit 7).
- [ ] P3-F8: OUTPUT review → fold → push.

## Go/No-Go gates
1. Operator rulings P3-D1/P3-D2 required before F3 (D1 shapes local-cron + deep-scrapes YAML; D2 shapes backup-db env contract). Fallback defaults if unruled: D1→(b), D2→A [DeepSeek HIGH-1].
2. F2 spec sweep gates all code.
3. F5 migrations green (replay + battery) before F6's purge deletion (retention never uncovered).
4. Schedules stay commented out — activation is Phase 4.3 (verified at output review).
5. Nothing touches the local dev stack's data.

## Resolved v1 uncertainties
#1 inertness → P3-D6 (commented-out schedules; default-branch semantics per P3-G9). #2 deep_scrapes propagation → P3-G4 (exit-0 caveat; status-read detection). #3 account_deleted_at → `user_profiles` only (P3-G12). #4 seed mechanism → P3-G11 (expression-index ON CONFLICT script + enum extension).

## Panel Adjudication (2026-07-20 — Gemini, DeepSeek, Integration, Ground-truth, Schema-Fidelity)
| # | Finding | Disposition |
|---|---|---|
| 1 | GT BLOCKING-1: freshness watchdog dropped from program mandate | **FOLDED** — P3-D9 (workflow + dashboard surfacing) |
| 2 | GT BLOCKING-2: DST guard panel decision contradicted | **FOLDED** — P3-D3 reconciliation (single cron + observability ET check; Spec 115 §2.1 amended) |
| 3 | GT BLOCKING-3: weekend deep-scrape slots | **MOOT** — fixed mid-panel by operator-refinement edit; slots additionally moved to clear the nightly window (GT HIGH-5) |
| 4 | SF F1 + Integration + DeepSeek CRITICAL (converged): pg_cron schema pin impossible | **FOLDED** — P3-G8 schema-determinism rewrite; program bullet amended |
| 5 | SF F2 + Integration (converged): pipeline_schedules seed trap (dead 048 precedent, missing rows) | **FOLDED** — P3-G11 |
| 6 | SF F3: RESTRICT aborts batch offboarding sweep | **FOLDED** — P3-D4 per-user loop + skip-and-surface |
| 7 | Gemini HIGH + DeepSeek MED (converged): inertness double-gate flawed/noisy | **FOLDED** — P3-D6 commented-out schedules |
| 8 | Gemini HIGH: DB-error fail-safe silent green | **FOLDED** — P3-D8 skip+exit-1 (spec amendment; double-fire posture preserved) |
| 9 | Integration HIGH-2: orchestrator exit-0 on failure | **FOLDED** — status-read detection in deep-scrapes YAML |
| 10 | Integration MED-3 / P3-G9: public-repo self-hosted warning | **FOLDED** into P3-D1(a) mitigations; operator decides |
| 11 | GT MED-6: PG17 client provisioning | **FOLDED** — workflow anatomy |
| 12 | GT MED-7: Vault scope thin / GH-secrets tension | **FOLDED** — P3-F7 corrected scope + §11 carve-out amendment |
| 13 | GT MED-8: migrate --verify pre-flight (runbook rule 2) | **FOLDED** — workflow anatomy |
| 14 | DeepSeek HIGH-3: safety-net before rewrite ordering | **FOLDED** — watchdog rides F6 |
| 15 | Gemini MED SIGKILL parity, LOW guard-race, LOW cascade test; DeepSeek MED conditional local-cron, LOW Vault seeding; Integration LOW SIGINT, slug shapes, dep ordering; GT LOWs (misquote, OP4 second edit, ADR overdue, §8.4 argument, L502 nit) | **ALL FOLDED** at cited sites |
| — | Gemini CRITICAL "self-hosted runner unacceptable, use GH-hosted+proxy" | **NOT ADOPTED as a ruling** — presented to operator as option (c) with Gemini's standing objection recorded (P3-D1); WAF history is empirical (Playwright→nodriver), profile-reset + xvfb + spend costs stated |
| — | DeepSeek NIT "parallelize spec amendments with code" | **REJECTED** — spec-first is Prime Directive #10; F2 gate stays |
| — | DeepSeek NIT "sidecar storage undefined" | **REFUTED** — Spec 112 §4.2 defines it (same path, `.manifest.json` suffix) |
