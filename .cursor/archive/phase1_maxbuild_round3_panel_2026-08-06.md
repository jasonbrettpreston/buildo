# Round-3 PLAN panel record — Max-build v3 (2026-08-06, lean 4-agent roster)

Roster: Integration+Schema · Reality-Check · Compliance+Ground-truth · Adversarial+Observability. Target: the v3 REMEDIES (round-2-verified numbers settled). Verdicts: SOUND-WITH-CORRECTIONS / SOUND-WITH-CORRECTIONS / READY-AFTER-FOLD / NOT-READY. Full agent texts in the session transcript; this record carries every finding that drives v4.

## BLOCKERS
**R3-M1 (RC 1d) — `buildTier`'s coverage fallback reopens the ravine bug for ~75% of the class.** `optimal-config.js:187-190`: footprint NULL → falls back to `coverageCapFrac × lotSizeSqm`; `coverageCapFrac` = `bylaw_max_coverage_pct` (ravine-blind). D-C's NULL-out is what CREATES the trigger; D-D's predicate correctly detects the change and hands the parcel to exactly this fallback → a fresh non-ravine-aware `opt_aor_gfa_sqm`, which cost prices via COALESCE. Sized: 3,651 of 4,921 (74.2%) OR-class parcels have non-NULL `bylaw_max_coverage_pct`. Today `orphan_opt_aor_no_envelope` = 0 — this failure mode does not exist yet; the fix creates it. REQUIRED: gate the fallback on the constrained reason (or footprint-NULL∧ravine) + the zero-baseline invariant `ravine_constrained_carries_priced_cost` (applies: reason='ravine_constrained'; bad: cost_fb/cost_solar/opt_aor/opt_coa NOT NULL; HIGH, gate:true).
**R3-M2 (RC 4) — the ≥75% ×2-benchmark gate is falsified.** Healthy population (both dims ≥3 m) measures **48.6%** (170,179/350,234) — 26-30 pts under the cited ~79% ceiling, which Compliance+GT independently showed is **RD-only** (report :66-76: RS 41-42%, RM 41% "don't chase to 95%", RT 16-31%). Ship as absolute → unattainable or quietly loosened. REQUIRED: relative before/after gate + RD-scoped absolute, population defined identically to the measurement.

## CRITICAL
**R3-M3 (Adv F1 + Int F4, independent) — the heritage arm of D-C is wrong.** Heritage envelopes are the existing-structure FREEZE (`enrich-parcels.js:596-600` — width/length emitted only when NOT heritage; reason 'heritage' :647; confidence 'high' :630). Sub-floor box is irrelevant; the arm has no defined population (RC: heritage∧sub-floor = 0 live) — implemented literally it matches nothing or deletes 5,293 valid frozen envelopes. REQUIRED: drop heritage from D-C entirely.

## HIGH
- **R3-M4 (Int F1):** D-D as ruled misses **3,193 lot-NULL value-stale parcels** (footprint non-NULL, `lot_size_sqm IS NULL` → outside stream AND outside the :1271-1274 reset; 2,522 carry priced cost). Extend the reset to `(lot_size_sqm > 0) IS NOT TRUE` (three-valued logic — `NOT(lot>0)` drops NULLs). Real in-stream stale count ~3,964 (not ~375; Int F2 — figure 10× low).
- **R3-M5 (Adv F2):** step 2's `--full` = unguarded write-storm (`flushOptConfigBatch` :1243-1254 has NO IS DISTINCT FROM; full drops the predicate) against still-broken geometry, making the anti-thrash red vacuous. Step 2 live run = INCREMENTAL (the predicate selects the stale set — the diagnostic goal); `--full` at step 8 only; idempotency measured after step 8.
- **R3-M6 (Adv F3):** high-side blindness — zero checks on width/length; overcorrected corner (width>frontage) passes every gate and the LEAST silently un-binds the box. REQUIRED: `max_build_width_m ≤ frontage_m + ε` / `length ≤ depth + ε` INVARIANT (zero-baseline, gateable) + high-side clause in the corner recovery gate.
- **R3-M7 (Adv F5 + Comp A-1):** step 10 unexecutable at its position (cloud count 0 only after step 14). Pins: F2 promotion = separate commit pushed AFTER step-14 cloud measurement; chain-sources stays disabled until step 14 done; Phase B lands nothing between steps 13-14 (else cloud's incremental runs recompute stale rows to garbage-consistent opt_aor and price them).
- **R3-M8 (RC 1a-1c):** ravine contract numbers wrong: population is OR-based **4,921** ($2.130B fb + $62.5M solar suppressed), not width-only 4,423/$1.838B; post-D-A residual = **4,775** (146 of the 152 corner∩ravine clear both axes; 6 remain via length — neither 4,423 nor 4,271 is right). And the `'ravine'` reason today covers **24,526** parcels unconditionally (:650 branch has no nullness check) — the plan must give the explicit before/after CASE diff: `'ravine_constrained'` ONLY for the sub-floor residual; non-sub-floor ravine keeps `'ravine'`; new branch ordered above it (db-test :267 keeps an above-floor fixture).
- **R3-M9 (Comp B-1/B-2):** ≥75% population unstated (see R3-M2) and the GT-4 adjudication (floor+clamp vs WF1 WF3-B "coverage never undercut") still silent after two demands — one recorded paragraph required.

## MED / corrective
- Comp A-2: migration ceremony dropped (UP+DOWN all-comments, `validate-migration.js`) — restore. A-3: §14 pinning — MB-3 edit rides step 5; MB-4 conditional rides step 4; Spec 88 (cost contract note) rides step 5; Spec 78 :265-266 (comps predicate — the second IS-NULL gate) amend-or-explicit-defer. A-4: explicit test file paths. B-3: §5:282 correction overclaims — garden-suite tests EXIST (:107/:133/:268 ravine half); scope the spec fix to corner/through-lot/narrow-lot. B-4: D-E item 6 retires the documented **Option A** ruling (review_followups.md:18; live at audit :71/:73 incl. `lot_implausible_correctly_excluded`) — cite and knowingly retire; state the fate of the ~3,260 COMMON/CONDO exclusions. B-8: "step 13"/"step 14" money-measurement wobble; "five blind spots" header over six items.
- Int F5: `coverage_only` consumers incomplete — add producer basis-distribution rows (`:710-711`→`:1448-1449`), the PERMITS branch (`assert-global-coverage.js:726-727`→`:1133-1134`), runbook `:23,:31` + field-spec `:48` docs. F6: both columns free TEXT; mobile renders reason RAW ("ravine_constrained" as literal UI text — own it). F8: cost suppression needs NO code change (Mutator full-stream rewrites to NULL under IS DISTINCT FROM; COALESCE fallback also NULLs) — but only ENVELOPE lines suppress (kitchen/bath/basement/gut/addition survive via cur_*); release note says "envelope lines suppressed", not "menus deleted".
- Adv F6: inert detector breaks `assert-parcel-sanity.infra.test.ts:47-51` (source-regex on statusFor ternary) + `:80` (gate-HIGH-0→PASS) + the CLI export-shape lock; policy: empty-fixture DB reads inert-INFO. F7: D-E per-item spec — item 1 has no data source (raws are CTE-internal; residual signal = the emitted count row + persisted-column bounds on emitted rows); item 3/4 need numbers (now supplied by RC — see table) + item 4's 212 standing violators go INFO-first per §3.6; item 6 per B-4. F8: step-8 coverage gate ~0.4 pt margin — pre-compute the exact expected number and define carve-out (numerator vs denominator). F9: step 7's "0 rows" must include `ocResult.updated` + `reset_ineligible`; per-row engine-error parcels re-fire benignly (INFO count).
- RC 2 — **the D-E bounds table (measured live):** `opt_aor_gfa_sqm`: lowrise (RD/RS/RT) flag >2,500 m² HIGH (max obs 1,998.9); non-lowrise flag >3,500 m² HIGH (catches the 3,843 outlier — it is NON-lowrise; a lowrise-only bound misses it). `comp_fsi_p50`: flag >4.0 HIGH residential-wide (p99 2.37, max 6.62; lowrise split TIMED OUT — fast follow-up, don't pin a lowrise number unmeasured). `max_build_width_m`: flag >30 m MED (p995 27.2, max 42.66); note p005=0.41 m corroborates the defect class; post-fix the below-floor range goes structurally vacuous → wire the inert detector to it. `max_build_length_m`: flag >100 m HIGH (p995 58.9, max 316.23).
- RC 3 — **D-B signal exists, quantified YES:** on the 14,342 class, `existing_width_m` avg **8.29 m** vs frontage avg **4.51 m**; 99.7% of parcels with data have building ≥ 0.7× frontage (only 19/6,930 below) — one footprint spanning multiple party-wall units. Cite in D-B.
- RC 5 — the 54-ha parcel: **ravine-fabric mega-parcel pattern** (4 of top-5 lot outliers ravine-flagged; frontage×depth ≈ lot exactly; 1.19 km "depth") — already neutered (`oob`, footprint NULL, surfaced by `lot_implausible_correctly_excluded`). File the pattern to review_followups (distinct from this WF).
- RC note: cloud analytics slow; **13 orphaned backends** from killed clients had to be `pg_terminate_backend()`'d (statement_timeout=0 + client kill ≠ server cancel) — RC0 followup gains a third head: analysis sessions must cancel server-side on abort.

## Confirmed sound (no action)
D-D keystone anti-thrash: byte-level convergent (NUMERIC(12,2); round2 identity; 0 float-noise mismatches live; 0 malformed jsonb) · migration 239 next; logic_variables DECIMAL fits · five registration surfaces exact · recovery denominators stable (is_corner_lot written only by enrich-centreline; ravine flag only by enrich-ravines) · compute_build_norms manifest :44/:81 · parity test writable; CHECKS sync-require confirmed · NULL propagation clean; factories N/A holds · no coverage gate red-lights from suppression (cost_fb is infoRow; permit ladder degrades T3→T4 with coverage held — one release-note line; CoA accepted-baseline self-retires upward only) · WF3 template/roster/§14-mostly/lessons compliance clean · Spec 59 corroborates the ravine ruling · idempotency of the NULL contract sound (IS DISTINCT FROM :686; reset self-limits; NULL-vs-NULL quiet).
