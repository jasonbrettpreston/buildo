# Active Task: Phase A — chain halt + revision fan-out + chain-completeness (WF3)
**Status:** Implementation — **C1 ✅ main (`cdaea415`) · C2 ✅ main (`bc87d292`) · C6 ✅ main (`220803d1`) · migration 241 ✅ applied · C3 ✅ main (`c01031f7`) + BACKFILL RUN ON CLOUD 2026-08-13 (1,416 corrected, smear 0, dated backup) · C0 DELETED by its own panel · C4 ✅ COMMITTED + ON MAIN 2026-08-13 (`d1dadc0f`+`4d5df9e7` on branch = `5b445ec7`+`1fad5ee4` on main, patch-identical — zero arrival gap) · C7 queued NEXT (loader invalidation) · C5 LAST**
> **📌 2026-08-13 watchdog red — EXPLAINED:** "No completed chain_deep_scrapes run within 30h" — yesterday's step-timeout kill left the chain row `failed` (∉ RAN_STATUSES); correct fail-loud. Self-heals when a slot completes — but today's in-flight run carries the SAME 140-vs-150 envelope. Mini-WF3 below.
> **✅ C3 BACKFILL RUN AGAINST CLOUD 2026-08-13 ~14:4xZ (operator go):** corrected **1,416** · backup `_backup_smeared_enriched_status_20260813` (1,416 rows, restore UPDATE in run log) · post-verify smear = **0**, 2,959 legitimate Inspection-row values kept. Expect tonight: cross_check_stalled/active_inspection → 0; **cross_check_permit_issued keeps its 8 pre-existing `status='Inspection'` offenders (filed, NOT a C3 failure)**; the §4.9 WARN drift row self-retires unless the loader regenerates rows before the run — nonzero reappearance is the DOCUMENTED mechanism, re-run C3 on demand until the loader invalidation rule lands.
> *(superseded record below)* the C3 backfill script is on main but had NOT been run against cloud — **cloud dry-run 2026-08-13 ~13:50Z: 1,416 rows** (was 1,350 on 08-12 — empirical proof of CORRECTION 1's regeneration mechanism), no `_backup_smeared_*` table. It is a MANUAL one-off, not a workflow — it will never appear in GitHub Actions. **⚠ TARGET-DB GOTCHA (lesson `:83` recurred 2026-08-13):** the script loads no dotenv, so a bare `node scripts/backfill/backfill-smeared-enriched-status.js` silently hits the LOCAL dev DB (localhost default in `createPool`) — two dry runs reported dev's 5 rows as if cloud. **Correct invocation from an operator machine: `PG_HOST= node -r dotenv/config scripts/backfill/backfill-smeared-enriched-status.js [--confirm]`** (verified: evaluated=1,416 = the cloud population). **FILE with the C4 commit:** the script prints no connection target — lesson `:83` mandates printing host+database; runbook row needs the invocation incantation. ~~Cherry-pick C2+C6~~ ✅ done 2026-08-12 evening. ~~apply-migrations for 241~~ ✅ done (runs 31626774745 + 31629202253). **`--confirm` awaits explicit operator go.**
> **⚠ FILED 2026-08-13:** Wed 08-12 deep-scrapes slot FAILED on the **chain STEP timeout** (`chain-deep-scrapes.yml:212` default 150 min; job `timeout-minutes: 170` at `:99` — Integration re-verify #17), not a code defect — the scraper's soft time-budget (150−10 = 140 min, `:237-239`) leaves only ~10 min for the remaining 6 steps; the step was axed mid-chain (`status=running` → verdict FAIL). Envelope-sizing follow-up: scraper budget vs step-timeout headroom.
**Authorization:** operator, 2026-08-11 — "resolve these questions and proceed with the plan". v3 FINAL + four review rounds (9 + 9 + 4 reviewer reports + the Ground-truth final pass). Scope: **do it all, land D3 last.**
**All three blocking gaps closed by operator ruling — G1 = lock 44, C4 = split-only, C5 flip = §4.9 pair + follow-up commit. Do not re-open; the rulings are written into C3/C4/C5 below.**
**Domain Mode:** **Backend/Pipeline.** Read `scripts/CLAUDE.md` **+ Spec 44** (missing from its Required Reading — flag).
**Workflow:** WF3 + **WF2's `Spec Update` step grafted in** (WF3 has none; six specs change).
**Rollback Anchor:** `912a640a`. **Pre-Flight:** `node scripts/ai-env-check.mjs`.
**Severity: CRITICAL** (live backup-SLA breach). Spec 05 §4/§5 routing + `tasks/lessons.md` entry required.
**Database Impact: NO.** No migration, no `logic_variables` key, no `_contracts.json` row, no `db:generate`, no `factories.ts`.
**OUTPUT panel: pipeline 5-reviewer roster** (not WF3's 2), per Compliance — six specs + five commits.

> Phase B preserved at `.cursor/phase_b_active_task_INPROGRESS.md`; resumes after.

---

# ⛔ 2026-08-12 SESSION RECORD — C1 SHIPPED · C2 HALTED ON A REFUTED PREMISE · PLAN RE-ORDERED

**Read this whole section before touching C2, C3, C4 or C5. It supersedes the Evidence block below.**

## 1. C1 IS ON MAIN AND DEPLOYED — `cdaea415` (2026-08-12)

The outage was **still live** through 2026-08-12 because C1 had never left the dev machine.
Measured from `pipeline_runs` (cloud) at 2026-08-12:

| pipeline | status | started | error |
|---|---|---|---|
| `chain_permits` | failed | 2026-08-12T12:04Z | `Stopped at step: assert_lifecycle_phase_distribution` |
| `chain_coa` | failed | 2026-08-12T11:51Z | same |
| `chain_permits` | failed | 2026-08-11T12:00Z | same |
| `chain_coa` | failed | 2026-08-11T11:47Z | same |

`git merge-base --is-ancestor 67663a81 origin/main` → **NO**. Cloud runs main; C1 sat on `wf2/deep-scrapes-restore-l0` for a full day while the chains died nightly and `backup_db` kept being skipped.

**Shipped as a CHERRY-PICK, not a branch merge — deliberately.** The branch carries three other commits (`0b230472`, `e279b2b0`, `912a640a` = Phase B B1 + B2) including **`migrations/240_phase_b_massing_watermark_and_pass3_scope.sql`**. Merging the branch would have shipped an unapplied schema migration alongside the hotfix. File sets do not overlap, so C1 cherry-picked clean except one append-conflict in `review_followups.md` (resolved: kept **only** C1's entry; the three Phase B entries stay with their own commits).
**Equivalence proven, not assumed:** `git show` patch of C1 restricted to `scripts/ src/ docs/specs/ tasks/` is **byte-identical** (908 lines both sides) between `67663a81` and `cdaea415`.

> **PROCESS GAP #3 (see §5): every gate in this workflow measures QUALITY; none measures ARRIVAL.** C1 passed red-first evidence, 5 panel reviews, 250 tests, typecheck and lint — and stopped dead on a local branch.

## 2. ⛔ C2's CORE PREMISE IS REFUTED BY LIVE DATA — DO NOT COMMIT C2 AS WRITTEN

The plan asserts at `:22` (and this claim is now propagated into Spec 44 §3, both source-comment blocks, and the test docstrings):

> "**397 permits** spell their base revision `'0'` not `'00'` … those permits could never be marked Stalled and never reactivated."

**The COUNT is right. The INFERENCE is wrong.** Measured independently on cloud (this session) and on dev (Integration reviewer):

* **100% of `revision_num='0'` rows are `permit_type='DCs DeferredFees'`**, `permit_type_classifications.class = 'administrative'` — Development-Charges fee-deferral stubs with **zero physical work**.
* dev: 564 `'0'` rows — `status` Open 559 / Pending Closed 5 · **`issued_date` 0 non-null · `enriched_status` 0 non-null · `last_scraped_at` 0 non-null**.
* **No permit's base revision is spelled `'0'`.** There is no 397-permit blind spot. `classify-inspection-status.js`'s `= '00'` literal was **never dropping a real permit**: `matched_by_literal_00 = 6`, `matched_by_min = 6`, **lost 0, gained 0**.

### What C2 would actually DO if shipped (cloud numbers, this session)

| Measure | Cloud |
|---|---|
| permits with BOTH a `'0'` and a `'00'` row | **184** |
| permits where lexicographic-MIN row is `administrative` while a non-admin row exists | **185** |
| …of those, inside the scraper's target set (`status='Inspection'`) | **71** |
| lexicographic-vs-numeric MIN disagreement | **0 — ⚠ MISLEADING AS ORIGINALLY STATED, corrected below** |

> **⚠ CORRECTION (Schema-Fidelity, 2026-08-12) — my own measurement above was true for the rule I tested and FALSE as a general claim.** My query's tiebreak was `ORDER BY revision_num::int, revision_num` (lexicographic second key), which picks `'0'` over `'00'` exactly as lex-min does → 0 disagreement. With the tiebreak `ORDER BY revision_num::int ASC, length(revision_num) DESC` the two rules disagree on **184 permit_nums — precisely the `'0'`+`'00'` dual population.**
> **Restate as: "numeric ordering fixes NOTHING on its own; the LENGTH TIEBREAK is what disagrees, and it disagrees on exactly the dual population."** The narrow claim survives — the `'10' < '2'` worry IS dead (the single `'2'` row, `23 115885 BLD`, has a `'00'` sibling that both rules pick). But left as written, "disagreement = 0" would justify a lexicographic rule that **provably picks the fee stub for 184 permits**.
> *(Noted for the record: this is the same defect class as the `:22` premise — a true measurement stated as a broader claim than it supports.)*

`MIN(revision_num)` returns the **fee stub**. So C2 would:
1. write the scraped `enriched_status` (e.g. `'Permit Issued'`) onto an `Open` administrative stub → phase **P3** → **NEW cross-check offenders in the exact metric this WF3 exists to drive to zero**;
2. leave the real `'00'` construction row `enriched_status` NULL → it keeps passing `aic-orchestrator.py:180-181` → **re-queued every 7 days forever**. Pre-fix, the unscoped UPDATE at least hit the correct row. **This is a REGRESSION, not a residual.**
3. On dev, **41 of the 170 dual permits are queued right now with `last_scraped_at IS NULL`** — the first post-C2 run hits them immediately.

**The old "buggy" `= '00'` literal was ACCIDENTALLY CORRECT for this population.** C2 does not carry that protection forward and nothing recorded the trade — an **undefended fence**.

**The knowledge was already in this repo and was never consulted:** `.cursor/closed_task_wf3_lpad_revision_num_collision.md:44-54` (closed, Green-Lit WF3, live-DB audit 2026-05-14) characterized this exact population, and **`migrations/138_a_exclude_administrative_from_lead_id.sql:10-25` (APPLIED)** already encodes the convention: administrative rows must be excluded from any logic that treats one row as "the" identity of a `permit_num`.

## 3. THE REAL ROOT CAUSE — "which row IS this permit" has FOUR implementations and ZERO definition

| Spelling | Where |
|---|---|
| `revision_num = '00'` literal | `classify-inspection-status.js`, `classify-permit-phase.js` (`:39,:52,:70`) |
| ~~`LPAD(revision_num,2,'0')` canonical~~ **NOT A SPELLING — see below** | `scripts/lib/leads/lead-id.js:14,68` |
| `MIN(revision_num)` | C2 — a **fifth** spelling, invented here |
| exclude `class='administrative'` | `migrations/138_a` |

> **⚠ CORRECTED 2026-08-12 — there are THREE spellings, not four.** Regression Guardian and Schema-Fidelity **independently** reached the same verdict: `lead-id.js`'s `LPAD` is a **per-row identity FORMATTER**, not a row **SELECTOR**. It maps *every* `(permit_num, revision_num)` to its own `lead_id`; it never asks which row is canonical. It is a **CONSUMER** that keeps formatting every row regardless of what C0 decides — and its `LPAD` is what *caused* the `'0'`/`'00'` collision `138_a` had to fix. The three real spellings are: **`= '00'` literal · `MIN(revision_num)` (C2, refuted) · administrative-exclusion (`138_a`, APPLIED and machine-enforced)**.

**Reviews cannot catch an undefined concept** — there is no spec text to review against, so four rounds correctly verified code-against-plan and the plan-against-itself.

### Predicate scoring (cloud, 127,462 target permits with exactly one non-admin `status='Inspection'` row)

| Candidate rule | Wrong |
|---|---|
| `MIN(revision_num)` — **what C2 ships today** | **123** |
| `ORDER BY revision_num::int ASC, length DESC` (Integration's suggestion) | **53** |
| exclude `class='administrative'` then canonical-min | **53** |

**No rule reaches zero, and that is the finding.** The residual class: permits like `00 128454 CMB` where a valid **non-administrative** `'00'` row exists but the row actually carrying `status='Inspection'` is `'01'` — every candidate picks `'00'`. Also `02 156062 BLD`: `'00'` is `Multiple Use Permit` (**administrative**) while `'01'` is construction — which is why the count is 185 not 184, and which **defeats the numeric-ordering fix**.
**Conclusion: "the base row" and "the row under inspection" are DIFFERENT CONCEPTS and this plan conflated them. That is a definitional question, not a SQL question.** Additionally **119** target permits have *every* row administrative → "write nothing" must be a **defined outcome**, not an accident.

## 4. C2 TEST PANEL — 6 reviewers, run 2026-08-12 (all main-tree: C2 is uncommitted, a worktree would show them HEAD)

### Red-first evidence — captured against pre-fix code at `67663a81`
Method: hash-verified backup of both fix files → `git checkout HEAD --` on **only** the two fix files (tests left in place) → run → restore. Pre-fix state confirmed: **zero** `revision_num` occurrences in the Python scraper; literal `'00'` live at `classify-inspection-status.js:57,:83`.
* **pytest `2 failed, 6 passed`** — red for the RIGHT reason, printing the genuine pre-fix SQL:
  `UPDATE permits SET enriched_status = %s, last_scraped_at = NOW() WHERE permit_num = %s AND enriched_status IS DISTINCT FROM %s` (no revision term), failing independently on both write paths.
* **vitest `2 failed, 93 passed`** — the `MIN(p2.revision_num)` assertion and the "literal `'00'` absent" assertion at `inspections.logic.test.ts:1004`.
* **Restored byte-exact** — index blobs back to `acf831cf` / `954db7ea`, staged diff identical (6 files, +344/−28), both suites green again (8 / 95).

### Findings that SURVIVE adjudication (all in `scripts/tests/test_scraper_revision_scoping.py` unless noted)
1. **`:189-193` the `continue` escape hatch — 4/4 INDEPENDENT HITS** (Gemini CRITICAL, DeepSeek HIGH, Code Reviewer Important, + noted pre-panel). It skips any write also carrying `enriched_status =`. Pre-fix the only captured touch IS the combined write → those 2 passes are **vacuous**; worse, it would **green a future refactor that re-merges the two writes and scopes both** — the exact R4 violation. DeepSeek: the comment at `:190-192` is **backwards**. Fix: assert *at least one* `last_scraped_at` touch is unscoped; never skip.
2. **`:144` — `'revision_num' in sql` blesses the wrong family.** `revision_num = (SELECT MAX(...))` passes. Red only for today's unscoped bug. Fix: require `MIN(`. (Text-level check — **inside** the fake's proven ability, NOT the accepted semantic ceiling.)
3. **`:164-167` — the triple-`or` assertion cannot fail.** `'revision_num = %s' in sql` is true for any parameterized implementation; short-circuits every time. Dead code whose message implies a distinction it structurally cannot make. Fix: delete.
4. **`no_stages` write site has ZERO coverage** — `aic-scraper-nodriver.py:2691-2694` (`last_scraped_at`). Neither fixture sets `error: 'no_stages'`; dropping or scoping that write passes all 8 tests. **Not covered by the file's stated ceiling.** Fix: third fixture.
5. **`emitMeta` reads missing `revision_num`** — `aic-scraper-nodriver.py:3530`, `:3795`, `aic-orchestrator.py:788`. `fetch_base_revision()` is a genuine new read; Spec 47 §R11 violation; understates the producer in `data-lineage-map.md`. JS side already correct → the "deliberate parallel" is asymmetric.

### Adjudicated AWAY (do not re-open)
* **Gemini HIGH / DeepSeek #3 (fake always returns `('00',)`)** — this is **the ceiling the plan already accepted at `:188-189`** ("a FakeCursor cannot catch `= '00'` vs `MIN(...)`; C3's `--confirm` dry-run is the empirical proof"). Not folded. *(The narrow half — assert the MIN SELECT carries `WHERE permit_num = %s` to catch a global-MIN bug — is cheap and worth keeping if C2 is rebuilt.)*
* **DeepSeek #6 "no JS-side coverage" — REFUTED.** The CLI saw only the Python file; the JS side IS pinned by this same diff at `inspections.logic.test.ts:995-1005`.
* **Gemini NIT "docstring too long" — REJECTED.** The header carries the stated ceiling TEST DISCIPLINE requires.
* **`RecordingCursor.rowcount = 1` — REFUTED by Code Reviewer.** All three `cur.rowcount` reads feed local counters only; gate no branch; no assertion inspects them. Inert.
* **`enriched_updates` counter drift — PASS, 2 independent traces** (Guardian + Observability). The code always gated a fixed `+1`, never summed `rowcount`. Meaning unchanged; INFO-only audit row (`aic-orchestrator.py:741`, `threshold: null`); nothing gates on it. *(Its VALUE will drop post-C2 — runbook line, not an incident.)*
* **Stalled-spike risk — PASS.** Only gate reachable is `cross_check_stalled`, threshold **1000** (`seeds/logic_variables.json:1150-1155`) vs a one-time bump ≤397. Ample headroom.

### Corrections to the plan's own text, found by Integration
* **"FIVE permit-grain UPDATE sites" is MISLEADING.** Only **2 of the 5** write `enriched_status` (post-fix `:2667`, `:2744` — both scoped, no site missed); three are cooldown touches that MUST stay row-wide; **`:2691` is `UPDATE permit_inspections`, not `permits`.**
* **C2 silently fixes a SECOND latent bug, undocumented:** pre-fix, rows already holding the new value were never stamped with `last_scraped_at` → permanently queue-eligible. Post-fix every row is stamped every scrape. Strict improvement; record it.
* **`fetch_base_revision → None` is a new observability hole** — the code now KNOWS the permit is absent and discards it with no log/counter, while `queue_outcome('scraped', …)` still records success.
* **Race is benign but real:** same cursor/transaction, `psycopg2` default (no autocommit), READ COMMITTED — a base row inserted between SELECT and UPDATE leaves `base_rev` stale. Relevant because **652 permits have no base row at all** (min `'01'`-`'04'`; 225 of those min-rows are `status='Inspection'`, 196 `'Revision Issued'`).
* **The queue predicate is DUPLICATED** — `aic-scraper-nodriver.py:3701-3717` is a second copy of the orchestrator's eligibility query (cooldown `:3712`). Spec 44 §3 pins only `aic-orchestrator.py:181`. **Both must move together.**
* **R4 ruling CONFIRMED but magnitude overstated:** only **51 non-base rows** currently satisfy the full queue WHERE (47 `year_seq`s). The ruling gets *stronger* after C3 NULLs non-base `enriched_status` (4,347 `Revision Issued` + 768 `Pending Closed` rows would then pass the enriched-status filter with the cooldown as sole brake).
* **`review_followups.md:2883` NOW GIVES WRONG ADVICE** — it instructs mirroring C2 into `classify-permit-phase.js`, which would propagate the stub-targeting regression. Its correct disposition may be **"do nothing"**. Must be rewritten before anyone follows it.
* **Deploy-order note for C3:** the backfill's `EXISTS a base row` guard must use the SAME base-revision rule as C2 or the two disagree on the dual permits.
* **Tripwire warning:** if C2 ships as-is the offender count will NOT reach zero and will gain up to 41 members of a NEW shape (P3 + `'Permit Issued'`). Because C1 made cross-checks non-halting, it appears as persistent `completed_with_errors` — **easy to misread as "C3 backfill not run yet."**

## 5. WHY FOUR REVIEW ROUNDS MISSED IT — process lessons

1. **The plan contains both the antibody and the infection.** `:58` says *"Never state a count as fact in this plan — compute at run time."* `:22` states a count as fact and calls it *"verified exactly."* The count was TRUE; the inference was FALSE. **Observability of a number says nothing about its meaning.** The original query asked "how many rows have `revision_num='0'`?" — one extra column (`permit_type`) would have ended this in 90 seconds.
2. **`pipeline-reality-check` — the ONE role whose job is "are these values domain-plausible?" — was never triggered.** Its CLAUDE.md trigger reads *"pipeline WF1/WF2/WF3 that add or change any enriched/derived **parcel** field."* C2 changes `enriched_status` on **permits**, so by the letter of the rule it was correctly skipped. **The role is data-semantic; the trigger is table-specific. Fix the trigger.**
3. **No gate measures ARRIVAL** (see §1).
4. **What DID catch it:** the two reviewers that read the **live DB and git history** instead of the plan (Integration, Regression Guardian) — the Spec 08 §6.4 "reality grounders". A fifth review round would not have helped; four didn't.

> **PROPOSED RULE (replaces nothing, adds one line):** *any plan line asserting a fact about data must carry the query that produced it, and one grounder must re-run that query and report the ROWS, not the count.*

## 6. REVISED EXECUTION ORDER — operator-approved 2026-08-12

1. **✅ DONE — C1 to main** (`cdaea415`).
2. **PARK C2.** Operator ruling: **revert the `classify-inspection-status.js` half entirely** — it fixes a bug that does not exist and introduces one that does; reverting leaves **zero** known defect (`lost 0, gained 0`). The Python-scraper half cannot be responsibly finished until step 3 defines the rule. **C2's staged work is preserved** at `.cursor/`-adjacent scratch and reproducible from `git diff --cached` (6 files, +344/−28); the 5 surviving test findings above apply to whatever is rebuilt.
3. **NEW WF2 — define "the permit's canonical row" ONCE:** one shared helper + one Spec 44 section + one lock, adopted by all four call sites in §3. Must rule on: (a) administrative exclusion; (b) what "base row" means when the `status='Inspection'` row is NOT the base (the 53); (c) the write-nothing outcome for the 119 all-administrative permits; (d) whether `enriched_status` belongs on the base row or the row under inspection. **After that, C2 is a two-line change against a defined rule instead of a fifth invented spelling.**
4. **THEN** C3 / C4 / C5 as originally planned — C3's guard must consume the step-3 helper.

---

## Evidence (cloud, re-verified independently by 3 reviewers; live at 2026-08-11T18:14Z)
> ⚠ **PARTLY SUPERSEDED — see §2 above.** The counts below are accurate; the `'0'`-spelling INFERENCE at `:22` is refuted. Every `'0'` row is an administrative DCs-DeferredFees stub, not a base revision.
* Offenders **724** and climbing (583 May → 475 → 621 → 690 → 718 → 724). Smear class **1,257**. Backup age **28.08h** vs 25h SLA.
* **Root cause is categorical:** offender rate at base revision **0/1587 = 0.0%**; at every non-base revision **99.5–100%**. 99.0% of offenders are non-base.
* **Why:** `aic-scraper-nodriver.py` has **five** permit-grain UPDATEs (`:2629, :2637, :2650, :2699, :2708`), zero occurrences of `revision_num` in the file. AIC has no revision dimension. Non-base rows carry their own CKAN status (`"Revision Issued"` → P8), outside the allowed set.
* **The halt:** one class-blind throw at `:837` → `run-chain.js:622-623` `failedStep = slug; break;` → permits dies at step 25/33, coa at 14/16 → **10 steps skipped incl. `backup_db`**.
* **Why the fallback didn't save it:** `pipeline-watchdog.yml:190-192` suppresses the `backup-db.js` fallback while permits is running (the race guard — **correct, do not weaken**). Permits *was* running, then died at 25. **Guard × halt = the 28h gap.**
* ~~**397 permits** spell their base revision `'0'` not `'00'` (581 `'0'` permits, only 184 with a `'00'` row) — verified exactly.~~ **⛔ REFUTED 2026-08-12 — THE COUNT IS RIGHT, THE INFERENCE IS WRONG.** 100% of `revision_num='0'` rows are `DCs DeferredFees`, `class='administrative'` fee stubs — zero with `issued_date`, zero that ever held `'Active Inspection'`/`'Stalled'`. **No permit's base revision is spelled `'0'`.** This single line is the root of C2's defect and it propagated into Spec 44 §3, both source-comment blocks and the test docstrings — **all must be corrected before C2 is rebuilt.** See §2 of the SESSION RECORD.

---

## ⚠ FINAL-PASS CORRECTIONS — THESE SUPERSEDE EVERY CITATION BELOW (Ground-truth, 2026-08-11T18:43Z)
**Read this block before touching any file. 11 errors found in v3's own citations.**
1. **`infra.test.ts` block is `:457-468`, NOT `:456-467`.** `:456` is blank, `:468` is the closing `});`.
   Deleting the v3 range leaves a **dangling `});` and breaks the file.** C1 says DELETE — use `:457-468`.
2. **`pipeline-watchdog.yml` race guard is `:191-193`, guard term at `:193`** (`permits_running != 'true'`).
   v3's `:190-192` **excludes the very line being fenced**.
3. **`run-chain.js` `metaObj` block is `:654-663`** (`:654` decl, `:655` step_verdicts, `:657-663` budget_stopped).
   v3's `:645-661` points at the **`chainError` ternary** — an implementer would edit the error string.
4. **The jsonb merge is `:670`** (inside the UPDATE at `:667-672`), not `:664-670`. The "no schema change" claim is correct; only the citation was wrong.
5. **Throw: always write `:836-838`, never `:837`.** `:836` holds the `if (failures.length > 0)` guard — editing `:837` alone leaves the OLD halt condition intact.
6. **Spec 84 cross-check bullets are `:1178-1180`**, not `:1179-1181` (`:1181` is blank). v3's range **drops `:1178`, the stalled cross-check** — which C1 also makes non-halting (`:624`).
7. **`assert-data-bounds.js`: say "3 exception-DERIVED pushes (`:679`, `:810`, `:941`)", not "catch sites".**
   `:679`/`:810` are `errors.push(err.message)` in an `else` branch, NOT `catch` statements — grepping `catch` there finds nothing. Only `:88` and `:940` are catches. **Threshold-derived count is 24, not ~25** (27 total − 3).
8. **Spec 48 §3.7 is the WRONG target** (it is "First-deploy spike pattern"). Spec 48 has no producer/consumer section — retarget the `step_completeness` contract to **§3.5 (`:145`) or §4 (`:230`)**.
9. **Seeds claim corrected:** only `:2744` says "→ pipeline halt"; `:2751`/`:2758` say `1=FAIL (rare)` / `1=FAIL (after verifying…)`. Conclusion (no edit needed) survives; the stated reason does not. All three DO cite phantom **§3.4**.
10. **Phantom §3.4 citations are ≥20 across ≥6 files**, not 8 — incl. `migrations/150` ×8, `migrations/119:5`, `148:53`, `151:37`, and **`assert-…js:732/:738/:744`, which are LIVE operator-visible audit-row `threshold` strings** pointing at a nonexistent section.
11. **C3 blast radius is 1,059 distinct permits, not ~577** (577 was the distinct-permit count of the old 690 snapshot). The don't-touch-`last_scraped_at` ruling gets STRONGER; the number was wrong.

**Contradictions to resolve while writing:**
* **X2 — add one line to C5:** post-C1 a cross-check FAIL yields `completed_with_errors`, which is **NOT** in `OK_STATUSES`, so `classifyStepCompleteness` is never consulted on those nights. That is correct (already red via `classifyVerdict`) but unstated — C5 is the net for chains that die silently *inside* `OK_STATUSES`.
* **X3 — C4 alters halting posture on FOUR chains**, not one: `assert_data_bounds` is permits #22 (strands 11), coa #8 (8), sources #26 (2), deep_scrapes #5 (3). Budget for all four.

**Gaps — RESOLVED by operator 2026-08-11 (were STOP-AND-ASK; do not re-open):**
* **G1 — RESOLVED: `ADVISORY_LOCK_ID = 44`.** Spec 47 §5.2 default ("spec number when globally unique") applies unmodified: Spec 44 is the owning spec (the `enriched_status` base-row rule) and **44 is free** — verified by scanning every live `const ADVISORY_LOCK_ID` under `scripts/` (122 = `one-time/wf2-p13-null-legacy-cost-tail.js`, 123 = `dispatch-notifications.js`; 44 unused). No §5.2 exception note needed, and the §47:137 "SPEC LINK number MUST match `ADVISORY_LOCK_ID`" rule is satisfied by construction.
  **Still TWO more edit targets or `npm run test` reds:** `docs/specs/01-pipeline/47_pipeline_script_protocol.md` §A.5 registry table **and** `src/tests/pipeline-advisory-lock.infra.test.ts` `LOCK_ID_REGISTRY` (`:24-153`). Both are checked against each other by the `:303` agreement test — edit both or neither.
* **G2 — RESOLVED: `scripts/backfill/backfill-non-base-enriched-status.js`.** Convention `scripts/backfill/backfill-<thing>.js`, dir precedent `backfill-permits-location.js`. Rename is a one-word operator call if preferred; the `:185` `EXTRA_LOCK_DIRS` scan already covers `scripts/backfill`, so the file is lock-scanned the moment it lands.
* **G3 — C2's "one shared fragment" CANNOT cross Python↔JS.** Specify one Python constant + one JS constant as deliberate parallels.
* **G4 — the assert file has NO `module.exports` and NO `require.main` guard today** (zero grep hits). C1's guard/export is net-new plumbing; specify placement relative to the top-level `pipeline.run(` call or requiring it in `.logic.test.ts` still executes the script.

**All four previously-unverified load-bearing claims: CONFIRMED.** 7 `failures.push` sites at `385/410/439/549/624/645/665` — exactly 3 classes, no eighth · `prevChainFailed` sole read at `:565` · data-bounds substance · `assert-engine-health` verdict can never be FAIL (`:282`), exclusion correct.

**Live at 18:43Z: offenders 733 (+9 in 30 min, ~+9/hr) · smear 1,269 rows / 1,040 permits · backup 28.58h.**
Never state a count as fact in this plan — compute at run time.

## Execution order — **SIX commits** (C0 inserted 2026-08-12), D3 LAST (operator-directed)
> **ORDER IS (amended 2026-08-13, operator): C1 ✅ → C2 ✅ → C6 ✅ → [MERGE + APPLY 241 ✅] → C3 ✅ (run on cloud ✅) → C4 (output panel in flight) → C7 (loader invalidation — inserted, see its section) → C5 LAST.** ~~C0~~ was inserted 2026-08-12 and **DELETED the same day by its own panel** — the canonical-row concept it would have defined turned out to be unnecessary. C6 was added the same day (independent CRITICAL, own commit). **The merge + migration apply is now the gate: C3 must not run against cloud until C2 is live on main**, and the C6 audit rows do not start watching until then either. C4/C5 are independent of all of this.

### C1 — D1: per-failure halt classification  *(stops the outage)*
`scripts/quality/assert-lifecycle-phase-distribution.js`. `failures[]` has **7 push sites in exactly 3 classes**:
halting → E.5 bands (`:385, :410, :439`) + `unclassified_count` hard limit (`:549`); **non-halting** → `cross_check_*` (`:624, :645, :665`).
**Shape (5 edited lines):** add `const haltingFailures = []`; the four halting sites push to **both** arrays; the three `cross_check_*` sites push to `failures` **only**; `:836-838` becomes `if (haltingFailures.length > 0) throw`.
**DO NOT** use `failures.push({class,msg})` — breaks `:837` `join('\n')`, the `:797` log payload, and the E.5 `renderPrefix()` text that migration 150 persists.
**`:794` verdict UNCHANGED** — all 7 classes still yield FAIL, which is what keeps `check-chain-verdict.js:104` red.
**Also in C1 (AD1, mandatory):** `run-chain.js:225` `prevChainFailed` keys only on `'failed'` → add `|| 'completed_with_errors'`. One consumer (`:565`), verified.
**Also in C1:** `if (require.main === module)` guard (precedent: `audit-fk-orphans.js`, `check-chain-verdict.js:201-205`) + export `classifyHaltDecision({unclassifiedCount, unclassifiedMax, seqBandsFailing})`.
**Retire (knowingly):** `src/tests/assert-lifecycle-phase-distribution.infra.test.ts:456-467` — **DELETE, do not amend.** Its regex assumes `if (failures.length > 0) { throw }`; leaving it invites preserving a dead shape to keep it green, silently reinstating the bug. **Fence statement:** it defended "emitSummary precedes the throw so the audit row survives a FAIL" — now defended behaviourally by C1's Case B.
**Amend:** `chain.logic.test.ts:1610-1611` (names this script as the always-halts counter-example — now conditional).

### C0 — ⛔ **RESOLVED AND DELETED (2026-08-12) — the panel killed it. Do not build this.**
**VERDICT after the 6-lens panel: C0 is the wrong solution to a mis-framed question. `enriched_status` is a PER-ROW refinement of that row's OWN `status`, not a per-permit projection. There is no canonical row to define.**

**THE ANSWER — `AND status = 'Inspection'` on the two `enriched_status` UPDATEs. Nothing else.**

**Evidence, cloud-measured 2026-08-12:**
* **The probe that settled it** — of 4,183 rows carrying `enriched_status` today: **2,833 have `status='Inspection'` (kept)**, **1,350 do not (dropped)** — and the 1,350 **ARE THE SMEAR**: `Revision Issued`+`Active Inspection` 654 · `Revision Issued`+`Permit Issued` 453 · `Pending Closed` 98 · `Examiner's Notice Sent` 43. Dropping them **is the fix**, achieved structurally.
* **Administrative rows are excluded BY CONSTRUCTION** — **0 of 21,827** `status='Inspection'` rows in the write population are administrative. Q2's admin-exclusion is **redundant on this path**, which also means C0 never has to repair the fallen 138_a fence.
* **Ground-truth's domain ruling on the 53** — the base row is `Closed`/`Pending Cancellation`; writing an inspection status there resurrects a closed permit. **Only the `status='Inspection'` row is correct.** 2 independent lenses agree.
* **Exposure is 2.2%, not 22%** — of 4,416 actually-scraped permits, only **97** have no `Inspection` row (4,314 exactly one, 5 more than one). *(My earlier 6,161/22% was a SUPERSET measurement — every `folderSection` sibling sharing a `year_seq` — not the write set.)*
* **My "AIC statusDesc vs DB status" blocker was WRONG** — `aic-scraper-nodriver.py:2624` filters `target_folders` on **`folderSection`**, never on status; `statusDesc` appears only in the log line at `:2633`. No mismatch exists. *(That is also WHY the smear reaches `Revision Issued` rows.)*
* **Multi-Inspection permits need no tiebreak** — 413 permits have 2 concurrent `Inspection` rows, 32 have 3, 3 have 4 (e.g. `01 180337 BLD`, `'00'` and `'01'` both live). Write to **all** of them: each is genuinely under inspection, each derives the same phase, none becomes an offender.

**WHAT REPLACES C0 — rebuild C2 as a TWO-LINE CHANGE (smaller than the currently-staged C2):**
1. `aic-scraper-nodriver.py` — add `AND status = 'Inspection'` to both `enriched_status` UPDATEs (`:2667`, `:2744`). **DELETE `BASE_REVISION_SQL` + `fetch_base_revision()` (`:281-289`)** — that removes the extra per-permit SELECT, the READ-COMMITTED race, the `None` observability hole, and the Spec 47 §R11 `emitMeta` gap in one stroke.
2. **REVERT `classify-inspection-status.js` entirely** (operator ruling; and it is *positively* correct — its step 1 already keys on `enriched_status='Active Inspection'`, which under a status-scoped writer only exists on `Inspection` rows). **Revert the `inspections.logic.test.ts` "Bug 4" rewrite in the same commit** — leaving it reds the suite.
3. `last_scraped_at` stays **row-wide, unchanged** (R4 survives untouched).
4. **C3's guard becomes `NULL enriched_status WHERE status <> 'Inspection'`** — precise, and it dissolves the C2/C3 rule-agreement problem.
5. **Spec 44 §3** — replace the `enriched_status` grain row with *"the row(s) whose own `status` the scrape is about (`status='Inspection'`) — a per-row refinement of `status`, never a per-`permit_num` projection."* **RETRACT both false claims:** the "397 permits" paragraph AND **"The portal has no revision dimension"** (`docs/reports/2026-07-30-scraper-research-digest.md:44` records a MEASURED curl showing the folders response carries **`folderRevision`**, which `:2393`/`:2517` discard). Keep the `last_scraped_at` negative rule verbatim.
6. **`review_followups.md:2883` → DELETE, don't rewrite** — `classify-permit-phase.js` measures **lost 0 / gained 0** (49 eligible rows; `'00'` 48, lex-min 48). Do nothing there but fix its comment.
7. **Surviving C2 test findings** (§4): items 1 (the `continue` escape hatch) and 4 (`no_stages` fixture) remain live. Items 2 and 5 **evaporate** — no `MIN(` to assert, no new read to declare.

**✅ THE 97 — RESOLVED, MEASURED 2026-08-12 (was the last open item).** Their rows are **`Pending Closed` 109 (95 carrying `enriched_status`)**, `Closed` 1, `Permit Issued` 1. They **closed after being scraped**. Writing an AIC inspection status onto a `Pending Closed` permit is exactly the smear class — so **"no write" is the CORRECT outcome, not a gap.** The hypothesis is now measured; nothing blocks the rebuild.

---
### C6 — ✅ COMMITTED `5ef51de7` (2026-08-12) · **MIGRATION 241 PENDING CLOUD APPLY**  *(Database Impact: YES)*

> **📌 MIGRATION `241_fix_lead_id_trigger_column_scope.sql` — COMMITTED, NOT YET APPLIED TO CLOUD.**
> **Contents:** ① `DROP`/`CREATE TRIGGER trg_permits_lead_id BEFORE INSERT OR UPDATE OF permit_num, revision_num, **permit_type**` (the function from 138_a is correct and deliberately NOT redefined) · ② repair `UPDATE permits SET lead_id = NULL` for the 1,190 administrative rows (unbatched — bounded and small, matching 138_a's own precedent; mig 240's batching ruling is about magnitude and does not reach here) · ③ apply-time DO-block verification that RAISEs on any remaining administrative `lead_id` and WARNs on residual duplicate groups.
> **`-- UP` / `-- DOWN` blocks present; validated clean** by `node scripts/validate-migration.js`. **DOWN is manual-rollback-only and MUST NOT revert Component 2** — restoring those `lead_id`s re-creates the 171 duplicate groups.
> **statement_timeout:** every statement is well under the cloud 2-min cap (largest touches 1,190 rows) — no special plan needed per `apply-migrations.yml`'s own contract note.
> **APPLY PATH:** `apply-migrations.yml`, `workflow_dispatch` ONLY, `production-db` environment approval + required reviewer, `dry_run` defaults **true**. Runbook §3 rule 2a.
> **⚠ ORDER — merge BEFORE dispatch.** `migrate.js --verify` (`:150-160`) iterates the REPO's files against the applied set and never checks the reverse, so applying from a branch would not red main's pre-flight — it is *technically* safe. Do it anyway in this order because ① C2 (`eff28a7e`) is what stops NEW smear and must not repeat C1's never-arrived mistake, ② C6's standing audit rows live in `assert-global-coverage.js`, which runs **from main** — apply the migration without the code and the invariant meant to prevent recurrence is not watching, ③ cloud DB and main stay coherent.

**What shipped in `5ef51de7`:** migration 241 · the two standing audit rows (`lead_id_administrative_drift`, `lead_id_duplicate_groups`, both FAIL-on-nonzero) in `assert-global-coverage.js` · both JS ledger bypasses (`load-permits.js`, `classify-lifecycle-phase.js`) now call `deriveLeadId` instead of a hand-rolled `padStart` · the `load-permits.lifecycle-status-history.infra.test.ts` fence amended (intent preserved — `deriveLeadId` is pure JS and the negative assertion against SQL `LPAD` is unchanged).

#### Original diagnosis (retained — this is the record of the defect)

**✅ MECHANISM IDENTIFIED 2026-08-12 (ground-truth reproduced the effect but could not find the cause; this is the cause).**
```sql
CREATE TRIGGER trg_permits_lead_id
  BEFORE INSERT OR UPDATE OF permit_num, revision_num   -- ← tgattr = {permit_num, revision_num}
  ON public.permits FOR EACH ROW EXECUTE FUNCTION permits_set_lead_id()
```
**The trigger is NOT "inert on UPDATE" — it is correctly scoped, to the wrong column set.** A row's administrative-ness is a function of **`permit_type`**, which is **absent from the trigger's column list** — while `permit_type` **IS** in `load-permits.js:336`'s `ON CONFLICT DO UPDATE SET` list. So when CKAN reclassifies a permit into an administrative type on re-ingest, the trigger **never fires** and the stale `lead_id` persists forever. For an existing row, `permit_num`/`revision_num` are the PK and essentially never change — so the UPDATE path is dead in practice.
**This explains all three of ground-truth's reproductions exactly:** `UPDATE ... SET last_seen_at = now()` (not in scope → no fire); the controlled `permit_type` flip construction→`DCs DeferredFees` (not in scope → no fire). `tgenabled='O'` and `tgtype=23` were both red herrings — `tgtype` does not encode column scoping; **`tgattr` does**, and it reads `{permit_num, revision_num}` (confirmed live).

**Live blast radius (cloud, 2026-08-12):** 1,190 administrative rows carry a non-NULL `lead_id` · **171 duplicate-`lead_id` groups** in `permits` (e.g. `permit:20 202524 BLD:00` × 2 — the DCs stub and the real construction permit sharing one key) · **1,303 `lifecycle_status_history` rows** under colliding keys · only 34 rows repo-wide have NULL `lead_id`, all 2026-vintage. First-seen clusters **Feb/Jun/Jul 2026 = ongoing active drift, not pre-migration residue.** Migration `138_a`'s own post-condition (`RAISE EXCEPTION` if any administrative row retains a `lead_id`) **would fail if re-run today.** Corrupts `lead_id`-keyed joins into `cost_estimates`, `trade_forecasts`, `tracked_projects`.

**FIX (three parts, one commit — NOT yet authorized, Database Impact YES):**
1. **Re-scope the trigger** to include `permit_type`: `BEFORE INSERT OR UPDATE OF permit_num, revision_num, permit_type`. *(Consider whether an unscoped `BEFORE INSERT OR UPDATE` is safer — the scoping bought nothing and cost this; weigh against per-row cost on the nightly upsert.)*
2. **Re-run `138_a`'s repair** — NULL `lead_id` on the 1,190 administrative rows. **Must be batched** per the mig-240 panel ruling (485K rows took 94s against a 120s ceiling); 1,190 rows is small, but `permits` is a 1,007 MB / 28-index heap.
3. **Close the JS bypasses** — `load-permits.js:423` and `classify-lifecycle-phase.js:911` write `lifecycle_status_history.lead_id` with inline `padStart(2,'0')` and **no admin gate**, bypassing `deriveLeadId` entirely. They must call the shared deriver or the ledger re-drifts regardless of the trigger.
4. **Add the invariant as a standing audit row**, not a one-shot DO block — `138_a`'s check passed at apply time and silently went false afterward. That is the actual lesson.

**⇒ BINDING CONSTRAINT ON ALL FUTURE WORK (independent of C6 landing):** re-derive administrative/construction class **LIVE from `permit_type_classifications` at query time**. **NEVER** use `permits.lead_id IS NULL` (or any persisted flag) as a proxy for "this row is administrative" — measurably wrong for 1,190+ rows right now.

### Other findings worth keeping (ground-truth, cloud)
* **`'00'` is NOT a pure-construction spelling — 594 `'00'` rows are administrative** (`Multiple Use Permit`). The collision was never only about `'0'`.
* **Q3's 119 all-administrative permits carry live `status='Inspection'` AND real `issued_date`s** (98 `Multiple Use Permit` + 21 `Site Inspection(Scarborough)`). **Open domain question: is "administrative = zero physical work" actually true for these types, or is the classification coarse?**
* **Q4 is 654, not 652 — and 95% (620) have exactly ONE row total.** Not "a base row is missing" but "the only row is numbered non-`'00'`". Under the status rule they need no special handling.
* **Do NOT generalize any resolver** — `permit_parcels`, `cost_estimates`, `permit_trades` key on **every** revision, correctly. This was only ever an `enriched_status`/lifecycle-phase problem.
* **CoA does not share this shape** — `coa_applications.application_number` is 1:1 (33,562 = 33,562 distinct). Permits-only.
* Plan numbers **185 / 71 / 184 / 127,462 / 53 / 119 all reproduced exactly.** The critic's A2 stands as a *scoping* correction: 127,462 is right for "permits with one non-admin Inspection row" but is **not** the scraper's write population.

---
### C0 — original framing (SUPERSEDED — retained for history only)

**Why it exists:** §3 of the SESSION RECORD — four spellings of one concept, zero definitions, and predicate scoring proves **no candidate rule reaches zero** (MIN wrong 123, best alternatives wrong 53 / 127,462). C2 cannot be written against an undefined term; writing a fifth spelling is what produced this defect.

**Deliverable (all three, one commit):**
1. **One shared helper** — the single canonical-row resolver, callable from **both** languages (one JS export + one Python constant as the deliberate G3-style pair, since no fragment crosses Python↔JS).
2. **One Spec 44 section** defining the term, its edge cases, and the negative rules.
3. **One lock** pinning the definition, plus adoption at the `enriched_status` write sites ONLY: `classify-inspection-status.js`, `classify-permit-phase.js` (`:39,:52,:70`), and whatever C2 becomes.

> ⛔ **`scripts/lib/leads/lead-id.js` IS NOT A C0 CALL SITE — corrected 2026-08-12 after the Guardian pass. An earlier draft of this section listed it and that was WRONG.**
> **Q5 as originally posed is a CATEGORY ERROR.** `LPAD(revision_num,2,'0')` is a per-row identity **generator** — it labels every qualifying row. C0 needs a row **selector** — which ONE row among several is the permit. LPAD never selects, so it cannot "become the definition"; there is no tiebreak to add.
> **The hazard:** `lead_id` is live production identity — `permits.lead_id`, **`cost_estimates.lead_id` (245,785 NOT-NULL + UNIQUE rows)**, `trade_forecasts`, `tracked_projects`, `lead_analytics`, `lead_trades`, `lead_parcels` — and `get-lead-feed.ts` depends on the exact zero-padded shape for **cursor sort ordering** (`fefc2a39`). Touching the derivation re-keys all of it with no migration budgeted. **`closed_task_wf3_lpad_revision_num_collision.md` ALREADY REJECTED exactly this** (its Option A, "drop LPAD and re-derive", was rejected for blast radius in favour of Option B, "exclude administrative").
> **The only safe borrowing:** C0 defines the administrative-exclusion predicate; `lead-id.js` may CALL it; what LPAD computes for non-administrative rows does not change.

**THE QUESTIONS THE PANEL MUST ANSWER — these are the deliverable, not the implementation:**
* **Q1 — Is `enriched_status` even a base-row concept?** AIC answers per `permit_num`; the row that carries `status='Inspection'` is NOT always the base row (**53 permits**, e.g. `00 128454 CMB`: valid non-admin `'00'` exists, Inspection row is `'01'`). Should the write target the *base* row or the *row under inspection*? **The plan conflated these and that conflation is the root defect.**
* **Q2 — Administrative exclusion.** `migrations/138_a` (APPLIED) already established that administrative rows must be excluded from "which row IS this permit" logic. Does the canonical rule adopt it? Note `02 156062 BLD` proves the collision is NOT only the `'0'`/`'00'` spelling — its `'00'` row is itself administrative (`Multiple Use Permit`) while `'01'` is construction.
* **Q3 — The write-nothing case.** **119** target permits have EVERY row administrative. Skipping must be a *defined outcome with telemetry*, not a silent `if base_rev is not None` fallthrough.
* **Q4 — No base row at all.** **652** permits have min `'01'`–`'04'` (225 of those min-rows are `status='Inspection'`, 196 `'Revision Issued'`). Writing an AIC status onto a `'Revision Issued'` row recreates the smear class in miniature.
* **Q5 — Does the existing `LPAD(revision_num,2,'0')` canonical form (`lead-id.js`) become THE definition?** It is the only existing spelling with repo-wide standing. It matches two rows for the dual permits, so it needs a tiebreak.
* **Q6 — Grain separation survives.** `last_scraped_at` must stay row-wide (R4) regardless of what C0 decides. C0 defines the *identity* row, not the *cooldown* grain.

**FENCES C0 MUST PRESERVE — "if C0 picks rule R, fence F breaks" (Regression Guardian, 2026-08-12):**
1. **Denominator integrity** (`classify-permit-phase.js`, lock "Bug 3: Cross-Revision State Bleed", `inspections.logic.test.ts:1087-1092` — *"Without scoping, an UPDATE hits all revisions and inflates metrics by 3-5x"*). **BREAKS IF** C0 picks a filter-only rule (`WHERE class <> 'administrative'`) without ALSO collapsing to exactly one row per `permit_num` — you stop inflating on admin rows and still inflate 3-5× across construction-revision siblings.
2. **Only one row gets the inspection-derived write** (lock "Bug 4", `:984-1005`). **BREAKS IF** C0 keeps unmodified `MIN(revision_num)` — the 185-permit population.
3. **Administrative rows never carry an identity role** (`migrations/138_a`, APPLIED). **BREAKS IF** C0 omits the class exclusion. **Well-defended in the `lead_id` ecosystem, completely undefended in the `enriched_status` ecosystem — closing that gap IS C0's job.**
4. **`lead_id` stays per-`(permit_num,revision_num)`** — see the ⛔ block above.
5. **"Base row" ≠ "row under inspection" is a PRE-EXISTING GAP, not a regression.** No commit ever separated them; both the `'00'` literal and `MIN()` conflate them. **BREAKS IF** C0 resolves ONE identity concept and uses it for BOTH write-targeting AND denominators — the 53/119/652 residual classes prove these are different questions.

**The LPAD-collision class has RECURRED THREE TIMES** — `4b9ff32d` (mig 138_a, 2026-05-14), `fefc2a39` (lead-feed cursor sort), `4b7b634b` (P25 `lead_analytics` cardinality crash, 2026-07-13; `review_followups.md:2596-2607`). **"Exclude administrative" closed one instance, not the class.** C0 must state why its rule closes the class rather than a fourth instance.
**`tasks/lessons.md:38` ALREADY carries the general lesson** (*"exclude administrative-class records at every write site"*) — it was scoped to the lead_id ecosystem and never generalized to `enriched_status`. **The lesson existed; its scope was too narrow.** C0 must re-scope that lessons entry, not add a new one.

**SCHEMA-FIDELITY RULINGS (2026-08-12, live-measured w/ EXPLAIN plans) — proposed answers to Q1-Q6, pending operator ruling:**
* **Q1 → THE BASE ROW, not the row under inspection.** The schema has ALREADY ruled: **`permit_inspections` carries `UNIQUE (permit_num, stage_name)`** — a hard constraint declaring an inspection stage belongs to a `permit_num`, not a revision. Revisions *cannot* hold separate inspection records. And "row under inspection" is **ambiguous for 448 permits** (413 with 2 Inspection rows, 32 with 3, 3 with 4) vs the base row's **55**. Both branches need a tiebreak; the base-row branch needs an 8× smaller one, and the base row is the *stable* choice.
* **Q2 → ADOPT administrative-exclusion.** Only machine-enforced precedent in the repo (`138_a` + 4 DO-block invariants). **The join is TOTAL**: `permits.permit_type IS NULL` → 0 rows; unclassified permit_types → 0. Resolves **90.8%** of permit_nums outright (209,023 with exactly one non-admin row + 1,005 with none).
* **Q3 → `NULL` IS THE DEFINED OUTCOME** for **119** target / **1,005** total all-administrative permits. Caller must treat NULL as a defined result **with a counter**, never a silent fallthrough.
* **Q4 → CLOSED BY ADOPTING Q2.** The 652 no-base-row permits are not a special case under admin-exclusion + numeric-min: the resolver returns their lowest non-admin row, the correct and only available answer. They only break under a `= '00'` literal.
* **Q5 → NO** (category error — see the ⛔ block above).
* **Q6 → UNAFFECTED.** `last_scraped_at` has no constraint tying it to any identity concept; **R4 survives C0 untouched.**
* **DATABASE IMPACT: NO — recomputed predicate, NO marker column.** Ranked reasoning: ① canonicity is a **set-level** property while every derived column on `permits` today (`lead_id`, `location`) is row-local, so no existing trigger shape can maintain it — a `BEFORE ... FOR EACH ROW` trigger would read siblings and write other rows inside `load-permits.js:333-335`'s batched multi-row upsert (deadlock exposure on the ingest hot path); ② three live staleness paths no trigger covers (a later-arriving lower revision; `permit_type` flipping via the ingest `ON CONFLICT DO UPDATE`; `permit_type_classifications` being reclassified with **no `permits` write to hook**); ③ a partial unique index enforces "at most one" but **cannot** enforce "at least one" (1,005 legitimately have zero), so a trigger bug becomes a **nightly ingest hard-abort**; ④ the migration cannot be atomic — `permits` ∈ `validate-migration.js:33 LARGE_TABLES` forces `CONCURRENTLY`, which `migrate.js:247` routes down the **non-transactional best-effort** path, alongside a 230,275-row backfill on a **1,007 MB heap with 28 indexes** (precedent: mig 240's 485K-row backfill took **94s against a 120s ceiling**); ⑤ **zero perf upside** — see below. **A marker would be a FIFTH persisted spelling, with the DB asserting it is authoritative.**
* **ONE DEFINITION, TWO SQL SHAPES** (measured, `EXPLAIN`): the **correlated** form costs **4.19** for the scraper's per-permit UPDATE (`permits_pkey` prefix scan — `permit_num` is the leading PK column, **no standalone index is missing**). But admin-exclusion defeats PG's `MIN → Limit 1` InitPlan rewrite, making the correlated form **3.2× costlier (1.30M vs 406K)** for the two *set-based* callers. A **`DISTINCT ON` CTE costs 166K — ~8× cheaper than the admin-excluding correlated form** while carrying both the exclusion and the tiebreak. **The helper must expose two fragments, not one.**
* **TIEBREAK: `revision_num::int ASC, length(revision_num) DESC`.** All 17 distinct values are numeric today (`revision_num !~ '^[0-9]+$'` → **0 rows**), but **nothing enforces it** — no CHECK, no domain, no ingest validation; values come straight from CKAN. A single `'A'` in a future feed would abort the scraper UPDATE *and* both classifiers simultaneously. **Add `CHECK (revision_num ~ '^[0-9]+$') NOT VALID`** (cheap, no rewrite; precedent `migrations/155:44-51`) or use a non-crashing form.
* **⚠ THE REAL TIEBREAK SCOPE IS 21,252, NOT 185.** `21,252` permit_nums have **more than one non-administrative row**. Admin-exclusion alone resolves 90.8%; the tiebreak governs the rest — and today's `= '00'` literal happens to answer **20,600 of them correctly by accident**. **C0's spec must state 21,252 as the scope**, not 185 (which is only where the *current* rules visibly disagree).

**⚠ F1 — THE REGRESSION HAS ALREADY HAPPENED, 9 TIMES, ON CLOUD.** Nine `class='administrative'` rows already carry a scraped `enriched_status`: `DCs DeferredFees` rev `'0'` × 8 `Active Inspection` + 1 `Permit Issued`; `Site Inspection(Scarborough)` rev `'00'` × 1 `Examination`. **This contradicts the dev-DB reading of "`enriched_status` 0 non-null" in §2** (different databases — cloud vs dev; both readings are correct for their own DB). **Consequence: C3's backfill scope must include administrative-row cleanup, not only non-base cleanup.**
**Separate defect to file:** `'Examination'` is **not** in the documented value set — `COMMENT ON COLUMN permits.enriched_status` (`migrations/059:17`) declares only `Permit Issued, Active Inspection, Not Passed, Stalled, Inspections Complete`. Unconstrained `VARCHAR(30)`, no CHECK.
**⚠ F6 — ORDER OF OPERATIONS:** Spec 44 §3 and `classify-inspection-status.js:29-43` both carry the refuted "397 permits" text and the `MIN(revision_num)` rule **as spec/source truth right now** (uncommitted C2). **They must be reverted BEFORE C0 writes its replacement section**, or the spec will hold two contradictory definitions simultaneously.

## ⚖ C0 PANEL VERDICT (5 of 6 reporting, 2026-08-12) — UNANIMOUS ITEMS, THEN THE LIVE DISPUTE

### Settled — multiple independent agreement, do not re-open
* **Q5 = NO. `lead-id.js` is OUT OF SCOPE.** 3/3 (Guardian · Schema-Fidelity · Integration), each reaching "category error" independently. Also: the plan's citation `lead-id.js:14,68` points at **two COMMENTS**; the implementation is `:74-76`.
* **NO PERSISTED MARKER. `Database Impact: NO` confirmed.** 2/2 (Schema · Integration). **Integration's proof is decisive: `permits.lead_id` IS ALREADY a trigger-maintained derived marker on this exact table, and it has silently drifted for months** (see below). A canonical-row column would rot identically — worse, since its truth depends on sibling rows. If a materialization is ever wanted, prefer a **view** (`permits_canonical`), self-healing by construction.
* **`MIN(revision_num)` is wrong at scale.** My cloud measurement over the REAL write population: it picks a **non-`Inspection` row 6,205 times** — not 185. The 185 figure was the subset where the *current* rules visibly disagree.
* **G3 IS REFUTED — a shared fragment DOES cross Python↔JS today.** `scripts/lib/status_mapping.json` is loaded by BOTH `aic-scraper-nodriver.py:242-247` (`json.load`) and `src/lib/inspections/parser.ts:2-7` (`require` + cast — `tsconfig.json:27` excludes `scripts/`, which is why it is untyped). **Delete the "deliberate parallel constants" language from the plan, Spec 44 and both source comments — it documents a constraint that does not exist and will be cited again.** Mechanism for C0: `scripts/lib/canonical_permit_row.json`. **Gap: no test pins the two `status_mapping.json` consumers in parity — C0's lock must pin both loaders.**
* **`classify-permit-phase.js` adoption buys NOTHING — measured.** Its eligible set is 49 rows; `'00'` matches 48, lex-min matches 48, **lost 0 / gained 0**. Do not sell adoption there as a fix.

### ⛔ NEW CRITICAL — the 138_a convention is CODE-LIVE but DATA-BROKEN on cloud (Integration)
Q2 rests on *"138_a (APPLIED) already encodes the convention."* The function and trigger are live and correct. **The data is not:**
* **1,190 of 1,190 administrative rows carry a non-NULL `lead_id`** (e.g. `25 193153 B01` rev `'0'` `DCs DeferredFees` → `permit:25 193153 B01:00`). **Migration 138_a's own post-condition (`RAISE EXCEPTION` if any remain) WOULD FAIL IF RE-RUN TODAY.**
* **171 duplicate `lead_id` values live in `permits`** — the exact collision 138_a existed to kill. **1,303 `lifecycle_status_history` rows** sit under the colliding `permit:<num>:00` keys (the DCs stub's timeline merged into the real permit's).
* Only **34** rows repo-wide have NULL `lead_id`, all 2026-vintage: the trigger is `BEFORE INSERT OR UPDATE **OF permit_num, revision_num**`, so it fires on fresh loader inserts and **never re-fires on the bulk-restored population** — and `load-permits.js:423` + `classify-lifecycle-phase.js:911` write the ledger key with inline `padStart(2,'0')` and **no admin gate at all**, bypassing `deriveLeadId` entirely.
**⇒ "Adopt 138_a's convention" is REPAIRING A FALLEN FENCE, not adopting a working one. C0 must either fold the repair or file it CRITICAL — it must NOT claim the convention while it is violated on 100% of its target rows.**

### More spellings than the plan found — SEVEN or EIGHT, including the INVERSE
`src/lib/leads/lead-inspect-query.ts:791-798` uses **`ORDER BY revision_num DESC LIMIT 1`** — *highest* revision wins, the exact inverse of every candidate rule, live in the admin CoA→permit link. Plus `record-lead-view.ts:90` takes the **rightmost** 2 chars where `lead-id.js:75` takes the **leftmost** (inert today, max length 2).

### Corrections to §2 of this record (Integration, cloud-measured)
* **"`'0'` rows are inert" is DEV-ONLY.** On cloud: **9** carry `enriched_status`, **13** carry `last_scraped_at` (latest 2026-08-11T18:17Z), and **all 581 carry a `lifecycle_phase`** (P3 456 / O1 121 / P19 4). They are already in the phase engine. The *conclusion* (MIN picks the stub → bad) survives and strengthens; the "inert" premise does not.
* **"lost 0 / gained 0" holds only for the fully-qualified UPDATE.** On the base populations: Step 1 (`enriched_status='Active Inspection'`, 2,233 rows) **lost 7 / gained 8**; Step 2 (`'Stalled'`, 893 rows) **lost 1 / gained 0**. The 7 losses are real `'00'` construction rows on dual permits where the stub wins MIN.

### ⚔ THE LIVE DISPUTE — Q1 is NOT settled. Three rules on the table:
| Rule | Champion | Case |
|---|---|---|
| **base row** (lowest non-admin revision) | Schema-Fidelity, Integration | `permit_inspections` carries **`UNIQUE (permit_num, stage_name)`** — the schema already ruled inspection facts are permit-grain. "Row under inspection" is ambiguous for **448** permits vs base-row's **55**. |
| **`status='Inspection'` rows** | Adversarial critic | `enriched_status` is a **per-row refinement of that row's own `status`**; all three cross-checks at `assert-lifecycle-phase-distribution.js:663-717` **count ROWS**. A canonical row cannot satisfy a row-grained invariant — it only moves *which* row is wrong. Scores **residual 0** on every C0 edge case. |
| **delete C0 entirely** | Adversarial critic | Two-line change: `AND status='Inspection'`, delete `fetch_base_revision`. Smaller than today's staged C2. |

**MY CLOUD MEASUREMENT OF THE REAL WRITE POPULATION (27,979 permit_nums / 32,468 rows — the queue predicate verbatim from `aic-orchestrator.py:169-183`, joined to every row sharing an emitted `year_seq`):**
| Fact | Value |
|---|---|
| `status='Inspection'` rows that are **administrative** | **0** of 21,827 (21,826 construction + 1 unclassified) |
| permit_nums with **exactly one** `status='Inspection'` row | 21,809 |
| permit_nums with **two** | 9 |
| **permit_nums with ZERO `status='Inspection'` rows** | **6,161 (22%)** |
| `MIN(revision_num)` picks a non-`Inspection` row | **6,205** · picks an administrative row: 49 |
| the smear targets (non-Inspection rows) | `Permit Issued` 3,979 · **`Revision Issued` 3,783** · `Closed` 1,419 · `Pending Closed` 616 |

* **CONFIRMS the critic's structural claim:** administrative rows are **never** `status='Inspection'` — a status predicate excludes every fee stub *by construction*, making admin-exclusion redundant on this path.
* **PARTIALLY REFUTES its "zero permits have no Inspection row":** on this broader population **6,161 (22%) have none**. ⚠ **BUT my population is a SUPERSET of what the scraper writes** (it includes every `folderSection` sibling sharing a `year_seq`), so 6,161 is an **upper bound on a "writes nothing" class, not a measured write-miss.**
* **⚠ UNRESOLVED AND DECISIVE — the two `status` fields are not the same field.** The scraper selects target permits from **AIC's `folderStatusDesc`** (`"24 100000 BLD [Inspection]"`), NOT from `permits.status`. A predicate on OUR `status` writes **zero rows** whenever the two disagree — the identical silent-drop failure mode that made `= '00'` wrong. **NOBODY ON THE PANEL TESTED THIS. It must be measured before Q1 is ruled.**

**Operating Boundaries:** Target = `scripts/lib/` (new helper), Spec 44 §3, the `enriched_status` call sites, one lock. **Out of scope:** the C2 diff itself (rebuilt after), `permit_scrape_outcomes` (Spec 44 `:118` records permit-grain as a *knowing* decision — do NOT harmonize).
**Database Impact:** presumed NO (a resolver, not a column) — but the panel must rule on whether a persisted canonical marker is the better answer than recomputing a predicate in five places.
**Blocking follow-up:** `review_followups.md:2883` currently instructs mirroring C2 into `classify-permit-phase.js` — **that advice is now WRONG** and must be rewritten in the C0 commit before anyone acts on it.

### C2 — D2a: revision scoping  *(fixes the writer)*
> ✅ **REBUILT AND COMMITTED `eff28a7e` (2026-08-12) — as `AND status = 'Inspection'`, NOT as base-row scoping.** Everything below is the ORIGINAL text, retained for history only; its `MIN(revision_num)` predicate is WRONG and was never shipped. See the C0-RESOLVED section for the rule that shipped and why.
> **Output panel (3 lenses) run on the rebuild — 4 real defects, all folded before commit:** ① Spec 44 Edge Cases `:240` still carried "only rev 00 has them", contradicting the same file's new §3 (found independently by Code Reviewer AND Guardian) · ② **`classify-inspection-status.js` — step 2 of THIS chain — was left writing `enriched_status` with no status predicate**, which post-C3 would have re-created the very smear the commit removes; both writers now enforce the same rule and the "Bug 4" lock moved with it · ③ "administrative excluded **by construction**" was an overclaim — the predicate has no `permit_type` term; exclusion comes from `TARGET_TYPES` gating the *queue*, so it is now stated as **measured, not enforced** (this was the §5 defect class committed a THIRD time) · ④ Q3's "write nothing must be counted" ruling was documented but uninstrumented — added `no_inspection_row` counter + WARN log + INFO audit row.
> **DeepSeek's 10 findings were all PRE-EXISTING** (it reviewed the whole file, not the diff) → filed in `review_followups.md`, incl. two HIGH: the egress tripwire can false-PASS under an ambient `HTTP_PROXY`, and early-abort marks unscraped queue rows `completed` (silent data loss).
> **Still open (LOW, filed):** the R4 lock is a two-literal denylist rather than an exact match; `classify-permit-phase.js:33`'s comment fix lost its tracking home when the follow-up entry was reverted.
Seven sites, two languages: `aic-scraper-nodriver.py:2629, :2637, :2650, :2699, :2708` + `classify-inspection-status.js:57, :83` (pre-existing latent bug — those 397 permits can never be Stalled/reactivated).
**Predicate: `MIN(revision_num)` per `permit_num`** — **NOT `= '00'`** (would write zero rows for 397 permits, silently dropping their scrape result and re-queueing them forever). **DRY: one shared fragment/constant**, not 7 hand-written spellings; prefer a pre-fetched `SELECT MIN(revision_num)` bound as a parameter over a correlated subquery repeated 5×.
**`enriched_status` → base row only. `last_scraped_at` → UNCHANGED, every row** (`aic-orchestrator.py:181` evaluates the 7-day cooldown per row before `DISTINCT year_seq`; scoping it strands 7 measured `year_seq`s churning forever).

### C3 — Backfill  *(clears the existing smear)*  — ⚠ **REWRITTEN 2026-08-12 for the shipped rule. NOT YET IMPLEMENTED.**

> **The original C3 text below the line is DEAD** — it was written against the base-row model the panel killed. Every load-bearing element referenced a retired concept: the filename `backfill-non-base-enriched-status.js` ("non-base"), the guard `EXISTS a base row`, and the scope figures (~1,257 / ~577). **Do not implement it.**

**The rule is now one predicate, and C3 gets SIMPLER:**
```sql
UPDATE permits SET enriched_status = NULL
 WHERE enriched_status IS NOT NULL AND status IS DISTINCT FROM 'Inspection'
```
> **`IS DISTINCT FROM`, NOT `<>` — Reality-Check ruling 2026-08-12.** Identical result today (**both return exactly 1,350**, measured), but `<>` is **FALSE for NULL status** in SQL three-valued logic. `permits.status` is nullable (`is_nullable='YES'`, no CHECK) and **2 rows carry `status IS NULL` right now** (0 of them with `enriched_status`). A future NULL-status row with a stale `enriched_status` would be **structurally invisible**: never nulled, never surfaced as a miss (replay-to-`UPDATE 0` cannot see what the predicate excludes), and the cross-checks read `enriched_status` with no `status` gate — so it would fail `cross_check_*` forever with no mechanism to clear it. Cost of the fix is zero.
> **Whitespace/case: CLEARED.** Full enumeration of all 18 distinct `status` values on enriched rows — `'Inspection'` is byte-clean (`len=10`, no padding) and there is **no** `'Inspection '` / `'inspection'` near-miss. The live trailing-space value `'Under Review '` (30 rows) is correctly caught as ≠ Inspection. **My destructive-predicate worry was unfounded — recorded so it is not re-raised.**
### ⚖ C3 PLAN PANEL — Integration (2026-08-12): 7 verdicts, **10 required corrections**. Folded below.

**⛔ CORRECTION 1 — "replay to `UPDATE 0`" is REFUTED ON CLOUD, and it reframes C3.**
No *writer* can re-create the class — all three `enriched_status` producers are now status-scoped (`aic-scraper-nodriver.py:2684`/`:2763`, `classify-inspection-status.js:85`/`:111`, and `classify-permit-phase.js:51` which already keyed on `status='Inspection'` pre-existing). **But the class regenerates with NO `enriched_status` write at all:** `load-permits.js`'s `ON CONFLICT DO UPDATE SET` includes **`status = EXCLUDED.status`** (`:338-359`). A row that legitimately received `enriched_status` while `status='Inspection'` becomes a C3 target the moment CKAN moves it to `Revision Issued`/`Permit Issued`/`Pending Closed`/`Closed`. **Nothing clears the stale value.**
**The proof was already in this plan and went unconnected** — the C0 block's "THE 97 … `Pending Closed` 109 (95 carrying `enriched_status`) … they closed AFTER being scraped" IS this mechanism, measured.
⇒ **`UPDATE 0` on replay is valid ONLY as an isolated-test assertion** (same DB, no intervening `load_permits`). **State that in the test file** or it becomes the next false premise.
⇒ **C3 IS NOT TERMINAL. The population regenerates nightly.** This repeats the exact defect C6 fixed ONE COMMIT EARLIER (`5ef51de7`: *an apply-time invariant is not enforcement; it needs a standing audit row*). **REQUIRED: add a standing audit row counting `enriched_status IS NOT NULL AND status IS DISTINCT FROM 'Inspection'`** — or record an explicit operator decision to accept a residual WITH an expected steady-state rate. It cannot be left implicit.

**⛔ CORRECTION 2 — "those 1,350 ARE the smear" is RETRACTED. Count-as-inference, FOURTH occurrence this session.**
`Revision Issued`+`Active Inspection` (654) is **fully explainable by ordinary lifecycle transition on a legitimately-written row** — no smear involved. C3's UPDATE is correct for BOTH mechanisms so the *fix* survives; the *characterization* does not, and it is what produced the false replay-to-zero claim. **The two are separable at run time: a smear row has a sibling `status='Inspection'` row for the same `permit_num`; a transition row does not. Measure and report BOTH counts.**

**⛔ CORRECTION 3 — NO ROLLBACK PATH. Both prior destructive NULL-outs in this repo back up first.**
`wf2-reset-coa-trade-classification.js:43-58` (`_backup_coa_trade_classified_20260707`, added on a Gemini P9 ruling: *"a destructive NULL-out with no rollback path is not acceptable"*, restore UPDATE printed to stdout) and `wf2-p13-null-legacy-cost-tail.js:64-70`+`:19-23` (`_backup_p13_legacy_cost_tail`, restore UPDATE in the header). **REQUIRED: backup table + printed restore UPDATE before the NULL.** Values are otherwise unrecoverable — the scraper only rewrites `status='Inspection'` rows.

**CORRECTION 4 — the precedent was WRONG.** `backfill-permits-location.js` has **no lock, no `require.main` guard, no export**, and its flag is `--dry-run` defaulting to **WRITE** — the inverse of C3. It is the filename/dir precedent ONLY.
⇒ **Model the body on `scripts/one-time/wf2-p13-null-legacy-cost-tail.js`** (105 lines; one-off, lock at `:36`, destructive NULL-out, count-first `:51-55`, **early idempotent return when count is 0** `:56-61`, `withTransaction`, backup `:64-70`, `emitSummary`+`audit_table` `:81-97`, `emitMeta` `:98-101`, `if (!lockResult.acquired) return;` `:104`).
⇒ **Model the guard/export on `scripts/backfill/migrate-entities.js:101-105`** (`module.exports = { run }` + `if (require.main === module)`, injectable pool `:23-34`, exercised that way by `src/tests/migrate-entities.infra.test.ts:9`). **Its signature is `run(opts)` with `opts.pool` — NOT `runBackfill(pool, {confirm})`. Deviating is fine; note it.**

**CORRECTION 5 — G4 RECURRING: `pipeline.run(...)` MUST sit INSIDE the `require.main` block.** No script in the repo combines `pipeline.run` with a `require.main` guard — `wf2-p13:38` and `backfill-permits-location:69` call it unguarded at top level; `migrate-entities.js` has the guard but no `pipeline.run`. C3 would be the first. Otherwise a test that `require()`s the module to reach the export **opens a real pool against whatever `SUPABASE_DATABASE_URL`/`PG_HOST` is in `.env`.**

**CORRECTION 6 — the Spec 47 §R1-R12 citation is a DEFECT (this plan's own standard: "a wrong citation is a defect").** `47:5-6` scopes itself: *"Applies to: Every new CommonJS script … that runs as a step in any pipeline chain."* **C3 is not a chain step, so §R1-R12 does not bind it.** Keep the skeleton — it is right — but say **"adopts the §R1-R12 skeleton BY CHOICE, precedent `wf2-p13`"**. §R4 (Zod) and §R5 (startup guards) are **N/A** (C3 reads no logic vars); §2 (`:41`) requires a **one-line comment** for each omitted IF-APPLICABLE item. `pipeline.run` is safe here — `lib/pipeline.js:463-488` only creates/ends a pool and writes **no** `pipeline_runs` row, so a dry-run leaves no observability residue.

**CORRECTION 7 — the registry claim is overstated.** `ADVISORY_LOCK_ID = 44` **CONFIRMED still free** (exhaustive scan of 72 live IDs; absent from §A.5 and `LOCK_ID_REGISTRY`; note 45/46 are adjacent). But **omitting the registry entries does NOT red `npm run test`** — `:291` filters on manifest scripts only, and `scripts/backfill/*` is not in the manifest. Live counter-example: `scripts/one-time/wf2-p13-null-legacy-cost-tail.js:36` uses 122 and is in neither, suite green (filed at `review_followups.md:2542`). **Only UNIQUENESS is test-enforced** for `scripts/backfill` (`:185` `EXTRA_LOCK_DIRS` — confirmed exact — consumed `:188-193`, merged `:198`, checked `:259-276`). Adding to **one of the two without the other DOES red** (`:303`, bidirectional `:308-320`). So: register both **as convention**, and say the test does not enforce it — because if an implementer believes it does, the next omission goes unnoticed.

**CORRECTION 8 — `--confirm` has NO single house pattern; C3's combination exists nowhere.** Pattern A `--confirm` default-safe (`scripts/analysis/backfill-admin-watchlist.js:29`, `:108-112` — count → print → return, zero writes; raw `new Pool()`, no lock, no `emitSummary`); Pattern B `--dry-run` default-WRITE (`backfill-permits-location.js:31`, `migrate.js:88`, others); Pattern C hard-refuse (`wipe-supabase-auth-state.js:75`). **Match Pattern A's shape exactly, and DOCUMENT the deliberate deviation from the dir's default-write `--dry-run`** — or the next reader "harmonizes" it back to default-write. *(C2's own test file at `test_scraper_enriched_status_scoping.py:39` names C3's `--confirm` dry-run count as the empirical proof closing its FakeCursor ceiling — so default-safe is load-bearing.)*

**CORRECTION 9 — §11 counter scoping.** `records_total` = permits **evaluated** (the pre-count); `records_updated` = permits **changed** (`47:189-191`). Copy `wf2-p13:81-97`, which gets this right and adds the `audit_table`.

**CORRECTION 10 — runbook is TWO edits, not one.** Add a row to the **two-column** (`| Script | Job |`) table at `docs/runbook/README.md:62-68` **and bump its heading count `(3)` → `(4)`**. Copy the `--confirm` doc convention from `:76`. The `C2 → C3 → verify` deploy rule goes in **§3 as item 6** (`:83-105`; closest existing shape is rule 5 "Reset-then-drain").

**Verified sound, no change:** lock 44 free · `EXTRA_LOCK_DIRS` covers `scripts/backfill` · one-off, correctly NOT chain-registered (`manifest.json` has exactly one backfill, a root-level script; zero from either subdir) · `enriched_status`-only, never `last_scraped_at` · `C2 → C3 → verify` with C2 **not** on `origin/main` (`origin/main` tip is `cdaea415` = C1) · the C2/C3 rule-agreement problem genuinely IS dissolved.
**Also confirmed:** the F1 administrative rows are `revision_num='0'` `DCs DeferredFees` stubs, and `classify-permit-phase.js:51-53` **cannot** be their producer (it requires `revision_num='00'`) — so their values came from the pre-C2 smear, and they are caught iff their own `status <> 'Inspection'`, exactly as the plan says to verify.

---
* **File:** `scripts/backfill/backfill-smeared-enriched-status.js` (convention `backfill-<thing>.js`; `:185` `EXTRA_LOCK_DIRS` already lock-scans `scripts/backfill`). **Skeleton precedent: `wf2-p13-null-legacy-cost-tail.js`; guard/export precedent: `migrate-entities.js:101-105`** (see CORRECTION 4).
* **Adopts the Spec 47 skeleton BY CHOICE** (CORRECTION 6), `ADVISORY_LOCK_ID = 44` — plus the §A.5 registry row AND the `LOCK_ID_REGISTRY` entry, **both or neither** (CORRECTION 7).
* `--confirm`-gated, **default-safe** (CORRECTION 8) · `require.main` guard **wrapping `pipeline.run`** (CORRECTION 5) · **backup table + printed restore UPDATE** (CORRECTION 3) · §11-correct counters (CORRECTION 9) · **isolated-DB** replay-to-`UPDATE 0` only (CORRECTION 1).
* **`enriched_status` → NULL ONLY. Do NOT touch `last_scraped_at`.** Still correct — but **⛔ MY STATED REASON WAS WRONG, corrected by Reality-Check 2026-08-12.** I wrote "the cooldown becomes the SOLE brake on 4,347 `Revision Issued` + 768 `Pending Closed` rows". **Both figures are stale** (carried verbatim from the Integration finding under the DEAD base-row model and never recomputed — actual smear totals are **1,107 `Revision Issued` / 102 `Pending Closed`**), **and the mechanism is BACKWARDS.** Both queue predicates — `aic-orchestrator.py:176-181` **and its duplicate at `aic-scraper-nodriver.py:3733-3741`** — carry **`p.status = 'Inspection'` as a hard AND-ed term**. Every row C3 nulls has `status <> 'Inspection'` **by construction of C3's own WHERE clause**. ⇒ **ZERO rows become queue-eligible after C3. `status` is the gate, not the cooldown.** Even on a later status flip to `'Inspection'`, 1,343 of the 1,350 carry `'Active Inspection'`/`'Permit Issued'`, which the orchestrator's `enriched_status IN (...)` term **already admits** — so nulling is a no-op there; it changes eligibility for only the **7** `'Stalled'`/`'Examination'` rows. The `last_scraped_at` rule stays for its own sake, not this one.
* **Expected scope, re-measured 2026-08-12: 1,350 rows — reproduces the cited breakdown EXACTLY** across 37 `status`×`enriched_status` pairs (`Revision Issued`+`Active Inspection` 654 · `Revision Issued`+`Permit Issued` 453 · `Pending Closed` 102 [plan rounded to 98] · `Examiner's Notice Sent` 43 · tail to n=1). **Still compute at run time — never hard-code.**
* **⚠ PANEL CONFLICT, ADJUDICATED — the population GROWS; the standing audit row is REQUIRED.** Reality-Check called the population "static/shrinking post-C2 since no new smear can be *written*". **Integration is right and that reasoning is incomplete:** a row written legitimately while `status='Inspection'` **JOINS** the population the moment `load-permits.js`'s `ON CONFLICT DO UPDATE SET status = EXCLUDED.status` (`:338-359`) moves it to `Revision Issued`/`Closed` — no `enriched_status` write involved. **Direct evidence already in this plan: the 97 `Pending Closed` rows that "closed AFTER being scraped."** Permits move forward far more often than back, so the population accretes. **CORRECTION 1's standing audit row stands.**
* **⚠ ADMINISTRATIVE ROWS: 10, NOT 9 — my arithmetic slip (8+1+1=10), caught by Reality-Check.** **9 of 10 ARE cleared** (8 `Active Inspection` + 1 `Permit Issued`, all `DCs DeferredFees` rev `'0'`, `status` `Open`/`Closed`). **1 of 10 is NOT: `04 166058 STE`** (`Site Inspection(Scarborough)` rev `'00'`, `enriched_status='Examination'`, **`status='Inspection'`**) — the predicate is FALSE for it, so its out-of-spec value **survives C3 untouched**, exactly as the "verify, don't assume" instruction anticipated. State this per-row in the commit.
* **⚠ SUCCESS CRITERIA — "clears the existing smear" is OVERSTATED for one of three cross-checks.** Measured overlap against `assert-lifecycle-phase-distribution.js:663-717`: `cross_check_stalled` **1/1 = 100%** · `cross_check_active_inspection` **733/733 = 100%** · **`cross_check_permit_issued` 58/66 = 88% — 8 offenders REMAIN.** Those 8 (e.g. `25 156034 BLD`, `26 140845 BLD`) all have `status='Inspection'` (correctly out of C3's scope) with `enriched_status='Permit Issued'` but `lifecycle_phase` already advanced to P9–P14 — **a separate, pre-existing bug C3 does not address.** Do not attribute it to C3 failing; **file it.**
* **`lifecycle_phase` is transiently stale, and SELF-HEALS — state it so nobody mistakes it for a strand.** C3 touches only `enriched_status`, not `last_seen_at`, so `classify-lifecycle-phase.js`'s reclassify gate (`:1005-1006`) does not fire immediately; 1,120 of the 1,350 sit at P8 derived from the now-retracted value. But `load-permits.js:386-399` bumps `last_seen_at` for **every** permit on every nightly load, so the next `chain_permits` reclassifies **before** `assert_lifecycle_phase_distribution` runs later in the same chain. The three cross-checks gate on `LOWER(enriched_status)`, which goes false the instant it is NULL — independent of `lifecycle_phase` freshness. `permit_phase_transitions` is append-only; nothing orphans.

#### 🛡 REGRESSION GUARDIAN (2026-08-12) — **YES, part of the 1,350 WAS deliberately created.** 4 required folds.
**The Chesterton's Fence question is answered.** `git log -S enriched_status --all` → 3 producing commits. The population splits three ways:
* **(a) The bulk — `Revision Issued` 654+453: ACCIDENTAL, no fence, C3 SAFE.** Origin `91ed25d3` (2026-03-28) wrote `WHERE permit_num = $2` with no status/revision term. It *did* delete a status filter — but the deleted filter was `folder.statusDesc !== 'Inspection'`, **AIC's folder status**, while the DB queue in the SAME commit kept `WHERE p.status = 'Inspection'`. **Writer and reader disagreed from day one.** Nothing intended it.
* **(b) `Pending Closed`/`Closed`: DELIBERATELY PRESERVED — a DOCUMENTED fence.** `4c5009ca` (2026-07-30, `Severity: HIGH` + `Lesson-routing:` footer) added the **FEED-STATUS GUARD** (`classify-inspection-status.js:66-75`) hand-enumerating eight closed-ish statuses: *"A permit the feed calls closed is not stalled, whatever its stage dates say."* Combined with operator ruling `e4a7b6e6` — *"lifecycle completion truth is the feed's own status, never inferred from stages"* — which made `'Active Inspection'` **the resting value of a FINISHED permit**. So a `Pending Closed` row carrying `'Active Inspection'` is the **designed end state, not smear.**
  **FENCE STATEMENT for the C3 commit:** it existed to stop finished permits drifting to a sticky `'Stalled'`. **C3 still covers that** — NULL fails `computeStalled` signal 1 (`lifecycle-phase.ts:646`) more strongly than `'Active Inspection'` did — and under `e4a7b6e6`'s own ruling the preserved value carries **no completion truth**; `permits.status` does. **Knowingly retired, safely. Say so explicitly.**
* **(c) `enriched_status='Stalled'` on non-Inspection rows: DELIBERATELY WRITTEN.** Pre-C2 step 1 wrote `'Stalled'` under the 8-word denylist, which **deliberately left `'Revision Issued'`, `'Permit Issued'`, `"Examiner's Notice Sent"` eligible**. `eff28a7e` narrowed the scope but said nothing about the denylist, now dead code at `:85-88`.

**⇒ FOLD 1:** C3 must report the **`enriched_status` VALUE breakdown** at run time (not just the `status` breakdown the plan carries) and state the (b)/(c) fences. Any `'Stalled'` count is deliberately-written data being deleted.

#### ⛔⛔ COMPLIANCE F18 — **BLOCKING, and the single sharpest catch of the C3 panel: re-running C3 DESTROYS ITS OWN ROLLBACK.**
**Two corrections I folded separately and never cross-read, colliding:**
* **CORRECTION 1** establishes the population **regenerates nightly** ⇒ **C3 WILL be re-run.**
* **CORRECTION 3** mandates the backup **modelled on `wf2-p13:64`** — which is `DROP TABLE IF EXISTS _backup_… ; CREATE TABLE _backup_… AS SELECT`.
⇒ **Run 2 silently destroys run 1's rollback data.** The `wf2-p13` precedent is safe only because it is a genuine ONE-OFF; C3 is not.
**FIX: use the DATED-table precedent — `scripts/analysis/wf2-reset-coa-trade-classification.js:46` (`_backup_coa_trade_classified_20260707`).** C3's backup becomes **`_backup_smeared_enriched_status_<YYYYMMDD>`**, so **each run keeps its own rollback**. State in the header that C3 is **re-runnable by design** (CORRECTION 1) and that the dated name is *why*.
**Nobody else caught this** — not Integration, not Reality-Check, not the Guardian. It is exactly what the **DeepSeek idempotency lens** (Compliance F12, `08:198`) exists for, and that lens was **missing from the roster**. It is now running.

#### ⛔⛔ DeepSeek IDEMPOTENCY LENS (2026-08-12, the roster gap Compliance F12 named) — **BLOCKING: the backup is NOT ATOMIC with the mutation.**
Run against `wf2-p13-null-legacy-cost-tail.js`, the skeleton C3 is mandated to copy. **It inherits a genuine unrecoverable-data-loss path:**
* The **pre-count is taken OUTSIDE the transaction** (`:51-53`); the **backup `CREATE TABLE AS` and the `UPDATE` are inside a default READ COMMITTED transaction** (`:63-78`). **Under READ COMMITTED every statement takes a FRESH snapshot.** A row that enters the breaching state after the backup's snapshot but before the UPDATE's snapshot is **nulled WITHOUT being backed up** — and the backup is the only rollback. The pre-count can also disagree with `rowCount`, silently corrupting `records_total`.
* **The advisory lock does NOT save this.** Lock 44 serializes C3 against *itself*, not against the other writers.
* **C3's exposure is WORSE than the precedent's**, because concurrent writers demonstrably exist and are scheduled: `classify_permit_phase` (chain_permits ~11:00Z) and the scraper + `classify_inspection_status` (chain_deep_scrapes ~15:00Z) all write `enriched_status`, and **`load-permits.js` changes `status` itself** — which is precisely how a row ENTERS C3's predicate. A backfill run overlapping any of those can null rows it never backed up.
**REQUIRED for C3:** ① take the count **inside** the same transaction; ② run at **`REPEATABLE READ` or `SERIALIZABLE`** (or `SELECT … FOR UPDATE` the targets before the backup); ③ **assert `backupRowCount === updateRowCount` and ABORT on mismatch** — the cheap belt-and-braces that makes the failure loud instead of silent.

**Independently CONFIRMS Compliance F18** (fixed backup name ⇒ re-run destroys the prior rollback; use a dated name + emit the restore SQL so the operator knows which backup maps to which run) — **two independent sources, no longer a single-reviewer call.**
**Also confirms CORRECTION 5** (`:38` `pipeline.run` at top level with no `require.main` guard ⇒ an accidental `require()` from a test or REPL runs the destructive null-out against whatever `.env` names).
**New for C3's `emitMeta`:** the precedent **underreports its reads** — the backup `SELECT` reads columns absent from the declared list. C3 must declare **every** column its backup reads, not just the ones the UPDATE touches, or the data-lineage map is wrong.
*(Its `cost_source` spelling and `Number()` precision findings are precedent-specific and do not transfer — C3's predicate reads `status`/`enriched_status` with no numeric ceiling.)*

#### ⚠ COMPLIANCE — the other BLOCKING items, folded
* **F2 — predicate consistency.** The canonical block at `:388` is `IS DISTINCT FROM` ✅, but **prose at `:227`, `:423`, `:429`, `:604` still says `<>`.** `47:455` §6.4 mandates `IS DISTINCT FROM` on write-guarded UPDATEs. **The backfill AND the audit row must both use `IS DISTINCT FROM`** — otherwise a `status IS NULL` row is *skipped by the backfill and counted by the audit row*: **a permanent, unclearable red on day one**, which is precisely FOLD 4's failure mode. Test 1(c) locks the choice.
* **F8 — three live "blocking" instructions point at text that no longer exists.** `review_followups.md:2883` is cited at `:129`, `:229`, `:370` as *"NOW GIVES WRONG ADVICE, must be rewritten."* **Verified: `grep classify-permit-phase docs/reports/review_followups.md` → 0 hits.** The entry was removed with C2's revert (the plan's own `:376` says so). **The instruction is discharged — the entry is gone, which is what "delete, don't rewrite" wanted.** Strike all three references.
* **F15 — `:423` is FALSE and hides a live defect.** It claims `classify-permit-phase.js:51-53` *"cannot be their producer"*. **Verified false:** `classify-permit-phase.js:49` is the **sole** `enriched_status = 'Examination'` writer in the repo, gated by `:51` `status='Inspection'` + `:52` `revision_num='00'` — which `04 166058 STE` (rev `'00'`, `status='Inspection'`) **satisfies exactly**. ⇒ **A LIVE PRODUCER IS STILL WRITING DERIVED STATE ONTO AN ADMINISTRATIVE ROW**, violating this plan's own BINDING CONSTRAINT and the `138_a` convention. C3 correctly spares the row; **nothing closes the producer.** Re-file it with the CORRECT cause, not "pre-C2 smear".
* **F11 — TEST DISCIPLINE violation in my own test plan.** Item 3 mandates an *"exact-match assertion on the UPDATE's SET clause"* — a **source regex where the behaviour is directly testable**: seed a row with a known `last_scraped_at`, run, assert unchanged. The discipline permits regex only where *forced*, and requires stating the ceiling. **Replace with the behavioural assertion.**
* **F9 — the §11 WF3 sibling-bug scan was never done** (`ES:287`; zero occurrences of "sibling" in this file). Required: 3–5 siblings of *"derived state persists after its basis changes"*. Three are already surfaced incidentally — the 8 residual `cross_check_permit_issued` offenders, the missing loader-invalidation rule, and the 119 all-administrative `status='Inspection'` permits.
* **F10 — `Database Impact: NO` vs FOLD 4's threshold.** If the audit row carries a steady-state rate, it needs a `_contracts.json` key + a `contracts.infra.test.ts` row (`ES:290-292`). Prefer a **zero-threshold WARN** and say so — that keeps Database Impact NO honest.
* **F13 — FOLD 4 is UNRESOLVED and blocks implementation.** CORRECTION 1 says "add a standing audit row"; FOLD 4 says C6's FAIL-on-nonzero makes it a permanent red. **Ruling needed: WARN with a stated steady-state, or the Spec 48 §4.9 self-announcing pair C5 already specifies (reuse it).**
* **Wrong citations to fix:** `47:189-191` → **`47:1971` §11** (rows `:1980`/`:1982`) · `§2 (:41)` → **`47:33`** · `migrations/059:17` → **`:16`** (both sites) · `review_followups.md:2542` does NOT file the lock-122 claim (different scripts) — **the claim is true but UNFILED** · `wf2-reset-coa-trade-classification.js` lives in **`scripts/analysis/`**, not `scripts/one-time/`.
* **Correction to the Guardian:** §A.5 widening is **not new** — `scripts/one-time/` scripts are already registered at `:91-95`. C3 is the first **`scripts/backfill/`** entry. Registration also makes constant-vs-source agreement **machine-enforced** (`:278-289`), so CORRECTION 7's "only uniqueness is enforced" holds **only while unregistered**.
* **Reality-Check's trigger IS defective, but RC firing here was CORRECT.** `CLAUDE.md:134` scopes RC to *"parcel field"*, but **`08:200` overrides — RC fires by SUBJECT MATTER ("when values/derived-fields are in play"), not by table** — and `CLAUDE.md:114` says **Spec 08 governs on conflict**. ⇒ The `CLAUDE.md:134` trigger text is the defect; fix it to *"any enriched/derived field"*. RC earned its keep here (it caught the stale 4,347/768 and the backwards queue mechanism).

#### 🔑 THE SYSTEMIC LESSON — stronger than count-as-inference
**Seven reviewers have now passed over C3, and the two most consequential findings (F2, F18) are each a COLLISION BETWEEN TWO CORRECTIONS THE PLAN MADE SEPARATELY AND NEVER CROSS-READ.** The gap is not review depth. **It is that no pass re-reads the folded corrections against each other.** Route this alongside count-as-inference — it is arguably the stronger lesson, and it explains the §5.1 pattern (*"the plan contains both the antibody and the infection"*) recurring on every single pass.

**⇒ ⛔ FOLD 2 — CRITICAL, nobody else caught it: C3 must bump `last_seen_at`.**
`classify-lifecycle-phase.js:1005-1007` selects dirty rows on `lifecycle_classified_at IS NULL OR last_seen_at > lifecycle_classified_at OR matched_rule IS NULL`. **`enriched_status` is NOT a dirty key.** Both existing `enriched_status` writers bump `last_seen_at` in the SAME UPDATE precisely so the reclassifier re-derives (`classify-inspection-status.js:82-83`, `:108-109`; `classify-permit-phase.js:49-50`). **C3 as planned breaks that convention.**
Rows still in the CKAN feed are rescued *by accident* (`load-permits.js:391-400` touches `last_seen_at` for every permit). **Rows that have LEFT the feed are not — their `lifecycle_stalled = true` persists forever with its basis deleted.** Downstream and user-visible: `compute-trade-forecasts.js:611,:661,:1144,:1189` all filter `AND p.lifecycle_stalled = false`, so a permit stays excluded from trade forecasts on evidence that no longer exists.
**Worse, it is UNFALSIFIABLE:** all three cross-checks (`assert-lifecycle-phase-distribution.js:663-711`) are one-directional (`enriched_status = X AND lifecycle_* inconsistent`). **NULLing the left-hand side makes them pass by DELETING THE EVIDENCE, not by fixing the derivation — the metric this whole WF3 exists to drive to zero goes green partly for the wrong reason.**
⇒ Bump `last_seen_at` on touched rows (matching `classify-inspection-status.js:82`), **or** explicitly decline, count the affected rows, and document the accidental rescue path. **NOTE: this is `last_seen_at`, a DIFFERENT column from `last_scraped_at` — the don't-touch ruling is untouched by this.**

**⇒ FOLD 3 — CONFIRMS Reality-Check independently:** the `:427` queue-churn rationale is false; `aic-orchestrator.py:175`'s `p.status='Inspection'` excludes 100% of C3's targets. **Already folded above.**

**⇒ ⛔ FOLD 4 — the standing audit row must be WARN or the §4.9 pair, NOT C6's FAIL-on-nonzero.** C6's rows are FAIL-on-nonzero (`assert-global-coverage.js:1337-1346`). But CORRECTION 1 establishes this population **regenerates nightly**. A FAIL-on-nonzero row on a permanently-nonzero population = **a permanently red audit verdict every night.** It does not halt (`assert-global-coverage.js:13` is non-halting) but it reddens `check-chain-verdict.js` — **a standing red everyone learns to ignore, which is the exact noise class C1 shipped to eliminate.** Model it as **WARN with an expected steady-state rate**, or as the **Spec 48 §4.9 self-announcing pair C5 already specifies**.

**Other Guardian findings:**
* **No prior attempt exists** — `git log --all -S "enriched_status = NULL"` → **zero commits**. Nothing was tried and abandoned; no hidden reason to recover. (PASS)
* **Consumers: 3 PASS.** `enriched_status` has exactly ONE logic consumer (`computeStalled` signal 1) and **does NOT feed `lifecycle_phase`** (`lifecycle-phase.ts:7-8` says so explicitly). `permit_phase_transitions` cannot orphan (phase is status-derived, C3 can't change a phase). **No notification storm** — `LIFECYCLE_STALLED` dispatches only on `false→true` (`classify-lifecycle-phase.js:962-966`); C3 can only produce `true→false`. `lead-inspect-query.ts` is display-only, no WHERE.
* **Backup precedent origin is `6af3d537`** (2026-07-07) — and read the history: **that script SHIPPED WITHOUT a backup and a reviewer bolted it on afterwards** (`// [Gemini P9-pass]: a destructive NULL-out with no rollback path is not acceptable`). **The same failure mode C3 is one review pass from repeating.** Second instance `e99ae61a` ("4,474 rows nulled, backed up"). **Neither generated a `tasks/lessons.md` entry** — the rule exists in two script bodies and one commit message and NOWHERE ELSE. Cite `6af3d537` by SHA; this is a strong candidate for the first real lessons entry on it.
* **Spec 47 §A.5 is FOUR columns** (`Lock ID | Script | Wave | Writes Timestamps?`), not three, and its header scopes to *"All 40 JS pipeline scripts"* — C3 is a one-off, so adding it is a **deliberate widening**, and the `Wave` cell has **no natural value**. Say so rather than inventing one. *(This is why `wf2-p13`'s lock 122 legitimately sits outside it.)*
* **Runbook rule 5 INVERTS — do not model rule 6 on it.** Rule 5 nulls a `*_classified_at` column, whose whole point is that it **re-fires** the dirty predicate. C3 nulls a column that is **not a dirty key and re-fires nothing** — the exact inverse. New rule 6 must state that difference or it reads as another rule-5 case.

**⇒ ⛔ THE DEEPEST FINDING — C3 deletes a SYMPTOM; the missing rule is the DEFECT.**
`load-permits.js`'s `status = EXCLUDED.status` traces to `d398d5e7`, the original upsert, **never revisited**. The decisive recorded intent is on the other side — `classify-permit-phase.js:10-12`: *"This step writes to `enriched_status` (not raw status) **so the permits loader upsert won't conflict** — raw CKAN status is preserved."* `enriched_status` is **deliberately excluded** from the `ON CONFLICT DO UPDATE SET` list so re-ingest cannot clobber derived state.
**Someone answered exactly HALF the question** — *"the loader must never touch `enriched_status`"* — and **nobody ever wrote the other half: what happens to `enriched_status` when `status` moves out from under it.** There is **no recorded intent, in any commit, spec, or comment, for that case.** The persistence C3 cleans up is the deliberate exclusion working as designed **with no matching invalidation rule**.
⇒ **File the missing loader status-change invalidation rule in `review_followups.md` IN THE C3 COMMIT** (a `WHEN status IS DISTINCT FROM` clause on the loader, or a nightly sweep). Out of C3's scope — but without it **C3 is a one-off that gets re-run by hand forever.**

#### 📋 SPEC + DOC UPDATES REQUIRED IN THE C3 COMMIT (operator-directed 2026-08-12)
| Target | Change |
|---|---|
| **Spec 44 §3** | Add the **backfill + regeneration rule**: the smear population is NOT terminal — `load-permits.js`'s `status = EXCLUDED.status` re-creates it whenever CKAN moves a permit past `Inspection`. Document the standing audit row as the enforcement, and C3 as the one-time clear. Add the script to §6 Target Files. |
| **Spec 47 §A.5** (`:1792+`, rows `:1807-1878`) | Registry row for `ADVISORY_LOCK_ID = 44` → `scripts/backfill/backfill-smeared-enriched-status.js`. **Convention, not test-enforced** for `scripts/backfill` (CORRECTION 7) — but the `:303` agreement test reds if you add ONE of the two. |
| `src/tests/pipeline-advisory-lock.infra.test.ts` `:24-153` | Matching `LOCK_ID_REGISTRY` entry — **same commit as the §A.5 row**. |
| `docs/runbook/README.md` | **TWO edits** (CORRECTION 10): row in the two-column table at `:62-68` **+ bump the heading count `(3)` → `(4)`**; and the `C2 → C3 → verify` deploy-ordering rule as **§3 item 6** (`:83-105`). |
| `tasks/lessons.md` | Spec 05 §4/§5 routing — this WF is `Severity: CRITICAL`. Candidate lessons this session generated: **count-as-inference** (4 occurrences), **an apply-time invariant is not enforcement** (C6), **measurement stated as guarantee**. Compliance panel is ruling on placement. |
| **File, do not fix** | the 8 residual `cross_check_permit_issued` offenders · the `'Examination'` value absent from `migrations/059:17`'s documented set · `04 166058 STE` surviving C3. |

#### 🔴 RED-FIRST TEST PLAN (operator-directed — TEST DISCIPLINE is non-negotiable)
**Red-first here means proving the DATA FACT before the script exists**, then proving each behaviour fails for the right reason.
1. **RED — the smear exists and the predicate finds it.** Against a testcontainer seeded with the four shapes: (a) `status='Revision Issued'` + `enriched_status='Active Inspection'` → **must be selected**; (b) `status='Inspection'` + `enriched_status='Permit Issued'` → **must NOT be selected** (this is `04 166058 STE`'s shape — the one C3 deliberately spares); (c) **`status IS NULL`** + `enriched_status` set → **must be selected** — *this case is RED under `<>` and GREEN under `IS DISTINCT FROM`, and is the ONLY test that distinguishes them*; (d) `enriched_status IS NULL` → untouched.
2. **RED — confirm-gating.** Without `--confirm`: **zero writes**, non-zero reported count. Assert row values are byte-identical after a dry run (a dry-run that writes is the failure this pins).
3. **RED — `last_scraped_at` is NEVER touched.** **Exact-match assertion on the UPDATE's SET clause**, not a two-literal denylist (the existing R4 lock is already filed as weak, `:376`). Nothing else pins this.
4. **RED — the backup table exists and the restore round-trips.** Seed → run → restore from backup → assert the original values return. CORRECTION 3 requires the backup; this is what proves it works rather than merely existing.
5. **GREEN — replay.** Second run ⇒ `UPDATE 0`. **State in the file that this holds ONLY in an isolated DB with no intervening `load_permits`** (CORRECTION 1) — otherwise it is a false claim.
6. **RED — the standing audit row.** Seed a violation ⇒ the metric is non-zero and the row is `FAIL`; clear it ⇒ `PASS`. This is what makes C3 non-terminal-safe.
7. **`SPEC LINK` headers on every new test file.** Tests go to panel review **before** the script lands.
* **The C2/C3 rule-agreement problem is DISSOLVED.** The old plan flagged that C3's `EXISTS a base row` guard had to match C2's base-revision rule or the two would disagree on the dual permits. With both keyed on the row's own `status` there is no second rule to agree with.
* **Deploy order is still load-bearing: C2 → C3 → verify.** Backfill-first = re-smeared. C2 is committed (`eff28a7e`) but **NOT on main** — C3 must not run against cloud until it is.
* **Separate defect to file, found by Schema-Fidelity:** `'Examination'` is not in the documented value set — `COMMENT ON COLUMN permits.enriched_status` (`migrations/059:17`) declares only `Permit Issued, Active Inspection, Not Passed, Stalled, Inspections Complete`. Unconstrained `VARCHAR(30)`, no CHECK.

<details><summary>ORIGINAL C3 text (DEAD — base-row model, retained for history)</summary>

`scripts/backfill/backfill-non-base-enriched-status.js` (G2), `--confirm`-gated, `require.main` guard + exported `runBackfill(pool, {confirm})`, **Spec 47 §R1-R12 compliant with `ADVISORY_LOCK_ID = 44` (G1)** — plus the §A.5 table row and the `LOCK_ID_REGISTRY` entry, both, same commit.
**`enriched_status` → NULL ONLY. Do NOT touch `last_scraped_at`** — nulling it makes ~577 permits instantly queue-eligible, re-creating exactly the churn C2 prevents. The preserved cooldown is the brake that makes this safe.
**Compute scope at run time** (~1,257 and growing daily; never hard-code). Guard: `EXISTS` a base row, so a permit whose only row is non-base keeps its status. Emit a **rows-corrected count**. Replay to `UPDATE 0`.
**Deploy order is load-bearing: C2 → C3 → verify.** Backfill-first = re-smeared. C2-live/C3-pending is safe *only because C1 already landed* (metric stays ~724 but no longer halts).
</details>

### C4 — WF2 v2 (2026-08-13): per-site halt-classification gate — convert `assert-data-bounds` + `assert-network-health`; `assert-staleness` AUDITED-AND-EXCLUDED
**Status: ✅ AUTHORIZED 2026-08-13 (operator: "proceed with C4 if you think its ready" — orchestrator judged ready post-re-verify, both blocking findings folded). IMPLEMENTATION IN PROGRESS per the WF2 step list below.**

> **📜 RE-VERIFY ROUND (2026-08-13, 2 agents on the v2 fold itself):**
> **Integration grounder — READY.** All load-bearing citations verified against the live tree (27-push split · [1,19] band · blast radii from manifest · RENAME≡DROP error-path · both TRAP mechanics · Spec 30/44/41 text · scope grep exact). 4 wording corrections folded: two-commit fence trace (#10) · staleness already cites Spec 44 at `:24`, delete `:21-23` (#11) · `>=` 5% operator precision (#18) · the §11 STEP-FAILED banner claim was false for the converted class — real delta is the status chip only (#21). Plus: step timeout 150 / job 170 precision.
> **Cross-read adversary — NOT READY (2 blocking), BOTH the folded-corrections-collision class it was commissioned for; folded same day:** **#9** watchdog silence applies to ALL FOUR chains for data-bounds, not just deep_scrapes/network-health (A2's mechanism × the fatalErrors split — now stated at full scope in KNOWN RISKS) · **#10** v1's `PIPELINE_CHAIN=permits` harness requirement was dropped and the C1 harness doesn't set it — empty-container sources magnitude floors would red Cases A/C for the wrong reason (restored, with the WHY mandated in the test file). Line-edits folded: #11 demoted push ALIGNS to `>= 20` (ruling recorded, Case C pins it) · #12 Spec 44 names the tunable, not a literal · #14 rename-back-if-exists pre-check · #8 scope grep added to implementation-time re-runs · #6 correction ① recorded as MOOTED (v2 removed every `:228` reference; nothing left to fix) · #17 confirmed NO C3/C4 Spec 44 collision (C3 edited §3/§6; C4 edits §4; all three §4 defects verified live on this branch). Findings #18/#19 duplicated Integration #11/#21 — independent convergence, already folded.
**Domain Mode: Backend/Pipeline.** **Database Impact: NO** — honest ONLY because staleness is excluded (repairing its rows would touch the mig-121 tunables). No migration, no logic-var, no `_contracts.json` row, no `db:generate`, no `factories.ts`.
**Severity: MED, not the WF-level CRITICAL** (panel ruling: latent trap; `assert_data_bounds` never threw on any chain 08-07→08-12, both live gating counts 0 — zero measured coverage gained today).
**Supersedes v1 (2026-08-12, preserved in the details block below) and the 2026-08-11 single-script scope (`.cursor/_c4_old_section.md`).**

#### THE DEFECT (unchanged from v1)
A quality assert that throws on **every** failure it can detect will eventually halt a chain on one that proves nothing — killing every downstream step, including `backup_db` (permits chain only — see blast radius).

#### SCOPE — CONVERT TWO, AUDIT THREE (A2 fold)
| Script | Disposition | Why |
|---|---|---|
| `assert-data-bounds.js` | **CONVERT** | 27 `errors.push` = 24 threshold-derived + 3 exception-derived (`:679`,`:810`,`:941`). Runs on all 4 chains; the ONLY script that can strand `backup_db` (permits #22/33 → 11 · coa #11/16 → 5 · sources #26/27 → 1 · deep_scrapes #5/7 → 2 = **19 total**). |
| `assert-network-health.js` | **CONVERT** | deep_scrapes-only, step 3/7, strands 4 steps (incl. all THREE other asserts — data_bounds, engine_health, staleness; Integration re-verify precision). Its ONE `errors.push` (`:97`) has a row (`:98`) with literal `'FAIL'` — passes the gate with ZERO repair; no `try/catch` in the file so NO two-array split: **the entire change is deleting the throw at `:178`**. No fence (`git log -L 178` traces TWO commits — `7391b016` file creation introducing the halt as `process.exit(1)`, then `31c18da0` mechanical →`throw`; neither is a documented fix-fence — re-verify correction #10). The `:38` logicVars-validation throw STAYS — exception class, fires before `emitSummary`: zero verdict representation by construction. |
| `assert-staleness.js` | **⛔ EXCLUDED from conversion — audit COMPLETE, zero repairs needed** | A2's three independent reasons: ① step 7/7, terminal — strands ZERO steps on every chain, ever; the defect sentence is vacuously false for it. ② Conversion is net SIGNAL LOSS: `failed` sits outside `check-pipeline-freshness.js:84` `RAN_STATUSES` → watchdog fires today; `completed_with_errors` sits inside → watchdog silent; the GH job reds either way — lose a channel, save zero steps. ③ Its halting posture is a DOCUMENTED FENCE: mig-121 3-tier operator-tunable gate (`:144-157`), regression lock `assert-staleness.infra.test.ts:115-119` — recorded intent is "TUNE the halt", never "REMOVE the halt". Audit result: its single push `:147` and row `:148` AGREE exactly. **Follow-up filed: 3-line conversion iff deep_scrapes ever gains an 8th step.** |
| `assert-engine-health.js` | **EXCLUDED — rationale CORRECTED per panel refutation** | ⛔ v1's "verdict `:282` can never be FAIL" is FALSE as stated — `:282` is only the third branch of the chain-aware selector `:267-283`; on deep_scrapes (`:219`) and coa (`:252`) its audit_table CAN reach FAIL. The correct exclusion reason is **gate criterion (1)**: its sole `errors.push` (`:180`) is exception-derived (inside a catch). |
| `assert-schema.js` | **STAYS HALTING** | Step 1 of every chain — fail fast before any write. Its `:567` accumulated-error throw was checked and is correctly excluded. Decision recorded in the step-1 spec section. |

**Scope-completeness (this WF's own rule — the claim carries its query):** `grep -l "errors.push" scripts/quality/*.js` + per-file throw inspection; panel-verified 2026-08-12: no other `scripts/quality/` script carries an accumulated-error throw.

#### THE GATE — A3's four criteria (replaces v1's two-clause gate)
> A failure site may be made NON-HALTING only if **all four** hold:
> **(1) Threshold-derived** — a breach means *"the data is wrong"*, not *"I could not check"*. A breach meaning the CHECKER could not run (missing table, transport failure, absent telemetry) is NOT threshold-derived even when expressed as a number.
> **(2) Represented** — on every chain where the site can fire, its row lands in the table `records_meta.audit_table` actually emits (`:957-968`), emitted unconditionally whenever the site can fire. Conditional emission allowed ONLY where the emission condition is logically implied by the push condition — state the implication per site. A site with NO row is ineligible: add the row or keep it fatal.
> **(3) AGREEING** — the row's FAIL condition is **IDENTICAL** to the push condition, not merely FAIL-capable. Any band where one is true and the other false is a silent failure.
> **(4) Reachable** — the bound is attainable by data the pipeline can produce. Record the last measured value beside the bound.
> **Repair branches for a failing site** — never auto-"make the row FAIL": **(a)** align the ROW up to the push · **(b)** align the PUSH up to the row · **(c)** demote to `warnings.push` because the breach was never actionable. **Whichever threshold was set by a commit that NAMED it a false positive is the one that stays** — and the durable guard from the A1 post-mortem: *any correction that changes an existing threshold expression must cite `git log -L` on that line and state the intent of the commit that set it.* (Both go into the step-1 spec section.)
> **Criterion-(1) ruling for `assert-network-health.js:97`** (the gate's own self-contradiction, ruled by the panel): `proxy_error_rate` is threshold-derived AND unambiguously means "the scrape did not work". **Convert anyway** — the alternative strands 4 steps — but the script header must state the accepted cost: the 4 steps it no longer strands will now report on data the scrape failed to collect.

#### THE `cost_outliers` SITE — SHIP BRANCH (c); NEVER (a) (A1 fold)
v1's banked correction #1 would have restored the exact expression `f238b814` (2026-03-27: *"16 permits with bad source data from Toronto Open Data is a known baseline, not actionable"*) deliberately removed — and escalated it WARN→FAIL. **Dead. The fence is preserved:**
* **Branch (c):** `:118-121` — the push becomes `warnings.push(...)` + the `console.log('  WARN: …')` idiom (`warnings[]` exists at `:100`, idiom at `:140-141`); **row `:211` UNCHANGED** (`'< 20'`, `>= 20 ? 'WARN' : 'PASS'` — `f238b814`'s own shape). **The demoted push condition ALIGNS to the row: `if (costOutliers >= 20)` (cross-read #11 ruled, 2026-08-13)** — a `> 0` warn-push would re-create the [1,19] disagreement one severity down AND spam `error_message` WARN lines for the baseline `f238b814` named "not actionable"; per the gate's own threshold rule this cites `git log -L 118,121` + `f238b814`'s stated intent. Push and row now agree at WARN level, zero silent bands; **Case C pins it** (1–19 → row PASS AND no `warnings` entry; 20 → row WARN + `warnings` entry, no throw). Branch (b) (push+row both `>= 20 → FAIL`) only on explicit operator preference.
* **Measured 2026-08-13 (cloud), query attached, re-run at implementation time:** `SELECT count(*) FILTER (WHERE est_const_cost < 0 OR est_const_cost > 500000000) AS outliers_500m, count(*) FILTER (WHERE est_const_cost < 0 OR est_const_cost > 2000000000) AS outliers_2b FROM permits` → **15 at $500M** (f238b814's ~16 baseline, intact) · **0 at the live $2B logic-var ceiling**. The live `cost_outliers = 0` is an artifact of the 4× ceiling raise, not clean data.
* **Filed, not fixed:** the $500M/$2B dual definition (`src/lib/quality/metrics.ts:625` vs the logic var) and the false-green `quality.infra.test.ts:741` (titled "matches at $500M" while only ever reading `metrics.ts`).

#### IMPLEMENTATION SHAPE
* **`assert-data-bounds.js`:** keep `errors[]` full — the REAL reason is `:949` `errorMsg → pipeline_runs.error_message` (NOT `checks_failed`: its only UI consumer `FreshnessTimeline.tsx:1073` is unreachable for this script per `:1080` `if (hasAuditTable) return null`). Add `fatalErrors[]`; the 3 exception-derived sites (`:679`,`:810`,`:941`) push to BOTH; **`:998`** becomes `if (fatalErrors.length > 0) throw`. **Do NOT touch `:947`** (`status = hasErrors ? 'failed'` — the only DB-visible signal on the standalone path). **Stated incoherence, accepted:** post-C4 a standalone threshold failure writes `pipeline_runs.status='failed'` AND exits 0 — the same status/exit divergence C1 introduced at chain level; record it in the header.
* **`assert-network-health.js`:** delete `:178`. Nothing else.
* **Exceptions stay fatal for the MECHANICAL reason** (v1 correction #2, unchanged): audit tables are assigned only at the END of each block (`:240`/`:393`/`:603`) — a mid-block exception leaves the selector at `:957-968` falling through to `return {}`: **zero verdict representation by construction.** Stated in the plan and every test header.
* **No exported pure helper** — the two-array split IS the classification; a helper adds surface with no second consumer. (The reopened guard/export question closes as: not needed for a deletion (network-health); minimal for data-bounds. Operator re-affirms by authorizing.)
* **No AD1-style consumer work** — same terminal state C1 introduced. A4's correction stands RECORDED: the verdict runs as a **GH Actions step** (`chain-coa-permits.yml:139/:179`, `chain-sources.yml:85`, `chain-deep-scrapes.yml:372`), NOT a manifest step — and deep_scrapes' verdict step is **skipped on a SKIPPED slot** (`if: always() && steps.deep_scrapes_guard.outputs.skip != 'true'`). Filed, not fixed here.

#### RED-FIRST TESTS — written into this plan for panel review BEFORE any fix lands (TEST DISCIPLINE)
Harness: the C1 shape (`assert-lifecycle-phase-distribution-halt.db.test.ts` — `spawnSync` + `PG_HOST` pinning + `BUILDO_TEST_DB=1 || CI` + loopback guard, `timeout`/`maxBuffer` on the child). **⛔ PLUS `PIPELINE_CHAIN=permits` in the child env for the data-bounds cases — the C1 harness does NOT set it (grep empty; cross-read blocking #10, restoring v1's dropped sentence).** Without it the script runs standalone-mode (all four blocks) and an empty container REDS the sources magnitude floors (`:418-419` address_points ≥ 500K, `:441` parcels ≥ 460K, `:473` footprints ≥ 400K, `:492` neighbourhoods ≥ 158) — Case C's "no `errors` entry" becomes unsatisfiable and Case A's exact-set proof is polluted. Write the WHY into the test file. One new db.test file per script; `SPEC LINK` headers on both.
**`assert-data-bounds`:**
* **Case A — threshold failure: RED NOW (throws), GREEN AFTER (exit 0 AND row FAIL).** Seed ONE `permits` row with `permit_type='Pre-Permit'` (fires `:235`). Assert the **exact `failMetrics` set** (C1 idiom, `…-halt.db.test.ts:135-136`) — this is also what proves Case A's seed disjoint from Case C (panel ⑤). ⚠ Baseline restated (panel ⑦): an empty container is an all-PASS **VERDICT**, not all-PASS rows — `:838-840` unconditionally warns `cost_estimates table is empty`; assert on verdict + exact FAIL set, never on `checks_warned`.
* **Case B — exception failure: RED NOW AND RED AFTER.** Provoke `:171` → outer catch `:941`; assert non-zero exit AND `records_meta` carries **no `audit_table`** (proving fatality AND why). **Teardown (panel ⑥ resolved): `ALTER TABLE permit_trades RENAME TO _c4_permit_trades_bak` + rename back in `afterEach` — NEVER `DROP … CASCADE`** in the shared testcontainer (same 42P01 `does not exist` error — verified the outer catch has no `'does not exist'` filter, only the inner wsib/inspections catches do — fully reversible, FKs follow the OID). **Idempotent pre-check in `beforeEach`: rename-back-if-exists (cross-read #14) — a crashed prior run otherwise leaves the bak table and every later run's RENAME fails.** *The single most important assertion in this WF: a naive "drop the throw" flips it green and defangs the entire exception class.*
* **Case C — `cost_outliers` regression lock (row-level, panel ⑤ + cross-read #11):** seed outlier count ∈ [1,19] → row **PASS**, no throw, **no `errors` entry AND no `warnings` entry** (pins the aligned `>= 20` push — distinguishes it from a `> 0` warn-push implementation); boundary at 20 → row **WARN** + `warnings` entry, still no throw. Distinct from Case A via the exact-set check.
* **Stated ceiling:** `:679`/`:810` are NOT behaviourally reachable (the `'does not exist'` substring filters `:676`/`:807` route every cheap provocation to SKIP) — source assertion that all three exception sites push to `fatalErrors`, ceiling stated in the file.
**`assert-network-health`:**
* **Case A — threshold: RED NOW, GREEN AFTER.** Seed one `pipeline_runs` row (`pipeline='inspections'`, newest `started_at`, `records_meta.scraper_telemetry = {permits_attempted:10, proxy_errors:10}`) → 100% ≥ 5%. Post-fix: exit 0 AND row FAIL.
* **Case B — exception: RED NOW AND AFTER.** `UPDATE logic_variables SET variable_value='2.5' WHERE variable_key='scraper_empty_streak_warn'` → `:38` throws. **TRAP ① written into the file: UPDATE to a bad-but-parseable value, NEVER `DELETE`** — a missing row silently falls back to `FALLBACK_LOGIC_VARS` (`config-loader.js:85`) and runs green; `2.5` survives the sanitiser (`:224-240`) and reds Zod.
* **TRAP ② written into the file:** the script **SKIPs by default** (`:62-76`) — an unseeded container exits 0 with verdict SKIP; asserting exit 0 without seeding proves nothing.
* Hygiene: delete the seeded `pipeline_runs` row in `afterEach`; snapshot/restore `logic_variables` (C1 pattern); assert the FAIL **message pattern**, never exact counts.
**Overall ceiling (v1, restated):** with no exported helper there is no pure-function lock — a same-behaviour re-merge of the arrays is caught only via exit-code/verdict change. Accepted cost of the ONE-layer ruling; stated in each file header. Red-first output pasted into this file as evidence; **tests go to panel review BEFORE the fix lands.**

#### EXECUTION PLAN — WF2 shape verbatim (`.claude/workflows.md:67-95`; every step named, N/A stated with reason)
- [x] **State Verification:** ✅ measurements re-grounded 2026-08-13 implementation-day (cloud): cost outliers **15** @ $500M / **0** @ live $2B ceiling · both live gating counts 0 · 11 runs 08-03→08-12 never threw · scope-completeness grep re-run by the Integration re-verify agent (exactly 5 scripts, no other accumulated-error throw).
- [ ] **Contract Definition:** N/A — no API route altered. The CONSUMER contract change (step `pipeline_runs.status` `failed`→`completed` on threshold breaches; chain `failed`→`completed_with_errors`) is documented in the step-1 spec section; all four consumers verified: `check-chain-verdict.js:89` (reds it) · `check-pipeline-freshness.js:84` (watchdog silent — accepted, stated) · `FreshnessTimeline.tsx:1065` (rendering delta declared) · `run-chain.js` AD1 (already treats `completed_with_errors` as failed predecessor, landed in C1). `npm run typecheck` after.
- [x] **Spec Update — DONE 2026-08-13 (agent-implemented, before tests/code per Compliance ruling):** ✅ Spec 30 §5.4.1 created (full four-criteria gate + repair branches + `git log -L` rule + the three standing decisions + lesson routing statement) · §5.4 amended · `:7` pointer fixed · Spec 44 §4: 24h-row DELETED, proxy-rate cell names `scraper_error_rate_warn_pct` (default 5%), consecutive-empty row MOVED to Network health naming `scraper_empty_streak_warn` (default 20) · SPEC LINK headers fixed in all three scripts (comment-only) · `41:141` caveat appended · `npm run system-map` regenerated clean (the spec-44 row delta it shows comes from this branch's C3 commit, not these edits).
- [ ] **Schema Evolution:** N/A — Database Impact NO (no migration, no logic-var, no `_contracts.json`; honest only with staleness excluded).
- [x] **Guardrail Test:** ✅ authored 2026-08-13 — `src/tests/db/assert-data-bounds-halt.db.test.ts` (5 tests: Case A Pre-Permit · Case B RENAME-provoked exception w/ idempotent restore · Case C 1–19 + =20 boundary · source assertion pinning the 3 fatal pushes) + `src/tests/db/assert-network-health-halt.db.test.ts` (Case A seeded telemetry 10/10 · Case B `'2.5'` logic-var Zod red, both TRAPs written in). **Panel review of the tests: ✅ COMPLETE 2026-08-13 — 4 reviewers (Gemini CLI · DeepSeek CLI · Integration grounder · Observability), all main-tree/CLI (lesson `:69`, worktrees can't see untracked files). 2 BLOCKING + 5 SHOULD-FIX, all folded; re-run in flight.**
  * **BLOCKING (Integration):** the =20 boundary regex `/cost.*outlier/i` doesn't match the live push message ("out of bounds") — the test would STAY RED under the exact planned fix, inviting a post-hoc test edit. Broadened to `(outlier|out of bounds)` ×4 sites.
  * **BLOCKING (Observability):** Case A pinned row + exit code but never `verdict` — the SOLE red channel post-C4 (`run-chain` lifts `audit_table.verdict` → `check-chain-verdict` reds on literal `'FAIL'`); a fix rebasing the verdict on `fatalErrors` would have passed the suite while silencing chain gating. C1's precedent asserts it (`:284`); added, + verdict WARN pinned at the =20 boundary.
  * **Folded (DeepSeek/Gemini/Integration):** exception-safe `afterEach` ordering (rename-restore can't be skipped by a `clearFixtures` throw) · `if (!pool) return` → throw (silent zero-test pass) · guarded `JSON.parse` · newline-spanning stderr regex · `pool.end()` in `afterAll` both files (C1 pattern) · re-merge ceiling sentence added to the data-bounds header · Case A/C pre-spawn residue preconditions · 2 stale digit comments.
  * **ADJUDICATED AWAY:** Gemini HIGH "fragile PIPELINE_SUMMARY parsing" — REFUTED per the standing C1 Observability ruling (byte-for-byte the real consumer's channel; verified again this round: `run-chain.js:504-505`/`:585-586` last-match on both paths) · Gemini MED "delete the source assertion" — REJECTED per TEST DISCIPLINE's stated-ceiling clause (`:680/:811` behaviourally unreachable) · **DeepSeek HIGH "`PG_HOST` is not what node-postgres reads" — REFUTED, VERBATIM RECURRENCE of the C1-refuted claim** (underscored names are the deliberate safety mechanism; `createPool` demonstrably branches on `PG_HOST` — re-proven live today by the C3 target-DB incident) · DeepSeek env-allowlist/URL-scheme hardening — FILED, not folded (scope) · Gemini shared-isolation-guard extraction — FILED (touches the C1 file).
  * **Deferred to the fix commit BY DESIGN:** the static source assertion extends to require `fatalErrors.push` at the 3 exception sites (post-fix lock, C1's logic-test pattern; DeepSeek's "cannot distinguish a missing fatal push" is true and this is its designed closure).
  * **Verified clean by 2 reviewers independently:** the placeholder `verdict:'UNKNOWN'` + all-`sys_*` assertion (no false-green path; vacuous-empty edge examined and survives) · both TRAPs against live config-loader code · consumer-channel fidelity · records_meta contracts (one pre-existing producer-side note filed: network-health's verdict at `:159` is a parallel-boolean, masked by exact push/row agreement and double-covered by the test).
- [x] **Red Light:** ✅ CAPTURED 2026-08-13 (testcontainer, `BUILDO_TEST_DB=1`, no-file-parallelism): **`Tests 4 failed | 3 passed (7)`** — data-bounds Case A RED at `expect(r.status).toBe(0)` with row-FAIL + exact-failMetrics assertions passing first · Case C (1–19) RED at no-throw with row `PASS`/`5` passing first · Case C (=20) RED at no-throw with row `WARN`/`20` passing first · net-health Case A RED at exit-code with row-FAIL + verdict-FAIL passing first · **both Case B locks GREEN now and asserted to stay green post-fix** · source assertion GREEN. Every red is at the designed post-fix flip, none at an import/fixture error.
  **⚠ PLAN CORRECTION FOUND DURING RED (the measurement-vs-claim class, 3rd instance):** Case B's "records_meta has NO audit_table" is NOT literally true for data-bounds — `pipeline.js:336-340` `emitSummary` unconditionally back-fills a placeholder `audit_table {phase:0, name:'Auto', verdict:'UNKNOWN', rows:[sys_* only]}`, and the outer catch falls through to the script's own unconditional emitSummary. Assertion rewritten to the STRONGER form: `verdict === 'UNKNOWN'` + all rows `sys_*` — still proves "zero DOMAIN verdict representation by construction". (Does not apply to network-health Case B — its Zod throw fires before any emitSummary is reachable, `summaryEmitted === false` asserted directly.)
  **⚠ LINE DRIFT from the Spec Update step's header edits — implementer uses THESE numbers:** data-bounds +1 (exception pushes `:680/:811/:942`, cost push `:119-122`, row `:212`, throw `:999`); network-health −2 (Zod throw `:36`, **the throw to delete is `:176`**).
- [x] **Implementation:** ✅ 2026-08-13 — data-bounds: `fatalErrors[]` added, 3 exception sites dual-push, throw gates on `fatalErrors` alone, cost_outliers demoted to `>= 20` `warnings.push` with the f238b814 + `git log -L` citation comment, `:948` status untouched (incoherence comment added) · network-health: `:176` throw DELETED with the criterion-(1) accepted-cost comment, `:36` Zod throw untouched · test post-fix locks extended (3 `fatalErrors.push` pins + throw-shape + old-throw-gone). **Suite 7/7 GREEN post-fix; both Case B halts intact.** ⚠ One transient Case A failure on the first post-fix run (child took the placeholder path once; not reproducible in isolation or on identical re-run) — recorded as a flake candidate, investigate if it recurs.
  **ALSO (blocking discovery): main was TYPECHECK-RED** — 9 `TS18047` in C3's committed `backfill-smeared-enriched-status.db.test.ts` (the C3 commit bypassed the gate; the green DB-Integration workflow doesn't typecheck). Fixed with 4 non-null assertions at the lock-always-acquired call sites; typecheck now **0 errors**, C3's 8 db tests still green. **Ships as its own commit BEFORE C4's** (per-finding cadence).
- [x] **UI Regression Check:** N/A — no shared component modified (the `FreshnessTimeline` rendering delta is a data-driven change, declared in Standards Compliance; zero UI code touched).
- [x] **Pre-Review Self-Checklist (derived from Spec 30 §5.4.1 + Spec 44 §4, walked against the ACTUAL diff):**
  1. Every site made non-halting is threshold-derived (criterion 1) — the diff changes ONLY the throw gate; the 3 exception sites dual-push and still halt — **PASS**
  2. The demoted cost_outliers push AGREES with its row, `>= 20` both sides (criterion 3), zero silent bands — **PASS** (Case C pins both directions)
  3. The threshold-expression change carries its `git log -L` + f238b814-intent citation at the site — **PASS**
  4. `errors[]` stays full; `errorMsg → pipeline_runs.error_message` and `:948` standalone status unchanged — **PASS** (comment-only at :948)
  5. network-health: exactly one executable line deleted (`:176`); `:36` exception throw intact; accepted cost stated at the site — **PASS**
  6. No unintended behavior deltas: warn message wording kept ("out of bounds" — the broadened test regex covers it); console FAIL→WARN/OK strings changed (log-only) — **PASS with note**
  7. Spec truth in-commit: §5.4.1 exists BEFORE the code (authored first); Spec 44 §4 corrected naming tunables; SPEC LINK headers now truthful — **PASS**
  8. Red-first discipline: pre-fix red output pasted above; 7/7 green post-fix; the two survive-the-fix locks asserted the halts remained — **PASS**
  9. Cross-layer: proxy-rate spec literal replaced by the named tunable; the $500M/$2B duplication pre-existing + FILED — **PASS (declared)**
  10. Per-site gate applied to every qualifying script (scope grep: 5 scripts; 2 converted, 3 excluded with recorded reasons in §5.4.1) — **PASS**
- [x] **Multi-Agent Review (OUTPUT panel) — 5 of 6 reported 2026-08-13 (Integration in flight), all main-tree (uncommitted diff invisible in worktrees):**
  * **Gemini CLI — diff-PASS.** Reviewed the whole file (the known CLI pattern); ALL 7 findings pre-existing → FILED (Pre-Permit dup query · WSIB dual-append · unrecorded-runId warn path · hardcoded COST_MAG_ACCEPT · heritage two-queries-one-catch ["I could not check" masking sibling] · permit_num substring NULL-skip · pct-calc NIT).
  * **DeepSeek CLI — 1 GENUINE diff-induced catch, FIXED:** the file header still said "Exit 1 = fail" after the throw deletion — header rewritten to the true §5.4.1 contract. 1 REFUTED (lock-not-acquired "emits nothing" — SDK emits the SKIP summary, `pipeline.js:905-917`, §R12). 7 pre-existing → FILED (empty-`{}` telemetry false-PASS · no status filter on latest-run query · unproxied blind spot [dedupe: already Spec 44 §4 known-gap + follow-up ①] · schema_drift type assumption · non-finite NaN false-PASS · ORDER BY tiebreaker · checks_passed:'all' with warnings).
  * **Observability — PASS.** Audit rows byte-identical; cascade row-derived (fatalErrors feeds ONLY the throw); count-shift errors→warnings walked through every consumer and ruled non-breaking; FAIL red channel proven end-to-end incl. all 4 workflows. NOTES pinned: ≥20 cost breach terminates `completed_with_warnings` = green-class, amber WARN is its ONLY signal (planned A1 semantics — stated so the ":the verdict step is the red channel" comment isn't read as covering WARN-class sites) · net-health `:159` verdict is a pre-existing parallel-boolean masked by exact push/row agreement (filed) · commit message must say THREE script files (staleness header rides along) · final line drift: exception pushes `:690/:822/:955`, throw `:1019`.
  * **Regression Guardian — PASS.** Fence statements delivered for every deletion: `f238b814` PRESERVED (verified against the code, not the paraphrase; push+row now symmetric for the first time) · net-health throw = knowing cited retirement, no fence (creation `7391b016` + mechanical `31c18da0`) · `hasErrors` throw had zero repo-wide pins (grep-corroborated) · console strings consumer-free (grep-proven) · mig-121 staleness locks intact. Stated gap: no Bash tool → relied on the recorded git archaeology + own greps.
  * **Code Reviewer — PASS all areas except 1 Important, FIXED:** my `!pool` guard fold landed in only ONE of the two new test files — parity applied to the network-health file, both modes re-verified (7/7 opted-in · plain-mode skip). Also surfaced (ruled out as diff defect, FILED): `ceErr`/`ghostErr` catches in data-bounds swallow ANY exception with no push and no rethrow — a 4th/5th silent-failure class, pre-existing.
  * **Reality-Check NOT triggered — stated, not skipped silently:** control-flow only, no derived data value changes (Spec 08 subject-matter rule).
- [x] **Green Light:** ✅ 2026-08-13 — full suite **`8,586 passed | 355 skipped`**, typecheck **0 errors**, lint exit 0 (one pre-existing unrelated warning), DB suites 7/7 + C3's 8/8, plain-mode file skip restored.
  **⚠ FOLD×ENVIRONMENT COLLISION found by the Green Light itself (the §5.1 lesson class, new axis):** the panel's `if (!pool) throw` fold (DeepSeek) redded the PLAIN `npm run test` Husky path — `dbAvailable()` can be true off an ambient `DATABASE_URL` while `getTestPool()` correctly refuses, which is the C1 model's DESIGNED silent skip, not the false-green trap. Fixed: throw only under `BUILDO_TEST_DB/CI` opt-in, silent skip otherwise. **The cross-read pass could not catch this — it required RUNNING the non-opted-in mode. Route with the fold lessons: a fold must be exercised in every execution mode the file has.**

#### SPEC + DOC UPDATES (in-commit)
| Target | Change |
|---|---|
| `30_pipeline_architecture.md` §5.4 + `:7` | NEW per-site-gate subsection (step 1) + §5.4 amendment + `:7` pointer fix (panel ⑧) |
| **Spec 44 §4** (`:291-297`, `:306`) | THREE factual corrections: *"Proxy error rate > 50% → FAIL"* → **"≥ `scraper_error_rate_warn_pct` (default 5%)"** — NAME THE TUNABLE, never a spec literal (cross-read #12: a hardcoded "5%" re-creates the spec-vs-tunable divergence class the mig-121 fence exists to prevent; code truth `>=` at `:96`) · DELETE *"Last successful scrape within 24h → WARN"* (no such check exists) · move `Consecutive empty max` from the Staleness table (`:306`) to network-health (`:127-128`) |
| SPEC LINK headers | ✅ DONE 2026-08-13. `assert-network-health.js:18-20` (the cross-read's ":19-21" precision claim was itself off by one — implementer verified `:18-20` by direct read and edited there) — three Spec 41/42/43 lines replaced with one Spec 44 §4 line. `assert-staleness.js:21-23` stale lines DELETED (its correct Spec 44 §4 line was already present). `assert-data-bounds.js:3-5` — Spec 44 line ADDED. |
| `41_chain_permits.md:141` | *"Any FAIL verdict is logged but does not halt (CQA is observational)"* — already false today, still false after for fatal sites; add the caveat |
| `tasks/lessons.md` | **NO new entry** — `:106` already carries *"data is wrong vs could not check"* (adding again = duplicate noise in a session-start file, panel ruling). The genuinely-new lesson (*a hard gate stated per-script gets applied per-script — state it per-site*) routes **Test (Case C) + Spec (step-1 section)** per Spec 05 §2 strongest-destination — NOT lessons. |
| `review_followups.md` (file, don't fix — 9 items) | ① WAF shadow-ban detector `:128-136` can only WARN, and `completed_with_warnings` is green at `check-chain-verdict.js:89` — **a confirmed WAF ban is invisible to gating** (panel: worth more than the halt reclassification itself) · ② `scraper_error_rate_warn_pct` is the script's only FAIL gate yet named `_warn` · ③ network-health SKIP path `:62-76` → verdict SKIP → green with zero telemetry · ④ `config-loader.js:244-248`: if `logic_variables` is unreachable EVERY script judges against hardcoded fallbacks with no audit row saying so — repo-wide "I could not check" masquerading as "I checked" · ⑤ the `'does not exist'` filters `:676`/`:807` masking a genuinely-missing `entities` table as SKIP · ⑥ $500M/$2B dual outlier definition + false-green `quality.infra.test.ts:741` · ⑦ deep_scrapes verdict GH step skipped on SKIPPED slots (A4) · ⑧ staleness 3-line conversion iff deep_scrapes gains an 8th step · ⑨ threshold re-ranging: `stale_over_30d` runs 41–55 vs threshold 10,000 (244× headroom — mig-121 default absorbed a 6,514 snapshot that collapsed to 41) |

#### STANDARDS COMPLIANCE (§11 — was MISSING from v1, Compliance blocking)
* **Try-Catch Boundary:** no new API routes; both scripts keep their existing `pipeline.run` boundary; **no new catch blocks** ⇒ logError mandate N/A.
* **Unhappy Path Tests:** Cases A/B/C above ARE the unhappy paths (threshold breach · checker death · regression band).
* **UI Layout:** NOT plain N/A — but the delta is SMALLER than v2 first stated (re-verify correction #21): `FreshnessTimeline.tsx:1065`'s `STEP FAILED` banner requires `!info?.records_meta`, and both scripts emit `records_meta` BEFORE throwing (data-bounds self-UPDATEs `pipeline_runs` at `:972-979`; `run-chain.js:582-596`/`:623` parses+COALESCEs the summary even on failure) — so a threshold failure ALREADY renders the audit table today. The real rendering delta is the step's `failed`→`completed` status chip; the banner remains for pre-emitSummary deaths, which stay fatal. No UI code change; declared.
* **Cross-Layer Contracts:** the $500M/$2B duplication is FILED not fixed — declared as a known, pre-existing open contract violation.
* **Commit footer (cross-read #7 residual, choice stated):** the Spec 05 §5 six-field footer IS drafted at commit time with `Severity: MED` — MED does not exempt the footer; it exempts only the lessons-entry mandate (routing already ruled Test + Spec).

#### KNOWN RISKS (restated per panel)
* **Not urgent — plainly:** no measured incident; both live gating counts 0; this retires a latent trap. Honest severity MED.
* **⛔ Watchdog silence applies to EVERY converted threshold site on ALL FOUR chains, not only network-health (cross-read finding #9 — the A2-mechanism × fatalErrors-split collision, caught 2026-08-13).** Today ANY `assert-data-bounds` threshold breach halts its chain → `failed` → outside `RAN_STATUSES` → watchdog fires. Post-C4 → `completed_with_errors` → inside → **watchdog silent on permits, coa, sources AND deep_scrapes; the per-chain verdict GH step becomes the single red channel for the whole threshold class** (same accepted posture as C1, now stated at full scope — accepted as the price of 19 un-stranded steps incl. `backup_db`). The staleness EXCLUSION is what keeps the stale-gate breach's watchdog channel alive.
* Post-C4 a network-health threshold breach additionally reports on a scrape that failed (criterion-(1) accepted cost, stated in the script header).
* The 4 steps network-health no longer strands will run on data a failed scrape didn't collect — accepted cost, stated in the script header.
* Standalone mode: `:947`'s `status='failed'` remains the standalone signal; threshold failures exit 0 there too. Stated.

<details><summary>ORIGINAL C4 v1 (2026-08-12) — SUPERSEDED by the v2 fold above; retained for history</summary>

#### THE DEFECT (one sentence)
A quality assert that throws on **every** failure it can detect will eventually halt a chain on one that proves nothing — killing every downstream step, including `backup_db`.

#### WHY A SWEEP, NOT THREE COMMITS (operator-directed 2026-08-12)
The C4 panel found the blocking defect (`assert-data-bounds.js:119`) is **not in the fix design — it is in the EXISTING audit-row wiring**: a site pushes an error its own audit row calls PASS. `assert-network-health.js` and `assert-staleness.js` have **never been audited for that class**. One script at a time is how a gate gets applied inconsistently. One WF, one gate, applied uniformly.

#### THE GATE — CORRECTED TO PER-SITE (this is the whole plan)
> A failure may become NON-HALTING only if **BOTH**: (1) it is **threshold-derived** ("the data is wrong", not "I could not check"), **AND** (2) **its own audit row can reach `FAIL`**.
>
> **Applied PER-SITE, not per-script.** The 2026-08-11 gate was per-script — which is why `assert-engine-health.js` was correctly excluded (verdict at `:282` can never be FAIL) but why `assert-data-bounds.js:119` slipped through *inside* a qualifying script.
>
> **Any site failing (2) is a PRE-EXISTING BUG in that script's audit wiring** and is repaired in this WF (align the row's status expression to the push condition) — never routed around.

#### SCOPE
| Script | State |
|---|---|
| `scripts/quality/assert-data-bounds.js` | **Analyzed.** 27 `errors.push` = 24 threshold + 3 exception (`:679`, `:810`, `:941`). One gate failure: `:119`/`:211`. |
| `scripts/quality/assert-network-health.js` | **UNANALYZED — the panel must classify every push site and apply the gate.** |
| `scripts/quality/assert-staleness.js` | **UNANALYZED — same.** |
| `scripts/quality/assert-engine-health.js` | **EXCLUDED, confirmed twice.** Only `errors.push` is the top-level catch; verdict `:282` can never be FAIL. Its throw is already correct. |
| `scripts/quality/assert-schema.js` | **STAYS HALTING.** Step 1 of every chain — fail fast before any write. Record the decision in the spec. |

#### CORRECTIONS ALREADY BANKED BY THE PANEL (do not re-derive)
1. **`assert-data-bounds.js:211` becomes `threshold: '== 0', status: costOutliers > 0 ? 'FAIL' : 'PASS'`** — closes the gate hole. Strictly LESS severe than today (today `>0` halts the whole chain).
2. **Exceptions stay fatal for a MECHANICAL reason, not prudence:** audit tables are assigned only at the END of each block (`:240`/`:393`/`:603`), so a mid-block exception leaves the selector at `:957-968` falling through to `return {}` — **no `audit_table` at all**. Exception failures have **zero verdict representation by construction**. State this in the plan and in every test header.
3. **Do NOT touch `assert-data-bounds.js:947`** (`status = hasErrors ? 'failed'`) — must stay keyed on the FULL `errors` array; it is the only DB-visible signal on the standalone path (`:972-979`).
4. **Implementation shape:** keep `errors[]` as the full source (so `checks_failed` stays accurate); add a separate `fatalErrors[]`; gate the throw on that alone.
5. **Corrected latent blast radius** (measured from `manifest.json`): permits #22/33 strands **11** (incl. `backup_db` #33) · coa **#11/16 strands 5** · sources #26/27 strands **1** · deep_scrapes #5/7 strands **2**. **Total 19, not 24.**
6. **The guard/export question is REOPENED.** All three evidentiary claims behind the 2026-08-11 "no guard, no export" ruling are now false: there are **two** guard+export scripts in `scripts/quality/` not one (`audit-fk-orphans.js:305/:307` and `assert-lifecycle-phase-distribution.js:142/:147`); **C1 created the pure-halt-classifier precedent** the ruling said did not exist (`classifyHaltDecision`); and the non-indenting guard style (`if (require.main === module) {` on its own line, block left at original indentation, closed `} // require.main === module`) means the "re-indent 946 lines" cost **does not exist**. **Panel recommends; operator re-affirms.**
7. **No AD1-style consumer work** — same terminal state C1 already introduced; AD1 landed at `run-chain.js:225-235`; all four chains carry a verdict step.
8. **Fence surface near-zero** — nothing repo-wide pins `if (hasErrors) throw`.

#### RED-FIRST TEST PLAN (non-negotiable — TEST DISCIPLINE governs)
Harness: `src/tests/db/assert-lifecycle-phase-distribution-halt.db.test.ts` is the proven shape (`spawnSync` + `PG_HOST` pinning `:121`, `timeout`/`maxBuffer` `:149`). Child needs `PIPELINE_CHAIN` set.

Per script — two behavioural cases plus one stated-ceiling source assertion:
* **Case A — threshold failure: RED NOW (throws), GREEN AFTER (exit 0 AND verdict FAIL).** For data-bounds this is trivially seedable: `PIPELINE_CHAIN=permits` on an empty testcontainer is an all-PASS baseline; seed ONE `permits` row with `permit_type='Pre-Permit'` and `:235` fires while row `:228` is FAIL.
* **Case B — exception failure: RED NOW *AND RED AFTER*.** For data-bounds: `DROP TABLE permit_trades CASCADE` makes `:171` throw into the outer catch `:941`, and `permitsAuditTable` is never assigned so `records_meta` carries **no `audit_table`** — proving fatality AND why. **This is the single most important assertion: a naive "just drop the throw" flips it green and defangs the entire exception class.**
* **Case C — gate-hole regression lock:** seed `costOutliers` in the 1–19 range and assert the audit row is **FAIL**, not PASS. Pins correction #1 so it cannot silently revert.
* **STATED CEILING — do not overclaim.** `:679`/`:810` are **NOT** behaviourally reachable: the `'does not exist'` substring filters at `:676`/`:807` route every cheap provocation (dropped table, dropped `entities`, type mismatch) to **SKIP**, not to the error branch. Only `:941` is provable. Cover the other two with a source assertion that all three catch-derived pushes target the fatal array, and **state in the test file what that cannot catch.**
* Red-first output pasted into this file as evidence; each test fails for the RIGHT reason; **tests go to panel review BEFORE the fix lands**; `SPEC LINK` headers on every new test file.

#### SPEC + DOC UPDATES (in-commit)
| Target | Change |
|---|---|
| **NEW spec section** | **This WF currently has NO spec target** — no spec text anywhere describes these asserts' halting posture (grepped). Create one (Spec 41/42/43, alongside the "assert-schema stays halting" decision) defining the **per-site gate** as the standing rule. |
| `41_chain_permits.md:141` | *"Any FAIL verdict is logged but does not halt (CQA is observational, not blocking)"* — **already false today**, still false after for the fatal sites. Add the caveat. |
| `30_pipeline_architecture.md:7` | *"a FAIL verdict does NOT halt a chain"* — same overclaim, same caveat. |
| `tasks/lessons.md` | *"An assert must distinguish 'the data is wrong' from 'I could not check'."* Plus the new one: **a hard gate stated per-script gets applied per-script — state it per-site.** |
| File, don't fix | the over-broad `'does not exist'` filters at `assert-data-bounds.js:676`/`:807` — a genuinely missing `entities` table silently SKIPs the whole WSIB block: "I could not check" masquerading as "nothing to check". |

#### KNOWN RISKS
* **Standalone-mode ceiling:** post-change, `node scripts/quality/assert-data-bounds.js` with a non-permits-scope failure exits 0 (`:957-968` emits only one, permits-preferred, table). State it.
* **Not urgent — say so plainly.** `assert_data_bounds` has never gone FAIL or thrown on any chain 08-07 → 08-12, and both live gating counts are 0. This WF retires a **latent** trap; zero measured `backup_db` coverage is gained today.

</details>

#### ⚖ WF PANEL — script classification (2026-08-12). **Both scripts QUALIFY, but the WF's stated motivation is FALSE for both.**

**⛔ THE MOTIVATION DOES NOT APPLY TO 2 OF THE 3 SCRIPTS.** Measured from `scripts/manifest.json`: **`assert-network-health.js` and `assert-staleness.js` run in `deep_scrapes` ONLY** — not permits, not coa, not sources. Their `SPEC LINK` headers citing Specs 41/42/43 are stale fiction. **`backup_db` is not in the `deep_scrapes` chain at all, so neither script can EVER strand it.**

| Script | Chain | Step | Strands if it throws | `backup_db` downstream? |
|---|---|---|---|---|
| `assert_network_health` | deep_scrapes **only** | **3 / 7** | **4** (`refresh_snapshot`, `assert_data_bounds`, `assert_engine_health`, `assert_staleness`) | **NO** |
| `assert_staleness` | deep_scrapes **only** | **7 / 7 — TERMINAL** | **ZERO** | **NO** |
| `assert_data_bounds` | permits/coa/sources/deep_scrapes | #22/#11/#26/#5 | 11 / 5 / 1 / 2 = **19** | yes (permits #33) |
**Sweep-wide latent stranding = 23, and only `assert-data-bounds` (permits) can reach `backup_db`.** Note the irony: `assert_network_health` throwing strands **the two other asserts this WF is fixing.**
⇒ **`assert-staleness` must be justified as GATE UNIFORMITY, not blast radius.** Its only behavioural delta is this step's `pipeline_runs.status` (`failed`→`completed`) and the chain's (`failed`→`completed_with_errors`). **Say that in the plan; do not let a reader infer a blast radius that does not exist.**

**✅ BOTH QUALIFY — and neither has a `cost_outliers`-shape hole.** Each has **exactly ONE** `errors.push`, and its audit row is authored **in the same `if` branch** with a literal `'FAIL'`:
* `assert-network-health.js:97` (`errorRate >= scraper_error_rate_warn_pct`) → row `:98` `status:'FAIL'`. Verdict `:161` reaches FAIL. **AGREE exactly.**
* `assert-staleness.js:147` (`stale30d > staleness_max_stale_over_30d`) → row `:148` `status:'FAIL'`. Verdict `:160` reaches FAIL. **AGREE exactly.**
**Zero push/row disagreements in either file.** The `:119` defect is `assert-data-bounds`-specific.

**✅ THE FIX FOR THESE TWO IS ONE DELETED LINE EACH — correction #4 does NOT apply here.** Neither script has ANY `try`/`catch`; every exception is a raw throw that bypasses `emitSummary` entirely, so **`errors[]` contains only threshold-derived items and no `fatalErrors[]` array is needed.** The whole change per script is deleting the throw (`assert-network-health.js:178`, `assert-staleness.js:177`). The two-array shape is a `assert-data-bounds` requirement only.
**And the mechanism is cleaner:** these scripts have ONE `emitSummary` on the main path placed immediately *before* the throw. Exceptions fire *before* it → `pipeline.run` re-throws with **no summary emitted** → zero verdict representation by construction (correction #2 holds, more simply). Threshold failures fire *after* it → **the FAIL audit row already reaches `pipeline_runs` today, even on the halting path** → converting to non-halting loses NO observability. Correction #7 (no AD1 work) confirmed for both.

**⛔ REFUTATION — `assert-engine-health`'s EXCLUSION RATIONALE IS WRONG (the verdict is right).** The plan says twice that "verdict `:282` can never be FAIL". **`:282` is only the THIRD branch of a 3-way chain-aware selector at `:267-283`.** On `deep_scrapes` it returns `inspAuditTable` (verdict `:219` `hasFails ? 'FAIL' : 'PASS'`); on `coa` it returns `coaAuditTable` (`:252`). **On 2 of 4 chains that script's audit_table CAN be FAIL.** The correct exclusion reason is criterion **(1)**: its sole `errors.push` (`:180`) is inside a catch — **exception-derived**. *Restating an exclusion per-script is precisely the failure mode this WF exists to correct.* **Fix the rationale in the plan text.**

**✅ LATENT CONFIRMED for both** — 11 cloud runs 08-03 → 08-12, **every one `completed`, `checks_failed = 0`, never FAIL, never threw.** `proxy_error_rate` ran 0.0–0.2% against a 5% gate (**25× headroom**); `stale_over_30d` ran 41–55 against a threshold of **10,000** (**244× headroom** — a second instance of the already-filed threshold-re-ranging follow-up; mig 121's default absorbed a 6,514 snapshot that has since collapsed to 41).

**✅ TEST REACHABILITY IS BETTER HERE — the data-bounds ceiling does NOT transfer.** These scripts have no `'does not exist'` substring filter. For BOTH, a threshold case AND an exception case are provokable **without destructive DDL**:
* net threshold `:97` — one `INSERT` into `pipeline_runs` (`pipeline='inspections'`, newest `started_at`, `records_meta.scraper_telemetry = {permits_attempted:10, proxy_errors:10}`) → 100% ≥ 5%.
* net exception `:38` — `UPDATE logic_variables SET variable_value='2.5' WHERE variable_key='scraper_empty_streak_warn'`.
* stale threshold `:147` — set `staleness_max_stale_over_30d=0`, seed 1 permit + 1 `permit_inspections` row `scraped_at = NOW() - 60 days`.
* stale exception `:53` — `scrape_stale_days='2.5'`.
* `net :53/:35`, `stale :69/:109/:50` need DDL/connection loss — **source assertion with a stated ceiling.**
**TWO TRAPS THE TEST FILE MUST ENCODE:** ① **the logic-var provocation must be an `UPDATE` to a bad-but-parseable value, NEVER a `DELETE`** — `config-loader.js:85` seeds from `FALLBACK_LOGIC_VARS`, so a missing row silently falls back and the script runs GREEN. `0`/`-1`/`2.5` survive the sanitiser (`:224-240`; none of these 8 keys is in `ZERO_IS_INVALID`/`NEGATIVE_IS_INVALID`) and reach Zod. **Write the reason into the file or someone will "simplify" it into a `DELETE` and get a false green.** ② **`assert-network-health` SKIPs by default** (`:62-76`) — on an empty container there is no `inspections` run, so it exits 0 with verdict `SKIP`. A test asserting "exit 0" without seeding proves nothing.
**Hygiene:** delete the seeded `pipeline_runs` row in `afterEach`; snapshot/restore `logic_variables` (the C1 pattern); assert staleness on the FAIL **message pattern**, not an exact count.

**✅ Fence surface confirmed ZERO** — `grep` for both throw messages returns only the two source lines. Two inert infra tests exist; note `assert-staleness.infra.test.ts:112` forbids the literal `threshold: '== 0'` and `:119` forbids `if (stale30d > 0)` — **mig-121 fences on the GATE SHAPE, not the throw.** Deleting the throw does not trip them; restructuring the 3-tier gate would.

**⛔ SPEC TARGET CORRECTED — Spec 44 §4 is the governing section for these two, and it is FACTUALLY WRONG.** The plan's spec table does not mention it at all. `docs/specs/01-pipeline/44_chain_deep_scrapes.md:291-297` documents *"Proxy error rate > 50% → FAIL"* — **the code gates at 5, ten-fold off**; documents *"Last successful scrape within 24h → WARN"* — **no such check exists**; and files `Consecutive empty max` under the *Staleness* table (`:306`) when it lives in network-health `:128`. **All three corrections are in-commit.** Also fix both scripts' `SPEC LINK` headers, which cite chains they do not run in.

**⇒ THE MOST VALUABLE FINDING IS NOT THE HALT RECLASSIFICATION.** `assert-network-health`'s **WAF shadow-ban detector** (`:128-136`) — the script's headline purpose per its own docblock — **can only ever emit `WARN`**, and `check-chain-verdict.js:89` admits `completed_with_warnings` as **green**. Four of its seven checks are WARN-only (`schema_drift_count :84`, `avg_latency_ms :117`, `consecutive_empty_hit :131`, `session_failures :146`). **A confirmed WAF ban is invisible to gating.** Also: `scraper_error_rate_warn_pct` is the script's ONLY FAIL-and-halt gate yet is named `_warn`. **File both; the panel judges this worth more than the halt reclassification, which by measurement gains nothing today.**
**Repo-wide sibling (larger than the plan's filed one):** `config-loader.js:244-248` wraps the whole control-panel query in a catch that logs a warn and returns hardcoded fallbacks — **if `logic_variables` is unreachable, EVERY pipeline script proceeds to a confident PASS/WARN verdict judged against fallback thresholds, with no audit row saying the thresholds were not the operator's.** "I could not check" masquerading as "I checked", repo-wide.


#### ⛔⛔ WF PANEL — adversarial + compliance (2026-08-12). **NOT READY. 2 blocking. Correction #1 walks into a documented fence.**

**⛔ A1 — BANKED CORRECTION #1 REVERSES A DELIBERATE, DOCUMENTED FIX. DO NOT SHIP IT.**
`git log -L 211,211:scripts/quality/assert-data-bounds.js` → commit **`f238b814` (2026-03-27)**, *"fix(28_data_quality): adjust false WARN thresholds in CQA checks"*. Its diff at that exact line:
```
-  { metric:'cost_outliers', … threshold:'== 0', status: costOutliers >  0  ? 'WARN' : 'PASS' },
+  { metric:'cost_outliers', … threshold:'< 20', status: costOutliers >= 20 ? 'WARN' : 'PASS' },
```
Commit body: *"cost_outliers: == 0 → < 20 (**16 permits with bad source data from Toronto Open Data is a known baseline, not actionable**)"*.
**Correction #1 proposed `threshold: '== 0', status: costOutliers > 0 ? 'FAIL' : 'PASS'` — restoring the EXACT expression `f238b814` removed, and escalating it WARN→FAIL. The plan never mentions `f238b814`.**
**The "less severe" claim is HALF right, and the wrong half was never checked:**
* **Halt axis — REFUTED, the fix IS less severe.** `:119` sits inside the outer `try` reaching `:998`, so today `costOutliers ∈ [1,19]` **already halts the whole permits chain** while its row reads PASS. No previously-silent condition becomes chain-redding.
* **Audit axis — IT IS A SCOPE CHANGE AND A FENCE BREAK.** `chain_permits` currently tolerates a documented ~16-permit bad-source baseline. Post-fix ONE such permit ⇒ `verdict: FAIL` ⇒ `completed_with_errors` ⇒ **a permanent red on `check-chain-verdict.js:105`** — precisely the *"standing red everyone learns to ignore"* that **this plan's own C3 FOLD 4 rejected**, applied to C4 without anyone noticing.

**THE GATE HAD NO BRANCH FOR THIS — three repairs exist, the panel considered only the one that breaks the fence:**
| Branch | Change | Gate | Fence |
|---|---|---|---|
| (a) as banked | row down to `> 0 ? FAIL` | ✅ | ⛔ **BROKEN** |
| (b) align push UP | `:118` → `if (costOutliers >= 20)` + row `>= 20 ? 'FAIL'` | ✅ | ✅ preserved |
| **(c) demote the site** | `:119` → `warnings.push`; row unchanged | n/a (no error push) | ✅ preserved, matches `f238b814`'s own *"not actionable"* |
**⇒ SHIP (c), FALL BACK TO (b). NEVER (a).** And **measure first**: `SELECT count(*) FROM permits WHERE est_const_cost < 0 OR est_const_cost > 500000000` — if that returns ~16, branch (a) reds the chain on day one.
**Corroborating defect found while checking reachability:** `src/lib/quality/metrics.ts:625` counts outliers at **`> $500,000,000`** while `assert-data-bounds.js` reads the **`$2,000,000,000`** logic var (`seeds/logic_variables.json:359-365`). **Two live definitions of "cost outlier".** And `src/tests/quality.infra.test.ts:741` is titled *"metrics.ts upper bound matches assert-data-bounds at $500M"* yet **only ever reads `metrics.ts`** — a green test asserting an equality that is demonstrably false. The live `cost_outliers = 0` is an artifact of a 4× ceiling raise, not of clean data.

**⛔ A2 — NARROW THE SCOPE: `assert-data-bounds` + `assert-network-health`. EXCLUDE `assert-staleness`.**
**The plan conflates sweeping the AUDIT with sweeping the CONVERSION. Audit all three; convert two.** Three independent reasons:
1. **Blast radius ZERO** — step 7 of 7, terminal. The plan's own defect sentence is *"killing every downstream step, including `backup_db`"*; for staleness that set is **empty, on every chain, ever.**
2. **Converting is a net SIGNAL LOSS.** `check-pipeline-freshness.js:84` `RAN_STATUSES = ['completed','completed_with_warnings','completed_with_errors']`. Today a stale-gate breach ends deep_scrapes `failed` — **outside** RAN_STATUSES → **the watchdog fires**. Post-C4 it becomes `completed_with_errors` — **inside** → **watchdog goes silent.** The GH job reds either way. Net: lose a channel, save zero steps.
3. **Its halting posture is a DOCUMENTED FENCE WITH A LIVE REGRESSION LOCK.** Migration 121 / WF3 2026-05-08 built the 3-tier gate (`assert-staleness.js:144-157`) deliberately — PASS=0, WARN=1..threshold, FAIL=`> staleness_max_stale_over_30d`, **operator-tunable via `/admin/control-panel`**. `src/tests/assert-staleness.infra.test.ts:115-119` pins it: *"the previous binary gate made the chain halt on any non-zero stale count; post-mig-121 the gate must reference the loaded threshold."* **Recorded intent is "TUNE the halt", not "REMOVE the halt".** *(Near-miss worth noting: `:107-112` of that same file forbids the literal `threshold: '== 0'` — the exact string correction #1 wanted to write into a sibling file.)*
**`assert-network-health` is the opposite case and is the cheapest, highest-yield item in the sweep:** one `errors.push` (`:97`) whose row (`:98`) is a hard-coded `status:'FAIL'` — **passes the gate with ZERO repair**; no `try/catch` in the file so **no two-array split**; the change is deleting `:178`. Its throw carries **no fence** (`git log -L 178,178` → only `31c18da0`, a mechanical `process.exit(1)`→`throw` SDK migration). And it is the only script that retires a NEW blast radius (4 steps).
**Cost of being wrong:** *including* staleness = retire a documented operator-tunable halt with no fence statement, lose a watchdog channel, gain zero steps, build a third testcontainer fixture for nothing. *Excluding* it = one latent halt survives at a terminal step where there is nothing downstream to strand; a 3-line follow-up if deep_scrapes ever gains an 8th step. **Strictly the cheaper mistake.**

**⛔ A3 — THE GATE IS UNDER-SPECIFIED AND DOES NOT CATCH ITS OWN FOUNDING EXAMPLE.** A row written `costOutliers >= 20 ? 'FAIL' : 'PASS'` **can** reach FAIL, satisfies the two-clause gate as literally written, and leaves 1–19 exactly as silent. **The `:119` defect is DISAGREEMENT, not incapability.** Replace the gate with:
> A failure site may be made NON-HALTING only if **all four** hold:
> **(1) Threshold-derived** — a breach means *"the data is wrong"*, not *"I could not check"*. A breach meaning the CHECKER could not run (missing table, transport failure, absent telemetry) is NOT threshold-derived even when expressed as a number.
> **(2) Represented** — on every chain where the site can fire, its row lands in the table `records_meta.audit_table` actually emits (`:957-968`), emitted unconditionally whenever the site can fire. Conditional emission is allowed ONLY where the emission condition is logically implied by the push condition — **state the implication per site.** A site with NO row is ineligible: add the row or keep it fatal.
> **(3) AGREEING** — the row's FAIL condition is **IDENTICAL** to the push condition, not merely FAIL-capable. Any band where one is true and the other false is a silent failure.
> **(4) Reachable** — the bound is attainable by data the pipeline can produce. Record the last measured value beside the bound.
> **FOURTH REPAIR BRANCH (missing today):** a site failing (1)–(4) is NOT automatically repaired by "make the row FAIL". Choose (a) align the ROW up, (b) align the PUSH up, or (c) demote to `warnings.push` because it was never actionable. **Whichever threshold was set by a commit that NAMED it a false positive is the one that stays.**
**Also: criterion (1) is self-contradictory, and `assert-network-health.js:97` proves it** — `proxy_error_rate >= threshold` is unambiguously threshold-derived AND unambiguously means *"I could not check"* (the scrape did not work). **The gate must rule.** Panel recommendation: convert anyway (the alternative strands 4 steps), but write into the header that the 4 steps it no longer strands will now report on data the scrape failed to collect — **an accepted cost that must be STATED, not discovered.**

**⛔ A4 — BANKED CORRECTION #7 IS FALSE AS A MECHANISM CLAIM.** *"All four chains carry a verdict step"* — **`check_chain_verdict` appears in NO `manifest.chains` entry.** permits ends at `#33 backup_db`; coa `#16`; sources `#27`; deep_scrapes `#7`. The verdict runs as a **separate GitHub Actions step** (`chain-coa-permits.yml:139`/`:179`, `chain-sources.yml:85`, `chain-deep-scrapes.yml:372`) from `scripts/check-chain-verdict.js` (**not** `scripts/quality/`). True in substance, false in mechanism — **and the mechanism matters: deep_scrapes' verdict step carries `if: always() && steps.deep_scrapes_guard.outputs.skip != 'true'`, so on a SKIPPED slot there is no verdict check at all.**

**Other required corrections:** ① **`:228` → `:228-233`** (`:228` is the `push({` opener; the status expression is `:232` — an implementer editing `:228` edits nothing; this plan's own FINAL-PASS precedent #5 governs). ② **`:118` → `:118-119`** (branch (b) edits `:118`). ③ **Corrections #3+#4 DO compose**, but create a NEW standalone incoherence: post-C4 a standalone threshold failure writes `pipeline_runs.status='failed'` **AND exits 0**. ④ **Correction #4's stated reason is hollow** — `checks_failed`'s only consumer `FreshnessTimeline.tsx:1073` is **unreachable** for this script (`:1080` `if (hasAuditTable) return null;`). The real reason to keep `errors[]` full is **`:949 errorMsg → pipeline_runs.error_message`**. State that or the next reader collapses it. ⑤ **Cases A and C are the same test twice as specified** — Case C's seed alone satisfies Case A's assertion. Make Case C a **row-level regression lock** (or a 19/20 boundary test under A1), and prove Case A's seed disjoint with an **exact-set `failMetrics`** assertion (C1's idiom, `…-halt.db.test.ts:135-136`). ⑥ **Case B has NO TEARDOWN** — `DROP TABLE permit_trades CASCADE` is irreversible for every sibling file in a shared testcontainer, which this plan's own Observability fold already flagged. Resolve before code. ⑦ **"empty container is an all-PASS baseline" is the SEVENTH unmeasured premise** — `:838-840` unconditionally warns `cost_estimates table is empty`, so `checks_warned >= 1` at baseline. Restate as **"all-PASS audit VERDICT"**. ⑧ **`30_pipeline_architecture.md` — wrong half.** §5.4 (`:269-277`) is **already correct and precise** (*"a FAIL verdict from a step that exited 0 does NOT stop the chain"* = exactly post-C4 semantics); it is `:7`'s pointer that overclaims. Amend §5.4 to add that some asserts additionally throw on a defined subset. ⑨ **File: `assert-network-health`'s SKIP path** (`:62-76`) — absent scraper telemetry ⇒ `verdict:'SKIP'` ⇒ `run-chain.js:637` tests only `includes('FAIL')` ⇒ **green**. Same class as the `:676`/`:807` filters. ⑩ **Add the grep that establishes the SCOPE table's completeness** — it IS complete (verified: no other `scripts/quality/` script carries an accumulated-error throw; `assert-schema.js:567` does and is correctly excluded), but the plan asserts it without the query, violating this WF's own §5 rule.

**§11 COMPLIANCE — `Database Impact: NO` is HONEST (verified: no migration, no logic-var added, and none of these thresholds are in `_contracts.json`) — but only if `assert-staleness` is EXCLUDED, since repairing its rows touches the mig-121 tunables.** FAILS: **no Standards Compliance block at all** (Try-Catch / Unhappy Path / logError / UI must be declared — and UI is NOT plain N/A: `FreshnessTimeline.tsx:1065` currently renders `STEP FAILED` for these steps and will now render completed-with-a-FAIL-table). **The Pre-Review Self-Checklist is STRUCTURALLY IMPOSSIBLE as scoped** — it must derive from the governing spec, and the plan says *"this WF has NO spec target"*. **⇒ WRITE THE SPEC SECTION FIRST, then derive the checklist.** **Cross-Layer Contracts FAILS** on the `$500M`/`$2B` duplication above.

**LESSON ROUTING — MIS-FILED, fix before commit:** `tasks/lessons.md:106` **already carries** *"An assert must distinguish 'the data is wrong' from 'I could not check'"* with three bullets. **The plan proposes to add it AGAIN — delete that row**, it is duplicate noise in a file read at every session start. And the genuinely new lesson (*"a hard gate stated per-script gets applied per-script"*) is routed to the **weakest** fitting destination, violating Spec 05 §2 (*"choose the strongest destination that fits — never weaker"*): it has a deterministic reproducer (**Test** = Case C) and C4 is creating a **Spec** section in the same commit. **Route it Test + Spec; drop the lessons entry.** No commit footer is drafted; honest severity is **MED**, not the WF-level CRITICAL (the plan's own KNOWN RISKS says zero measured coverage is gained today).

**🔑 THE SYSTEMIC LESSON RECURRED ONE SECTION LATER.** A1 is — again — a **collision between two separately-folded corrections**: *"align the audit row to the push condition"* is right in general, and `f238b814`'s *"this baseline is not actionable"* is right in particular, and applying the first to the second **inverts a documented decision**. C3's section already named this exact pattern as the systemic lesson; **C4's corrections were banked without the cross-read and the identical defect recurred.** **Durable guard, to go in the new spec section: *any correction that changes an existing threshold expression must cite `git log -L` on that line and state the intent of the commit that set it.***

#### 📜 PANEL RECORD — every review run on this WF, what it found, and the standing rulings
*(Operator-directed 2026-08-12: preserve the reviews, conclusions and learnings in the WF itself. Compliance and ground-truth ran repeatedly; their rulings are the durable output, not the prose.)*

**Rounds run across Phase A (C1 → C4), 30+ reviewer-passes total:**
| Altitude | Roster | Highest-value output |
|---|---|---|
| C1 test panel (5) | Gemini · DeepSeek · Code Reviewer · Observability · Integration · Regression Guardian | CR found only 1 of 4 halting sites was pinned. Integration REFUTED the test-DB isolation premise. |
| C2 test panel (6) | + adjudication | 4/4 independent hits on one escape hatch; DeepSeek found `MIN` vs `MAX` blessed the wrong family. |
| **C0 decision panel (6)** | Ground-truth · Integration · Explorer · Schema-Fidelity · Regression Guardian · Adversarial critic | **Killed C0 entirely.** The adversarial critic's premise attack was correct; ground-truth's domain ruling on the 53 settled it. |
| C2 output panel (3) | Code Reviewer · Regression Guardian · DeepSeek | Found the JS writer left unscoped in the same chain; the "for free" overclaim; missing Q3 telemetry. |
| **C3 plan panel (8)** | Reality-Check · Integration · Regression Guardian · **Compliance** · DeepSeek-idempotency | Compliance: 22 findings / 10 blocking. Idempotency lens found the backup/UPDATE atomicity race NOBODY else saw. |
| C4 plan panel (2) | Integration · Observability | Both independently found `:119`; Integration refuted 3 of 4 blast-radius numbers AND all 3 claims behind the 8/11 scope ruling. |

**STANDING RULINGS — settled, do not re-open:**
1. **`enriched_status` is scoped by the row's own `status`.** Not a canonical/base row. `MIN(revision_num)` is a REJECTED ALTERNATIVE, pinned as a negative assertion.
2. **`last_scraped_at` stays row-wide, unscoped** (R4). Survived three separate attempts to scope it.
3. **`lead-id.js` is OUT of scope for any row-selection rule** — 3/3 reviewers: it is a per-row FORMATTER, not a selector. Touching it re-keys 245,785+ NOT-NULL UNIQUE rows.
4. **No persisted "canonical row" marker.** 2/2. `permits.lead_id` is the live proof that a trigger-maintained derived column on this table silently rots.
5. **A shared fragment DOES cross Python↔JS** (`scripts/lib/status_mapping.json`). The "deliberate parallel constants" claim was false; deleted.
6. **`assert-engine-health.js` stays halting** (verdict can never be FAIL). **`assert-schema.js` stays halting** (step 1, fail fast before any write).
7. **Re-derive administrative class LIVE from `permit_type_classifications`.** NEVER use `permits.lead_id IS NULL` as a proxy — measurably wrong for 1,190 rows.
8. **Standing audit rows, not one-shot assertions.** An apply-time invariant proves a state; only a recurring check defends it.
9. **A permanently-non-zero metric is WARN + a `_retighten` companion, never FAIL.** A standing red is the noise class C1 shipped to eliminate.

**LEARNINGS — all seven routed to `tasks/lessons.md` in `5ec3523a`:**
count-as-inference (4 occurrences) · the status-grain rule · dated backup before a destructive NULL-out · backup atomicity under READ COMMITTED · apply-time invariant ≠ enforcement · WARN-not-FAIL for permanently-nonzero metrics · **re-read the folded corrections as a SET before writing code.**

**PROCESS DEFECTS FOUND (route in this WF):**
* **`CLAUDE.md:134`'s Reality-Check trigger is scoped to "parcel field"** — but Spec 08 `:200` fires RC by SUBJECT MATTER, and `CLAUDE.md:114` says Spec 08 governs on conflict. The narrow wording has a documented miss on the record. **One-line fix, still unrouted.**
* **No gate measures ARRIVAL.** C1 passed every quality gate and sat un-pushed for a day while the outage it fixed ran nightly. Candidate: a WF6 "is it on `origin/main`?" exit step.
* **A hard gate stated per-SCRIPT gets applied per-SCRIPT.** State it per-SITE. This is the defect that produced `:119` and is the reason this WF exists in its current form.
* **Eight reviewers did not catch what cross-reading two corrections caught.** Both of C3's sharpest defects were collisions between separately-folded corrections. Adding reviewers does not find these.

### C7 — WF2 PLAN v1 (2026-08-13): status-writer invalidation for `enriched_status` — THE ROOT-CAUSE FIX
**Status: PLAN v2 COMPLETE — 4-reviewer panel folded (⚖ block below supersedes conflicting v1 text) · G1 RESOLVED benignly (guard sound; timeline corrected) · fold-set cross-read clean (G1's expected-first-emission vs fold-7's post-deploy zero are compatible phases, split by the final-C3 deploy step). AWAITING OPERATOR y/n. NO CODE before authorization.**
**PROMOTED into Phase A by operator 2026-08-13: NEXT after C4 ✅, BEFORE C5 (standing "C5 lands LAST" ruling preserved).**
**Domain Mode: Backend/Pipeline.** **Database Impact: NO** (a `CASE`/`SET` term in existing DML — no migration, no column, no logic-var, no `_contracts.json`). **Severity: MED** (the bridge holds: C3 re-runnable + nightly WARN row; no live incident).
**Rollback Anchor: `14642dae` (branch) / `20fa17f2` (main).**

#### VERIFIED EVIDENCE BASE (every line carries its source pass — do not re-derive)
| Fact | Source | 
|---|---|
| The mechanism: `load-permits.js:357` `status = EXCLUDED.status` moves a permit past `'Inspection'` while `enriched_status` (deliberately absent from the SET list) stays. Traces to `d398d5e7`, never revisited. Zero prior fix attempts (`git log --all -S "enriched_status = NULL"` → 0 pre-C3). | C3 Regression Guardian + Integration, 2026-08-12; site re-verified by grep 2026-08-13 |
| **⛔ NEW 2026-08-13 — a SECOND writer all prior passes missed: `close-stale-permits.js` has TWO `permits.status` UPDATE sites** — `:127-134` (`→ 'Pending Closed'` for rows that LEFT the feed, `last_seen_at < lastLoad`) and `:145-152` (`Pending Closed → 'Closed'` after grace). Site 1 moves `Inspection` rows to non-Inspection **without touching `enriched_status`** — smear on left-the-feed rows, the exact class a loader-only fix can NEVER see. The C3-era claim "the class regenerates via the loader" was INCOMPLETE. | This plan's grounding grep + file read, 2026-08-13. Grep: `grep -rn "status = EXCLUDED.status\|SET status" scripts/*.js` → permits writers = load-permits `:357` + close-stale `:128`/`:146` only (load-coa/load-heritage are other tables) |
| The fence: `classify-permit-phase.js:10-12` — *"writes to enriched_status (not raw status) so the permits loader upsert won't conflict — raw CKAN status is preserved."* Documented NEVER-CLOBBER intent; the invalidation half was never written by anyone. | C3 Guardian ("the deepest finding") |
| The rule the fix enforces: `enriched_status` is a per-row refinement of the row's OWN `status='Inspection'` — C3's predicate `enriched_status IS NOT NULL AND status IS DISTINCT FROM 'Inspection'`, `IS DISTINCT FROM` mandatory (`permits.status` nullable, 2 NULL rows live; `<>` is UNKNOWN for NULL → structurally invisible forever). | C0 panel ruling + C3 Reality-Check + test 1c |
| Regeneration measured: 1,350 (08-12) → 1,408 → 1,416 (08-13 pre-backfill) — ~50-60 rows/day accretion. The "97 closed AFTER being scraped" cohort is the measured instance. | C3 CORRECTION 1 + this session's three live counts |
| Consumers: `enriched_status` has exactly ONE logic consumer (`computeStalled` signal 1, `lifecycle-phase.ts:646`); does NOT feed `lifecycle_phase`; `permit_phase_transitions` cannot orphan; NO notification storm (LIFECYCLE_STALLED fires only false→true; invalidation only produces true→false); `lead-inspect-query` display-only. | C3 Guardian consumer trace (3 PASS) |
| Reclassification path: `enriched_status` is NOT a dirty key; the loader bumps `last_seen_at` for every fed permit (`load-permits.js:386-399`) → `classify_lifecycle_phase` re-derives SAME chain, BEFORE the asserts. Left-the-feed rows: `close-stale-permits` touches them at the same moment it moves status — the invalidation lands in the SAME UPDATE, and phase is status-derived so reclassify follows normally. | C3 FOLD 2 + self-heal analysis |
| Retirement contract: the §4.9 pair (`enriched_status_status_scope_drift` WARN + `_retighten` INFO, `assert-global-coverage.js:1376-1397`) SELF-RETIRES at 0 — no code change needed to retire it; its threshold text names this WF as the owning fix and MUST be amended in-commit (it currently says "until then C3 must be re-run"). | C3 commit `c01031f7` + this session's read |
| Hot-path safety: a row-local `CASE` in the SET list reads NO sibling rows — the C0 DB-impact analysis ruled sibling-reading trigger shapes deadlock-exposed on the batched multi-row upsert and row-local recomputation safe. No index, no migration, O(1)/row. | C0 Schema-Fidelity DATABASE IMPACT ruling |
| Test harness precedents: direct-pool db test `backfill-smeared-enriched-status.db.test.ts` (8/8 green, same table+column, seed→run→assert shape); `load-permits.js` HAS a `require.main` guard (lessons `:64`); the C1/C4 harness for child-process fallback; TEST DISCIPLINE + both-directions lock proof (today's envelope-lock lesson). | C3 tests + lessons + this session |

#### THE DESIGN (panel refines; the operator authorizes)
**One rule, THREE sites — the same predicate C2's writers and C3's backfill already enforce, now enforced at every basis-mover:**
1. `load-permits.js:357` upsert SET list gains: `enriched_status = CASE WHEN EXCLUDED.status IS DISTINCT FROM 'Inspection' THEN NULL ELSE permits.enriched_status END` — **simplified from the C3-record draft**: the status-CHANGE term is unnecessary — any non-Inspection incoming status ⇒ no enriched_status, which IS the standing rule, idempotently (a NULL→NULL rewrite is a no-op). Staying-Inspection preserves the value byte-for-byte — clobber structurally impossible.
2. `close-stale-permits.js:128` (`→ Pending Closed`): add `enriched_status = NULL` to the SET (target status is definitionally non-Inspection — no CASE needed).
3. `close-stale-permits.js:146` (`→ Closed`): same, defense-in-depth (input rows are Pending Closed so site 2 already cleared them — but lessons `:30`: *a guard applied in ONE pass consuming the same source must be applied in EVERY pass*; one line, zero cost).
**Sweep option ② is REJECTED as primary** (a nightly sweep is C3-as-a-step — treats the symptom on a schedule instead of closing the producers) **but the WARN row STAYS as the residual watcher** — if any fourth writer exists or appears, the row reddens the count instead of silence.

#### FENCES (Guardian pre-brief — the plan panel's Guardian must RULE, not rubber-stamp)
* **F1 `classify-permit-phase.js:10-12`** — never-clobber PRESERVED by construction (CASE returns `permits.enriched_status` on the Inspection branch); the invalidation is the OTHER HALF being written for the first time, knowingly. **In-commit: amend that comment to record both halves.**
* **F2** the loader's `last_seen_at` bump (`:386-399`) and the rest of the SET list — untouched.
* **F3** C2's status-scoped writers + C3's backfill — untouched (C3 becomes an emergency tool, not routine).
* **F4** the §4.9 pair's self-retire semantics — text amended, mechanism untouched.
* **F5** `close-stale-permits`' abort-gate (`:111` pending_closed_rate FAIL) and grace-day flow — untouched; only the SET lists gain a term.

#### OPEN QUESTIONS THE PLAN PANEL MUST GROUND (each with its query — the data-facts rule)
* **Q1 — writer completeness (query AMENDED by fold-validation #1 — the original spanned `scripts/ src/app/api/` and could NOT reproduce the fourth-writer finding):** `grep -rn "UPDATE permits" scripts/ src/ --include="*.ts" --include="*.js"` — ALL of `src/`, the fourth writer lives in `src/lib/sync/`. Census CLOSED at exactly 4 status writers (validated twice): loader `:357` · close-stale `:128`/`:146` · `process.ts:213-224` (`:279` is last_seen_at-only). Grounder reports ROWS, not counts.
* **Q2 — historical contribution split:** of the 1,416 cleared rows, how many moved via loader vs close-stale? Query: smeared-backup rows joined to `last_seen_at` age at backfill time (`SELECT (last_seen_at < '<lastLoad>') AS left_feed, count(*) FROM _backup_smeared_enriched_status_20260813 b JOIN permits p USING (permit_num, revision_num) GROUP BY 1`) — sizes the close-stale share; if ~0, sites 2/3 are cheap insurance and the plan says so honestly.
* **Q3 — the upsert's exact shape at `:357`:** does the DO UPDATE carry a WHERE write-guard (idempotency churn profile)? Read, don't assume.
* **Q4 — `load-permits.js` export surface for the direct-pool test:** what does `require()` expose (lessons `:64` says the guard exists) — can the test drive the upsert via the module, or does it seed+invoke via child process (C1 harness fallback)? Grounder reads and rules the test's invocation mode.

#### RED-FIRST TESTS — in THIS plan, reviewed BY THE PLAN PANEL before any fix (operator directive)
File: `src/tests/db/enriched-status-invalidation.db.test.ts` — direct-pool shape (model: `backfill-smeared-enriched-status.db.test.ts`), C4's opt-in `!pool` guard (throw under `BUILDO_TEST_DB/CI`, silent skip plain — the fold×environment lesson), `SPEC LINK: docs/specs/01-pipeline/44_chain_deep_scrapes.md §3` + `41_chain_permits.md`. Invocation mode per Q4 (module call preferred; child-process harness fallback).
* **Case A — loader move (RED now → GREEN after):** seed `status='Inspection'`, `enriched_status='Active Inspection'`; drive the loader upsert with the same PK at `status='Revision Issued'`; assert `enriched_status IS NULL`. Today it survives — RED at exactly this assertion.
* **Case B — never-clobber fence lock (GREEN now AND after — the survive-the-fix lock):** upsert with `status='Inspection'` unchanged → `enriched_status` PRESERVED byte-identical. **Locks F1 behaviorally for the first time ever** (today it holds only by the column's absence from the SET list — a shape, not a test).
* **Case C — close-stale move (RED now → GREEN after):** seed `status='Inspection'` + value + `last_seen_at` older than the cutoff; run close-stale's site-1 UPDATE (via the script per Q4, else the statement); assert status `'Pending Closed'` AND `enriched_status IS NULL`. Pins the second writer — the one prior passes missed.
* **Case D — NULL-status edge (pins `IS DISTINCT FROM`, the C3 test-1c precedent):** incoming `status = NULL` (2 live rows — `SELECT count(*) FROM permits WHERE status IS NULL`) → the CASE clears. RED under a `<>` implementation, GREEN under `IS DISTINCT FROM` — the only case distinguishing them.
* **Case E — idempotency/churn:** the same upsert twice → byte-identical row, no error (per Q3, assert the churn profile found).
* **Case F — the drift predicate reads 0** for every touched fixture row post-move (query-level: C3's predicate over the fixture set; **stated ceiling:** asserts the predicate, not a full `assert-global-coverage` run).
* Hygiene: prefix-scoped fixtures + cleanup in exception-safe `afterEach` + `afterAll` `pool.end()` (C4 panel folds inherited); every lock proven BOTH directions before red is claimed (today's envelope-lock lesson — the regex that never matched).

#### EXECUTION PLAN — WF2 verbatim (`.claude/workflows.md:67-95`)
- [ ] **State Verification:** Q1–Q4 (queries above; grounder reports rows).
- [ ] **Contract Definition:** N/A — no API route. Consumer contract restated: `computeStalled` sole logic consumer; invalidation produces only false→true-impossible transitions (no storm). `npm run typecheck` after.
- [ ] **Spec Update (FIRST, before tests/code — targets read directly this session, line-cited):**
  * **Spec 44 §3 `:100-118`** (the "POPULATION REGENERATES" block): amend to the C7 rule — *"every `permits.status` writer clears `enriched_status` when the new status is not `'Inspection'` (`load-permits.js` upsert CASE + both `close-stale-permits.js` sites); the §4.9 WARN pair watches residuals from any future writer"* · **correct the incomplete mechanism claim** (the block names ONLY the loader — `close-stale-permits.js:127-134`/`:145-152` is the second writer every prior pass missed) · **fix the stale citation `(:340, :357)` → `:357`** (grep: one site) · retire "until the loader invalidation rule lands / C3 must be re-run" in favor of "C3 is the emergency clear for residuals".
  * **⛔ Spec 41 `:129` is FACTUALLY WRONG TODAY and must be corrected, not just amended:** *"Stale permit closure — Permits not seen in 30+ days get `enriched_status` updated"* — the code updates `status` + `completed_date`, NEVER `enriched_status`. The spec documents C7's behavior as if it already existed (the lessons-`:37` class: spec text asserting behavior no code ever had — and plausibly the reason nobody looked for the second writer). Rewrite truthfully: status → `'Pending Closed'`/`'Closed'` + (post-C7) `enriched_status` cleared.
  * `classify-permit-phase.js:10-12` comment gains the other half (never-clobber + clear-on-move, both now deliberate).
  * `assert-global-coverage.js:1376-1397` threshold strings amended (rule landed; pair still self-retires at 0 — mechanism untouched).
  * Runbook C3 row: re-runs now EXCEPTIONAL, not routine. `npm run system-map`.
- [ ] **Schema Evolution:** N/A — Database Impact NO.
- [ ] **Guardrail Test:** the suite above — **panel-reviewed at PLAN altitude (this panel) AND red output re-reviewed before the fix lands.**
- [x] **Red Light: ✅ CAPTURED 2026-08-13 — `Tests 5 failed | 3 passed (8)`, the EXACT designed shape.** RED at designed assertions: **A** (`expected 'Active Inspection' to be null` — no CASE at `:357`) · **C** (both close-stale sites leave the value) · **C2** (`process.ts` UPDATE has no term) · **D** (NULL incoming status leaves it — the `IS DISTINCT FROM` discriminator's premise proven empirically) · **F** (drift predicate = 2 over its own moved fixtures). GREEN locks: **B sub-1** (genuine DO UPDATE w/ `EXCLUDED.status='Inspection'` preserves) · **B sub-2/E-main** (hash-guard skip, 0 rows) · **E-variant** (status-move payload twice, pass-2 = genuine 0-row skip). Plain mode: `2 skipped (0 tests)` clean. Typecheck 0. Files: `src/tests/db/enriched-status-invalidation.db.test.ts` (sites 1-3) + `sync-enriched-invalidation.db.test.ts` (site 4, env-before-dynamic-import, both-pools teardown).
- [ ] **Implementation (RE-STATED by fold-validation #2 — the "three terms, nothing else" scope is DEAD):** ① loader upsert CASE (`load-permits.js:357` SET list): `enriched_status = CASE WHEN EXCLUDED.status IS DISTINCT FROM 'Inspection' THEN NULL ELSE permits.enriched_status END` · ② `close-stale-permits.js:128` + ③ `:146`: `enriched_status = NULL` in the SET · ④ **`src/lib/sync/process.ts` UPDATE (parameterized, NO `EXCLUDED` scope — the loader CASE does NOT paste here; fold-validation #3 wrote the correct fourth expression):** `enriched_status = CASE WHEN $16 IS DISTINCT FROM 'Inspection' THEN NULL ELSE enriched_status END` (parameter reuse legal; NULL flows per Case D's rule; the clear is NOT `permit_history`-recorded — consistent with sites ①-③, stated asymmetry) · ⑤ FOUR `emitMeta` writes-declarations gain `enriched_status`: close-stale `:74-77` + `:117-120` + `:206-209` (all three, or the uniform-surface convention diverges) + loader `:703`; `process.ts` has none (app-side, resolution recorded) · ⑥ the doc/comment edits listed under Spec Update. Nothing else.
- [ ] **UI Regression Check:** N/A — no component touched.
- [x] **Pre-Review Self-Checklist (derived from amended Spec 44 §3, walked against the ACTUAL diff 2026-08-13):**
  1. Every `permits.status` writer carries the clear — 4/4 sites (loader CASE `:357+`, close-stale `:129`/`:148`, process.ts `$32`-CASE) — **PASS** (census closed at 4, validated twice)
  2. Never-clobber preserved — loader CASE ELSE returns `permits.enriched_status`; staying-Inspection byte-identical (Case B sub-1 locks it) — **PASS**
  3. `IS DISTINCT FROM`, never `<>` — both CASE sites; NULL incoming clears (Case D locks it) — **PASS**
  4. No hash perturbation — `enriched_status` absent from both hash functions (validated) — **PASS**
  5. emitMeta truthful — 4 declarations gained the column (3 close-stale uniform + loader); process.ts has none (recorded) — **PASS**
  6. Spec truth BEFORE code — Spec 44 §3 four-writer rule, Spec 41 `:129` fiction corrected, guard strings, runbook, C3 header, fence comment both-halves: all landed pre-test — **PASS**
  7. Red-first discipline — 5 red at designed assertions, 3 locks green-both, ALL 8 green post-fix; **Case C2 additionally caught a real SQL type-deduction defect (`$16` reuse → `inconsistent types`) that the panel + fold-validation + orchestrator had all approved on paper — fixed via dedicated `$32` param with the WHY commented** — **PASS**
  8. No trigger churn — status/enriched_status in no trigger column-list (validated) — **PASS**
  9. `last_scraped_at` untouched anywhere in the diff (R4 standing ruling) — **PASS**
  10. Deploy contract recorded — final C3 next UTC day, verify-drift-0 with statement_timeout, WARN-pair steady-state 0-per-run — **PASS**
- [x] **Multi-Agent Review (OUTPUT) — ✅ COMPLETE 2026-08-13, ALL SEATS PASS:** Gemini CLI + DeepSeek CLI — ZERO diff findings (both re-litigated whole-file history; all items pre-existing → FILED: Gemini's loader bundle incl. the `allMapped` full-dataset memory accumulation + first-non-empty-batch schema-check gap; DeepSeek's close-stale bundle incl. the DILUTED ABORT DENOMINATOR — `COUNT(*)` over all permits hides an active-permit outage — + `completed_date` overloading + no Step-2 guard + unbounded `abort_pct`; Gemini's first-run "critical enriched_status" verdict phrase did NOT reproduce on re-run and produced no line finding — CLI noise, overruled by the grounded agents). **Code Reviewer PASS** all 6 areas (32/32 param positional match; template-literal hazard clear; emitMeta uniform-surface adjudicated as the Spec 47 §R11 static-surface convention; both-pools teardown + transitive-import check sound). **Regression Guardian PASS** — all 5 fences hold; Case B sub-1 verified a GENUINE lock; the `$32` deviation blessed as a correctly-planted new fence. **⚠ Guardian catch (non-blocking, FILED + plan corrected): the "clear is NOT permit_history-recorded" claim was INVERTED** — `runSync`'s diff-of-union (`SELECT *` existing vs mapped-only fields, `toComparable(undefined)==='null'`) has been SPURIOUSLY recording `enriched_status → NULL` history rows all along on the Changed branch (pre-existing pollution hitting every existing-only non-null column via this path; false pre-C7, accidentally true on the clear branch post-C7, still false on the staying-Inspection branch). **Integration+RC seat: agent lost to a session limit; completed INLINE by the orchestrator** — trailing-space re-check: exactly ONE spelling `'Inspection'` (len 10, 128,436 rows) exists table-wide, no variant the byte-exact clear could wrongly hit · drift = 0 at review time (expected first WARN emission = tonight's loader, cleared by tomorrow's final C3) · pin-grep on the final diff was already Guardian-covered (CLEAN). **Reality-Check's standing output-altitude job remains for tomorrow: drift 0 on every chain_permits run post-final-C3.**
- [ ] **Green Light:** full battery; paste evidence → cherry-pick to main SAME SESSION (the arrival rule — no C1 repeat).

#### KNOWN RISKS
* Ingest hot-path: row-local CASE, no sibling reads (C0 ruling) — measured-risk ~zero; Q3 confirms the churn profile.
* A future FOURTH writer bypasses all three sites — accepted residual, watched by the WARN row (that is now its permanent job).
* Left-the-feed rows whose status never moves again keep NULL forever after close-stale — correct by the standing rule (their inspection basis is gone).

> ~~After the lean plan panel folds: PLAN LOCKED question to operator.~~ **Panel COMPLETE 2026-08-13 — see the ⚖ v2 FOLD below.**

#### ⚖ C7 PANEL VERDICTS + v2 FOLD (2026-08-13) — SUPERSEDES conflicting v1 text above
**Triple-blind convergence: Idempotency + Guardian + Integration each independently found Case B vacuous under the hash guard — the strongest signal this panel process has produced.**

**DESIGN AMENDMENTS (fold, all adopted):**
1. **FOUR sites, not three (Integration BLOCKING — a LIVE fourth writer exists TODAY):** `src/lib/sync/process.ts:214-224` — the legacy sync tool's changed-permit branch writes `status` (its own hash check passes status-only changes), exported via `runSync` to TWO live API routes (`/api/sync`, `/api/admin/sync`). **RULED: add the fourth term** (`enriched_status = CASE …` in that UPDATE's SET, same rule) — one line closes it; "documented acceptance" is how Spec 41's fiction happened. My scripts-only grep missed it; the census now spans `scripts/ + src/`.
2. **DEPLOY step added (Idempotency BLOCKING #1 — the stuck-interim-accretion collision):** none of the SET terms touches an already-smeared row whose status never moves again (loader gated by `WHERE data_hash IS DISTINCT FROM` `:380`; left-feed rows never reach the loader) → the interim accretion between the 08-13 backfill and C7's deploy is PERMANENTLY STUCK → WARN pair never self-retires, RC's green criterion unreachable, the amended runbook would forbid the fix. **Deploy order: C7 on main → ONE FINAL routine C3 `--confirm` run → verify drift 0** (mirrors runbook §3's `C2 → C3 → verify` rule; runbook row wording: "final routine run at C7 deploy; emergency-only thereafter; same-UTC-day re-run fails closed on the dated-backup collision — expected").
3. **Q2 ANSWERED with the honest split (RC):** of the 1,416 cleared rows — **loader-move 1,283 (90.6%) · close-stale 133 (9.4%)** (115 Pending Closed + 18 Closed; close-stale is the ONLY writer of those literals, grep-verified). The v1 "if ~0, cheap insurance" framing is RETRACTED — sites 2/3 are a **measured ~9% contributor**. v1's Q2 query was also WRONG as written (ambiguous `last_seen_at`, and `p.last_seen_at` was bumped by C3 itself — must read `b.last_seen_at`); RC used writer-exclusivity classification instead, which is sounder.
4. **Loader-CASE semantics stated precisely (Integration/Guardian):** the CASE executes ONLY when the hash guard passes (status IS hashed, `:186-187`/`:319` — every status move fires it ✓ Case A achievable); it does NOT retro-heal hash-unchanged residue — that stays C3/WARN-row territory. Spec 44 §3 amendment says **"clears on status MOVE"**, never "continuously re-asserts".
5. **F1 UPGRADED (Guardian archaeology):** the fence's FOUNDING commit (`c6bf9f1d`) explicitly expected the loader to move derived state on when status moved — C7 is a RETURN to founding intent; `f2724308`'s never-clobber bars ping-pong, structurally impossible under disjoint predicates. **Spec 41 `:129` fiction-from-birth git-proven** (arrived `2bdcb429`, 4 days post-script, zero `enriched_status` hits in close-stale's entire history — no hidden fence).
6. **In-commit additions:** the 3 stale `emitMeta` writes-declarations gain `enriched_status` (close-stale `:119`/`:208` + loader ~`:712` — Guardian F2… and now a 4th: `process.ts`'s equivalent if any) · the C3 tool's OWN header (`:13-24`) — the first document an operator reads when the WARN reddens (Idempotency #5) · `lifecycle-phase.ts` full path is `src/lib/classification/` (Integration cite fix).
7. **Steady-state PINNED (RC #3):** post-C7+final-C3, `enriched_status_status_scope_drift` reads **0 on EVERY `chain_permits` run** (both writers run steps 2-3, the guard reads step 30, same chain — synchronous) — a single nonzero IS a regression signal, not noise. The `_retighten` text tightens accordingly.
8. **Confirmed clean (no action):** trigger audit — `enriched_status`/`status` in NO trigger column-list, zero new churn · race analysis PASS and stronger than v1 (the scraper re-checks status INSIDE its UPDATE — no observable smear window at all) · site-3's justification upgraded: bounds any residual's life to ≤ grace days · NULL-status rows consistently handled · close-stale transitions are ledger-invisible (PRE-existing, mig-127 scope — do not let the output panel misfile it as a C7 regression).

**TESTS RESPECIFIED (all reviewers converged; supersedes v1's Cases A-F):**
* **Invocation ruling (Q4 SETTLED, Integration adjudicated over RC's misread; CORRECTED by fold-validation):** loader cases run the REAL upsert via the child-process **`--file <path>` mode** (argv parse `:76-77`, file block `:578-590`, CKAN-shaped JSON — full fidelity incl. the hash guard; fixture records MUST carry `CRITICAL_FIELDS` = PERMIT_NUM/STREET_NUM/STREET_NAME/RESIDENTIAL or the run aborts; no logic-var startup guard). `insertBatch` NOT exported — no module route, no new export needed for THESE cases. close-stale (NO `require.main` guard — `pipeline.run` fires at require; module route impossible): child-process with seeded `pipeline_runs` cutoff row + **BOTH Zod-required vars seeded: `stale_closure_abort_pct` (high) AND `pending_closed_grace_days`** (C1 snapshot/restore discipline); sibling-fixture hazard noted (table-wide — assert only own fixtures, `--no-file-parallelism`). **`process.ts` site (Case C2) — RECHARACTERIZED (fold-validation (a)/#4): a DB-INTEGRATION test, not a "fixture pool" unit** — `runSync(filePath)` takes NO pool; the connection is `src/lib/db/client.ts`'s module-load singleton resolved `POSTGRES_URL || DATABASE_URL || PG_*`. The test must (i) pin the env to the testcontainer AND explicitly clear/pin `POSTGRES_URL` (it takes precedence — the ambient-URL hazard with teeth: `runSync` INSERTs permits and DELETEs `permit_trades`) BEFORE a DYNAMIC import; (ii) provide a Toronto-CKAN-shaped temp JSON for `parsePermitsStream`; (iii) seed `sync_runs`/`trade_mapping_rules`/`trades`/`permit_type_classifications`/`permit_history`; (iv) tear down BOTH pools (its own + the client.ts singleton, or vitest hangs on the open handle — state the module-reset mechanism used).
* **Case A** (loader move, RED→GREEN): `--file` child, status change ⇒ hash change ⇒ CASE fires. 
* **Case B** (fence lock, GREEN-both) **— REDESIGNED per the triple-blind finding:** change a NON-status hashed field (e.g. description) so the DO UPDATE genuinely executes with `EXCLUDED.status='Inspection'` → value preserved. A second sub-case with identical payload pins the guard-skip path — labeled as proving the GUARD, not the fence.
* **Case C** (close-stale move, RED→GREEN): per the invocation ruling; both sites asserted.
* **Case C2 (NEW):** `process.ts` `runSync` status-move → cleared (RED→GREEN).
* **Case D** (NULL-status, `IS DISTINCT FROM` discriminator): unchanged — cloud premise re-confirmed (2 rows, both currently clean).
* **Case E** (idempotency) **— corrected (Idempotency #3):** byte-identical **modulo `last_seen_at` + `updated_at`** (the unconditional touch `:399` + unscoped mig-115 trigger fire regardless — asserting them is the incidental-failure trap); assert the second pass's DO UPDATE affects 0 rows; add a changed-hash/same-status variant for CASE-level idempotency.
* **Case F** (drift predicate 0 over fixtures): unchanged, ceiling stated.

**G1 — ✅ RESOLVED 2026-08-13 (dedicated grounder, all four hypotheses adjudicated with physical evidence): H4 — THE RECORD'S TIMELINE WAS WRONG, NOT THE GUARD.** The C3 backfill executed at **13:05:54Z** (its own `RUN_AT` stamped identically on all 1,416 rows; `updated_at` corroboration via the 13:10-13:14Z classify batches), 27 min BEFORE run 2770's TRUE drift=0 reading — **the guard's self-retire path, working, first production proof**. H1 refuted (blocks adjacent+unconditional in the permits branch; 194 audit rows intact) · H2 refuted (no try/catch; chain pools run statement_timeout 0) · H3 refuted (SUPABASE_DATABASE_URL lives ONLY in `.env`; both load orders = same cloud pooler; the local-supabase `PG_HOST=127.0.0.1:54322` second DB exonerated — no backup table, drift=5, no run row). **Consequences folded:** ① correct this record's backfill timestamp (13:05:54Z, not ~14:4x) — today's PERMITS chain asserts ran POST-backfill, which is why it finished green-class · ② the WARN branch has NEVER yet fired in production — do NOT cite run 2770 as proof it renders; **tomorrow's chain_permits (~8-60 re-smeared rows) is its first expected non-zero emission — EXPECTED, not a regression** (and post-C7+final-C3 it must read 0 per fold item 7) · ③ ops note for every future ad-hoc probe: pooler sessions default to a 2-min statement_timeout and the drift COUNT can exceed it — `SET statement_timeout` explicitly · ④ the guard C7 ships against needs NO fix.

**~~G1 original framing (superseded):~~ the drift-guard reading anomaly.**

#### ✅ FOLD-VALIDATION ROUND (2026-08-13, operator-directed "validate the fold — usually you get something wrong": 2 validators, 10 correction items, ALL FOLDED IN PLACE above)
**Grounder: FOLD ACCURATE + 3 corrections · Cross-read adversary: FOLD DEFECTS ×7 · nothing invalidates the four-site design.** Folded: Q1 query amended to span `src/` (the recorded query couldn't reproduce its own headline finding — the data-facts class) · Implementation + Red Light checklist steps RE-STATED in place, stale "three terms / nothing else" scope deleted · **the fourth site's parameterized CASE written** (`$16`-form; the EXCLUDED-form does not paste) · Case C2 recharacterized as a db-integration test (env-before-dynamic-import, `POSTGRES_URL` precedence hazard, both-pools teardown, fixture-table set) · emitMeta count corrected to FOUR (close-stale `:74-77` was missed; `process.ts` has none — recorded) · **Q2 split restated as a BAND: 94 provable left-feed smears (raw_json `STATUS='Inspection'`) + 39 loader-smeared-first ambiguous = close-stale share 6.6–9.4%, upper bound 133** · Case B/E overlap disambiguated: **E-variant = the changed-hash/same-status upsert run TWICE, second pass affects 0 rows** (distinct from B sub-1's single-pass preserve); B sub-2 and E-main share one identical-payload fixture flow, merged · **final-C3 timing rule: run on the NEXT UTC day after C7 lands** (same-day dated-backup collision fails closed by design) and the verify-drift-0 probe SETs `statement_timeout` explicitly (G1 ③ cross-ref) · Case C seeds BOTH Zod-required vars · **Domain ruling recorded:** Backend/Pipeline HOLDS for the fourth site (`src/lib/sync/` is pipeline logic behind API routes; zero UI/Expo consumers, grep-verified `mobile/src` — a ruling now, not an accident); output roster unchanged (pipeline 5 + Guardian + RC) · cite nits (`:319`→`:313`, loader emitMeta `:703`, guard path `scripts/quality/`) · `permit_history` asymmetry stated · confirmed clean: fold-4×fold-7 lifecycle walk (final C3 clears ALL survivors — its predicate is hash-independent), Case D survives the `--file` respec, no source-regex lock trips. Run 2770 (`permits:assert_global_coverage`, 08-13 13:32:35Z) emits the adjacent `lead_id` rows but NOT the drift row (⇒ its query read 0) while the same predicate read 1,408-1,416 at 12:01Z/13:3xZ/13:50Z on the printed cloud host — and +1,416 rows in minutes with no writer is physically impossible, so ONE of these is wrong: the guard's execution path (a hidden gate/function boundary my :1150-1420 scan missed), a swallowed query failure landing in `?? 0` ("could not check" masquerading as ZERO — this Phase's founding defect class, possibly shipped inside C3's own guard), or a measurement-channel divergence (two env files, two URLs?). **C7 ships against this guard — it must be explained before lock.** Grounder dispatched. Also filed from RC: the ORPHANED `chain_deep_scrapes` row from 08-12 still `running` (the timeout kill's known SIGTERM residue — terminalize + runbook note).

> **📌 2026-08-13 coa daily-run red — EXPLAINED, no action:** `chain_coa` = `completed_with_errors`, redded by `cross_check_active_inspection = 773 FAIL` — the chain ran 11:47Z, ~3h BEFORE C3 executed (14:4xZ). This is the LAST red from the pre-C3 smear; C1 behaved exactly as designed (all 16 steps ran, no halt). `cross_check_permit_issued = 67` is WARN-only, so the 8 post-C3 residual offenders will NOT red future runs. `chain_permits` reached `completed_with_warnings` — green-class, all 33 steps incl. `backup_db`. **Expect tomorrow: active_inspection ~0.**

### C5 — D3: chain completeness  *(LAST, per operator)* — **⚠ MERGED INTO PHASE B B2 (operator-directed 2026-08-14)**
> **C5 now lands WITH Phase B's B2 as ONE contract, not after it.** Reason: both edit the same `run-chain.js` metaObj region + `check-chain-verdict.js`, and their semantics interlock — a `deferred_to_full` chain is a run where `executed < expected` LEGITIMATELY, so C5's field set must account for B2's defer outcome or the second one to land hits drifted lines and a contradictory shape. The B0-item-7 grounder is mapping both contracts' insertion points on today's tree + ruling the one-contract field-set shape + the AD1×defer composition. **The "C5 lands last" ruling is PRESERVED in spirit: it is the last Phase A item, landing in the first Phase B commit that touches its files, with its first-run noise attributable to that single commit.** Execution record lives in `.cursor/phase_b_active_task_INPROGRESS.md` from here. The C5 design text below remains the binding spec for its half of the contract.
**Write** `metaObj.step_completeness = {expected, executed, died_at, skipped_gate, skipped_budget}` in `run-chain.js:645-661` (copy the `budget_stopped` shape; `:664-670` jsonb merge accepts it, no schema change).
**Enforce** via a third exported pure helper on `check-chain-verdict.js`: `classifyStepCompleteness(sc)`.
**ONLY `expected` + `died_at` are decision-relevant** — `failedStep` is set solely by genuine step failure, never by budget-stop, gate-skip, disabled-skip, or advisory-lock SKIP. **`executed`/`skipped_gate`/`skipped_budget` are INFORMATIONAL; the verdict function must not consume them.** (Wiring `executed.length !== expected.length` misclassifies every legitimate gate-skip as incomplete.)
**Contract:** consulted **only on rows already inside `OK_STATUSES`** (`check-chain-verdict.js:89` = `['completed','completed_with_warnings']`; everything else is already red via `classifyVerdict:99`). **Absent ≠ pass** — annotate-on-absent for one deploy cycle (legacy rows have no field), then fail-on-absent.
**The flip mechanism — RESOLVED by operator 2026-08-11: §4.9 self-announcing pair + follow-up commit.** The annotate phase IS a relaxation, so **Spec 48 §4.9 governs it** ("a relaxation must never be a silent, forgettable bypass" — `scripts/lib/accepted-baseline.js:19-21`). Emit the §4.9-shaped **pair**, modelled on `acceptedBaselineRows` (`:44-61`): ① the annotate row carrying the live absent-count every run, its `threshold` string naming what is suspended and why; ② a companion **`<metric>_retighten` INFO row** carrying the **machine-observable** flip condition (zero absent rows across a full cycle of all five scheduled chains). The flip itself is a **one-line follow-up commit, operator-triggered** — deliberately NOT self-executing: no date constant, no env flag, no deploy-stamp semantics (the panel never defined a stamp, and inventing one was rejected). The `_retighten` row is what makes forgetting impossible — it is in front of the operator in every audit run until the flip lands. **File the follow-up in `docs/reports/review_followups.md` in the C5 commit itself**, and do NOT let the annotate row self-retire silently: absent-count reaching zero is the *signal* to flip, not the flip.
**D3(iii):** `check-pipeline-freshness.js:244` `::notice` → `::error` only. It already gates the exit code (`:256-260`). **Do not weaken the race guard** (it lives in `pipeline-watchdog.yml:190-192`, not this script).
**Pre-deploy:** survey all five scheduled chains for existing early termination + a runbook pre-ack with an exit-criteria window.

---

## ✅ C1 RED-FIRST EVIDENCE — captured 2026-08-11T15:13:37, pre-fix code, testcontainer (empty DB + all 237 migrations)
`BUILDO_TEST_DB=1 npx vitest run src/tests/db/assert-lifecycle-phase-distribution-halt.db.test.ts --no-file-parallelism`
**Result: `Tests 1 failed | 1 passed (2)` — exactly the designed asymmetry.**

**Case A — RED, and red for the RIGHT reason** (the script threw; it did not fail on a missing import or a fixture error):
```
{"level":"ERROR","tag":"[assert-lifecycle-phase-distribution]","msg":"FAILURES",
 "context":{"failures":["3 permits have enriched_status=Stalled but lifecycle_stalled=false (exceeds 2 threshold)"]}}
Error: Distribution sanity check FAILED (1 failures):
    at ...assert-lifecycle-phase-distribution.js:837:13
: expected 1 to be +0   ← exit code 1, assertion at the test's :160
```
**`(1 failures)` is the proof of isolation** — the ONLY failure in the array was the `cross_check_*` (non-halting) class, and the class-blind throw at `:837` still killed the process. That is the outage, reproduced in a test.
The three assertions *before* the exit-code one all PASSED pre-fix: `PIPELINE_SUMMARY` present, `cross_check_stalled` row `FAIL`, verdict `FAIL` — so **`:794`'s verdict and `:803`'s emitSummary already behave correctly; C1 must change the HALT and nothing else.**

**Case B — PASSED pre-fix** (`unclassified_count` over the hard limit halts today). It must **still pass post-fix**; if it ever goes green-to-red or the exit code becomes 0, C1 was built per-SCRIPT and the hard limit is defanged.

**Bonus fence-proof:** Case B's `summaryEmitted` assertion passing on a *halting* run behaviourally re-erects the exact contract retired from `infra.test.ts:457-468` (emitSummary precedes the throw). The deletion is now defended, not merely asserted.

**Also verified incidentally:** the three E.5 posture flags are genuinely the promote-to-FAIL switches — 116 per-seq band violations were emitted as WARN-only (`0 FAIL, 116 WARN`) with the flags at 0, so band-class failures are isolatable exactly as the plan claims.

### Test panel — CLI round (Gemini + DeepSeek), folded; re-verified 15:21:36 → same `1 failed | 1 passed`
Both models converged independently on two defects in the FIRST draft; both are closed:
* **Gemini CRITICAL / DeepSeek MED — Case B passed on ANY non-zero exit.** A syntax error or a dead connection would have masqueraded as a working halt, in the one assertion the plan leans on. Now the halt is pinned to its CAUSE (`stderr` must name `unclassified_count 3 exceeds hard limit 0` and must NOT name the cross-check) before the exit code is read.
* **Gemini HIGH / DeepSeek HIGH — `logic_variables` is GLOBAL testcontainer state** and the suite left `999_999` sentinels behind for every later file. Now snapshot/restore around the suite.
* **DeepSeek MED (the round's strongest) — assert the EXACT set of FAIL metrics**, since `QUIET` can only suppress classes known at authoring time. Both cases now assert `failMetrics` equals exactly one metric. **This assertion PASSES today**, which upgrades the plan's isolation claim from asserted to *measured*: `cross_check_stalled` really is the only FAIL row in Case A.
* **DeepSeek MED — `spawnSync` blocks the event loop**, so vitest's per-test timeout can NEVER fire on a hung child. Child now carries its own `timeout` + `maxBuffer` + an `r.error` assertion.
* **REFUTED against the code — DeepSeek's `PG_HOST`→`PGHOST` "typo".** `pipeline.js:99-112` branches on `PG_HOST` FIRST, falling back to `SUPABASE_DATABASE_URL`. The underscored names are the SAFETY mechanism: without `PG_HOST`, a `SUPABASE_DATABASE_URL` inherited from `.env` would aim this child — which seeds rows, deletes rows, and rewrites `logic_variables` — at a REAL database. Rationale written into the test file so it is not "fixed" later.

### Test panel — Regression Guardian: **PASS, no undefended fence** (2026-08-11)
* **Range `:457-468` CONFIRMED by inspection.** `:456` blank, `:457` the `it(...)` opener, `:468` the closing `});`, `:469` blank. Deleting it leaves two adjacent blank lines and NO dangling brace. v3's original `:456-467` would have orphaned `:468`.
* **The block is a DOCUMENTED fence with a dated, severity-tagged origin** — not an incidental test. `closed_task_phase_e5_per_kind_posture_gate.md` (Complete 2026-05-16, 4-reviewer round) finding **`v2-Obs-E-seq`, Observability HIGH**: if the throw fires before `emitSummary`, the observer sees a FAILED step with **no PIPELINE_SUMMARY and therefore no audit rows to extract**. `review_followups.md:209` tags it **"REAL BUG FIX"**; `:1525/:1528` name this very test as the enforcement mechanism.
* **It asserts THREE behaviours, not one** (the plan's fence statement named only the third):
  1. `emitSummary(` exists → **covered**, behaviourally, by both new cases' `summaryEmitted`.
  2. The **shape** `if (failures.length > 0) {…throw}` → **knowingly retired**; it is *supposed* to stop matching, which is exactly why amending instead of deleting would have forced faking the dead shape to stay green.
  3. **Source-order emit-before-throw** — the load-bearing one → **covered more strongly than before**: Case B proves it at RUNTIME on an actually-halting child (`summaryEmitted` AND non-zero exit together), where the old test only compared text offsets. A future `async` emit would fool the regex; it cannot fool Case B.
* **Repo-wide grep: NO other test pins the old shape.** `chain.logic.test.ts:1610` is a **comment-only** reference (it tests `assert-entity-tracing.js`, not this script) — so the plan's "Amend" action there is a comment-accuracy fix, not a behavioural dependency. **Re-characterize it in C1; do not expect a test to break.** Six other files referencing this script (advisory-lock, SPEC-LINK scans, coercion) are all orthogonal.
* **Guardian's own stated gap:** it had no Bash tool, so no literal `git blame`. It substituted the closed-task + followups records, which carry the severity tag a bare blame line would not have. Accepted as stronger, not weaker — but the git-level corroboration is formally missing.

### Test panel — Observability: **PASS, all 4 items, no findings ≥80 confidence** (2026-08-11)
**C1's central premise is now VERIFIED end-to-end, not assumed** — "the audit stays FAIL while the chain stops dying" survives every hop:
`:794` verdict unchanged (still counts all 7 classes) → `pipeline.js:401-409` escalate-only recompute cannot downgrade FAIL→anything → **`run-chain.js:511-514` extracts `audit_table.verdict` into `stepVerdicts` on the SUCCESS path too — the extraction is NOT gated on exit code** (this is the single load-bearing fact C1 rests on) → `:630/:636` `hasVerdictFails` → `completed_with_errors` → `check-chain-verdict.js:89` `OK_STATUSES` excludes it. **Two independent mechanisms** red the chain (status-outside-allowlist AND the `step_verdicts` scan). The FAIL cannot be swallowed.
* **No new audit row needed for halted-vs-continued.** Spec 48 was grepped in full: zero "halt"/`died_at` requirements. The signal already exists at chain level and C1 touches none of it — step `pipeline_runs.status` `completed` vs `failed`, chain `completed_with_errors` vs `failed`, `chainError` null vs `"Stopped at step: <slug>"`. Spec 79 §2 already treats `exit_code == 0` and `verdict = PASS` as two SEPARATE checked facts, i.e. it expects exactly the divergence C1 introduces.
* **The test targets the real consumer channel** — `runScript()`'s last-`PIPELINE_SUMMARY:` parse is byte-for-byte `run-chain.js:497-514`'s own convention. Case B's stderr assertion correctly traces to `pipeline.js:184-195` `log.error()` writing `err.message` to stderr before the rethrow.
* **QUIET's isolation independently re-verified line-by-line** against `:130-132`, `:616`, `:641-642`, `:661-662`, `:786-791` — no unintended overlap for either fixture.
* **§11 / producer-consumer: zero surface.** C1 is control-flow only; no counter, no `emitMeta` read/write, no `records_meta` key shape changes.
* **Sub-threshold observation FOLDED:** Case B pinned the literal `unclassified_count 3 exceeds hard limit 0`, but the testcontainer is shared across every `db.test.ts` and `beforeEach` clears only FX-prefixed rows — a sibling file leaving a NULL-`lifecycle_phase` live-status permit would push the count past 3 and red the assertion for an unrelated reason. Now matched as `/unclassified_count \d+ exceeds hard limit 0/` — cause pinned, count-agnostic.

### Test panel — Code Reviewer: **1 Important finding, FOLDED** (the round's most valuable — three other panelists missed it)
* **Case B pinned only 1 of the 4 halting sites.** `QUIET` forces all three E.5 posture flags to 0 and never arms them, so `:385/:410/:439` were exercised in NEITHER direction. **An implementation that wires `:549` correctly but drops or mis-copies one of the three E.5 branches would have passed Cases A and B unchanged** — in the file this plan calls THE authoritative proof. The header also claimed it defended "the E.5 band contract" while exercising none of it.
* **FOLD (both halves of the suggested fix, not just the cheap one):**
  * **Added Case C** — arms `lifecycle_seq_band_promote_to_fail_band_violation: 1`, asserts halt cause-pinned to `/outside expected band/` and NOT to the other two classes. Covers `:385`. **Verified: passes (halts) on today's code at 15:27:21.**
  * **Wrote the honest ceiling into the file header:** `:410 no_band_configured` and `:439 expected_data_missing` have their own separate posture flags, are still NOT exercised, and **ship review-verified only** until the deferred four-quadrant `logic.test.ts` lands. Stated as a limitation, not papered over.
* **Finding 2 folded:** the header cited Spec 47 **§R1**, which is "SDK imports" — wrong section for a claim about the halting contract. Citation **dropped** (the claim stands alone) rather than swapped, per the plan's own precedent that a wrong citation is a defect.
* Its two sub-threshold notes were already closed independently: the shared-container exact-count fragility (folded from Observability) and the missing mixed halting+non-halting case (scoped out by this plan's TEST DISCIPLINE).

**Current red-first state, 15:27:21 — `1 failed | 2 passed (3)`:** Case A RED (exit 1 at the exit-code assertion, all preceding assertions pass) · Case B GREEN (halts, cause-pinned) · Case C GREEN (halts, cause-pinned).

### Test panel — Integration: items 1-5 CONFIRMED, **item 6 REFUTED** (2026-08-11)
* **CONFIRMED and load-bearing:** `run-chain.js:484-485` detects a failed step by **EXIT CODE ONLY** — stdout is never consulted for the halt decision, while `PIPELINE_SUMMARY` is parsed on BOTH paths (`:497-509` success, `:578-591` failure), last-match, exactly as the test does. **Case A's premise — exit 0 but still red — is sound at the source.** Also confirmed: exactly 7 push sites in 3 classes, no 8th; all three E.5 flags gate all band paths with no unguarded route; all 9 seeded keys real and consumed; `'Under Review'` not in `DEAD_STATUS_ARRAY`; `lifecycle_stalled` defaults false (mig 085:51) and the cross-check uses `IS NOT TRUE`.
* **REFUTED — the test-DB isolation premise.** `setup-testcontainer.ts:41-46` returns early on an ambient `DATABASE_URL` **before** reading `BUILDO_TEST_DB` (:49), and `dbAvailable()` keys on `DATABASE_URL` alone. Since `vitest.config.ts` matches `db.test.ts`, **plain `npm run test` — which Husky runs pre-commit — could have executed this file against an exported `DATABASE_URL`**, and the 9 `logic_variables` UPSERTs restore only on a CLEAN exit (a Ctrl-C strands `lifecycle_unclassified_max = 0` → reds the next real chain_permits).
  **FOLDED (point fix):** hard guard — requires `BUILDO_TEST_DB=1 || CI=true` **AND** a loopback host, both checked before the first write. **FILED (repo-wide fix):** `review_followups.md` `[HIGH · Integration]` — the early-return affects EVERY `db.test.ts`; central fix is to reorder the opt-in check ahead of the `DATABASE_URL` branch. Deliberately not folded into Phase A (different file, predates the diff).
* **Two corrections folded:** (a) a typo'd logic-var key falls back to `FALLBACK_LOGIC_VARS` from `scripts/seeds/logic_variables.json` (`config-loader.js:63-66,85`) — the registry JSON, NOT the migration, is the fallback authority; (b) `'lifecycle_stalled=false'` appears in BOTH the FAIL (`:624`) and WARN (`:626`) branches and `log.warn` writes to stderr, so the negative assertion is now `/exceeds \d+ threshold/`, which only the FAIL branch emits.
* Registry `min`/`max` bounds are NOT enforced by `validateLogicVars` or any DB CHECK, so the `999_999` sentinels load — confirmed, no action.

## ✅ C1 IMPLEMENTED + VERIFIED — 15:37:02, fresh container (237 migrations), **`3 passed (3)`**
**Case A GREEN** (exit 0, verdict still FAIL) · **Case B GREEN** (still halts, cause-pinned) · **Case C GREEN** (still halts, cause-pinned). The outage is closed and both halting classes are provably intact.
Then `250 passed` across `assert-lifecycle-phase-distribution.{logic,infra}.test.ts` + `chain.logic.test.ts`; `npm run typecheck` clean; eslint clean on all four changed files.

**What landed:**
1. `assert-lifecycle-phase-distribution.js` — `haltingFailures[]` added; the 4 halting sites push to BOTH arrays; the 3 `cross_check_*` sites unchanged (push to `failures` only); `:794` verdict UNCHANGED.
2. **`classifyHaltDecision({unclassifiedCount, unclassifiedMax, seqBandsFailing})`** — pure, exported, and used AS the throw condition, so the decision has ONE source of truth; `haltingFailures` carries only the message text. The two agree by construction (`haltingFailures` non-empty ⟺ `seqBandsFailing > 0 || unclassifiedCount > unclassifiedMax`). Operator-approved resolution of the plan's `:62`-vs-`:66` shape ambiguity.
3. `if (require.main === module)` guard wrapping the CLI body (precedent `audit-fk-orphans.js:307`) — **verified inert**: requiring the module returns `['classifyHaltDecision']` and starts no pipeline run.
4. **AD1** — `run-chain.js:225` now treats `completed_with_errors` as a failed predecessor. Without it, C1's new terminal status would have silently re-enabled gate-skip and skipped the backlog a red run left behind. Sole consumer `:565`.
5. `infra.test.ts:457-468` DELETED with the three-part fence recorded in place of the block.
6. `chain.logic.test.ts:1610` comment corrected (Guardian: comment-only, no behavioural dependency).
7. NEW `assert-lifecycle-phase-distribution.logic.test.ts` — 8 tests: four quadrants + the `>` vs `>=` boundary against the audit row's PASS line + the contract that `cross_check_*` is not even a parameter + import purity.

## TEST DISCIPLINE (operator-directed — non-negotiable)
Red first, failing output pasted into this file as evidence; each test fails for the **right reason**; **tests go to panel review before the fix lands**; no source-regex where behaviour is testable (and where forced, state what it cannot catch); `SPEC LINK` headers.

* **C1 red-first (the authoritative proof) —** `src/tests/db/assert-lifecycle-phase-distribution-halt.db.test.ts`, `BUILDO_TEST_DB=1`, spawning the **real script as a child process against today's code, no refactor**:
  * **Case A** — cross-check-only FAIL: **red now** (throws); green after (exit 0 **and** `audit_table.verdict==='FAIL'`).
  * **Case B** — `unclassified_count` over hard limit: **red now AND red after.** *This is the single most important assertion in the plan* — a naive per-script "drop the throw" flips it green and silently defangs the hard limit. Nothing else pins R1.
  * A pure-function test **cannot** be the red-first proof (the export doesn't exist pre-fix → fails on a missing import = the incidental-failure trap). Add `assert-lifecycle-phase-distribution.logic.test.ts` for the four quadrants **as a post-refactor lock**.
* **C2 red-first —** `scripts/tests/test_scraper_revision_scoping.py` using a `FakeConn/FakeCursor` capturing `(sql, params)` — precedent `test_scrape_outcome_persistence.py:86-104` (`StampConn`) and `test_scraper_egress.py:108-203`; `pytest-asyncio` already installed. Drives the real `scrape_year_sequence`. **Red for the right reason:** captured SQL genuinely reads `WHERE permit_num = %s`. Plus a **negative lock** that `last_scraped_at` is NOT revision-scoped (R4 is a negative ruling; nothing pins it today).
  * **Stated ceiling:** a FakeCursor **cannot** catch `= '00'` vs `MIN(...)` — that is semantic, and no live-DB Python harness exists (building one is out of scope). **C3's `--confirm` dry-run row count is the empirical proof** that closes this gap.
* **C3 — ⚠ REWRITTEN 2026-08-12.** ~~red-first means proving the `'0'`/`'00'` split as a *data fact*~~ — that framing is DEAD with the base-row model. **Red-first now means proving the smear as a data fact:** a row with `status <> 'Inspection'` carrying a non-NULL `enriched_status` exists (1,350 measured), and it is exactly what `assert-lifecycle-phase-distribution`'s cross-checks count. Do not overclaim red-first for a from-scratch mutation script; test green for **confirm-gating** (no `--confirm` ⇒ zero writes), **correction**, **replay-to-`UPDATE 0`**, and the **negative lock that `last_scraped_at` is never touched** (nothing else pins that, and it is the brake that keeps the backfill safe).
* **C4 — ONE layer** (scope ruling above): the db child-process layer only, Case A/B shaped like C1, with the 3 exception-derived sites asserted **still fatal**. **State the ceiling in the test file:** with no exported helper there is no pure-function lock, so a future refactor that re-merges the two arrays is caught only if it changes observable exit-code/verdict behaviour — a same-behaviour re-merge would pass. That is the accepted cost of the ruling; record it, do not paper over it.
* **C5 —** pure-helper unit tests: early death · gate-skip · budget-stop · advisory-lock SKIP · absent field. Here "the export doesn't exist" **is** the diagnostic defect (net-new capability) — state that in the file rather than papering over it.

## Spec + doc amendments (IN-COMMIT — corrected citations)
| Target | Change |
|---|---|
| **Spec 42 `:41`** | "Throws on failure (halting)." → halting only for E.5 + `unclassified_count`; `cross_check_*` non-halting |
| **Spec 41 `:185`, `:188-190`** (**not `:69`** — no halting text there) | three `FAIL (halting)` rows → `FAIL (non-halting)` for cross-checks; **add the two undocumented cross-check rows**; fix step-22→25 drift **and** `:192` step-30→31 |
| **Spec 84 `### 8. Distribution Health Bands` `:1168`** — **"§3.4" IS A PHANTOM SECTION** (8 code/seed/migration citations point at a heading that does not exist) | add `id="3-4"` anchor + note the alias; amend `:1179-1181`; **fix `:321` stale pointer `lifecycle-phase.js:160-213` → `:342`**; **add `## Known Failure Modes`** (file has none) |
| **Spec 44 §3** | ⛔ **THIS ROW IS DEAD — DO NOT ACTION IT (Compliance F3, 2026-08-12).** ~~`enriched_status` base-row-only rule~~ was retracted by the C0 panel and **the opposite shipped** in `eff28a7e` (status-scoping); Spec 44 §3 now carries `MIN(revision_num)` as an explicit REJECTED ALTERNATIVE. ~~Add `classify-inspection-status.js` to §6 Target Files~~ — **already done**, `44:335`. **The live instruction is the C3-local table below, which supersedes this row.** Only the `permit_scrape_outcomes` clause survives (do NOT harmonize — `:118` records permit-grain as a *knowing* decision). |
| **Spec 115 §2.5** (**not §2.4** — that is deep-scrapes; §2.2 is failure isolation) | watchdog/RAN semantics + race-guard interaction |
| **Spec 112 §6 + §9** (§9 **already exists** at `:446` — amend, don't create) | the halt × race-guard interaction that produced the 28h gap |
| **Spec 48 §3.7** | `records_meta.step_completeness` producer/consumer contract (C5) |
| **Spec 79 §5 / Spec 42 §6.11.1** | canonical shared-step halting posture, if recorded there |
| `docs/runbook/README.md` | index the backfill **+ the `C2 → C3 → verify` deploy-ordering rule** |
| `tasks/lessons.md` | **"an assert must distinguish 'the data is wrong' from 'I could not check'"** + ⛔ ~~the revision-grain class~~ **— REMOVED (Compliance F1, BLOCKING). The revision-grain model is RETRACTED; writing it into the file read at EVERY session start (`CLAUDE.md:36`) would durably record a refuted model.** Replace with **the status-grain lesson**: *derived state (`enriched_status`) is scoped by the row's OWN status, never by a canonical/base row — the writer must use the same predicate as the reader that selected it.* |
| Also | `npm run system-map` (new file) · `npm run lineage-docs` check |
| **Verified NOT needing edits** | migration 150, `seeds/logic_variables.json:2744/2751/2758`, `logic-variables-registry.md` — all say "→ pipeline halt", which **stays true** under per-failure classification |

## Known risks
* C1 must be **per-failure**; per-script silently retires the E.5 contract and the `unclassified_count` hard limit. Case B is the guard.
* **Post-C1 the aggregate watchdog goes silent on this condition by design** (`completed_with_errors ∈ RAN_STATUSES`), leaving the per-chain verdict step as the single channel. Accepted, stated.
* **First-green night:** 10 steps run for the first time since 8/10 → one-off push/CRM burst. Pre-ack.
* **C5 will surface other chains terminating early** — landed last, with its own survey, so noise is attributable.
* **Threshold re-ranging is a required follow-up** (post-fix the metric lands ~7-28 against 500 — blind for years). Filed, not in scope.

> **PLAN LOCKED. Do you authorize this WF3 plan? (y/n)**
> §11 note: Database Impact **NO**. Five commits, D3 last. `assert-engine-health` excluded from the sweep (its throw is infrastructure-fatal; converting it would green a dead DB). Backfill touches `enriched_status` only — nulling `last_scraped_at` would re-queue ~577 permits.

---

# WF3 (2026-08-15): deep-scrapes final fix — `refresh_snapshot` pathology + the missing envelope layers
**Status: ✅ CLOSED (2026-08-16) — all six findings (F1–F6) implemented, across 7 commits (F1, F2, F3, F4, F5, F6, F2-followup). Landing is SPLIT by design — see "Landing Discipline — branch-only by design" at the end of this file. F1 (the root-cause fix) is on `origin/main`; F2/F3/F2-followup are branch-only pending the B7/B8 cloud sequence (migrations 240, 242, 243 not yet applied on cloud). The Spec 118 §7.7 Monday interim decision (planner override / pull step from manifest / accept failure) is now MOOT — F1 landing on main resolved the pathology directly, no interim workaround needed.**
**Domain Mode: Backend/Pipeline. Database Impact: NO** (query + workflow + run-chain changes; no migration — unless the panel rules a partial index in F1, which re-opens it). **Severity: HIGH** (chain red 3 consecutive slots; next slot Monday 15:00Z).
**Rollback anchor: `6a7d2241` (branch) / `766edbd1` (main).**

## Grounded premise (Spec 118 §1 — executed, not asserted)
`refresh_snapshot` 3 min → 64 min (quiet-DB measured); its stats query index-fetches 187,187 rows = 73% of permits via `idx_permits_status`; heap correlation destroyed by the week's mass updates; same plan, random I/O. NOT bloat (2,310 dead), NOT locks (wait_event = IO:DataFileRead), NOT a plan flip.

## Commit order (per-finding cadence)
**F1 — the query fix (root cause).** The stats query must stop fetching 73% of the table through an index. Candidate shapes FOR THE GROUNDER TO MEASURE (EXPLAIN on cloud, read-only — costs + expected I/O pattern; the plan adopts the winner, never a guess): (a) force-seq shape (rewrite so the planner chooses seq scan: drop the status IN-list into a HAVING/CASE aggregation over one pass); (b) split per-status indexed COUNTs (5 small index-only probes IF a covering index exists — check `idx_permits_status` includes/visibility); (c) pre-aggregate from `pg_class`-cheap sources or an existing MV; (d) partial index tailored to the 5-status set + covering columns (Database Impact flips YES). Ship the measured winner.
**F2 — layer 3: per-step ceilings in run-chain** (Spec 118 §3's named gap). Manifest-configurable `step_timeout_minutes` per step (default none); run-chain enforces via child-spawn timeout; a ceiling kill marks the step `failed` with a `step_timeout` reason in records_meta (NOT the platform axe — the row terminalizes cleanly, C1 semantics decide chain fate). ⚠ B2 moved run-chain lines AGAIN (+~100) — the grounder re-maps insertion points; do NOT trust Spec 118's or B2's numbers.
**F3 — the duration-trend tripwire** (the instrument whose absence cost two failure days). In `check-chain-verdict.js` (it already reads pipeline_runs): per step of the just-finished chain, compare duration to the trailing median of the last N completed runs of that step; WARN ≥3×, FAIL ≥10× (audit-row shaped, non-halting, Spec 30 §5.4.1-compliant by construction — threshold-derived + represented + agreeing + reachable). Backstops EVERY chain, not just deep-scrapes.
**F4 — layer 2 for deep-scrapes:** `CHAIN_TIME_BUDGET_MINUTES` env in chain-deep-scrapes.yml (140) — boundary-stop coverage (stated limit: cannot preempt mid-step; that is F2's job).
**F5 — watchdog cron shift** to ~18:45Z (Spec 118 §2's two-red geometry) + window re-derivation in the SAME commit (lessons:100).
**F6 — `early_abort` connection-hygiene audit** (Spec 118 §7.6): trace the scraper's abort path for uncommitted txns/lingering backends; investigation-first (WF3 premise verification), fix only what's proven.

## RED-FIRST TESTS (in-plan; per §11.3 every lock proven both directions)
* F1: `refresh-snapshot` gains an exported stats-query builder; logic test pins the ADOPTED shape (source lock, ceiling stated) + db test asserts value-equivalence old-vs-new on a seeded fixture (same counts out) — RED pre-swap on the shape lock, GREEN-both on equivalence.
* F2: pure test on the ceiling plumbing (manifest → spawn timeout arg); db child test: a step script that sleeps > ceiling → step row `failed` with `step_timeout` reason, chain continues per C1 classification — RED (no mechanism).
* F3: pure `classifyDurationTrend(history, current)` — 3×/10× boundaries, insufficient-history arm (annotate, never red), median-not-mean lock — RED (export absent, the ⓔ diagnostic).
* F4: workflow infra lock — env present with value ≥120 — RED today.
* F5: workflow infra lock — watchdog cron ≥ 18:30Z — RED today; window assertions updated same commit.
* F6: premise tests only after the investigation reports (no speculative fixtures).

## KNOWN CONSTRAINTS
B2's uncommitted... COMMITTED (`e8793c8f`) run-chain/verdict shapes are the CURRENT tree — F2/F3 build on them and must not trip B2's new locks (grounder enumerates). ~~The interim operator decision (Spec 118 §7.7 a/b/c) is INDEPENDENT of this WF3 and still open — (b) recommended for Monday if this WF3 hasn't landed by then.~~ **MOOT as of 2026-08-16: F1 (the root-cause query-battery fix) is on `origin/main` (`a4dcdf09`) — the Monday 15:00Z interim decision no longer applies; nothing to choose between (a)/(b)/(c) for.**

## ⚖ WF3 GROUNDING FOLDED (2026-08-15 — full report in the grounding agent output; headline corrections)
* **F1 CORRECTED — it's a BATTERY, not one query**: refresh-snapshot has NO MV; ~5 of its ~25 stats queries share the pathological 5-status index-fetch shape. Candidate (a) BACKFIRES for DISTINCT aggregates (pkey random scan, corr 0.317); hashed-subplan shapes REFUTED (cost 10.2e9); index-only limited (single-col index + visibility collapses daily post-scraper). **MEASURED WINNER: ① consolidate the ~10 scalar permits queries into ONE no-WHERE single-pass FILTER aggregation (cost 153,220, deterministic Parallel Seq Scan) ② tags queries one-pass with `(status IN …) IS TRUE` index-defeat ③ tradeByType keeps join shape under session-scoped `SET enable_indexscan=off`+RESET (Bitmap Heap, correlation-immune)**. Expected: ~3 heap-order passes ≈ the historical 3-min class. Partial covering index speced (~20-30MB) but NOT primary. Planner costs rank the bad plan cheapest (stale correlation 0.586) — decisions rest on I/O pattern, stated in-plan.
* **F2**: spawn site `:531-570` (`:535`), NO timeout of any kind exists; a killed child lands in the `:680-735` catch which already builds failMeta → **but that path HALTS the chain (sets failedStep)** — ⚠ DESIGN DECISION REQUIRED: ceiling-kill = halting (current catch) or C1-style non-halting? RECOMMEND: halting (a ceiling kill means "I could not run this step" = exception-class per Spec 30 §5.4.1 criterion 1) with the reason string in failMeta. `step_timeout_minutes` slots in manifest.scripts. B2-lock no-trip list enumerated (budget regex 1200-char window, ladder literals, chain_gates keys, LIMIT 2, no `ORDER BY started_at DESC LIMIT 1` text in verdict file).
* **F3**: helper beside `:129-142`, exports `:404-411`; per-step LIMIT-7 history probes cost ~9.7 each — negligible; executed-list source = `step_completeness.executed`.
* **F5**: watchdog cron is `30 15 * * *` daily (yml:34; the "commented out" header comment is STALE — fix it); windows 25h/`{sources 204, entities 26}`/deep Mon 80h Tue-Fri 30h, exact-pinned w/ 15:30-rationale comments — move cron to 18:45Z + re-derive windows+pins SAME commit; lastIndexOf pins constrain step ordering (no new step after the migration gate).
* **F6 SUSPICION REFUTED by code**: abort path hygiene CLEAN (per-permit commit/rollback, dedicated autocommit ledger conn, finally-close). TWO REAL adjacent defects found instead → F6 becomes: (a) early_abort doesn't stop the outer claim loop (:2858) — decide intended semantics; (b) **never-attempted year_seqs marked `completed` on abort (:3059-3065) — the C2-era filed defect, now line-confirmed; fix in F6**.
* **Ops**: 3 orphaned `refresh_snapshot` step rows (08-12/13/14) → terminalized 2026-08-15.
**Next per §11.2: fold-validation pair on THIS fold, then operator y/n.**

## ✅ JOINT FOLD-VALIDATION (2026-08-15) — WF3 corrections FOLDED (verdict: FOLD DEFECTS → all resolved here)
* **W5 SETTLED — F2 = HALTING.** A ceiling kill is "I could not run this step" (§5.4.1 criterion 1, exception-class) → the existing `:680-735` catch path (failedStep, chain halts) IS the design; minimal diff, no ladder edit, all B2 locks untouched. **The F2 red case is REWRITTEN: ceiling-killed child → step row `failed` with `step_timeout` reason in failMeta → CHAIN HALTS (exit 1).** (Asymmetry recorded: the non-halting arm would edit the ladder + trip ⑦b/budget/cascade locks — rejected.)
* W2: the pathological battery is **9 queries** (+1 FILTER-level), not ~5 — the three-part winner covers all 9 (validated complete against the real inventory).
* **W6 (footgun): F3 must NEVER produce `ORDER BY started_at DESC LIMIT 1<digit?>` text in check-chain-verdict.js** — the defer-suite ban at `:167` has no word boundary (LIMIT 10-19 also trip). F3 fetches current+history in ONE `LIMIT 7` probe; rows[0] = current.
* **W8 SETTLED — F3's carrier is the file's own precedent:** GH annotations + exit code (this CLI emits no audit rows). Trend ≥3× → `::warning`; ≥10× → `::error` + exit 1. §5.4.1's "Represented" is satisfied by the annotation channel, stated as a deliberate deviation (the file is outside the Spec 47 skeleton).
* **T2: pipeline_runs fixture discipline (binds F1/F2 suites too):** FX-prefixed slugs, DELETE-by-prefix per suite (the B2 precedent `:45/:185`); any helper querying run history takes the slug set AS A PARAMETER so tests seed prefixed slugs.
* **Landing order (X5): WF3 F1→F6 per-finding commits FIRST (Monday clock) → B3 → B6**; F5 carries a one-line comment that the sources 204h window is pending B6's bi-weekly re-derivation.

---

## Landing Discipline — branch-only by design (recorded 2026-08-16)

**Why this section exists:** several commits below are deliberately NOT on `origin/main` despite being fully implemented, tested, and closed. That is a designed landing split, not unfinished work — recorded here explicitly so a session crash or a future session never mistakes "not on main" for "not done."

Landing split, verified with `git cherry main wf2/deep-scrapes-restore-l0` (`-` = patch-identical on main, `+` = branch-only):

### LANDED on `origin/main` (patch-identity verified)

| Item | Description | Branch SHA | Main SHA |
|---|---|---|---|
| WF3 F1 | `refresh_snapshot` query-battery pathology fix (root cause) | `8cc99c78` | `a4dcdf09` |
| WF3 F4 | chain soft time-budget (Layer 2) for deep_scrapes | `d6d0bab0` | `cad1017a` |
| WF3 F5 | watchdog cron shift to 18:45Z (closes the two-red geometry) | `ae4d8f91` | `89379f60` |
| WF3 F6 | `early_abort` claim-loop + queue-completion defects | `f91ad77b` | `52d0bc8e` |
| Spec 118 | execution envelope doc | — | `766edbd1` |
| Spec 119 | backend doctrine doc | — | `fcde4604` |

### BRANCH-ONLY BY DESIGN (must NOT be cherry-picked to main; land together at the B7/B8 cloud sequence)

| Item | Description | Branch SHA |
|---|---|---|
| WF3 F2 | `run-chain.js` per-step ceilings | `766424fe` |
| WF3 F3 | step-duration trend tripwire | `c856c093` |
| WF3 F2 followup | widen flaky 60ms kill-ceiling test margin | `539c40a7` |
| Phase B B1 | `source-version.js` lib + tier-2 content-hash gate | `0b230472` (+ docs followup `e279b2b0`) |
| Phase B B2 (1/n) | migration 240 — massing watermark + pass-3 scope | `912a640a` |
| Phase B B2+C5 | scope-defer mechanism + `step_completeness` one-contract | `e8793c8f` |
| Phase B B3 | run-ledger gate (link-wsib / link-parcel-addresses / compute-parcel-cost-estimates) + enrich-heritage #418 port + **migration 243** | `74653a8f` |

**Why this set is branch-only:** migrations **240, 242 and 243 are not applied on cloud** (243 is new — added by B3, `243_wsib_unlinked_partial_index.sql`; note **241 IS on `origin/main`** and must not be re-applied at B7). Nothing in this set may reach `origin/main` until the B7 deploy event applies them — landing any of it early would run branch code against a half-migrated cloud database. B7 is the single arrival point: migrations apply → one bi-weekly-shape green run → one quarterly-shape run → one simulated-escalation run. F2/F3/F2-followup ride along in this set by operator landing-order ruling even though they don't themselves touch a migration, because they build on B2's `run-chain.js`/verdict shapes (see `## KNOWN CONSTRAINTS` above) and are meant to arrive as one bundle with B2+C5.

**Operational gotcha worth recording (cost a session real time):** the local `main` ref goes stale while working a long-lived branch. Always `git fetch` and compare against `origin/main`, never local `main`, before concluding whether something was cherry-picked. Use `git cherry main <branch>` for patch-identity, not `git log`/`git branch --contains` against a stale local ref.

**Note on C1/C2 (Phase A, above):** `git cherry` shows **C1 and C2 as `+` (branch-only-looking)** against main even though both are in fact landed — they were amended during their own cherry-pick, so they are not byte-identical to their branch originals and therefore not patch-identical under `git cherry`. This is expected and must **not** be misread as "unlanded" when reconciling the B7/B8 merge — see §1/§6 of the C1 SESSION RECORD above for the actual landing evidence (`cdaea415` etc.), which is independent of `git cherry`'s patch-identity heuristic.
