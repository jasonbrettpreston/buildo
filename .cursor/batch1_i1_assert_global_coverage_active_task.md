# Active Task: WF2-C — C4 Batch 1 Step I1: `assert_global_coverage` → Spec 122 Step Standard
**Status:** Implementation (AUTHORIZED 2026-09-11 by operator; Asks A1/A2/A3 ruled = the plan's recommendations: tool-generated checks[] census adjudicated in PH-3; keep-and-correct the infra test; Spec 49 §2 fixed in commit 9)

**Domain Mode: Cross-Domain** (`.cursor/c4_batching_entry_active_task.md` Execution Plan §Sequence row **I1** declares Domain = Cross-Domain for I1–I5, not Backend/Pipeline — read `.claude/domain-crossdomain.md` Scenario A + `.claude/domain-admin.md` + `scripts/CLAUDE.md`, all three read). Trigger: the step's `audit_table.rows` are a declared consumer contract for the admin `FreshnessTimeline.tsx` renderer (Spec 49 §6 Cross-Spec Dependencies) — no admin file is edited here, but Scenario A's handoff-note obligation applies (§12 below).

## Context
* **Goal:** Convert `scripts/quality/assert-global-coverage.js` (slug `assert_global_coverage`) to the Spec 122 step standard — descriptor + `scripts/lib/compute/assert-global-coverage.js` + the frozen 8-line shell — behaviour-neutral, per Spec 123 §1.1/KFM 3. This is C4 batch 1's **first** step (`.cursor/c4_batching_entry_active_task.md` §3.2 order 1, Execution Plan row **I1**).
* **Target Spec (G0, system-map owner row FIRST):** `docs/specs/00-architecture/00_system_map.md:49` → **Spec 49** (`docs/specs/01-pipeline/49_data_completeness_profiling.md`) is the owning spec, infra test `src/tests/assert-global-coverage.infra.test.ts`. Cross-spec (SPEC LINK headers in the file itself, and this step's 3 chains): Spec 41 (`41_chain_permits.md`), Spec 42 (`42_chain_coa.md`), Spec 43 (`43_chain_sources.md`). Architecture specs: 122 (§1.10 ASSERT profile, §5.1/§5.3/§5.5, §8.1), 123 (§6 gates, §7 nine commits), 124 (§2 Rules, register R-C/R-D/R-K/R-K.1/R-AA/R-AB/R-AC).
* **Key Files:** `scripts/quality/assert-global-coverage.js` (1,464 L, to become the 8-line frozen shell) · new `scripts/quality/assert-global-coverage.descriptor.json` · new `scripts/lib/compute/assert-global-coverage.js` · `scripts/manifest.json` (no change expected — slug/file path stay) · `src/tests/steps/assert_global_coverage/violations.test.ts` (new, red-first) · `src/tests/assert-global-coverage.infra.test.ts` (927 L, existing — pre-conversion shape, disposition below) · `scripts/steps/_schema/converted.json` (`pending[]` then final registration) · `docs/specs/01-pipeline/49_data_completeness_profiling.md` (spec-diff obligation, §9 below).

---

## 0. Measured ground truth — every claim, its command, its value

| # | Claim | Command executed | Value |
|---|---|---|---|
| 1 | Current line count | `wc -l scripts/quality/assert-global-coverage.js` | **1,464** (churn-complexity report's `LOC=559` is at `window_end` ancestor SHA `39313d92`, not HEAD — file has ~grown 2.6× since; commits/branches columns below are the same stale-SHA caveat) |
| 2 | Advisory lock | grep `ADVISORY_LOCK_ID` + Spec 47 §A.5 registry | **111**, registered `docs/specs/01-pipeline/47_pipeline_script_protocol.md:1955` — `scripts/quality/assert-global-coverage.js`, category "6 — Quality", **writes-DB = NO (read-only probe)**. Already registered; conversion keeps it via `identity.lock` + the §5.4 source-text convention (`const ADVISORY_LOCK_ID = 111;` stays textual in the frozen shell). |
| 3 | Chains + position | `node -e` over `scripts/manifest.json.chains` | **permits**: pos 32/33 (last = `backup_db`) · **coa**: pos **16/16 — last step** · **sources**: pos 24/28 (last = `assert_engine_health`). 3 chains total (matches `122-conversion-roadmap.md:24` "coa+permits+sources (3)"). |
| 4 | DML | `grep -niE "^\s*(insert into|update |delete from)"` over the whole file, comments excluded | **zero** non-comment hits — genuinely verdict-only (confirmed independently by Fold A low-confidence #3 "UPGRADED to measured"). Residual: a grep cannot see dynamically-composed SQL (§8 low-confidence). |
| 5 | Tables read | `grep -oE "FROM \|JOIN "` distinct targets | **22**: `permits`, `coa_applications`, `entities`, `wsib_registry`, `parcel_buildings`, `building_footprints`, `permit_trades`, `cost_estimates`, `permit_parcels`, `parcels`, `phase_calibration`, `phase_stay_calibration`, `lead_trades`, `lead_products`, `lead_parcels`, `tracked_projects`, `lead_analytics`, `data_quality_snapshots`, `engine_health_snapshots`, `pipeline_runs` (read-only — last-verdict lookup for `assert_entity_tracing`), `permit_type_classifications`, `information_schema.columns`. |
| 6 | `pool.query` call sites | `grep -c "pool\.query("` | **22** |
| 7 | Logic vars consumed | `grep -oE "logicVars\.[a-zA-Z_]+"` | **6**, all verdict-affecting (feed PASS/WARN/FAIL boundaries directly): `profiling_coverage_pass_pct`/`warn_pct`, `vocab_coverage_pass_pct`/`warn_pct`, `cost_coverage_pass_pct`/`warn_pct`. Rule 3 (Spec 124 §2) ⇒ all six need `on_invalid:"fail"` absent a dated `deviations[]` ruling. |
| 8 | Row-builder call sites | `grep -c` per helper | `coverageRow`: 97 · `infoRow`: 153 · `calibratedRow`: 17 · `externalRow`: 3 · `vocabRow`: 2 · `profileVocabTriple`: 3. **`rows.push(...)` total: 273** call sites across three **mutually exclusive** `if(isCoaChain)/else if(isSourcesChain)/else{permits}` branches — no single run executes more than one branch + the shared `VOCAB_COVERAGE` loop (permits/coa only) + the two conditional lead-id/scope-drift invariant rows. |
| 9 | Existing (pre-conversion) test file | `wc -l src/tests/assert-global-coverage.infra.test.ts` | **927 L**. Contains two now-**stale** measured claims per its own docblock: `"(c) Chain count — permits chain = 28 steps, coa chain = 12 steps"` vs **measured 33 / 16** (claim #3 above). This drift is itself a G0/PH-0 finding, not something to silently fix. |
| 10 | WD-1 disposition (governs this step's write class) | `.cursor/wf5_wd1_write_class_audit_active_task.md:36` | Class **L `verdict_only`** — **RETIRED, structurally undeclarable** (`outputs` forced `{const:"none"}` under the ASSERT `x-profile`). `assert-schema.descriptor.json:314` already carries the precedent `deviations[]` entry from the same class. **BUILT** (`programme-items.json` WD-1 = BUILT, measured live). |
| 11 | Entry-gate readiness | `node -r dotenv/config scripts/analysis/step-validate.mjs --staged --fast` | `programme: blocks batching: 0` — **STD-7 / FREEZE-1 / WD-1 / VRD-SKIP / ADMIN-1 / C4-GATE all BUILT** (`node -e` over `programme-items.json`). C4 batch 1 is genuinely clear to open; this step (order 1) is the correct next unit of work. |
| 12 | Precedent scale | `node -e` over `scripts/lib/compute/enrich-parcels.js` | `enrich_parcels` (pilot 9, the largest converted compute) declares **31** checks, 1:1 with `ctx.report(id, …)` call sites. `assert_schema` (pilot 1, the ASSERT precedent) declares **10**. This step's 273 row-push sites are **≥9× the largest live precedent** — see Ask A1. |
| 13 | Churn (window_end SHA, not HEAD) | `docs/reports/generated/122-churn-complexity.md:50` | commits=**50** (highest of the 27-step population) · lines_changed=2,328 · LOC@window_end=559 · branches@window_end=137 · quadrant **top-right** (median split: churn≥21, complexity≥54 — both true here by a wide margin). |
| 14 | Git history (G1/G3 seed) | `git log --oneline -- scripts/quality/assert-global-coverage.js` | 28+ commits since the file's creation (`2c6efadb` "WF1 — global field-level coverage profile"); most recent 10: `50837ef4`(C7 status-writer), `5ec3523a`(C3 backfill), `5ef51de7`(C6 trigger scope fix), `23cbe89c`(D-C below-floor clamp), `1a2d586d`(producer-side accepted baselines), `e054ba24`(entities 3-layer truth), `1f8ca38a`(sources honesty gates), `173f0d1e`(entity-tracing + bylaw WARN floors), `4442fb75`(archetype cost), `d72ce2ff`(parcels-table coverage profile WF3). |

---

## 1. G0 Boundary freeze — scope (full statement-level enumeration is commit 1's own deliverable, not this plan's)

* **Writes:** none (row 4). `emitMeta({}, {})` — Observer. `outputs`/`recovery`/`counters` all forced `"none"` by the ASSERT `x-profile` (Spec 122 §1.10); matches `assert_schema`'s descriptor exactly.
* **Reads:** 22 tables (row 5), 22 `pool.query` sites (row 6).
* **Audit rows:** up to 273 named `{metric,value,threshold,status}` rows across 3 mutually exclusive branches, plus the 3-triple `VOCAB_COVERAGE` loop (permits+coa only, skipped in `sources`), plus 2 always-on invariant rows (`lead_id_administrative_drift`, `lead_id_duplicate_groups` — permits branch only) and 2 conditional WARN+INFO rows (`enriched_status_status_scope_drift[_retighten]`, self-retiring at 0, permits branch only).
* **Exit codes / stdout:** standard `pipeline.run` Observer semantics — non-halting on coverage gaps (worst-status verdict, never throws), halting only on Zod/DB infra errors (unchanged post-conversion; the library owns this distinction identically to `assert_schema`).
* **`sharing.varies_by_chain.phase`** (measured, §1.7 map requirement — never a ternary): `{ permits: 32, coa: 16, sources: 24 }` (row 3). **Spec 49 §2's own architecture text is stale** ("permits: step 26/last", "coa: step 10/last") — neither is last except coa; commit 9's spec-diff must correct this alongside the chain-length correction (row 9).
* **`sharing.chains` / `shared` / `slug_forms`:** `"derived"` (per §1.7, never hand-declared — matches all 9 converted precedents).
* **`sharing.on_contention`:** `"self_skip"` (A3 RULED, all 5 batch-1 steps; zero executor reads, library's advisory-lock behaviour by construction).

---

## 2. Archetype + write class (already ruled — do not re-litigate at commit 1, only re-verify)

**ASSERT** (5-member archetype; `assert_schema` = pilot 1 precedent). Required: `checks` (≥1, likely on the order of hundreds — §8 Ask A1). Forced `"none"`: `outputs`, `recovery`, `counters`. Write class **L `verdict_only`** is RETIRED (row 10) — do not declare it, do not attempt to express it; the schema forbids a non-`"none"` `outputs` under this profile and §1.2/Operating Boundaries ban every `step.schema.json` byte (no RE-FREEZE is expected or in scope here, unlike `assert_engine_health`'s I3b, which genuinely needs one).

---

## 3. The nine-commit ledger — full form (R-AA: `assert_global_coverage` is the SECOND ASSERT-archetype step to convert, after `assert_schema` — full nine-commit form applies, no abbreviation)

| # | Phase | Gate | Done-test (Spec 123 §7 shape, C4 §3.3 adjustments applied) |
|---|---|---|---|
| **1** | PH-0 boundary freeze | G0 | Full statement-level table (22 reads, 273 row-push sites disposed by branch/metric) committed to the assessment report; **Target Spec line = Spec 49 (row confirmed above), cross-cited 41/42/43** — not skipped in favour of 122/123/124. Corrects the two stale test claims (row 9) and the two stale Spec 49 §2 architecture lines in the SAME commit's report (not deferred). |
| **2** | PH-3 Intent Ledger — human-adjudicated (Spec 121 §12.5) | G3 | **4 of 5 batch-1 steps are top-right ⇒ full ledger, no Class-C skip** (C4 §3.3 row 2) — this step included. Unusually, the file's own comments already carry inline intent-ledger citations (`DEC-1`/`DEC-2`/`DEC-B`, `Bug 1`–`4`, `F2`, `C3`, `C6`, `WF2 #4`/`#415`/`#428`, `WF3 #406`, `WF3-A`/`WF3-B`, `WF3 F4`, Spec 65/78/88 phase tags) — PH-3's job here is **confirming these against `git log -S`/`blame`** (row 14 gives the commit seed list), not discovering them cold. Each gets a disposition: `preserved-as-check` / `preserved-in-compute` (with a named `why` location, Rule 4) / `knowingly-retired` / `INTENT-UNKNOWN`. |
| **3** | PH-5 seam map | G5 | DB seam = `ctx.pool`; clock = none consumed (no `Date.now()`/`new Date(` in this file — confirm at commit 1); network = none; argv/env = `process.env.PIPELINE_CHAIN` (branch selector) → becomes the library's chain-derived `ctx.checks` selection (mirrors `assert_schema`'s "compute no longer knows what a chain is", peel 8a precedent) — **this is the one seam requiring real design work**: today's `if(isCoaChain)/else if(isSourcesChain)/else` branch must become three (near-disjoint, not overlapping-subset like `assert_schema`'s) `checks[].chains` scopes. |
| **4** | PH-6 classification | G6 | Every behaviour CONTRACT/INCIDENTAL/DEFECT; every DEFECT a ledger ID. Expect: `Bug 1`–`4` inline comments already read as DEFECT-class historical fixes (CONTRACT now, not open defects) — PH-6 confirms, does not re-open. |
| **5** | Golden master — 4-tuple, non-determinism declared first | G1′ | **4 capture pairs** (`|C(S)|+1 = 3+1`, C4 §3.4): `pre/permits.json`+`post/permits.json`, `pre/coa.json`+`post/coa.json`, `pre/sources.json`+`post/sources.json`, `pre/standalone.json`(≡`none`)+`post/standalone.json`. Non-determinism inventory: `EXTRACT(days FROM NOW() - MAX(last_seen_at))` (coa `days_since_latest`), `NOW() - INTERVAL '25 hours'`/`'18 months'`/`'3 years'` windows — declare as clock-relative, non-pinnable-to-exact-value, diff-tolerant. |
| **6** | PH-7 red-first — `converted.json.pending` entry (R-K/R-K.1) | G7 | `stage: "red_suite"` initially; new `src/tests/steps/assert_global_coverage/violations.test.ts` with `it.fails(...)` per claim, "flips at commit N" comments, tied to the pending slug (conformance enforces `pending ∩ converted = ∅`). |
| **7** | Descriptor + compute verbatim — genuine no-op diff | G2′ | `scripts/quality/assert-global-coverage.descriptor.json` + `scripts/lib/compute/assert-global-coverage.js` (dispatch table, one function per declared check, `ctx.report()` only, no `console.*`, `ctx.fetch`/`ctx.clock` seams — N/A here, no fetch/clock used — `ctx.config` for the 6 logic vars). **⛔ No schema bump expected or authorized without an explicit RE-FREEZE payment** (C4 §3.3 row 7) — if the checks-at-scale design (§8 Ask A1) turns out to need one, escalate per C2 kill-criterion, do not pay silently. |
| **8** | Peel — one policy concern per commit | green diff every peel | Likely peels: (a) gating/lock → (b) verdict/audit-table assembly → (c) thresholds/checks scoping per chain. `sharing.varies_by_chain.phase` as an explicit map (§1 above), never a ternary. |
| **9** | Differential + cutover | G8, G4d, G-shape | `node scripts/analysis/step-validate.mjs --step=assert_global_coverage --write`; **CLOUDPARITY** applies (`cutover_prereq`, one `chain_run_id` per chain with `status='completed'`, no `skipped`); spec-diff-or-N/A for Specs 41/42/43/49/122/123/124 in this commit (row 1's two corrections land here if not already folded into an earlier commit); `converted.json` gains the entry, `pending[]` entry deleted in the **same** commit (R-K); `pipeline.run(` gone from the frozen shell. |

---

## 4. Database Impact

**NO.** Confirmed by measurement (row 4: zero DML), not assumed. `outputs`/`recovery`/`counters` forced `"none"` by the ASSERT profile (§2). The conversion's golden captures **read** the DB; they write nothing new. No migration, no backfill.

## 5. Standards Compliance
* **Try-Catch Boundary:** N/A — no API route. Compute errors are caught per-check by the dispatch loop's own error boundary (`ctx.report(id, { error })`, mirrors `assert-schema.js`'s peel-8b precedent — replaces this file's current ad-hoc substring-matched error attribution, which the Intent Ledger should flag as a defect this conversion structurally fixes, not merely preserves).
* **Unhappy Path Tests:** red-first suite (commit 6) proves every claim RED before the descriptor exists; G4d requires a both-directions lock per fence found in PH-3 (row: the two invariant rows `lead_id_administrative_drift`/`lead_id_duplicate_groups` are themselves regression fences for migration 138_a/241 — their both-directions lock is the conversion's own G4d obligation, not optional).
* **logError Mandate:** N/A — pipeline scripts use `pipeline.log.*`/`ctx.log`, no `src/` catch blocks added.
* **UI Layout:** N/A — no UI file modified (Cross-Domain trigger is a consumer-contract dependency only; see §12 Handoff Note).
* **Cross-Layer Contracts:** the `{metric,value,threshold,status}` row shape consumed by `FreshnessTimeline.tsx`'s legacy metric-row renderer (Spec 49 §5) must stay byte-identical — this is exactly what the G8 zero-unexplained-diff golden gate proves; no separate UI test needed.
* **Spec-diff-or-N/A (Spec 123 §7 commit 9(b)):** Specs 41/42/43 (chain step-list references, if any name this step by old numbering), Spec 49 (§2 architecture text corrections, row 9/§1), Spec 122 (§1.10 ASSERT member table — no change expected, already lists this step), Spec 123/124 (no change expected unless Ask A1 forces a scale ruling).

## 6. Validator surface declared at plan time (pilot 7 lesson)

| Surface | State entering this step | Must satisfy at commit 9 |
|---|---|---|
| `step-validate --step=assert_global_coverage --write` | not yet run (unconverted) | **≥14/17, G6–G8 full, hard-stop=false** |
| Fast invariant **#22 GOLD-PRE-FRESH** | live (`2f4bd3f7`) | PRE captures committed before any re-run; `--overwrite` only over tracked/clean files |
| Fast invariant **#9** (registry CLOUDPARITY) | clean (measured, row 11) | stays clean; this step's own CLOUDPARITY row added at commit 9 |
| Rule 10(b) (VRD-SKIP) | **BUILT/enforced-green** (measured, row 11 — no longer pinned) | self-skip on lock 111 contention folds to non-PASS per VRD-SKIP's ruling |
| §3.4 "every chain" capture gate (`C4-GATE`) | **BUILT** (measured, row 11) | 4 capture pairs present, fingerprint-fresh, completeness+freshness both checked by `derivedInvocations()`/R-C |
| Policy matrix Rules 1–13 | N/A pre-conversion | Rule 3 (6 logic vars, all verdict-affecting ⇒ `on_invalid:"fail"` unless a dated `deviations[]`), Rule 10 (verdict row-derived, no parallel boolean — this file's CURRENT `verdict = rows.some(FAIL)?FAIL:...` line is exactly the pattern the library's `deriveVerdict` replaces), Rule 5 (`checks` ≥1 — trivially true at 200+), Rule 13 (scorecard generated, not hand-typed) |
| `check-step-shape.mjs` | 9+9 enforced, this file report-only | moves into the enforced set at cutover |

## 7. Panel roster — both altitudes (Spec 08 §6.4)

**Cross-Domain, per C4 Execution Plan row I1: 5 + Regression Guardian** (`Reality-Check` seated **only if** a derived/enriched field changes — it does not here: this is a pure observability/coverage step reading existing columns, never deriving a new parcel field, so **Reality-Check is N/A** by the CLAUDE.md subject-matter trigger).
* **PLAN altitude (before code):** Integration (wiring/manifest/chain-position reality vs the plan's row 3/§1 claims) + Regression Guardian (WD-1's `verdict_only` retirement is itself a fence this plan must not re-litigate; the two invariant rows are fences from migration 138_a/241) — WF2 always seats the Guardian (diff modifies existing code by definition).
* **OUTPUT altitude (diff + re-run values):** Gemini + DeepSeek (lens set: spec/security/idempotency/error-paths) + `code-reviewer-grounded` + `observability-reviewer` (audit-row completeness vs Spec 49 §3/§4, verdict cascade row-derived not parallel-boolean, §11 counter scoping) + Integration (real manifest/chain wiring, real downstream consumer — `FreshnessTimeline.tsx`) + `regression-guardian` (main tree, states the fence for every altered/deleted line — especially the substring-based error-attribution code this conversion structurally replaces, and the two lead-id integrity FAIL rows).

## 8. Asks — only where no rule answers, one recommendation each

| # | Ask | Why no rule answers it | Recommendation |
|---|---|---|---|
| **A1** | **Scale of `checks[]`.** This step's compute currently emits up to 273 distinct named `{metric,...}` rows across 3 near-disjoint branches (row 8) — ≥9× the largest live precedent (`enrich_parcels`, 31 checks, row 12). Spec 122/123/124 assume "many checks" but never state a ceiling or an authoring method for hundreds of them, and Rule 1 (nothing lives only in code) still requires each threshold/metric to be declared data, not a loop-generated string. | No spec addresses descriptor authoring at this scale; it is genuinely unprecedented in this programme. | **Tool-generate the `checks[]` array from a census of the current row-builder call sites** (one script pass over `coverageRow`/`infoRow`/`calibratedRow`/`externalRow`/`vocabRow` invocations, extracting `stepTarget`+`field`+threshold args programmatically), then **human-adjudicate the generated list in PH-3** exactly as the Intent Ledger already requires — not hand-typed from scratch, not silently collapsed into fewer, coarser checks (that would lose the field-level PASS/WARN/FAIL granularity Spec 49 §3 promises). Flag for the operator: this makes `assert_global_coverage` the scale-defining case for the programme's descriptor-authoring tooling; expect the generation script itself to need a brief review. |
| **A2** | **Disposition of the pre-conversion `src/tests/assert-global-coverage.infra.test.ts` (927 L, 2 stale claims — row 9).** Spec 124/123 describe the NEW per-step `src/tests/steps/<slug>/violations.test.ts` convention but are silent on what happens to an OLD, pre-existing `*.infra.test.ts` for the same step. | No rule states retire-vs-keep-vs-migrate for a pre-existing legacy test file at conversion time. | **Keep it, corrected** — the `link_wsib` precedent (`src/tests/link-wsib.infra.test.ts` coexists with `src/tests/steps/link_wsib/violations.test.ts`) is the only measured precedent and this plan follows it: fix the two stale chain-length claims at commit 1 (PH-0 finding), leave the file as a standing regression lock on denominator/gate behaviour, and let the new `violations.test.ts` own the conversion-specific claims. Do not delete. |
| **A3** | **Spec 49 §2 architecture text correction — same commit as this conversion, or a separate docs commit?** Spec 123 §7 commit 9(b) requires a spec diff "in this commit... or N-A stated," which covers *this* conversion's behaviour changes, but the stale step-position text (permits step 26/coa step 10, both wrong per row 3/§1) is a **pre-existing drift**, not something this conversion causes. | Spec 123/124 are silent on pre-existing drift discovered incidentally during a conversion's own G0. | Fix it in **this conversion's commit 1 report** (cheap, already measured) and land the actual spec-file edit in **commit 9** alongside the conversion's own required diff — riding the same commit is legal per Fold A X-4's reading of §7(b) ("does not forbid a docs-only amendment riding alongside"), and it is the step whose own PH-0 found the drift. |

## 9. Low confidence / to verify — every claim not fully executed

| # | Claim | Why unexecuted here | Command that would settle it |
|---|---|---|---|
| 1 | Exact distinct-metric-id count per branch (permits/coa/sources), after de-duplicating loop-generated rows (e.g. the dynamic `envelope_constraint_reason` GROUP BY in the sources branch, and the `for (const c of COST_PROP_COLS)` loop in coa) | full statement-level enumeration is commit-1 (PH-0) work, not this plan's | re-run the file against a live DB once per chain and count `records_meta.audit_table.rows.length` per branch |
| 2 | Whether any dynamically-composed SQL exists beyond what a case-insensitive DML grep can see (row 4's own caveat) | grep cannot see string-built SQL | manual read of all 22 `pool.query(` call sites at PH-0, confirming each is a static template literal |
| 3 | Whether `sharing.varies_by_chain.checks` should be modelled as three genuinely disjoint per-chain lists (vs. `assert_schema`'s per-chain SUBSET of one shared list) | depends on A1's resolution | once the checks census (A1) exists, diff the permits/coa/sources id sets — expect near-zero overlap except the 3 shared `VOCAB_COVERAGE` triples |
| 4 | Whether `scripts/lib/vocab-coverage.js`, `scripts/lib/coverage-status.js`, `scripts/lib/accepted-baseline.js`, `scripts/lib/parcel-cost-cols.js` (all imported here) need any seam change to satisfy compute-shape (§5.5) | not read line-by-line in this plan | `scripts/hooks/check-step-shape.mjs` run against a draft compute referencing them — these are pure-function libs, expected to pass unchanged |
| 5 | Whether the two conditional rows (`enriched_status_status_scope_drift`[+retighten]) currently read 0 in production (self-retired per Spec 48 §4.9) or are still live | requires a DB read | `SELECT COUNT(*) FROM permits WHERE enriched_status IS NOT NULL AND status IS DISTINCT FROM 'Inspection'` against the live/cloud DB — PH-0 or golden-capture time |

## 10. Operating Boundaries
* **Target Files:** `scripts/quality/assert-global-coverage.js` (→ frozen shell) · `scripts/quality/assert-global-coverage.descriptor.json` (new) · `scripts/lib/compute/assert-global-coverage.js` (new) · `src/tests/steps/assert_global_coverage/violations.test.ts` (new) · `src/tests/assert-global-coverage.infra.test.ts` (corrected, kept — Ask A2) · `scripts/steps/_schema/converted.json` · `docs/specs/01-pipeline/49_data_completeness_profiling.md` (§2 correction) · `docs/reports/golden/assert_global_coverage/**` (new, 4 pairs) · a new dated assessment report under `docs/reports/`.
* **Out-of-Scope Files:** `scripts/steps/_schema/step.schema.json` (frozen, zero edits expected — escalate per Ask A1 only if genuinely forced) · `scripts/manifest.json` (no slug/path change) · every OTHER unconverted step file (`assert_data_bounds`, `assert_engine_health`, `link_neighbourhoods`, `geocode_permits` — each its own WF2-C, I2–I5) · `src/components/FreshnessTimeline.tsx` (consumer, unmodified — contract unchanged per §12) · `scripts/lib/step/**` (library-owned, not per-step surface).
* **Cross-Spec Dependencies:** Spec 49 (owner) · Specs 41/42/43 (chains) · Spec 122 (§1.10 ASSERT profile, §5.1/§5.3/§5.5, §8.1) · Spec 123 (§6 gates, §7 commits) · Spec 124 (§2 Rules, register) · Spec 47 §A.5 (lock 111, unchanged) · Spec 48 §3.6/§3.7 (verdict cascade, row-derived) · Spec 79 (chain-end validation cap, this step's profile is that cap per Spec 49 §6).

## 11. Not in scope
Converting `assert_data_bounds`/`assert_engine_health`/`link_neighbourhoods`/`geocode_permits` (each its own WF2-C, I2–I5, sequenced strictly after this step's commit 9 is green) · the `run-chain.js` `isInfraStep` gate-skip-exemption retirement (WF3 I3a, precedes `assert_engine_health` only, not this step) · any `step.schema.json` edit · any admin UI change · any migration.

## 12. Handoff note (Cross-Domain Scenario A)
No admin file is created or modified by this conversion. The JSON contract `FreshnessTimeline.tsx` already renders — `{ metric, value, threshold, status }` rows under `records_meta.audit_table`, `phase: 111`, `name: 'Global Data Completeness Profile'`, no `columns[]` (legacy metric-row path, Spec 49 §5) — is asserted **unchanged** by the golden differential's zero-unexplained-diff gate (G8) at commit 9. If G8 finds any diff in row shape, that is a build-blocking defect, not an accepted behaviour change, and must be fixed before cutover, not waived.

---

> **PLAN LOCKED. Do you authorize this WF2-C plan? (y/n)**
> §11 note: three non-obvious choices — (1) Ask A1 (tool-generate `checks[]` from a row-builder census, human-adjudicated) rather than hand-authoring ~270 descriptor entries; (2) Ask A2 (keep the pre-existing `assert-global-coverage.infra.test.ts`, corrected, following the sole measured precedent `link_wsib`) rather than deleting or silently migrating it; (3) Domain Mode declared Cross-Domain per the C4 entry plan's own Execution Plan row, though no admin file is touched in this plan — only a consumer-contract dependency (§12).
> DO NOT generate code. DO NOT run commands. TERMINATE RESPONSE.

## Fold A (plan-altitude panel 2026-09-11: Integration + Regression Guardian) — BINDING
1. **Integration: no refutations.** ASSERT is the no-runner archetype (`template-freeze.json` ASSERT `shapes: [], runners: []`; `runWithPool` builds `stepCtx` and calls compute directly); `step.schema.json` `checks[]` has no `maxItems`, each check requires 10 fields, `id` pattern `^[a-z][a-z0-9_]*$`; no `chain_args` for this slug → 4 capture pairs (permits/coa/sources/standalone); `converted.json.pending` REQUIRED_KEYS `file/registers_at/reason/declared/stage`, `stage:"red_suite"` at commit 6.
2. **Guardian FAIL → ledger item (commit 6, red-first; verified green at commit 7):** `src/tests/assert-global-coverage.infra.test.ts` reads the STEP file via `src()` for **173** `toContain`/`toMatch` source-text assertions; after commit 7's 8-line thin shell every one goes dark. Mirror `src/tests/link-wsib.infra.test.ts:19-23`: add a second `COMPUTE = readFileSync(scripts/lib/compute/assert-global-coverage.js)` read and repoint every source-text assertion that encodes BEHAVIOUR (thresholds, table lists, row builders) at `COMPUTE`; assertions that encode only the step file's own skeleton (lock constant, `pipeline.step` call) stay on the step file. Count and classify all 173 in commit 6's body; the two stale chain-length claims (28/12 → 33/16) corrected in the same commit.
3. **Guardian LOW → Ask A1 amendment:** the row-builder census that generates `checks[]` copies the hard-coded step labels (`'CoA Step 5 — classify_coa_scope'` class, review_followups #405 / DEC-2 DEFER LOW) **verbatim** — never "corrects" them; #405 closes in its own WF or not at all.
4. Fences preserved by name: C6 `lead_id` integrity rows (`5ef51de7`, migration 138_a/241) → both-directions G4d lock; C3 status-scope drift self-retiring WARN pair (`5ec3523a`/`50837ef4`) → row-derived under Rule 10; DEC-1 zoning_class 80/75 threshold (`3ab4fa83`, #406) → declared per-field limit via the census (verify at commit 2 the census extracts `calibratedRow` args, not a default); WD-1 `verdict_only` retirement not re-litigated.
