# PROMOTED — see `.cursor/active_task.md`

> **2026-08-23:** This plan was promoted to `.cursor/active_task.md` (v2, operator rulings R1–R6 + vocabulary rulings V1–V6 applied — see Spec 122's round-2 ratification block). That file is now the programme's single source of record (Spec 122 §10).
>
> ⚠️ `scripts/violations/build-active-task.mjs` still targets this path — retargeting or retiring it is an S0 item; do not run it until then. `active_task_programme.md` is stale for the same reason.
>
> The v1 content is preserved in git history (last full version: this file at `623a1ce8`).
