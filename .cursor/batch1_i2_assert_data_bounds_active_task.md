# Active Task: WF2-C — C4 Batch 1 Step I2: `assert_data_bounds` → Spec 122 Step Standard
**Status:** Implementation (AUTHORIZED 2026-09-12 by operator; Ask A1 ruled YES — build the R-D probe-list generator + --check in this conversion)

**Domain Mode: Cross-Domain** (`.cursor/c4_batching_entry_active_task.md` Execution Plan row **I2** declares Domain = Cross-Domain for I1–I5 — read `.claude/domain-crossdomain.md` Scenario A + `.claude/domain-admin.md` + `scripts/CLAUDE.md`). Trigger: the step's 4 `audit_table` shapes are a declared consumer contract for `src/components/FreshnessTimeline.tsx` (Spec 49 §5 legacy metric-row path), `src/components/DataQualityDashboard.tsx`, `src/lib/admin/funnel.ts` (`assert_data_bounds` summary row + `pipeline_runs` table-key map) and `src/lib/quality/types.ts` — no admin file is edited here; §12 carries the handoff obligation.

## Context
* **Goal:** Convert `scripts/quality/assert-data-bounds.js` (slug `assert_data_bounds`) to the Spec 122 step standard — descriptor + `scripts/lib/compute/assert-data-bounds.js` + the frozen 8-line shell — behaviour-neutral, per Spec 123 §1.1/KFM 3. C4 batch 1's **second** step (`.cursor/c4_batching_entry_active_task.md` §3.2 order 2, Execution Plan row **I2**), immediately after `assert_global_coverage` (I1, **landed** — `converted.json.converted` is 10/10 incl. `scripts/quality/assert-global-coverage.js`, `pending: []`, HEAD `d851c66e`).
* **Target Spec (G0):** ⚠ **no system-map owner row exists for this file** — `grep -n assert-data-bounds docs/specs/00-architecture/00_system_map.md` = zero hits (unlike I1's clean Spec 49 row). Governing specs are the file's own four `SPEC LINK` headers: **Spec 44 §4 "Data bounds (assert_data_bounds, deep_scrapes scope)"** (`:318`) is the one named subsection specific to this step and is this plan's Target Spec line; cross-cited **Spec 41** (`:169` "Post-ingestion (assert_data_bounds)", `:66` step-table row), **Spec 42** (`:38` step-table row, `:261` CoA-side extension, `:759`/`:800`/`:848` Phase G Pre-Permit gate), **Spec 43** (`:57`/`:97` sources magnitude-floor table). **Spec 30 §5.4.1** governs the halt-classification architecture this step is the named exhibit for (`30_pipeline_architecture.md:322`: *"(Case C, `assert-data-bounds` regression lock)"*). Architecture specs: 122 (§1.10 ASSERT profile, §5.1/§5.3/§5.5, §8.1), 123 (§6 gates, §7 nine commits, §7.2), 124 (§2 Rules, register R-C/R-D/R-K/R-K.1/R-AA/R-AB/R-AC/R-AD).
* **Key Files:** `scripts/quality/assert-data-bounds.js` (1,055 L → 8-line frozen shell) · new `scripts/quality/assert-data-bounds.descriptor.json` · new `scripts/lib/compute/assert-data-bounds.js` · new `scripts/lib/assert-data-bounds-fields.js` (row-builder census data module, §3 commit 2/7) · new `scripts/generate-assert-data-bounds-descriptor.js` (generator, mirrors `scripts/generate-assert-global-coverage-descriptor.js`) · `src/tests/steps/assert_data_bounds/violations.test.ts` (new, red-first) · `src/tests/assert-data-bounds.infra.test.ts` (129 L, existing) · `src/tests/db/assert-data-bounds-halt.db.test.ts` (518 L, existing — the Spec 30 §5.4.1 regression lock) · `scripts/quality/assert-schema.descriptor.json` (R-D probe-list growth, commit 9) · `scripts/seeds/logic_variables.json` (9 `CONSUMED by` tags, commit 7) · `scripts/steps/_schema/converted.json` · Specs 41/42/43/44/30 (spec-diff obligation, §5 below).

---

## 0. Measured ground truth — every claim, its command, its value

| # | Claim | Command executed | Value |
|---|---|---|---|
| 1 | Current line count | `wc -l scripts/quality/assert-data-bounds.js` | **1,055** (churn report's `LOC=731` is at `window_end`, not HEAD — grown since). |
| 2 | Advisory lock | grep `ADVISORY_LOCK_ID` + Spec 47 §A.5 registry | **103**, registered `docs/specs/01-pipeline/47_pipeline_script_protocol.md:1947` — category "6 — Quality", **writes-DB = NO (read-only probe)**. Kept via `identity.lock` + §5.4's source-text convention. |
| 3 | Chains + position | `node -e` over `scripts/manifest.json.chains` | **permits** 22/33 (last=`backup_db`) · **coa** 11/16 (last=`assert_global_coverage`) · **sources** 27/28 (last=`assert_engine_health`) · **deep_scrapes** 5/7 (last=`assert_staleness`). **4 chains** — the widest fan-out of any batch-1 step. Spec-drift measured: Spec 41 states step 22 (**matches**), Spec 44 states step 5 (**matches**), **Spec 42 states step 8 (measured 11, +3 drift)**, **Spec 43 states step 26 (measured 27, +1 drift)** — both corrections land in commit 9's spec-diff (§5), riding the same commit (Fold A X-4 precedent, I1 Ask A3). |
| 4 | DML | `grep -niE "insert into\|update pipeline_runs\|delete from"` over the whole file | **Two hits, both the step's own ledger row**: `:91` `INSERT INTO pipeline_runs (...) RETURNING id`, `:1003` `UPDATE pipeline_runs SET completed_at=..., status=..., duration_ms=..., error_message=..., records_meta=... WHERE id=$4`. **Zero domain DML.** Confirms C4 plan Fold A I-1/I-2 independently for this step: these are the SDK-owned ledger row (`scripts/lib/step/ledger.js`), **RETIRED** under conversion, not declared — not a genuine step write, **not an Ask**. |
| 5 | Tables read | `grep -oE "FROM [a-zA-Z_.]+\|JOIN [a-zA-Z_.]+"`, false positive excluded | **16 distinct**: `address_points`, `building_footprints`, `coa_applications`, `cost_estimates`, `entities`, `heritage_districts`, `heritage_properties`, `neighbourhoods`, `parcels`, `permit_inspections`, `permit_parcels`, `permit_trades`, `permits`, `ravines`, `toronto_centreline`, `wsib_registry`. (Grep also matched `FROM inspection_date` — an `EXTRACT(YEAR FROM inspection_date)` expression, not a table; excluded.) |
| 6 | SQL execution sites | `grep -n "pool\.query("` (9) + `grep -c "await count("` (50) | **9** direct `pool.query(` sites = 1 helper definition (`:58`) + 2 ledger (`:90` INSERT, `:1002` UPDATE, retired per row 4) + 6 ad-hoc SELECT (`:130` cost outliers, `:293` CoA sub-0.85, `:860` cost-tier count, `:886`/`:892` magnitude gates, `:935` ghost records). **50** call sites to the `count()` helper. **56 total domain SQL executions** in a standalone (all-chain) run. |
| 7 | Logic vars consumed | `grep -oE "logicVars\.[a-zA-Z0-9_]+"` | **9**, all pre-existing (all defined in `LOGIC_VARS_SCHEMA`, all present in `scripts/seeds/logic_variables.json` today): `cost_outlier_ceiling_cad`, `desc_null_rate_warn_pct`, `builder_null_rate_warn_pct`, `cost_est_null_rate_warn_pct`, `cost_est_min_tiers`, `calibration_freshness_warn_hours`, `coa_forward_link_sub085_warn_pct`, `cost_est_legacy_cost_ceiling_cad`, `cost_est_legacy_gfa_ceiling_sqm`. **All 9 are verdict-affecting** (each feeds a PASS/WARN/FAIL boundary directly) ⇒ Rule 3 (Spec 124 §2) requires `on_invalid:"fail"` for all 9 absent a dated `deviations[]` ruling. **None of the 9 seed entries carry a `CONSUMED by <slug>` annotation today** (`node -e` read of `scripts/seeds/logic_variables.json`, verified per-var) — the exact I1 RECURRING #4 gap, to close at commit 7 not commit 9 (§3 row 7). |
| 8 | Row-builder / metric census | `node -e` regex over `metric: '...'` literals + `checkInsp('...'` call sites | **49 distinct named metrics**: 37 static `metric: '<id>'` literals + 12 dynamic ones from the `checkInsp(metric, value, threshold, level)` helper (`:741`, inspection-scoped). Spread across **4 audit tables**: `permitsAuditTable` (phase 15, `:262`), `coaAuditTable` (phase 8, `:415`), `sourcesAuditTable` (phase 14, `:625`), `inspectionAuditTable` (phase 3, `:843`). **4 WSIB metrics are cross-injected into BOTH `permitsAuditTable` AND `sourcesAuditTable`** at runtime (`:709-725`, `wsibAuditRows.push(...wsibAuditRows)` into both — counted once in the 49, expressed at commit 7 as `checks[].chains: ["permits","sources"]`, the exact multi-chain array shape `assert_global_coverage`'s own descriptor already uses 4× — not a new mechanism, verified `step.schema.json:685-691`). |
| 9 | ⚠ Non-parameterised tunable interpolation (found independently, not named by the C4 plan) | `grep -n '\${' \| grep -iE "select\|from\|where"` | **`:888`/`:894`** interpolate `${Number(legacyCostCeiling)}` / `${Number(legacyGfaCeiling)}` directly into SQL text rather than binding as `$2`/`$3` (the accompanying `${acceptClause}` array param IS correctly bound via `[COST_MAG_ACCEPT]`). Violates `scripts/CLAUDE.md`'s "parameterised queries only" rule and is exactly the pattern `compute-no-literal-url-tunable`/`-threshold` (`compute-shape.yml`) exists to catch. **Mechanical, behaviour-neutral fix required at commit 7** (bind the two ceilings as query params) — not a DEFECT under Spec 123 §3 (identical query semantics for numeric literals), but the shape rule will not build green otherwise. |
| 10 | Existing tests (pre-conversion) | `grep -rln` + per-file `grep -c` | **Narrow, load-bearing:** `src/tests/assert-data-bounds.infra.test.ts` (129 L, **18** `expect(SRC)` source-text assertions, logic-var threshold lock) · `src/tests/db/assert-data-bounds-halt.db.test.ts` (518 L, 5 cases — **the Spec 30 §5.4.1 halt-classification regression lock**: Case A Pre-Permit threshold non-halting, **Case B the load-bearing exception-must-still-halt proof**, Case C **the `f238b814` cost_outliers≥20 fence**, Case C-boundary, a source assertion pinning the 3 `fatalErrors.push` sites). **Incidental (broad-file) hits, disposition = leave alone, cite only if their own claim is stale:** `quality.infra.test.ts` (11), `quality.logic.test.ts` (10), `chain.logic.test.ts` (5), `db/quality-ledger-strand.db.test.ts` (4), `pipeline-sdk.logic.test.ts` (3), `admin.ui.test.tsx` (3), `wsib.infra.test.ts` (2), `quality-ledger-window.logic.test.ts` (2), plus 6 files at 1 hit each (`migration-137-lead-id-orphan-audit`, `migration-157-retire-pre-permits`, `pipeline-advisory-lock`, `conversion-roadmap.infra`, `check-chain-verdict.logic`, `pipeline-logic-vars-coercion`). |
| 11 | WD-1 disposition | `.cursor/c4_batching_entry_active_task.md` Fold A I-1/I-2 (independently re-verified, row 4) | Class **L `verdict_only`** — **RETIRED, structurally undeclarable** (`outputs` forced `{const:"none"}` under the ASSERT `x-profile`). `programme-items.json` WD-1 = **BUILT**. |
| 12 | Entry-gate readiness | `node -r dotenv/config scripts/analysis/step-validate.mjs --staged --fast` | `programme: blocks batching: 0`. `converted.json` = 10/10 converted incl. I1, `pending: []`. `EP-PIN-D17.gate.blocks` (`programme-items.json`, direct read) = **`["enrich_centreline"]` only** — re-pointed away from `assert_data_bounds` by the second 2026-09-11 operator ruling; **not** a cutover_prereq blocker here (confirms the task brief's hint). `GOLD-PRE`/`C4-GATE` (both BUILT) list this slug only as a forward-declared, already-satisfied `blocks[]` entry. |
| 13 | Precedent scale | `node -e` over converted descriptors' `checks.length` | `assert_schema` (pilot 1) = **10**, hand-authored. `assert_global_coverage` (I1) = **305**, tool-generated (row-builder census + `scripts/generate-assert-global-coverage-descriptor.js`). This step's 49 metrics sit between the two — per I1's own RECURRING #1 finding, still budget the census+generator from PH-0, not discover the need mid-commit. |
| 14 | Churn (window_end SHA) | `docs/reports/generated/122-churn-complexity.md:53` | commits=**59**, lines_changed=2,315, LOC@window_end=731, branches@window_end=171, quadrant **top-right**. |
| 15 | Git history (G1/G3 seed) | `git log --oneline -- scripts/quality/assert-data-bounds.js` | **58** commits (churn report's 59 is off by one against a slightly different window — reported, not reconciled by assumption). Named fences found this session: `f238b814` "adjust false WARN thresholds in CQA checks" (the cost_outliers≥20 fence, `:135-140`'s own comment cites it + "C4 panel A1, 2026-08-13"), `4d5df9e7` "C4 — per-site halt-classification gate, convert assert-data-bounds + assert-network-health" (the Spec 30 §5.4.1 `fatalErrors[]` split), `adec1f68` "WF1 Phase G — PRE-permit retirement shims + assert gate" (the duplicated `permits_pre_permit_count` gate in both `runPermitChecks`/`runCoaChecks` blocks, `:255-258` comment states the duplication is deliberate — disjoint guards preclude a shared variable). |

---

## 1. G0 Boundary freeze — scope (full statement-level enumeration is commit 1's own deliverable)

* **Writes:** none (row 4). `outputs`/`recovery`/`counters` all forced `"none"` by the ASSERT `x-profile` (Spec 122 §1.10) — matches `assert_schema`/`assert_global_coverage` exactly.
* **Reads:** 16 tables (row 5), 56 SQL executions (row 6).
* **Audit rows:** 49 named `{metric,value,threshold,status}` rows across 4 chain-scoped tables (row 8); WSIB's 4 rows are the one genuine cross-chain overlap in the batch so far (permits ∧ sources), expressible as `checks[].chains: ["permits","sources"]` (no new mechanism — `step.schema.json:685-691`, `assert_global_coverage` precedent ×4).
* **Exit codes / stdout:** standard Observer semantics, **and this is the step Spec 30 §5.4.1 is written around**: threshold-derived pushes (`errors[]`) never halt; only the 3 exception-derived `fatalErrors.push` sites (`:701-702` WSIB, `:833-834` inspection, `:965-966` outer catch) throw (`:1035`). This split is regression-locked by `db/assert-data-bounds-halt.db.test.ts` Case B (row 10) — it must survive the conversion byte-for-byte in the library's error-boundary semantics (mirrors `assert-schema.js`'s peel-8b precedent, per I1's own Standards Compliance note).
* **`sharing.varies_by_chain.phase`** (manifest step position, distinct from the runtime `audit_table.phase` dashboard numbers below — do not conflate the two, they share a name and nothing else): `{ permits: 22, coa: 11, sources: 27, deep_scrapes: 5 }` (row 3).
* **`sharing.varies_by_chain.audit_table`:** `"per_chain"` — and, unlike every prior precedent (`assert_schema`'s phase is a constant 1 across all 3 chains, `assert_global_coverage`'s a constant 111), **the runtime `audit_table.phase` number itself varies per chain here** (15/8/14/3, row 8) — first live case of this; flag it explicitly at PH-5 so the generator's per-chain template carries the right phase per branch.
* **`sharing.chains` / `shared` / `slug_forms`:** `"derived"` (§1.7, never hand-declared).
* **`sharing.on_contention`:** `"self_skip"` (A3 RULED, all 5 batch-1 steps).
* **Mechanical fix required at commit 7 (row 9):** bind `legacyCostCeiling`/`legacyGfaCeiling` as query parameters instead of string-interpolating them — required for `compute-shape.yml` to pass, not a behaviour change.

---

## 2. Archetype + write class (already ruled — re-verify only)

**ASSERT** (5-member archetype; `assert_schema` = pilot 1, `assert_global_coverage` = I1). Required: `checks` (≥1, ~49 — see §8 for the generator decision). Forced `"none"`: `outputs`, `recovery`, `counters`. Write class **L `verdict_only`** is RETIRED (row 11) — do not declare it, the schema forbids a non-`"none"` `outputs` under this profile, and no RE-FREEZE is expected (unlike `assert_engine_health`'s I3b).

---

## 3. The nine-commit ledger — full form (second batch-1 ASSERT conversion; compressed form only from I3 per C4 §3.3 row 2/Spec 123 §7.2)

| # | Phase | Gate | Done-test |
|---|---|---|---|
| **1** | PH-0 boundary freeze | G0 | Full statement-level table (16 reads, 56 SQL sites, 49 metrics disposed by audit table) committed to the report; **Target Spec = Spec 44 §4** (row confirmed above, no system-map row exists — state that explicitly, not silently), cross-cited 41/42/43/30. Corrects the two stale chain-position claims (Spec 42 step 8→11, Spec 43 step 26→27) in the SAME commit's report, landed in commit 9's spec-diff. |
| **2** | PH-3 Intent Ledger — human-adjudicated (Spec 121 §12.5) | G3 | Top-right quadrant ⇒ full ledger, no Class-C skip (C4 §3.3 row 2). **Start the row-builder census here, not at commit 7** (I1 RECURRING #1 obligation): `scripts/lib/assert-data-bounds-fields.js` (a data module of `{id, table, metric, threshold, severity, chains}` census rows, mechanically extracted from the 37 static + 12 `checkInsp` sites) adjudicated against named fences: `f238b814` (cost_outliers≥20 → `preserved-as-check`, cite the fence in `why`), the Phase-G Pre-Permit duplication (`adec1f68` → `preserved-as-check` ×2, one row per chain, NOT collapsed to one — the disjoint-guard reason is itself the `why`), P13-1 `COST_MAG_ACCEPT` allowlist (→ `preserved-in-compute`, `why` cites the 3 accepted `permit_num`s and the 2026-07-09 investigation), P12-B2 CoA sub-0.85 watch (→ `preserved-as-check`). |
| **3** | PH-5 seam map | G5 | DB seam = `ctx.pool`; clock = `Date.now()` ×3 (elapsed-time only, harness-standard volatile, not a real seam) + 6 distinct clock-relative SQL predicate classes (`NOW() - INTERVAL '1 day'` ×4 identical sites, `CURRENT_DATE - INTERVAL '2 years'`/`'30 days'`, bare `CURRENT_DATE`) — declare non-determinism inventory here, before commit 5's captures. argv/env = `process.env.PIPELINE_CHAIN` → becomes the library's chain-derived `ctx.checks` selection. **The one real design seam:** the WSIB block's `if (runPermitChecks || runSourceChecks)` OR-guard (row 8) — confirm the generator emits `chains:["permits","sources"]` on exactly the 4 WSIB check ids, not a 5th synthetic branch. |
| **4** | PH-6 classification | G6 | CONTRACT/INCIDENTAL/DEFECT per behaviour. Expect: the `emitMeta` read-declaration under-count (declares 7 tables, 16 are actually read, row 5/6) classifies as **CONTRACT-correcting structural side-effect** of moving to descriptor-declared `inputs.reads` — not a Spec 123 §3 "DEFECT fixed during conversion" (no computational change, only the declaration becomes complete). The `${...}` interpolation (row 9) classifies as a mechanical DEFECT with a ledger ID, fixed same-commit-as-descriptor (identical query semantics). |
| **5** | Golden master — 4-tuple, non-determinism declared first | G1′ | **5 capture pairs** (`\|C(S)\|+1 = 4+1`, C4 §3.4): `pre/permits.json`+`post/permits.json`, `pre/coa.json`+`post/coa.json`, `pre/sources.json`+`post/sources.json`, `pre/deep_scrapes.json`+`post/deep_scrapes.json`, `pre/standalone.json`(≡`none`)+`post/standalone.json` — naming mirrors `refresh_snapshot`'s own 4-chain+standalone precedent exactly (`docs/reports/golden/refresh_snapshot/{pre,post}/`). `manifest.json`'s entry declares `supports_full: false` — no forced-FULL variant needed, unlike `link_wsib`/`link_massing`. |
| **6** | PH-7 red-first — `converted.json.pending` entry (R-K/R-K.1) | G7 | `stage: "red_suite"`; new `src/tests/steps/assert_data_bounds/violations.test.ts` with `it.fails(...)` per census claim, tied to the pending slug. |
| **7** | Descriptor + compute verbatim | G2′ | `scripts/quality/assert-data-bounds.descriptor.json` + `scripts/lib/compute/assert-data-bounds.js`, generated from `scripts/lib/assert-data-bounds-fields.js` via `scripts/generate-assert-data-bounds-descriptor.js --check` (§8 A1). Fixes row 9's `${...}` interpolation to bound params in the SAME diff (mechanical, no behaviour change). **Adds `CONSUMED by assert_data_bounds.` to all 9 `scripts/seeds/logic_variables.json` entries in THIS commit** (I1 RECURRING #4 obligation — do not defer to commit 9). ⛔ No schema bump expected. |
| **8** | Peel — one policy concern per commit | green diff every peel | Likely peels: (a) gating/lock → (b) verdict/audit-table assembly per chain (4 tables) → (c) WSIB cross-injection → (d) thresholds/checks-as-config. `sharing.varies_by_chain.phase` as an explicit map, never a ternary. |
| **9** | Differential + cutover | G8, G4d, G-shape | `step-validate --step=assert_data_bounds --write`; CLOUDPARITY (row 12 — no blocking cutover_prereq measured); spec-diff for 41/42/43/44/30 (chain-position corrections, row 1/3) + N/A for 122/123/124 unless §8 forces a ruling; `converted.json` gains the entry, `pending[]` deleted same commit (R-K); **R-D three-way lock, 4th occurrence** — `assert-schema.descriptor.json`'s `checks[0].expect`/`config.probe_presence` grows 106→115 (row 7's 9 names, all measured NEW — no overlap with the existing 106, verified this session) via §8's generator decision, not a hand-splice. |

---

## 4. Database Impact

**NO.** Confirmed by measurement (row 4: zero domain DML — the two `pool.query` writes are the SDK-owned `pipeline_runs` ledger row, retired under conversion). `outputs`/`recovery`/`counters` forced `"none"` by the ASSERT profile (§2). Golden captures read the DB; they write nothing new. No migration, no backfill.

## 5. Standards Compliance
* **Try-Catch Boundary:** N/A — no API route. The dispatch-loop error boundary (`ctx.report(id,{error})`) replaces this file's 3 hand-rolled `fatalErrors.push` catch sites — behaviourally identical (still exactly 3 exception classes: WSIB, inspection, outer), regression-locked by `db/assert-data-bounds-halt.db.test.ts` Case B, which the Regression Guardian must confirm still passes unmodified against the converted shell.
* **Unhappy Path Tests:** red-first suite (commit 6) proves every census claim RED before the descriptor exists; G4d requires a both-directions lock per PH-3 fence — at minimum: `f238b814` (cost_outliers boundary, already exercised by the existing db test Case C/C-boundary — extend, don't duplicate), the Phase-G duplication (`adec1f68`), and the exception-vs-threshold halt split (Case B) itself.
* **logError Mandate:** N/A — `pipeline.log.*`/`ctx.log`, no `src/` catch blocks added.
* **UI Layout:** N/A — no UI file modified (Cross-Domain trigger is consumer-contract only; §12).
* **Cross-Layer Contracts:** the `{metric,value,threshold,status}` row shape consumed by `FreshnessTimeline.tsx`, `DataQualityDashboard.tsx`, and `funnel.ts`'s `pipeline_runs` table-key map must stay byte-identical per chain — proven by G8's zero-unexplained-diff gate; no separate UI test needed.
* **Spec-diff-or-N/A (Spec 123 §7 commit 9(b)):** Spec 42 (step 8→11 correction), Spec 43 (step 26→27 correction), Spec 41/44 (no change expected, already correct), Spec 30 §5.4.1 (no change expected — this step is cited as the exhibit, unaffected in substance), Spec 122/123/124 (N/A unless §8's generator decision is ruled a scope expansion).

## 6. Validator surface declared at plan time (pilot 7 lesson)

| Surface | State entering this step | Must satisfy at commit 9 |
|---|---|---|
| `step-validate --step=assert_data_bounds --write` | not yet run | **≥14/17, G6–G8 full, hard-stop=false** |
| Fast invariant **#22** GOLD-PRE-FRESH | live, 42 PRE captures / 10 steps tracked+clean | 47 PRE captures / 11 steps after this cutover |
| Fast invariant **#9** (registry CLOUDPARITY) | clean (row 12) | stays clean; `EP-PIN-D17` does not gate this slug |
| Rule 10(b) (VRD-SKIP) | BUILT/enforced-green | self-skip on lock 103 contention folds to non-PASS |
| §3.4 "every chain" capture gate (`C4-GATE`) | BUILT | 5 capture pairs present, fingerprint-fresh |
| Policy matrix Rules 1–13 | N/A pre-conversion | Rule 3 (9 vars, all `on_invalid:"fail"` absent a deviation), Rule 4 (every preserved-in-compute row names its `why`), Rule 10 (4 per-chain verdicts, each row-derived — this file's own `permitHasFails ? 'FAIL' : ... ? 'WARN' : 'PASS'` pattern ×4 is exactly what `deriveVerdict` replaces), Rule 11 (N/A — no `pre_write` checks, no writes to order against), Rule 13 (scorecard generated) |
| `check-step-shape.mjs` | 10+10 enforced, this file report-only | moves into enforced set at cutover |

## 7. Panel roster — both altitudes (Spec 08 §6.4)

**Cross-Domain, per C4 Execution Plan row I2: 5 + Regression Guardian.** `Reality-Check` **N/A** — pure observability/verdict step reading existing columns, no derived/enriched field (same reasoning as I1).
* **PLAN altitude:** Integration (wiring/manifest/4-chain-position reality vs §1/row 3 claims; the `${...}` interpolation fix's shape-rule conformance) + Regression Guardian (the exception-vs-threshold halt split is THE fence — `4d5df9e7`/Case B — plus the `f238b814` cost_outliers fence and the Phase-G duplication `adec1f68`; WD-1's `verdict_only` retirement not re-litigated).
* **OUTPUT altitude:** Gemini + DeepSeek (lens set: spec/security/idempotency/error-paths — idempotency lens should specifically probe the WSIB cross-injection's `if (permitsAuditTable) {...} if (sourcesAuditTable) {...}` double-push, row 8) + `code-reviewer-grounded` + `observability-reviewer` (audit-row completeness vs Spec 44 §4/41/42/43, 4 row-derived verdict cascades not parallel booleans, §11 counter scoping across 4 tables) + Integration (real manifest/chain wiring, real downstream consumers — 3, more than I1's 1) + `regression-guardian` (main tree, names the fence for every altered/deleted line — **especially the 3-site `fatalErrors[]` halt split and Case B's exact assertion**).

## 8. Asks — only where no rule answers, one recommendation each

| # | Ask | Why no rule answers it | Recommendation |
|---|---|---|---|
| **A1** | **Build the R-D probe-list generator now, or hand-splice a 4th time?** `assert-schema.descriptor.json`'s `config.probe_presence`/`checks[0].expect` regeneration has fired 2 measured times by hand (pilot 9: 42→86, I1: 86→106) with "zero guard against a transcription slip" (I1 RECURRING #2) and this cutover will be the 3rd-measured/4th-actual occurrence (106→115) — past the "second occurrence promotes it" threshold (Spec 124 §4.6) I1 itself flagged as due. No Spec 124 rule mandates building vs. hand-splicing; it is a scope-boundary judgment (a new script touching a FILE this step doesn't otherwise own). | No rule states the threshold at which a repeated manual step must become a checked generator — Spec 124 §4.6 only names the promotion CRITERION, not who pays it. | **Build it**: `scripts/generate-assert-schema-probe-lists.js`, reading `collectDeclaredLogicVariableNames()` (already exists, `scripts/lib/declared-logic-variables.js`) and rewriting both `assert-schema.descriptor.json` arrays in place, with a `--check` drift mode mirroring peel 8c's `buildDescriptor --check` convention (I1's own precedent). Scoped, mechanical, touches only the probe-list array this step must touch anyway (not new surface) — land it in commit 9 alongside this step's own R-D update, so I2 both PAYS and CLOSES the RECURRING #2 followup rather than deferring it a 3rd time. |

## 9. Low confidence / to verify — every claim not fully executed

| # | Claim | Why unexecuted here | Command that would settle it |
|---|---|---|---|
| 1 | Exact per-chain runtime row count (permits/coa/sources/deep_scrapes), after the WSIB cross-injection and the 2 conditional `permitsAuditTable.rows.push` appends outside the main array literal | requires a live DB read per chain; PH-0/commit-1 work, not this plan's | run standalone + per-chain once against a live DB, count `records_meta.audit_table.rows.length` per chain (mirrors I1's own unresolved item 1) |
| 2 | Whether any further dynamically-composed SQL exists beyond the 2 `${...}` sites found (row 9) | one grep pass, not a line-by-line read of all 9 ad-hoc `pool.query` sites | manual read of the 6 direct + `count()`-mediated sites at PH-0, confirming each is a static template literal apart from the 2 named exceptions |
| 3 | Whether `permit_num <> ALL($1::text[])` (`acceptClause`, `COST_MAG_ACCEPT`) needs its own `checks[].chains` scoping distinct from the two rows it feeds, or can share one census entry with two threshold branches | depends on the generator's row shape, not yet designed | once the census module exists (commit 2), diff its extraction against the 2 real `permitAuditRows.push` call sites (`:902-909`) |
| 4 | Whether the coa-branch duplicate Pre-Permit query (`:400-409`, mirrors `:251-258`'s comment about disjoint guards) produces the SAME `prePermitCount` value as the permits-branch one in a standalone run (both chains active) | requires a live DB read | `SELECT COUNT(*) FROM permits WHERE permit_type='Pre-Permit'` once, compare to both branches' captured value in the standalone golden |
| 5 | Whether `scripts/lib/vocab-coverage.js`-class shared libs are imported here (none found this session — only `zod`, `../lib/pipeline`, `../lib/config-loader`, `../lib/ledger-window`) needs re-confirmation once compute is drafted | not exhaustively re-checked beyond the top-of-file `require()` list | `scripts/hooks/check-step-shape.mjs` run against the draft compute |

## 10. Operating Boundaries
* **Target Files:** `scripts/quality/assert-data-bounds.js` (→ frozen shell) · `scripts/quality/assert-data-bounds.descriptor.json` (new) · `scripts/lib/compute/assert-data-bounds.js` (new) · `scripts/lib/assert-data-bounds-fields.js` (new, census data module) · `scripts/generate-assert-data-bounds-descriptor.js` (new, generator) · `scripts/generate-assert-schema-probe-lists.js` (new, Ask A1) · `src/tests/steps/assert_data_bounds/violations.test.ts` (new) · `src/tests/assert-data-bounds.infra.test.ts` (kept, unmodified unless a claim is found stale) · `src/tests/db/assert-data-bounds-halt.db.test.ts` (kept, unmodified — regression lock) · `scripts/steps/_schema/converted.json` · `scripts/seeds/logic_variables.json` (9 `CONSUMED by` annotations) · `scripts/quality/assert-schema.descriptor.json` (R-D growth, generator-authored) · Specs 41/42/43/44/30 (§5 corrections) · `docs/reports/golden/assert_data_bounds/**` (new, 5 pairs) · a new dated assessment report.
* **Out-of-Scope Files:** `scripts/steps/_schema/step.schema.json` (frozen) · `scripts/manifest.json` (no slug/path/chain_args change — `supports_full`/`supports_dry_run` both already `false`) · every OTHER unconverted step (`assert_engine_health`/`link_neighbourhoods`/`geocode_permits` — I3b/I4/I5) · `src/components/FreshnessTimeline.tsx`, `DataQualityDashboard.tsx`, `src/lib/admin/funnel.ts`, `src/lib/quality/types.ts` (consumers, unmodified) · `scripts/lib/step/**` (library-owned).
* **Cross-Spec Dependencies:** Spec 44 §4 (owner) · Specs 41/42/43 (chains) · Spec 30 §5.4.1 (halt-classification architecture, this step's own exhibit) · Spec 122 (§1.10 ASSERT profile, §5.1/§5.3/§5.5, §8.1) · Spec 123 (§6 gates, §7/§7.2 commits) · Spec 124 (§2 Rules, register) · Spec 47 §A.5 (lock 103, unchanged) · Spec 48 §3.6/§3.7 (verdict cascade, row-derived ×4) · Spec 49 (adjacent consumer — `FreshnessTimeline.tsx` renders both steps' rows, unmodified here).

## 11. Not in scope
Converting `assert_engine_health`/`link_neighbourhoods`/`geocode_permits` (I3b–I5, each its own WF2-C, sequenced strictly after this step's commit 9, and after the WF3 `isInfraStep` retirement precedes I3b only) · any `step.schema.json` edit · any admin UI change · any migration · re-litigating WD-1/VRD-SKIP/C4-GATE (all measured BUILT, row 11/12).

## 12. Handoff note (Cross-Domain Scenario A)
No admin file is created or modified by this conversion. Four JSON contracts render unchanged, asserted by G8's zero-unexplained-diff gate at commit 9: `FreshnessTimeline.tsx`'s legacy metric-row path (phase 15/8/14/3 per chain — row 8), `DataQualityDashboard.tsx`'s `assert_data_bounds: { label: 'Daily' }` cadence entry, `funnel.ts`'s summary string + `pipeline_runs` table-key map, and `quality/types.ts`'s `'Data Quality Checks'` label. If G8 finds any diff in row shape or the per-chain phase numbers, that is a build-blocking defect, not an accepted behaviour change.

---

> **PLAN LOCKED. Do you authorize this WF2-C plan? (y/n)**
> §11 note: two non-obvious choices — (1) Ask A1 (build `scripts/generate-assert-schema-probe-lists.js` now, closing I1's RECURRING #2 followup in this same cutover rather than deferring a 3rd time); (2) starting the row-builder census (`scripts/lib/assert-data-bounds-fields.js`) at commit 2 (PH-3) rather than commit 7, per I1's RECURRING #1 obligation, even though this step's 49-metric scale would tolerate hand-authoring on its own.
> DO NOT generate code. DO NOT run commands. TERMINATE RESPONSE.

## Fold A (plan-altitude panel 2026-09-12: Integration + Regression Guardian) — BINDING
1. **Guardian FAIL → ledger obligation (commit 6 classify, commit 7 repoint):** `src/tests/assert-data-bounds.infra.test.ts` = 18 `expect(SRC)` source-text assertions over the STEP file; after commit 7's thin shell they go dark. Same as I1 Fold A item 2 (`.cursor/batch1_i1_assert_global_coverage_active_task.md` Fold A): add a `COMPUTE` read of `scripts/lib/compute/assert-data-bounds.js` and repoint every BEHAVIOUR assertion (the externalized thresholds E7–E10, `4f6114ce`) at it; SKELETON assertions (lock constant, `pipeline.step`) stay on the step file. Count + classify all 18 in commit 6's body.
2. **Unnamed fences, now named (Guardian 6/7):** `ea087109` — `ghost_permits_30d` excludes terminal permits (`lifecycle_phase NOT IN ('P19','P20')`), preserve verbatim in the census SQL and lock it (G4d); **LPA-D6** (`tasks/lessons.md:137`) — this file is the PRECEDENT for the library's severity-separated `checks_failed`/`checks_warned`: commit 7's G2′ must show `checks_failed`/`checks_warned` in POST `records_meta` equal to the PRE file's own counts per chain (verify, never assume).
3. **Integration refutes one claim:** no ast-grep/eslint rule fires on the `:888`/`:894` `${Number(...)}` interpolations (parameterised-queries is prose-only, `scripts/CLAUDE.md`). The bind-param fix stays in commit 7 as correct practice and a behavioural no-op (identical predicate) — the plan's "will not build green otherwise" wording is withdrawn. `acceptClause` is a SQL fragment with its value already bound — never bind the fragment.
4. **Integration corrections:** the harness path is `scripts/analysis/capture-step-golden.js`; `--chain=deep_scrapes` spawns only this step with `PIPELINE_CHAIN=deep_scrapes` (no python/xvfb); local `permit_inspections` = 816 rows and the code no-ops safely at `inspCount===0`. System map is GENERATED from each spec's `### Target Files` — commit 1 adds `scripts/quality/assert-data-bounds.js` to Spec 44's Target Files (it is named in §4's step table but omitted from Target Files) then `npm run system-map`. WSIB dual-chain check expression via `checks[].chains` — verify against `step.schema.json` `definitions.check.chains` before commit 7 (Integration could not confirm a live ×4 precedent in budget).
5. Integration PASS on all §0 numbers (1,055 L; lock 103; positions; 6 DML = SDK ledger only; 16 tables / 56 SQL sites; 9 vars, 0 CONSUMED tags; 49 metrics; 106→115 probe growth; `collectDeclaredLogicVariableNames()` at `scripts/lib/declared-logic-variables.js:31` → A1 generator feasible touching only the two arrays).
