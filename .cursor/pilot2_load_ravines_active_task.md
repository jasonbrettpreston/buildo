# Active Task: Pilot 2 — convert `load_ravines` to the Spec 122 step standard (LOADER/INGESTOR)
**Status:** Implementation (authorized 2026-08-25 "proceed. Looks good!" — rulings: A-1 (b) library owns acquire→validate→write, compute = pure functions · A-2 `ctx.acquire` seam, no new category · A-3 `override.force_run` env `RAVINE_FORCE_RELOAD` · A-4 `checks[].limit_from_config` · A-5 `override.accept_anomaly[]` box · fences batch-adjudicated at commit 2)

> **Operator context (2026-08-25):** ravines is a small dataset that rarely if ever changes — new ravines don't pop up. Therefore the **skip terminal is the normal, correct outcome**, not a defect; `force_run` (A-3) exists to PROVE the write path in the differential and for a deliberate operator reload, never to change cadence. Observability priority: "skipped for the right reason, visibly" (validator + content-hash decisions in the audit table), then the write counters when a real load happens.

## Context

* **Goal:** Convert `load_ravines` — the INGESTOR representative of C1's eight archetype pilots and **the first pilot that WRITES a domain table** — from a 605-line `pipeline.run()` island into the Spec 122 §5.1 frozen shape. Write class **B** (`upsert_scoped_departure_delete`, §1.4/§8.2), 1 chain, 10 exit paths.
* **Target Spec:** `docs/specs/01-pipeline/122_pipeline_step_optimization.md` §1.2a (P1–P5), §1.4 (`write_discipline`), §1.5 (`staleness` 3 axes), §1.6 (`on_*_error`), §5.1–§5.5, §8.1/§8.2 + `123_step_opt_assessment_validation.md` §6, §6.1, §7. The step's own spec: `docs/specs/01-pipeline/59_source_ravine_protection.md` (§3, §9 frozen producer contract).
* **Domain Mode:** Backend/Pipeline → `scripts/CLAUDE.md`. **Workflow:** WF2. **Branch:** `wf2/deep-scrapes-restore-l0`.
* **What pilot 1 proved and pilot 2 must NOT re-derive:** the library, the frozen file shape, `ctx.config`, the compute dispatch table, the report-table conventions the violations test reads by column header. **Do not copy pilot 1's numbers** — every number below was re-executed in this planning session (2026-08-25).

### ⚠️ The three findings that shape this whole plan (measured, not inherited)

1. **The compute shape (§5.5 (1)) is ASSERT-shaped and does not fit a LOADER.** `src/tests/step-conformance.infra.test.ts:485-503` HARD-GATES `module.exports.checks` as a dispatch table whose keys are **exactly the descriptor's check ids, in order**, each a function whose `.name === id`, and §5.5 (1) says `compute(ctx)` "iterates `ctx.checks` and does nothing else". A loader is one sequential pipeline (acquire → validate → write → measure), not N independent probes. **PLAN-altitude adjudication required (see Asks A-1).**
2. **A compute may not touch the filesystem.** `scripts/ast-grep-rules/compute-shape.yml` rule `compute-forbidden-require` bans `require('fs'|'node:fs'|'child_process'|'pg'|'dotenv'|'../pipeline'|'../step'|'../resolve-db')` over `scripts/lib/compute/**`. `load-ravines.js` uses `fs.mkdtempSync/rmSync/createWriteStream/readdirSync/mkdirSync`, `os.tmpdir`, `crypto`, `node-stream-zip`, `shapefile`, `stream/promises` — **acquisition has no legal home**. Spec 122 §1.5 already says so: *"this is why `acquisition` is a P0 missing category"*. It is not one of the 18. **Ask A-2.**
3. **The write path is currently unreachable, so the differential cannot see it.** Live probe (`127.0.0.1:54322/postgres`, migrations **242**): `pipeline_runs WHERE pipeline='sources:load_ravines'` → **8 rows, all `completed`, last `2026-07-08T13:55:44Z`**; the last two runs' `records_meta.ravine_load.skipped_reason = "unchanged_last_modified"`, `polygons_inserted/updated/deleted = 0/0/0`, 3 audit rows + 2 `sys_`. The CKAN `last-modified` is **`Mon, 14 Mar 2022 15:25:09 GMT`** — the source has not changed in 4 years, so **every run takes the tier-1 skip**. `grep -c "process.env" scripts/load-ravines.js` → **2**, and neither is a force: `RAVINE_ACCEPT_FEATURE_COUNT_DRIFT`, `RAVINE_ACCEPT_MASS_DELETE` are drift *acceptors*. **There is no `force_run`.** G8 for the one thing this pilot exists to prove requires a mechanism that does not exist. **Ask A-3.**

## Key files — every path verified this session (`ls`/`wc -l`/`node -e`)

| Path | Measured |
|---|---|
| `scripts/load-ravines.js` | **605 lines** — the step under conversion; `:588` `pipeline.run('load-ravines', main)` under `require.main === module`; `:591-605` `module.exports` = **13 named exports** |
| `scripts/manifest.json` | `load_ravines` at `:13` (`file: scripts/load-ravines.js`, `supports_full:false`, `supports_dry_run:false`, `telemetry_tables:["ravines"]`); chain member at `:101` |
| `migrations/167_create_ravines_table.sql` | `ravines(id BIGSERIAL PK, source_id BIGINT UNIQUE NOT NULL, geom GEOMETRY(MultiPolygon,4326) NOT NULL, source_dataset_version TEXT NOT NULL, created_at, updated_at)` + 2 GIST indexes |
| `scripts/lib/step/index.js` | 351 lines — `step`, `deriveMeta`, `deriveCounters`, `skipRecordsMeta`, `assertDatabaseTarget`; header names the **GROWTH WAVES**, incl. *"generated write SQL from `write_discipline` → INGESTOR pilot"* |
| `scripts/lib/step/verdict.js` | 189 lines — `evaluateLimit` implements **only** `viol ==`/`viol <=` + `{warn,fail}`; `pct`/`pop`/`ratio` return `unevaluable` |
| `scripts/lib/step/config.js` | 148 lines — `resolveConfig`; a declared name in no registry **throws** |
| `scripts/lib/step/ledger.js` | 133 lines — `RUN_STATUS`, `ownsLedgerRow`, `openLedgerRow`, `finalizeLedgerRow` |
| `scripts/steps/_schema/step.schema.json` | **18 required** categories (`node -e` on `.required.length`); `terminal.kind` enum has 9 values; `writeDiscipline.class` enum has the 13 ported mechanics |
| `scripts/steps/_schema/converted.json` | `converted: ["scripts/quality/assert-schema.js"]` — **1** entry |
| `scripts/steps/_schema/fixtures/valid/` | `assert_schema` · `enrich_heritage` · `grandfathered-unguarded`. **No `load_ravines` draft descriptor exists** — pilot 2 authors it from scratch |
| `scripts/analysis/capture-step-golden.js` | 409 lines; its **only** table query is `:278` `FROM pipeline_runs WHERE id > $1` — **zero domain-table capture** |
| `scripts/ast-grep-rules/compute-shape.yml` | 7 rules; `compute-forbidden-require` bans `fs` |
| `src/tests/load-ravines.infra.test.ts` | 153 lines, **16 `it(`**; `SCRIPT = readFileSync(scripts/load-ravines.js)` at `:19-22` — ~40 source-text assertions |
| `src/tests/load-ravines.logic.test.ts` | 132 lines, **22 `it(`**; `require('../../scripts/load-ravines.js')` at `:11` |
| `src/tests/source-version.logic.test.ts` | `LOADER_SOURCES` at `:45-51` reads the file; adoption-locks at `:228-247`, `:476-480`; wrapper-signature test `:114` |
| `src/tests/pipeline-advisory-lock.infra.test.ts` | `:35` `'scripts/load-ravines.js': 59`; source-text loops `:253`, `:268` (**already widened for `pipeline.step(` at pilot 1**), `:301` |
| `docs/reports/defect-ledger.md` | 21 lines, **13 rows, all `AS-D*`** — no `LR-D*` register yet |
| `docs/reports/golden/` | `assert_schema/` only |

**Does not exist yet (measured):** `scripts/load-ravines.descriptor.json` · `scripts/load-ravines.notes.json` · `scripts/lib/compute/load-ravines.js` · `src/tests/steps/load_ravines/` · any `load_ravines` assessment report · any acquisition seam.

## Technical Implementation

* **New files:** `scripts/load-ravines.descriptor.json` · `scripts/lib/compute/load-ravines.js` · `src/tests/steps/load_ravines/violations.test.ts` · `docs/reports/2026-08-<dd>-pilot2-load-ravines-assessment.md` · `docs/reports/golden/load_ravines/*.json` · `scripts/load-ravines.notes.json` (recommended — see vacuity) · (Ask-dependent) `scripts/lib/step/acquire.js`, `scripts/lib/step/write.js`.
* **Modified:** `scripts/load-ravines.js` (→ frozen shape, SPEC LINK header kept) · `scripts/lib/step/{index,verdict}.js` (growth) · `scripts/seeds/logic_variables.json` (+6) · `src/features/admin-controls/components/GlobalConfigCard.tsx` (+1 GROUP) · `scripts/analysis/capture-step-golden.js` (table capture) · `scripts/steps/_schema/converted.json` (+1, final commit) · the 4 test files above.
* **Database Impact:** **NO migration.** The step writes `ravines` (4 of 6 columns; `created_at`/`updated_at` defaults) — the table exists at mig 167. Measured live: **854 rows, 1 distinct `source_dataset_version`, 7,640 kB**.

## Gate ledger — G0–G8 + G4d + G-shape

Requirement text quoted from Spec 123 §6/§6.1; cite by grep anchor. "Measured" = executed in this planning session, 2026-08-25.

| Gate | Spec 123 requirement (anchor) | Command that proves it | CURRENT measured state |
|---|---|---|---|
| **G0** | §6 `Boundary freeze` — *"every table/column written, every audit row, exit codes, stdout — enumerated"* | `wc -l scripts/load-ravines.js` · `grep -cF` per construct · `grep -oE "push\('[a-z_]+'"` | ⛔ **ABSENT — no assessment report exists for this step** (`ls docs/reports \| grep -i ravine` → none). Measured surface today: **605 lines · 5 `throw new Error` · 5 `try` / 3 `catch` / 4 `finally` (`:194`, `:216`, `:229`, `:372`) · 7 `emitSummary` call sites · 2 `fetch(` · 1 `pool.query` + 2 `client.query` · 0 `Date.now()` / 0 `new Date(` · 0 `process.argv` · 2 `process.env` · 20 distinct audit metrics · 10 terminals** (enumerated below). Domain writes: `ravines` — 1 UPSERT (`:478-486`, guarded `IS DISTINCT FROM`, `RETURNING (xmax=0)`), 1 scoped DELETE (`:496`, `source_id <> ALL($1)`). ⚠️ `ravine_geometry_skipped_source_id` (`:441`) is pushed **once per skipped feature** — the audit table is unbounded in the number of rows. |
| **G1** | §6 `Archaeology` — *"churn + fix density + fence density + 20% coupling computed (batch)"* | `git log --oneline -- scripts/load-ravines.js \| wc -l` · `git log --pretty=%s … \| grep -c "^fix("` · `git log --pretty=%B … \| grep -ci "^Severity:"` | ✅ **GREEN: 2 commits · 0 `fix(` · fix density 0% · fence density 2.** (`--follow` also returns 2 — no rename history.) The two commits are `1ceebd17` `feat(59_source_ravines)` and `0b230472` `feat(43_chain_sources)`. **20% change-coupling: NOT computed** — no batch artifact, no generator (same U-item as pilot 1). |
| **G2** | §6 `Structure` — *"churn×complexity plot; top-right quadrant named"* | *(no command exists)* | ⛔ **ABSENT.** `ls scripts/analysis \| grep -i "churn\|complex\|risk"` → nothing. Record **`ASSESSMENT-INCOMPLETE`** per §6.2 clause 3, as pilot 1 did — do not silently score it. |
| **G3** | §6 `Intent Ledger` — *"100% of top-right + fence>0 constructs have a recovered why or an explicit INTENT-UNKNOWN"* | `git log -S'<constant>' -- scripts/load-ravines.js` per constant; adjudication per §7.1 | ⛔ **RED: 2 fences, 0 ledger entries.** **F-1 `1ceebd17` Severity HIGH** — *"L7c mass-delete abort was non-functional pre-review; fixed"* → the `(massDeleteCheckPassed \|\| acceptMassDelete)` return at `:553`, locked textually by `load-ravines.infra.test.ts` (`it('L7c mass-delete without RAVINE_ACCEPT_MASS_DELETE terminates the run as failed…')`). **F-2 `0b230472` Severity HIGH** — the tier-2 content-hash gate + streamed hash-through-to-disk (§9.5: *"never `Buffer.from(await res.arrayBuffer())`"*), locked by 5 source-text adoption-locks in `source-version.logic.test.ts`. §7.1: **a human adjudicates**; the agent that discovers may not retire. |
| **G4** | §6 `Risk class` — *"A/B/C with chance and impact factors shown"* | derived from G1 + measured blast radius | ✅ **CLASS A.** *Chance* is low (2 commits, 0 fixes) but *impact* is the highest of any pilot so far and decides the class: this is the **first pilot to write domain rows**. Measured downstream — `information_schema` on the live DB returns **7 ravine-derived columns across 3 tables**: `parcels.{is_in_ravine_protection_area, ravine_distance_m, ravine_dataset_version_when_enriched}` (**486,530 rows; 32,339 true; 486,530 distances populated**), `permits.{is_in_ravine_protection_area, ravine_distance_m}` (**254,082 rows; 18,327 true**), `coa_applications.{…}`. `enrich-ravines.js:33-50` HALTs the chain on a missing/mismatched producer row. Class A ⇒ §6 G7's mutation clause applies. |
| **G5** | §6 `Seam map` — *"DB, clock, network, argv/env each have a named seam"* | `grep -cF` per seam | ⚠️ **PARTIAL — four of five seams are green and the fifth has no name in §6 at all.** **DB** = the `pool` from `pipeline.run` (1 `pool.query` = `VALIDATION_SQL`; 2 `client.query` inside `pipeline.withTransaction`). **Clock** = `pipeline.getDbTimestamp(pool)` at `:276`; **0** `Date.now()`, **0** `new Date(` — Spec 47 §R3.5 clean, and `:277`'s comment says why. **Network** = 2 `fetch(` (`:191` HEAD, `:207` GET), both `AbortController`-timed. **argv/env** = 0 `process.argv`, 2 `process.env` (`:283`, `:284`). ⛔ **FILESYSTEM is the fifth seam and §6 G5 does not enumerate it**: `mkdtempSync :339`, `mkdirSync :225`, `createWriteStream :213`, `readdirSync :239`, `rmSync :373`, plus `node-stream-zip` and `shapefile` reading paths. It has no `ctx` slot and `compute-forbidden-require` bans reaching it. **This is finding #2 above.** |
| **G6** | §6 `Classification` — *"every behaviour CONTRACT / INCIDENTAL / DEFECT; every DEFECT has a ledger ID"* | `grep -c "^| " docs/reports/defect-ledger.md` | ⛔ **RED: 0 `LR-D*` rows.** The register exists (`docs/reports/defect-ledger.md`, **13 rows, all `AS-D*`**) so the pilot-1 blocker is retired — but nothing classifies this step. Candidates already visible and to be adjudicated at commit 4, not asserted here: the unbounded `ravine_geometry_skipped_source_id` rows (`:441`); the `.catch(warn → null)` prior-run swallow (`:291-294`) which silently degrades every drift check to "first run"; the FAIL-verdict-with-exit-0 shape (see Declared diffs D-8); `datasetVersion`'s `String(runAt)` fallback (`:459`) which makes the version unstable on a validator-less run; `#411`'s BIGINT > 2^53 precision gap. **§6: "Any zero in G6–G8 is a hard stop."** |
| **G7** | §6 `Test adequacy` — *"class-A: mutation ≥80% on covered code; every class-A behaviour proven red"* | `ls src/tests/steps/load_ravines` · `grep -n "mutate" stryker.config.mjs` | ⛔ **RED, and the mutation clause is unsatisfiable as written** (`stryker.config.mjs` `mutate:` lists 3 files, all `src/features/leads/lib/*.ts`; `scripts/` is outside `src/` — Spec 123 §4.8 already says this is *"a NEW dependency, not existing capability"*). **Prove-red half:** `src/tests/steps/load_ravines/` does not exist → **0 of 44** 55-A claims red or green. |
| **G8** | §6 `Differential` — *"zero unexplained diffs; every explained diff points at a Defect Ledger ID"* | `node -r dotenv/config scripts/analysis/capture-step-golden.js --step=scripts/load-ravines.js --chain=sources` (+ `--chain=none`) | ⛔ **RED FOR TWO INDEPENDENT REASONS, both measured.** (a) **The harness cannot see a LOADER's output**: its only table query is `capture-step-golden.js:278` over `pipeline_runs`; nothing captures `ravines`. A 4-tuple missing the written table is not a 4-tuple for write class B. (b) **The write path does not execute**: 8/8 recorded runs are tier-1 skips against a source frozen at `Mon, 14 Mar 2022 15:25:09 GMT`, and no force override exists. Commit 5 must fix (a) and Ask A-3 must resolve (b), or G8 covers only the skip terminal and the pilot proves nothing about writes. |
| **G4d** | §6.1 — *"every fence found in P3 has a **both-directions** lock test"* | `git log --format=%B -- scripts/load-ravines.js \| grep -ci "^Severity:"` then one lock per fence | ⛔ **RED: 2 fences, 0 both-directions locks.** No `*.regression.test.ts` covers this file. What exists instead is a large **source-text** surface across **4 files** that all go red at commit 7 and must be **re-homed, not deleted**: `load-ravines.infra.test.ts` (16 `it`, ~40 assertions over `SCRIPT`), `load-ravines.logic.test.ts` (22 `it` over the 13 named exports), `source-version.logic.test.ts` (`LOADER_SOURCES` + 5 adoption-locks + the `:114` wrapper-signature test), `pipeline-advisory-lock.infra.test.ts` (`:35` registry entry SURVIVES; `:253`/`:268` loops SURVIVE — `:268` already accepts `pipeline.step(` from pilot 1; `:301` registry match SURVIVES because `const ADVISORY_LOCK_ID = 59;` stays textual per §5.4). |
| **G-shape** | §6.1 — *"the converted file passes the ast-grep shape rule (Spec 122 **§5.1**, not §4.1 — doc error C-7) and `pipeline.run(` no longer appears in it"* | `node scripts/hooks/check-step-shape.mjs` | ⛔ **RED by design, correctly armed.** Measured output: `footgun[step-shape] (info): 61/61 unconverted manifest step files violate the frozen shape … ✅ Step-shape gate clean (1 converted step file(s) enforced; 1 compute module(s) enforced…)`. Live: `load-ravines.js:588` `pipeline.run('load-ravines', main)`. Arms when the path is appended to `converted.json` at commit 9 (→ 2/62). |

**Score today: G1 ✅(1) + G4 ✅(2) = 3/17**, with G5 partial and **G6/G7/G8 at zero**. §6 threshold: *"Ship at ≥14/17 with G6, G7 and G8 full."*

## P4 tunable inventory (§1.2a P4 — every literal, no exceptions)

**The measured baseline is worse than pilot 1's.** `assert_schema` declared `config: "none"` while hard-coding three knobs. `load_ravines` declares a `ConfigSchema` of **six** named variables at `:46-53` and calls `loadMarketplaceConfigs(pool, 'source-ravines')` at `:272` — but **not one of the six is registered anywhere**:

* `scripts/seeds/logic_variables.json` has **403 keys, all snake_case, 0 camelCase** (`node -e` walk). Each of the six camelCase names greps to **0**.
* Live DB: `SELECT count(*) FROM logic_variables` → **422**; `WHERE variable_key ILIKE '%ravine%'` → **0 rows**.
* Therefore `ConfigSchema.parse(logicVars || {})` **always** falls through to its Zod `.default()`. All six are hidden literals wearing a variable's name — the exact P1 failure `scripts/lib/step/config.js`'s "declared but in NO registry" throw exists to close.
* This was a **recorded, deliberate deferral**: `docs/reports/review_followups.md` **#421** — *"load-ravines (the direct precedent) seeds none — its ConfigSchema Zod defaults apply when keys are absent… Defer seeding to when operator tuning is actually needed."* **P4 supersedes #421**; close it in this pilot.

Naming follows the pilot-1 precedent (`<slug>_<what>`, seeded with a `CONSUMED by <slug> (…, via ctx.config)` sentence, added to a `GlobalConfigCard` GROUP — measured at `src/features/admin-controls/components/GlobalConfigCard.tsx:125-141`, group **"Data Quality Thresholds"**; a new **"Source Ingestion"** group is the cleaner home for six).

| # | Literal (site) | Disposition | Proposed var / bounds / group |
|---|---|---|---|
| T1 | `20` — `ravineSkipCheckThresholdYears` `:47`, used `:316` | **TUNABLE** | `load_ravines_dataset_age_warn_years` · default 20 · min 1 · max 200 · `on_invalid: default` · Source Ingestion |
| T2 | `0.5` — `ravineDriftFeatureCountPct` `:48`, used `:405` | **TUNABLE + verdict bound** (see Ask A-4) | `load_ravines_count_drift_fail_pct` · default 0.5 · min 0 · max 1 · `on_invalid: fail` |
| T3 | `0.5` — `ravineDriftGeometryUpdatePct` `:49`, used `:503` | **TUNABLE + verdict bound** | `load_ravines_geometry_update_warn_pct` · default 0.5 · min 0 · max 1 · `on_invalid: default` |
| T4 | `0.05` — `ravineInvalidGeometryFailPct` `:50`, used `:447` | **TUNABLE + verdict bound** | `load_ravines_invalid_geometry_fail_pct` · default 0.05 · min 0 · max 1 · `on_invalid: fail` |
| T5 | `0.5` — `ravineMassDeletePct` `:51`, used `:512` | **TUNABLE + verdict bound** | `load_ravines_mass_delete_fail_pct` · default 0.5 · min 0 · max 1 · `on_invalid: fail` |
| T6 | `60000` — `ravineDownloadTimeoutMs` `:52`, used `:300`, `:347` | **TUNABLE** *and* descriptor data (P1.1 vs P4 tension — Ask A-4) | `load_ravines_download_timeout_ms` · default 60000 · min 1000 · max 600000 · `on_invalid: clamp`; mirrored into `execution.network.timeout` which must AGREE |
| S1 | `59` — `ADVISORY_LOCK_ID :35` | **STRUCTURAL** | → `identity.lock: 59`; kept textually per §5.4 so `pipeline-advisory-lock.infra.test.ts:253/:301` stay green |
| S2 | `'1.2'` — `SPEC_VERSION :36` | **STRUCTURAL — a frozen consumer contract** | → `identity.spec_version`; `enrich-ravines.js:42` hard-pins `rl.spec_version !== '1.2'` → **any change HALTs the sources chain**. Never a tunable. |
| S3 | `365.25` `:159`, `:316` | **STRUCTURAL** | days/Julian-year; the comment at `:159` already states why (leap years). Not a knob — changing it changes what "a year" means, not how much of it we accept. |
| S4 | `86400000` `:153` | **STRUCTURAL** | ms/day |
| S5 | `1000` ×2 — `round3 :584` | **STRUCTURAL** | display precision of audit-row values; never compared against anything. Record the why so P4's grep does not re-open it. |
| S6 | `4326` `:475` | **STRUCTURAL** | → `guards.srid: 4326`; matches `migrations/167` `GEOMETRY(MultiPolygon,4326)` |
| S7 | `3` — `ST_CollectionExtract(repaired, 3)` `:67` | **STRUCTURAL** | PostGIS polygon type code, an API constant |
| S8 | `'md5'` `:209` / `'sha1'` `:459` | **STRUCTURAL — compatibility pins** | the hash is compared against stored `content_hash` baselines (live: `97b4ac7f…`); changing the algorithm invalidates every baseline and forces a full reload. `0b230472`'s body says *"md5 pinned throughout"*. |
| S9 | `65535 / columnsPerRow` → **16383** (`pipeline.js:702-704`, called `:468`) | **STRUCTURAL** | the Postgres bind-parameter ceiling → `execution.batch: 16383` with a `why`, not an operator knob |
| S10 | `-4` `:244`, `> 1`/`=== 0` `:241-242`, `'1'` `:283-284` | **STRUCTURAL** | extension length; is-there-any tests; env truthy compare — none is a threshold |
| **E1** | `process.env.RAVINE_ACCEPT_FEATURE_COUNT_DRIFT` `:283` | ⛔ **NO DESCRIPTOR HOME** | `override` requires exactly `{force_full, force_run, dry_run}` (schema dump). An *accept-anomaly* override is none of the three. **P1.4 new-box ask (A-5).** |
| **E2** | `process.env.RAVINE_ACCEPT_MASS_DELETE` `:284` | ⛔ **NO DESCRIPTOR HOME** | same; and it is fence F-1's subject, so it cannot be dropped |

**Count: 6 tunables to externalize (0 registered today) · 10 structural groups justified · 2 env overrides with no legal box.**

## The 55-A "proven red" step

**Measured, not transcribed.** `node scripts/violations/plan-claims.mjs --checklist` prints `SELF-TEST PASSED — 17 totality assertions incl. 4 negative controls (6 original + 11 K-axis)` first, so the generator's output is admissible per Spec 121 §12b.6, and then the headings:

* `## 55-A — the hard per-conversion gate (44)` · `## 55-B — partial now, closure at the named k (5)` · `## 55-C — DEFERRED, arming k named (6)` → **55-A = 44.**
* The generator is **claim-scoped, not step-scoped** — there is no `load_ravines` row and there will not be one. §5.2's list is a **template copied per conversion**.

**Mechanism, three artifacts:**
1. `src/tests/steps/load_ravines/violations.test.ts` — the 44 A items + the 5 B partials + 2 both-directions fence locks. Does not exist; `vitest.config.ts` includes `src/**/*.test.ts`, so it is picked up the moment it lands. Model it on `src/tests/steps/assert_schema/violations.test.ts` (1,389 lines, 57 `it`).
2. `src/tests/step-conformance.infra.test.ts` — sibling-descriptor uniqueness, AJV validation, `pg.Pool` require-probe, named exports, lock↔registry, `checks.length > 0`, `loaded.length === converted.length`, and **§5.5 dispatch ≡ declared checks in order** (`:485-503`).
3. `scripts/hooks/check-step-shape.mjs` + `converted.json` — armed by appending `scripts/load-ravines.js` at commit 9 (1 → **2** enforced).

⚠️ **The report is machine-read by column header, not by section name.** `violations.test.ts` `mdTables()`/`reportTable()` (`:272-330`) locate tables by their **column headers**, so the pilot-2 assessment report MUST carry: a table with `key` + `disposition` (non-determinism inventory) under a heading containing **"Non-determinism inventory"** (`firstCommitTime(REPORT_REL, 'Non-determinism inventory')`); `commit` + `done-test`; `table` + `rows` (Appendix-H boundary freeze); `lines` + `category` + `evidence` (line accounting) plus a literal `"605 lines"` string; and the Intent Ledger's four columns `construct` · `discovered by` · `disposition` · `adjudicated by`, with a row naming each fence commit SHA.

⚠️ **Vacuity risk on 7 of the 44** — #30/#31/#33/#34/#35/#37/#38 are about `notes.json`. Declaring `interpretation: "none"` makes them vacuously green. **Recommend authoring a real `scripts/load-ravines.notes.json`**: this step has genuine interpretation content that exists only as comments today (`:37-39` the `#409` pipeline-name correction; `:12-14` the DEC-B divergence from `geometry-validator.js`; the 2022-frozen source; `:431` the "unnest miss is anomalous" note) — moving it into `notes.json` is exactly P1.2/§5.5 (5).

**The 6 55-C items are NOT gated here** (`#160 k=2`, `#161 k=20`, `#168 k=27`, `#177/#178/#179 k=27`, the last three blocked on `nock`). Re-verify `nock`'s absence before claiming any of them.

## The nine commits (Spec 123 §7 / Spec 122 §8.1)

Commit format enforced by `scripts/hooks/validate-commit-msg.sh`: `type(NN_spec): description`.

| # | Phase / Gate | Files touched | Test(s) | Commit message |
|---|---|---|---|---|
| **1** | PH-0 boundary freeze → **G0** | `docs/reports/2026-08-<dd>-pilot2-load-ravines-assessment.md` (§1: grep transcript at 605 lines; the 10 terminals; the 20 audit metrics; `ravines` row count 854; the Appendix-H `table`/`rows` table) | none (doc) | `docs(122_step_optimization): C1 pilot 2 PH-0 - load_ravines boundary freeze (605 lines, 10 terminals, 1 domain table)` |
| **2** | PH-3 intent ledger → **G3** (mandatory: fence density 2 > 0) | same report (§2 Intent Ledger, four required columns) + `docs/reports/defect-ledger.md` (`LR-D*` rows) | none (doc); **a human adjudicates** per §7.1 | `docs(122_step_optimization): C1 pilot 2 PH-3 - intent ledger for 2 fences; LR-D1..Dn registered` |
| **3** | PH-5 seam map → **G5** | same report (§3), incl. **the FS seam and its lack of a home** | none (doc) | `docs(122_step_optimization): C1 pilot 2 PH-5 - seam map (DB/DB-clock/2 fetch/6 fs sites/2 env)` |
| **4** | PH-6 classification → **G6** | same report (§4) — CONTRACT / INCIDENTAL / DEFECT, every DEFECT ledger-IDed | none (doc) | `docs(122_step_optimization): C1 pilot 2 PH-6 - CONTRACT/INCIDENTAL/DEFECT, every defect ledger-IDed` |
| **5** | Golden master (4-tuple) → **G1′** | `scripts/analysis/capture-step-golden.js` — **grow it**: per `outputs.writes[].table`, capture `count(*)` + an ordered content hash; + `docs/reports/golden/load_ravines/{sources,standalone}.json`; + the non-determinism inventory (§5, `key`/`disposition`) | the harness's own self-test + `--compare` exit 0 on a repeat capture | `feat(122_step_optimization): C1 pilot 2 - golden master x2 + table-state capture for write-class steps` |
| **6** | PH-7 test design + **prove red** → **G7** | `src/tests/steps/load_ravines/violations.test.ts`; 2 both-directions fence locks | `npx vitest run src/tests/steps/load_ravines/` — **must be RED** | `test(122_step_optimization): C1 pilot 2 PH-7 - 44 55-A violations + 5 partials + 2 fence locks (red)` |
| **7** | Descriptor + compute verbatim + **library growth** → **G2′** | `scripts/load-ravines.descriptor.json` · `scripts/lib/compute/load-ravines.js` · `scripts/load-ravines.js` → frozen shape · `scripts/lib/step/*` (LG-1…LG-6) · `scripts/seeds/logic_variables.json` (+6) · `GlobalConfigCard.tsx` (+1 group) · the 4 test files re-homed | `npx vitest run src/tests/step-conformance.infra.test.ts src/tests/load-ravines.*.test.ts src/tests/source-version.logic.test.ts src/tests/pipeline-advisory-lock.infra.test.ts` | `refactor(122_step_optimization): C1 pilot 2 - load_ravines to frozen shape; compute extracted verbatim` |
| **8** | Peel — one concern per commit, green diff after **every** peel | **8a** gating (the two-tier staleness gate → library) · **8b** verdict/audit (metric rows → declared checks; drop the unbounded per-source_id rows) · **8c** thresholds/checks (the six `ctx.config` reads + `pct <=` limits) | full differential re-run after each | `refactor(122_step_optimization): C1 pilot 2 peel a/b/c - <concern>` (three commits) |
| **9** | Differential + cutover → **G8, G4d, G-shape** | `scripts/steps/_schema/converted.json` (+1 → 2) | `node scripts/hooks/check-step-shape.mjs` exit 0 with **2** enforced; differential green in `sources` + standalone, **on both the skip terminal and a forced full load** | `feat(122_step_optimization): C1 pilot 2 cutover - load_ravines converted; shape gate armed (2/62)` |

> §7's note holds: *"old and new are the same file at two commits, invoked identically by the same `spawnStepChild`."* Verified — `manifest.json` declares **no `chain_args`** for `load_ravines` (only `enrich_parcels :18` and `link_massing :30` have any), so `extraArgs` is empty and there is zero invocation divergence to normalise.

## Declared diffs the differential WILL show (state before capture, §5.3)

| # | Diff | Old | New | Disposition |
|---|---|---|---|---|
| D-1 | `PIPELINE_META.reads` | `{"ckan:ravine-natural-feature-protection-area-wgs84": []}` (`:577`) | `{}` + `external: ["ckan:ravine-…"]` | **CONTRACT change** — `deriveMeta` puts externals on the third argument |
| D-2 | `PIPELINE_META.writes` | `{ravines: [source_id, geom, source_dataset_version, created_at, updated_at]}` (`:578`) | derived from `outputs.writes[].columns[].name` — **must reproduce all 5, incl. `created_at` which the step never writes** (DB default) | declared normalisation; if the descriptor lists only the 4 written columns this is a real diff — state the choice, do not discover it |
| D-3 | audit-table shape | 20 possible metrics, hand-pushed, **verdict from `verdictCascade(rows)` in the step** (`:143-147`, `:563`) | one row per **selected check**, keyed by `check.id`, `threshold` rendered from `check.limit`, verdict from `verdict.js deriveVerdict` | **STRUCTURAL to §5.1.** Measured skip-path baseline: **3 rows + 2 `sys_`, verdict PASS, phase 59** |
| D-4 | INFO metric rows with no check | `dataset_source_license`, `ravine_feature_count`, `ravine_geometry_repaired_pct`, `ravine_geometry_collection_extracted` | `buildAuditTable` builds rows **only** `for (const check of selected)` and `index.js` passes no `extraRows` | **must be declared as INFO checks** (P1.2) or they vanish. Same Fold-B mechanism pilot 1 hit. |
| D-5 | per-feature WARN rows | `ravine_geometry_skipped_source_id` pushed once per skipped feature (`:441`) | one row per check; the ids move into `observation.detail` | **declared normalisation** + closes an unbounded-row-count defect |
| D-6 | `records_meta.ravine_load` | 18 fields, hand-built (`:522-541`) | same 18, via `computeResult.records_meta` (spread first by `index.js`) + declared in `emits[]` with `consumers: ["enrich_ravines"]` | **must be byte-identical** — `enrich-ravines.js:42` pins `spec_version === '1.2'` and `:50` reads `drift_check_passed`/`mass_delete_check_passed`. Any drift HALTs the chain. |
| D-7 | `records_meta.config` | absent | **NEW** — the six resolved values stamped by `config.js` (~30 B/var ≈ **180 B/run**) | **NEW, intended** (§1.2a P4 *"the value in force is observable"*). P3 cost stated. |
| D-8 | ⚠️ **exit code / halt semantics** | a FAIL row + `return {failed:true}`; `pipeline.run` does not inspect the return, so **the process exits 0** and `run-chain.js:670-672` merely records the FAIL verdict — the chain **continues** | `index.js` throws after the emit when `built.blockingFailures.length > 0` → **exit 1 → chain HALT** | **PIN, DO NOT FIX** (§3.1). Declare every check `blocking: false` at commit 7 so commit 7 stays a genuine no-op; promoting the halt is a peel with its own lock. **Getting this wrong smuggles a chain-halt into a "no-op diff".** |
| D-9 | ledger row | `owns = ownsLedgerRow('sources')` → false in-chain; `run-chain.js` writes the row (measured: 8 rows under `sources:load_ravines`) | unchanged | **no diff in-chain**; standalone gains the library's own row + `self_skipped` on lock contention |
| D-10 | lock contention | `:556` `if (!lockResult.acquired) return;` — SDK's bare SKIP, no `audit_table` | library SKIP summary with a row-derived verdict + `status='self_skipped'` | **FIX** — point at an `LR-D*` id; same consumer list pilot 1 enumerated |
| D-11 | DB target refusal | none (no `assertDbTarget` in the step) | refuses below `database.min_migration` / non-`postgres` | **NEW behaviour.** Floor must be **167** (`migrations/167_create_ravines_table.sql` — the only DDL this step depends on), not the current head 242. |
| D-12 | log tag | `pipeline.run('load-ravines', …)` `:588` | `pipeline.run('load_ravines', …)` — library passes `identity.name` | INCIDENTAL; declare as a normalisation |
| D-13 | **table-write observability** | nothing in `records_meta` says how many rows were *scanned* vs *changed*; `polygons_inserted/updated/deleted` are counted from `RETURNING (xmax=0)` inside the compute | `write_discipline` emits `rows_scanned` / `rows_changed` and checks them against `expected_change_ratio`; `idempotent_rerun: "zero_writes"` asserted (run twice, second run updates 0) | **NEW, and it is the point of the pilot.** P3 cost: **+2 `records_meta` ints (~40 B) + 1 audit row (~90 B) per run**; the assertion itself is free (the counts already exist). Baseline to beat: the last real load wrote **854 rows** and every subsequent run wrote **0/0/0**. |
| D-14 | golden 4-tuple | rows = `pipeline_runs` only | + `ravines` row count and ordered content hash | **harness growth.** P3 cost per capture: 1 `count(*)` + 1 `md5(string_agg(… ORDER BY source_id))` over **854 rows / 7,640 kB** — sub-second; ×2 captures × 2 invocations = 4 queries per differential. **Gate it on a declared row ceiling** so the same code over `parcels` (486,530 rows) does not become the differential's cost. |

## Library-growth items for a LOADER (P3 cost stated per item)

`scripts/lib/step/index.js`'s own header schedules the first three: *"generated write SQL from `write_discipline` → **INGESTOR pilot**"*, *"staleness/gating axes + the run-ledger gate → **INGESTOR**/ENRICHER"*, *"`pct`/`pop`/`ratio` limit forms → validator wave"*. This pilot is where they land.

| ID | Gap (measured) | Why a LOADER needs it | I/O cost (§1.2a P3) |
|---|---|---|---|
| **LG-1** | **No write path.** `deriveMeta` reads `outputs.writes` for `PIPELINE_META` and nothing else; no SQL is generated, no `rows_scanned`/`rows_changed`, no `expected_change_ratio` check, no `idempotent_rerun` assertion | class **B** = guarded upsert + scoped departure DELETE, in declared order. §1.4: *"`class` is not decoration — it selects the generated SQL"* | **0 net** (replaces hand-written SQL) · **+~130 B/run** for the two counters + one audit row |
| **LG-2** | **No transaction seam.** The library never calls `pipeline.withTransaction`; a compute may not `require('../pipeline')` (`compute-forbidden-require`) | `execution.txn_scope: "step"` — the upsert loop and the departure DELETE must commit together (`:466-499`) | **0** — same one transaction, owned one layer up |
| **LG-3** | **No staleness/gating.** `staleness` is declared and has zero runtime consumer (`grep -rn "staleness" scripts/lib/step/` → 0) | two triggers at two lifecycle positions: `source_validator @ pre_acquisition` (`:317`) and `content_hash @ post_acquisition` (`:354`) — the schema already models `position` and `signal` for exactly this | **0 net** — 1 HEAD + 1 prior-run `SELECT` move from the step to the library |
| **LG-4** | ⛔ **No acquisition seam.** `ctx` offers `{pool, chainId, runId, descriptor, checks, log, fetch, clock, config, report}`. A LOADER needs temp-dir, streamed hash-to-disk, unzip and shapefile parse — all `fs`-bound and all banned in compute | §1.5 already names `acquisition` a **P0 missing category**; four loaders short-circuit on `fs.existsSync` today. **Ask A-2.** | **0 net** bytes downloaded; **+~120 B/run** if acquisition emits its own audit rows (recommended: `bytes_downloaded`, `content_hash`) |
| **LG-5** | **`evaluateLimit` cannot evaluate `pct <=`** (`verdict.js` `VIOL_RE` + the object form only; everything else returns `unevaluable`) | three of this step's checks are pct-shaped (T2/T4/T5). Today `unevaluable` resolves to the **declared severity**, so a FAIL-severity pct check would **red every single run**. Hard blocker. | **0** |
| **LG-6** | **Only 2 terminals exist** (verdict-derived + lock-contention SKIP). This step has **10** | `terminals[]` is a required category with a 9-value `kind` enum built for exactly this; §1.2a P1 forbids a terminal shape that lives only in code | **0** — the `records_meta` shapes already exist |
| **LG-7** | **`outputs.invalidates` has no consumer.** Declared in the schema; `grep` finds no reader | `parcels.ravine_dataset_version_when_enriched` is the lineage stamp `enrich_ravines` reads; `load-parcels.js` NULLs it on geometry change (`#418`) | **0 at pilot 2** — declare-only; the consumer is the LINK/MATCHER wave |
| **LG-8** | **`extraRows` is never passed** by `index.js` (`buildAuditTable(descriptor, chainId, observations)` — 3 args) | the four INFO metric rows (D-4) | prefer **declared INFO checks** (0 new mechanism, ~+200 B/run of rows) over growing `extraRows` — P1.2 beats P1.4 |
| **LG-9** | **Golden harness is `pipeline_runs`-only** (`capture-step-golden.js:278`) | write class B without table state is an unverifiable differential | see D-14 |
| **LG-10** | **No `force_run`** anywhere in the estate for a source-validator gate | without it the write path is untestable end-to-end (finding #3) | **Ask A-3** |

## PLAN-altitude adjudication asks

*(Both Reality-Check and Schema-Fidelity FIRE at plan altitude — this pilot writes domain rows.)*

**A-1 — §5.5 (1) compute shape for non-ASSERT archetypes.** Ratify **either** (a) a second legal shape: `compute(ctx)` runs a declared phase pipeline and the dispatch table becomes *observers over a shared result* (each still `checkId → fn`, so `step-conformance.infra.test.ts:485-503` stays as-is); **or** (b) the library owns acquire + validate + write (LG-1…LG-4) and compute shrinks to the pure domain functions already exported at `:591-605` (`computeCountDeltaPct`, `computeGeometryUpdatePct`, `computeMassDeletePct`, `shouldSkipDelete`, `validatorCounterDelta`, `dedupeBySourceId`, `ageDaysFrom`, `datasetAgeStatus`, `coerceSourceId`). **(b) is more faithful to P2** (*"compute is JUST compute"*) and to the growth waves already scheduled in `index.js`; it is also the larger build. **Operator ruling required before commit 7** — this decides the whole pilot's shape.

**A-2 — the `acquisition` seam / category.** A LOADER cannot acquire from inside a compute. Options in P1 order: (1) a `ctx.acquire` seam in the library driven by `inputs.reads.externals[]` + `staleness.trigger[].position` (no new category — preferred); (2) a 19th category `acquisition` (§1.5 calls it a P0 gap; a category is a C3 contract change). **Cost of (1): zero new bytes; it moves ~120 lines out of the step.**

**A-3 — a force mechanism for a source-validator gate.** The `sources` CKAN resource is frozen at 2022-03-14; 8/8 runs skip. Without a forced full load, G8 covers only the skip terminal. Options: (a) add `override.force_run: "RAVINE_FORCE_RELOAD"` (fits the existing box — no schema change, one env var, one gate arm); (b) capture the golden against a scratch database whose prior-run baseline is cleared; (c) accept skip-only differential and record `ASSESSMENT-INCOMPLETE` on the write half. **(a) is the P1.1 answer and is reusable by all 9 INGESTORs.**

**A-4 — where a verdict-bound tunable lives (the real P1↔P4 tension).** T2/T3/T4/T5 are simultaneously operator knobs (P4 ⇒ logic variable) and verdict bounds rendered in the audit row's `threshold` column (P1.1 ⇒ `checks[].limit`, which is a static string the schema `x-frozen`s). Nothing today can be both. Options: (1) `checks[].limit_from_config: "<var_name>"` — the resolved value renders as the threshold and is already stamped in `records_meta.config`, **zero extra bytes**; (2) declare `limit` as a literal and duplicate the value in `config` — a two-source-of-truth invitation, refused; (3) keep them in `ctx.config` and let the compute report `{violations, value}` with `limit: {warn, fail}` — but then the audit row's threshold is a JSON blob, not the number in force. **Recommend (1).** Note the compute-shape rule `compute-no-literal-threshold` already forces this decision (it fails a literal compared against `violations`/`errors`).

**A-5 — a box for accept-anomaly overrides (E1/E2).** `override` is `{force_full, force_run, dry_run}`. `RAVINE_ACCEPT_*` are *"proceed despite a FAIL, but never suppress the FAIL row"* — a fourth kind, present in ≥2 loaders. P1.4 candidate: `override.accept_anomaly: [{env, check_id, why}]`, which also makes the *"override never suppresses FAIL"* rule (locked textually today at `load-ravines.infra.test.ts`) **structural**. Cost: **~90 B of descriptor, 0 runtime rows** (the two `*_present` WARN rows already exist).

**A-6 — Reality-Check ask (FIRES).** The trigger is *"the diff adds/changes an enriched parcel field"* and this pilot writes the table those fields are derived from. Grounded numbers to stress: `ravines` **854 rows / 1 distinct `source_dataset_version` / 7,640 kB**; `parcels` **486,530 rows, 32,339 `is_in_ravine_protection_area` true (6.6%), 486,530 `ravine_distance_m` populated (100%)**; `permits` **254,082 / 18,327 true (7.2%)**; plus `coa_applications`. **Ask RC: is a 6.6% parcel / 7.2% permit in-ravine rate credible for Toronto's Chapter 658 area, and would a forced reload under A-3 move it?** A silent shift here is the `$105.24M`-shaped failure at plan altitude.

**A-7 — Schema-Fidelity ask (FIRES).** Live `127.0.0.1:54322/postgres`, **242 migrations**. Verify against the live catalog, not the migration file: every column/type/nullability/default in `ravines` used by the generated write SQL (`source_id BIGINT UNIQUE NOT NULL` — the ON CONFLICT target; `geom GEOMETRY(MultiPolygon,4326) NOT NULL`; `source_dataset_version TEXT NOT NULL`; `created_at`/`updated_at` defaults `now()`); both GIST indexes (`idx_ravines_geom_gist`, `idx_ravines_geog_gist`) → `guards.requires`; **RLS** — `migrations/227_rls_class_b_default_deny.sql:81` enables RLS on `ravines`, so confirm the pipeline role still writes under the generated SQL; and `pipeline_runs.records_meta`'s shape for the 18-field `ravine_load` block against `enrich-ravines.js`'s reads.

## Standards Compliance

* **Try-Catch Boundary:** no API route touched — §11's route block N/A. The step's boundary becomes `scripts/lib/step/index.js`'s `try/catch/finally` in `runWithPool`. ⚠️ The step's own 4 `finally` blocks (`:194`, `:216`, `:229`, `:372`) are **resource cleanup**, not ledger work — `:372`'s `fs.rmSync(tmpRoot, {recursive:true, force:true})` is textually locked by `load-ravines.infra.test.ts` and **must survive wherever acquisition lands** (A-2). A temp dir leaked per run is a disk defect, not a style point.
* **Unhappy Path Tests:** every declared check ships a must-fail fixture (55-A #165); both fences proven in **both** directions (G4d). Unhappy paths in scope, all live in the code today: HEAD 4xx/5xx (`:301`) · CDN strips both validators (`:310`) · download/unzip/parse failure (`:364`) · zero `.shp` / >1 `.shp` / missing `.dbf` (`:241-246`) · bad OBJECTID (`:261`) · null geometry (`:262`) · duplicate `source_id` (`:397`) · a `source_id` missing from the validation result (`:430`) · count drift with and without the override · >5% invalid geometry · mass delete with and without the override · empty-set DELETE guard (`:492`) · lock contention · prior-run query failure (`:291`).
* **logError Mandate:** **N/A for `scripts/`** — the corpus uses `pipeline.log.*`.
* **UI Layout:** **N/A for layout**, but **not N/A for files**: `GlobalConfigCard.tsx` gains a GROUP entry (commit 7). A seeded var absent from GROUPS is invisible to operators — the same hidden-knob failure P4 exists to close, and `step-conformance.infra.test.ts` asserts the linkage.
* **Database Impact:** **NO** — no migration. Writes to `ravines` are pre-existing and the table is at mig 167.
* **Cross-Layer Contracts (§11, always applies):** **three** cross this pilot. (1) `SPEC_VERSION '1.2'` — `load-ravines.js:36` ↔ `enrich-ravines.js:20/42` (exact-match pin) ↔ Spec 59 §9. (2) The six thresholds — seed JSON ↔ live `logic_variables` ↔ descriptor `config[].min/max` ↔ `checks[].limit` (A-4). (3) The `RUN_STATUS` vocabulary — `ledger.js` ↔ `/api/quality/route.ts` `WHERE status='failed'`; per `tasks/lessons.md`, **no new terminal status may be introduced**; `self_skipped` already exists in the enum.

## Panel plan

**PLAN altitude (BEFORE code — `scripts/CLAUDE.md` "Two altitudes", Spec 08 §6.4):**
1. **Integration** (`general-purpose`, main tree, NO worktree) — manifest/chain wiring, `spawnStepChild` argv/env, the `pipeline.step` export surface vs the 13 named exports at `:591-605`, and every one of the 4 test files' re-homing targets.
2. **Ground-truth** (grounder) — re-executes **every** executable claim in this plan.
3. **Descriptor-vs-schema** (not a Spec 08 seat) — the authored descriptor vs `step.schema.json` incl. the INGESTOR `allOf` profile (`outputs` may not be `"none"`), `writeDiscipline.class = upsert_scoped_departure_delete`, and all 10 `terminals[]`.
4. **DB Schema-Fidelity** (Spec 08 §5.4, live DB) — **FIRES**, ask A-7.
5. **Reality-Check** — **FIRES**, ask A-6. *(Pilot 1 correctly marked this N/A because `outputs: "none"`; that justification does not transfer.)*

**OUTPUT altitude (on the diff + re-run values) — pipeline WF2 = 5 seats + Guardian + Reality-Check:**
1. Bash: `npm run review:gemini -- review scripts/lib/compute/load-ravines.js --context docs/specs/01-pipeline/59_source_ravine_protection.md`
2. Bash: `npm run review:deepseek -- review scripts/lib/step/index.js --context docs/specs/01-pipeline/122_pipeline_step_optimization.md`
3. Agent `code-reviewer-grounded`, worktree — spec conformance / code quality.
4. Agent `observability-reviewer`, worktree — audit-row completeness, row-derived verdict, §11 counter scoping (`records_total=feature_count`, `_new=inserted`, `_updated=updated`), and the **`records_meta.ravine_load` producer/consumer contract** vs `enrich-ravines.js`.
5. Agent `general-purpose`, NO worktree — **Integration** vs the real codebase.
6. Agent `regression-guardian`, **main tree** — anchored on the 2 `Severity:` fences, the ~40 source-text assertions in `load-ravines.infra.test.ts`, the 5 adoption-locks in `source-version.logic.test.ts`, the `fs.rmSync` cleanup, and `tasks/lessons.md` (script-must-be-run-live; status vocabulary).
7. **Reality-Check** — **FIRES**: re-run the parcel/permit in-ravine rates after the differential and compare against the plan-altitude baseline.

Then **Fold Validation** (Spec 08 §11.2): Backend/Pipeline ⇒ **mandatory** — one grounder re-executes every claim in the fold + one Cross-read Adversary checks the folded decisions pairwise and walks every checklist line for staleness.

## Contradictions / unverifiable

| # | Item | Evidence | Status |
|---|---|---|---|
| C-1 | `review_followups.md` **#421** rules that `load-ravines` seeding **zero** logic variables is the correct precedent, to be deferred *"to when operator tuning is actually needed"* | §1.2a **P4** is a DIRECTIVE: a hard-coded knob is a hidden variable | **SUPERSEDED.** P4 wins; close #421 in this pilot and say so in the followup. |
| C-2 | §5.5 (1): *"`compute(ctx)` iterates `ctx.checks` and does nothing else"*, hard-gated at `step-conformance.infra.test.ts:485-503` | a LOADER is a sequential acquire→write pipeline, not N probes | **CONFLICT — Ask A-1. Blocks commit 7.** |
| C-3 | `compute-shape.yml` `compute-forbidden-require` bans `fs`; §1.5 concedes *"`acquisition` is a P0 missing category"* | `load-ravines.js` has **6** `fs` call sites + `node-stream-zip` + `shapefile` | **CONFLICT — Ask A-2. Blocks commit 7.** |
| C-4 | §1.2a P4 (*every threshold is a logic variable*) vs P1.1 (*descriptor data first*) vs `checks[].limit`'s `x-frozen` static string | `verdict.js checkRow` renders `check.limit` as the row's `threshold` | **CONFLICT — Ask A-4.** |
| C-5 | `override` = `{force_full, force_run, dry_run}` | the step's 2 env vars are accept-anomaly, not force | **NO HOME — Ask A-5.** |
| C-6 | `verdict.js evaluateLimit` supports only `viol ==`/`viol <=` + `{warn,fail}` | three checks here are `pct <=` | **BLOCKER (LG-5)** — an unevaluable FAIL check reds every run. |
| C-7 | Spec 123 §6.1 cites the shape rule as *"Spec 122 §4.1"* | the rule is §5.1 | **Doc error** (pilot 1 recorded it; still unfixed). Do not follow literally. |
| C-8 | Spec 122 §8.2 says `load_ravines` has *"4 `finally`, drift + mass-delete env overrides, two-tier gate"* | measured: **4** `finally` (`:194/:216/:229/:372`), 2 env overrides, 2-tier gate — **all three confirmed** | ✅ **VERIFIED** — the only §8.2 pilot claim that survives re-execution unchanged. |
| C-9 | Spec 59 header is **v1.3**; the code pins `SPEC_VERSION = '1.2'`; `enrich-ravines.js:42` fails on anything `!== '1.2'` | `grep -n SPEC_VERSION` on both files | **DOC/CODE DRIFT, load-bearing.** The pin is the contract; the spec version number is not. Do not "fix" it during conversion. |
| C-10 | `docs/reports/review_followups.md` **#410** defers the post-download content-hash skip as *"DEFER LOW"* | `0b230472` implemented it (`:354` `contentHashDecision`) and locked it in `source-version.logic.test.ts:228-247` | **STALE followup** — the item shipped. Mark it resolved; do not re-file it as a gap. |
| C-11 | `records_total/new/updated` written as `0` on skip runs, but the step emits `null` (`:304`, `:321`, `:367`, `:380`) | live rows 1391/1469 read `0,0,0` | **NORMALISATION by `run-chain`**, not by the step. The differential must not read this as a step-level diff. |
| **U-1** | **UNVERIFIABLE:** whether the write path's differential is green | the source is frozen at 2022-03-14 and no force exists | **NOT MEASURABLE** until A-3 is ruled. The single largest hole in this plan. |
| **U-2** | **UNVERIFIABLE:** G1's *"20% change coupling"* and G2's churn×complexity quadrant | `ls scripts/analysis` has no churn/complexity instrument | **NOT MEASURED — a real work item**, inherited unchanged from pilot 1. |
| **U-3** | 55-A per-step rows for `load_ravines` | the generator is claim-scoped; `grep -rn "load_ravines" docs/reports/generated/*.md` → no step row | **NOT PRODUCIBLE.** 44 is the template's A-block size, not a measured per-step count. |
| **U-4** | Mutation ≥80% (G7, class A) | `stryker.config.mjs` `mutate:` = 3 files under `src/features/leads/lib`, `break: 75`; `scripts/` unreachable | **UNSATISFIABLE AS WRITTEN** (Spec 123 §4.8 concedes it). Record, do not fake. |

## Execution Plan

- [ ] **State Verification:** live DB reachable this session (`127.0.0.1:54322/postgres`, **242** migrations) — re-print host+database before every capture per `tasks/lessons.md`. Measured baselines to re-confirm at commit 5: `ravines` **854 / 1 version / 7,640 kB**; `sources:load_ravines` **8 completed runs, last 2026-07-08**; `logic_variables` **422 rows, 0 ravine keys**.
- [ ] **Contract Definition:** N/A for API routes. The load-bearing contract is `records_meta.ravine_load` (18 fields, `spec_version` exact-pinned by `enrich-ravines.js:42`) — declare it in `emits[]` with `consumers: ["enrich_ravines"]` and diff it field-by-field at commit 7. `npm run typecheck` after the test re-homing.
- [ ] **Spec Update:** amend Spec 59 §3.1/§9 to the as-built forms (`sources:load_ravines`, the 20 measured audit metrics vs §9's 9, the 2 override rows); amend Spec 122 §5.5 per A-1 and §1.5/§4 per A-2; correct Spec 123 §6.1's `§4.1`→`§5.1`. Run `npm run system-map`.
- [ ] **Schema Evolution:** N/A — Database Impact NO. `step.schema.json` changes (A-4/A-5) are a **contract** change, not a migration, and gate at C3.
- [ ] **Guardrail Test:** `src/tests/steps/load_ravines/violations.test.ts` — 44 A + 5 B partials + 2 both-directions fence locks.
- [ ] **Red Light:** `npx vitest run src/tests/steps/load_ravines/` must FAIL before commit 7. Record the red set.
- [ ] **Implementation:** commit 7 = frozen-shape wrap + LG-1…LG-6 library growth + the 6 seeded variables + the GROUPS entry; commits 8a/8b/8c = the three peels; green differential after each. **Every non-identical output is a row in the Declared-diffs table — nothing undeclared.**
- [ ] **UI Regression Check:** `GlobalConfigCard.tsx` is a shared admin component — run `npx vitest run src/tests/*.ui.test.tsx src/tests/control-panel.logic.test.ts` after the GROUPS edit.
- [ ] **Pre-Review Self-Checklist:** 5–10 items from Spec 122 §1.2a/§5.1/§5.5 walked against the ACTUAL diff, PASS/FAIL each, BEFORE running tests.
- [ ] **Multi-Agent Review:** the 5-seat panel + Regression Guardian + **Reality-Check** in ONE message (roster above).
- [ ] **Fold Validation:** mandatory (Backend/Pipeline) — grounder re-executes every claim in the fold + Cross-read Adversary pairwise.
- [ ] **Green Light:** `npm run test && npm run lint -- --fix` + `node scripts/hooks/check-step-shape.mjs` (must report **2** enforced step files and **2** compute modules, exit 0) + both differentials green. Baseline to beat, to be measured immediately before commit 7: `npx vitest run src/tests/load-ravines.infra.test.ts src/tests/load-ravines.logic.test.ts src/tests/source-version.logic.test.ts src/tests/pipeline-advisory-lock.infra.test.ts src/tests/step-conformance.infra.test.ts`. → WF6.

**PLAN COMPLIANCE GATE (§11):** Database Impact **NO** (no migration; `ravines` exists at mig 167) · Pipeline Script Modified **YES** — uses the SDK via `pipeline.step()`; streaming N/A (bounded 854-row set, batch 16383 from the PG parameter ceiling) · Shared Logic Touched **YES** (`scripts/lib/step/*` — consumed by `assert_schema` today, so every pilot-1 test is a dual-path consumer; `npx vitest related` planned) · Cross-Layer Contracts: **three** identified above, no new status value · UI/Frontend: one shared admin component (`GlobalConfigCard.tsx`) — UI regression check scheduled · Pre-Review Self-Checklist scheduled.
