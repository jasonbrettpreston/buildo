# Active Task: C4 step H — `c4_chain_completeness_gate`: the "every chain" capture gate as a CHECKER (completeness + freshness)
**Status:** Implementation (AUTHORIZED 2026-09-11 by operator; Asks ruled: A1 NO — a dirty tracked capture is refused even with `--overwrite`; A2 NO — #22 covers PRE only) · **Workflow:** WF2 (Enhance) · **Domain Mode:** Backend/Pipeline (`scripts/CLAUDE.md` read this session — the Spec 47 §R1–R12 skeleton does not apply: this task touches analysis tooling under `scripts/analysis/` and tests only, no pipeline step, no SDK call, no DB write)

## Context
* **Goal:** Close the C4 batching-entry plan's step **H** (`.cursor/c4_batching_entry_active_task.md` §3.4 + Fold A D-1/I-8): the C4 "every chain" gate — *for every converted step S and every `c ∈ C(S) ∪ {none}` a PRE and a POST capture exist, the POST is current against the descriptor fingerprint, and `--compare` shows zero unexplained diffs* — must be a **checker that fires**, plus the **freshness** half D-1 named: a re-run must not be able to destroy a not-yet-committed reference capture in place.
* **What is ALREADY built — measured 2026-09-11 at HEAD `2c3dea53`, not transcribed from the C4 plan (whose §3.4 sentence "the completeness half is NOT enforced by a checker" is now STALE):**
  | Half | Mechanism | Where | Status |
  |---|---|---|---|
  | POST completeness from `manifest.chains` | `derivedInvocations(manifest, slug)` (`scripts/analysis/step-validate.mjs:693-699`: every chain whose slug list contains the step, with its `chain_args`, plus `none`) → `findings.invocationsMissing` → G8 | step-validate G8 (hard-stop on missing) | **BUILT** (pre-existing) |
  | PRE completeness | GOLD-PRE (`preInvocationsMissing`, `GOLD_PRE_KNOWN_GAPS` pin — 2 declared gaps: `link_wsib sources --full`, `enrich_parcels none::`) | step-validate G8, landed `ec28216a` 2026-09-11 | **BUILT** |
  | POST currency (R-C) | `computeSourceFingerprint` vs each POST `source_fingerprint` → `staleFingerprints` | step-validate G8 + `golden-fingerprint.infra.test.ts` | **BUILT** (proved today: commit 9's recapture flipped G8 0/3 → 3/3) |
  | Differential | `--compare` newest PRE vs POST by shared filename → `unexplainedDiffs` (deepest-leaf citation rule) | step-validate G8 | **BUILT** |
  | **D-1 freshness — the harness overwrites in place** | `capture-step-golden.js:903-905`: `fs.mkdirSync` + bare `fs.writeFileSync(outPath, …)` — no refuse-if-exists, no dated name, no git check | — | **NOT BUILT** (this task, commit 1) |
  | **D-1 freshness — PRE captures committed before any re-run (R-AC)** | nothing asserts a converted step's `pre/` files are git-tracked and clean; measured today: 9/9 steps, 38 files, all tracked, 0 dirty — green by discipline, not by lock | — | **NOT BUILT** (this task, commit 2) |
  | Declared programme item for step H | none exists (`programme-items.json` has no C4-gate row; R-T says cross-cutting promises are declared data) | — | **NOT BUILT** (this task, commit 3) |
* **Target Spec:** `docs/specs/01-pipeline/122_pipeline_step_optimization.md` §5.3 (Condition 3 — the golden differential; R-C) · `docs/specs/01-pipeline/123_step_opt_assessment_validation.md` §6 G8 · `docs/specs/01-pipeline/124_step_standard_policy.md` R-C (mechanical fingerprint lockfile), **R-AC** ("PRE captures are committed before any re-run — the harness overwrites in place"), R-T (declared programme promises) · governing plan `.cursor/c4_batching_entry_active_task.md` §3.4, Fold A D-1, I-8, step H row.
* **Key Files:** `scripts/analysis/capture-step-golden.js` (`:893-908` the `--out` write site; `:823` usage line) · `scripts/analysis/step-validate.mjs` (fast invariants — ids 1–21 exist, `:732` `GOLD_PRE_KNOWN_GAPS`, `selfTest()` arms) · NEW `src/tests/capture-harness-overwrite.infra.test.ts` · `src/tests/programme-backlog.infra.test.ts` (mirrors fast invariants) · `scripts/steps/_schema/programme-items.json` · `docs/reports/generated/122-programme-backlog.md` · the 9 assessment scorecards (fast-invariant rows render into them).

## Technical Implementation
* **New/Modified Components:**
  1. **Harness overwrite guard (`capture-step-golden.js`, `--out`).** If `outPath` exists: refuse (exit 1, message names the file and the rule) unless `--overwrite` is passed **and** the existing file is git-tracked and clean (`git status --porcelain -- <file>` empty) — i.e. the ONLY reference a re-run may replace is one git history can restore. An untracked or dirty existing capture is refused even with `--overwrite` (R-AC: commit first). Pure predicate `overwriteDecision({exists, tracked, dirty, overwriteFlag}) → {allow, reason}` exported for the test; the git probe is a thin wrapper.
  2. **Fast invariant #22 `GOLD-PRE-FRESH` (`step-validate.mjs`).** For every converted slug: every `docs/reports/golden/<slug>/pre/*.json` is git-tracked (`git ls-files`) and clean (`git status --porcelain`). Per-row, hard-stop on FAIL (a dirty/untracked PRE is a reference nothing can restore). Pure predicate over `{files, tracked, dirty}` + `selfTest()` arms: GREEN (all tracked+clean), RED (one untracked), RED (one dirty). Scoped honestly: PRE only — POST freshness is R-C's fingerprint (already built).
  3. **Programme item `C4-GATE`** (`programme-items.json`): spec `122 §5.3 / C4 plan §3.4`, promised "every declared chain has a PRE+POST pair, POST current, PRE committed, zero unexplained diffs — a checker, not a discipline", status **BUILT** at commit 3 with the evidence table above, `gate.kind: cutover_prereq`, `blocks: ["assert_global_coverage"]` (batch 1's first cutover may not proceed on a gate that is not built — satisfied the moment this lands), owner `wf`.
  4. **Docs (commit 3, same commit as the item — Spec 124 §R-8):** C4 plan §3.4 stale sentence corrected in place (amend, dated); Spec 122 §5.3 gains one dated line naming the four halves and where each is enforced; Spec 124 R-AC row gains "(harness guard + GOLD-PRE-FRESH, 2026-09-11)".
* **Data Hooks/Libs:** none new. `git` via `child_process.execFileSync` (already the pattern in `step-conformance.infra.test.ts`).
* **Database Impact:** **NO.** No migration, no DB read or write in any new code (step-validate's fast invariants are fs/git only by contract).

## Standards Compliance
* **Try-Catch Boundary:** N/A — no API route. Analysis scripts throw with a named message (existing convention, `no-empty` lint enforced); the harness guard exits 1 through the existing usage-error path.
* **Unhappy Path Tests:** harness — exists+no flag → refused; exists+flag+dirty → refused; exists+flag+untracked → refused; exists+flag+tracked+clean → allowed; absent → allowed. Validator #22 — untracked PRE → FAIL; dirty PRE → FAIL; all clean → PASS; a slug with no `pre/` dir → FAIL (a converted step with no PRE reference at all is the worst case, not a vacuous pass).
* **logError Mandate:** N/A (no new catch blocks; `scripts/analysis/` is outside the `src/` lint scope for the mandate).
* **UI Layout:** N/A.
* **Pipeline Script:** N/A — no `pipeline.run` / SDK surface touched; `scripts/CLAUDE.md`'s Spec 47 skeleton governs step scripts, not analysis tooling.
* **Test headers:** every new/edited test file carries `SPEC LINK: docs/specs/01-pipeline/122_pipeline_step_optimization.md §5.3 (R-C) + docs/specs/01-pipeline/124_step_standard_policy.md R-AC`.

## Execution Plan — one committer, three commits, each hook-green
- [ ] **Commit 1 — harness overwrite guard.** `overwriteDecision` predicate + git probe + `--overwrite` flag + usage line; `src/tests/capture-harness-overwrite.infra.test.ts` (5 cases above, predicate-level, no fs mutation of committed captures; one integration case against a temp file under the scratch dir proving the real CLI refuses). Done-test: the 5 cases + the CLI case green; a deliberate `--out` onto an existing committed capture without `--overwrite` exits 1 (run once by hand, output pasted in the commit body).
- [ ] **Commit 2 — fast invariant #22 GOLD-PRE-FRESH.** Predicate + `selfTest()` arms (GREEN/RED/RED/RED) + wiring into the per-row fast-invariant table (renders into scorecards) + `programme-backlog.infra.test.ts` mirror row; regenerate the 9 scorecards (`--all --write`, full mode, capped vitest workers). Done-test: `step-validate --all --fast` prints `| 22 | <slug> | PASS |` ×9; `selfTest` RED arms fire.
- [ ] **Commit 3 — declare + document.** `C4-GATE` item BUILT (evidence: commit 1/2 hashes, the 9/9 measurement), `npm run programme-backlog`, `generate-template-freeze.mjs --check` (no flag needed: no tier change), C4 plan §3.4 amendment, Spec 122 §5.3 line, Spec 124 R-AC note; `spec:split-check --check`. Done-test: backlog shows C4-GATE BUILT; registry invariant #9 still clean; hook green.
- [ ] **Panel (Spec 08 §6.4, both altitudes):** PLAN roster — Integration (does `derivedInvocations` really read `manifest.chains`? does any caller of `--out` rely on silent overwrite — `grep -rn "capture-step-golden" scripts/ .husky .github` — the pre-commit hook and the recapture runbook lines must not break) + Idempotency Lens (run-2-vs-run-1 is exactly this task's subject) + Cross-read Adversary. OUTPUT roster — 5 (Gemini + DeepSeek lens set + Code Reviewer + Observability + Integration) + **Regression Guardian** (existing code altered: the harness write path — fence: pilot 9 commit 8 P6 deliberately overwrote `sources_run1.json` in place; the new guard must still permit that flow via `--overwrite` on a committed file, and the runbook must say so).

## Asks — where no rule answers
| # | Ask | Recommendation |
|---|---|---|
| **A1** | With `--overwrite`, may the harness replace an existing capture that is git-tracked but **dirty** (uncommitted local edit)? | **NO.** R-AC's rule is "committed before any re-run"; a dirty tracked file is a reference git cannot restore. Refuse; the operator commits or `git checkout -- <file>` first. One rule, no second flag. |
| **A2** | Should #22 also cover POST captures (tracked + clean)? | **NO, not here.** POST currency is R-C's fingerprint (built); a dirty POST is the normal state mid-commit-9 (recapture then commit). Adding it would red every cutover's working tree by design. Scope stays PRE. |

## Grounding — every executable claim, EXECUTED (2026-09-11, HEAD `2c3dea53`)
| Claim | Command | Result |
|---|---|---|
| POST invocations derive from `manifest.chains` + `chain_args` + `none` | `sed -n 693,699p scripts/analysis/step-validate.mjs` | as quoted above |
| Harness writes `--out` with a bare `writeFileSync`, no existence check | `sed -n 893,908p scripts/analysis/capture-step-golden.js` | `fs.mkdirSync … fs.writeFileSync(outPath, …)` — no guard |
| PRE captures today: tracked + clean for all 9 converted steps | `for d in docs/reports/golden/*/pre; git ls-files / git status --porcelain` | 38 files, tracked=38, dirty=0 |
| Fast invariant ids in use | `grep -n "id: " step-validate.mjs` | 2,3,4,5,7,8,20,21 per-row + registry-scoped 4/5/9 → next per-row id **22** |
| GOLD-PRE declared gaps | `sed -n 732,740p step-validate.mjs` | `link_wsib sources --full`, `enrich_parcels none::` |
| R-AC wording | `grep -n "^| R-AC " docs/specs/01-pipeline/124_step_standard_policy.md` | "PRE captures are committed before any re-run (the harness overwrites in place)" |

## Not in scope
Closing the two `GOLD_PRE_KNOWN_GAPS` (those are captures to take, not checker work — filed already) · dated/versioned capture filenames (D-1's other option; refuse-if-exists is the simplest close, `feedback_fold_simplicity`) · any batch-1 conversion (step I) · touching `manifest.json`.

## Operating Boundaries
* **Target files:** `scripts/analysis/capture-step-golden.js` · `scripts/analysis/step-validate.mjs` · `src/tests/capture-harness-overwrite.infra.test.ts` (new) · `src/tests/programme-backlog.infra.test.ts` · `scripts/steps/_schema/programme-items.json` · `docs/reports/generated/122-programme-backlog.md` · the 9 assessment scorecards · `.cursor/c4_batching_entry_active_task.md` §3.4 · Spec 122 §5.3 · Spec 124 R-AC row · `docs/runbook/README.md` (one line: `--overwrite` semantics).
* **Out-of-scope files:** every `*.descriptor.json` · `scripts/lib/**` · `migrations/**` · `manifest.json` · `docs/reports/golden/**` (no capture is taken or rewritten by this task).
* **Cross-spec dependencies:** Spec 122 §5.3 · Spec 123 §6 G8 · Spec 124 R-C / R-AC / R-T · Spec 08 §6.4/§11 · Spec 121 §12b.6 (both-directions locks).

> **PLAN LOCKED. Do you authorize this WF2 plan? (y/n)**
> §11 note: no Database/API/UI/pipeline-script block applies (analysis tooling + tests only); the applicable items (test headers, unhappy paths, boundary) are answered above.

## Fold A (plan-altitude panel 2026-09-11: Integration + Idempotency Lens) — BINDING, overrides the text above where they conflict
1. **Drop the `programme-backlog.infra.test.ts` mirror row** (Integration item 3, FAIL): that file mirrors fast invariant #9 only; HB-1/CEIL-1 (ids 20/21, the precedent for #22) are locked in `step-validate.mjs` `selfTest()` arms and rendered through the conformance spawn test — #22 follows exactly that shape. `programme-backlog.infra.test.ts` is NOT touched.
2. **#22 is REGISTRY-scoped over `converted[]`** (Idempotency finding 2), pushed like ids 4/5/9 (one result for the fleet, `slug: '(registry)'`), never per-row over `filterToStaged`'s targets — a step enters `converted[]` only at commit 9, by which time its PRE captures landed at commit 5. Residual (called out, not hidden): a cutover commit that ALSO stages a PRE file is handled by item 3's definition of clean.
3. **ONE definition of "recoverable", shared by both mechanisms** (Idempotency findings 3/4): a capture is recoverable iff it is tracked in the INDEX (`git ls-files --error-unmatch <file>` — includes a staged-new `A ` file) AND its worktree content equals the index (`git status --porcelain -- <file>` second column is a space or the line is absent). `??` (untracked), ` M`/`MM`/`AM` (worktree differs from index) are NOT recoverable. Exported from the harness as `captureGitState(file) → {exists, tracked, worktreeClean}` + pure `overwriteDecision({exists, tracked, worktreeClean, overwriteFlag})`; `step-validate.mjs` REUSES them (it already `require()`s the harness) — no second implementation. An ignored path reads untracked (`ls-files` decides), so the single-probe misclassification named by the Lens cannot occur.
4. **Refusal message branches on state** (Idempotency finding 1): untracked → `rm <file>` (or `git clean -f -- <file>`) then re-run; tracked but worktree ≠ index → `git checkout -- <file>` (restore) or commit it, then re-run with `--overwrite`; tracked+clean without the flag → re-run with `--overwrite`. A1 stands (refuse), the remedy is just named correctly.
5. Integration items 1/2/4/5 PASS: `parseArgs` (`:119-126`) accepts a bare `--overwrite`; `fastInvariants()` per-row push shape `:912-932`, `selfTest()` arms `:~2852-2897`, hard-stop wiring `:1584-1589` (registry-scoped results hard-stop the fleet the same way id 9 does — verify at implementation); no external caller relies on silent overwrite; 38/38 PRE files `i/lf w/lf`, porcelain empty.
