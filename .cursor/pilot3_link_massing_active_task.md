# Active Task: Pilot 3 — convert `link_massing` to the Spec 122 step standard (LINK/MATCHER)
**Status:** Planning

> **Every number below was executed in this planning session (2026-08-27) against `127.0.0.1:54322/postgres` (242 migrations, floor 223) or against the working tree at HEAD `7f15cd5f`.** Nothing is transcribed from pilot 1/2, from memory, or from a prior report. Anchors are greppable strings, not line numbers, wherever the plan's own commits will move the line.

## Context

* **Goal:** Convert `link_massing` — the **LINK** representative of C1's eight archetype pilots (Spec 122 §8.2: *"the **only** step with a code+data signal (G3), full retraction"*) — from a 740-line `pipeline.run()` island into the Spec 122 §5.1 frozen shape. It is **the first step that joins two domain tables** (`parcels` × `building_footprints`) and writes a **junction** table (`parcel_buildings`), and the first with a live `chain_args` invocation divergence.
* **Target Spec:** `docs/specs/01-pipeline/122_pipeline_step_optimization.md` §1.2a (P1–P5), §1.4 (`write_discipline`), §1.5 (`staleness` 3 axes + `fingerprint_inputs`), §1.6 (`on_*_error`), §1.7 (`sharing`), §1.10, §5.1–§5.5, §8.1/§8.2 + `123_step_opt_assessment_validation.md` §6, §6.1, §7. **The step's own specs are three, not one:** `docs/specs/01-pipeline/56_source_massing.md` (§2 `geom` contract + §3 Core Logic + §3 *"`link-massing.js` `--full` gate (WF2 P11-2)"*), `docs/specs/01-pipeline/60_shared_steps.md` §3 *"Link Massing (`link-massing.js`)"* + §2 Step Registry, and `docs/specs/01-pipeline/43_chain_sources.md` §15/§7/§Runtime. Downstream contract: `docs/specs/01-pipeline/65_enrich_parcels.md` (MB-7 + §5 existing-structure).
* **Domain Mode:** Backend/Pipeline → `scripts/CLAUDE.md`. **Workflow:** WF2. **Branch:** `wf2/deep-scrapes-restore-l0` (HEAD `7f15cd5f` = pilot 2 Fold D landed).
* **What pilots 1–2 proved and pilot 3 must NOT re-derive:** the frozen file shape, `ctx.config` + the four-surface P4 battery, the compute dispatch table, `checks[].when` incl. `pre_write`, `terminals[]` selection, `override.accept_anomaly`, `limit_from_config`, `pct <=` limits, the golden harness's `table_state` + `--invariants`, the report-table column headers the violations suite reads. **What pilot 3 must derive fresh:** everything about a LINK.

### ⚠️ The six findings that shape this whole plan (measured, not inherited)

1. **The step has NO library path.** `scripts/lib/step/index.js` `isIngestStep` requires `inputs.reads.externals[].url` to be a non-empty string. `link_massing` reads no external — it is a pure DB→DB join. So under today's library it falls to the **pilot-1 (ASSERT) path**: `compute(ctx)` iterates `ctx.checks` and the library writes nothing. **A LINK's join and write have no home.** `index.js`'s own header schedules the wave — *"`invalidation + counters scoped by writes.key` → **LINK/MATCHER**"* — but the write phase itself is INGESTOR-gated. **Ask A-1.**
2. **⛔ `guard_columns: "all_declared"` is a chain-scale regression, and it is the single most dangerous line in this conversion.** The live upsert (`scripts/link-massing.js` `flushInsertBatch`) guards on **exactly four** columns — `is_primary`, `structure_type`, `match_type`, `confidence` — and deliberately **excludes `linked_at`**, even though `linked_at = EXCLUDED.linked_at` is in the SET list. `scripts/lib/step/write.js` `resolveGuardColumns` expands `all_declared` to *every step-written column except the key*, which **includes `linked_at`**. `EXCLUDED.linked_at` is `RUN_AT`, so it is distinct on every run ⇒ **every one of the 520,492 rows updates every run**, and because `enrich-parcels.js buildMassingScopeWhere` re-scopes on `pb.linked_at > p.massing_enriched_at`, the next `enrich_parcels` run re-enriches **all 485,135 linked parcels** instead of 1,395. Measured `enrich_parcels --full` cost from `43_chain_sources.md`: ~46–53 min. **The descriptor MUST declare `guard_columns` explicitly.** This is the plan's #1 regression risk and gets a both-directions lock.
3. **Spec 122 §8.2's write-class label for this step (`F link_full_retraction`) does not survive re-derivation** — exactly the failure §1.4 warns about (*"the port must be verified per step, not trusted"*). Measured, this step performs **three disciplines to ONE table in sequence**, and none of them is a departure delete: **(W1)** a FULL-mode **step-scoped mass DELETE** of every link for centroid-bearing parcels (`DELETE FROM parcel_buildings WHERE parcel_id IN (SELECT id FROM parcels WHERE ${baseFilter})`), **(W2)** a **batch-scoped UNGUARDED `UPDATE … SET is_primary = false WHERE parcel_id = ANY($1) AND is_primary = true`** — class **G** `set_based_scoped` hiding inside a "link" step, **(W3)** a **batch-scoped guarded upsert** — class **A**. There is no `<> ALL($1)` departure delete and no zero-match delete anywhere in the file. **Incremental mode never retracts anything.** §1.4's per-target form is what makes this expressible; §8.2's scalar label is what concealed it.
4. **The write key is COMPOSITE and the generator refuses composite keys BY NAME.** `parcel_buildings` unique key = `(parcel_id, building_id)` (`parcel_buildings_parcel_id_building_id_key`, live catalog). `scripts/lib/step/write.js buildWritePlan` throws: *"composite keys are not supported by the generated class-B write … declare a single-column key or extend write.js first."* The schema ALLOWS an array key (`definitions.write.key` is `anyOf: [columnName, array]`); the generator does not. **Hard blocker for any library-owned write. Ask A-1/LG-2.**
5. **The differential has THREE invocations, not two — and one of them costs 21.9 minutes.** `scripts/manifest.json` gives `link_massing` `"chain_args": { "sources": ["--full"] }`; `run-chain.js` builds `extraArgs = [...(scriptEntry.chain_args?.[chainId] || [])]`. So `sources` runs with `--full`, `permits` with `[]`, standalone with neither. Pilot 2 explicitly recorded *"zero invocation divergence to normalise"*; **that does not transfer.** Measured durations from `pipeline_runs`: the last genuine FULL relink (2026-07-07, `sources`) took **1,312,996 ms = 21.9 min**; every gated-incremental run since is **4.5–7.0 s**. A forced-FULL capture also **bumps `linked_at` on all 520,492 rows**, which re-scopes `enrich_parcels`. **The FULL capture is a budgeted, declared act — not a repeatable convenience.**
6. **Fix density is 69.8% and the `Severity:` fence heuristic returns ZERO.** `git log --oneline -- scripts/link-massing.js | wc -l` → **43**; `git log --pretty=%s … | grep -c "^fix("` → **30**; `git log --format=%B … | grep -ci "^severity:"` → **0** (also 0 for `lesson-routing`). Pilot 2's fence-discovery instrument (count Spec 05 §5 footers) is **blind here** — the newest commit is 2026-07-08 and the footer schema post-dates all 43. **G3's fence corpus for this step is the 30 `fix(` commits**, adjudicated by `git log -S` per construct, not a footer grep. This is the highest-churn, highest-fix-density step converted so far.

## Key files — every path verified this session (`ls` / `wc -l` / `node -e` / live query)

| Path | Measured |
|---|---|
| `scripts/link-massing.js` | **740 lines** · `pipeline.run('link-massing', …)` at `:164` · **0** `module.exports` (nothing exported) · 2 `throw new Error` · 1 `try` / 1 `catch` / **0** `finally` · 1 `emitSummary` · 1 `emitMeta` · 10 `pool.query` + 3 `client.query` · 4 `Date.now()` · 0 `new Date(` · 0 `process.argv` · 2 `process.env` · 0 `fetch(` · 0 `console.` · 9 declared audit metrics |
| `scripts/lib/massing-full-gate.js` | **60 lines** · `LINK_MASSING_CODE_VERSION = 'v2-building-centroid-in-parcel'` · `decideMassingFull` · `evaluateMassingFullGate` · hand-maintained 4-slug array `('sources:link_massing','permits:link_massing','link_massing','link-massing')` · git: **1** commit, **0** `Severity:` |
| `scripts/manifest.json` | `link_massing` at `:30` — `file: scripts/link-massing.js`, `supports_full: true`, `supports_dry_run: false`, **`chain_args: {"sources": ["--full"]}`**, `telemetry_tables: ["parcel_buildings"]`. Chain member at `:80` (`permits`, after `link_neighbourhoods`) and `:105` (`sources`, after `massing`) |
| live `parcel_buildings` | **520,492 rows · 485,135 distinct `parcel_id` · 417,702 distinct `building_id` · 113 MB** · 8 columns, ALL `NOT NULL` with defaults · unique `(parcel_id, building_id)` · partial unique `idx_parcel_buildings_one_primary (parcel_id) WHERE is_primary` (**0 violations**) · **RLS enabled, 0 policies** · `linked_at` **0 NULL**, min == max == `2026-07-07T15:23:54.234Z` |
| live `parcels` / `building_footprints` | `parcels` **486,530** (486,530 with centroid, 486,530 with `geom`; **4,062 MB**) · `building_footprints` **427,077** (all with `geom` + centroid; **511 MB**; 0 NULL/0 zero `footprint_area_sqm`) · GiST: `idx_parcels_geom_gist`, `idx_building_footprints_geom_gist` |
| live `pipeline_runs` | **59 rows** across 4 slug forms: `sources:link_massing` 14 completed / 3 failed (last 2026-07-08) · `permits:link_massing` 33 completed / 1 failed / **6 skipped** (last 2026-07-17) · `link_massing` 1+1 (2026-03-03) · `link-massing` 0. `records_meta` **2,026–2,039 B**, **11 audit rows** (9 + 2 `sys_`), verdict PASS, `phase` **8** in sources / **9** in permits |
| `scripts/lib/step/{index,write,acquire,staleness,verdict,config,ledger,validate}.js` | 815 / 349 / 373 / 255 / 236 / 148 / 133 / 113 lines. `index.js` growth waves name **`invalidation + counters scoped by writes.key` → LINK/MATCHER** as the next unshipped wave |
| `scripts/steps/_schema/step.schema.json` | **18** required categories · `writeDiscipline.class` 13-value enum incl. `link_full_retraction`, `set_based_scoped`, `guarded_upsert` · `identity.archetype` 8-value enum incl. `LINK`/`MATCHER` · `terminal.kind` 9 values · `check.kind` 10 values · `check.when` 3 values (`pre`/`pre_write`/`post`) · **6** `allOf` archetype profiles |
| — LINK/MATCHER profile | `x-profile: "LINK / MATCHER — invalidates required (non-empty) and counters scoped by writes.key."` ⇒ `outputs.invalidates` `minItems: 1` and `counters` must be an object (never `"none"`) |
| `scripts/steps/_schema/converted.json` | **2** entries (`scripts/quality/assert-schema.js`, `scripts/load-ravines.js`) |
| `scripts/steps/_schema/fixtures/valid/` | `assert_schema` · `enrich_heritage` · `grandfathered-unguarded`. **No `link_massing` draft descriptor exists** — pilot 3 authors it from scratch |
| `scripts/hooks/check-step-shape.mjs` | measured now: `60/60 unconverted manifest step files violate the frozen shape … ✅ Step-shape gate clean (2 converted step file(s) enforced; 2 compute module(s) enforced)` ⇒ **62** manifest step files; pilot 3 arms **3/62** |
| `scripts/analysis/capture-step-golden.js` | **599 lines** · `--tables`, `--table-row-ceiling` (default **100000**), `--invariants`, `--args`, `--compare` all present · `tableStateDecision` records `skipped_reason: 'over_ceiling'` above the ceiling |
| `scripts/seeds/logic_variables.json` | 3 massing vars present: `massing_shed_threshold_sqm` (20 / 1–200) · `massing_garage_max_sqm` (60 / 1–500) · `massing_nearest_max_distance_m` (50 / 1–500). Live `logic_variables` **428 rows**, the same 3 present with the same values; **0** rows matching `%link_rate%` |
| `src/features/admin-controls/components/GlobalConfigCard.tsx` | all three already in a GROUP at `:113-115`; `:248` special-cases `massing_shed_threshold_sqm` |
| `docs/reports/defect-ledger.md` | **32 lines, 24 rows** — 13 `AS-D*` + 11 `LR-D*`. Pilot 3's prefix is `LM-D*` |
| `docs/reports/golden/` | `assert_schema/`, `load_ravines/` only |
| `src/tests/steps/` | `assert_schema/`, `load_ravines/` only (`load_ravines/violations.test.ts` = 2,247 lines, 97 `it`) |

**Does not exist yet (measured):** `scripts/link-massing.descriptor.json` · `scripts/link-massing.notes.json` · `scripts/lib/compute/link-massing.js` · `src/tests/steps/link_massing/` · any `link_massing` assessment report · any `LM-D*` ledger row · any library write path for a composite-key or DB→DB step.

### Tests that read this step's source text or exports (the G4d re-homing surface)

| File | Measured | What goes RED at commit 7 |
|---|---|---|
| `src/tests/link-massing.infra.test.ts` | 62 lines, **5 `it`**, `SRC = readFileSync(scripts/link-massing.js)` | asserts `/logicVars\.massing_shed_threshold_sqm/` ×3, `LOGIC_VARS_SCHEMA`, `loadMarketplaceConfigs`, `validateLogicVars` — **all four strings leave the file** under `ctx.config`. Re-home to the descriptor's `config.logic_variables[]` + the compute's `ctx.config.*` reads (the four-surface battery already proves this shape) |
| `src/tests/massing-full-gate.logic.test.ts` | 91 lines, **12 `it`**; a source-contract `describe` reads `SRC` | `decideMassingFull(`, `/const FULL_MODE = decideMassingFull\(/`, `/if \(FULL_MODE\)/`, `/cleared .* existing links for re-evaluation/`, `code_version: LINK_MASSING_CODE_VERSION`, `building_footprints_count: massingGate.buildingCount` — **the whole gate moves to `scripts/lib/step/staleness.js`**; the 9 pure-function `it`s survive against `massing-full-gate.js` |
| `src/tests/massing.logic.test.ts` | 523 lines, **85 `it`**, **3** `describe`s read the source (B13 perf · PostGIS detection guard · the WF3 predicate flip) | the predicate-flip lock is a **fence** (`b16c036`) and must be re-homed, not deleted |
| `src/tests/pipeline-sdk.logic.test.ts` | 2,467 lines, **202 `it`**, **5** `readFileSync` sites on this file | `IS DISTINCT FROM` guard · `records_updated uses parcelsLinked` · `uses streamQuery (not pool.query)` ×2 · `uses ST_Contains when PostGIS is available` |
| `src/tests/chain.logic.test.ts` | 1,672 lines; `:1544` reads `src('link-massing.js')` | `not /records_updated\s*:\s*buildingsUpserted/` + `toContain('parcel_buildings_written')` — **`parcel_buildings_written` becomes a declared check id, so the string survives only if the check keeps that id** |
| `src/tests/pipeline-logic-vars-coercion.infra.test.ts` | 112 lines; `:25` lists `scripts/link-massing.js` in an 18-file array with a hard `expect(PIPELINE_SCRIPTS).toHaveLength(18)` | `LOGIC_VARS_SCHEMA` + `z.coerce.number()` leave the file ⇒ remove the entry and move the literal to **17**, or retarget the loop. **Same shape as pilot 2's `LOADER_SOURCES`** |
| `src/tests/pipeline-advisory-lock.infra.test.ts` | 340 lines; `:77` `'scripts/link-massing.js': 91` | **SURVIVES**: `:246-253` needs `const ADVISORY_LOCK_ID = 91;` textually (§5.4); `:258-269` already accepts `pipeline.step(` from pilot 1; `:275` uniqueness and `:294-301` registry match are path-keyed |
| `src/tests/db/link-massing.db.test.ts` | 72 lines, 1 `it`, `describe.skipIf(!dbAvailable())` | live geometric proof of the predicate fix — path-keyed, survives |
| `src/tests/admin.ui.test.tsx` `:892-908` | reads `load-massing.js` source about the `link-massing` string | **not this file** — no action |

## Before/after guarantees in the step's own specs — and the library phase that must honour each

> **`tasks/lessons.md` (last line):** *"An archetype ruling that reorders phases (acquire→write→checks) must re-derive every 'before X' guarantee in the step's own spec — Pilot 2 silently retired Spec 59 L7/L8 abort-before-write until the Regression Guardian caught it (Fold C, `e64a5676`)."* This is that enumeration, done **before** the archetype ruling rather than after it. **13 guarantees.**

| # | Guarantee (anchor) | Source | Library phase that must honour it |
|---|---|---|---|
| **B-1** | **`massing` (load-massing) runs BEFORE `link_massing`** — *"`link-massing.js` runs as the next manifest chain step in the `sources` chain (a chain-step, not an auto-trigger fired from within the loader)"* | Spec 56 §3 Core Logic item 5 · `manifest.chains.sources` `"massing", "link_massing"` | `inputs.reads.steps[]` with a version pin; chain ordering is `run-chain`'s, but the declaration is what makes a reorder visible |
| **B-2** | **`compute_centroids` runs BEFORE `link_massing`** — the base filter is `centroid_lat IS NOT NULL AND centroid_lng IS NOT NULL`; with NULL centroids the work set is empty and the run silently succeeds | grep anchor `const baseFilter = 'centroid_lat IS NOT NULL` (2 sites) · `manifest.chains.sources` order · Spec 122 §6.4 *"`:237`/`:434` … a NOT-NULL **eligibility filter**"* | `inputs.reads.tables[].expect_nonempty` + a declared `guards.empty_source`; **a zero-work run must not read PASS** |
| **B-3** | **`link_massing` runs BEFORE `enrich_parcels`** — *"**Precondition:** `link-massing` should precede enrich-parcels for the heritage freeze; otherwise heritage parcels emit `heritage_no_massing`"* | Spec 65 §MB-7 (grep `heritage_no_massing`) | `outputs.invalidates` → `parcels.massing_enriched_at`; declared, and the LINK/MATCHER wave is where it gains a consumer |
| **B-4** | **The GiST-index precondition HALTS BEFORE the join** — *"refusing the building-centroid-in-parcel join (would seq-scan 486K parcels)"* | grep anchor `no GiST index on parcels.geom` | `guards.requires[].kind: index`, asserted **before** any query in the join phase. If it moves after, a missing index turns a 22-min run into an unbounded one |
| **B-5** | **The full-gate decision is made BEFORE the ghost-link DELETE and BEFORE the count query** — `FULL_MODE = decideMassingFull(...)` is computed at the top; the DELETE is `if (FULL_MODE)` | grep anchors `const FULL_MODE = decideMassingFull(` and `if (FULL_MODE)` | `staleness.trigger` evaluated **pre-compute**, `mode_select` handed to the write phase. A gate evaluated after the DELETE deletes 520K rows it then decides it did not need |
| **B-6** | **The gate reads the LAST COMPLETED PRIOR run** — `WHERE pipeline IN (…4 forms…) AND status='completed' ORDER BY completed_at DESC LIMIT 1` | `massing-full-gate.js` `evaluateMassingFullGate` | The library opens its ledger row with a **non-`completed`** status before the gate runs (`ledger.js openLedgerRow`) — **verify, do not assume**: if the library ever finalized early, the step would read *itself* and the gate would always say "unchanged". Both directions locked |
| **B-7** | **The FULL DELETE is ONE statement, BEFORE the loop, in ONE transaction, scoped identically to the parcels being re-evaluated** — *"ONE DELETE, before the loop, in a transaction, scoped identically to the parcels being re-evaluated"* | grep anchor `ONE DELETE, before the loop` | `outputs.writes[]` **order is declared and the runner executes it in order** (§1.4). A retraction after a partial rebuild leaves the table half-empty for the window |
| **B-8** | **`is_primary` is cleared for affected parcels BEFORE the upsert** — *"prevents partial unique index violation (mig 081 `idx_parcel_buildings_one_primary`) when primary shifts to a different building"* | grep anchor `Clear is_primary for affected parcels before upserting` | Declared write **W2** must precede **W3** in `outputs.writes[]`. **⚠️ asymmetric:** the JS-fallback path has **no** such clear — adjudicate at G3 as a fence-or-redundancy, do not silently unify |
| **B-9** | **The nearest fallback runs only AFTER the centroid pass, and only over the batch's unmatched parcels** — `const unmatchedIds = parcelIds.filter(id => !matchedParcelIds.has(id))` | grep anchor `Nearest-fallback (parity with the JS path)` | Phase order inside the join. Reordering makes every parcel take the 0.60-confidence path |
| **B-10** | **The nearest query's degree-span bbox prefilter is applied BEFORE the geography distance** — *"MANDATORY before the geography `ST_DWithin`/`ST_Distance` or it runs a nested-loop over all footprints (`tasks/lessons.md`)"* | grep anchor `MANDATORY before the geography` | The generated SQL must keep `bf.geom && ST_Expand(p.geom, $2)` ahead of `ST_DWithin(::geography)`. A generator that emits the distance predicate alone reproduces the lesson |
| **B-11** | **`RUN_AT` is captured from the DB clock BEFORE any write** — `const RUN_AT = await pipeline.getDbTimestamp(pool)` (Spec 47 §R3.5) | grep anchor `getDbTimestamp` | Library already does this inside the lock for the ingest path; the LINK path must too, or two batches straddle a second and `linked_at` stops being a single watermark |
| **B-12** | **`linked_at` is written on INSERT and NOT part of the change guard** — *"§47 §6.1 — `linked_at` on INSERT so first-time links are never NULL"*, and the `WHERE … IS DISTINCT FROM` clause names only `is_primary`/`structure_type`/`match_type`/`confidence` | grep anchors `linked_at on INSERT` and the four `IS DISTINCT FROM` lines | **Finding 2.** `write_discipline.guard_columns` must be the explicit four, never `all_declared` |
| **B-13** | **Config validation happens AFTER the lock, not before it** — `withAdvisoryLock(...)` opens at `:165`, `loadMarketplaceConfigs` + `validateLogicVars` run inside | grep anchors `withAdvisoryLock` then `validateLogicVars` | **This is the opposite of `link_wsib`'s A1/A2 fence** (Spec 122 §1.3: *"config validation must be hoisted **above** the gate — a SKIP-eligible step must never let an invalid threshold hide behind a green SKIPPED summary"*). `config.js`'s `hoisted_above_gate` exists for exactly this. Converting silently changes WHERE the throw happens: **declare the choice, do not inherit it** |

## Gate ledger — G0–G8 + G4d + G-shape

Requirement text quoted from Spec 123 §6 / §6.1. "Measured" = executed in this planning session, 2026-08-27, at HEAD `7f15cd5f`.

| Gate | Spec 123 requirement (anchor) | Command that proves it | CURRENT measured state |
|---|---|---|---|
| **G0** | §6 `Boundary freeze` — *"every table/column written, every audit row, exit codes, stdout — enumerated"* | `wc -l scripts/link-massing.js` · `grep -cF` per construct · `grep -oE "metric: '[a-z_]+'"` | ⛔ **ABSENT — no assessment report exists** (`ls docs/reports \| grep -i massing` → none). Measured surface: **740 lines · 2 `throw new Error` · 1 `try` / 1 `catch` / 0 `finally` · 1 `emitSummary` · 1 `emitMeta` · 10 `pool.query` + 3 `client.query` · 4 `Date.now()` · 0 `new Date(` · 0 `process.argv` · 2 `process.env` · 0 `fetch(` · 0 `console.` · 9 declared audit metrics (+2 `sys_` from the SDK = 11 measured live) · 8 exit constructs**. Domain writes: `parcel_buildings` — **W1** step-scoped DELETE (FULL only) · **W2** batch-scoped unguarded `UPDATE … is_primary = false` · **W3** batch-scoped guarded upsert (4-column `IS DISTINCT FROM`, `linked_at` excluded) — ×2 code paths (PostGIS / JS fallback), and the JS path has **no W2** |
| **G1** | §6 `Archaeology` — *"churn + fix density + fence density + 20% coupling computed (batch)"* | `git log --oneline -- <f> \| wc -l` · `git log --pretty=%s \| grep -c "^fix("` · `git log --format=%B \| grep -ci "^Severity:"` | ⚠️ **COMPUTED, and it is the worst of the three pilots: 43 commits · 30 `fix(` · fix density 69.8% · `Severity:` fence density 0** (`--follow` also 43 — no rename history). Range 2026-02-25 → 2026-07-08. **The 0 is an instrument limit, not a clean record** (finding 6). Gate lib: 1 commit, 0 `Severity:`. **20% change-coupling: NOT computed** — no batch artifact, no generator (same U-item as pilots 1–2) |
| **G2** | §6 `Structure` — *"churn×complexity plot; top-right quadrant named"* | *(no command exists)* | ⛔ **ABSENT.** `ls scripts/analysis \| grep -i "churn\|complex\|risk"` → nothing. Record **`ASSESSMENT-INCOMPLETE`** per §6.2 clause 3, as pilots 1–2 did. ⚠️ Note it stings here: 740 lines × 69.8% fix density is the definition of a top-right quadrant, and the instrument that would say so does not exist |
| **G3** | §6 `Intent Ledger` — *"100% of top-right + fence>0 constructs have a recovered why or an explicit INTENT-UNKNOWN"* | `git log -S'<construct>' -- scripts/link-massing.js` per construct; adjudication per §7.1 (**a human adjudicates**) | ⛔ **RED: ~12 fence candidates identified from the 30 `fix(` subjects, 0 ledger entries.** Named now so commit 2 is retrieval, not discovery: `b16c036d` **predicate flip** (parcel↔building coverage 58%→99.7%) · `e99f6753` the same flip in the JS path · `7a0fc5a0` EPSG:3857→4326 reprojection · `fc038790` `building_footprints.geom` via `ST_Transform` · `45906c31` PostGIS detection guard against a missing `geom` column · `5bb31faf` PostGIS argument order + deterministic primary + the partial unique index (**B-8**) · `06320b39` `linked_at` NULL on first INSERT (**B-12**) · `b36d0596` `IS DISTINCT FROM` guard on the upsert (**finding 2**) · `82e33ad2` N+1 → in-memory grid (B13) · `3cc93feb` OFFSET → keyset cursor · `0d5edf40` cumulative link rate in the audit table · `c1ef0b73` advisory-lock retrofit (lock 91) · `d324ab27` externalize the three massing heuristics · `2f3d0e4e` the P11-2 `--full` gate. §7.1: **the agent that discovers may not retire** |
| **G4** | §6 `Risk class` — *"A/B/C with chance and impact factors shown"* | derived from G1 + measured blast radius | ✅ **CLASS A, on BOTH factors — the first pilot where neither is low.** *Chance*: 43 commits / 30 fixes / 69.8% fix density (pilot 2 was 2 / 0 / 0%). *Impact*: `parcel_buildings` is read by **7 pipeline scripts and 6 app modules** (`enrich-parcels.js` ×11 sites, `compute-cost-estimates.js`, `compute-coa-cost-estimates.js`, `refresh-snapshot.js`, `quality/assert-global-coverage.js`, `quality/audit-fk-orphans.js`, `load-massing.js`; `src/app/api/admin/stats/route.ts`, `src/app/api/permits/[id]/route.ts`, `src/app/api/quality/route.ts`, `src/lib/admin/funnel.ts`, `src/lib/leads/lead-inspect-query.ts`, `src/lib/quality/metrics.ts`). `enrich-parcels.js` derives **10 `existing_*` / `imagery_roof_*` parcel columns + `massing_enriched_at`** from it, propagated to `permits` and `coa_applications`. Class A ⇒ §6 G7's mutation clause applies |
| **G5** | §6 `Seam map` — *"DB, clock, network, argv/env each have a named seam"* | `grep -cF` per seam | ⚠️ **PARTIAL — four of four §6 seams are present, and the fifth thing that matters is not a §6 seam.** **DB** = the `pool` from `pipeline.run` (10 `pool.query` + 3 `client.query`, the latter inside `pipeline.withTransaction`) — `ctx.pool` already exists. **Clock** = split: `pipeline.getDbTimestamp(pool)` for the written `linked_at` (Spec 47 §R3.5 clean, 0 `new Date(`) **and 4 `Date.now()` for elapsed time** (`startTime`, `loadStart`, `loadElapsed`, `durationMs`) — `Date.now()` is banned in a compute by `compute-no-wall-clock`, so all four must go through `ctx.clock`. **Network** = **none** (0 `fetch(`) — the first pilot with an empty network seam. **argv/env** = 0 `process.argv` **direct**, but `pipeline.isFullMode()` reads `process.argv.includes('--full')` **one frame up**, which no compute-shape regex can see; 2 `process.env` (`LINK_MASSING_FORCE_FULL`, `PIPELINE_CHAIN`). ⛔ **The fifth surface is the SPATIAL JOIN PREDICATE itself** — a domain SQL string with no declaration slot and no seam. **This is finding 1** |
| **G6** | §6 `Classification` — *"every behaviour CONTRACT / INCIDENTAL / DEFECT; every DEFECT has a ledger ID"* | `grep -c "^| " docs/reports/defect-ledger.md` | ⛔ **RED: 0 `LM-D*` rows** (register has 24: 13 `AS-D*` + 11 `LR-D*`). Candidates visible now, **to be adjudicated at commit 4, not asserted here**: the **58,050** parcels flagged `footprint_exceeds_lot` downstream while `link_massing` emits **no counter** for a footprint-exceeds-lot link · the **103,530** `nearest` links at confidence 0.60, of which **48,559 (46.9%)** have a footprint larger than their own lot · **66,906** buildings that are `is_primary` for more than one parcel (**167,443** parcels affected) · the true silent swallow Spec 122 §1.6 already names by name (*"`link-massing.js:557-559` (invalid geometry → parcel silently reclassified as no-match, **no counter**)"*) · the two-batch-size divergence (`BATCH_SIZE = 500` in the JS path vs `pipeline.BATCH_SIZE = 1000` in the PostGIS path) · the hardcoded `>= 50` link-rate floor with **0** registered logic variables · the JS path's missing W2 · `records_total = processed` where `processed` counts the permanently-unmatchable tail. **§6: "Any zero in G6–G8 is a hard stop."** |
| **G7** | §6 `Test adequacy` — *"class-A: mutation ≥80% on covered code; every class-A behaviour proven red"* | `ls src/tests/steps/link_massing` · `grep -n "mutate" stryker.config.mjs` | ⛔ **RED, and the mutation clause is unsatisfiable as written** (`stryker.config.mjs` `mutate:` lists 3 files under `src/features/leads/lib/`; `scripts/` is outside `src/` — Spec 123 §4.8 concedes this is *"a NEW dependency"*). **Prove-red half:** `src/tests/steps/link_massing/` does not exist → **0 of 44** 55-A claims red or green |
| **G8** | §6 `Differential` — *"zero unexplained diffs; every explained diff points at a Defect Ledger ID"* | `node -r dotenv/config scripts/analysis/capture-step-golden.js --step=scripts/link-massing.js --chain=sources --args=--full --tables=parcel_buildings …` | ⛔ **RED FOR THREE INDEPENDENT REASONS, all measured.** (a) **Three invocations, not two** (finding 5): `sources` + `--full`, `permits` bare, `--chain=none`. (b) **The default table-state capture is unusable on this table**: `parcel_buildings` is **520,492 rows > the 100,000 ceiling** ⇒ `skipped_reason: 'over_ceiling'`, and even above the ceiling the harness's `md5(string_agg(t::text …))` renders the **whole row incl. `id` (bigserial) and `linked_at` (`RUN_AT`)** — both change on any FULL relink, so the hash would differ on a logically-identical result. **A column projection is REQUIRED, not an optimisation. Ask A-4.** (c) **A FULL capture costs 21.9 min and mutates 520,492 rows**, and the `linked_at` bump re-scopes `enrich_parcels` (finding 5) — so the forced-FULL half of the differential is a budgeted act with a declared cleanup, not a repeatable command |
| **G4d** | §6.1 — *"every fence found in P3 has a **both-directions** lock test"* | `git log --format=%B -- <f> \| grep -ci "^Severity:"` **→ 0, so use the 30 `fix(` corpus instead** | ⛔ **RED: ~12 fence candidates, 0 both-directions locks.** No `*.regression.test.ts` covers this file. What exists instead is a **source-text surface across 9 test files** (5 of which `readFileSync` the step) — enumerated in the Key-files table above. Every one must be **re-homed, not deleted**; the count that must not silently shrink is **`link-massing.infra` 5 `it` + `massing-full-gate.logic` 12 `it` + `massing.logic` 3 source `describe`s + `pipeline-sdk.logic` 5 sites + `chain.logic` 1 + `pipeline-logic-vars-coercion` 1 entry (with its `toHaveLength(18)`)** |
| **G-shape** | §6.1 — *"the converted file passes the ast-grep shape rule (Spec 122 **§5.1**, not §4.1 — doc error, pilots 1+2 both recorded it) and `pipeline.run(` no longer appears in it"* | `node scripts/hooks/check-step-shape.mjs` | ⛔ **RED by design, correctly armed.** Measured now: `60/60 unconverted manifest step files violate the frozen shape … ✅ Step-shape gate clean (2 converted step file(s) enforced; 2 compute module(s) enforced)`. Live: `scripts/link-massing.js:164` `pipeline.run('link-massing', …)`. Arms when the path is appended to `converted.json` at commit 9 (→ **3/62**) |

**Score today: G1 ⚠️(1, partial — coupling not computed) + G4 ✅(2) = 3/17**, with G5 partial and **G6/G7/G8 at zero**. §6 threshold: *"Ship at ≥14/17 with G6, G7 and G8 full."*

## P4 tunable inventory (§1.2a P4 — every literal, no exceptions)

**The baseline is BETTER than pilot 2's on one axis and worse on another.** `load_ravines` declared six variables and registered **zero**. `link_massing` declares **three** (`LOGIC_VARS_SCHEMA`) and **all three ARE registered** — seeds (`logic_variables.json`, defaults 20/60/50 with min/max), live DB (428 rows, the same three at the same values), and `GlobalConfigCard.tsx:113-115` already has them in a GROUP. `d324ab27` *"externalize massing heuristics in link-massing.js"* did the P4 work in 2026-04. **But the step's one VERDICT-BOUND threshold — the `>= 50` link-rate floor — is a bare literal with 0 registered variables** (`SELECT count(*) FROM logic_variables WHERE variable_key ILIKE '%link_rate%'` → **0**), which is precisely the P4 violation, and it is the number that decides PASS vs FAIL.

| # | Literal (greppable anchor) | Disposition | Var name / default / bounds / group — or the structural why |
|---|---|---|---|
| **T1** | `massing_shed_threshold_sqm` (`LOGIC_VARS_SCHEMA`, used in `classifyStructure`) | **TUNABLE — already registered** | `massing_shed_threshold_sqm` · 20 · min 1 · max 200 · `on_invalid: default` · existing GROUP. Declare in `config.logic_variables[]`; compute reads `ctx.config` |
| **T2** | `massing_garage_max_sqm` (same) | **TUNABLE — already registered** | `massing_garage_max_sqm` · 60 · min 1 · max 500 · `on_invalid: default` |
| **T3** | `massing_nearest_max_distance_m` (same; also feeds `nearestDegSpan`) | **TUNABLE — already registered** | `massing_nearest_max_distance_m` · 50 · min 1 · max 500 · `on_invalid: clamp` (a 0 would silently disable the fallback; a 5,000 would nested-loop) |
| **T4** | ⛔ **`50`** — `massingLinkRate >= 50 ? 'PASS' : …` **and** the duplicated render string `'>= 50%'` | ⛔ **TUNABLE + VERDICT BOUND — 0 registered today. THE P4 violation of this step.** | `link_massing_link_rate_fail_pct` · default 50 · min 0 · max 100 · `on_invalid: fail` · GROUP "Data Quality Thresholds". Rendered via `checks[].limit_from_config` (pilot 2's A-4 ruling) so the threshold column shows the number in force. **Two literals collapse to one source.** Live headroom: **99.71%** |
| **T5** | `0.95` — the `centroid_in_parcel` confidence, written to `parcel_buildings.confidence` (**2 sites**: PostGIS + JS paths) | **TUNABLE (proposed) — 0 registered** | `link_massing_centroid_confidence` · 0.95 · min 0 · max 1 · `on_invalid: fail`. **It is consumed**: `enrich-parcels.js` reads `pb.confidence AS link_confidence` in the existing-structure pass. Two literals in two paths that must agree is exactly the `link_parcels` ternary class §1.7 names |
| **T6** | `0.60` — the `nearest` confidence (**2 sites**) | **TUNABLE (proposed) — 0 registered** | `link_massing_nearest_confidence` · 0.60 · min 0 · max 1 · `on_invalid: fail`. Same consumer, same two-site risk |
| **T7** | `GRID_SIZE = 0.003` (`~333m grid cells`) | **TUNABLE (proposed, JS-path only) — 0 registered** | `link_massing_grid_degrees` · 0.003 · min 0.0005 · max 0.05 · `on_invalid: clamp`. It is a **search radius**, not an API constant; it also seeds `searchRadius` and `gridSpan`. If the JS path is declared a `limitations[]` legacy path instead, record that ruling explicitly |
| **T8** | `BATCH_SIZE = 500` (JS path) **vs `pipeline.BATCH_SIZE = 1000`** (PostGIS path, `pipeline.js:693`) | ⚠️ **STRUCTURAL, but the DIVERGENCE is a finding** | → `execution.batch` — **one** declared value with a `why`. Two batch sizes in one file for the same work is undeclared and unexplained. Adjudicate at G3 (`git log -S 'BATCH_SIZE'`), do not unify silently |
| **S1** | `ADVISORY_LOCK_ID = 91` | **STRUCTURAL** | → `identity.lock: 91`; **kept textually** per §5.4 so `pipeline-advisory-lock.infra.test.ts:246-253/:294-301` stay green. Registry `:77` already says 91; `backfill-realtor-permit-trades.infra.test.ts:20,125` records a past collision on 91 — do not renumber |
| **S2** | `PARAM_FLUSH_THRESHOLD = 30000` (*"§9.2: flush before hitting PG 65,535 limit"*) | **STRUCTURAL** | the Postgres bind-parameter ceiling ÷ 7 columns, with headroom → `execution.partial_fill: batched` + a `why`. Not an operator knob (pilot 2's S9 precedent) |
| **S3** | `MERCATOR_ORIGIN = 20037508.342789244`, `180`, `Math.PI` | **STRUCTURAL** | EPSG:3857 half-circumference and degree math — API constants |
| **S4** | `200` — the CRS auto-detect sentinel (`Math.abs(sample[0]) < 200`) | **STRUCTURAL** | WGS84 longitude is ±180; 200 is the margin. Record the why so P4's grep does not re-open it |
| **S5** | `R = 6371000` (haversine) | **STRUCTURAL** | mean Earth radius |
| **S6** | `4326` (**2 sites**: `ST_SetSRID(ST_MakePoint(…), 4326)` and the `ST_Expand` join) | **STRUCTURAL** | → `guards.srid: 4326`; matches both live tables' `geom` |
| **S7** | `78000.0` — `nearestDegSpan = nearestMaxDistanceM / 78000.0` | **STRUCTURAL** | metres-per-degree at Toronto's latitude, over-covering E-W; the in-file comment cites the `enrich-parcels` precedent AND `tasks/lessons.md`. Changing it changes what "50 m" means, not how much of it we accept |
| **S8** | `10000` — `if (processed % 10000 === 0 …) pipeline.progress(…)` (**2 sites**) | **STRUCTURAL — and the CALL has no compute home** | narration cadence. §5.5 (2): *"no `console.*` — narration uses the `ctx.log` seam"*. `pipeline.progress` writes to stdout directly ⇒ either route through `ctx.log` or retire. **A stdout diff either way — declare it (D-9)** |
| **S9** | `'centroid_in_parcel'` / `'nearest'` · `'primary'` / `'garage'` / `'shed'` / `'other'` | **STRUCTURAL — a written vocabulary** | `parcel_buildings.match_type` / `.structure_type` value domains, live-verified (5 `(match_type, is_primary, structure_type)` combinations exist and no others). Candidate for a `checks[].kind: vocab_coverage` |
| **S10** | `LINK_MASSING_CODE_VERSION = 'v2-building-centroid-in-parcel'` (in `scripts/lib/massing-full-gate.js`) | **STRUCTURAL — and §1.5 names it by name** | → `staleness.trigger: [..., "code_version"]` + **`staleness.fingerprint_inputs: ["scripts/lib/massing-full-gate.js"]`**. Spec 122 §1.5 uses *this exact file* as the worked example: *"`LINK_MASSING_CODE_VERSION` lives in `scripts/lib/`, not in the step, and it triggers a full destructive rebuild. Claim **#52b** is an orphan today for exactly this reason."* **Never a tunable** — bumping it is a destructive-rebuild decision |
| **S11** | The 4-form slug array in `evaluateMassingFullGate` | **STRUCTURAL — RETIRED by declaration** | `sharing.slug_forms: "derived"` (§1.7: *"`chains` and `slug_forms` are `~` derived and MUST NOT be declared"*). Live check: `link-massing` (hyphen form) has **0** rows; `link_massing` has 2 — the array is over-broad but harmless. Retire it, do not port it |
| **S12** | `8` / `9` — `phase: (process.env.PIPELINE_CHAIN === 'sources') ? 8 : 9` | **STRUCTURAL — RETIRED by declaration** | → `sharing.varies_by_chain.phase: { "sources": 8, "permits": 9 }` (§1.7: *"an explicit map, never a ternary"*). **Live-verified both values** in `records_meta.audit_table.phase`. Retires 1 of the 2 `process.env` reads |
| **E1** | `process.env.LINK_MASSING_FORCE_FULL === '1'` | **HAS A HOME** | → `override.force_full: "LINK_MASSING_FORCE_FULL"` — the box exists (`{force_full, force_run, dry_run}`). ⚠️ Unlike pilot 2, **no `accept_anomaly` is needed**: this step has no accept-override. Retires the 2nd `process.env` read |
| **E2** | `pipeline.isFullMode()` → `process.argv.includes('--full')`, **one call frame up** | ⚠️ **NO COMPUTE HOME, AND NO LINT CAN SEE IT** | `--full` arrives from `manifest.chain_args.sources`. Under §5.5 (3) a compute may not read argv; `compute-no-process-env` matches `process.env` only, so this reaches a compute **invisibly**. → `execution.invocation` + `staleness.mode_select: "tri_state"`; the library reads argv, the compute never does. **State it — a rule a linter cannot enforce is a rule that needs a check** |

**Count: 3 tunables already registered · 4 proposed new registrations (T4 verdict-bound, T5/T6 written confidences, T7 grid) · 1 undeclared batch-size divergence (T8) · 12 structural groups justified (S1–S12) · 2 env/argv reads, both with a declared home.**

## The 55-A "proven red" step

**Measured, not transcribed.** `node scripts/violations/plan-claims.mjs --checklist` prints `SELF-TEST PASSED — 17 totality assertions incl. 4 negative controls (6 original + 11 K-axis)` first — so the generator's output is admissible per Spec 121 §12b.6 — then the headings:

* `## 55-A — the hard per-conversion gate (44)` · `## 55-B — partial now, closure at the named k (5)` · `## 55-C — DEFERRED, arming k named (6)` → **55-A = 44**, unchanged from pilot 2.
* The generator is **claim-scoped, not step-scoped** — there is no `link_massing` row and there will not be one. §5.2's list is a **template copied per conversion**.
* ⚠️ **k has moved and three items' arming condition is now past.** This is conversion **#3**. `#181` (`R-181`, pg_trgm precision/recall ratchet, arms k=2) is **N/A by subject** — this step uses no trigram matching; record the N/A with its reason rather than a green tick. `#206` (`R-206`, `records_meta` merge-collision detection, arms k=2) now has **two converted producers plus a third**, so its partial is genuinely exercisable. `#160` (`R-160`, conversions rate-limited to review capacity, arms k=2, stage C1) is a **C-block programme gate at C1** — surface it in the assessment, do not silently carry pilot 2's "not gated here". **Re-verify all three against the generator at commit 6; do not assume this paragraph.**

**Mechanism, three artifacts:**
1. `src/tests/steps/link_massing/violations.test.ts` — the 44 A items + the 5 B partials (with `#181`'s N/A reasoned) + one both-directions lock per adjudicated fence. Does not exist; `vitest.config.ts` includes `src/**/*.test.ts`, so it is collected the moment it lands. Model on `src/tests/steps/load_ravines/violations.test.ts` (2,247 lines, 97 `it`).
2. `src/tests/step-conformance.infra.test.ts` — sibling-descriptor uniqueness, AJV validation (**incl. the LINK `allOf` profile: `outputs.invalidates` `minItems: 1`, `counters` not `"none"`**), the `pg.Pool` require-probe, named exports, lock↔registry (**91**), `checks.length > 0`, `loaded.length === converted.length`, the §5.5 dispatch ≡ declared checks in order, and the four-surface P4 battery.
3. `scripts/hooks/check-step-shape.mjs` + `converted.json` — armed by appending `scripts/link-massing.js` at commit 9 (**2 → 3** enforced of 62).

⚠️ **The report is machine-read by COLUMN HEADER, not by section name** (`violations.test.ts` `mdTables()` / `reportTable()`). The pilot-3 assessment report MUST carry: a `key` + `disposition` table under a heading containing **"Non-determinism inventory"**; a `commit` + `done-test` table; a `table` + `rows` table (Appendix-H boundary freeze); a `lines` + `category` + `evidence` table **plus a literal `"740 lines"` string**; and the Intent Ledger's four columns `construct` · `discovered by` · `disposition` · `adjudicated by`, with a row naming each fence commit SHA.

⚠️ **Vacuity risk on 7 of the 44** — `#30/#31/#33/#34/#35/#37/#38` are about `notes.json`; `interpretation: "none"` makes them vacuously green. **Author a real `scripts/link-massing.notes.json`**: this step has more genuine interpretation content than either prior pilot, and all of it lives only as comments today — the b16c036 predicate-flip rationale (*"a house covers ~35% of its lot so the lot centroid lands in the yard"*), the EPSG:3857 auto-detection heuristic, the `/78000.0` degree-span precedent, the two-path (PostGIS/JS) divergence, the cumulative-vs-run link-rate choice (*"run-specific rate is misleading in incremental mode where the unlinked pool is permanently unmatchable parcels"*), and the 1,395-parcel permanently-unmatchable tail.

## The nine commits (Spec 123 §7 / Spec 122 §8.1)

Commit format enforced by `scripts/hooks/validate-commit-msg.sh`: `type(NN_spec): description`.

| # | Phase / Gate | Files touched | Test(s) | Commit message |
|---|---|---|---|---|
| **1** | PH-0 boundary freeze → **G0** | `docs/reports/2026-08-<dd>-pilot3-link-massing-assessment.md` (§1: grep transcript at 740 lines · the 8 exit constructs · the 9+2 audit metrics · the 3 write disciplines W1/W2/W3 × 2 code paths · the Appendix-H `table`/`rows` table with `parcel_buildings` 520,492) | none (doc) | `docs(122_step_optimization): C1 pilot 3 PH-0 - link_massing boundary freeze (740 lines, 8 terminals, 3 write disciplines)` |
| **2** | PH-3 intent ledger → **G3** (mandatory: 30 `fix(` commits, fence density by footer is 0 and that is itself the finding) | same report (§2, four required columns) + `docs/reports/defect-ledger.md` (`LM-D*` rows) | none (doc); **a human adjudicates** per §7.1 | `docs(122_step_optimization): C1 pilot 3 PH-3 - intent ledger over 30 fix( commits; LM-D1..Dn registered` |
| **3** | PH-5 seam map → **G5** | same report (§3), incl. **the join-predicate surface with no seam** and the `isFullMode()` argv-read-one-frame-up | none (doc) | `docs(122_step_optimization): C1 pilot 3 PH-5 - seam map (pool/2-clock/0-network/1-argv-indirect/2-env/1-predicate)` |
| **4** | PH-6 classification → **G6** | same report (§4) — CONTRACT / INCIDENTAL / DEFECT, every DEFECT ledger-IDed | none (doc) | `docs(122_step_optimization): C1 pilot 3 PH-6 - CONTRACT/INCIDENTAL/DEFECT, every defect ledger-IDed` |
| **5** | Golden master (4-tuple ×3 invocations) → **G1′** | `scripts/analysis/capture-step-golden.js` — **grow it** with the column projection (Ask A-4); `docs/reports/golden/link_massing/{sources-full,permits,standalone}.json` + the non-determinism inventory (§5, `key`/`disposition`) + an `--invariants` file | the harness's own self-test + `--compare` exit 0 on a repeat capture of the **incremental** path | `feat(122_step_optimization): C1 pilot 3 - golden master x3 + column-projected table hash for the 520K junction` |
| **6** | PH-7 test design + **prove red** → **G7** | `src/tests/steps/link_massing/violations.test.ts`; one both-directions lock per adjudicated fence; **the `guard_columns` lock (finding 2) is the first one written** | `npx vitest run src/tests/steps/link_massing/` — **must be RED** | `test(122_step_optimization): C1 pilot 3 PH-7 - 44 55-A violations + partials + fence locks (red)` |
| **7** | Descriptor + compute verbatim + **library growth** → **G2′** | `scripts/link-massing.descriptor.json` · `scripts/link-massing.notes.json` · `scripts/lib/compute/link-massing.js` · `scripts/link-massing.js` → frozen shape · `scripts/lib/step/*` (LG-1…LG-7) · `scripts/steps/_schema/step.schema.json` (schema pre-pull, R4) · `scripts/seeds/logic_variables.json` (+4) · `GlobalConfigCard.tsx` (+4 GROUP entries) · the 6 test files re-homed | `npx vitest run src/tests/step-conformance.infra.test.ts src/tests/link-massing.infra.test.ts src/tests/massing*.test.ts src/tests/pipeline-sdk.logic.test.ts src/tests/chain.logic.test.ts src/tests/pipeline-logic-vars-coercion.infra.test.ts src/tests/pipeline-advisory-lock.infra.test.ts` | `refactor(122_step_optimization): C1 pilot 3 - link_massing to frozen shape; compute extracted verbatim` |
| **8** | Peel — one concern per commit, green diff after **every** peel | **8a** gating (the two-signal full-gate → library `staleness`, `fingerprint_inputs`, `mode_select: tri_state`) · **8b** verdict/audit (9 metric rows → declared checks; retire the parallel-boolean cascade; the `phase` map) · **8c** thresholds/checks (T4–T7 `ctx.config` + `limit_from_config`) | full differential re-run after each | `refactor(122_step_optimization): C1 pilot 3 peel a/b/c - <concern>` (three commits) |
| **9** | Differential + cutover → **G8, G4d, G-shape** | `scripts/steps/_schema/converted.json` (+1 → **3**) | `node scripts/hooks/check-step-shape.mjs` exit 0 with **3** enforced; differential green on **all three invocations**, incl. one forced FULL | `feat(122_step_optimization): C1 pilot 3 cutover - link_massing converted; shape gate armed (3/62)` |

> ⚠️ §7's *"old and new are the same file at two commits, invoked identically by the same `spawnStepChild`"* holds only per-invocation here. `manifest.json` declares `chain_args` for exactly **two** steps (`enrich_parcels` and `link_massing`), and this is one of them.

## Declared diffs the differential WILL show (state before capture, §5.3)

| # | Diff | Old (measured) | New | Disposition |
|---|---|---|---|---|
| **D-1** | `PIPELINE_META.reads` | `{parcels: [id, centroid_lat, centroid_lng, geometry], building_footprints: [id, geometry, footprint_area_sqm, centroid_lat, centroid_lng]}` | derived from `inputs.reads.tables[]` — **must reproduce both tables and all 9 columns**. ⚠️ The step also reads `parcels.geom` and `building_footprints.geom` in the PostGIS path and declares **neither** | **CONTRACT change + a pre-existing under-declaration.** Declare `geom` on both; that is a real, explainable diff pointing at an `LM-D` id |
| **D-2** | `PIPELINE_META.writes` | `{parcel_buildings: [parcel_id, building_id, is_primary, structure_type, match_type, confidence, linked_at]}` (7 of 8; `id` is the serial) | derived from `outputs.writes[].columns[].name` | declared normalisation — list the same 7, `written: "step"` on all |
| **D-3** | audit-table shape | 9 hand-pushed metrics + 2 `sys_`, **verdict from a parallel boolean** (`massingAuditRows.some(r => r.status === 'FAIL') ? …`) | one row per **selected check**, keyed by `check.id`, `threshold` from `check.limit` / `limit_from_config`, verdict from `verdict.js deriveVerdict` | **STRUCTURAL to §5.1.** Measured baseline: 11 rows, verdict PASS, phase 8 (sources) / 9 (permits) |
| **D-4** | INFO metric rows with no check | 7 of the 9 metrics are `status: 'INFO'`, `threshold: null` (`buildings_indexed`, `grid_cells`, `parcels_processed`, `run_matched`, `match_centroid_in_parcel`, `match_nearest_fallback`, `no_match`, `parcel_buildings_written`) | `buildAuditTable` builds rows **only** `for (const check of selected)` | **must be declared as INFO checks** or they vanish. Same mechanism pilots 1 and 2 both hit. ⚠️ `parcel_buildings_written` is **asserted by name** in `chain.logic.test.ts` — the check id must be exactly that string |
| **D-5** | ⛔ **`guard_columns`** | 4 columns: `is_primary`, `structure_type`, `match_type`, `confidence`. **`linked_at` excluded from the guard while present in the SET** | `write_discipline.guard_columns: ["is_primary","structure_type","match_type","confidence"]` — **explicit, never `all_declared`** | **THE regression risk (finding 2).** `all_declared` ⇒ 520,492 rows rewritten per run ⇒ `enrich_parcels` re-scopes 485,135 parcels. **Both-directions lock, written first at commit 6** |
| **D-6** | write-discipline decomposition | one class label in prose | **three declared targets in order** on the same table: W1 `staging_full_replace`-shaped step-scoped retraction gated on FULL · W2 `set_based_scoped` (the unguarded `is_primary` clear) · W3 `guarded_upsert` | **§8.2's `link_full_retraction` label is REFUTED by measurement** (finding 3). Declare what is there; the `class` selects the SQL |
| **D-7** | composite key | `ON CONFLICT (parcel_id, building_id)` | `outputs.writes[].key: ["parcel_id","building_id"]` — **`write.js buildWritePlan` throws today** | **BLOCKER — Ask A-1/LG-2.** The schema allows it; the generator refuses it by name |
| **D-8** | `records_meta` counters | `records_total: processed` (1,395 on the last two runs — **the permanently-unmatchable tail**), `records_new: 0` (hardcoded), `records_updated: parcelsLinked` (0) | `counters[].source` resolved by the runner, **scoped by `writes.key`** per the LINK profile | **§11 Counter Semantic Contract.** `records_new: 0` is a hardcoded literal, not a measurement — `flushInsertBatch` returns `rowCount` from a guarded upsert and cannot distinguish insert from update. Point at an `LM-D` id |
| **D-9** | stdout | `pipeline.progress(...)` every 10,000 parcels + ~6 `pipeline.log.info` lines incl. the full-gate decision line | `ctx.log` narration + the `descriptor.database` target assertion line (`target: … migrations=242 (floor …)`) | **declared normalisation.** Measure the byte delta at commit 5, both directions |
| **D-10** | `records_meta.config` | absent | **NEW** — 3 existing + up to 4 new resolved values stamped by `config.js` (~30 B/var ⇒ **~210 B/run**) | **NEW, intended** (§1.2a P4). P3 cost stated |
| **D-11** | `records_meta.terminal` | absent | **NEW** — the runner stamps the selected terminal id (~35 B/run) | **NEW, intended.** Expected: a success terminal on both incremental captures, a distinct one on the forced FULL |
| **D-12** | `records_meta.checks_passed` / `checks_failed` | absent | **NEW** — `"all"` / `0`, library-standard (~30 B/run) | **NEW, library-standard**; not declared in `emits[]` (runner defaults) |
| **D-13** | DB target refusal | none | refuses below `database.min_migration` / non-`postgres` | **NEW behaviour.** Floor must be the **latest migration this step depends on** — `081` (`idx_parcel_buildings_one_primary`, the B-8 guard) and `039` (the two GiST indexes) are the DDL dependencies; **not** the current head 242. Verify by filename before declaring |
| **D-14** | lock contention | `if (!lockResult.acquired) return;` — the SDK's bare SKIP, no `audit_table`. **Live: 6 `skipped` rows under `permits:link_massing`** | library SKIP summary with a row-derived verdict + `status='self_skipped'` | **FIX** — point at an `LM-D` id. Unlike pilots 1–2 this path has **live evidence** (6 rows) rather than being hypothetical |
| **D-15** | log tag | `pipeline.run('link-massing', …)` | `pipeline.run('link_massing', …)` — the library passes `identity.name` | INCIDENTAL; declare as a normalisation. ⚠️ `massing-full-gate.js`'s slug array contains **both** spellings, so the gate keeps matching either way |
| **D-16** | `sharing.varies_by_chain.phase` | `(process.env.PIPELINE_CHAIN === 'sources') ? 8 : 9` | explicit map `{sources: 8, permits: 9}` | declared normalisation. **Both values live-verified** — a ternary that cannot be wrong today but is the exact `link_parcels` shape §1.7 cites |
| **D-17** | `outputs.invalidates` | nothing declares it; invalidation is carried implicitly by `linked_at > massing_enriched_at` | **required, non-empty** by the LINK `allOf` profile → `parcels.massing_enriched_at` | **NEW declaration; the CONSUMER is LG-6.** Measured today: 1,395 parcels `NULL` stamp, 485,135 stamped, and `buildMassingScopeWhere` currently scopes exactly **1,395** |
| **D-18** | table state in the golden 4-tuple | `pipeline_runs` only for this step | `parcel_buildings` **520,492 rows** + a **column-projected** content hash (excluding `id` and `linked_at`) | **harness growth (Ask A-4).** Measured: `count(*)` 57 ms · full-row `md5(string_agg(t::text …))` **922–2,879 ms** (stable value `63ad18bc…`) · **projected hash 666 ms** (`d714dc77…`) · a 4-scalar invariants query **323 ms**. Over the 100,000 ceiling ⇒ must be raised explicitly with the measured cost |
| **D-19** | ⚠️ **the forced-FULL run is not a no-op, and it is expensive** | the last genuine FULL relink (2026-07-07, `sources`) took **1,312,996 ms = 21.9 min**; every run since is a gated incremental at **4.5–7.0 s** and writes **0** rows | a `--full` capture under `LINK_MASSING_FORCE_FULL=1` rewrites all **520,492** rows and bumps every `linked_at` | **REAL, PRE-EXISTING, NOT A CONVERSION DIFF — and it has a downstream cost the plan must own**: the `linked_at` bump re-scopes `enrich_parcels` from 1,395 parcels to **485,135**. Declare the cleanup (or accept the next `enrich_parcels` run's cost) **before** capturing, not after |
| **D-20** | `checks[].when` placement | no such concept; the verdict is computed after everything | the link-rate / drift checks are `when: "post"`; **any check whose FAIL should stop a write is `when: "pre_write"`** | **Pilot 2's LR-D9 lesson applied forward, not retrofitted.** Candidate `pre_write`: a mass-retraction guard on W1 — today the FULL DELETE has **no** guard at all and would happily delete 520,492 rows against an empty `building_footprints` |

## Library-growth items for a LINK/MATCHER (P3 cost stated per item)

`scripts/lib/step/index.js`'s own header schedules this wave by name: *"`invalidation + counters scoped by writes.key` → **LINK/MATCHER**"*. That line covers LG-5 and LG-6 only; LG-1…LG-4 are unscheduled and are what Ask A-1 decides.

| ID | Gap (measured) | Why a LINK needs it | I/O cost (§1.2a P3) |
|---|---|---|---|
| **LG-1** | ⛔ **No DB→DB write path.** `isIngestStep` requires `inputs.reads.externals[].url`; `link_massing` has no external, so the library drives nothing and the step falls to the ASSERT path | a LINK is read-join-write with no acquisition. §5.5 (1) says `compute(ctx)` *"iterates `ctx.checks` and does nothing else"*, so the join+write cannot live in a compute either. **Ask A-1** | **0 net** (replaces hand-written SQL) · **+~130 B/run** for `rows_scanned`/`rows_changed` + 1 audit row |
| **LG-2** | ⛔ **Composite keys refused by name.** `write.js buildWritePlan` throws on `keys.length > 1`; `parcel_buildings`'s key is `(parcel_id, building_id)` | *"the departure DELETE casts a single key array"* — the real work is an array-of-records `unnest`. The schema already permits an array key | **0 net** — the same statements, one `unnest` wider |
| **LG-3** | **No multi-target ordered write.** `runIngestPhase` refuses more than one write target by name (*"Every line below indexes `writes[0]`"*) | this step writes **three disciplines to one table in a required order** (W1 → W2 → W3, B-7/B-8) and §1.4 says *"Order is declared and the runner executes it in order"* | **0** — the same three statements, sequenced by declaration |
| **LG-4** | **No spatial/predicate seam.** `write.js` owns a fixed `GEOMETRY_VALIDATION_SQL` for a WKB-bound geometry column; nothing models *"join table A to table B on a domain predicate"* | the predicate `bf.geom && p.geom AND ST_Contains(p.geom, ST_SetSRID(ST_MakePoint(bf.centroid_lng, bf.centroid_lat), 4326))` is the step's entire domain, and B-10's bbox-before-geography ordering is load-bearing | **0** — the query already runs; the question is who holds the string. **Ask A-2** |
| **LG-5** | **Counters not scoped by `writes.key`.** `deriveCounters` resolves `counters[].source` against `{acquired, written, records_meta}`; there is no key-scoped `written` for a LINK | the LINK profile mandates it, and D-8 is the live defect (`records_new: 0` hardcoded; `records_total` = the unmatchable tail) | **0** — the counts exist; the scoping is a declaration |
| **LG-6** | **`outputs.invalidates` still has no consumer.** Declared in the schema; pilot 2 shipped it declare-only (its LG-7) | required non-empty for LINK; the real edge is `parcel_buildings.linked_at` → `parcels.massing_enriched_at` → `enrich_parcels`' D1′ scope, plus migration 242's geometry trigger on the other side | **0 at declare-time**; a consumer that asserts the edge is ~1 query/run (**~60 ms** measured for the `stale_scope` count) |
| **LG-7** | **`staleness` cannot express a two-signal mode-selecting gate.** `staleness.js` today owns the pre-acquisition validator gate and `acquire.js` the content-hash tier; neither models *"code_version OR upstream row count ⇒ run in FULL mode"* | §1.5 names this step's gate as the mechanism that forced the reshape: *"the output is a **mode**, not a skip … `on_fingerprint_change` offered only `queue · run` — never 'run in full mode'"*, and `fingerprint_inputs` was added for `massing-full-gate.js` specifically | **0 net** — 1 `COUNT(*)` on `building_footprints` + 1 prior-run `SELECT` move from the step to the library |
| **LG-8** | **Golden harness cannot fingerprint a 520K junction honestly.** Ceiling 100,000; the hash renders `t::text` incl. `id` (bigserial) and `linked_at` (`RUN_AT`) | both change on a FULL relink with identical logical output ⇒ a guaranteed false diff | **Ask A-4.** Measured: projected hash **666 ms** vs full-row **922–2,879 ms** vs 4-scalar invariants **323 ms**; ×2 captures × 3 invocations |
| **LG-9** | **No `guard_columns` sanity rule.** `resolveGuardColumns` will happily expand `all_declared` over a `RUN_AT`-valued column | finding 2 — this is the mechanism by which a single descriptor word rewrites 520K rows every run and re-scopes 485K parcels downstream | **0** — a validator rule: a column bound to the run clock may not be a guard column without a `why` |

## Reality-Check + DB Schema-Fidelity — plan-altitude asks

*(**Both FIRE.** Spec 08 fires Reality-Check by SUBJECT MATTER, not table: `link_massing` writes the junction from which `enrich-parcels.js` derives 10 `existing_*` / `imagery_roof_*` parcel columns + `massing_enriched_at`, propagated to `permits` and `coa_applications`. This is the first pilot where the derived-field chain is two hops, not one.)*

**A-6 — Reality-Check ask (FIRES). The plausibility bounds RC must state, with the numbers measured this session.**

| Quantity | Measured live | The bound RC must state |
|---|---|---|
| **link_rate** (`count(DISTINCT parcel_id) / count(parcels with centroid)`) | **99.71%** (485,135 / 486,530); the live gate floor is `>= 50%` — a **49.7-point** dead band | zone-aware or flat? A 99.7% link rate cannot fall below 50% without a catastrophe, so the gate cannot fire before the damage. **What is the real WARN band?** |
| **footprints per parcel** | 520,492 rows / 485,135 parcels = **1.073 mean**; max primaries per parcel **1** (0 violations of `idx_parcel_buildings_one_primary`) | plausible for Toronto lots? |
| **`nearest`-fallback share** | **103,530 of 485,135 linked parcels (21.3%)** are linked ONLY by the ≤50 m nearest fallback at confidence **0.60** — no building centroid is on the lot at all | is a 21.3% fallback share credible, or is it the mislink population? |
| **footprint ÷ lot area, by match type** | `centroid_in_parcel` (n=381,605): mean footprint **246.2 m²** on mean lot **932.2 m²**; **13,347 (3.5%)** exceed their lot, **4,462 (1.2%)** exceed it 2×. `nearest` (n=103,530): mean footprint **534.7 m²** on mean lot **863.3 m²**; **48,559 (46.9%)** exceed their lot, **33,182 (32.1%)** exceed it **2×** | **⚠️ the headline.** Mean primary coverage ratio over all primaries is **7.157** and **61,906** primaries have coverage > 1. **A ratio bound is mandatory** |
| **shared "primary" buildings** | **66,906** buildings are `is_primary` for **more than one** parcel; **167,443 parcels (34.5% of linked)** sit on a shared primary | one building cannot be the primary structure of 2+ parcels in the sense the cost model uses it |
| **downstream guard hit rate** | `parcels.existing_data_quality_flag = 'footprint_exceeds_lot'` on **58,050 parcels (11.9%)** — the WF3-A mislink guard in `enrich-parcels.js` NULLing the whole existing-structure block | **the link step emits NO counter for this.** 58,050 bad links are produced, shipped, and silently nulled one step later |
| **the permanent tail** | **1,395** parcels have zero links and `massing_enriched_at IS NULL`; every incremental run re-processes exactly those 1,395, matches 0, writes 0, and reports `records_total = 1395`, verdict **PASS** | `docs/reports/review_followups.md` line 13 files this as **MED/ACT**: *"`massing_zero_link_ghost`'s founding comment claims a 'measured day-one value 0' that no cloud observation supports … both cloud rows read 1395, not 0."* **Confirmed on local this session: 1,395.** The followup is CORRECT and this pilot's boundary freeze is the place to say so |

> **The ask, in one sentence:** *is a 21.3% nearest-fallback share with 46.9% of those links carrying a footprint larger than their own lot a real property of Toronto's parcel fabric, or is it the mislink population that `review_followups.md`'s "Massing mislink inventory (P13-2, HIGH)" already named — and would a forced FULL relink under `LINK_MASSING_FORCE_FULL=1` move any of these seven numbers?* A silent shift here is the `$105.24M`-shaped failure at plan altitude, and that entry is filed against this exact chain (`review_followups.md` line 77: *"live massing-mislink poisoning costs — `cur_floor_gfa_sqm` 14,171 m² on NULL-lot RT parcels → `parcel_cost_menu` gut line **$105.24M**"*).

> **Reality-Check instrument blind spot, measured:** `scripts/analysis/parcel-sanity-audit.js` has **zero** BOUND / INVARIANT / DISTRIBUTION rules over `parcel_buildings` itself. What it has is the **downstream** consequence — `footprint_gt_lot_x105`, `existing_floor_gt_lot_x105`, `heritage_basis_footprint_gt_lot`, `cost_fb_on_footprint_gt_lot` (all HIGH, all `gate: true`) — so a mislink is caught after it has become a cost, never at the link. `scripts/analysis/parcel-field-dump.js` shows the derived `existing_*` fields and **no** link-level field. **File the gap as a separate WF3 (not this pilot's scope), per the pilot-2 precedent:** add a link-level `nearest_share` DISTRIBUTION, a `primary_footprint_gt_lot` INVARIANT scoped to `parcel_buildings`, and a `shared_primary_building` INVARIANT.

**A-7 — DB Schema-Fidelity ask (FIRES).** Live `127.0.0.1:54322/postgres`, **242 migrations**, floor 223. Verify against the live catalog, not the migration files:

* **All 8 `parcel_buildings` columns are `NOT NULL` and 6 carry defaults** (`id` serial · `is_primary` `false` · `structure_type` `'other'` · `linked_at` `now()` · `match_type` `'polygon'` · `confidence` `0.85`). ⚠️ Three defaults (`'other'`, `'polygon'`, `0.85`) are values the step **never writes** — a generated INSERT that omits a column silently adopts them. `match_type='polygon'` is **not** in the live value domain (only `centroid_in_parcel` and `nearest` exist). Confirm the generated column list is exhaustive.
* **The ON CONFLICT target is `parcel_buildings_parcel_id_building_id_key`** — a composite unique index, not the PK (`parcel_buildings_pkey` is on `id`). The generator must target the composite (LG-2).
* **`idx_parcel_buildings_one_primary (parcel_id) WHERE is_primary`** — a **partial unique** index; 0 violations live. Any generated write that reorders W2/W3 (B-8) violates it. Declare it in `guards.requires`.
* **RLS:** `parcel_buildings` has `relrowsecurity = true` with **0 policies** — identical to pilot 2's `ravines` (`LR-D7`). Writes succeed only because the role has `rolbypassrls`. `write.js` already ships the `rls_bypass_or_policy` preflight; declare it, and confirm the probe covers this table.
* **Both GiST indexes exist** (`idx_parcels_geom_gist`, `idx_building_footprints_geom_gist`) → `guards.requires[].kind: index`; **B-4** makes the `parcels` one a HALT, not a warning.
* **`confidence` is `numeric` with no precision constraint** — T5/T6 as logic variables need bounds at the descriptor, since the column will not reject a 5.
* **`database.min_migration`:** the DDL dependencies are `039` (GiST) and `081` (the partial unique index) — **derive the floor from those, not from head 242**.

## PLAN-altitude adjudication asks

**A-1 — the LINK archetype's execution shape (blocks commit 7; this is pilot 3's A-1).** `isIngestStep` gates the library's whole acquire→validate→write pipeline on `inputs.reads.externals[].url`. A LINK has no external. Options, in P1 order:
 **(a)** generalise the branch to a declared `execution.shape` (`ingest` · `link` · `observe`) and add a `runLinkPhase` that reads `outputs.writes[]` in declared order (LG-1 + LG-3), with LG-2's composite key — the library owns W1/W2/W3, the compute shrinks to pure classifiers (`classifyStructure`, `haversineDistance`, `gridKey`, `reprojectGeometry`, `mercatorToWgs84`) plus the check observers;
 **(b)** widen `isIngestStep` to *"declares writes AND a pre-compute trigger"* and reuse `runIngestPhase` with a null acquisition — smaller, but it makes "acquisition" mean nothing;
 **(c)** let the compute hold the join and write through `ctx.pool` — **refused**: it violates P2 (*"no gating, no ledger, no lock, no emit"*) and §5.5 (1), and the compute would need `pipeline.withTransaction`, which `compute-forbidden-require` bans.
 **(a) is the P1/P2-faithful answer and is what `index.js`'s own LINK/MATCHER wave line anticipates. Operator ruling required before commit 7 — it decides the whole pilot's shape** (exactly as pilot 2's A-1 did).

**A-2 — where the spatial join predicate lives (P1.1 vs the open compute).** The predicate is domain SQL with three load-bearing properties (B-4 index precondition, B-9 fallback ordering, B-10 bbox-before-geography). Options: **(1)** a declared `outputs.writes[].source` / `inputs.match` block naming table, join columns and a named predicate template — maximal P1, but a SQL DSL is a large C3 contract change; **(2)** the compute exports a **pure SQL-builder** (`buildMatchSql(descriptor)` returning a string, no I/O) that the library executes — P2-clean (no socket in the compute), P1-partial (the predicate is code, not data), and it is what `write.js buildWritePlan` already does for the upsert; **(3)** the predicate stays in the library keyed by archetype — refuses to generalise. **Recommend (2)** and record the P1 shortfall in `limitations[]` with `check_id`. **Cost of (2): 0 new bytes.**

**A-3 — `guard_columns` and the `RUN_AT` column (the finding-2 ruling).** Should `write_discipline` gain a declared `clock_columns: ["linked_at"]` (excluded from the guard by construction, LG-9), or is an explicit `guard_columns` array plus a validator rule sufficient? **(1)** `clock_columns` — structural, unforgettable, ~40 B of descriptor, and reusable by every LINK; **(2)** explicit array + a rule that a run-clock-bound column may not appear in `guard_columns` without a `why` — no schema change; **(3)** explicit array only — one word away from a 520K-row-per-run regression forever. **Recommend (1) or (2); (3) is refused.**

**A-4 — the golden table-state strategy for a 520K junction (blocks commit 5).** Measured: ceiling 100,000 < 520,492; full-row hash **922–2,879 ms** and **includes `id` + `linked_at`**, both of which change on a FULL relink with identical logical content; a projection over the 6 logical columns hashes in **666 ms** and is stable; a 4-scalar invariants query costs **323 ms**. Options: **(a)** add `--table-columns=<t>:<c1,c2,…>` (or read a `golden` flag off `outputs.writes[].columns[]`) **and** raise the ceiling for this table with the measured 666 ms stated — the honest 4-tuple; **(b)** skip the hash and rely on `--invariants` scalars (323 ms) — cheaper, but a reordering or a swapped `building_id` would pass; **(c)** raise the ceiling only — **refused**, it produces a guaranteed false diff on every FULL capture. **Recommend (a)**, with the projection declared in the descriptor so a future column addition cannot silently leave the fingerprint.

**A-5 — where the config resolution sits relative to the lock (the B-13 ruling).** Today `validateLogicVars` runs **inside** `withAdvisoryLock`. `link_wsib`'s A1/A2 fence and `config.js`'s `hoisted_above_gate` exist because *"a SKIP-eligible step must never let an invalid threshold hide behind a green SKIPPED summary"* — and this step **is** SKIP-eligible (6 live `skipped` rows, plus the full-gate). Options: **(a)** declare `hoisted_above_gate: true` — matches the fence, changes WHERE an invalid threshold throws (a real, declarable diff); **(b)** keep it inside the lock and declare the deviation with a `why`. **Recommend (a)** and declare it as a diff, not discover it.

**A-6 / A-7** — see the Reality-Check and Schema-Fidelity sections above.

**A-8 — the JS fallback path's status.** The PostGIS path is the only one that runs (live: `buildings_indexed = 0`, `grid_cells = "N/A (PostGIS)"` on every recorded run since PostGIS landed). The JS path is ~270 of the 740 lines, has **no** `is_primary` pre-clear (B-8 asymmetry), uses a different batch size (T8) and a different nearest-distance implementation (haversine vs `ST_Distance::geography`). Options: **(a)** convert both paths faithfully and declare the divergences in `limitations[]`; **(b)** declare the JS path a `deviations[]` legacy branch with an `adjudicated_by` and pin it verbatim; **(c)** retire it — **out of scope** (Spec 123 §3: *"a DEFECT gets fixed during conversion"* is Known Failure Mode #3). **Recommend (a) or (b) — operator ruling, because it decides ~36% of the line accounting.**

## Standards Compliance

* **Try-Catch Boundary:** no API route touched — §11's route block N/A. The step's boundary becomes `scripts/lib/step/index.js`'s `try/catch/finally` in `runWithPool`. ⚠️ The step has **1 `try` / 1 `catch` / 0 `finally`**: the single catch is the `booleanPointInPolygon` guard that Spec 122 §1.6 names as one of *"four true silent swallows [that] have no legal value under any of the four fields and must be **fixed, not declared**"* (invalid geometry → the parcel is silently reclassified as no-match, **no counter**). It is on the JS path only. **Per Spec 123 §3.1 it is PINNED at conversion with an `LM-D` id and fixed after** — §1.6's "must be fixed" and §3.1's "pin, do not fix" collide, and the collision is recorded in Contradictions (C-6).
* **Unhappy Path Tests:** every declared check ships a must-fail fixture (55-A `#165`); every adjudicated fence proven in **both** directions (G4d). Unhappy paths in scope, all live in the code today: missing GiST index on `parcels.geom` (throw) · `logicVars` validation failure (throw) · PostGIS extension present but `building_footprints.geom` column absent (warn → JS fallback) · zero building footprints (JS path FAIL row) · a parcel with NULL `geometry` (JS path: counted `noMatch`) · invalid geometry in the PiP test (**the silent swallow**) · zero candidates in the grid · no polygon match and no building within `massing_nearest_max_distance_m` · lock contention (**6 live rows**) · an empty `parcel_buildings` after a FULL DELETE if `building_footprints` is empty (**unguarded today — D-20**) · the 1,395-parcel zero-match steady state.
* **logError Mandate:** **N/A for `scripts/`** — the corpus uses `pipeline.log.*`.
* **UI Layout:** **N/A for layout**, but **not N/A for files**: `GlobalConfigCard.tsx` gains up to **4** GROUP entries (T4–T7) alongside the 3 already at `:113-115`. A seeded var absent from GROUPS is invisible to operators — the hidden-knob failure P4 exists to close; `step-conformance.infra.test.ts`'s four-surface battery asserts the linkage.
* **Database Impact:** **NO migration.** `parcel_buildings` exists; its indexes exist (039, 081); RLS is already on (227). Every write is pre-existing.
* **Cross-Layer Contracts (§11, always applies):** **five** cross this pilot. (1) `parcel_buildings.{confidence, is_primary}` → `enrich-parcels.js`'s `prim`/`allb` CTEs → 10 `existing_*` / `imagery_roof_*` parcel columns → `permits` + `coa_applications`. (2) `parcel_buildings.linked_at` → `enrich-parcels.js buildMassingScopeWhere` → the entire incremental scope of `enrich_parcels` (**finding 2**). (3) `records_meta.{code_version, building_footprints_count}` → the **next run's own gate** (`evaluateMassingFullGate`) — a self-referential producer/consumer contract that must be byte-stable, and it is read via `status='completed'` (**B-6**). (4) `parcel_buildings` counts → `quality/assert-global-coverage.js` Step 11 (5 `coverageRow`s + 1 `infoRow`, denominator `pbTotal`) and `refresh-snapshot.js`'s `parcels_with_buildings`. (5) The `RUN_STATUS` vocabulary — `ledger.js` ↔ `/api/quality/route.ts`; per `tasks/lessons.md` **no new terminal status may be introduced**; `self_skipped` already exists.

## Panel plan

**PLAN altitude (BEFORE code — `scripts/CLAUDE.md` "Two altitudes", Spec 08 §6.4):**
1. **Integration** (`general-purpose`, main tree, **NO worktree**) — manifest/chain wiring incl. `chain_args.sources = ["--full"]` and `run-chain.js`'s `extraArgs`, `spawnStepChild` argv/env, the **zero-export** surface (this step exports nothing, unlike pilot 2's 13), and every one of the **9** test files' re-homing targets incl. `pipeline-logic-vars-coercion.infra.test.ts`'s `toHaveLength(18)`.
2. **Ground-truth** (grounder) — re-executes **every** executable claim in this plan, ROWS not counts (`feedback_data_facts_need_queries`).
3. **Descriptor-vs-schema** (not a Spec 08 seat) — the authored descriptor vs `step.schema.json` incl. the **LINK `allOf` profile** (`outputs.invalidates` `minItems: 1`, `counters` not `"none"`), the three `writeDiscipline.class` values across three targets, the composite `key` array, and every `terminals[]` entry.
4. **DB Schema-Fidelity** (Spec 08 §5.4, live DB) — **FIRES**, ask A-7.
5. **Reality-Check** (`pipeline-reality-check`, main tree) — **FIRES**, ask A-6. *(This is the first pilot where RC's own instrument is measurably blind to the table under conversion — say so in the brief.)*

**OUTPUT altitude (on the diff + re-run values) — pipeline WF2 = 5 seats + Guardian + Reality-Check:**
1. Bash: `npm run review:gemini -- review scripts/lib/compute/link-massing.js --context docs/specs/01-pipeline/56_source_massing.md`
2. Bash: `npm run review:deepseek -- review scripts/lib/step/write.js --context docs/specs/01-pipeline/122_pipeline_step_optimization.md` — **target the generated write SQL** (composite key, three ordered targets, `guard_columns`), the lens set: spec / idempotency / error-paths.
3. Agent `code-reviewer-grounded`, worktree — spec conformance / type safety / dead code / telemetry.
4. Agent `observability-reviewer`, worktree — audit-row completeness, **the row-derived verdict replacing `massingAuditRows.some(…) ? 'FAIL' : …`**, §11 counter scoping (`records_total` today = the unmatchable tail; `records_new` hardcoded 0), and the **`records_meta.{code_version, building_footprints_count}` self-consumed contract** vs `evaluateMassingFullGate`.
5. Agent `general-purpose`, **NO worktree** — Integration vs the real codebase.
6. Agent `regression-guardian`, **main tree** — anchored on the **30 `fix(` commits** (the `Severity:` footer grep returns 0 — brief it explicitly so it does not conclude "no fences"), the ~12 named fence candidates, the 5 source-text-reading test files, and `tasks/lessons.md`'s phase-reorder + status-vocabulary entries. **Priority: the 13 before/after guarantees B-1…B-13.**
7. **Reality-Check** — **FIRES**: re-run the seven plausibility numbers after the differential and compare against this plan's baseline.

Then **Fold Validation** (Spec 08 §11.2): Backend/Pipeline ⇒ **mandatory** — one grounder re-executes every claim in the fold + one Cross-read Adversary checks the folded decisions **pairwise** and walks every checklist line for staleness. Plus the **Idempotency Lens** (Spec 08 §10): this pilot touches a destructive write (W1 deletes 520,492 rows), a transaction boundary, and a re-runnable script whose second run must write 0.

## Contradictions / unverifiable

| # | Item | Evidence | Status |
|---|---|---|---|
| **C-1** | ⛔ **The task brief says this step "stamps a link on parcels" and "writes enriched parcel link fields."** | Measured: `emitMeta` declares writes to **`parcel_buildings` only**; `manifest.telemetry_tables` = `["parcel_buildings"]`; the step issues **no** UPDATE against `parcels`. The `parcels.massing_enriched_at` stamp is written by **`enrich-parcels.js`**, and migration 242's trigger NULLs it | **CORRECTED.** The step writes a junction table, not a parcel column. **Reality-Check still FIRES** — by subject matter (Spec 08: RC fires by SUBJECT, not table), because 10 `existing_*` / `imagery_roof_*` parcel columns are derived from this junction one step later |
| **C-2** | §5.5 (1): *"`compute(ctx)` iterates `ctx.checks` and does nothing else"*, hard-gated by `step-conformance.infra.test.ts` | a LINK is a read-join-write pipeline, and `isIngestStep` excludes it from the library path that pilot 2 built for exactly this | **CONFLICT — Ask A-1. Blocks commit 7.** Same shape as pilot 2's C-2, different resolution needed |
| **C-3** | `write.js buildWritePlan` throws on `keys.length > 1`; `step.schema.json` `definitions.write.key` accepts an array | `parcel_buildings`'s conflict target is `(parcel_id, building_id)` | **BLOCKER (LG-2).** The declaration is expressible and the generator refuses it |
| **C-4** | **Spec 122 §8.2 labels `link_massing` write class `F link_full_retraction`** — *"upsert + DELETE stale + DELETE zero-match"* | measured: **no** departure delete, **no** zero-match delete; instead a FULL-gated step-scoped mass DELETE + an unguarded scoped `is_primary` UPDATE + a guarded upsert | ⛔ **§8.2's label is REFUTED.** §1.4 already warns *"two of the 13 ported classes are wrong at the source and must be re-derived, not copied"* — this is a **third**. Declare per-target; do not port the label |
| **C-5** | **Spec 60 §3 "Link Massing" is stale on four counts** | it says *"Nearest-neighbour spatial match within bbox"* (the primary predicate is building-centroid-in-parcel), *"Process parcels in batches of 500"* (the live PostGIS path uses `pipeline.BATCH_SIZE = 1000`), *"find building footprints within spatial bbox"* (describes only the JS fallback), and says nothing about the `--full` gate, the ghost-link cleanup, structure classification or the confidence values | **DOC DRIFT.** Amend Spec 60 §3 to the as-built form in the Spec Update step; **do not "fix" the code to match the spec** |
| **C-6** | **Spec 122 §1.6 says the `link-massing.js` PiP catch "must be fixed, not declared"**; **Spec 123 §3.1 + Known Failure Mode #3 say a DEFECT must be PINNED at conversion and fixed after** | both are normative and both name this construct's class | **CONFLICT — needs a ruling.** Recommended resolution: **PIN** it at commit 7 with an `LM-D` id, and land the counter as a declared check in peel 8b — which satisfies §1.6's substance (it stops being silent) without smuggling a behaviour change into the no-op diff |
| **C-7** | Spec 123 §6.1 cites the shape rule as *"Spec 122 §4.1"*; the rule is **§5.1** | pilots 1 and 2 both recorded this | **Doc error, still unfixed.** Do not follow literally |
| **C-8** | `review_followups.md` line 13 (**MED/ACT**): `massing_zero_link_ghost`'s *"measured day-one value 0"* claim is unsupported; both cloud rows read **1395** | **confirmed locally this session: 1,395** parcels with zero links and `massing_enriched_at IS NULL`, and `buildMassingScopeWhere` scopes exactly 1,395 | ✅ **CONFIRMED — the followup is right.** The pilot's boundary freeze (commit 1) is the correct place to record it; the *fix* to `enrich-parcels.js`'s comment is a **separate WF3**, not this pilot |
| **C-9** | `review_followups.md` line 181 (Gemini, MED): `link_massing`'s coverage row *"claims a spec-defined coverage % but code emits two raw `infoRow` counts"* | `assert-global-coverage.js` Step 11 emits 1 `infoRow` + 5 `coverageRow`s over `parcel_buildings`; `link-massing.js` itself emits `link_rate` as a formatted string `'99.7%'` with threshold `'>= 50%'` | **OPEN followup, adjacent.** The conversion makes the threshold a declared `limit_from_config` — record whether that closes it or not |
| **C-10** | `review_followups.md` (**HIGH**, 2026-08-23): `parcels.centroid_lat/lng` is *"a JOIN KEY with NO invalidator"* and names `link-massing.js` as one of its two consumers | measured: the step's `baseFilter` is exactly `centroid_lat IS NOT NULL AND centroid_lng IS NOT NULL`, and Spec 122 §6.4 corrects the earlier claim to *"one join plus one **eligibility filter**"* — link_massing only filters | **OPEN, and it BOUNDS the descriptor.** `inputs.reads.tables[]` may not declare a `staleness.scope` keyed on a centroid column, because that column has no invalidator (claim #54 would make it unexpressible). State the bound; do not fix the upstream gap here |
| **C-11** | `load-massing.js` DELETEs from `parcel_buildings` (2 sites, `source_id` format-change cleanup) while its `emitMeta` declares only `building_footprints` | Spec 122 §1.4 names this exact pair as *"the undeclared-telemetry class (orphan #234)"* and rules *"a target this step does not own is a `cascade`, never a `write`"* | **CROSS-STEP.** `link_massing` OWNS `parcel_buildings`; `load_massing` (unconverted) has an undeclared cascade into it. Record in `limitations[]` with the check id; the fix rides `load_massing`'s own conversion |
| **C-12** | `pipeline.isFullMode()` reads `process.argv` one call frame above the compute; `compute-no-process-env` matches `process.env` only | the compute-shape rule set has **no** argv rule | **ENFORCEMENT GAP.** Either add `compute-no-process-argv` + an indirect-read rule, or accept that E2 is enforced only by review. State the choice |
| **U-1** | **UNVERIFIABLE until A-1/A-4 are ruled:** whether the FULL-path differential is green | it needs the library write path (A-1), the composite key (LG-2), the column-projected hash (A-4), a 21.9-min run and a declared cleanup of the `linked_at` bump | **NOT MEASURABLE YET.** The single largest hole in this plan |
| **U-2** | **UNVERIFIABLE:** G1's *"20% change coupling"* and G2's churn×complexity quadrant | `ls scripts/analysis` has no churn/complexity instrument | **NOT MEASURED — a real work item**, inherited unchanged from pilots 1–2 |
| **U-3** | 55-A per-step rows for `link_massing` | the generator is claim-scoped; there is no per-step row and there will not be one | **NOT PRODUCIBLE.** 44 is the template's A-block size, not a measured per-step count |
| **U-4** | Mutation ≥80% (G7, class A) | `stryker.config.mjs` `mutate:` = 3 files under `src/features/leads/lib`; `scripts/` unreachable | **UNSATISFIABLE AS WRITTEN** (Spec 123 §4.8 concedes it). Record, do not fake |
| **U-5** | Whether the JS fallback path has EVER run since PostGIS landed | every recorded run shows `buildings_indexed = 0` and `grid_cells = "N/A (PostGIS)"`, i.e. the PostGIS branch; `pipeline_runs` holds 59 rows but only the most recent carry the P11 meta | **PARTIALLY MEASURED.** Decide A-8 on this basis, and say what the evidence does and does not cover |

## Execution Plan

- [ ] **State Verification:** live DB reachable this session (`127.0.0.1:54322/postgres`, **242** migrations, floor 223) — re-print host+database before every capture per `tasks/lessons.md`. Baselines to re-confirm at commit 5: `parcel_buildings` **520,492 / 485,135 parcels / 417,702 buildings / 113 MB**; `parcels` **486,530** (all with centroid + geom); `building_footprints` **427,077**; link_rate **99.71%**; `nearest` share **103,530**; `footprint_exceeds_lot` **58,050**; the zero-link tail **1,395**; `logic_variables` **428 rows, 3 massing keys, 0 link_rate keys**.
- [ ] **Contract Definition:** N/A for API routes. The load-bearing contracts are (a) `records_meta.{code_version, building_footprints_count}` — **self-consumed by the next run's gate** — declared in `emits[]` with `consumers: ["link_massing"]` and diffed field-by-field at commit 7; (b) `parcel_buildings.{confidence, is_primary, linked_at}` → `enrich-parcels.js`. `npm run typecheck` after the test re-homing.
- [ ] **Spec Update:** amend **Spec 60 §3 "Link Massing"** to the as-built form (C-5); amend **Spec 56 §3** with the two-path divergence and the confidence values; amend **Spec 122 §8.2's write-class label** for this step per C-4; amend **§5.5 / §1.10** per the A-1 ruling. Correct Spec 123 §6.1's `§4.1`→`§5.1`. Run `npm run system-map`.
- [ ] **Schema Evolution:** **no DB migration.** `step.schema.json` grows IN commit 7 as a C3 pre-pull (per pilot 2's R4 precedent): whatever A-1 / A-3 rule in (`execution.shape`, `write_discipline.clock_columns`), plus composite-key support in `write.js` (LG-2) and ordered multi-target writes (LG-3). Fixtures under `fixtures/valid/` regenerated; conformance green **before** the descriptor is authored.
- [ ] **Guardrail Test:** `src/tests/steps/link_massing/violations.test.ts` — 44 A + the B partials (with `#181` N/A-reasoned) + one both-directions lock per adjudicated fence. **The `guard_columns` lock (finding 2 / D-5) is written first.**
- [ ] **Red Light:** `npx vitest run src/tests/steps/link_massing/` must FAIL before commit 7. Record the red set.
- [ ] **Implementation:** commit 7 = frozen-shape wrap + LG-1…LG-7 + the 4 new seeded variables + the GROUPS entries; commits 8a/8b/8c = the three peels; green differential after each, on **all three invocations**. **Every non-identical output is a row in the Declared-diffs table — nothing undeclared.**
- [ ] **UI Regression Check:** `GlobalConfigCard.tsx` is a shared admin component — `npx vitest run src/tests/*.ui.test.tsx src/tests/control-panel.logic.test.ts` after the GROUPS edit.
- [ ] **Pre-Review Self-Checklist:** 5–10 items from Spec 122 §1.2a / §1.4 / §5.1 / §5.5 walked against the ACTUAL diff, PASS/FAIL each, BEFORE running tests. **Item 1 is always: does the descriptor name `guard_columns` explicitly, and does the generated SQL exclude `linked_at`?**
- [ ] **Multi-Agent Review:** the 5-seat panel + Regression Guardian + **Reality-Check** in ONE message (roster above), at **both** altitudes.
- [ ] **Fold Validation:** mandatory (Backend/Pipeline) — grounder re-executes every claim in the fold + Cross-read Adversary pairwise + **Idempotency Lens** (destructive W1, transaction boundary, re-runnable script).
- [ ] **Green Light:** `npm run test && npm run lint -- --fix` + `node scripts/hooks/check-step-shape.mjs` (must report **3** enforced step files and **3** compute modules, exit 0) + all three differentials green. Baseline to beat, measured immediately before commit 7: `npx vitest run src/tests/link-massing.infra.test.ts src/tests/massing.logic.test.ts src/tests/massing-full-gate.logic.test.ts src/tests/chain.logic.test.ts src/tests/pipeline-sdk.logic.test.ts src/tests/pipeline-logic-vars-coercion.infra.test.ts src/tests/pipeline-advisory-lock.infra.test.ts src/tests/step-conformance.infra.test.ts`. → WF6.

**PLAN COMPLIANCE GATE (§11):** Database Impact **NO** (no migration; `parcel_buildings` + indexes 039/081 + RLS 227 all pre-exist) · Pipeline Script Modified **YES** — uses the SDK via `pipeline.step()`; **streaming IS in scope** (Spec 47 §R7 >10K rows: the JS path streams `building_footprints` via `pipeline.streamQuery`, and two `pipeline-sdk.logic.test.ts` assertions lock it — the PostGIS path is keyset-paginated at `pipeline.BATCH_SIZE` 1000) · Shared Logic Touched **YES** (`scripts/lib/step/*` — consumed by `assert_schema` AND `load_ravines` today, so **every pilot-1 and pilot-2 test is a dual-path consumer**; `npx vitest related` planned) · Cross-Layer Contracts: **five** identified above, no new status value · UI/Frontend: one shared admin component (`GlobalConfigCard.tsx`) — UI regression check scheduled · Pre-Review Self-Checklist scheduled · **Reality-Check and DB Schema-Fidelity both FIRE at plan altitude** (A-6/A-7).

> **PLAN LOCKED. Do you authorize this WF2 plan? (y/n)**
> §11 note: eight adjudication asks (A-1…A-8) are open; **A-1 (the LINK execution shape), A-3 (`guard_columns` / `linked_at`) and A-4 (the golden table-state strategy) block commits 5 and 7** and need rulings before implementation begins.
