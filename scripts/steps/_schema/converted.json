{
  "$comment": [
    "THE ENFORCEMENT SCOPE FOR THE A2 SHAPE RULE (Spec 122 §5.1).",
    "",
    "Zero steps are converted today, so the shape rule CANNOT be enforced over",
    "manifest.chains[*].file — every one of those 27 files would fail. This list is",
    "the blocking scope: a file appears here only once its conversion has landed, and",
    "each C-track pilot appends exactly one entry as its final commit.",
    "",
    "The unconverted remainder is still SCANNED, in report-only mode, by",
    "scripts/hooks/check-step-shape.sh --report-only. That report is the prove-red:",
    "src/tests/step-conformance.infra.test.ts asserts the rule fires on every",
    "manifest sources file NOT in this list, so an empty list is never a vacuous pass.",
    "",
    "Paths are repo-root-relative and MUST each be a manifest.chains[*] step file.",
    "The conformance suite enforces converted ⊆ manifest files, no duplicates, and",
    "loaded.length === converted.length (the silent-import-death guard, Spec 122 §5.2).",
    "",
    "PENDING ENTRIES (R-K.1, 2026-08-29): each entry is {file, registers_at, reason,",
    "declared, stage}. stage is closed-vocabulary: \"red_suite\" | \"shape_clean\".",
    "red_suite = the per-step violations.test.ts (with it.fails() call sites) has",
    "landed but the sibling <slug>.descriptor.json does NOT exist yet — the file MAY",
    "still be shape-dirty (still calls pipeline.run()). shape_clean = the descriptor",
    "exists and scripts/lib/step/validate.js's conformanceFindings() returns [] for",
    "it, but converted.json registration is deferred to the cutover commit. A pending",
    "entry whose descriptor exists while stage is still \"red_suite\" is RED (\"stage",
    "not advanced\") — src/tests/step-conformance.infra.test.ts enforces both halves.",
    "step-validate.mjs's fast invariant #5 (it.fails( call sites must sit under a",
    "declared pending slug) keys on EITHER stage."
  ],
  "contract_version": 1,
  "converted": [
    "scripts/quality/assert-schema.js",
    "scripts/load-ravines.js",
    "scripts/link-massing.js",
    "scripts/link-wsib.js",
    "scripts/link-parcel-addresses.js",
    "scripts/compute-centroids.js",
    "scripts/link-parcels.js",
    "scripts/refresh-snapshot.js"
  ],
  "pending": []
}
