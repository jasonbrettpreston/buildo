{
  "$comment": [
    "THE ENFORCEMENT SCOPE FOR THE A2 SHAPE RULE (Spec 122 §5.1).",
    "",
    "Zero steps are converted today, so the shape rule CANNOT be enforced over",
    "manifest.chains[*].file — every one of those 27 files would fail. This list is",
    "the blocking scope: a file appears here only once its conversion has landed, and",
    "each C-track pilot appends exactly one entry as its final commit.",
    "",
    "The unconverted remainder is still SCANNED, in report-only mode, by",
    "scripts/hooks/check-step-shape.sh --report-only. That report is the prove-red:",
    "src/tests/step-conformance.infra.test.ts asserts the rule fires on every",
    "manifest sources file NOT in this list, so an empty list is never a vacuous pass.",
    "",
    "Paths are repo-root-relative and MUST each be a manifest.chains[*] step file.",
    "The conformance suite enforces converted ⊆ manifest files, no duplicates, and",
    "loaded.length === converted.length (the silent-import-death guard, Spec 122 §5.2).",
    "",
    "PENDING ENTRIES (R-K.1, 2026-08-29; five-value vocabulary widened at pilot 9's",
    "two 2026-09-04 commits — descriptor-lands-first landed, then step-validate.mjs",
    "hard-stop-gating landed as its own commit, Spec 124 R-K amendment): each entry",
    "is {file, registers_at, reason, declared, stage}. stage is closed-vocabulary:",
    "\"red_suite\" | \"descriptor_only\" | \"compute_ported\" | \"runner_wired\" |",
    "\"shape_clean\". red_suite = the per-step violations.test.ts (with it.fails()",
    "call sites) has landed but the sibling <slug>.descriptor.json does NOT exist",
    "yet — the file MAY still be shape-dirty (still calls pipeline.run()).",
    "descriptor_only = the descriptor exists and independently validates against",
    "step.schema.json (scripts/lib/step/validate.js validateDescriptor), but the",
    "step's own compute/runner have not yet landed — conformanceFindings() is NOT",
    "expected to return [] at this stage (module.exports.compute is still absent",
    "on the shell). compute_ported/runner_wired are the two later partial stages",
    "(compute module ported; shell frozen onto pipeline.step()) — declared in the",
    "vocabulary for the commit that first needs them, not yet exercised by any",
    "live entry. shape_clean = the descriptor exists AND",
    "scripts/lib/step/validate.js's conformanceFindings() returns [] for it (compute",
    "wired, shell frozen), but converted.json registration is deferred to the",
    "cutover commit. The middle values exist because pilot 9 (ENRICHER, the first",
    "archetype to split its own commit 7 into 7a schema / 7b descriptor / 7c compute",
    "/ 7d runner / 7e golden-gate sub-commits, per the operator's own dispatch) is",
    "the FIRST pending file whose descriptor genuinely lands before its compute —",
    "every earlier pilot landed descriptor+compute+runner in one commit, so",
    "red_suite -> shape_clean was always a single atomic jump and the middle states",
    "were never observable. A pending entry whose descriptor exists while stage",
    "stays \"red_suite\" is RED (\"stage not advanced\") — src/tests/",
    "step-conformance.infra.test.ts enforces this (R-K.1's own concern: does the",
    "declared stage match the artifact state on disk). SEPARATELY, `stage` also",
    "gates scripts/analysis/step-validate.mjs's own G7/G8/G9 + Rules 4/11/12",
    "hard-stop set (Spec 124 R-K amendment) — descriptor_only excludes",
    "G7/G8/G9+Rules 4/11/12, compute_ported excludes G8/G9, runner_wired excludes",
    "G9 only; the excluded row's score/status is UNCHANGED, only hardStop and its",
    "own detail/note text (\"— stage-gated (<stage>)\") reflect the exclusion — see",
    "step-validate.mjs's stageExclusions/aggregateHardStop and their selfTest()",
    "locks. step-validate.mjs's fast invariant #5 (it.fails( call sites must sit",
    "under a declared pending slug) keys on ANY of the five stages."
  ],
  "contract_version": 1,
  "converted": [
    "scripts/quality/assert-schema.js",
    "scripts/load-ravines.js",
    "scripts/link-massing.js",
    "scripts/link-wsib.js",
    "scripts/link-parcel-addresses.js",
    "scripts/compute-centroids.js",
    "scripts/link-parcels.js",
    "scripts/refresh-snapshot.js"
  ],
  "pending": [
    {
      "file": "scripts/enrich-parcels.js",
      "registers_at": "C1 pilot 9 commit 9 (cutover)",
      "reason": "PH-7 red suite landed commit 6 (src/tests/steps/enrich_parcels/violations.test.ts, it.fails() call sites); commit 7a landed the schema bump (execution.shape \"enrich\" + execution.phases[]); commit 7b landed the descriptor (scripts/enrich-parcels.descriptor.json, validates standalone) and advanced stage red_suite -> descriptor_only; commit 7c landed scripts/lib/compute/enrich-parcels.js (the 5 pass functions + computeDeferScope + SQL builders, seam-rewritten per Spec 122 §5.5). Pilot 9 commit 1 (Rule 11 amendment) resolved the 7b architectural tension — checkOrderGuaranteesCited (scripts/analysis/step-validate.mjs) now accepts a spec-qualified anchor \"<specnum>:<anchor text>\" so pass 5's order_guarantee can genuinely cite Spec 78 §P3A.1 while identity.spec stays \"65\"; the descriptor's pass5_post_commit_read_order check's anchor is now \"78:read would be invisible\" and Rule 11 reads enforced-green in the scorecard. Pilot 9 commit 2 (7d, runEnrichPhase) lands the runner: shared-txn passes 1-4 with per-phase SET LOCAL statement_timeout/lock_timeout, the post_commit pass on a dedicated connection inside its OWN transaction (Fold B2), the D4' scope hand-off INSERT, whole-step heartbeat/stall diagnostics for all 5 phases, and R-B's staleness.detectInterruptedRetraction folded UNCONDITIONALLY into `full` (Rule 12 reachability — step-validate.mjs's runnerReachability gains a third recognized shape for ENRICHER; SHAPE_RUNNER_FN gains enrich:\"runEnrichPhase\", now enforced-green). Pass 5 also reverted to ctx.stream (cursor-streamed, config.enrich_parcels_pass5_stream_batch_size) from the 7c intermediate buffered client.query select. generate-template-freeze.mjs's RUNNER_NAMES gains runEnrichPhase (RE-FREEZE #3, Spec 122 §8) — a side effect corrected runRecorderPhase's own previously-inflated frozen phase_order (it had inherited executeOrderedWrites' calls as the LAST run\\w+ match in the file; runEnrichPhase now bounds it correctly, and inherits the identical boundary-heuristic artifact in turn, documented not hidden). STAGE DELIBERATELY HELD at descriptor_only through BOTH commits, not advanced to compute_ported/runner_wired: measured live (`step:validate --staged --fast`), Rule 11 (commit 1) and Rule 12 (commit 2) both going green is NOT enough — Rule 4 (compute preserved-in-compute grounding, a pre-existing 7c finding, unrelated to either commit) and G7/G8 (golden-capture RED-evidence, explicitly commit 7e's own deliverable) are ALSO still enforced-red, and advancing the stage would make the pre-commit hook hard-stop on THOSE, not on anything commits 1/2 could honestly claim to have resolved. Advancing a declared stage past what the hook can actually pass would be the exact false-readiness claim R-K.1/STA-2's own precedent (`tasks/lessons.md`) warns against. Stage advances once Rule 4 and G7/G8 are ALSO addressed (a separate, later WF) and the golden differential (7e) lands, before commit 9 cutover Commit 7e/2 (2026-09-08) lands the thin shell (scripts/enrich-parcels.js, 2391 lines -> 41), the WF3 pass-5 stream/write deadlock fix (dedicated stream client) + the double-run advisory-lock fix (two-key inner lock on the shared-txn/post_commit connections), the POST golden capture (docs/reports/golden/enrich_parcels/post/{sources_run1,none_incremental}.json, exit 0), and the G2' same-day comparator (docs/reports/golden/enrich_parcels/g2-comparator-report.md: 99/100 columns byte-identical, the 1 diff is EP-D9 tie-break, 0 unexplained, timing ratio 1.00x -> EP-D11 REFUTED). Stage advances to runner_wired: the runner (7d) and the thin-shell wiring (7e/2) are both now live and golden-verified; Rule 4 (compute preserved-in-compute grounding) is the one remaining precondition before shape_clean, addressed in commit 3.",
      "declared": "2026-09-04",
      "stage": "runner_wired"
    }
  ]
}
