{
  "identity": {
    "name": "assert_schema",
    "display_name": "Schema Validation",
    "owner": "data",
    "description": "CQA Tier 1 pre-ingestion schema validation: CKAN field sets for permits/CoA/zoning, CSV headers for address points and parcels, GeoJSON property keys for neighbourhoods, and URL reachability for the shapefile archives.",
    "lock": 102,
    "why_lock": {
      "text": "Shared across specs 41/42/43, so no single spec number identifies it; 102 was assigned from the Spec 47 §A.5 next-free registry.",
      "liveness": { "kind": "file", "ref": "src/tests/pipeline-advisory-lock.infra.test.ts" }
    },
    "spec": "43",
    "spec_version": "1.0",
    "archetype": "ASSERT",
    "contract_version": 1,
    "gate_exempt": true
  },

  "inputs": {
    "reads": {
      "steps": [],
      "tables": [],
      "externals": [
        { "id": "ckan_datastore_api", "kind": "http_api", "url": "https://ckan0.cf.opendata.inter.prod-toronto.ca/api/3/action/datastore_search", "cache": "none" },
        { "id": "address_points_csv", "kind": "http_file", "cache": "none" },
        { "id": "parcels_csv", "kind": "http_file", "cache": "none" },
        { "id": "massing_zip", "kind": "http_file", "cache": "none" },
        { "id": "ravine_zip", "kind": "http_file", "cache": "none" },
        { "id": "heritage_register_zip", "kind": "http_file", "cache": "none" },
        { "id": "heritage_hcd_zip", "kind": "http_file", "cache": "none" },
        { "id": "centreline_zip", "kind": "http_file", "cache": "none" },
        { "id": "neighbourhoods_geojson", "kind": "http_file", "cache": "none" }
      ]
    },
    "expect_nonempty": false,
    "on_missing": "halt"
  },

  "outputs": "none",

  "staleness": {
    "scope": "none",
    "trigger": [{ "signal": "always", "position": "pre_compute" }],
    "mode_select": "none",
    "checkpoint": "none",
    "interval": "none",
    "fingerprint": "derived",
    "fingerprint_inputs": ["scripts/lib/address-points-csv-drift.js"],
    "logic_version": "none",
    "on_fingerprint_change": "queue"
  },

  "guards": {
    "requires": [],
    "srid": "none",
    "empty_source": "none",
    "schema_drift": "pause"
  },

  "execution": {
    "budget": "10m",
    "txn_scope": "none",
    "txn_budget": "none",
    "chunked": false,
    "statement_timeout": "none",
    "step_timeout": "15m",
    "batch": "none",
    "needs_disk_mb": "none",
    "partial_fill": "none",
    "on_row_error": "fail_fast",
    "on_batch_error": "fail_step",
    "on_check_error": "fail_step",
    "on_check_error_why": {
      "text": "Fold A correction 4. Eight of the nine checks already fail the step when their own fetch throws: the per-source catch sets allPassed=false and the circuit breaker halts the chain. fail_step is therefore the POLICY. The single omit — a non-OK sample fetch inside validateTypeSample returning true — is a per-check limitation, recorded under limitations[] against permit_cost_type_sample, not a step-wide error policy.",
      "liveness": { "kind": "check", "ref": "permit_cost_type_sample" }
    },
    "on_degrade": "none",
    "criticality": "required",
    "network": {
      "egress": ["ckan0.cf.opendata.inter.prod-toronto.ca"],
      "timeout": "none",
      "retries": 0,
      "redact": "none"
    },
    "invocation": {
      "permits": { "argv": [], "env": { "PIPELINE_CHAIN": "permits" } },
      "coa": { "argv": [], "env": { "PIPELINE_CHAIN": "coa" } },
      "sources": { "argv": [], "env": { "PIPELINE_CHAIN": "sources" } }
    },
    "maintenance": "none"
  },

  "checks": [
    {
      "id": "permit_columns",
      "kind": "schema",
      "expect": ["PERMIT_NUM", "REVISION_NUM", "PERMIT_TYPE", "STATUS", "DESCRIPTION", "EST_CONST_COST", "STREET_NUM", "STREET_NAME", "BUILDER_NAME", "ISSUED_DATE", "APPLICATION_DATE"],
      "limit": "viol == 0",
      "severity": "FAIL",
      "blocking": true,
      "when": "pre",
      "chains": ["permits"],
      "accept_until": "none",
      "why": {
        "text": "Allowing downstream scripts to run against a drifted permit schema would silently corrupt 240K+ permit records.",
        "liveness": { "kind": "external", "ref": "ckan:6d0229af-bc54-46de-9c2b-26759b01dd05" }
      }
    },
    {
      "id": "permit_cost_type_sample",
      "kind": "schema",
      "expect": { "column": "EST_CONST_COST", "parseable_as": "number", "sample": { "logic_variable": "assert_schema_type_sample_rows" }, "ignore": ["DO NOT UPDATE", "DO NOT DELETE"] },
      "limit": "viol == 0",
      "severity": "FAIL",
      "blocking": true,
      "when": "pre",
      "chains": ["permits"],
      "accept_until": "none",
      "why": {
        "text": "CKAN injects sentinel rows and comma-formatted numbers; a type flip to unparseable text would zero out every cost estimate downstream. blocking:true states what the code does today — a failed sample sets allPassed=false and reaches the circuit breaker — and the schema's allOf then forces when:\"pre\".",
        "liveness": { "kind": "file", "ref": "scripts/load-permits.js" }
      }
    },
    {
      "id": "coa_columns",
      "kind": "schema",
      "expect": ["REFERENCE_FILE#", "IN_DATE", "STATUSDESC", "STREET_NUM", "STREET_NAME", "STREET_TYPE", "C_OF_A_DESCISION", "HEARING_DATE", "WARD", "DESCRIPTION", "SUB_TYPE"],
      "limit": "viol == 0",
      "severity": "FAIL",
      "blocking": true,
      "when": "pre",
      "chains": ["coa"],
      "accept_until": "none",
      "why": {
        "text": "WARD is text in the CoA Active resource and WARD_NUMBER is int4 in Closed; only the Active set is validated here because only Active is fetched pre-ingestion.",
        "liveness": { "kind": "file", "ref": "scripts/load-coa.js" }
      }
    },
    {
      "id": "address_point_columns",
      "kind": "schema",
      "expect": ["ADDRESS_POINT_ID", "ADDRESS_NUMBER", "LINEAR_NAME_FULL", "ADDRESS_FULL", "LO_NUM", "HI_NUM", "MAINT_STAGE", "ADDRESS_STATUS", "ADDRESS_CLASS_DESC", "CLASS_FAMILY_DESC", "PLACE_NAME"],
      "limit": "viol == 0",
      "severity": "FAIL",
      "blocking": true,
      "when": "pre",
      "chains": ["sources"],
      "accept_until": "none",
      "why": {
        "text": "Address Points became the canonical source for ADDRESS_NUMBER and LINEAR_NAME_FULL when Toronto stripped them from the Property Boundaries CSV on 2026-05-20.",
        "liveness": { "kind": "table", "ref": "address_points" }
      }
    },
    {
      "id": "address_point_coordinate_source",
      "kind": "invariant",
      "expect": { "one_of": ["geometry", ["LATITUDE", "LONGITUDE"]] },
      "limit": "viol == 0",
      "severity": "FAIL",
      "blocking": true,
      "when": "pre",
      "chains": ["sources"],
      "accept_until": "none",
      "why": {
        "text": "An OR-contract, not a flat column requirement: the live CSV ships a geometry GeoJSON column and LAT/LONG are the fallback. The real loss mode is neither being present, which yields a 0-row spatial bridge. Fence 646ea5a7 — the OR is evaluated in the compute by hasCoordinateSource(); geometry/LATITUDE/LONGITUDE must NEVER re-enter address_point_columns.expect as flat required columns.",
        "liveness": { "kind": "file", "ref": "scripts/lib/address-points-csv-drift.js" }
      }
    },
    {
      "id": "parcel_columns",
      "kind": "schema",
      "expect": ["PARCELID", "FEATURE_TYPE", "STATEDAREA", "geometry"],
      "limit": "viol == 0",
      "severity": "FAIL",
      "blocking": true,
      "when": "pre",
      "chains": ["permits", "coa", "sources"],
      "accept_until": "none",
      "why": {
        "text": "Parcels feeds all three chains (link_parcels in permits, link_coa_to_parcels in coa, and the sources loaders), so its drift rows are emitted into every chain's audit table per Spec 79 CRIT-3a. Peel 8a retired the constant-zero fiction: the compute no longer reads the chain, so wherever this check is SELECTED the parcels CSV header is actually fetched and the row reports a measurement. The cost is one extra ranged GET (bytes=0-2048) on the permits and coa chains; the gain is that a permits-chain parcels drift is no longer invisible.",
        "liveness": { "kind": "table", "ref": "parcels" }
      }
    },
    {
      "id": "source_archives_reachable",
      "kind": "schema",
      "expect": { "http_head_ok": ["massing_zip", "ravine_zip", "heritage_register_zip", "heritage_hcd_zip", "centreline_zip"] },
      "limit": "viol == 0",
      "severity": "FAIL",
      "blocking": true,
      "when": "pre",
      "chains": ["sources"],
      "accept_until": "none",
      "why": {
        "text": "These resources have datastore_active=false, so no field-set check is possible pre-download; attribute presence is validated post-download inside each loader. Reachability is the only pre-flight signal available. Fences 1ceebd17 (ravine_zip) and f6047e89 (centreline_zip) are carried by this subject set together with inputs.reads.externals.",
        "liveness": { "kind": "spec", "ref": "docs/specs/01-pipeline/62_chain_centreline.md" }
      }
    },
    {
      "id": "neighbourhood_id_property",
      "kind": "schema",
      "expect": { "one_of": ["AREA_SHORT_CODE", "AREA_ID"] },
      "limit": "viol == 0",
      "severity": "FAIL",
      "blocking": true,
      "when": "pre",
      "chains": ["sources"],
      "accept_until": "none",
      "why": {
        "text": "The neighbourhoods GeoJSON upsert key is the ID property; losing both spellings would key every neighbourhood to null.",
        "liveness": { "kind": "table", "ref": "neighbourhoods" }
      }
    },
    {
      "id": "zoning_resource_columns",
      "kind": "schema",
      "expect": {
        "resources": {
          "76a2620f-a6b4-495d-8e41-c0ede1f8a928": ["_id", "geometry", "ZN_ZONE", "ZN_STRING", "COVERAGE", "FSI_TOTAL"],
          "f0a88d06-2430-4025-b15d-362cabd00f31": ["_id", "geometry", "HT_LABEL"],
          "58ad8814-ca4e-43d6-848d-d5fd8d873574": ["_id", "geometry", "PRCNT_CVER"],
          "8d75cab6-ab97-4158-8ba5-8874860b26f7": ["_id", "geometry"],
          "1a6469f8-1eaf-4ba6-a1f6-07179efbc2f2": ["_id", "geometry"],
          "4e2f9292-6082-4627-be8e-61b87a2cb273": ["_id", "geometry"],
          "75b9805b-bc65-4c30-97fa-9c57c17233b2": ["_id", "geometry"],
          "8f969df7-9008-49fd-a50b-df53f1f680e6": ["_id", "geometry"],
          "499de5f6-194a-4da3-a18f-27a8e684721d": ["_id", "geometry"],
          "1f18bd73-bbbc-4ad6-ac27-6c9cae7385b4": ["_id", "geometry"]
        }
      },
      "limit": "viol == 0",
      "severity": "FAIL",
      "blocking": true,
      "when": "pre",
      "chains": ["sources"],
      "accept_until": "none",
      "why": {
        "text": "Pre-flight reachability plus upsert-key and geometry presence over 10 DataStore resources; full attribute drift is enforced at load time by zoning-attr-drift.js. Fold A correction 2 and fence 58914fa8: the code has THREE distinct required sets, not two — base (6 columns), the height overlay's HT_LABEL and the lot-coverage overlay's PRCNT_CVER — so expect is a per-resource map keyed by CKAN resource id.",
        "liveness": { "kind": "file", "ref": "scripts/lib/zoning-attr-drift.js" }
      }
    }
  ],

  "override": "none",

  "emits": [
    { "key": "checks_passed", "type": "string", "consumers": [] },
    { "key": "checks_failed", "type": "int", "consumers": ["src/components/FreshnessTimeline.tsx"] },
    { "key": "errors", "type": "array", "consumers": [] },
    { "key": "config", "type": "object", "consumers": [] }
  ],

  "deviations": [
    {
      "from": "Spec 122 §8.2's pilot table, which assigns assert_schema write class L (verdict_only)",
      "why": {
        "text": "The standalone pipeline_runs INSERT and UPDATE are the RUNNER's ledger row, not a step output; claim #39 consolidates ledger ownership into the library, so post-conversion this step writes nothing and outputs is \"none\" as the ASSERT profile requires.",
        "liveness": { "kind": "file", "ref": "scripts/run-chain.js" }
      },
      "adjudicated_by": "Spec 122 §1.10 required-field profile",
      "date": "2026-08-24"
    },
    {
      "from": "the live emission records_total: 0 at assert-schema.js:571",
      "why": {
        "text": "§1.10 forces an ASSERT to declare counters \"none\". Spec 122 §12.5 records that 0 of 5 ASSERTs comply today, so this profile is NORMATIVE, not descriptive; the differential at conversion must show the counter emission moving to the runner rather than changing value silently.",
        "liveness": { "kind": "spec", "ref": "docs/specs/01-pipeline/122_pipeline_step_optimization.md" }
      },
      "adjudicated_by": "operator ruling R2 / §1.10",
      "date": "2026-08-24"
    },
    {
      "from": "the draft descriptor's database.min_migration: 241",
      "why": {
        "text": "241 had no basis — it is fix_lead_id_trigger_column_scope, which this step never touches. The only table this step reads or writes is pipeline_runs (created by 033); the library writes records_meta (finalizeLedgerRow), a column added by migration 041, and pipeline.run applies no floor of its own, so 41 is the real floor (OUTPUT-panel grounder, 2026-08-25; earlier 33 under-counted). The database object admits no why field (additionalProperties:false), so the justification is recorded here",
        "liveness": { "kind": "table", "ref": "pipeline_runs" }
      },
      "adjudicated_by": "Fold A correction 5 / Fold B",
      "date": "2026-08-25"
    },
    {
      "from": "the draft descriptor's two distinct failed terminals, fail_check (schema_drift_detected) and fail_error (fetch_error)",
      "why": {
        "text": "Both are one throw in the code — the circuit breaker — so nothing observable distinguishes them and a two-terminal declaration would be a fiction. Fold B merged them at commit 7; the fetch-path liveness handle is carried onto the survivor rather than lost. RE-EXAMINED AT PEEL 8b AND LEFT MERGED: check-id attribution now distinguishes the two CAUSES inside the audit table (an errored check renders as `check errored: <msg>` while a violated one renders its violation count), but `terminals[]` has no runtime consumer anywhere in the library — `grep -rn terminals scripts/lib/` returns 0 — so the runner selects no terminal and a second declared terminal would still be a fiction. Splitting them needs the terminal-selection wave, not another compute peel.",
        "liveness": { "kind": "external", "ref": "ckan0.cf.opendata.inter.prod-toronto.ca" }
      },
      "adjudicated_by": "Fold B ruling 7",
      "date": "2026-08-25"
    },
    {
      "from": "the §1.2a P4 standard posture on_invalid: \"fail\" for a declared logic variable",
      "why": {
        "text": "These three vars are seeded by the mig-099 seed contract (scripts/seeds/logic_variables.json applied at the END of npm run migrate), NOT by a migration, so every DB that has not re-run the seed lacks the rows. Under \"fail\" the config resolver would REFUSE this step on those DBs — a pre-ingestion gate that halts three chains because a knob was externalized. Under \"default\" the resolver falls back to the seed default, which the parity lock pins byte-equal to the pre-externalization literal (20 / 2048 / 8192), so behaviour is IDENTICAL pre-seed and the only cost is that an out-of-bounds operator edit degrades loudly (a pipeline.log.warn naming the raw value, the bound and the substituted default) instead of halting. \"fail\" is the standard for a variable with no safe default; each of these has one, and it is the value the step ran on for its entire history. The config object admits no per-variable why field (additionalProperties:false), so the justification is recorded here.",
        "liveness": { "kind": "file", "ref": "src/tests/assert-schema-config-parity.logic.test.ts" }
      },
      "adjudicated_by": "Spec 122 §1.2a P4 / Pilot 1 P4 remediation plan",
      "date": "2026-08-25"
    }
  ],

  "limitations": [
    {
      "what": "A non-OK HTTP response inside validateTypeSample returns true, so the permit cost-type check cannot distinguish 'verified' from 'never ran'.",
      "measured": "scripts/lib/compute/assert-schema.js — `if (!res.ok) return true; // non-fatal` inside validateTypeSample (ported verbatim from assert-schema.js:163)",
      "check_id": "permit_cost_type_sample"
    },
    {
      "what": "Shapefile and ZIP sources are checked for reachability only; their attribute contracts are enforced post-download inside each loader, so a reachable-but-drifted archive passes this step.",
      "measured": "assert-schema.js:365-404 — five checkUrlAccessible calls, no attribute inspection",
      "check_id": "source_archives_reachable"
    },
    {
      "what": "checks_passed and errors have NO reader anywhere in the estate, and checks_failed's one reader is effectively dead: FreshnessTimeline.tsx renders it only on the branch where records_meta carries no audit_table, which the step library always populates. They are emitted for continuity with the pre-conversion payload, not because anything consumes them — the earlier consumer list naming run-chain.js and /api/quality was false.",
      "measured": "grep -rn 'checks_passed|checks_failed' over src/ and scripts/run-chain.js, 2026-08-25 — zero matches in run-chain.js and src/app/api/quality/; one match at src/components/FreshnessTimeline.tsx:1092",
      "check_id": "none"
    },
    {
      "what": "The coordinate-source OR-contract (geometry OR LATITUDE+LONGITUDE) cannot be expressed by any limit form the schema offers, so its rule lives as prose here and as logic in the compute's hasCoordinateSource() call; the descriptor states the contract, the check id carries it, and the fence lock proves both directions.",
      "measured": "scripts/lib/compute/assert-schema.js — one hasCoordinateSource(new Set(apHeaders)) call site; assert-schema.js:339 pre-conversion",
      "check_id": "address_point_coordinate_source"
    }
  ],

  "interpretation": { "file": "assert-schema.notes.json", "entries": 9 },

  "recovery": "none",

  "database": {
    "class": "primary",
    "min_migration": 41,
    "assert_current_database": "postgres"
  },

  "counters": "none",

  "config": {
    "logic_variables": [
      { "name": "assert_schema_type_sample_rows", "min": 1, "max": 1000, "on_invalid": "default" },
      { "name": "assert_schema_csv_header_bytes", "min": 256, "max": 65536, "on_invalid": "default" },
      { "name": "assert_schema_geojson_probe_bytes", "min": 1024, "max": 1048576, "on_invalid": "default" }
    ],
    "validation": "strict",
    "hoisted_above_gate": true
  },

  "sharing": {
    "chains": "derived",
    "shared": "derived",
    "slug_forms": "derived",
    "varies_by_chain": {
      "checks": "per_chain",
      "phase": { "permits": 1, "coa": 1, "sources": 1 },
      "audit_table": "per_chain",
      "scope": "per_chain"
    },
    "on_contention": "self_skip"
  },

  "terminals": [
    {
      "id": "lock_held_elsewhere",
      "kind": "skip_lock_contention",
      "status": "self_skipped",
      "records_meta": { "skipped": "bool", "reason": "string" },
      "why": {
        "text": "pipeline.js emits PIPELINE_SUMMARY with records_meta {skipped:true, reason:'advisory_lock_held_elsewhere'} because assert-schema passes no opts to withAdvisoryLock. Contention IS observable today; what was missing is a declared policy.",
        "liveness": { "kind": "file", "ref": "scripts/lib/pipeline.js" }
      }
    },
    {
      "id": "all_checks_passed",
      "kind": "success",
      "status": "completed",
      "records_meta": { "checks_passed": "string", "checks_failed": "int", "errors": "array", "audit_table": "object", "config": "object" }
    },
    {
      "id": "schema_drift_detected",
      "kind": "fail_check",
      "status": "failed",
      "records_meta": { "checks_failed": "int", "errors": "array", "audit_table": "object", "config": "object" },
      "why": {
        "text": "Schema drift must halt the chain — the step throws after emitting, so the audit table is recorded before the failure propagates. Fold B merged fail_error into this terminal: a CKAN metadata fetch or CSV range request that throws is caught per-source, reddens its own audit row and reaches the SAME circuit-breaker throw, so the two exit paths are indistinguishable in the code and declaring them separately would be a fiction. The fetch path's liveness handle is carried here.",
        "liveness": { "kind": "external", "ref": "ckan0.cf.opendata.inter.prod-toronto.ca" }
      }
    }
  ]
}
